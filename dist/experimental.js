import { aO as ul, aP as Bd, aQ as Di, aR as Ht, aS as qs, aT as xn, aU as nr, aV as $d, aW as fl, aX as Hd, aY as Ys, aZ as rn, a_ as Ae, a$ as jd, b0 as Vd, b1 as ut, b2 as Vr, b3 as Ud, b4 as tn, b5 as Mt, b6 as Wd, w as Ge, b7 as Gd, b8 as Kd, h as _, b9 as qd, ba as Yd, bb as Jd, at as pt, E as $, S as A, k as Oe, u as ot, t as G, ap as mt, r as Et, bc as hl, aK as Xd, bd as Js, ai as vr, aC as Zd, j as Y, x as pl, be as Qd, al as eu, bf as tu, bg as nu, bh as ml, bi as rr, bj as ru, bk as su, az as iu, aJ as ds, bl as ou, bm as lu, ao as au, ak as Xs, ar as Zs, U as gl, bn as cu, am as du, bo as uu, s as kr, Q as bl, aM as jt, bp as fu, aj as hu, m as us, aw as pu, bq as mu, aL as wl, y as xl, br as gu, z as yl, as as vl, O as bu, C as Cr, l as wu, n as xu, o as yu, q as vu, aN as ku, aI as Cu, v as Su, T as Nu, d as Iu, e as _u, f as Au, bs as kl, bt as Tu, aD as Eu } from "./index-DyAxXmVo.js";
import { ax as Tk, ay as Ek, bv as Dk, bu as Rk } from "./index-DyAxXmVo.js";
import { cu as Du, p as Sr, S as Cl, ax as Nr, ap as Sl, a2 as Qs, aq as sn, c5 as Ru, c6 as Lu, c7 as Mu, bl as Ou, c8 as Fu, c9 as zu, cv as Pu, cw as Bu, ca as $u, cc as Hu, cb as ju, bS as Vu, cd as Uu, bV as Wu, bd as Nl, cx as Gu, cy as Ku, ae as yn, cz as qu, cA as Yu, cB as Ju, cC as Xu, b1 as Il, cD as _l, aF as Al, aI as Zu, ak as fs, ck as Qu, cE as ef, bq as tf, br as nf, bt as rf, cF as sf, cG as of, cH as Ft, cI as Tl, cJ as ei, cK as lf, cL as El, at as af, aE as cf, bi as En, cM as Ri, I as Li, cN as df, cO as uf, cP as ff, cQ as hf, cm as ti, t as pf, ar as mf, aG as Dl, a3 as gf, cl as Mi, cR as Oi, cS as bf, cT as Fi, cU as zi, cV as Pi, cW as wf, cX as xf, cY as yf, cZ as vf, bz as kf } from "./F0AiChat-CL30fYVh.js";
import { ad as Mk, bf as Ok, be as Fk, d1 as zk, d2 as Pk, O as Bk, aN as $k, Z as Hk, aH as jk, c_ as Vk, d0 as Uk, q as Wk, c$ as Gk } from "./F0AiChat-CL30fYVh.js";
import { s as Bi, t as $i } from "./useExportAction-D_nHjuDO.js";
import { v as qk, w as Yk, A as Jk, j as Xk, c as Zk, B as Qk, C as eC, e as tC, D as nC, a as rC, F as sC, I as iC, b as oC, g as lC, M as aC, O as cC, l as dC, d as uC, P as fC, h as hC, i as pC, S as mC, f as gC, k as bC, T as wC, n as xC, o as yC, x as vC, p as kC, q as CC, m as SC, r as NC, u as IC } from "./useExportAction-D_nHjuDO.js";
import { N as Fe, P as Xe, a as Ne, i as Cf, f as Vn, T as $e, d as Rl, S as Ee, b as ni, m as Me, G as Hi, k as Sf, c as Ll, F as ke, e as Re, D as on, g as sr, h as Ml, j as Nf, E as Dt, l as If, n as _f, o as Af, R as vn, p as kn, q as ri, r as Tf, s as Ol, U as Fl, t as zl, C as Ef, u as Pl, v as Bl, w as $l, H as Hl, x as jl, L as Vl, y as Df, z as ji, A as Rf, B as Lf, I as Mf, J as Ul, K as Of, M as Wl, O as hs, Q as Ff, V as zf, W as Pf, X as Bf, Y as $f, Z as Hf, _ as jf, $ as Vf, a0 as Uf, a1 as Wf, a2 as Gf, a3 as Kf, a4 as Vi } from "./index-nF9hptwB.js";
import { a5 as AC, a8 as TC, ad as EC, a7 as DC, ag as RC, ae as LC, ab as MC, af as OC, ac as FC, aa as zC, a6 as PC, a9 as BC } from "./index-nF9hptwB.js";
import { jsx as f, jsxs as w, Fragment as ae } from "react/jsx-runtime";
import * as le from "react";
import se, { forwardRef as F, useMemo as we, useState as K, useCallback as ne, useEffect as ue, useRef as ge, useImperativeHandle as Gl, useLayoutEffect as qf, memo as Kl, useId as Yf, Children as ql, cloneElement as Jf } from "react";
import { T as Ui, U as Xf, W as Zf, Z as Qf, $ as eh, k as th } from "./index-gM-GQqGn.js";
import { R as HC, _ as jC } from "./index-gM-GQqGn.js";
import { p as UC } from "./presets-BRy0Mx4C.js";
import { W as Un } from "./exports-CJJ1WxCQ.js";
import { A as GC, B as KC, L as qC, P as YC, a as JC, R as XC, S as ZC, V as QC } from "./exports-CJJ1WxCQ.js";
import './experimental.css';const nh = Du("Search", [
  ["circle", { cx: "11", cy: "11", r: "8", key: "4ej97u" }],
  ["path", { d: "m21 21-4.3-4.3", key: "1qie3q" }]
]), ir = (t, e) => e.view.domAtPos(t).node.offsetParent !== null, rh = (t, e, n) => {
  for (let r = t.depth; r > 0; r -= 1) {
    const s = t.node(r), i = e(s), o = ir(t.start(r), n);
    if (i && o)
      return {
        pos: r > 0 ? t.before(r) : 0,
        start: t.start(r),
        depth: r,
        node: s
      };
  }
}, Wi = (t, e) => {
  const { state: n, view: r, extensionManager: s } = t, { schema: i, selection: o } = n, { empty: l, $anchor: a } = o, c = !!s.extensions.find((x) => x.name === "gapCursor");
  if (!l || a.parent.type !== i.nodes.detailsSummary || !c || e === "right" && a.parentOffset !== a.parent.nodeSize - 2)
    return !1;
  const u = ni((x) => x.type === i.nodes.details)(o);
  if (!u)
    return !1;
  const d = Vn(u.node, (x) => x.type === i.nodes.detailsContent);
  if (!d.length || ir(u.start + d[0].pos + 1, t))
    return !1;
  const p = n.doc.resolve(u.pos + u.node.nodeSize), m = Hi.findFrom(p, 1, !1);
  if (!m)
    return !1;
  const { tr: g } = n, b = new Hi(m);
  return g.setSelection(b), g.scrollIntoView(), r.dispatch(g), !0;
}, sh = Fe.create({
  name: "details",
  content: "detailsSummary detailsContent",
  group: "block",
  defining: !0,
  isolating: !0,
  allowGapCursor: !1,
  addOptions() {
    return {
      persist: !1,
      openClassName: "is-open",
      HTMLAttributes: {}
    };
  },
  addAttributes() {
    return this.options.persist ? {
      open: {
        default: !1,
        parseHTML: (t) => t.hasAttribute("open"),
        renderHTML: ({ open: t }) => t ? { open: "" } : {}
      }
    } : [];
  },
  parseHTML() {
    return [
      {
        tag: "details"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t }) {
    return [
      "details",
      Me(this.options.HTMLAttributes, t),
      0
    ];
  },
  addNodeView() {
    return ({ editor: t, getPos: e, node: n, HTMLAttributes: r }) => {
      const s = document.createElement("div"), i = Me(this.options.HTMLAttributes, r, {
        "data-type": this.name
      });
      Object.entries(i).forEach(([c, u]) => s.setAttribute(c, u));
      const o = document.createElement("button");
      o.type = "button", s.append(o);
      const l = document.createElement("div");
      s.append(l);
      const a = (c) => {
        if (c !== void 0)
          if (c) {
            if (s.classList.contains(this.options.openClassName))
              return;
            s.classList.add(this.options.openClassName);
          } else {
            if (!s.classList.contains(this.options.openClassName))
              return;
            s.classList.remove(this.options.openClassName);
          }
        else
          s.classList.toggle(this.options.openClassName);
        const u = new Event("toggleDetailsContent"), d = l.querySelector(':scope > div[data-type="detailsContent"]');
        d?.dispatchEvent(u);
      };
      return n.attrs.open && setTimeout(() => a()), o.addEventListener("click", () => {
        if (a(), !this.options.persist) {
          t.commands.focus(void 0, { scrollIntoView: !1 });
          return;
        }
        if (t.isEditable && typeof e == "function") {
          const { from: c, to: u } = t.state.selection;
          t.chain().command(({ tr: d }) => {
            const h = e(), p = d.doc.nodeAt(h);
            return p?.type !== this.type ? !1 : (d.setNodeMarkup(h, void 0, {
              open: !p.attrs.open
            }), !0);
          }).setTextSelection({
            from: c,
            to: u
          }).focus(void 0, { scrollIntoView: !1 }).run();
        }
      }), {
        dom: s,
        contentDOM: l,
        ignoreMutation(c) {
          return c.type === "selection" ? !1 : !s.contains(c.target) || s === c.target;
        },
        update: (c) => c.type !== this.type ? !1 : (c.attrs.open !== void 0 && a(c.attrs.open), !0)
      };
    };
  },
  addCommands() {
    return {
      setDetails: () => ({ state: t, chain: e }) => {
        var n;
        const { schema: r, selection: s } = t, { $from: i, $to: o } = s, l = i.blockRange(o);
        if (!l)
          return !1;
        const a = t.doc.slice(l.start, l.end);
        if (!r.nodes.detailsContent.contentMatch.matchFragment(a.content))
          return !1;
        const u = ((n = a.toJSON()) === null || n === void 0 ? void 0 : n.content) || [];
        return e().insertContentAt({ from: l.start, to: l.end }, {
          type: this.name,
          content: [
            {
              type: "detailsSummary"
            },
            {
              type: "detailsContent",
              content: u
            }
          ]
        }).setTextSelection(l.start + 2).run();
      },
      unsetDetails: () => ({ state: t, chain: e }) => {
        const { selection: n, schema: r } = t, s = ni((x) => x.type === this.type)(n);
        if (!s)
          return !1;
        const i = Vn(s.node, (x) => x.type === r.nodes.detailsSummary), o = Vn(s.node, (x) => x.type === r.nodes.detailsContent);
        if (!i.length || !o.length)
          return !1;
        const l = i[0], a = o[0], c = s.pos, u = t.doc.resolve(c), d = c + s.node.nodeSize, h = { from: c, to: d }, p = a.node.content.toJSON() || [], m = u.parent.type.contentMatch.defaultType, b = [
          m?.create(null, l.node.content).toJSON(),
          ...p
        ];
        return e().insertContentAt(h, b).setTextSelection(c + 1).run();
      }
    };
  },
  addKeyboardShortcuts() {
    return {
      Backspace: () => {
        const { schema: t, selection: e } = this.editor.state, { empty: n, $anchor: r } = e;
        return !n || r.parent.type !== t.nodes.detailsSummary ? !1 : r.parentOffset !== 0 ? this.editor.commands.command(({ tr: s }) => {
          const i = r.pos - 1, o = r.pos;
          return s.delete(i, o), !0;
        }) : this.editor.commands.unsetDetails();
      },
      // Creates a new node below it if it is closed.
      // Otherwise inside `DetailsContent`.
      Enter: ({ editor: t }) => {
        const { state: e, view: n } = t, { schema: r, selection: s } = e, { $head: i } = s;
        if (i.parent.type !== r.nodes.detailsSummary)
          return !1;
        const o = ir(i.after() + 1, t), l = o ? e.doc.nodeAt(i.after()) : i.node(-2);
        if (!l)
          return !1;
        const a = o ? 0 : i.indexAfter(-1), c = Rl(l.contentMatchAt(a));
        if (!c || !l.canReplaceWith(a, a, c))
          return !1;
        const u = c.createAndFill();
        if (!u)
          return !1;
        const d = o ? i.after() + 1 : i.after(-1), h = e.tr.replaceWith(d, d, u), p = h.doc.resolve(d), m = Ee.near(p, 1);
        return h.setSelection(m), h.scrollIntoView(), n.dispatch(h), !0;
      },
      // The default gapcursor implementation can’t handle hidden content, so we need to fix this.
      ArrowRight: ({ editor: t }) => Wi(t, "right"),
      // The default gapcursor implementation can’t handle hidden content, so we need to fix this.
      ArrowDown: ({ editor: t }) => Wi(t, "down")
    };
  },
  addProseMirrorPlugins() {
    return [
      // This plugin prevents text selections within the hidden content in `DetailsContent`.
      // The cursor is moved to the next visible position.
      new Xe({
        key: new Ne("detailsSelection"),
        appendTransaction: (t, e, n) => {
          const { editor: r, type: s } = this;
          if (!t.some((b) => b.selectionSet) || !e.selection.empty || !n.selection.empty || !Cf(n, s.name))
            return;
          const { $from: l } = n.selection;
          if (ir(l.pos, r))
            return;
          const c = rh(l, (b) => b.type === s, r);
          if (!c)
            return;
          const u = Vn(c.node, (b) => b.type === n.schema.nodes.detailsSummary);
          if (!u.length)
            return;
          const d = u[0], p = (e.selection.from < n.selection.from ? "forward" : "backward") === "forward" ? c.start + d.pos : c.pos + d.pos + d.node.nodeSize, m = $e.create(n.doc, p);
          return n.tr.setSelection(m);
        }
      })
    ];
  }
}), Yl = sh.configure({
  persist: !0,
  HTMLAttributes: {
    class: "rich-text-details"
  }
}), ih = Fe.create({
  name: "detailsContent",
  content: "block+",
  defining: !0,
  selectable: !1,
  addOptions() {
    return {
      HTMLAttributes: {}
    };
  },
  parseHTML() {
    return [
      {
        tag: `div[data-type="${this.name}"]`
      }
    ];
  },
  renderHTML({ HTMLAttributes: t }) {
    return [
      "div",
      Me(this.options.HTMLAttributes, t, { "data-type": this.name }),
      0
    ];
  },
  addNodeView() {
    return ({ HTMLAttributes: t }) => {
      const e = document.createElement("div"), n = Me(this.options.HTMLAttributes, t, {
        "data-type": this.name,
        hidden: "hidden"
      });
      return Object.entries(n).forEach(([r, s]) => e.setAttribute(r, s)), e.addEventListener("toggleDetailsContent", () => {
        e.toggleAttribute("hidden");
      }), {
        dom: e,
        contentDOM: e,
        ignoreMutation(r) {
          return r.type === "selection" ? !1 : !e.contains(r.target) || e === r.target;
        },
        update: (r) => r.type === this.type
      };
    };
  },
  addKeyboardShortcuts() {
    return {
      // Escape node on double enter
      Enter: ({ editor: t }) => {
        const { state: e, view: n } = t, { selection: r } = e, { $from: s, empty: i } = r, o = ni((N) => N.type === this.type)(r);
        if (!i || !o || !o.node.childCount)
          return !1;
        const l = s.index(o.depth), { childCount: a } = o.node;
        if (!(a === l + 1))
          return !1;
        const u = o.node.type.contentMatch.defaultType, d = u?.createAndFill();
        if (!d)
          return !1;
        const h = e.doc.resolve(o.pos + 1), p = a - 1, m = o.node.child(p), g = h.posAtIndex(p, o.depth);
        if (!m.eq(d))
          return !1;
        const x = s.node(-3);
        if (!x)
          return !1;
        const v = s.indexAfter(-3), S = Rl(x.contentMatchAt(v));
        if (!S || !x.canReplaceWith(v, v, S))
          return !1;
        const E = S.createAndFill();
        if (!E)
          return !1;
        const { tr: I } = e, k = s.after(-2);
        I.replaceWith(k, k, E);
        const T = I.doc.resolve(k), C = Ee.near(T, 1);
        I.setSelection(C);
        const D = g, y = g + m.nodeSize;
        return I.delete(D, y), I.scrollIntoView(), n.dispatch(I), !0;
      }
    };
  }
}), Jl = ih, oh = Fe.create({
  name: "detailsSummary",
  content: "text*",
  defining: !0,
  selectable: !1,
  isolating: !0,
  addOptions() {
    return {
      HTMLAttributes: {}
    };
  },
  parseHTML() {
    return [
      {
        tag: "summary"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t }) {
    return [
      "summary",
      Me(this.options.HTMLAttributes, t),
      0
    ];
  }
}), Xl = oh;
var ps, ms;
if (typeof WeakMap < "u") {
  let t = /* @__PURE__ */ new WeakMap();
  ps = (e) => t.get(e), ms = (e, n) => (t.set(e, n), n);
} else {
  const t = [];
  let n = 0;
  ps = (r) => {
    for (let s = 0; s < t.length; s += 2)
      if (t[s] == r) return t[s + 1];
  }, ms = (r, s) => (n == 10 && (n = 0), t[n++] = r, t[n++] = s);
}
var Z = class {
  constructor(t, e, n, r) {
    this.width = t, this.height = e, this.map = n, this.problems = r;
  }
  // Find the dimensions of the cell at the given position.
  findCell(t) {
    for (let e = 0; e < this.map.length; e++) {
      const n = this.map[e];
      if (n != t) continue;
      const r = e % this.width, s = e / this.width | 0;
      let i = r + 1, o = s + 1;
      for (let l = 1; i < this.width && this.map[e + l] == n; l++)
        i++;
      for (let l = 1; o < this.height && this.map[e + this.width * l] == n; l++)
        o++;
      return { left: r, top: s, right: i, bottom: o };
    }
    throw new RangeError(`No cell with offset ${t} found`);
  }
  // Find the left side of the cell at the given position.
  colCount(t) {
    for (let e = 0; e < this.map.length; e++)
      if (this.map[e] == t)
        return e % this.width;
    throw new RangeError(`No cell with offset ${t} found`);
  }
  // Find the next cell in the given direction, starting from the cell
  // at `pos`, if any.
  nextCell(t, e, n) {
    const { left: r, right: s, top: i, bottom: o } = this.findCell(t);
    return e == "horiz" ? (n < 0 ? r == 0 : s == this.width) ? null : this.map[i * this.width + (n < 0 ? r - 1 : s)] : (n < 0 ? i == 0 : o == this.height) ? null : this.map[r + this.width * (n < 0 ? i - 1 : o)];
  }
  // Get the rectangle spanning the two given cells.
  rectBetween(t, e) {
    const {
      left: n,
      right: r,
      top: s,
      bottom: i
    } = this.findCell(t), {
      left: o,
      right: l,
      top: a,
      bottom: c
    } = this.findCell(e);
    return {
      left: Math.min(n, o),
      top: Math.min(s, a),
      right: Math.max(r, l),
      bottom: Math.max(i, c)
    };
  }
  // Return the position of all cells that have the top left corner in
  // the given rectangle.
  cellsInRect(t) {
    const e = [], n = {};
    for (let r = t.top; r < t.bottom; r++)
      for (let s = t.left; s < t.right; s++) {
        const i = r * this.width + s, o = this.map[i];
        n[o] || (n[o] = !0, !(s == t.left && s && this.map[i - 1] == o || r == t.top && r && this.map[i - this.width] == o) && e.push(o));
      }
    return e;
  }
  // Return the position at which the cell at the given row and column
  // starts, or would start, if a cell started there.
  positionAt(t, e, n) {
    for (let r = 0, s = 0; ; r++) {
      const i = s + n.child(r).nodeSize;
      if (r == t) {
        let o = e + t * this.width;
        const l = (t + 1) * this.width;
        for (; o < l && this.map[o] < s; ) o++;
        return o == l ? i - 1 : this.map[o];
      }
      s = i;
    }
  }
  // Find the table map for the given table node.
  static get(t) {
    return ps(t) || ms(t, lh(t));
  }
};
function lh(t) {
  if (t.type.spec.tableRole != "table")
    throw new RangeError("Not a table node: " + t.type.name);
  const e = ah(t), n = t.childCount, r = [];
  let s = 0, i = null;
  const o = [];
  for (let c = 0, u = e * n; c < u; c++) r[c] = 0;
  for (let c = 0, u = 0; c < n; c++) {
    const d = t.child(c);
    u++;
    for (let m = 0; ; m++) {
      for (; s < r.length && r[s] != 0; ) s++;
      if (m == d.childCount) break;
      const g = d.child(m), { colspan: b, rowspan: x, colwidth: v } = g.attrs;
      for (let S = 0; S < x; S++) {
        if (S + c >= n) {
          (i || (i = [])).push({
            type: "overlong_rowspan",
            pos: u,
            n: x - S
          });
          break;
        }
        const E = s + S * e;
        for (let I = 0; I < b; I++) {
          r[E + I] == 0 ? r[E + I] = u : (i || (i = [])).push({
            type: "collision",
            row: c,
            pos: u,
            n: b - I
          });
          const k = v && v[I];
          if (k) {
            const T = (E + I) % e * 2, C = o[T];
            C == null || C != k && o[T + 1] == 1 ? (o[T] = k, o[T + 1] = 1) : C == k && o[T + 1]++;
          }
        }
      }
      s += b, u += g.nodeSize;
    }
    const h = (c + 1) * e;
    let p = 0;
    for (; s < h; ) r[s++] == 0 && p++;
    p && (i || (i = [])).push({ type: "missing", row: c, n: p }), u++;
  }
  (e === 0 || n === 0) && (i || (i = [])).push({ type: "zero_sized" });
  const l = new Z(e, n, r, i);
  let a = !1;
  for (let c = 0; !a && c < o.length; c += 2)
    o[c] != null && o[c + 1] < n && (a = !0);
  return a && ch(l, o, t), l;
}
function ah(t) {
  let e = -1, n = !1;
  for (let r = 0; r < t.childCount; r++) {
    const s = t.child(r);
    let i = 0;
    if (n)
      for (let o = 0; o < r; o++) {
        const l = t.child(o);
        for (let a = 0; a < l.childCount; a++) {
          const c = l.child(a);
          o + c.attrs.rowspan > r && (i += c.attrs.colspan);
        }
      }
    for (let o = 0; o < s.childCount; o++) {
      const l = s.child(o);
      i += l.attrs.colspan, l.attrs.rowspan > 1 && (n = !0);
    }
    e == -1 ? e = i : e != i && (e = Math.max(e, i));
  }
  return e;
}
function ch(t, e, n) {
  t.problems || (t.problems = []);
  const r = {};
  for (let s = 0; s < t.map.length; s++) {
    const i = t.map[s];
    if (r[i]) continue;
    r[i] = !0;
    const o = n.nodeAt(i);
    if (!o)
      throw new RangeError(`No cell with offset ${i} found`);
    let l = null;
    const a = o.attrs;
    for (let c = 0; c < a.colspan; c++) {
      const u = (s + c) % t.width, d = e[u * 2];
      d != null && (!a.colwidth || a.colwidth[c] != d) && ((l || (l = dh(a)))[c] = d);
    }
    l && t.problems.unshift({
      type: "colwidth mismatch",
      pos: i,
      colwidth: l
    });
  }
}
function dh(t) {
  if (t.colwidth) return t.colwidth.slice();
  const e = [];
  for (let n = 0; n < t.colspan; n++) e.push(0);
  return e;
}
function be(t) {
  let e = t.cached.tableNodeTypes;
  if (!e) {
    e = t.cached.tableNodeTypes = {};
    for (const n in t.nodes) {
      const r = t.nodes[n], s = r.spec.tableRole;
      s && (e[s] = r);
    }
  }
  return e;
}
var ft = new Ne("selectingCells");
function Kt(t) {
  for (let e = t.depth - 1; e > 0; e--)
    if (t.node(e).type.spec.tableRole == "row")
      return t.node(0).resolve(t.before(e + 1));
  return null;
}
function uh(t) {
  for (let e = t.depth; e > 0; e--) {
    const n = t.node(e).type.spec.tableRole;
    if (n === "cell" || n === "header_cell") return t.node(e);
  }
  return null;
}
function Ke(t) {
  const e = t.selection.$head;
  for (let n = e.depth; n > 0; n--)
    if (e.node(n).type.spec.tableRole == "row") return !0;
  return !1;
}
function Ir(t) {
  const e = t.selection;
  if ("$anchorCell" in e && e.$anchorCell)
    return e.$anchorCell.pos > e.$headCell.pos ? e.$anchorCell : e.$headCell;
  if ("node" in e && e.node && e.node.type.spec.tableRole == "cell")
    return e.$anchor;
  const n = Kt(e.$head) || fh(e.$head);
  if (n)
    return n;
  throw new RangeError(`No cell found around position ${e.head}`);
}
function fh(t) {
  for (let e = t.nodeAfter, n = t.pos; e; e = e.firstChild, n++) {
    const r = e.type.spec.tableRole;
    if (r == "cell" || r == "header_cell") return t.doc.resolve(n);
  }
  for (let e = t.nodeBefore, n = t.pos; e; e = e.lastChild, n--) {
    const r = e.type.spec.tableRole;
    if (r == "cell" || r == "header_cell")
      return t.doc.resolve(n - e.nodeSize);
  }
}
function gs(t) {
  return t.parent.type.spec.tableRole == "row" && !!t.nodeAfter;
}
function hh(t) {
  return t.node(0).resolve(t.pos + t.nodeAfter.nodeSize);
}
function si(t, e) {
  return t.depth == e.depth && t.pos >= e.start(-1) && t.pos <= e.end(-1);
}
function Zl(t, e, n) {
  const r = t.node(-1), s = Z.get(r), i = t.start(-1), o = s.nextCell(t.pos - i, e, n);
  return o == null ? null : t.node(0).resolve(i + o);
}
function Nt(t, e, n = 1) {
  const r = { ...t, colspan: t.colspan - n };
  return r.colwidth && (r.colwidth = r.colwidth.slice(), r.colwidth.splice(e, n), r.colwidth.some((s) => s > 0) || (r.colwidth = null)), r;
}
function Ql(t, e, n = 1) {
  const r = { ...t, colspan: t.colspan + n };
  if (r.colwidth) {
    r.colwidth = r.colwidth.slice();
    for (let s = 0; s < n; s++) r.colwidth.splice(e, 0, 0);
  }
  return r;
}
function ph(t, e, n) {
  const r = be(e.type.schema).header_cell;
  for (let s = 0; s < t.height; s++)
    if (e.nodeAt(t.map[n + s * t.width]).type != r)
      return !1;
  return !0;
}
var W = class nt extends Ee {
  // A table selection is identified by its anchor and head cells. The
  // positions given to this constructor should point _before_ two
  // cells in the same table. They may be the same, to select a single
  // cell.
  constructor(e, n = e) {
    const r = e.node(-1), s = Z.get(r), i = e.start(-1), o = s.rectBetween(
      e.pos - i,
      n.pos - i
    ), l = e.node(0), a = s.cellsInRect(o).filter((u) => u != n.pos - i);
    a.unshift(n.pos - i);
    const c = a.map((u) => {
      const d = r.nodeAt(u);
      if (!d)
        throw RangeError(`No cell with offset ${u} found`);
      const h = i + u + 1;
      return new Ll(
        l.resolve(h),
        l.resolve(h + d.content.size)
      );
    });
    super(c[0].$from, c[0].$to, c), this.$anchorCell = e, this.$headCell = n;
  }
  map(e, n) {
    const r = e.resolve(n.map(this.$anchorCell.pos)), s = e.resolve(n.map(this.$headCell.pos));
    if (gs(r) && gs(s) && si(r, s)) {
      const i = this.$anchorCell.node(-1) != r.node(-1);
      return i && this.isRowSelection() ? nt.rowSelection(r, s) : i && this.isColSelection() ? nt.colSelection(r, s) : new nt(r, s);
    }
    return $e.between(r, s);
  }
  // Returns a rectangular slice of table rows containing the selected
  // cells.
  content() {
    const e = this.$anchorCell.node(-1), n = Z.get(e), r = this.$anchorCell.start(-1), s = n.rectBetween(
      this.$anchorCell.pos - r,
      this.$headCell.pos - r
    ), i = {}, o = [];
    for (let a = s.top; a < s.bottom; a++) {
      const c = [];
      for (let u = a * n.width + s.left, d = s.left; d < s.right; d++, u++) {
        const h = n.map[u];
        if (i[h]) continue;
        i[h] = !0;
        const p = n.findCell(h);
        let m = e.nodeAt(h);
        if (!m)
          throw RangeError(`No cell with offset ${h} found`);
        const g = s.left - p.left, b = p.right - s.right;
        if (g > 0 || b > 0) {
          let x = m.attrs;
          if (g > 0 && (x = Nt(x, 0, g)), b > 0 && (x = Nt(
            x,
            x.colspan - b,
            b
          )), p.left < s.left) {
            if (m = m.type.createAndFill(x), !m)
              throw RangeError(
                `Could not create cell with attrs ${JSON.stringify(x)}`
              );
          } else
            m = m.type.create(x, m.content);
        }
        if (p.top < s.top || p.bottom > s.bottom) {
          const x = {
            ...m.attrs,
            rowspan: Math.min(p.bottom, s.bottom) - Math.max(p.top, s.top)
          };
          p.top < s.top ? m = m.type.createAndFill(x) : m = m.type.create(x, m.content);
        }
        c.push(m);
      }
      o.push(e.child(a).copy(ke.from(c)));
    }
    const l = this.isColSelection() && this.isRowSelection() ? e : o;
    return new Re(ke.from(l), 1, 1);
  }
  replace(e, n = Re.empty) {
    const r = e.steps.length, s = this.ranges;
    for (let o = 0; o < s.length; o++) {
      const { $from: l, $to: a } = s[o], c = e.mapping.slice(r);
      e.replace(
        c.map(l.pos),
        c.map(a.pos),
        o ? Re.empty : n
      );
    }
    const i = Ee.findFrom(
      e.doc.resolve(e.mapping.slice(r).map(this.to)),
      -1
    );
    i && e.setSelection(i);
  }
  replaceWith(e, n) {
    this.replace(e, new Re(ke.from(n), 0, 0));
  }
  forEachCell(e) {
    const n = this.$anchorCell.node(-1), r = Z.get(n), s = this.$anchorCell.start(-1), i = r.cellsInRect(
      r.rectBetween(
        this.$anchorCell.pos - s,
        this.$headCell.pos - s
      )
    );
    for (let o = 0; o < i.length; o++)
      e(n.nodeAt(i[o]), s + i[o]);
  }
  // True if this selection goes all the way from the top to the
  // bottom of the table.
  isColSelection() {
    const e = this.$anchorCell.index(-1), n = this.$headCell.index(-1);
    if (Math.min(e, n) > 0) return !1;
    const r = e + this.$anchorCell.nodeAfter.attrs.rowspan, s = n + this.$headCell.nodeAfter.attrs.rowspan;
    return Math.max(r, s) == this.$headCell.node(-1).childCount;
  }
  // Returns the smallest column selection that covers the given anchor
  // and head cell.
  static colSelection(e, n = e) {
    const r = e.node(-1), s = Z.get(r), i = e.start(-1), o = s.findCell(e.pos - i), l = s.findCell(n.pos - i), a = e.node(0);
    return o.top <= l.top ? (o.top > 0 && (e = a.resolve(i + s.map[o.left])), l.bottom < s.height && (n = a.resolve(
      i + s.map[s.width * (s.height - 1) + l.right - 1]
    ))) : (l.top > 0 && (n = a.resolve(i + s.map[l.left])), o.bottom < s.height && (e = a.resolve(
      i + s.map[s.width * (s.height - 1) + o.right - 1]
    ))), new nt(e, n);
  }
  // True if this selection goes all the way from the left to the
  // right of the table.
  isRowSelection() {
    const e = this.$anchorCell.node(-1), n = Z.get(e), r = this.$anchorCell.start(-1), s = n.colCount(this.$anchorCell.pos - r), i = n.colCount(this.$headCell.pos - r);
    if (Math.min(s, i) > 0) return !1;
    const o = s + this.$anchorCell.nodeAfter.attrs.colspan, l = i + this.$headCell.nodeAfter.attrs.colspan;
    return Math.max(o, l) == n.width;
  }
  eq(e) {
    return e instanceof nt && e.$anchorCell.pos == this.$anchorCell.pos && e.$headCell.pos == this.$headCell.pos;
  }
  // Returns the smallest row selection that covers the given anchor
  // and head cell.
  static rowSelection(e, n = e) {
    const r = e.node(-1), s = Z.get(r), i = e.start(-1), o = s.findCell(e.pos - i), l = s.findCell(n.pos - i), a = e.node(0);
    return o.left <= l.left ? (o.left > 0 && (e = a.resolve(
      i + s.map[o.top * s.width]
    )), l.right < s.width && (n = a.resolve(
      i + s.map[s.width * (l.top + 1) - 1]
    ))) : (l.left > 0 && (n = a.resolve(i + s.map[l.top * s.width])), o.right < s.width && (e = a.resolve(
      i + s.map[s.width * (o.top + 1) - 1]
    ))), new nt(e, n);
  }
  toJSON() {
    return {
      type: "cell",
      anchor: this.$anchorCell.pos,
      head: this.$headCell.pos
    };
  }
  static fromJSON(e, n) {
    return new nt(e.resolve(n.anchor), e.resolve(n.head));
  }
  static create(e, n, r = n) {
    return new nt(e.resolve(n), e.resolve(r));
  }
  getBookmark() {
    return new mh(this.$anchorCell.pos, this.$headCell.pos);
  }
};
W.prototype.visible = !1;
Ee.jsonID("cell", W);
var mh = class ea {
  constructor(e, n) {
    this.anchor = e, this.head = n;
  }
  map(e) {
    return new ea(e.map(this.anchor), e.map(this.head));
  }
  resolve(e) {
    const n = e.resolve(this.anchor), r = e.resolve(this.head);
    return n.parent.type.spec.tableRole == "row" && r.parent.type.spec.tableRole == "row" && n.index() < n.parent.childCount && r.index() < r.parent.childCount && si(n, r) ? new W(n, r) : Ee.near(r, 1);
  }
};
function gh(t) {
  if (!(t.selection instanceof W)) return null;
  const e = [];
  return t.selection.forEachCell((n, r) => {
    e.push(
      sr.node(r, r + n.nodeSize, { class: "selectedCell" })
    );
  }), on.create(t.doc, e);
}
function bh({ $from: t, $to: e }) {
  if (t.pos == e.pos || t.pos < e.pos - 6) return !1;
  let n = t.pos, r = e.pos, s = t.depth;
  for (; s >= 0 && !(t.after(s + 1) < t.end(s)); s--, n++)
    ;
  for (let i = e.depth; i >= 0 && !(e.before(i + 1) > e.start(i)); i--, r--)
    ;
  return n == r && /row|table/.test(t.node(s).type.spec.tableRole);
}
function wh({ $from: t, $to: e }) {
  let n, r;
  for (let s = t.depth; s > 0; s--) {
    const i = t.node(s);
    if (i.type.spec.tableRole === "cell" || i.type.spec.tableRole === "header_cell") {
      n = i;
      break;
    }
  }
  for (let s = e.depth; s > 0; s--) {
    const i = e.node(s);
    if (i.type.spec.tableRole === "cell" || i.type.spec.tableRole === "header_cell") {
      r = i;
      break;
    }
  }
  return n !== r && e.parentOffset === 0;
}
function xh(t, e, n) {
  const r = (e || t).selection, s = (e || t).doc;
  let i, o;
  if (r instanceof Ml && (o = r.node.type.spec.tableRole)) {
    if (o == "cell" || o == "header_cell")
      i = W.create(s, r.from);
    else if (o == "row") {
      const l = s.resolve(r.from + 1);
      i = W.rowSelection(l, l);
    } else if (!n) {
      const l = Z.get(r.node), a = r.from + 1, c = a + l.map[l.width * l.height - 1];
      i = W.create(s, a + 1, c);
    }
  } else r instanceof $e && bh(r) ? i = $e.create(s, r.from) : r instanceof $e && wh(r) && (i = $e.create(s, r.$from.start(), r.$from.end()));
  return i && (e || (e = t.tr)).setSelection(i), e;
}
var yh = new Ne("fix-tables");
function ta(t, e, n, r) {
  const s = t.childCount, i = e.childCount;
  e: for (let o = 0, l = 0; o < i; o++) {
    const a = e.child(o);
    for (let c = l, u = Math.min(s, o + 3); c < u; c++)
      if (t.child(c) == a) {
        l = c + 1, n += a.nodeSize;
        continue e;
      }
    r(a, n), l < s && t.child(l).sameMarkup(a) ? ta(t.child(l), a, n + 1, r) : a.nodesBetween(0, a.content.size, r, n + 1), n += a.nodeSize;
  }
}
function na(t, e) {
  let n;
  const r = (s, i) => {
    s.type.spec.tableRole == "table" && (n = vh(t, s, i, n));
  };
  return e ? e.doc != t.doc && ta(e.doc, t.doc, 0, r) : t.doc.descendants(r), n;
}
function vh(t, e, n, r) {
  const s = Z.get(e);
  if (!s.problems) return r;
  r || (r = t.tr);
  const i = [];
  for (let a = 0; a < s.height; a++) i.push(0);
  for (let a = 0; a < s.problems.length; a++) {
    const c = s.problems[a];
    if (c.type == "collision") {
      const u = e.nodeAt(c.pos);
      if (!u) continue;
      const d = u.attrs;
      for (let h = 0; h < d.rowspan; h++) i[c.row + h] += c.n;
      r.setNodeMarkup(
        r.mapping.map(n + 1 + c.pos),
        null,
        Nt(d, d.colspan - c.n, c.n)
      );
    } else if (c.type == "missing")
      i[c.row] += c.n;
    else if (c.type == "overlong_rowspan") {
      const u = e.nodeAt(c.pos);
      if (!u) continue;
      r.setNodeMarkup(r.mapping.map(n + 1 + c.pos), null, {
        ...u.attrs,
        rowspan: u.attrs.rowspan - c.n
      });
    } else if (c.type == "colwidth mismatch") {
      const u = e.nodeAt(c.pos);
      if (!u) continue;
      r.setNodeMarkup(r.mapping.map(n + 1 + c.pos), null, {
        ...u.attrs,
        colwidth: c.colwidth
      });
    } else if (c.type == "zero_sized") {
      const u = r.mapping.map(n);
      r.delete(u, u + e.nodeSize);
    }
  }
  let o, l;
  for (let a = 0; a < i.length; a++)
    i[a] && (o == null && (o = a), l = a);
  for (let a = 0, c = n + 1; a < s.height; a++) {
    const u = e.child(a), d = c + u.nodeSize, h = i[a];
    if (h > 0) {
      let p = "cell";
      u.firstChild && (p = u.firstChild.type.spec.tableRole);
      const m = [];
      for (let b = 0; b < h; b++) {
        const x = be(t.schema)[p].createAndFill();
        x && m.push(x);
      }
      const g = (a == 0 || o == a - 1) && l == a ? c + 1 : d - 1;
      r.insert(r.mapping.map(g), m);
    }
    c = d;
  }
  return r.setMeta(yh, { fixTables: !0 });
}
function Ze(t) {
  const e = t.selection, n = Ir(t), r = n.node(-1), s = n.start(-1), i = Z.get(r);
  return { ...e instanceof W ? i.rectBetween(
    e.$anchorCell.pos - s,
    e.$headCell.pos - s
  ) : i.findCell(n.pos - s), tableStart: s, map: i, table: r };
}
function ra(t, { map: e, tableStart: n, table: r }, s) {
  let i = s > 0 ? -1 : 0;
  ph(e, r, s + i) && (i = s == 0 || s == e.width ? null : 0);
  for (let o = 0; o < e.height; o++) {
    const l = o * e.width + s;
    if (s > 0 && s < e.width && e.map[l - 1] == e.map[l]) {
      const a = e.map[l], c = r.nodeAt(a);
      t.setNodeMarkup(
        t.mapping.map(n + a),
        null,
        Ql(c.attrs, s - e.colCount(a))
      ), o += c.attrs.rowspan - 1;
    } else {
      const a = i == null ? be(r.type.schema).cell : r.nodeAt(e.map[l + i]).type, c = e.positionAt(o, s, r);
      t.insert(t.mapping.map(n + c), a.createAndFill());
    }
  }
  return t;
}
function kh(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t);
    e(ra(t.tr, n, n.left));
  }
  return !0;
}
function Ch(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t);
    e(ra(t.tr, n, n.right));
  }
  return !0;
}
function Sh(t, { map: e, table: n, tableStart: r }, s) {
  const i = t.mapping.maps.length;
  for (let o = 0; o < e.height; ) {
    const l = o * e.width + s, a = e.map[l], c = n.nodeAt(a), u = c.attrs;
    if (s > 0 && e.map[l - 1] == a || s < e.width - 1 && e.map[l + 1] == a)
      t.setNodeMarkup(
        t.mapping.slice(i).map(r + a),
        null,
        Nt(u, s - e.colCount(a))
      );
    else {
      const d = t.mapping.slice(i).map(r + a);
      t.delete(d, d + c.nodeSize);
    }
    o += u.rowspan;
  }
}
function Nh(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t), r = t.tr;
    if (n.left == 0 && n.right == n.map.width) return !1;
    for (let s = n.right - 1; Sh(r, n, s), s != n.left; s--) {
      const i = n.tableStart ? r.doc.nodeAt(n.tableStart - 1) : r.doc;
      if (!i)
        throw RangeError("No table found");
      n.table = i, n.map = Z.get(i);
    }
    e(r);
  }
  return !0;
}
function Ih(t, e, n) {
  var r;
  const s = be(e.type.schema).header_cell;
  for (let i = 0; i < t.width; i++)
    if (((r = e.nodeAt(t.map[i + n * t.width])) == null ? void 0 : r.type) != s)
      return !1;
  return !0;
}
function sa(t, { map: e, tableStart: n, table: r }, s) {
  var i;
  let o = n;
  for (let c = 0; c < s; c++) o += r.child(c).nodeSize;
  const l = [];
  let a = s > 0 ? -1 : 0;
  Ih(e, r, s + a) && (a = s == 0 || s == e.height ? null : 0);
  for (let c = 0, u = e.width * s; c < e.width; c++, u++)
    if (s > 0 && s < e.height && e.map[u] == e.map[u - e.width]) {
      const d = e.map[u], h = r.nodeAt(d).attrs;
      t.setNodeMarkup(n + d, null, {
        ...h,
        rowspan: h.rowspan + 1
      }), c += h.colspan - 1;
    } else {
      const d = a == null ? be(r.type.schema).cell : (i = r.nodeAt(e.map[u + a * e.width])) == null ? void 0 : i.type, h = d?.createAndFill();
      h && l.push(h);
    }
  return t.insert(o, be(r.type.schema).row.create(null, l)), t;
}
function _h(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t);
    e(sa(t.tr, n, n.top));
  }
  return !0;
}
function Ah(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t);
    e(sa(t.tr, n, n.bottom));
  }
  return !0;
}
function Th(t, { map: e, table: n, tableStart: r }, s) {
  let i = 0;
  for (let c = 0; c < s; c++) i += n.child(c).nodeSize;
  const o = i + n.child(s).nodeSize, l = t.mapping.maps.length;
  t.delete(i + r, o + r);
  const a = /* @__PURE__ */ new Set();
  for (let c = 0, u = s * e.width; c < e.width; c++, u++) {
    const d = e.map[u];
    if (!a.has(d)) {
      if (a.add(d), s > 0 && d == e.map[u - e.width]) {
        const h = n.nodeAt(d).attrs;
        t.setNodeMarkup(t.mapping.slice(l).map(d + r), null, {
          ...h,
          rowspan: h.rowspan - 1
        }), c += h.colspan - 1;
      } else if (s < e.height && d == e.map[u + e.width]) {
        const h = n.nodeAt(d), p = h.attrs, m = h.type.create(
          { ...p, rowspan: h.attrs.rowspan - 1 },
          h.content
        ), g = e.positionAt(s + 1, c, n);
        t.insert(t.mapping.slice(l).map(r + g), m), c += p.colspan - 1;
      }
    }
  }
}
function Eh(t, e) {
  if (!Ke(t)) return !1;
  if (e) {
    const n = Ze(t), r = t.tr;
    if (n.top == 0 && n.bottom == n.map.height) return !1;
    for (let s = n.bottom - 1; Th(r, n, s), s != n.top; s--) {
      const i = n.tableStart ? r.doc.nodeAt(n.tableStart - 1) : r.doc;
      if (!i)
        throw RangeError("No table found");
      n.table = i, n.map = Z.get(n.table);
    }
    e(r);
  }
  return !0;
}
function Gi(t) {
  const e = t.content;
  return e.childCount == 1 && e.child(0).isTextblock && e.child(0).childCount == 0;
}
function Dh({ width: t, height: e, map: n }, r) {
  let s = r.top * t + r.left, i = s, o = (r.bottom - 1) * t + r.left, l = s + (r.right - r.left - 1);
  for (let a = r.top; a < r.bottom; a++) {
    if (r.left > 0 && n[i] == n[i - 1] || r.right < t && n[l] == n[l + 1])
      return !0;
    i += t, l += t;
  }
  for (let a = r.left; a < r.right; a++) {
    if (r.top > 0 && n[s] == n[s - t] || r.bottom < e && n[o] == n[o + t])
      return !0;
    s++, o++;
  }
  return !1;
}
function Ki(t, e) {
  const n = t.selection;
  if (!(n instanceof W) || n.$anchorCell.pos == n.$headCell.pos)
    return !1;
  const r = Ze(t), { map: s } = r;
  if (Dh(s, r)) return !1;
  if (e) {
    const i = t.tr, o = {};
    let l = ke.empty, a, c;
    for (let u = r.top; u < r.bottom; u++)
      for (let d = r.left; d < r.right; d++) {
        const h = s.map[u * s.width + d], p = r.table.nodeAt(h);
        if (!(o[h] || !p))
          if (o[h] = !0, a == null)
            a = h, c = p;
          else {
            Gi(p) || (l = l.append(p.content));
            const m = i.mapping.map(h + r.tableStart);
            i.delete(m, m + p.nodeSize);
          }
      }
    if (a == null || c == null)
      return !0;
    if (i.setNodeMarkup(a + r.tableStart, null, {
      ...Ql(
        c.attrs,
        c.attrs.colspan,
        r.right - r.left - c.attrs.colspan
      ),
      rowspan: r.bottom - r.top
    }), l.size) {
      const u = a + 1 + c.content.size, d = Gi(c) ? a + 1 : u;
      i.replaceWith(d + r.tableStart, u + r.tableStart, l);
    }
    i.setSelection(
      new W(i.doc.resolve(a + r.tableStart))
    ), e(i);
  }
  return !0;
}
function qi(t, e) {
  const n = be(t.schema);
  return Rh(({ node: r }) => n[r.type.spec.tableRole])(t, e);
}
function Rh(t) {
  return (e, n) => {
    var r;
    const s = e.selection;
    let i, o;
    if (s instanceof W) {
      if (s.$anchorCell.pos != s.$headCell.pos) return !1;
      i = s.$anchorCell.nodeAfter, o = s.$anchorCell.pos;
    } else {
      if (i = uh(s.$from), !i) return !1;
      o = (r = Kt(s.$from)) == null ? void 0 : r.pos;
    }
    if (i == null || o == null || i.attrs.colspan == 1 && i.attrs.rowspan == 1)
      return !1;
    if (n) {
      let l = i.attrs;
      const a = [], c = l.colwidth;
      l.rowspan > 1 && (l = { ...l, rowspan: 1 }), l.colspan > 1 && (l = { ...l, colspan: 1 });
      const u = Ze(e), d = e.tr;
      for (let p = 0; p < u.right - u.left; p++)
        a.push(
          c ? {
            ...l,
            colwidth: c && c[p] ? [c[p]] : null
          } : l
        );
      let h;
      for (let p = u.top; p < u.bottom; p++) {
        let m = u.map.positionAt(p, u.left, u.table);
        p == u.top && (m += i.nodeSize);
        for (let g = u.left, b = 0; g < u.right; g++, b++)
          g == u.left && p == u.top || d.insert(
            h = d.mapping.map(m + u.tableStart, 1),
            t({ node: i, row: p, col: g }).createAndFill(a[b])
          );
      }
      d.setNodeMarkup(
        o,
        t({ node: i, row: u.top, col: u.left }),
        a[0]
      ), s instanceof W && d.setSelection(
        new W(
          d.doc.resolve(s.$anchorCell.pos),
          h ? d.doc.resolve(h) : void 0
        )
      ), n(d);
    }
    return !0;
  };
}
function Lh(t, e) {
  return function(n, r) {
    if (!Ke(n)) return !1;
    const s = Ir(n);
    if (s.nodeAfter.attrs[t] === e) return !1;
    if (r) {
      const i = n.tr;
      n.selection instanceof W ? n.selection.forEachCell((o, l) => {
        o.attrs[t] !== e && i.setNodeMarkup(l, null, {
          ...o.attrs,
          [t]: e
        });
      }) : i.setNodeMarkup(s.pos, null, {
        ...s.nodeAfter.attrs,
        [t]: e
      }), r(i);
    }
    return !0;
  };
}
function Mh(t) {
  return function(e, n) {
    if (!Ke(e)) return !1;
    if (n) {
      const r = be(e.schema), s = Ze(e), i = e.tr, o = s.map.cellsInRect(
        t == "column" ? {
          left: s.left,
          top: 0,
          right: s.right,
          bottom: s.map.height
        } : t == "row" ? {
          left: 0,
          top: s.top,
          right: s.map.width,
          bottom: s.bottom
        } : s
      ), l = o.map((a) => s.table.nodeAt(a));
      for (let a = 0; a < o.length; a++)
        l[a].type == r.header_cell && i.setNodeMarkup(
          s.tableStart + o[a],
          r.cell,
          l[a].attrs
        );
      if (i.steps.length == 0)
        for (let a = 0; a < o.length; a++)
          i.setNodeMarkup(
            s.tableStart + o[a],
            r.header_cell,
            l[a].attrs
          );
      n(i);
    }
    return !0;
  };
}
function Yi(t, e, n) {
  const r = e.map.cellsInRect({
    left: 0,
    top: 0,
    right: t == "row" ? e.map.width : 1,
    bottom: t == "column" ? e.map.height : 1
  });
  for (let s = 0; s < r.length; s++) {
    const i = e.table.nodeAt(r[s]);
    if (i && i.type !== n.header_cell)
      return !1;
  }
  return !0;
}
function ln(t, e) {
  return e = e || { useDeprecatedLogic: !1 }, e.useDeprecatedLogic ? Mh(t) : function(n, r) {
    if (!Ke(n)) return !1;
    if (r) {
      const s = be(n.schema), i = Ze(n), o = n.tr, l = Yi("row", i, s), a = Yi(
        "column",
        i,
        s
      ), u = (t === "column" ? l : t === "row" ? a : !1) ? 1 : 0, d = t == "column" ? {
        left: 0,
        top: u,
        right: 1,
        bottom: i.map.height
      } : t == "row" ? {
        left: u,
        top: 0,
        right: i.map.width,
        bottom: 1
      } : i, h = t == "column" ? a ? s.cell : s.header_cell : t == "row" ? l ? s.cell : s.header_cell : s.cell;
      i.map.cellsInRect(d).forEach((p) => {
        const m = p + i.tableStart, g = o.doc.nodeAt(m);
        g && o.setNodeMarkup(m, h, g.attrs);
      }), r(o);
    }
    return !0;
  };
}
ln("row", {
  useDeprecatedLogic: !0
});
ln("column", {
  useDeprecatedLogic: !0
});
var Oh = ln("cell", {
  useDeprecatedLogic: !0
});
function Fh(t, e) {
  if (e < 0) {
    const n = t.nodeBefore;
    if (n) return t.pos - n.nodeSize;
    for (let r = t.index(-1) - 1, s = t.before(); r >= 0; r--) {
      const i = t.node(-1).child(r), o = i.lastChild;
      if (o)
        return s - 1 - o.nodeSize;
      s -= i.nodeSize;
    }
  } else {
    if (t.index() < t.parent.childCount - 1)
      return t.pos + t.nodeAfter.nodeSize;
    const n = t.node(-1);
    for (let r = t.indexAfter(-1), s = t.after(); r < n.childCount; r++) {
      const i = n.child(r);
      if (i.childCount) return s + 1;
      s += i.nodeSize;
    }
  }
  return null;
}
function Ji(t) {
  return function(e, n) {
    if (!Ke(e)) return !1;
    const r = Fh(Ir(e), t);
    if (r == null) return !1;
    if (n) {
      const s = e.doc.resolve(r);
      n(
        e.tr.setSelection($e.between(s, hh(s))).scrollIntoView()
      );
    }
    return !0;
  };
}
function zh(t, e) {
  const n = t.selection.$anchor;
  for (let r = n.depth; r > 0; r--)
    if (n.node(r).type.spec.tableRole == "table")
      return e && e(
        t.tr.delete(n.before(r), n.after(r)).scrollIntoView()
      ), !0;
  return !1;
}
function Dn(t, e) {
  const n = t.selection;
  if (!(n instanceof W)) return !1;
  if (e) {
    const r = t.tr, s = be(t.schema).cell.createAndFill().content;
    n.forEachCell((i, o) => {
      i.content.eq(s) || r.replace(
        r.mapping.map(o + 1),
        r.mapping.map(o + i.nodeSize - 1),
        new Re(s, 0, 0)
      );
    }), r.docChanged && e(r);
  }
  return !0;
}
function Ph(t) {
  if (!t.size) return null;
  let { content: e, openStart: n, openEnd: r } = t;
  for (; e.childCount == 1 && (n > 0 && r > 0 || e.child(0).type.spec.tableRole == "table"); )
    n--, r--, e = e.child(0).content;
  const s = e.child(0), i = s.type.spec.tableRole, o = s.type.schema, l = [];
  if (i == "row")
    for (let a = 0; a < e.childCount; a++) {
      let c = e.child(a).content;
      const u = a ? 0 : Math.max(0, n - 1), d = a < e.childCount - 1 ? 0 : Math.max(0, r - 1);
      (u || d) && (c = bs(
        be(o).row,
        new Re(c, u, d)
      ).content), l.push(c);
    }
  else if (i == "cell" || i == "header_cell")
    l.push(
      n || r ? bs(
        be(o).row,
        new Re(e, n, r)
      ).content : e
    );
  else
    return null;
  return Bh(o, l);
}
function Bh(t, e) {
  const n = [];
  for (let s = 0; s < e.length; s++) {
    const i = e[s];
    for (let o = i.childCount - 1; o >= 0; o--) {
      const { rowspan: l, colspan: a } = i.child(o).attrs;
      for (let c = s; c < s + l; c++)
        n[c] = (n[c] || 0) + a;
    }
  }
  let r = 0;
  for (let s = 0; s < n.length; s++) r = Math.max(r, n[s]);
  for (let s = 0; s < n.length; s++)
    if (s >= e.length && e.push(ke.empty), n[s] < r) {
      const i = be(t).cell.createAndFill(), o = [];
      for (let l = n[s]; l < r; l++)
        o.push(i);
      e[s] = e[s].append(ke.from(o));
    }
  return { height: e.length, width: r, rows: e };
}
function bs(t, e) {
  const n = t.createAndFill();
  return new Nf(n).replace(0, n.content.size, e).doc;
}
function $h({ width: t, height: e, rows: n }, r, s) {
  if (t != r) {
    const i = [], o = [];
    for (let l = 0; l < n.length; l++) {
      const a = n[l], c = [];
      for (let u = i[l] || 0, d = 0; u < r; d++) {
        let h = a.child(d % a.childCount);
        u + h.attrs.colspan > r && (h = h.type.createChecked(
          Nt(
            h.attrs,
            h.attrs.colspan,
            u + h.attrs.colspan - r
          ),
          h.content
        )), c.push(h), u += h.attrs.colspan;
        for (let p = 1; p < h.attrs.rowspan; p++)
          i[l + p] = (i[l + p] || 0) + h.attrs.colspan;
      }
      o.push(ke.from(c));
    }
    n = o, t = r;
  }
  if (e != s) {
    const i = [];
    for (let o = 0, l = 0; o < s; o++, l++) {
      const a = [], c = n[l % e];
      for (let u = 0; u < c.childCount; u++) {
        let d = c.child(u);
        o + d.attrs.rowspan > s && (d = d.type.create(
          {
            ...d.attrs,
            rowspan: Math.max(1, s - d.attrs.rowspan)
          },
          d.content
        )), a.push(d);
      }
      i.push(ke.from(a));
    }
    n = i, e = s;
  }
  return { width: t, height: e, rows: n };
}
function Hh(t, e, n, r, s, i, o) {
  const l = t.doc.type.schema, a = be(l);
  let c, u;
  if (s > e.width)
    for (let d = 0, h = 0; d < e.height; d++) {
      const p = n.child(d);
      h += p.nodeSize;
      const m = [];
      let g;
      p.lastChild == null || p.lastChild.type == a.cell ? g = c || (c = a.cell.createAndFill()) : g = u || (u = a.header_cell.createAndFill());
      for (let b = e.width; b < s; b++) m.push(g);
      t.insert(t.mapping.slice(o).map(h - 1 + r), m);
    }
  if (i > e.height) {
    const d = [];
    for (let m = 0, g = (e.height - 1) * e.width; m < Math.max(e.width, s); m++) {
      const b = m >= e.width ? !1 : n.nodeAt(e.map[g + m]).type == a.header_cell;
      d.push(
        b ? u || (u = a.header_cell.createAndFill()) : c || (c = a.cell.createAndFill())
      );
    }
    const h = a.row.create(null, ke.from(d)), p = [];
    for (let m = e.height; m < i; m++) p.push(h);
    t.insert(t.mapping.slice(o).map(r + n.nodeSize - 2), p);
  }
  return !!(c || u);
}
function Xi(t, e, n, r, s, i, o, l) {
  if (o == 0 || o == e.height) return !1;
  let a = !1;
  for (let c = s; c < i; c++) {
    const u = o * e.width + c, d = e.map[u];
    if (e.map[u - e.width] == d) {
      a = !0;
      const h = n.nodeAt(d), { top: p, left: m } = e.findCell(d);
      t.setNodeMarkup(t.mapping.slice(l).map(d + r), null, {
        ...h.attrs,
        rowspan: o - p
      }), t.insert(
        t.mapping.slice(l).map(e.positionAt(o, m, n)),
        h.type.createAndFill({
          ...h.attrs,
          rowspan: p + h.attrs.rowspan - o
        })
      ), c += h.attrs.colspan - 1;
    }
  }
  return a;
}
function Zi(t, e, n, r, s, i, o, l) {
  if (o == 0 || o == e.width) return !1;
  let a = !1;
  for (let c = s; c < i; c++) {
    const u = c * e.width + o, d = e.map[u];
    if (e.map[u - 1] == d) {
      a = !0;
      const h = n.nodeAt(d), p = e.colCount(d), m = t.mapping.slice(l).map(d + r);
      t.setNodeMarkup(
        m,
        null,
        Nt(
          h.attrs,
          o - p,
          h.attrs.colspan - (o - p)
        )
      ), t.insert(
        m + h.nodeSize,
        h.type.createAndFill(
          Nt(h.attrs, 0, o - p)
        )
      ), c += h.attrs.rowspan - 1;
    }
  }
  return a;
}
function Qi(t, e, n, r, s) {
  let i = n ? t.doc.nodeAt(n - 1) : t.doc;
  if (!i)
    throw new Error("No table found");
  let o = Z.get(i);
  const { top: l, left: a } = r, c = a + s.width, u = l + s.height, d = t.tr;
  let h = 0;
  function p() {
    if (i = n ? d.doc.nodeAt(n - 1) : d.doc, !i)
      throw new Error("No table found");
    o = Z.get(i), h = d.mapping.maps.length;
  }
  Hh(d, o, i, n, c, u, h) && p(), Xi(d, o, i, n, a, c, l, h) && p(), Xi(d, o, i, n, a, c, u, h) && p(), Zi(d, o, i, n, l, u, a, h) && p(), Zi(d, o, i, n, l, u, c, h) && p();
  for (let m = l; m < u; m++) {
    const g = o.positionAt(m, a, i), b = o.positionAt(m, c, i);
    d.replace(
      d.mapping.slice(h).map(g + n),
      d.mapping.slice(h).map(b + n),
      new Re(s.rows[m - l], 0, 0)
    );
  }
  p(), d.setSelection(
    new W(
      d.doc.resolve(n + o.positionAt(l, a, i)),
      d.doc.resolve(n + o.positionAt(u - 1, c - 1, i))
    )
  ), e(d);
}
var jh = Sf({
  ArrowLeft: Rn("horiz", -1),
  ArrowRight: Rn("horiz", 1),
  ArrowUp: Rn("vert", -1),
  ArrowDown: Rn("vert", 1),
  "Shift-ArrowLeft": Ln("horiz", -1),
  "Shift-ArrowRight": Ln("horiz", 1),
  "Shift-ArrowUp": Ln("vert", -1),
  "Shift-ArrowDown": Ln("vert", 1),
  Backspace: Dn,
  "Mod-Backspace": Dn,
  Delete: Dn,
  "Mod-Delete": Dn
});
function Wn(t, e, n) {
  return n.eq(t.selection) ? !1 : (e && e(t.tr.setSelection(n).scrollIntoView()), !0);
}
function Rn(t, e) {
  return (n, r, s) => {
    if (!s) return !1;
    const i = n.selection;
    if (i instanceof W)
      return Wn(
        n,
        r,
        Ee.near(i.$headCell, e)
      );
    if (t != "horiz" && !i.empty) return !1;
    const o = ia(s, t, e);
    if (o == null) return !1;
    if (t == "horiz")
      return Wn(
        n,
        r,
        Ee.near(n.doc.resolve(i.head + e), e)
      );
    {
      const l = n.doc.resolve(o), a = Zl(l, t, e);
      let c;
      return a ? c = Ee.near(a, 1) : e < 0 ? c = Ee.near(n.doc.resolve(l.before(-1)), -1) : c = Ee.near(n.doc.resolve(l.after(-1)), 1), Wn(n, r, c);
    }
  };
}
function Ln(t, e) {
  return (n, r, s) => {
    if (!s) return !1;
    const i = n.selection;
    let o;
    if (i instanceof W)
      o = i;
    else {
      const a = ia(s, t, e);
      if (a == null) return !1;
      o = new W(n.doc.resolve(a));
    }
    const l = Zl(o.$headCell, t, e);
    return l ? Wn(
      n,
      r,
      new W(o.$anchorCell, l)
    ) : !1;
  };
}
function Vh(t, e) {
  const n = t.state.doc, r = Kt(n.resolve(e));
  return r ? (t.dispatch(t.state.tr.setSelection(new W(r))), !0) : !1;
}
function Uh(t, e, n) {
  if (!Ke(t.state)) return !1;
  let r = Ph(n);
  const s = t.state.selection;
  if (s instanceof W) {
    r || (r = {
      width: 1,
      height: 1,
      rows: [
        ke.from(
          bs(be(t.state.schema).cell, n)
        )
      ]
    });
    const i = s.$anchorCell.node(-1), o = s.$anchorCell.start(-1), l = Z.get(i).rectBetween(
      s.$anchorCell.pos - o,
      s.$headCell.pos - o
    );
    return r = $h(r, l.right - l.left, l.bottom - l.top), Qi(t.state, t.dispatch, o, l, r), !0;
  } else if (r) {
    const i = Ir(t.state), o = i.start(-1);
    return Qi(
      t.state,
      t.dispatch,
      o,
      Z.get(i.node(-1)).findCell(i.pos - o),
      r
    ), !0;
  } else
    return !1;
}
function Wh(t, e) {
  var n;
  if (e.ctrlKey || e.metaKey) return;
  const r = eo(t, e.target);
  let s;
  if (e.shiftKey && t.state.selection instanceof W)
    i(t.state.selection.$anchorCell, e), e.preventDefault();
  else if (e.shiftKey && r && (s = Kt(t.state.selection.$anchor)) != null && ((n = Ur(t, e)) == null ? void 0 : n.pos) != s.pos)
    i(s, e), e.preventDefault();
  else if (!r)
    return;
  function i(a, c) {
    let u = Ur(t, c);
    const d = ft.getState(t.state) == null;
    if (!u || !si(a, u))
      if (d) u = a;
      else return;
    const h = new W(a, u);
    if (d || !t.state.selection.eq(h)) {
      const p = t.state.tr.setSelection(h);
      d && p.setMeta(ft, a.pos), t.dispatch(p);
    }
  }
  function o() {
    t.root.removeEventListener("mouseup", o), t.root.removeEventListener("dragstart", o), t.root.removeEventListener("mousemove", l), ft.getState(t.state) != null && t.dispatch(t.state.tr.setMeta(ft, -1));
  }
  function l(a) {
    const c = a, u = ft.getState(t.state);
    let d;
    if (u != null)
      d = t.state.doc.resolve(u);
    else if (eo(t, c.target) != r && (d = Ur(t, e), !d))
      return o();
    d && i(d, c);
  }
  t.root.addEventListener("mouseup", o), t.root.addEventListener("dragstart", o), t.root.addEventListener("mousemove", l);
}
function ia(t, e, n) {
  if (!(t.state.selection instanceof $e)) return null;
  const { $head: r } = t.state.selection;
  for (let s = r.depth - 1; s >= 0; s--) {
    const i = r.node(s);
    if ((n < 0 ? r.index(s) : r.indexAfter(s)) != (n < 0 ? 0 : i.childCount)) return null;
    if (i.type.spec.tableRole == "cell" || i.type.spec.tableRole == "header_cell") {
      const l = r.before(s), a = e == "vert" ? n > 0 ? "down" : "up" : n > 0 ? "right" : "left";
      return t.endOfTextblock(a) ? l : null;
    }
  }
  return null;
}
function eo(t, e) {
  for (; e && e != t.dom; e = e.parentNode)
    if (e.nodeName == "TD" || e.nodeName == "TH")
      return e;
  return null;
}
function Ur(t, e) {
  const n = t.posAtCoords({
    left: e.clientX,
    top: e.clientY
  });
  return n && n ? Kt(t.state.doc.resolve(n.pos)) : null;
}
var Gh = class {
  constructor(e, n) {
    this.node = e, this.defaultCellMinWidth = n, this.dom = document.createElement("div"), this.dom.className = "tableWrapper", this.table = this.dom.appendChild(document.createElement("table")), this.table.style.setProperty(
      "--default-cell-min-width",
      `${n}px`
    ), this.colgroup = this.table.appendChild(document.createElement("colgroup")), ws(e, this.colgroup, this.table, n), this.contentDOM = this.table.appendChild(document.createElement("tbody"));
  }
  update(e) {
    return e.type != this.node.type ? !1 : (this.node = e, ws(
      e,
      this.colgroup,
      this.table,
      this.defaultCellMinWidth
    ), !0);
  }
  ignoreMutation(e) {
    return e.type == "attributes" && (e.target == this.table || this.colgroup.contains(e.target));
  }
};
function ws(t, e, n, r, s, i) {
  var o;
  let l = 0, a = !0, c = e.firstChild;
  const u = t.firstChild;
  if (u) {
    for (let d = 0, h = 0; d < u.childCount; d++) {
      const { colspan: p, colwidth: m } = u.child(d).attrs;
      for (let g = 0; g < p; g++, h++) {
        const b = s == h ? i : m && m[g], x = b ? b + "px" : "";
        if (l += b || r, b || (a = !1), c)
          c.style.width != x && (c.style.width = x), c = c.nextSibling;
        else {
          const v = document.createElement("col");
          v.style.width = x, e.appendChild(v);
        }
      }
    }
    for (; c; ) {
      const d = c.nextSibling;
      (o = c.parentNode) == null || o.removeChild(c), c = d;
    }
    a ? (n.style.width = l + "px", n.style.minWidth = "") : (n.style.width = "", n.style.minWidth = l + "px");
  }
}
var _e = new Ne(
  "tableColumnResizing"
);
function Kh({
  handleWidth: t = 5,
  cellMinWidth: e = 25,
  defaultCellMinWidth: n = 100,
  View: r = Gh,
  lastColumnResizable: s = !0
} = {}) {
  const i = new Xe({
    key: _e,
    state: {
      init(o, l) {
        var a, c;
        const u = (c = (a = i.spec) == null ? void 0 : a.props) == null ? void 0 : c.nodeViews, d = be(l.schema).table.name;
        return r && u && (u[d] = (h, p) => new r(h, n, p)), new qh(-1, !1);
      },
      apply(o, l) {
        return l.apply(o);
      }
    },
    props: {
      attributes: (o) => {
        const l = _e.getState(o);
        return l && l.activeHandle > -1 ? { class: "resize-cursor" } : {};
      },
      handleDOMEvents: {
        mousemove: (o, l) => {
          Yh(o, l, t, s);
        },
        mouseleave: (o) => {
          Jh(o);
        },
        mousedown: (o, l) => {
          Xh(o, l, e, n);
        }
      },
      decorations: (o) => {
        const l = _e.getState(o);
        if (l && l.activeHandle > -1)
          return np(o, l.activeHandle);
      },
      nodeViews: {}
    }
  });
  return i;
}
var qh = class Gn {
  constructor(e, n) {
    this.activeHandle = e, this.dragging = n;
  }
  apply(e) {
    const n = this, r = e.getMeta(_e);
    if (r && r.setHandle != null)
      return new Gn(r.setHandle, !1);
    if (r && r.setDragging !== void 0)
      return new Gn(n.activeHandle, r.setDragging);
    if (n.activeHandle > -1 && e.docChanged) {
      let s = e.mapping.map(n.activeHandle, -1);
      return gs(e.doc.resolve(s)) || (s = -1), new Gn(s, n.dragging);
    }
    return n;
  }
};
function Yh(t, e, n, r) {
  if (!t.editable) return;
  const s = _e.getState(t.state);
  if (s && !s.dragging) {
    const i = Qh(e.target);
    let o = -1;
    if (i) {
      const { left: l, right: a } = i.getBoundingClientRect();
      e.clientX - l <= n ? o = to(t, e, "left", n) : a - e.clientX <= n && (o = to(t, e, "right", n));
    }
    if (o != s.activeHandle) {
      if (!r && o !== -1) {
        const l = t.state.doc.resolve(o), a = l.node(-1), c = Z.get(a), u = l.start(-1);
        if (c.colCount(l.pos - u) + l.nodeAfter.attrs.colspan - 1 == c.width - 1)
          return;
      }
      oa(t, o);
    }
  }
}
function Jh(t) {
  if (!t.editable) return;
  const e = _e.getState(t.state);
  e && e.activeHandle > -1 && !e.dragging && oa(t, -1);
}
function Xh(t, e, n, r) {
  var s;
  if (!t.editable) return !1;
  const i = (s = t.dom.ownerDocument.defaultView) != null ? s : window, o = _e.getState(t.state);
  if (!o || o.activeHandle == -1 || o.dragging)
    return !1;
  const l = t.state.doc.nodeAt(o.activeHandle), a = Zh(t, o.activeHandle, l.attrs);
  t.dispatch(
    t.state.tr.setMeta(_e, {
      setDragging: { startX: e.clientX, startWidth: a }
    })
  );
  function c(d) {
    i.removeEventListener("mouseup", c), i.removeEventListener("mousemove", u);
    const h = _e.getState(t.state);
    h?.dragging && (ep(
      t,
      h.activeHandle,
      no(h.dragging, d, n)
    ), t.dispatch(
      t.state.tr.setMeta(_e, { setDragging: null })
    ));
  }
  function u(d) {
    if (!d.which) return c(d);
    const h = _e.getState(t.state);
    if (h && h.dragging) {
      const p = no(h.dragging, d, n);
      ro(
        t,
        h.activeHandle,
        p,
        r
      );
    }
  }
  return ro(
    t,
    o.activeHandle,
    a,
    r
  ), i.addEventListener("mouseup", c), i.addEventListener("mousemove", u), e.preventDefault(), !0;
}
function Zh(t, e, { colspan: n, colwidth: r }) {
  const s = r && r[r.length - 1];
  if (s) return s;
  const i = t.domAtPos(e);
  let l = i.node.childNodes[i.offset].offsetWidth, a = n;
  if (r)
    for (let c = 0; c < n; c++)
      r[c] && (l -= r[c], a--);
  return l / a;
}
function Qh(t) {
  for (; t && t.nodeName != "TD" && t.nodeName != "TH"; )
    t = t.classList && t.classList.contains("ProseMirror") ? null : t.parentNode;
  return t;
}
function to(t, e, n, r) {
  const s = n == "right" ? -r : r, i = t.posAtCoords({
    left: e.clientX + s,
    top: e.clientY
  });
  if (!i) return -1;
  const { pos: o } = i, l = Kt(t.state.doc.resolve(o));
  if (!l) return -1;
  if (n == "right") return l.pos;
  const a = Z.get(l.node(-1)), c = l.start(-1), u = a.map.indexOf(l.pos - c);
  return u % a.width == 0 ? -1 : c + a.map[u - 1];
}
function no(t, e, n) {
  const r = e.clientX - t.startX;
  return Math.max(n, t.startWidth + r);
}
function oa(t, e) {
  t.dispatch(
    t.state.tr.setMeta(_e, { setHandle: e })
  );
}
function ep(t, e, n) {
  const r = t.state.doc.resolve(e), s = r.node(-1), i = Z.get(s), o = r.start(-1), l = i.colCount(r.pos - o) + r.nodeAfter.attrs.colspan - 1, a = t.state.tr;
  for (let c = 0; c < i.height; c++) {
    const u = c * i.width + l;
    if (c && i.map[u] == i.map[u - i.width]) continue;
    const d = i.map[u], h = s.nodeAt(d).attrs, p = h.colspan == 1 ? 0 : l - i.colCount(d);
    if (h.colwidth && h.colwidth[p] == n) continue;
    const m = h.colwidth ? h.colwidth.slice() : tp(h.colspan);
    m[p] = n, a.setNodeMarkup(o + d, null, { ...h, colwidth: m });
  }
  a.docChanged && t.dispatch(a);
}
function ro(t, e, n, r) {
  const s = t.state.doc.resolve(e), i = s.node(-1), o = s.start(-1), l = Z.get(i).colCount(s.pos - o) + s.nodeAfter.attrs.colspan - 1;
  let a = t.domAtPos(s.start(-1)).node;
  for (; a && a.nodeName != "TABLE"; )
    a = a.parentNode;
  a && ws(
    i,
    a.firstChild,
    a,
    r,
    l,
    n
  );
}
function tp(t) {
  return Array(t).fill(0);
}
function np(t, e) {
  var n;
  const r = [], s = t.doc.resolve(e), i = s.node(-1);
  if (!i)
    return on.empty;
  const o = Z.get(i), l = s.start(-1), a = o.colCount(s.pos - l) + s.nodeAfter.attrs.colspan - 1;
  for (let c = 0; c < o.height; c++) {
    const u = a + c * o.width;
    if ((a == o.width - 1 || o.map[u] != o.map[u + 1]) && (c == 0 || o.map[u] != o.map[u - o.width])) {
      const d = o.map[u], h = l + d + i.nodeAt(d).nodeSize - 1, p = document.createElement("div");
      p.className = "column-resize-handle", (n = _e.getState(t)) != null && n.dragging && r.push(
        sr.node(
          l + d,
          l + d + i.nodeAt(d).nodeSize,
          {
            class: "column-resize-dragging"
          }
        )
      ), r.push(sr.widget(h, p));
    }
  }
  return on.create(t.doc, r);
}
function rp({
  allowTableNodeSelection: t = !1
} = {}) {
  return new Xe({
    key: ft,
    // This piece of state is used to remember when a mouse-drag
    // cell-selection is happening, so that it can continue even as
    // transactions (which might move its anchor cell) come in.
    state: {
      init() {
        return null;
      },
      apply(e, n) {
        const r = e.getMeta(ft);
        if (r != null) return r == -1 ? null : r;
        if (n == null || !e.docChanged) return n;
        const { deleted: s, pos: i } = e.mapping.mapResult(n);
        return s ? null : i;
      }
    },
    props: {
      decorations: gh,
      handleDOMEvents: {
        mousedown: Wh
      },
      createSelectionBetween(e) {
        return ft.getState(e.state) != null ? e.state.selection : null;
      },
      handleTripleClick: Vh,
      handleKeyDown: jh,
      handlePaste: Uh
    },
    appendTransaction(e, n, r) {
      return xh(
        r,
        na(r, n),
        t
      );
    }
  });
}
var sp = Fe.create({
  name: "tableCell",
  addOptions() {
    return {
      HTMLAttributes: {}
    };
  },
  content: "block+",
  addAttributes() {
    return {
      colspan: {
        default: 1
      },
      rowspan: {
        default: 1
      },
      colwidth: {
        default: null,
        parseHTML: (t) => {
          var e, n;
          const r = t.getAttribute("colwidth"), s = r ? r.split(",").map((i) => parseInt(i, 10)) : null;
          if (!s) {
            const i = (e = t.closest("table")) == null ? void 0 : e.querySelectorAll("colgroup > col"), o = Array.from(((n = t.parentElement) == null ? void 0 : n.children) || []).indexOf(t);
            if (o && o > -1 && i && i[o]) {
              const l = i[o].getAttribute("width");
              return l ? [parseInt(l, 10)] : null;
            }
          }
          return s;
        }
      }
    };
  },
  tableRole: "cell",
  isolating: !0,
  parseHTML() {
    return [{ tag: "td" }];
  },
  renderHTML({ HTMLAttributes: t }) {
    return ["td", Me(this.options.HTMLAttributes, t), 0];
  }
}), ip = Fe.create({
  name: "tableHeader",
  addOptions() {
    return {
      HTMLAttributes: {}
    };
  },
  content: "block+",
  addAttributes() {
    return {
      colspan: {
        default: 1
      },
      rowspan: {
        default: 1
      },
      colwidth: {
        default: null,
        parseHTML: (t) => {
          const e = t.getAttribute("colwidth");
          return e ? e.split(",").map((r) => parseInt(r, 10)) : null;
        }
      }
    };
  },
  tableRole: "header_cell",
  isolating: !0,
  parseHTML() {
    return [{ tag: "th" }];
  },
  renderHTML({ HTMLAttributes: t }) {
    return ["th", Me(this.options.HTMLAttributes, t), 0];
  }
}), op = Fe.create({
  name: "tableRow",
  addOptions() {
    return {
      HTMLAttributes: {}
    };
  },
  content: "(tableCell | tableHeader)*",
  tableRole: "row",
  parseHTML() {
    return [{ tag: "tr" }];
  },
  renderHTML({ HTMLAttributes: t }) {
    return ["tr", Me(this.options.HTMLAttributes, t), 0];
  }
});
function xs(t, e) {
  return e ? ["width", `${Math.max(e, t)}px`] : ["min-width", `${t}px`];
}
function so(t, e, n, r, s, i) {
  var o;
  let l = 0, a = !0, c = e.firstChild;
  const u = t.firstChild;
  if (u !== null)
    for (let h = 0, p = 0; h < u.childCount; h += 1) {
      const { colspan: m, colwidth: g } = u.child(h).attrs;
      for (let b = 0; b < m; b += 1, p += 1) {
        const x = s === p ? i : g && g[b], v = x ? `${x}px` : "";
        if (l += x || r, x || (a = !1), c) {
          if (c.style.width !== v) {
            const [S, E] = xs(r, x);
            c.style.setProperty(S, E);
          }
          c = c.nextSibling;
        } else {
          const S = document.createElement("col"), [E, I] = xs(r, x);
          S.style.setProperty(E, I), e.appendChild(S);
        }
      }
    }
  for (; c; ) {
    const h = c.nextSibling;
    (o = c.parentNode) == null || o.removeChild(c), c = h;
  }
  const d = t.attrs.style && typeof t.attrs.style == "string" && /\bwidth\s*:/i.test(t.attrs.style);
  a && !d ? (n.style.width = `${l}px`, n.style.minWidth = "") : (n.style.width = "", n.style.minWidth = `${l}px`);
}
var lp = class {
  constructor(t, e) {
    this.node = t, this.cellMinWidth = e, this.dom = document.createElement("div"), this.dom.className = "tableWrapper", this.table = this.dom.appendChild(document.createElement("table")), t.attrs.style && (this.table.style.cssText = t.attrs.style), this.colgroup = this.table.appendChild(document.createElement("colgroup")), so(t, this.colgroup, this.table, e), this.contentDOM = this.table.appendChild(document.createElement("tbody"));
  }
  update(t) {
    return t.type !== this.node.type ? !1 : (this.node = t, so(t, this.colgroup, this.table, this.cellMinWidth), !0);
  }
  ignoreMutation(t) {
    const e = t.target, n = this.dom.contains(e), r = this.contentDOM.contains(e);
    return !!(n && !r && (t.type === "attributes" || t.type === "childList" || t.type === "characterData"));
  }
};
function ap(t, e, n, r) {
  let s = 0, i = !0;
  const o = [], l = t.firstChild;
  if (!l)
    return {};
  for (let d = 0, h = 0; d < l.childCount; d += 1) {
    const { colspan: p, colwidth: m } = l.child(d).attrs;
    for (let g = 0; g < p; g += 1, h += 1) {
      const b = n === h ? r : m && m[g];
      s += b || e, b || (i = !1);
      const [x, v] = xs(e, b);
      o.push(["col", { style: `${x}: ${v}` }]);
    }
  }
  const a = i ? `${s}px` : "", c = i ? "" : `${s}px`;
  return { colgroup: ["colgroup", {}, ...o], tableWidth: a, tableMinWidth: c };
}
function io(t, e) {
  return t.createAndFill();
}
function cp(t) {
  if (t.cached.tableNodeTypes)
    return t.cached.tableNodeTypes;
  const e = {};
  return Object.keys(t.nodes).forEach((n) => {
    const r = t.nodes[n];
    r.spec.tableRole && (e[r.spec.tableRole] = r);
  }), t.cached.tableNodeTypes = e, e;
}
function dp(t, e, n, r, s) {
  const i = cp(t), o = [], l = [];
  for (let c = 0; c < n; c += 1) {
    const u = io(i.cell);
    if (u && l.push(u), r) {
      const d = io(i.header_cell);
      d && o.push(d);
    }
  }
  const a = [];
  for (let c = 0; c < e; c += 1)
    a.push(i.row.createChecked(null, r && c === 0 ? o : l));
  return i.table.createChecked(null, a);
}
function up(t) {
  return t instanceof W;
}
var Mn = ({ editor: t }) => {
  const { selection: e } = t.state;
  if (!up(e))
    return !1;
  let n = 0;
  const r = Af(e.ranges[0].$from, (i) => i.type.name === "table");
  return r?.node.descendants((i) => {
    if (i.type.name === "table")
      return !1;
    ["tableCell", "tableHeader"].includes(i.type.name) && (n += 1);
  }), n === e.ranges.length ? (t.commands.deleteTable(), !0) : !1;
}, fp = "";
function hp(t) {
  return (t || "").replace(/\s+/g, " ").trim();
}
function pp(t, e, n = {}) {
  var r;
  const s = (r = n.cellLineSeparator) != null ? r : fp;
  if (!t || !t.content || t.content.length === 0)
    return "";
  const i = [];
  t.content.forEach((m) => {
    const g = [];
    m.content && m.content.forEach((b) => {
      let x = "";
      b.content && Array.isArray(b.content) && b.content.length > 1 ? x = b.content.map((I) => e.renderChildren(I)).join(s) : x = b.content ? e.renderChildren(b.content) : "";
      const v = hp(x), S = b.type === "tableHeader";
      g.push({ text: v, isHeader: S });
    }), i.push(g);
  });
  const o = i.reduce((m, g) => Math.max(m, g.length), 0);
  if (o === 0)
    return "";
  const l = new Array(o).fill(0);
  i.forEach((m) => {
    var g;
    for (let b = 0; b < o; b += 1) {
      const v = (((g = m[b]) == null ? void 0 : g.text) || "").length;
      v > l[b] && (l[b] = v), l[b] < 3 && (l[b] = 3);
    }
  });
  const a = (m, g) => m + " ".repeat(Math.max(0, g - m.length)), c = i[0], u = c.some((m) => m.isHeader);
  let d = `
`;
  const h = new Array(o).fill(0).map((m, g) => u && c[g] && c[g].text || "");
  return d += `| ${h.map((m, g) => a(m, l[g])).join(" | ")} |
`, d += `| ${l.map((m) => "-".repeat(Math.max(3, m))).join(" | ")} |
`, (u ? i.slice(1) : i).forEach((m) => {
    d += `| ${new Array(o).fill(0).map((g, b) => a(m[b] && m[b].text || "", l[b])).join(" | ")} |
`;
  }), d;
}
var mp = pp, gp = Fe.create({
  name: "table",
  // @ts-ignore
  addOptions() {
    return {
      HTMLAttributes: {},
      resizable: !1,
      renderWrapper: !1,
      handleWidth: 5,
      cellMinWidth: 25,
      // TODO: fix
      View: lp,
      lastColumnResizable: !0,
      allowTableNodeSelection: !1
    };
  },
  content: "tableRow+",
  tableRole: "table",
  isolating: !0,
  group: "block",
  parseHTML() {
    return [{ tag: "table" }];
  },
  renderHTML({ node: t, HTMLAttributes: e }) {
    const { colgroup: n, tableWidth: r, tableMinWidth: s } = ap(t, this.options.cellMinWidth), i = e.style;
    function o() {
      return i || (r ? `width: ${r}` : `min-width: ${s}`);
    }
    const l = [
      "table",
      Me(this.options.HTMLAttributes, e, {
        style: o()
      }),
      n,
      ["tbody", 0]
    ];
    return this.options.renderWrapper ? ["div", { class: "tableWrapper" }, l] : l;
  },
  parseMarkdown: (t, e) => {
    const n = [];
    if (t.header) {
      const r = [];
      t.header.forEach((s) => {
        r.push(e.createNode("tableHeader", {}, [{ type: "paragraph", content: e.parseInline(s.tokens) }]));
      }), n.push(e.createNode("tableRow", {}, r));
    }
    return t.rows && t.rows.forEach((r) => {
      const s = [];
      r.forEach((i) => {
        s.push(e.createNode("tableCell", {}, [{ type: "paragraph", content: e.parseInline(i.tokens) }]));
      }), n.push(e.createNode("tableRow", {}, s));
    }), e.createNode("table", void 0, n);
  },
  renderMarkdown: (t, e) => mp(t, e),
  addCommands() {
    return {
      insertTable: ({ rows: t = 3, cols: e = 3, withHeaderRow: n = !0 } = {}) => ({ tr: r, dispatch: s, editor: i }) => {
        const o = dp(i.schema, t, e, n);
        if (s) {
          const l = r.selection.from + 1;
          r.replaceSelectionWith(o).scrollIntoView().setSelection($e.near(r.doc.resolve(l)));
        }
        return !0;
      },
      addColumnBefore: () => ({ state: t, dispatch: e }) => kh(t, e),
      addColumnAfter: () => ({ state: t, dispatch: e }) => Ch(t, e),
      deleteColumn: () => ({ state: t, dispatch: e }) => Nh(t, e),
      addRowBefore: () => ({ state: t, dispatch: e }) => _h(t, e),
      addRowAfter: () => ({ state: t, dispatch: e }) => Ah(t, e),
      deleteRow: () => ({ state: t, dispatch: e }) => Eh(t, e),
      deleteTable: () => ({ state: t, dispatch: e }) => zh(t, e),
      mergeCells: () => ({ state: t, dispatch: e }) => Ki(t, e),
      splitCell: () => ({ state: t, dispatch: e }) => qi(t, e),
      toggleHeaderColumn: () => ({ state: t, dispatch: e }) => ln("column")(t, e),
      toggleHeaderRow: () => ({ state: t, dispatch: e }) => ln("row")(t, e),
      toggleHeaderCell: () => ({ state: t, dispatch: e }) => Oh(t, e),
      mergeOrSplit: () => ({ state: t, dispatch: e }) => Ki(t, e) ? !0 : qi(t, e),
      setCellAttribute: (t, e) => ({ state: n, dispatch: r }) => Lh(t, e)(n, r),
      goToNextCell: () => ({ state: t, dispatch: e }) => Ji(1)(t, e),
      goToPreviousCell: () => ({ state: t, dispatch: e }) => Ji(-1)(t, e),
      fixTables: () => ({ state: t, dispatch: e }) => (e && na(t), !0),
      setCellSelection: (t) => ({ tr: e, dispatch: n }) => {
        if (n) {
          const r = W.create(e.doc, t.anchorCell, t.headCell);
          e.setSelection(r);
        }
        return !0;
      }
    };
  },
  addKeyboardShortcuts() {
    return {
      Tab: () => this.editor.commands.goToNextCell() ? !0 : this.editor.can().addRowAfter() ? this.editor.chain().addRowAfter().goToNextCell().run() : !1,
      "Shift-Tab": () => this.editor.commands.goToPreviousCell(),
      Backspace: Mn,
      "Mod-Backspace": Mn,
      Delete: Mn,
      "Mod-Delete": Mn
    };
  },
  addProseMirrorPlugins() {
    return [
      ...this.options.resizable && this.editor.isEditable ? [
        Kh({
          handleWidth: this.options.handleWidth,
          cellMinWidth: this.options.cellMinWidth,
          defaultCellMinWidth: this.options.cellMinWidth,
          View: this.options.View,
          lastColumnResizable: this.options.lastColumnResizable
        })
      ] : [],
      rp({
        allowTableNodeSelection: this.options.allowTableNodeSelection
      })
    ];
  },
  extendNodeSchema(t) {
    const e = {
      name: t.name,
      options: t.options,
      storage: t.storage
    };
    return {
      tableRole: If(_f(t, "tableRole", e))
    };
  }
}), bp = Dt.create({
  name: "tableKit",
  addExtensions() {
    const t = [];
    return this.options.table !== !1 && t.push(gp.configure(this.options.table)), this.options.tableCell !== !1 && t.push(sp.configure(this.options.tableCell)), this.options.tableHeader !== !1 && t.push(ip.configure(this.options.tableHeader)), this.options.tableRow !== !1 && t.push(op.configure(this.options.tableRow)), t;
  }
});
const wp = bp.configure({
  table: { resizable: !0 }
});
function xp(t) {
  if (typeof t != "string")
    throw new TypeError("Expected a string");
  return t.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}
function yp(t, e, n) {
  const s = ul((n || {}).ignore || []), i = vp(e);
  let o = -1;
  for (; ++o < i.length; )
    Bd(t, "text", l);
  function l(c, u) {
    let d = -1, h;
    for (; ++d < u.length; ) {
      const p = u[d], m = h ? h.children : void 0;
      if (s(
        p,
        m ? m.indexOf(p) : void 0,
        h
      ))
        return;
      h = p;
    }
    if (h)
      return a(c, u);
  }
  function a(c, u) {
    const d = u[u.length - 1], h = i[o][0], p = i[o][1];
    let m = 0;
    const b = d.children.indexOf(c);
    let x = !1, v = [];
    h.lastIndex = 0;
    let S = h.exec(c.value);
    for (; S; ) {
      const E = S.index, I = {
        index: S.index,
        input: S.input,
        stack: [...u, c]
      };
      let k = p(...S, I);
      if (typeof k == "string" && (k = k.length > 0 ? { type: "text", value: k } : void 0), k === !1 ? h.lastIndex = E + 1 : (m !== E && v.push({
        type: "text",
        value: c.value.slice(m, E)
      }), Array.isArray(k) ? v.push(...k) : k && v.push(k), m = E + S[0].length, x = !0), !h.global)
        break;
      S = h.exec(c.value);
    }
    return x ? (m < c.value.length && v.push({ type: "text", value: c.value.slice(m) }), d.children.splice(b, 1, ...v)) : v = [c], b + v.length;
  }
}
function vp(t) {
  const e = [];
  if (!Array.isArray(t))
    throw new TypeError("Expected find and replace tuple or list of tuples");
  const n = !t[0] || Array.isArray(t[0]) ? t : [t];
  let r = -1;
  for (; ++r < n.length; ) {
    const s = n[r];
    e.push([kp(s[0]), Cp(s[1])]);
  }
  return e;
}
function kp(t) {
  return typeof t == "string" ? new RegExp(xp(t), "g") : t;
}
function Cp(t) {
  return typeof t == "function" ? t : function() {
    return t;
  };
}
const Wr = "phrasing", Gr = ["autolink", "link", "image", "label"];
function Sp() {
  return {
    transforms: [Dp],
    enter: {
      literalAutolink: Ip,
      literalAutolinkEmail: Kr,
      literalAutolinkHttp: Kr,
      literalAutolinkWww: Kr
    },
    exit: {
      literalAutolink: Ep,
      literalAutolinkEmail: Tp,
      literalAutolinkHttp: _p,
      literalAutolinkWww: Ap
    }
  };
}
function Np() {
  return {
    unsafe: [
      {
        character: "@",
        before: "[+\\-.\\w]",
        after: "[\\-.\\w]",
        inConstruct: Wr,
        notInConstruct: Gr
      },
      {
        character: ".",
        before: "[Ww]",
        after: "[\\-.\\w]",
        inConstruct: Wr,
        notInConstruct: Gr
      },
      {
        character: ":",
        before: "[ps]",
        after: "\\/",
        inConstruct: Wr,
        notInConstruct: Gr
      }
    ]
  };
}
function Ip(t) {
  this.enter({ type: "link", title: null, url: "", children: [] }, t);
}
function Kr(t) {
  this.config.enter.autolinkProtocol.call(this, t);
}
function _p(t) {
  this.config.exit.autolinkProtocol.call(this, t);
}
function Ap(t) {
  this.config.exit.data.call(this, t);
  const e = this.stack[this.stack.length - 1];
  e.type, e.url = "http://" + this.sliceSerialize(t);
}
function Tp(t) {
  this.config.exit.autolinkEmail.call(this, t);
}
function Ep(t) {
  this.exit(t);
}
function Dp(t) {
  yp(
    t,
    [
      [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, Rp],
      [new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)", "gu"), Lp]
    ],
    { ignore: ["link", "linkReference"] }
  );
}
function Rp(t, e, n, r, s) {
  let i = "";
  if (!la(s) || (/^w/i.test(e) && (n = e + n, e = "", i = "http://"), !Mp(n)))
    return !1;
  const o = Op(n + r);
  if (!o[0]) return !1;
  const l = {
    type: "link",
    title: null,
    url: i + e + o[0],
    children: [{ type: "text", value: e + o[0] }]
  };
  return o[1] ? [l, { type: "text", value: o[1] }] : l;
}
function Lp(t, e, n, r) {
  return (
    // Not an expected previous character.
    !la(r, !0) || // Label ends in not allowed character.
    /[-\d_]$/.test(n) ? !1 : {
      type: "link",
      title: null,
      url: "mailto:" + e + "@" + n,
      children: [{ type: "text", value: e + "@" + n }]
    }
  );
}
function Mp(t) {
  const e = t.split(".");
  return !(e.length < 2 || e[e.length - 1] && (/_/.test(e[e.length - 1]) || !/[a-zA-Z\d]/.test(e[e.length - 1])) || e[e.length - 2] && (/_/.test(e[e.length - 2]) || !/[a-zA-Z\d]/.test(e[e.length - 2])));
}
function Op(t) {
  const e = /[!"&'),.:;<>?\]}]+$/.exec(t);
  if (!e)
    return [t, void 0];
  t = t.slice(0, e.index);
  let n = e[0], r = n.indexOf(")");
  const s = Di(t, "(");
  let i = Di(t, ")");
  for (; r !== -1 && s > i; )
    t += n.slice(0, r + 1), n = n.slice(r + 1), r = n.indexOf(")"), i++;
  return [t, n];
}
function la(t, e) {
  const n = t.input.charCodeAt(t.index - 1);
  return (t.index === 0 || Ht(n) || qs(n)) && // If it’s an email, the previous character should not be a slash.
  (!e || n !== 47);
}
aa.peek = Up;
function Fp() {
  this.buffer();
}
function zp(t) {
  this.enter({ type: "footnoteReference", identifier: "", label: "" }, t);
}
function Pp() {
  this.buffer();
}
function Bp(t) {
  this.enter(
    { type: "footnoteDefinition", identifier: "", label: "", children: [] },
    t
  );
}
function $p(t) {
  const e = this.resume(), n = this.stack[this.stack.length - 1];
  n.type, n.identifier = xn(
    this.sliceSerialize(t)
  ).toLowerCase(), n.label = e;
}
function Hp(t) {
  this.exit(t);
}
function jp(t) {
  const e = this.resume(), n = this.stack[this.stack.length - 1];
  n.type, n.identifier = xn(
    this.sliceSerialize(t)
  ).toLowerCase(), n.label = e;
}
function Vp(t) {
  this.exit(t);
}
function Up() {
  return "[";
}
function aa(t, e, n, r) {
  const s = n.createTracker(r);
  let i = s.move("[^");
  const o = n.enter("footnoteReference"), l = n.enter("reference");
  return i += s.move(
    n.safe(n.associationId(t), { after: "]", before: i })
  ), l(), o(), i += s.move("]"), i;
}
function Wp() {
  return {
    enter: {
      gfmFootnoteCallString: Fp,
      gfmFootnoteCall: zp,
      gfmFootnoteDefinitionLabelString: Pp,
      gfmFootnoteDefinition: Bp
    },
    exit: {
      gfmFootnoteCallString: $p,
      gfmFootnoteCall: Hp,
      gfmFootnoteDefinitionLabelString: jp,
      gfmFootnoteDefinition: Vp
    }
  };
}
function Gp(t) {
  let e = !1;
  return t && t.firstLineBlank && (e = !0), {
    handlers: { footnoteDefinition: n, footnoteReference: aa },
    // This is on by default already.
    unsafe: [{ character: "[", inConstruct: ["label", "phrasing", "reference"] }]
  };
  function n(r, s, i, o) {
    const l = i.createTracker(o);
    let a = l.move("[^");
    const c = i.enter("footnoteDefinition"), u = i.enter("label");
    return a += l.move(
      i.safe(i.associationId(r), { before: a, after: "]" })
    ), u(), a += l.move("]:"), r.children && r.children.length > 0 && (l.shift(4), a += l.move(
      (e ? `
` : " ") + i.indentLines(
        i.containerFlow(r, l.current()),
        e ? ca : Kp
      )
    )), c(), a;
  }
}
function Kp(t, e, n) {
  return e === 0 ? t : ca(t, e, n);
}
function ca(t, e, n) {
  return (n ? "" : "    ") + t;
}
const qp = [
  "autolink",
  "destinationLiteral",
  "destinationRaw",
  "reference",
  "titleQuote",
  "titleApostrophe"
];
da.peek = Qp;
function Yp() {
  return {
    canContainEols: ["delete"],
    enter: { strikethrough: Xp },
    exit: { strikethrough: Zp }
  };
}
function Jp() {
  return {
    unsafe: [
      {
        character: "~",
        inConstruct: "phrasing",
        notInConstruct: qp
      }
    ],
    handlers: { delete: da }
  };
}
function Xp(t) {
  this.enter({ type: "delete", children: [] }, t);
}
function Zp(t) {
  this.exit(t);
}
function da(t, e, n, r) {
  const s = n.createTracker(r), i = n.enter("strikethrough");
  let o = s.move("~~");
  return o += n.containerPhrasing(t, {
    ...s.current(),
    before: o,
    after: "~"
  }), o += s.move("~~"), i(), o;
}
function Qp() {
  return "~";
}
function em(t) {
  return t.length;
}
function tm(t, e) {
  const n = e || {}, r = (n.align || []).concat(), s = n.stringLength || em, i = [], o = [], l = [], a = [];
  let c = 0, u = -1;
  for (; ++u < t.length; ) {
    const g = [], b = [];
    let x = -1;
    for (t[u].length > c && (c = t[u].length); ++x < t[u].length; ) {
      const v = nm(t[u][x]);
      if (n.alignDelimiters !== !1) {
        const S = s(v);
        b[x] = S, (a[x] === void 0 || S > a[x]) && (a[x] = S);
      }
      g.push(v);
    }
    o[u] = g, l[u] = b;
  }
  let d = -1;
  if (typeof r == "object" && "length" in r)
    for (; ++d < c; )
      i[d] = oo(r[d]);
  else {
    const g = oo(r);
    for (; ++d < c; )
      i[d] = g;
  }
  d = -1;
  const h = [], p = [];
  for (; ++d < c; ) {
    const g = i[d];
    let b = "", x = "";
    g === 99 ? (b = ":", x = ":") : g === 108 ? b = ":" : g === 114 && (x = ":");
    let v = n.alignDelimiters === !1 ? 1 : Math.max(
      1,
      a[d] - b.length - x.length
    );
    const S = b + "-".repeat(v) + x;
    n.alignDelimiters !== !1 && (v = b.length + v + x.length, v > a[d] && (a[d] = v), p[d] = v), h[d] = S;
  }
  o.splice(1, 0, h), l.splice(1, 0, p), u = -1;
  const m = [];
  for (; ++u < o.length; ) {
    const g = o[u], b = l[u];
    d = -1;
    const x = [];
    for (; ++d < c; ) {
      const v = g[d] || "";
      let S = "", E = "";
      if (n.alignDelimiters !== !1) {
        const I = a[d] - (b[d] || 0), k = i[d];
        k === 114 ? S = " ".repeat(I) : k === 99 ? I % 2 ? (S = " ".repeat(I / 2 + 0.5), E = " ".repeat(I / 2 - 0.5)) : (S = " ".repeat(I / 2), E = S) : E = " ".repeat(I);
      }
      n.delimiterStart !== !1 && !d && x.push("|"), n.padding !== !1 && // Don’t add the opening space if we’re not aligning and the cell is
      // empty: there will be a closing space.
      !(n.alignDelimiters === !1 && v === "") && (n.delimiterStart !== !1 || d) && x.push(" "), n.alignDelimiters !== !1 && x.push(S), x.push(v), n.alignDelimiters !== !1 && x.push(E), n.padding !== !1 && x.push(" "), (n.delimiterEnd !== !1 || d !== c - 1) && x.push("|");
    }
    m.push(
      n.delimiterEnd === !1 ? x.join("").replace(/ +$/, "") : x.join("")
    );
  }
  return m.join(`
`);
}
function nm(t) {
  return t == null ? "" : String(t);
}
function oo(t) {
  const e = typeof t == "string" ? t.codePointAt(0) : 0;
  return e === 67 || e === 99 ? 99 : e === 76 || e === 108 ? 108 : e === 82 || e === 114 ? 114 : 0;
}
function rm(t, e, n, r) {
  const s = n.enter("blockquote"), i = n.createTracker(r);
  i.move("> "), i.shift(2);
  const o = n.indentLines(
    n.containerFlow(t, i.current()),
    sm
  );
  return s(), o;
}
function sm(t, e, n) {
  return ">" + (n ? "" : " ") + t;
}
function im(t, e) {
  return lo(t, e.inConstruct, !0) && !lo(t, e.notInConstruct, !1);
}
function lo(t, e, n) {
  if (typeof e == "string" && (e = [e]), !e || e.length === 0)
    return n;
  let r = -1;
  for (; ++r < e.length; )
    if (t.includes(e[r]))
      return !0;
  return !1;
}
function ao(t, e, n, r) {
  let s = -1;
  for (; ++s < n.unsafe.length; )
    if (n.unsafe[s].character === `
` && im(n.stack, n.unsafe[s]))
      return /[ \t]/.test(r.before) ? "" : " ";
  return `\\
`;
}
function om(t, e) {
  const n = String(t);
  let r = n.indexOf(e), s = r, i = 0, o = 0;
  if (typeof e != "string")
    throw new TypeError("Expected substring");
  for (; r !== -1; )
    r === s ? ++i > o && (o = i) : i = 1, s = r + e.length, r = n.indexOf(e, s);
  return o;
}
function lm(t, e) {
  return !!(e.options.fences === !1 && t.value && // If there’s no info…
  !t.lang && // And there’s a non-whitespace character…
  /[^ \r\n]/.test(t.value) && // And the value doesn’t start or end in a blank…
  !/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(t.value));
}
function am(t) {
  const e = t.options.fence || "`";
  if (e !== "`" && e !== "~")
    throw new Error(
      "Cannot serialize code with `" + e + "` for `options.fence`, expected `` ` `` or `~`"
    );
  return e;
}
function cm(t, e, n, r) {
  const s = am(n), i = t.value || "", o = s === "`" ? "GraveAccent" : "Tilde";
  if (lm(t, n)) {
    const d = n.enter("codeIndented"), h = n.indentLines(i, dm);
    return d(), h;
  }
  const l = n.createTracker(r), a = s.repeat(Math.max(om(i, s) + 1, 3)), c = n.enter("codeFenced");
  let u = l.move(a);
  if (t.lang) {
    const d = n.enter(`codeFencedLang${o}`);
    u += l.move(
      n.safe(t.lang, {
        before: u,
        after: " ",
        encode: ["`"],
        ...l.current()
      })
    ), d();
  }
  if (t.lang && t.meta) {
    const d = n.enter(`codeFencedMeta${o}`);
    u += l.move(" "), u += l.move(
      n.safe(t.meta, {
        before: u,
        after: `
`,
        encode: ["`"],
        ...l.current()
      })
    ), d();
  }
  return u += l.move(`
`), i && (u += l.move(i + `
`)), u += l.move(a), c(), u;
}
function dm(t, e, n) {
  return (n ? "" : "    ") + t;
}
function ii(t) {
  const e = t.options.quote || '"';
  if (e !== '"' && e !== "'")
    throw new Error(
      "Cannot serialize title with `" + e + "` for `options.quote`, expected `\"`, or `'`"
    );
  return e;
}
function um(t, e, n, r) {
  const s = ii(n), i = s === '"' ? "Quote" : "Apostrophe", o = n.enter("definition");
  let l = n.enter("label");
  const a = n.createTracker(r);
  let c = a.move("[");
  return c += a.move(
    n.safe(n.associationId(t), {
      before: c,
      after: "]",
      ...a.current()
    })
  ), c += a.move("]: "), l(), // If there’s no url, or…
  !t.url || // If there are control characters or whitespace.
  /[\0- \u007F]/.test(t.url) ? (l = n.enter("destinationLiteral"), c += a.move("<"), c += a.move(
    n.safe(t.url, { before: c, after: ">", ...a.current() })
  ), c += a.move(">")) : (l = n.enter("destinationRaw"), c += a.move(
    n.safe(t.url, {
      before: c,
      after: t.title ? " " : `
`,
      ...a.current()
    })
  )), l(), t.title && (l = n.enter(`title${i}`), c += a.move(" " + s), c += a.move(
    n.safe(t.title, {
      before: c,
      after: s,
      ...a.current()
    })
  ), c += a.move(s), l()), o(), c;
}
function fm(t) {
  const e = t.options.emphasis || "*";
  if (e !== "*" && e !== "_")
    throw new Error(
      "Cannot serialize emphasis with `" + e + "` for `options.emphasis`, expected `*`, or `_`"
    );
  return e;
}
function an(t) {
  return "&#x" + t.toString(16).toUpperCase() + ";";
}
function or(t, e, n) {
  const r = nr(t), s = nr(e);
  return r === void 0 ? s === void 0 ? (
    // Letter inside:
    // we have to encode *both* letters for `_` as it is looser.
    // it already forms for `*` (and GFMs `~`).
    n === "_" ? { inside: !0, outside: !0 } : { inside: !1, outside: !1 }
  ) : s === 1 ? (
    // Whitespace inside: encode both (letter, whitespace).
    { inside: !0, outside: !0 }
  ) : (
    // Punctuation inside: encode outer (letter)
    { inside: !1, outside: !0 }
  ) : r === 1 ? s === void 0 ? (
    // Letter inside: already forms.
    { inside: !1, outside: !1 }
  ) : s === 1 ? (
    // Whitespace inside: encode both (whitespace).
    { inside: !0, outside: !0 }
  ) : (
    // Punctuation inside: already forms.
    { inside: !1, outside: !1 }
  ) : s === void 0 ? (
    // Letter inside: already forms.
    { inside: !1, outside: !1 }
  ) : s === 1 ? (
    // Whitespace inside: encode inner (whitespace).
    { inside: !0, outside: !1 }
  ) : (
    // Punctuation inside: already forms.
    { inside: !1, outside: !1 }
  );
}
ua.peek = hm;
function ua(t, e, n, r) {
  const s = fm(n), i = n.enter("emphasis"), o = n.createTracker(r), l = o.move(s);
  let a = o.move(
    n.containerPhrasing(t, {
      after: s,
      before: l,
      ...o.current()
    })
  );
  const c = a.charCodeAt(0), u = or(
    r.before.charCodeAt(r.before.length - 1),
    c,
    s
  );
  u.inside && (a = an(c) + a.slice(1));
  const d = a.charCodeAt(a.length - 1), h = or(r.after.charCodeAt(0), d, s);
  h.inside && (a = a.slice(0, -1) + an(d));
  const p = o.move(s);
  return i(), n.attentionEncodeSurroundingInfo = {
    after: h.outside,
    before: u.outside
  }, l + a + p;
}
function hm(t, e, n) {
  return n.options.emphasis || "*";
}
function pm(t, e) {
  let n = !1;
  return $d(t, function(r) {
    if ("value" in r && /\r?\n|\r/.test(r.value) || r.type === "break")
      return n = !0, Hd;
  }), !!((!t.depth || t.depth < 3) && fl(t) && (e.options.setext || n));
}
function mm(t, e, n, r) {
  const s = Math.max(Math.min(6, t.depth || 1), 1), i = n.createTracker(r);
  if (pm(t, n)) {
    const u = n.enter("headingSetext"), d = n.enter("phrasing"), h = n.containerPhrasing(t, {
      ...i.current(),
      before: `
`,
      after: `
`
    });
    return d(), u(), h + `
` + (s === 1 ? "=" : "-").repeat(
      // The whole size…
      h.length - // Minus the position of the character after the last EOL (or
      // 0 if there is none)…
      (Math.max(h.lastIndexOf("\r"), h.lastIndexOf(`
`)) + 1)
    );
  }
  const o = "#".repeat(s), l = n.enter("headingAtx"), a = n.enter("phrasing");
  i.move(o + " ");
  let c = n.containerPhrasing(t, {
    before: "# ",
    after: `
`,
    ...i.current()
  });
  return /^[\t ]/.test(c) && (c = an(c.charCodeAt(0)) + c.slice(1)), c = c ? o + " " + c : o, n.options.closeAtx && (c += " " + o), a(), l(), c;
}
fa.peek = gm;
function fa(t) {
  return t.value || "";
}
function gm() {
  return "<";
}
ha.peek = bm;
function ha(t, e, n, r) {
  const s = ii(n), i = s === '"' ? "Quote" : "Apostrophe", o = n.enter("image");
  let l = n.enter("label");
  const a = n.createTracker(r);
  let c = a.move("![");
  return c += a.move(
    n.safe(t.alt, { before: c, after: "]", ...a.current() })
  ), c += a.move("]("), l(), // If there’s no url but there is a title…
  !t.url && t.title || // If there are control characters or whitespace.
  /[\0- \u007F]/.test(t.url) ? (l = n.enter("destinationLiteral"), c += a.move("<"), c += a.move(
    n.safe(t.url, { before: c, after: ">", ...a.current() })
  ), c += a.move(">")) : (l = n.enter("destinationRaw"), c += a.move(
    n.safe(t.url, {
      before: c,
      after: t.title ? " " : ")",
      ...a.current()
    })
  )), l(), t.title && (l = n.enter(`title${i}`), c += a.move(" " + s), c += a.move(
    n.safe(t.title, {
      before: c,
      after: s,
      ...a.current()
    })
  ), c += a.move(s), l()), c += a.move(")"), o(), c;
}
function bm() {
  return "!";
}
pa.peek = wm;
function pa(t, e, n, r) {
  const s = t.referenceType, i = n.enter("imageReference");
  let o = n.enter("label");
  const l = n.createTracker(r);
  let a = l.move("![");
  const c = n.safe(t.alt, {
    before: a,
    after: "]",
    ...l.current()
  });
  a += l.move(c + "]["), o();
  const u = n.stack;
  n.stack = [], o = n.enter("reference");
  const d = n.safe(n.associationId(t), {
    before: a,
    after: "]",
    ...l.current()
  });
  return o(), n.stack = u, i(), s === "full" || !c || c !== d ? a += l.move(d + "]") : s === "shortcut" ? a = a.slice(0, -1) : a += l.move("]"), a;
}
function wm() {
  return "!";
}
ma.peek = xm;
function ma(t, e, n) {
  let r = t.value || "", s = "`", i = -1;
  for (; new RegExp("(^|[^`])" + s + "([^`]|$)").test(r); )
    s += "`";
  for (/[^ \r\n]/.test(r) && (/^[ \r\n]/.test(r) && /[ \r\n]$/.test(r) || /^`|`$/.test(r)) && (r = " " + r + " "); ++i < n.unsafe.length; ) {
    const o = n.unsafe[i], l = n.compilePattern(o);
    let a;
    if (o.atBreak)
      for (; a = l.exec(r); ) {
        let c = a.index;
        r.charCodeAt(c) === 10 && r.charCodeAt(c - 1) === 13 && c--, r = r.slice(0, c) + " " + r.slice(a.index + 1);
      }
  }
  return s + r + s;
}
function xm() {
  return "`";
}
function ga(t, e) {
  const n = fl(t);
  return !!(!e.options.resourceLink && // If there’s a url…
  t.url && // And there’s a no title…
  !t.title && // And the content of `node` is a single text node…
  t.children && t.children.length === 1 && t.children[0].type === "text" && // And if the url is the same as the content…
  (n === t.url || "mailto:" + n === t.url) && // And that starts w/ a protocol…
  /^[a-z][a-z+.-]+:/i.test(t.url) && // And that doesn’t contain ASCII control codes (character escapes and
  // references don’t work), space, or angle brackets…
  !/[\0- <>\u007F]/.test(t.url));
}
ba.peek = ym;
function ba(t, e, n, r) {
  const s = ii(n), i = s === '"' ? "Quote" : "Apostrophe", o = n.createTracker(r);
  let l, a;
  if (ga(t, n)) {
    const u = n.stack;
    n.stack = [], l = n.enter("autolink");
    let d = o.move("<");
    return d += o.move(
      n.containerPhrasing(t, {
        before: d,
        after: ">",
        ...o.current()
      })
    ), d += o.move(">"), l(), n.stack = u, d;
  }
  l = n.enter("link"), a = n.enter("label");
  let c = o.move("[");
  return c += o.move(
    n.containerPhrasing(t, {
      before: c,
      after: "](",
      ...o.current()
    })
  ), c += o.move("]("), a(), // If there’s no url but there is a title…
  !t.url && t.title || // If there are control characters or whitespace.
  /[\0- \u007F]/.test(t.url) ? (a = n.enter("destinationLiteral"), c += o.move("<"), c += o.move(
    n.safe(t.url, { before: c, after: ">", ...o.current() })
  ), c += o.move(">")) : (a = n.enter("destinationRaw"), c += o.move(
    n.safe(t.url, {
      before: c,
      after: t.title ? " " : ")",
      ...o.current()
    })
  )), a(), t.title && (a = n.enter(`title${i}`), c += o.move(" " + s), c += o.move(
    n.safe(t.title, {
      before: c,
      after: s,
      ...o.current()
    })
  ), c += o.move(s), a()), c += o.move(")"), l(), c;
}
function ym(t, e, n) {
  return ga(t, n) ? "<" : "[";
}
wa.peek = vm;
function wa(t, e, n, r) {
  const s = t.referenceType, i = n.enter("linkReference");
  let o = n.enter("label");
  const l = n.createTracker(r);
  let a = l.move("[");
  const c = n.containerPhrasing(t, {
    before: a,
    after: "]",
    ...l.current()
  });
  a += l.move(c + "]["), o();
  const u = n.stack;
  n.stack = [], o = n.enter("reference");
  const d = n.safe(n.associationId(t), {
    before: a,
    after: "]",
    ...l.current()
  });
  return o(), n.stack = u, i(), s === "full" || !c || c !== d ? a += l.move(d + "]") : s === "shortcut" ? a = a.slice(0, -1) : a += l.move("]"), a;
}
function vm() {
  return "[";
}
function oi(t) {
  const e = t.options.bullet || "*";
  if (e !== "*" && e !== "+" && e !== "-")
    throw new Error(
      "Cannot serialize items with `" + e + "` for `options.bullet`, expected `*`, `+`, or `-`"
    );
  return e;
}
function km(t) {
  const e = oi(t), n = t.options.bulletOther;
  if (!n)
    return e === "*" ? "-" : "*";
  if (n !== "*" && n !== "+" && n !== "-")
    throw new Error(
      "Cannot serialize items with `" + n + "` for `options.bulletOther`, expected `*`, `+`, or `-`"
    );
  if (n === e)
    throw new Error(
      "Expected `bullet` (`" + e + "`) and `bulletOther` (`" + n + "`) to be different"
    );
  return n;
}
function Cm(t) {
  const e = t.options.bulletOrdered || ".";
  if (e !== "." && e !== ")")
    throw new Error(
      "Cannot serialize items with `" + e + "` for `options.bulletOrdered`, expected `.` or `)`"
    );
  return e;
}
function xa(t) {
  const e = t.options.rule || "*";
  if (e !== "*" && e !== "-" && e !== "_")
    throw new Error(
      "Cannot serialize rules with `" + e + "` for `options.rule`, expected `*`, `-`, or `_`"
    );
  return e;
}
function Sm(t, e, n, r) {
  const s = n.enter("list"), i = n.bulletCurrent;
  let o = t.ordered ? Cm(n) : oi(n);
  const l = t.ordered ? o === "." ? ")" : "." : km(n);
  let a = e && n.bulletLastUsed ? o === n.bulletLastUsed : !1;
  if (!t.ordered) {
    const u = t.children ? t.children[0] : void 0;
    if (
      // Bullet could be used as a thematic break marker:
      (o === "*" || o === "-") && // Empty first list item:
      u && (!u.children || !u.children[0]) && // Directly in two other list items:
      n.stack[n.stack.length - 1] === "list" && n.stack[n.stack.length - 2] === "listItem" && n.stack[n.stack.length - 3] === "list" && n.stack[n.stack.length - 4] === "listItem" && // That are each the first child.
      n.indexStack[n.indexStack.length - 1] === 0 && n.indexStack[n.indexStack.length - 2] === 0 && n.indexStack[n.indexStack.length - 3] === 0 && (a = !0), xa(n) === o && u
    ) {
      let d = -1;
      for (; ++d < t.children.length; ) {
        const h = t.children[d];
        if (h && h.type === "listItem" && h.children && h.children[0] && h.children[0].type === "thematicBreak") {
          a = !0;
          break;
        }
      }
    }
  }
  a && (o = l), n.bulletCurrent = o;
  const c = n.containerFlow(t, r);
  return n.bulletLastUsed = o, n.bulletCurrent = i, s(), c;
}
function Nm(t) {
  const e = t.options.listItemIndent || "one";
  if (e !== "tab" && e !== "one" && e !== "mixed")
    throw new Error(
      "Cannot serialize items with `" + e + "` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`"
    );
  return e;
}
function Im(t, e, n, r) {
  const s = Nm(n);
  let i = n.bulletCurrent || oi(n);
  e && e.type === "list" && e.ordered && (i = (typeof e.start == "number" && e.start > -1 ? e.start : 1) + (n.options.incrementListMarker === !1 ? 0 : e.children.indexOf(t)) + i);
  let o = i.length + 1;
  (s === "tab" || s === "mixed" && (e && e.type === "list" && e.spread || t.spread)) && (o = Math.ceil(o / 4) * 4);
  const l = n.createTracker(r);
  l.move(i + " ".repeat(o - i.length)), l.shift(o);
  const a = n.enter("listItem"), c = n.indentLines(
    n.containerFlow(t, l.current()),
    u
  );
  return a(), c;
  function u(d, h, p) {
    return h ? (p ? "" : " ".repeat(o)) + d : (p ? i : i + " ".repeat(o - i.length)) + d;
  }
}
function _m(t, e, n, r) {
  const s = n.enter("paragraph"), i = n.enter("phrasing"), o = n.containerPhrasing(t, r);
  return i(), s(), o;
}
const Am = (
  /** @type {(node?: unknown) => node is Exclude<PhrasingContent, Html>} */
  ul([
    "break",
    "delete",
    "emphasis",
    // To do: next major: removed since footnotes were added to GFM.
    "footnote",
    "footnoteReference",
    "image",
    "imageReference",
    "inlineCode",
    // Enabled by `mdast-util-math`:
    "inlineMath",
    "link",
    "linkReference",
    // Enabled by `mdast-util-mdx`:
    "mdxJsxTextElement",
    // Enabled by `mdast-util-mdx`:
    "mdxTextExpression",
    "strong",
    "text",
    // Enabled by `mdast-util-directive`:
    "textDirective"
  ])
);
function Tm(t, e, n, r) {
  return (t.children.some(function(o) {
    return Am(o);
  }) ? n.containerPhrasing : n.containerFlow).call(n, t, r);
}
function Em(t) {
  const e = t.options.strong || "*";
  if (e !== "*" && e !== "_")
    throw new Error(
      "Cannot serialize strong with `" + e + "` for `options.strong`, expected `*`, or `_`"
    );
  return e;
}
ya.peek = Dm;
function ya(t, e, n, r) {
  const s = Em(n), i = n.enter("strong"), o = n.createTracker(r), l = o.move(s + s);
  let a = o.move(
    n.containerPhrasing(t, {
      after: s,
      before: l,
      ...o.current()
    })
  );
  const c = a.charCodeAt(0), u = or(
    r.before.charCodeAt(r.before.length - 1),
    c,
    s
  );
  u.inside && (a = an(c) + a.slice(1));
  const d = a.charCodeAt(a.length - 1), h = or(r.after.charCodeAt(0), d, s);
  h.inside && (a = a.slice(0, -1) + an(d));
  const p = o.move(s + s);
  return i(), n.attentionEncodeSurroundingInfo = {
    after: h.outside,
    before: u.outside
  }, l + a + p;
}
function Dm(t, e, n) {
  return n.options.strong || "*";
}
function Rm(t, e, n, r) {
  return n.safe(t.value, r);
}
function Lm(t) {
  const e = t.options.ruleRepetition || 3;
  if (e < 3)
    throw new Error(
      "Cannot serialize rules with repetition `" + e + "` for `options.ruleRepetition`, expected `3` or more"
    );
  return e;
}
function Mm(t, e, n) {
  const r = (xa(n) + (n.options.ruleSpaces ? " " : "")).repeat(Lm(n));
  return n.options.ruleSpaces ? r.slice(0, -1) : r;
}
const va = {
  blockquote: rm,
  break: ao,
  code: cm,
  definition: um,
  emphasis: ua,
  hardBreak: ao,
  heading: mm,
  html: fa,
  image: ha,
  imageReference: pa,
  inlineCode: ma,
  link: ba,
  linkReference: wa,
  list: Sm,
  listItem: Im,
  paragraph: _m,
  root: Tm,
  strong: ya,
  text: Rm,
  thematicBreak: Mm
};
function Om() {
  return {
    enter: {
      table: Fm,
      tableData: co,
      tableHeader: co,
      tableRow: Pm
    },
    exit: {
      codeText: Bm,
      table: zm,
      tableData: qr,
      tableHeader: qr,
      tableRow: qr
    }
  };
}
function Fm(t) {
  const e = t._align;
  this.enter(
    {
      type: "table",
      align: e.map(function(n) {
        return n === "none" ? null : n;
      }),
      children: []
    },
    t
  ), this.data.inTable = !0;
}
function zm(t) {
  this.exit(t), this.data.inTable = void 0;
}
function Pm(t) {
  this.enter({ type: "tableRow", children: [] }, t);
}
function qr(t) {
  this.exit(t);
}
function co(t) {
  this.enter({ type: "tableCell", children: [] }, t);
}
function Bm(t) {
  let e = this.resume();
  this.data.inTable && (e = e.replace(/\\([\\|])/g, $m));
  const n = this.stack[this.stack.length - 1];
  n.type, n.value = e, this.exit(t);
}
function $m(t, e) {
  return e === "|" ? e : t;
}
function Hm(t) {
  const e = t || {}, n = e.tableCellPadding, r = e.tablePipeAlign, s = e.stringLength, i = n ? " " : "|";
  return {
    unsafe: [
      { character: "\r", inConstruct: "tableCell" },
      { character: `
`, inConstruct: "tableCell" },
      // A pipe, when followed by a tab or space (padding), or a dash or colon
      // (unpadded delimiter row), could result in a table.
      { atBreak: !0, character: "|", after: "[	 :-]" },
      // A pipe in a cell must be encoded.
      { character: "|", inConstruct: "tableCell" },
      // A colon must be followed by a dash, in which case it could start a
      // delimiter row.
      { atBreak: !0, character: ":", after: "-" },
      // A delimiter row can also start with a dash, when followed by more
      // dashes, a colon, or a pipe.
      // This is a stricter version than the built in check for lists, thematic
      // breaks, and setex heading underlines though:
      // <https://github.com/syntax-tree/mdast-util-to-markdown/blob/51a2038/lib/unsafe.js#L57>
      { atBreak: !0, character: "-", after: "[:|-]" }
    ],
    handlers: {
      inlineCode: h,
      table: o,
      tableCell: a,
      tableRow: l
    }
  };
  function o(p, m, g, b) {
    return c(u(p, g, b), p.align);
  }
  function l(p, m, g, b) {
    const x = d(p, g, b), v = c([x]);
    return v.slice(0, v.indexOf(`
`));
  }
  function a(p, m, g, b) {
    const x = g.enter("tableCell"), v = g.enter("phrasing"), S = g.containerPhrasing(p, {
      ...b,
      before: i,
      after: i
    });
    return v(), x(), S;
  }
  function c(p, m) {
    return tm(p, {
      align: m,
      // @ts-expect-error: `markdown-table` types should support `null`.
      alignDelimiters: r,
      // @ts-expect-error: `markdown-table` types should support `null`.
      padding: n,
      // @ts-expect-error: `markdown-table` types should support `null`.
      stringLength: s
    });
  }
  function u(p, m, g) {
    const b = p.children;
    let x = -1;
    const v = [], S = m.enter("table");
    for (; ++x < b.length; )
      v[x] = d(b[x], m, g);
    return S(), v;
  }
  function d(p, m, g) {
    const b = p.children;
    let x = -1;
    const v = [], S = m.enter("tableRow");
    for (; ++x < b.length; )
      v[x] = a(b[x], p, m, g);
    return S(), v;
  }
  function h(p, m, g) {
    let b = va.inlineCode(p, m, g);
    return g.stack.includes("tableCell") && (b = b.replace(/\|/g, "\\$&")), b;
  }
}
function jm() {
  return {
    exit: {
      taskListCheckValueChecked: uo,
      taskListCheckValueUnchecked: uo,
      paragraph: Um
    }
  };
}
function Vm() {
  return {
    unsafe: [{ atBreak: !0, character: "-", after: "[:|-]" }],
    handlers: { listItem: Wm }
  };
}
function uo(t) {
  const e = this.stack[this.stack.length - 2];
  e.type, e.checked = t.type === "taskListCheckValueChecked";
}
function Um(t) {
  const e = this.stack[this.stack.length - 2];
  if (e && e.type === "listItem" && typeof e.checked == "boolean") {
    const n = this.stack[this.stack.length - 1];
    n.type;
    const r = n.children[0];
    if (r && r.type === "text") {
      const s = e.children;
      let i = -1, o;
      for (; ++i < s.length; ) {
        const l = s[i];
        if (l.type === "paragraph") {
          o = l;
          break;
        }
      }
      o === n && (r.value = r.value.slice(1), r.value.length === 0 ? n.children.shift() : n.position && r.position && typeof r.position.start.offset == "number" && (r.position.start.column++, r.position.start.offset++, n.position.start = Object.assign({}, r.position.start)));
    }
  }
  this.exit(t);
}
function Wm(t, e, n, r) {
  const s = t.children[0], i = typeof t.checked == "boolean" && s && s.type === "paragraph", o = "[" + (t.checked ? "x" : " ") + "] ", l = n.createTracker(r);
  i && l.move(o);
  let a = va.listItem(t, e, n, {
    ...r,
    ...l.current()
  });
  return i && (a = a.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/, c)), a;
  function c(u) {
    return u + o;
  }
}
function Gm() {
  return [
    Sp(),
    Wp(),
    Yp(),
    Om(),
    jm()
  ];
}
function Km(t) {
  return {
    extensions: [
      Np(),
      Gp(t),
      Jp(),
      Hm(t),
      Vm()
    ]
  };
}
const qm = {
  tokenize: eg,
  partial: !0
}, ka = {
  tokenize: tg,
  partial: !0
}, Ca = {
  tokenize: ng,
  partial: !0
}, Sa = {
  tokenize: rg,
  partial: !0
}, Ym = {
  tokenize: sg,
  partial: !0
}, Na = {
  name: "wwwAutolink",
  tokenize: Zm,
  previous: _a
}, Ia = {
  name: "protocolAutolink",
  tokenize: Qm,
  previous: Aa
}, lt = {
  name: "emailAutolink",
  tokenize: Xm,
  previous: Ta
}, Qe = {};
function Jm() {
  return {
    text: Qe
  };
}
let yt = 48;
for (; yt < 123; )
  Qe[yt] = lt, yt++, yt === 58 ? yt = 65 : yt === 91 && (yt = 97);
Qe[43] = lt;
Qe[45] = lt;
Qe[46] = lt;
Qe[95] = lt;
Qe[72] = [lt, Ia];
Qe[104] = [lt, Ia];
Qe[87] = [lt, Na];
Qe[119] = [lt, Na];
function Xm(t, e, n) {
  const r = this;
  let s, i;
  return o;
  function o(d) {
    return !ys(d) || !Ta.call(r, r.previous) || li(r.events) ? n(d) : (t.enter("literalAutolink"), t.enter("literalAutolinkEmail"), l(d));
  }
  function l(d) {
    return ys(d) ? (t.consume(d), l) : d === 64 ? (t.consume(d), a) : n(d);
  }
  function a(d) {
    return d === 46 ? t.check(Ym, u, c)(d) : d === 45 || d === 95 || Ys(d) ? (i = !0, t.consume(d), a) : u(d);
  }
  function c(d) {
    return t.consume(d), s = !0, a;
  }
  function u(d) {
    return i && s && rn(r.previous) ? (t.exit("literalAutolinkEmail"), t.exit("literalAutolink"), e(d)) : n(d);
  }
}
function Zm(t, e, n) {
  const r = this;
  return s;
  function s(o) {
    return o !== 87 && o !== 119 || !_a.call(r, r.previous) || li(r.events) ? n(o) : (t.enter("literalAutolink"), t.enter("literalAutolinkWww"), t.check(qm, t.attempt(ka, t.attempt(Ca, i), n), n)(o));
  }
  function i(o) {
    return t.exit("literalAutolinkWww"), t.exit("literalAutolink"), e(o);
  }
}
function Qm(t, e, n) {
  const r = this;
  let s = "", i = !1;
  return o;
  function o(d) {
    return (d === 72 || d === 104) && Aa.call(r, r.previous) && !li(r.events) ? (t.enter("literalAutolink"), t.enter("literalAutolinkHttp"), s += String.fromCodePoint(d), t.consume(d), l) : n(d);
  }
  function l(d) {
    if (rn(d) && s.length < 5)
      return s += String.fromCodePoint(d), t.consume(d), l;
    if (d === 58) {
      const h = s.toLowerCase();
      if (h === "http" || h === "https")
        return t.consume(d), a;
    }
    return n(d);
  }
  function a(d) {
    return d === 47 ? (t.consume(d), i ? c : (i = !0, a)) : n(d);
  }
  function c(d) {
    return d === null || jd(d) || Ae(d) || Ht(d) || qs(d) ? n(d) : t.attempt(ka, t.attempt(Ca, u), n)(d);
  }
  function u(d) {
    return t.exit("literalAutolinkHttp"), t.exit("literalAutolink"), e(d);
  }
}
function eg(t, e, n) {
  let r = 0;
  return s;
  function s(o) {
    return (o === 87 || o === 119) && r < 3 ? (r++, t.consume(o), s) : o === 46 && r === 3 ? (t.consume(o), i) : n(o);
  }
  function i(o) {
    return o === null ? n(o) : e(o);
  }
}
function tg(t, e, n) {
  let r, s, i;
  return o;
  function o(c) {
    return c === 46 || c === 95 ? t.check(Sa, a, l)(c) : c === null || Ae(c) || Ht(c) || c !== 45 && qs(c) ? a(c) : (i = !0, t.consume(c), o);
  }
  function l(c) {
    return c === 95 ? r = !0 : (s = r, r = void 0), t.consume(c), o;
  }
  function a(c) {
    return s || r || !i ? n(c) : e(c);
  }
}
function ng(t, e) {
  let n = 0, r = 0;
  return s;
  function s(o) {
    return o === 40 ? (n++, t.consume(o), s) : o === 41 && r < n ? i(o) : o === 33 || o === 34 || o === 38 || o === 39 || o === 41 || o === 42 || o === 44 || o === 46 || o === 58 || o === 59 || o === 60 || o === 63 || o === 93 || o === 95 || o === 126 ? t.check(Sa, e, i)(o) : o === null || Ae(o) || Ht(o) ? e(o) : (t.consume(o), s);
  }
  function i(o) {
    return o === 41 && r++, t.consume(o), s;
  }
}
function rg(t, e, n) {
  return r;
  function r(l) {
    return l === 33 || l === 34 || l === 39 || l === 41 || l === 42 || l === 44 || l === 46 || l === 58 || l === 59 || l === 63 || l === 95 || l === 126 ? (t.consume(l), r) : l === 38 ? (t.consume(l), i) : l === 93 ? (t.consume(l), s) : (
      // `<` is an end.
      l === 60 || // So is whitespace.
      l === null || Ae(l) || Ht(l) ? e(l) : n(l)
    );
  }
  function s(l) {
    return l === null || l === 40 || l === 91 || Ae(l) || Ht(l) ? e(l) : r(l);
  }
  function i(l) {
    return rn(l) ? o(l) : n(l);
  }
  function o(l) {
    return l === 59 ? (t.consume(l), r) : rn(l) ? (t.consume(l), o) : n(l);
  }
}
function sg(t, e, n) {
  return r;
  function r(i) {
    return t.consume(i), s;
  }
  function s(i) {
    return Ys(i) ? n(i) : e(i);
  }
}
function _a(t) {
  return t === null || t === 40 || t === 42 || t === 95 || t === 91 || t === 93 || t === 126 || Ae(t);
}
function Aa(t) {
  return !rn(t);
}
function Ta(t) {
  return !(t === 47 || ys(t));
}
function ys(t) {
  return t === 43 || t === 45 || t === 46 || t === 95 || Ys(t);
}
function li(t) {
  let e = t.length, n = !1;
  for (; e--; ) {
    const r = t[e][1];
    if ((r.type === "labelLink" || r.type === "labelImage") && !r._balanced) {
      n = !0;
      break;
    }
    if (r._gfmAutolinkLiteralWalkedInto) {
      n = !1;
      break;
    }
  }
  return t.length > 0 && !n && (t[t.length - 1][1]._gfmAutolinkLiteralWalkedInto = !0), n;
}
const ig = {
  tokenize: hg,
  partial: !0
};
function og() {
  return {
    document: {
      91: {
        name: "gfmFootnoteDefinition",
        tokenize: dg,
        continuation: {
          tokenize: ug
        },
        exit: fg
      }
    },
    text: {
      91: {
        name: "gfmFootnoteCall",
        tokenize: cg
      },
      93: {
        name: "gfmPotentialFootnoteCall",
        add: "after",
        tokenize: lg,
        resolveTo: ag
      }
    }
  };
}
function lg(t, e, n) {
  const r = this;
  let s = r.events.length;
  const i = r.parser.gfmFootnotes || (r.parser.gfmFootnotes = []);
  let o;
  for (; s--; ) {
    const a = r.events[s][1];
    if (a.type === "labelImage") {
      o = a;
      break;
    }
    if (a.type === "gfmFootnoteCall" || a.type === "labelLink" || a.type === "label" || a.type === "image" || a.type === "link")
      break;
  }
  return l;
  function l(a) {
    if (!o || !o._balanced)
      return n(a);
    const c = xn(r.sliceSerialize({
      start: o.end,
      end: r.now()
    }));
    return c.codePointAt(0) !== 94 || !i.includes(c.slice(1)) ? n(a) : (t.enter("gfmFootnoteCallLabelMarker"), t.consume(a), t.exit("gfmFootnoteCallLabelMarker"), e(a));
  }
}
function ag(t, e) {
  let n = t.length;
  for (; n--; )
    if (t[n][1].type === "labelImage" && t[n][0] === "enter") {
      t[n][1];
      break;
    }
  t[n + 1][1].type = "data", t[n + 3][1].type = "gfmFootnoteCallLabelMarker";
  const r = {
    type: "gfmFootnoteCall",
    start: Object.assign({}, t[n + 3][1].start),
    end: Object.assign({}, t[t.length - 1][1].end)
  }, s = {
    type: "gfmFootnoteCallMarker",
    start: Object.assign({}, t[n + 3][1].end),
    end: Object.assign({}, t[n + 3][1].end)
  };
  s.end.column++, s.end.offset++, s.end._bufferIndex++;
  const i = {
    type: "gfmFootnoteCallString",
    start: Object.assign({}, s.end),
    end: Object.assign({}, t[t.length - 1][1].start)
  }, o = {
    type: "chunkString",
    contentType: "string",
    start: Object.assign({}, i.start),
    end: Object.assign({}, i.end)
  }, l = [
    // Take the `labelImageMarker` (now `data`, the `!`)
    t[n + 1],
    t[n + 2],
    ["enter", r, e],
    // The `[`
    t[n + 3],
    t[n + 4],
    // The `^`.
    ["enter", s, e],
    ["exit", s, e],
    // Everything in between.
    ["enter", i, e],
    ["enter", o, e],
    ["exit", o, e],
    ["exit", i, e],
    // The ending (`]`, properly parsed and labelled).
    t[t.length - 2],
    t[t.length - 1],
    ["exit", r, e]
  ];
  return t.splice(n, t.length - n + 1, ...l), t;
}
function cg(t, e, n) {
  const r = this, s = r.parser.gfmFootnotes || (r.parser.gfmFootnotes = []);
  let i = 0, o;
  return l;
  function l(d) {
    return t.enter("gfmFootnoteCall"), t.enter("gfmFootnoteCallLabelMarker"), t.consume(d), t.exit("gfmFootnoteCallLabelMarker"), a;
  }
  function a(d) {
    return d !== 94 ? n(d) : (t.enter("gfmFootnoteCallMarker"), t.consume(d), t.exit("gfmFootnoteCallMarker"), t.enter("gfmFootnoteCallString"), t.enter("chunkString").contentType = "string", c);
  }
  function c(d) {
    if (
      // Too long.
      i > 999 || // Closing brace with nothing.
      d === 93 && !o || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      d === null || d === 91 || Ae(d)
    )
      return n(d);
    if (d === 93) {
      t.exit("chunkString");
      const h = t.exit("gfmFootnoteCallString");
      return s.includes(xn(r.sliceSerialize(h))) ? (t.enter("gfmFootnoteCallLabelMarker"), t.consume(d), t.exit("gfmFootnoteCallLabelMarker"), t.exit("gfmFootnoteCall"), e) : n(d);
    }
    return Ae(d) || (o = !0), i++, t.consume(d), d === 92 ? u : c;
  }
  function u(d) {
    return d === 91 || d === 92 || d === 93 ? (t.consume(d), i++, c) : c(d);
  }
}
function dg(t, e, n) {
  const r = this, s = r.parser.gfmFootnotes || (r.parser.gfmFootnotes = []);
  let i, o = 0, l;
  return a;
  function a(m) {
    return t.enter("gfmFootnoteDefinition")._container = !0, t.enter("gfmFootnoteDefinitionLabel"), t.enter("gfmFootnoteDefinitionLabelMarker"), t.consume(m), t.exit("gfmFootnoteDefinitionLabelMarker"), c;
  }
  function c(m) {
    return m === 94 ? (t.enter("gfmFootnoteDefinitionMarker"), t.consume(m), t.exit("gfmFootnoteDefinitionMarker"), t.enter("gfmFootnoteDefinitionLabelString"), t.enter("chunkString").contentType = "string", u) : n(m);
  }
  function u(m) {
    if (
      // Too long.
      o > 999 || // Closing brace with nothing.
      m === 93 && !l || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      m === null || m === 91 || Ae(m)
    )
      return n(m);
    if (m === 93) {
      t.exit("chunkString");
      const g = t.exit("gfmFootnoteDefinitionLabelString");
      return i = xn(r.sliceSerialize(g)), t.enter("gfmFootnoteDefinitionLabelMarker"), t.consume(m), t.exit("gfmFootnoteDefinitionLabelMarker"), t.exit("gfmFootnoteDefinitionLabel"), h;
    }
    return Ae(m) || (l = !0), o++, t.consume(m), m === 92 ? d : u;
  }
  function d(m) {
    return m === 91 || m === 92 || m === 93 ? (t.consume(m), o++, u) : u(m);
  }
  function h(m) {
    return m === 58 ? (t.enter("definitionMarker"), t.consume(m), t.exit("definitionMarker"), s.includes(i) || s.push(i), ut(t, p, "gfmFootnoteDefinitionWhitespace")) : n(m);
  }
  function p(m) {
    return e(m);
  }
}
function ug(t, e, n) {
  return t.check(Vd, e, t.attempt(ig, e, n));
}
function fg(t) {
  t.exit("gfmFootnoteDefinition");
}
function hg(t, e, n) {
  const r = this;
  return ut(t, s, "gfmFootnoteDefinitionIndent", 5);
  function s(i) {
    const o = r.events[r.events.length - 1];
    return o && o[1].type === "gfmFootnoteDefinitionIndent" && o[2].sliceSerialize(o[1], !0).length === 4 ? e(i) : n(i);
  }
}
function pg(t) {
  let n = (t || {}).singleTilde;
  const r = {
    name: "strikethrough",
    tokenize: i,
    resolveAll: s
  };
  return n == null && (n = !0), {
    text: {
      126: r
    },
    insideSpan: {
      null: [r]
    },
    attentionMarkers: {
      null: [126]
    }
  };
  function s(o, l) {
    let a = -1;
    for (; ++a < o.length; )
      if (o[a][0] === "enter" && o[a][1].type === "strikethroughSequenceTemporary" && o[a][1]._close) {
        let c = a;
        for (; c--; )
          if (o[c][0] === "exit" && o[c][1].type === "strikethroughSequenceTemporary" && o[c][1]._open && // If the sizes are the same:
          o[a][1].end.offset - o[a][1].start.offset === o[c][1].end.offset - o[c][1].start.offset) {
            o[a][1].type = "strikethroughSequence", o[c][1].type = "strikethroughSequence";
            const u = {
              type: "strikethrough",
              start: Object.assign({}, o[c][1].start),
              end: Object.assign({}, o[a][1].end)
            }, d = {
              type: "strikethroughText",
              start: Object.assign({}, o[c][1].end),
              end: Object.assign({}, o[a][1].start)
            }, h = [["enter", u, l], ["enter", o[c][1], l], ["exit", o[c][1], l], ["enter", d, l]], p = l.parser.constructs.insideSpan.null;
            p && Vr(h, h.length, 0, Ud(p, o.slice(c + 1, a), l)), Vr(h, h.length, 0, [["exit", d, l], ["enter", o[a][1], l], ["exit", o[a][1], l], ["exit", u, l]]), Vr(o, c - 1, a - c + 3, h), a = c + h.length - 2;
            break;
          }
      }
    for (a = -1; ++a < o.length; )
      o[a][1].type === "strikethroughSequenceTemporary" && (o[a][1].type = "data");
    return o;
  }
  function i(o, l, a) {
    const c = this.previous, u = this.events;
    let d = 0;
    return h;
    function h(m) {
      return c === 126 && u[u.length - 1][1].type !== "characterEscape" ? a(m) : (o.enter("strikethroughSequenceTemporary"), p(m));
    }
    function p(m) {
      const g = nr(c);
      if (m === 126)
        return d > 1 ? a(m) : (o.consume(m), d++, p);
      if (d < 2 && !n) return a(m);
      const b = o.exit("strikethroughSequenceTemporary"), x = nr(m);
      return b._open = !x || x === 2 && !!g, b._close = !g || g === 2 && !!x, l(m);
    }
  }
}
class mg {
  /**
   * Create a new edit map.
   */
  constructor() {
    this.map = [];
  }
  /**
   * Create an edit: a remove and/or add at a certain place.
   *
   * @param {number} index
   * @param {number} remove
   * @param {Array<Event>} add
   * @returns {undefined}
   */
  add(e, n, r) {
    gg(this, e, n, r);
  }
  // To do: add this when moving to `micromark`.
  // /**
  //  * Create an edit: but insert `add` before existing additions.
  //  *
  //  * @param {number} index
  //  * @param {number} remove
  //  * @param {Array<Event>} add
  //  * @returns {undefined}
  //  */
  // addBefore(index, remove, add) {
  //   addImplementation(this, index, remove, add, true)
  // }
  /**
   * Done, change the events.
   *
   * @param {Array<Event>} events
   * @returns {undefined}
   */
  consume(e) {
    if (this.map.sort(function(i, o) {
      return i[0] - o[0];
    }), this.map.length === 0)
      return;
    let n = this.map.length;
    const r = [];
    for (; n > 0; )
      n -= 1, r.push(e.slice(this.map[n][0] + this.map[n][1]), this.map[n][2]), e.length = this.map[n][0];
    r.push(e.slice()), e.length = 0;
    let s = r.pop();
    for (; s; ) {
      for (const i of s)
        e.push(i);
      s = r.pop();
    }
    this.map.length = 0;
  }
}
function gg(t, e, n, r) {
  let s = 0;
  if (!(n === 0 && r.length === 0)) {
    for (; s < t.map.length; ) {
      if (t.map[s][0] === e) {
        t.map[s][1] += n, t.map[s][2].push(...r);
        return;
      }
      s += 1;
    }
    t.map.push([e, n, r]);
  }
}
function bg(t, e) {
  let n = !1;
  const r = [];
  for (; e < t.length; ) {
    const s = t[e];
    if (n) {
      if (s[0] === "enter")
        s[1].type === "tableContent" && r.push(t[e + 1][1].type === "tableDelimiterMarker" ? "left" : "none");
      else if (s[1].type === "tableContent") {
        if (t[e - 1][1].type === "tableDelimiterMarker") {
          const i = r.length - 1;
          r[i] = r[i] === "left" ? "center" : "right";
        }
      } else if (s[1].type === "tableDelimiterRow")
        break;
    } else s[0] === "enter" && s[1].type === "tableDelimiterRow" && (n = !0);
    e += 1;
  }
  return r;
}
function wg() {
  return {
    flow: {
      null: {
        name: "table",
        tokenize: xg,
        resolveAll: yg
      }
    }
  };
}
function xg(t, e, n) {
  const r = this;
  let s = 0, i = 0, o;
  return l;
  function l(y) {
    let N = r.events.length - 1;
    for (; N > -1; ) {
      const P = r.events[N][1].type;
      if (P === "lineEnding" || // Note: markdown-rs uses `whitespace` instead of `linePrefix`
      P === "linePrefix") N--;
      else break;
    }
    const M = N > -1 ? r.events[N][1].type : null, z = M === "tableHead" || M === "tableRow" ? k : a;
    return z === k && r.parser.lazy[r.now().line] ? n(y) : z(y);
  }
  function a(y) {
    return t.enter("tableHead"), t.enter("tableRow"), c(y);
  }
  function c(y) {
    return y === 124 || (o = !0, i += 1), u(y);
  }
  function u(y) {
    return y === null ? n(y) : tn(y) ? i > 1 ? (i = 0, r.interrupt = !0, t.exit("tableRow"), t.enter("lineEnding"), t.consume(y), t.exit("lineEnding"), p) : n(y) : Mt(y) ? ut(t, u, "whitespace")(y) : (i += 1, o && (o = !1, s += 1), y === 124 ? (t.enter("tableCellDivider"), t.consume(y), t.exit("tableCellDivider"), o = !0, u) : (t.enter("data"), d(y)));
  }
  function d(y) {
    return y === null || y === 124 || Ae(y) ? (t.exit("data"), u(y)) : (t.consume(y), y === 92 ? h : d);
  }
  function h(y) {
    return y === 92 || y === 124 ? (t.consume(y), d) : d(y);
  }
  function p(y) {
    return r.interrupt = !1, r.parser.lazy[r.now().line] ? n(y) : (t.enter("tableDelimiterRow"), o = !1, Mt(y) ? ut(t, m, "linePrefix", r.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(y) : m(y));
  }
  function m(y) {
    return y === 45 || y === 58 ? b(y) : y === 124 ? (o = !0, t.enter("tableCellDivider"), t.consume(y), t.exit("tableCellDivider"), g) : I(y);
  }
  function g(y) {
    return Mt(y) ? ut(t, b, "whitespace")(y) : b(y);
  }
  function b(y) {
    return y === 58 ? (i += 1, o = !0, t.enter("tableDelimiterMarker"), t.consume(y), t.exit("tableDelimiterMarker"), x) : y === 45 ? (i += 1, x(y)) : y === null || tn(y) ? E(y) : I(y);
  }
  function x(y) {
    return y === 45 ? (t.enter("tableDelimiterFiller"), v(y)) : I(y);
  }
  function v(y) {
    return y === 45 ? (t.consume(y), v) : y === 58 ? (o = !0, t.exit("tableDelimiterFiller"), t.enter("tableDelimiterMarker"), t.consume(y), t.exit("tableDelimiterMarker"), S) : (t.exit("tableDelimiterFiller"), S(y));
  }
  function S(y) {
    return Mt(y) ? ut(t, E, "whitespace")(y) : E(y);
  }
  function E(y) {
    return y === 124 ? m(y) : y === null || tn(y) ? !o || s !== i ? I(y) : (t.exit("tableDelimiterRow"), t.exit("tableHead"), e(y)) : I(y);
  }
  function I(y) {
    return n(y);
  }
  function k(y) {
    return t.enter("tableRow"), T(y);
  }
  function T(y) {
    return y === 124 ? (t.enter("tableCellDivider"), t.consume(y), t.exit("tableCellDivider"), T) : y === null || tn(y) ? (t.exit("tableRow"), e(y)) : Mt(y) ? ut(t, T, "whitespace")(y) : (t.enter("data"), C(y));
  }
  function C(y) {
    return y === null || y === 124 || Ae(y) ? (t.exit("data"), T(y)) : (t.consume(y), y === 92 ? D : C);
  }
  function D(y) {
    return y === 92 || y === 124 ? (t.consume(y), C) : C(y);
  }
}
function yg(t, e) {
  let n = -1, r = !0, s = 0, i = [0, 0, 0, 0], o = [0, 0, 0, 0], l = !1, a = 0, c, u, d;
  const h = new mg();
  for (; ++n < t.length; ) {
    const p = t[n], m = p[1];
    p[0] === "enter" ? m.type === "tableHead" ? (l = !1, a !== 0 && (fo(h, e, a, c, u), u = void 0, a = 0), c = {
      type: "table",
      start: Object.assign({}, m.start),
      // Note: correct end is set later.
      end: Object.assign({}, m.end)
    }, h.add(n, 0, [["enter", c, e]])) : m.type === "tableRow" || m.type === "tableDelimiterRow" ? (r = !0, d = void 0, i = [0, 0, 0, 0], o = [0, n + 1, 0, 0], l && (l = !1, u = {
      type: "tableBody",
      start: Object.assign({}, m.start),
      // Note: correct end is set later.
      end: Object.assign({}, m.end)
    }, h.add(n, 0, [["enter", u, e]])), s = m.type === "tableDelimiterRow" ? 2 : u ? 3 : 1) : s && (m.type === "data" || m.type === "tableDelimiterMarker" || m.type === "tableDelimiterFiller") ? (r = !1, o[2] === 0 && (i[1] !== 0 && (o[0] = o[1], d = On(h, e, i, s, void 0, d), i = [0, 0, 0, 0]), o[2] = n)) : m.type === "tableCellDivider" && (r ? r = !1 : (i[1] !== 0 && (o[0] = o[1], d = On(h, e, i, s, void 0, d)), i = o, o = [i[1], n, 0, 0])) : m.type === "tableHead" ? (l = !0, a = n) : m.type === "tableRow" || m.type === "tableDelimiterRow" ? (a = n, i[1] !== 0 ? (o[0] = o[1], d = On(h, e, i, s, n, d)) : o[1] !== 0 && (d = On(h, e, o, s, n, d)), s = 0) : s && (m.type === "data" || m.type === "tableDelimiterMarker" || m.type === "tableDelimiterFiller") && (o[3] = n);
  }
  for (a !== 0 && fo(h, e, a, c, u), h.consume(e.events), n = -1; ++n < e.events.length; ) {
    const p = e.events[n];
    p[0] === "enter" && p[1].type === "table" && (p[1]._align = bg(e.events, n));
  }
  return t;
}
function On(t, e, n, r, s, i) {
  const o = r === 1 ? "tableHeader" : r === 2 ? "tableDelimiter" : "tableData", l = "tableContent";
  n[0] !== 0 && (i.end = Object.assign({}, Ot(e.events, n[0])), t.add(n[0], 0, [["exit", i, e]]));
  const a = Ot(e.events, n[1]);
  if (i = {
    type: o,
    start: Object.assign({}, a),
    // Note: correct end is set later.
    end: Object.assign({}, a)
  }, t.add(n[1], 0, [["enter", i, e]]), n[2] !== 0) {
    const c = Ot(e.events, n[2]), u = Ot(e.events, n[3]), d = {
      type: l,
      start: Object.assign({}, c),
      end: Object.assign({}, u)
    };
    if (t.add(n[2], 0, [["enter", d, e]]), r !== 2) {
      const h = e.events[n[2]], p = e.events[n[3]];
      if (h[1].end = Object.assign({}, p[1].end), h[1].type = "chunkText", h[1].contentType = "text", n[3] > n[2] + 1) {
        const m = n[2] + 1, g = n[3] - n[2] - 1;
        t.add(m, g, []);
      }
    }
    t.add(n[3] + 1, 0, [["exit", d, e]]);
  }
  return s !== void 0 && (i.end = Object.assign({}, Ot(e.events, s)), t.add(s, 0, [["exit", i, e]]), i = void 0), i;
}
function fo(t, e, n, r, s) {
  const i = [], o = Ot(e.events, n);
  s && (s.end = Object.assign({}, o), i.push(["exit", s, e])), r.end = Object.assign({}, o), i.push(["exit", r, e]), t.add(n + 1, 0, i);
}
function Ot(t, e) {
  const n = t[e], r = n[0] === "enter" ? "start" : "end";
  return n[1][r];
}
const vg = {
  name: "tasklistCheck",
  tokenize: Cg
};
function kg() {
  return {
    text: {
      91: vg
    }
  };
}
function Cg(t, e, n) {
  const r = this;
  return s;
  function s(a) {
    return (
      // Exit if there’s stuff before.
      r.previous !== null || // Exit if not in the first content that is the first child of a list
      // item.
      !r._gfmTasklistFirstContentOfListItem ? n(a) : (t.enter("taskListCheck"), t.enter("taskListCheckMarker"), t.consume(a), t.exit("taskListCheckMarker"), i)
    );
  }
  function i(a) {
    return Ae(a) ? (t.enter("taskListCheckValueUnchecked"), t.consume(a), t.exit("taskListCheckValueUnchecked"), o) : a === 88 || a === 120 ? (t.enter("taskListCheckValueChecked"), t.consume(a), t.exit("taskListCheckValueChecked"), o) : n(a);
  }
  function o(a) {
    return a === 93 ? (t.enter("taskListCheckMarker"), t.consume(a), t.exit("taskListCheckMarker"), t.exit("taskListCheck"), l) : n(a);
  }
  function l(a) {
    return tn(a) ? e(a) : Mt(a) ? t.check({
      tokenize: Sg
    }, e, n)(a) : n(a);
  }
}
function Sg(t, e, n) {
  return ut(t, r, "whitespace");
  function r(s) {
    return s === null ? n(s) : e(s);
  }
}
function Ng(t) {
  return Wd([
    Jm(),
    og(),
    pg(t),
    wg(),
    kg()
  ]);
}
const Ig = {};
function _g(t) {
  const e = (
    /** @type {Processor<Root>} */
    this
  ), n = t || Ig, r = e.data(), s = r.micromarkExtensions || (r.micromarkExtensions = []), i = r.fromMarkdownExtensions || (r.fromMarkdownExtensions = []), o = r.toMarkdownExtensions || (r.toMarkdownExtensions = []);
  s.push(Ng(n)), i.push(Gm()), o.push(Km(n));
}
const Ag = Kd().use(qd).use(_g).use(Yd).use(Jd), Tg = F(function({ content: e, className: n, format: r = "html", ...s }, i) {
  const o = we(
    () => Gd.sanitize(
      r === "markdown" ? String(Ag.processSync(e)) : e,
      {
        ADD_ATTR: ["target"],
        ALLOWED_ATTR: ["href", "target", "rel", "class"]
      }
    ),
    [r, e]
  ), l = /<[^>]*>/.test(o);
  return /* @__PURE__ */ f(
    "div",
    {
      ref: i,
      className: _(
        "rich-text-display-container",
        !l && "whitespace-pre-wrap",
        n
      ),
      dangerouslySetInnerHTML: {
        __html: o
      },
      ...s
    }
  );
}), Ea = Ge(Tg), Eg = F(function({ title: e, onClose: n, content: r, primaryAction: s, secondaryAction: i }, o) {
  return /* @__PURE__ */ w(
    "div",
    {
      ref: o,
      className: "flex w-full flex-col rounded-lg bg-gradient-to-l from-[#A1ADE51F] via-[#E519431F] to-[#E556191F] p-[1px]",
      "data-testid": "ai-banner",
      children: [
        /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between px-4 py-2", children: [
          /* @__PURE__ */ f(pt, { className: "font-medium", children: e }),
          n && /* @__PURE__ */ f(
            $,
            {
              variant: "ghost",
              icon: Sr,
              size: "sm",
              hideLabel: !0,
              onClick: n,
              label: "Close"
            }
          )
        ] }),
        /* @__PURE__ */ w("div", { className: "flex flex-col gap-[1px]", children: [
          /* @__PURE__ */ f(
            "div",
            {
              className: _(
                "bg-f1-background px-4 py-3",
                i || s ? "rounded-t-[13.25px]" : "rounded-[13.25px]"
              ),
              children: /* @__PURE__ */ f(Ea, { content: r })
            }
          ),
          (i || s) && /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-3 rounded-b-[13.25px] bg-f1-background px-4 py-3", children: [
            /* @__PURE__ */ f("div", { children: i && /* @__PURE__ */ f(
              $,
              {
                label: i.label,
                onClick: i.onClick,
                variant: "outline",
                icon: i.icon
              }
            ) }),
            /* @__PURE__ */ f("div", { children: s && /* @__PURE__ */ f(
              $,
              {
                label: s.label,
                onClick: s.onClick,
                variant: "outline",
                icon: s.icon
              }
            ) })
          ] })
        ] })
      ]
    }
  );
}), Dg = ({ compact: t }) => /* @__PURE__ */ w(
  "div",
  {
    className: "flex w-full flex-col rounded-lg bg-gradient-to-l from-[#A1ADE51F] via-[#E519431F] to-[#E556191F] p-[1px]",
    "aria-busy": "true",
    "aria-live": "polite",
    children: [
      /* @__PURE__ */ f("div", { className: "flex flex-row items-center justify-between px-4 py-2", children: /* @__PURE__ */ f(A, { className: "h-5 w-32 rounded-md" }) }),
      /* @__PURE__ */ w("div", { className: "flex flex-col gap-[1px]", children: [
        /* @__PURE__ */ f(
          "div",
          {
            className: _(
              "rounded-t-[13.25px] bg-f1-background px-4 py-3",
              t && "rounded-[13.25px]"
            ),
            children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-2", children: [
              /* @__PURE__ */ f(A, { className: "h-4 w-full rounded-md" }),
              /* @__PURE__ */ f(A, { className: "h-4 w-3/4 rounded-md" }),
              /* @__PURE__ */ f(A, { className: "h-4 w-1/2 rounded-md" })
            ] })
          }
        ),
        !t && /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-3 rounded-b-[13.25px] bg-f1-background px-4 py-3", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-24 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-28 rounded-md" })
        ] })
      ] })
    ]
  }
), Da = F(
  (t, e) => /* @__PURE__ */ f(Eg, { ref: e, ...t })
), Rg = ({ compact: t }) => /* @__PURE__ */ f(Dg, { compact: t });
Da.displayName = "F0AiBanner";
const Ra = Oe(
  Ge(Da),
  Rg
), Lg = (t) => {
  if (!t?.content) return "";
  try {
    return Tf(t.content, [
      Ol,
      Fl,
      zl,
      Ef,
      Pl,
      Bl,
      $l,
      Hl,
      jl,
      Vl,
      Yl,
      Xl,
      Jl
    ]);
  } catch {
    return "";
  }
}, Mg = (t, e) => we(() => {
  if (e?.selectedTitle || e?.selectedEmoji)
    return {
      title: e.selectedTitle || t.title,
      emoji: e.selectedEmoji
    };
  const n = t.buttons?.find(
    (r) => r.type === e?.selectedAction
  );
  return n ? { title: n.label, emoji: n.emoji } : { title: t.title };
}, [e, t]), Og = (t, e) => {
  const [n, r] = K(!1), s = ne(
    async (i) => {
      const o = t.buttons?.find(
        (a) => a.type === i
      ), l = {
        selectedAction: i,
        selectedTitle: o?.label || i,
        selectedEmoji: o?.emoji || "🤖",
        isEditable: o?.editable ?? !1
      };
      r(!0), e({ data: { ...l, content: null } });
      try {
        const a = await t.onClick(i);
        e({ data: { ...l, content: a } });
      } catch (a) {
        console.error("AIBlock error:", a), e({ data: { ...l, content: null } });
      } finally {
        r(!1);
      }
    },
    [t, e]
  );
  return { isLoading: n, handleClick: s };
}, Fg = (t, e, n) => {
  ue(() => {
    if (!n?.selectedAction || !t?.buttons) return;
    if (!n?.selectedTitle || !n?.selectedEmoji || n?.isEditable === void 0) {
      const s = t.buttons.find(
        (i) => i.type === n.selectedAction
      );
      s && e({
        data: {
          ...n,
          selectedTitle: s.label,
          selectedEmoji: s.emoji,
          isEditable: s.editable ?? !1
        }
      });
    }
  }, [n, t, e]);
}, zg = (t, e, n) => {
  ue(() => {
    t?.shouldExecute && t?.selectedAction && e && n && (n({ data: { ...t, shouldExecute: !1 } }), e(t.selectedAction));
  }, [e, n, t]);
}, Pg = (t, e, n, r) => {
  ue(() => {
    if (!r?.content || !r?.isEditable || !t || !n) return;
    const s = n();
    s !== void 0 && (e(), r.content && t.chain().focus().setTextSelection(s).insertContent(r.content).run());
  }, [r, t, n, e]);
}, Bg = ({
  config: t,
  isLoading: e,
  onButtonClick: n
}) => /* @__PURE__ */ w("div", { className: "flex flex-col gap-2", children: [
  t.title && /* @__PURE__ */ f("div", { className: "text-f1-foreground-secondary", children: t.title }),
  /* @__PURE__ */ f("div", { className: "relative flex flex-row flex-wrap items-center gap-2", children: t.buttons?.map((r, s) => /* @__PURE__ */ f(
    $,
    {
      onClick: () => n(r.type),
      variant: "outline",
      icon: r.icon,
      label: r.label,
      disabled: e
    },
    s
  )) })
] }), $g = ({ isEditable: t }) => t ? /* @__PURE__ */ w("div", { className: "flex flex-col gap-2", children: [
  /* @__PURE__ */ f(A, { className: "h-4 w-1/2 rounded-md" }),
  /* @__PURE__ */ f(A, { className: "h-4 w-full rounded-md" }),
  /* @__PURE__ */ f(A, { className: "h-4 w-3/4 rounded-md" }),
  /* @__PURE__ */ f(A, { className: "h-4 w-1/3 rounded-md" })
] }) : /* @__PURE__ */ f(Ra.Skeleton, { compact: !0 }), Hg = ({
  node: t,
  updateAttributes: e,
  deleteNode: n,
  extension: r,
  editor: s,
  getPos: i
}) => {
  const o = t.attrs.data, l = r.options.currentConfig || t.attrs.config, { title: a } = Mg(l, o), { isLoading: c, handleClick: u } = Og(
    l,
    e
  ), d = !!(o?.selectedAction && !o?.content), h = c || d, p = Lg(o);
  if (Pg(s, n, i, o), Fg(l, e, o), zg(o, u, e), !o || !l || !l.buttons?.length) return null;
  const m = !!o?.content, b = !!(o?.selectedTitle || o?.selectedAction) && m && !o?.isEditable;
  return /* @__PURE__ */ f(kn, { contentEditable: !1, children: /* @__PURE__ */ w("div", { className: "mb-3", children: [
    h ? /* @__PURE__ */ f($g, { isEditable: o?.isEditable }) : b ? /* @__PURE__ */ f(
      Ra,
      {
        title: a,
        content: p,
        onClose: () => n()
      }
    ) : /* @__PURE__ */ f(
      "div",
      {
        className: "editor-ai-block mb-3 flex w-full flex-col gap-4 rounded-lg",
        onClick: (v) => v.stopPropagation(),
        children: /* @__PURE__ */ f(
          Bg,
          {
            config: l,
            isLoading: h,
            onButtonClick: u
          }
        )
      }
    ),
    /* @__PURE__ */ f(ri, { style: { display: "none" } })
  ] }) });
}, jg = Fe.create({
  name: "aiBlock",
  group: "block",
  atom: !0,
  selectable: !0,
  draggable: !0,
  addOptions() {
    return {
      currentConfig: null
    };
  },
  addAttributes() {
    return {
      data: {
        default: null,
        parseHTML: (t) => {
          const e = t.getAttribute("data-ai-block");
          return e ? JSON.parse(e) : null;
        },
        renderHTML: (t) => t.data ? {
          "data-ai-block": JSON.stringify(t.data)
        } : {}
      },
      config: {
        default: null
      },
      isCollapsed: {
        default: !1
      }
    };
  },
  parseHTML() {
    return [
      {
        tag: "div[data-ai-block]"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t, node: e }) {
    const n = e.attrs.data, r = e.attrs.config;
    return !n || !r ? ["div"] : [
      "div",
      {
        ...t,
        class: "ai-block",
        "data-ai-block": JSON.stringify(n)
      },
      ["div", { class: "ai-block-content" }, `AI Block: ${r.title}`]
    ];
  },
  addNodeView() {
    return vn(Hg);
  },
  addCommands() {
    return {
      insertAIBlock: (t, e) => ({ commands: n }) => n.insertContent({
        type: this.name,
        attrs: { data: t, config: e }
      }),
      executeAIAction: (t, e) => ({ commands: n }) => {
        const r = e.buttons?.find((s) => s.type === t);
        return r ? n.insertContent([
          {
            type: this.name,
            attrs: {
              data: {
                content: null,
                selectedAction: t,
                selectedTitle: r.label,
                selectedEmoji: r.emoji,
                isEditable: r.editable ?? !1,
                shouldExecute: !0
              },
              config: e
            }
          },
          {
            type: "paragraph"
          }
        ]) : !1;
      }
    };
  }
}), Vg = jg, La = [
  "paragraph",
  "heading",
  "blockquote",
  "codeBlock",
  "bulletList",
  "orderedList",
  "listItem",
  "table",
  "details"
], ho = new Set(La), Ug = Dt.create({
  name: "blockId",
  addGlobalAttributes() {
    return [
      {
        // Apply to all block-level node types
        types: La,
        attributes: {
          id: {
            default: null,
            // Parse ID from HTML when loading content
            parseHTML: (t) => t.getAttribute("data-id"),
            // Render ID as data-id attribute in HTML
            renderHTML: (t) => t.id ? {
              "data-id": t.id
            } : {},
            // Don't keep the ID when splitting blocks - generate a new one
            keepOnSplit: !1
          }
        }
      }
    ];
  },
  addProseMirrorPlugins() {
    return [
      new Xe({
        key: new Ne("blockIdPlugin"),
        appendTransaction: (t, e, n) => {
          if (!t.some((l) => l.docChanged))
            return null;
          const s = n.tr;
          let i = !1;
          const o = [];
          return t.forEach((l) => {
            l.docChanged && l.steps.forEach((a) => {
              a.getMap().forEach((u, d, h, p) => {
                const m = l.mapping.map(h), g = l.mapping.map(p), b = Math.max(
                  0,
                  Math.min(m, n.doc.content.size)
                ), x = Math.max(
                  0,
                  Math.min(g, n.doc.content.size)
                );
                b < x && o.push({ from: b, to: x });
              });
            });
          }), o.length > 0 ? o.forEach(({ from: l, to: a }) => {
            l >= 0 && a <= n.doc.content.size && l < a && n.doc.nodesBetween(l, a, (c, u) => {
              if (ho.has(c.type.name) && !c.attrs.id) {
                const d = Ui(5);
                s.setNodeMarkup(u, void 0, {
                  ...c.attrs,
                  id: d
                }), i = !0;
              }
            });
          }) : n.doc.descendants((l, a) => {
            if (ho.has(l.type.name) && !l.attrs.id) {
              const c = Ui(5);
              s.setNodeMarkup(a, void 0, {
                ...l.attrs,
                id: c
              }), i = !0;
            }
            return !0;
          }), i ? s : null;
        }
      })
    ];
  }
});
var Wg = ({ key: t, editor: e, onPaste: n, onDrop: r, allowedMimeTypes: s }) => new Xe({
  key: t || new Ne("fileHandler"),
  props: {
    handleDrop(i, o) {
      var l;
      if (!r || !((l = o.dataTransfer) != null && l.files.length))
        return !1;
      const a = i.posAtCoords({
        left: o.clientX,
        top: o.clientY
      });
      let c = Array.from(o.dataTransfer.files);
      return s && (c = c.filter((u) => s.includes(u.type))), c.length === 0 ? !1 : (o.preventDefault(), o.stopPropagation(), r(e, c, a?.pos || 0), !0);
    },
    handlePaste(i, o) {
      var l;
      if (!n || !((l = o.clipboardData) != null && l.files.length))
        return !1;
      let a = Array.from(o.clipboardData.files);
      const c = o.clipboardData.getData("text/html");
      return s && (a = a.filter((u) => s.includes(u.type))), !(a.length === 0 || (o.preventDefault(), o.stopPropagation(), n(e, a, c), c.length > 0));
    }
  }
}), Gg = Dt.create({
  name: "fileHandler",
  addOptions() {
    return {
      onPaste: void 0,
      onDrop: void 0,
      allowedMimeTypes: void 0
    };
  },
  addProseMirrorPlugins() {
    return [
      Wg({
        key: new Ne(this.name),
        editor: this.editor,
        allowedMimeTypes: this.options.allowedMimeTypes,
        onDrop: this.options.onDrop,
        onPaste: this.options.onPaste
      })
    ];
  }
});
const Kg = /(?:^|\s)(!\[(.+|:?)]\((\S+)(?:(?:\s+)["'](\S+)["'])?\))$/, qg = Fe.create({
  name: "image",
  addOptions() {
    return {
      inline: !1,
      allowBase64: !1,
      HTMLAttributes: {}
    };
  },
  inline() {
    return this.options.inline;
  },
  group() {
    return this.options.inline ? "inline" : "block";
  },
  draggable: !0,
  addAttributes() {
    return {
      src: {
        default: null
      },
      alt: {
        default: null
      },
      title: {
        default: null
      }
    };
  },
  parseHTML() {
    return [
      {
        tag: this.options.allowBase64 ? "img[src]" : 'img[src]:not([src^="data:"])'
      }
    ];
  },
  renderHTML({ HTMLAttributes: t }) {
    return ["img", Me(this.options.HTMLAttributes, t)];
  },
  addCommands() {
    return {
      setImage: (t) => ({ commands: e }) => e.insertContent({
        type: this.name,
        attrs: t
      })
    };
  },
  addInputRules() {
    return [
      Df({
        find: Kg,
        type: this.type,
        getAttributes: (t) => {
          const [, , e, n, r] = t;
          return { src: n, alt: e, title: r };
        }
      })
    ];
  }
}), Yg = 50 * 1024 * 1024, ai = [
  "image/jpeg",
  "image/png",
  "image/gif",
  "image/webp"
], Jg = 10, kt = 100, Xg = ({
  node: t,
  deleteNode: e,
  selected: n,
  editor: r,
  updateAttributes: s
}) => {
  const { src: i, alt: o, title: l, uploading: a, width: c } = t.attrs, u = r.isEditable, d = ot(), [h, p] = K(!1), m = ne(
    (g) => {
      g.preventDefault(), g.stopPropagation();
      const b = g.clientX, x = c ?? kt, v = r.view.dom.clientWidth, S = (I) => {
        const T = (I.clientX - b) / v * 100, C = Math.min(
          kt,
          Math.max(Jg, x + T)
        );
        s({ width: Math.round(C) });
      }, E = () => {
        p(!1), document.removeEventListener("mousemove", S), document.removeEventListener("mouseup", E);
      };
      p(!0), document.addEventListener("mousemove", S), document.addEventListener("mouseup", E);
    },
    [r, c, s]
  );
  return /* @__PURE__ */ f(kn, { className: "mb-2", children: /* @__PURE__ */ w(
    "div",
    {
      style: { width: `${c ?? kt}%` },
      className: _(
        "image-resizable-wrapper group/image relative rounded-lg",
        n && "border-2 border-f1-border-selected-bold border-solid",
        h && "select-none"
      ),
      children: [
        /* @__PURE__ */ f(
          "img",
          {
            src: i,
            alt: o,
            title: l,
            draggable: !1,
            className: "block h-auto w-full rounded-md transition-all duration-150 ease-out"
          }
        ),
        a && /* @__PURE__ */ f("div", { className: "absolute inset-0 flex items-center justify-center rounded-lg bg-f1-background-secondary backdrop-blur-[2px] transition-opacity duration-200", children: /* @__PURE__ */ f(Cl, { size: "medium" }) }),
        u && !a && /* @__PURE__ */ f("div", { className: "absolute right-2 top-2 opacity-0 transition-opacity group-hover/image:opacity-100", children: /* @__PURE__ */ f(
          $,
          {
            onClick: e,
            label: d.actions.delete,
            icon: Nr,
            variant: "default",
            hideLabel: !0
          }
        ) }),
        u && !a && /* @__PURE__ */ f(
          "div",
          {
            className: _(
              "absolute right-2 top-1/2 -translate-y-1/2 flex cursor-col-resize items-center justify-center",
              "h-12 w-2 rounded-sm border border-solid border-f1-border bg-f1-foreground-inverse-secondary",
              "opacity-0 transition-opacity group-hover/image:opacity-100",
              h && "opacity-100"
            ),
            onMouseDown: m,
            role: "separator",
            "aria-orientation": "vertical",
            "aria-label": "Resize image",
            tabIndex: 0
          }
        )
      ]
    }
  ) });
}, Zg = qg.extend({
  addAttributes() {
    return {
      ...this.parent?.(),
      width: {
        default: kt,
        parseHTML: (t) => {
          const e = t.style.width;
          return e?.endsWith("%") && parseInt(e, 10) || kt;
        },
        renderHTML: (t) => !t.width || t.width === kt ? {} : { style: `width: ${t.width}%` }
      },
      // We need it to track the uploading state and visual feedback
      uploading: {
        default: !1,
        renderHTML: () => ({}),
        parseHTML: () => !1
      },
      "data-upload-id": {
        default: null,
        renderHTML: () => ({}),
        parseHTML: () => null
      }
    };
  },
  addNodeView() {
    return vn(Xg);
  },
  renderHTML({ HTMLAttributes: t }) {
    return ["img", Me(this.options.HTMLAttributes, t)];
  }
}).configure({
  inline: !1,
  allowBase64: !0
}), vs = async (t, e, n, r) => {
  const s = n.maxFileSize ?? Yg, { onError: i } = n;
  if (!ai.includes(e.type)) {
    i?.("invalid-type");
    return;
  }
  if (e.size > s) {
    i?.("file-too-large");
    return;
  }
  const o = URL.createObjectURL(e), l = `upload-${Date.now()}-${Math.random().toString(36).slice(2)}`, a = r ?? t.state.selection.anchor;
  t.chain().focus().insertContentAt(a, [
    {
      type: "image",
      attrs: {
        src: o,
        alt: e.name,
        uploading: !0,
        "data-upload-id": l
      }
    }
  ]).run();
  try {
    const { url: c } = await n.onUpload(e), { doc: u } = t.state;
    let d = null;
    u.descendants((h, p) => h.type.name === "image" && h.attrs["data-upload-id"] === l ? (d = p, !1) : !0), d !== null && t.chain().setNodeSelection(d).updateAttributes("image", {
      src: c,
      uploading: !1,
      "data-upload-id": null
    }).run();
  } catch {
    i?.("upload-failed");
    const { doc: c } = t.state;
    c.descendants((u, d) => u.type.name === "image" && u.attrs["data-upload-id"] === l ? (t.chain().setNodeSelection(d).deleteSelection().run(), !1) : !0);
  } finally {
    URL.revokeObjectURL(o);
  }
}, Qg = (t) => Gg.configure({
  allowedMimeTypes: ai,
  onDrop: (e, n, r) => {
    n.forEach((s) => {
      vs(e, s, t, r);
    });
  },
  onPaste: (e, n) => {
    n.forEach((r) => {
      vs(e, r, t);
    });
  }
}), Ma = (t, e, n) => {
  vs(t, e, n);
}, eb = ({
  node: t,
  deleteNode: e,
  updateAttributes: n
}) => {
  const r = ot(), [s, i] = K(t.attrs.isOpen ?? !1), o = t.attrs.data;
  if (!o) return null;
  const l = () => {
    const c = !s;
    i(c), n({ isOpen: c });
  }, a = [
    {
      label: r.actions.delete,
      icon: Nr,
      critical: !0,
      onClick: () => e()
    }
  ];
  return /* @__PURE__ */ w(kn, { contentEditable: !1, children: [
    /* @__PURE__ */ w(
      "div",
      {
        className: "editor-mood-tracker mb-4 flex w-full flex-col gap-4 rounded-md border border-solid border-f1-border-secondary p-3",
        onClick: (c) => c.stopPropagation(),
        children: [
          /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-2", children: [
            /* @__PURE__ */ f("div", { className: "flex flex-row items-center gap-2", children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-1", children: [
              /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-3", children: [
                /* @__PURE__ */ f("p", { className: "text-f1-text-primary text-lg font-semibold", children: o.title }),
                /* @__PURE__ */ f("div", { className: "flex flex-row items-center", children: o.days.map((c, u) => /* @__PURE__ */ f(
                  "div",
                  {
                    className: "-ml-1.5 flex items-center justify-center rounded-full bg-f1-background",
                    children: /* @__PURE__ */ f(
                      G,
                      {
                        icon: $i[c.mood],
                        size: "lg",
                        color: Bi[c.mood]
                      }
                    )
                  },
                  u
                )) })
              ] }),
              /* @__PURE__ */ f("p", { children: /* @__PURE__ */ f("span", { className: "text-f1-text-primary text-md font-normal", children: o.averageMoodComment }) })
            ] }) }),
            /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-1", children: [
              /* @__PURE__ */ f(
                $,
                {
                  onClick: l,
                  variant: "outline",
                  hideLabel: !0,
                  label: s ? r.actions.collapse : r.actions.expand,
                  icon: s ? Sl : Qs,
                  size: "sm"
                }
              ),
              /* @__PURE__ */ f(sn, { items: a, size: "sm" })
            ] })
          ] }),
          s && /* @__PURE__ */ f("div", { className: "text-f1-text-primary flex flex-col gap-2", children: o.days.map((c, u) => /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-2", children: [
            /* @__PURE__ */ f("div", { className: "flex items-center justify-center rounded-full", children: /* @__PURE__ */ f(
              G,
              {
                icon: $i[c.mood],
                size: "lg",
                color: Bi[c.mood]
              }
            ) }),
            /* @__PURE__ */ w("p", { className: "text-f1-text-primary text-md font-normal", children: [
              /* @__PURE__ */ w("span", { className: "font-semibold", children: [
                c.day,
                ":"
              ] }),
              " ",
              c.comment || "-"
            ] })
          ] }, u)) })
        ]
      }
    ),
    /* @__PURE__ */ f(ri, { style: { display: "none" } })
  ] });
}, tb = Fe.create({
  name: "moodTracker",
  group: "block",
  atom: !0,
  selectable: !0,
  draggable: !0,
  addOptions() {
    return {
      currentConfig: null
    };
  },
  addAttributes() {
    return {
      data: {
        default: null,
        parseHTML: (t) => {
          const e = t.getAttribute("data-mood-tracker");
          return e ? JSON.parse(e) : null;
        },
        renderHTML: (t) => t.data ? {
          "data-mood-tracker": JSON.stringify(t.data)
        } : {}
      },
      config: {
        default: null
      },
      isOpen: {
        default: !1
      }
    };
  },
  parseHTML() {
    return [
      {
        tag: "div[data-mood-tracker]"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t, node: e }) {
    const n = e.attrs.data;
    return n ? [
      "div",
      {
        ...t,
        class: "mood-tracker-block",
        "data-mood-tracker": JSON.stringify(n)
      },
      ["div", { class: "mood-tracker-content" }, `Mood Tracker: ${n.title}`]
    ] : ["div"];
  },
  addNodeView() {
    return vn(eb);
  },
  addCommands() {
    return {
      insertMoodTracker: (t) => ({ commands: e }) => e.insertContent({
        type: this.name,
        attrs: { data: t }
      })
    };
  }
}), nb = tb, rb = /(?:https?:\/\/)?(?:www\.)?(?:youtube\.com\/(?:watch\?v=|embed\/|shorts\/)|youtu\.be\/)([a-zA-Z0-9_-]+)/, sb = /(?:https?:\/\/)?(?:www\.)?(?:vimeo\.com\/|player\.vimeo\.com\/video\/)(\d+)/, ib = /^(?:https?:\/\/)?(?:www\.)?(?:youtube\.com\/(?:watch\?v=|embed\/|shorts\/)|youtu\.be\/)([a-zA-Z0-9_-]+)(?:\S*)$/gm, ob = /^(?:https?:\/\/)?(?:www\.)?(?:vimeo\.com\/|player\.vimeo\.com\/video\/)(\d+)(?:\S*)$/gm;
function Kn(t) {
  const e = t.match(rb);
  if (e)
    return {
      provider: "youtube",
      videoId: e[1],
      embedUrl: `https://www.youtube-nocookie.com/embed/${e[1]}`
    };
  const n = t.match(sb);
  return n ? {
    provider: "vimeo",
    videoId: n[1],
    embedUrl: `https://player.vimeo.com/video/${n[1]}`
  } : null;
}
const lb = ({
  node: t,
  deleteNode: e,
  selected: n,
  editor: r
}) => {
  const { src: s, provider: i } = t.attrs, o = r.isEditable, l = ot();
  return /* @__PURE__ */ f(kn, { className: "mb-2", children: /* @__PURE__ */ w(
    "div",
    {
      className: _(
        "video-embed-wrapper relative overflow-hidden rounded-lg",
        n && "border-2 border-solid border-f1-border-selected-bold"
      ),
      children: [
        /* @__PURE__ */ f("div", { className: "relative w-full", style: { paddingBottom: "56.25%" }, children: /* @__PURE__ */ f(
          "iframe",
          {
            src: s,
            title: `${i} video`,
            className: "absolute inset-0 h-full w-full border-0",
            allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture",
            allowFullScreen: !0
          }
        ) }),
        o && /* @__PURE__ */ f("div", { className: "dark absolute right-2 top-2", children: /* @__PURE__ */ f(
          $,
          {
            onClick: e,
            label: l.actions.delete,
            icon: Nr,
            variant: "outline",
            hideLabel: !0,
            size: "sm"
          }
        ) })
      ]
    }
  ) });
}, ab = Fe.create({
  name: "videoEmbed",
  group: "block",
  atom: !0,
  draggable: !0,
  addAttributes() {
    return {
      src: { default: null },
      provider: { default: null },
      videoId: { default: null }
    };
  },
  parseHTML() {
    return [
      {
        tag: "div[data-video-embed]"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t }) {
    return [
      "div",
      Me(t, { "data-video-embed": "" }),
      [
        "iframe",
        {
          src: t.src,
          frameborder: "0",
          allowfullscreen: "true",
          allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture",
          style: "width:100%;aspect-ratio:16/9;"
        }
      ]
    ];
  },
  addNodeView() {
    return vn(lb);
  },
  addCommands() {
    return {
      setVideoEmbed: ({ src: t }) => ({ commands: e }) => {
        const n = Kn(t);
        return n ? e.insertContent({
          type: this.name,
          attrs: {
            src: n.embedUrl,
            provider: n.provider,
            videoId: n.videoId
          }
        }) : !1;
      }
    };
  },
  addPasteRules() {
    return [
      ji({
        find: ib,
        type: this.type,
        getAttributes: (t) => {
          const e = Kn(t[0]);
          return e ? {
            src: e.embedUrl,
            provider: e.provider,
            videoId: e.videoId
          } : !1;
        }
      }),
      ji({
        find: ob,
        type: this.type,
        getAttributes: (t) => {
          const e = Kn(t[0]);
          return e ? {
            src: e.embedUrl,
            provider: e.provider,
            videoId: e.videoId
          } : !1;
        }
      })
    ];
  }
}), cb = ({
  aiBlockConfig: t,
  translations: e,
  imageUploadConfig: n
}) => ks({
  aiBlockConfig: t,
  translations: e,
  imageUploadConfig: n
}).flatMap((s) => s.commands), ks = ({
  aiBlockConfig: t,
  translations: e,
  imageUploadConfig: n
}) => [
  // Only include AI Block group if config is provided
  ...t?.buttons && t.buttons.length > 0 ? [
    {
      title: t.title,
      commands: [
        ...t.buttons.map((r) => ({
          title: r.label,
          command: (s) => {
            s.chain().focus().executeAIAction(r.type, t).run();
          },
          icon: r.icon
        }))
      ]
    }
  ] : [],
  {
    title: e.richTextEditor.groups.textStyles,
    commands: [
      {
        title: e.richTextEditor.heading1,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleHeading({ level: 1 }).run();
        },
        icon: Ru
      },
      {
        title: e.richTextEditor.heading2,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleHeading({ level: 2 }).run();
        },
        icon: Lu
      },
      {
        title: e.richTextEditor.heading3,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleHeading({ level: 3 }).run();
        },
        icon: Mu
      }
    ]
  },
  {
    title: e.richTextEditor.groups.lists,
    commands: [
      {
        title: e.richTextEditor.bulletList,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleBulletList().run();
        },
        icon: Ou
      },
      {
        title: e.richTextEditor.orderedList,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleOrderedList().run();
        },
        icon: Fu
      },
      {
        title: e.richTextEditor.taskList,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleTaskList().run();
        },
        icon: zu
      }
    ]
  },
  {
    title: e.richTextEditor.groups.blocks,
    commands: [
      ...n ? [
        {
          title: "Image",
          command: (r) => {
            const s = document.createElement("input");
            s.type = "file", s.accept = ai.join(","), s.onchange = () => {
              const i = s.files?.[0];
              i && Ma(r, i, n);
            }, s.click();
          },
          icon: Pu
        }
      ] : [],
      {
        title: e.richTextEditor.video,
        command: (r) => {
          const s = window.prompt(
            e.richTextEditor.videoUrlPrompt
          );
          s && (Kn(s) ? r.commands.setVideoEmbed({ src: s }) : window.alert(e.richTextEditor.videoUrlInvalid));
        },
        icon: Bu
      },
      {
        title: e.richTextEditor.details,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).setDetails().run();
        },
        icon: Qs
      },
      {
        title: e.richTextEditor.codeBlock,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleCodeBlock().run();
        },
        icon: $u
      },
      {
        title: e.richTextEditor.quote,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).toggleBlockquote().run();
        },
        icon: Hu
      },
      {
        title: e.richTextEditor.divider,
        command: (r) => {
          const { from: s, to: i } = r.state.selection;
          r.chain().focus().setTextSelection({ from: s, to: i }).setHorizontalRule().run();
        },
        icon: ju
      }
    ]
  }
], Oa = F(
  ({ items: t, groups: e, command: n }, r) => {
    const [s, i] = K(0), o = ge(null), l = ge(null), a = e || [{ title: "", commands: t }], c = a.flatMap((b) => b.commands), u = ne(
      (b) => {
        const x = c[b];
        x && n(x);
      },
      [c, n]
    ), d = ne((b) => {
      const x = o.current;
      if (!x) return;
      const v = x.getBoundingClientRect(), S = b.getBoundingClientRect();
      S.top < v.top ? x.scrollTop += S.top - v.top : S.bottom > v.bottom && (x.scrollTop += S.bottom - v.bottom);
    }, []), h = ne(() => {
      i((b) => b <= 0 ? c.length - 1 : b - 1);
    }, [c.length]), p = ne(() => {
      i((b) => b >= c.length - 1 ? 0 : b + 1);
    }, [c.length]), m = ne(() => {
      u(s);
    }, [s, u]);
    ue(() => {
      l.current && d(l.current);
    }, [s, d]), ue(() => {
      i(0);
    }, [t.length]), Gl(
      r,
      () => ({
        onKeyDown: ({ event: b }) => b.key === "ArrowUp" ? (b.preventDefault(), h(), !0) : b.key === "ArrowDown" ? (b.preventDefault(), p(), !0) : b.key === "Enter" ? (b.preventDefault(), m(), !0) : !1
      }),
      [h, p, m]
    );
    const g = (b, x) => {
      let v = 0;
      for (let S = 0; S < b; S++)
        v += a[S].commands.length;
      return v + x;
    };
    return /* @__PURE__ */ f(
      "div",
      {
        ref: o,
        className: "scrollbar-macos max-h-96 w-72 overflow-y-auto rounded-lg border border-solid border-f1-border-secondary bg-f1-background drop-shadow-md",
        children: a.map((b, x) => /* @__PURE__ */ w("div", { children: [
          /* @__PURE__ */ w("div", { className: "p-1", children: [
            e && b.title && /* @__PURE__ */ f("div", { className: "p-2", children: /* @__PURE__ */ f("p", { className: "text-sm font-medium tracking-wide text-f1-foreground-secondary", children: b.title }) }),
            b.commands.map((v, S) => {
              const E = g(x, S), I = E === s;
              return /* @__PURE__ */ w(
                "div",
                {
                  ref: I ? l : null,
                  className: _(
                    "flex w-full cursor-pointer items-center gap-2 rounded px-2 py-1.5 text-left text-sm hover:bg-f1-background-hover",
                    I && "bg-f1-background-secondary"
                  ),
                  onClick: () => {
                    i(E), u(E);
                  },
                  onMouseEnter: () => i(E),
                  children: [
                    v.emoji ? /* @__PURE__ */ f("span", { className: "text-base", children: v.emoji }) : v.icon ? /* @__PURE__ */ f(
                      G,
                      {
                        icon: v.icon,
                        className: "text-f1-foreground-secondary"
                      }
                    ) : null,
                    /* @__PURE__ */ f("p", { className: "flex-grow text-sm font-medium text-f1-foreground", children: v.title })
                  ]
                },
                `${x}-${S}`
              );
            })
          ] }),
          e && x < a.length - 1 && /* @__PURE__ */ f("div", { className: "py-1", children: /* @__PURE__ */ f("div", { className: "h-[1px] w-full bg-f1-border-secondary" }) })
        ] }, x))
      }
    );
  }
);
Oa.displayName = "CommandList";
const db = ({
  aiBlockConfig: t,
  translations: e,
  imageUploadConfig: n
}) => Dt.create({
  name: "slashCommand",
  addOptions() {
    return {
      suggestion: {
        char: "/",
        allowSpaces: !0,
        allowedPrefixes: [" ", `
`],
        startOfLine: !1,
        command: ({
          editor: r,
          range: s,
          props: i
        }) => {
          const { state: o } = r, { from: l, to: a } = o.selection, u = o.doc.resolve(l), h = u.parent.textBetween(
            Math.max(0, u.parentOffset - 50),
            // Look back up to 50 chars
            u.parentOffset,
            void 0,
            "￼"
          ).lastIndexOf("/");
          if (h !== -1) {
            const p = l - (u.parentOffset - h), m = a;
            r.chain().focus().deleteRange({ from: p, to: m }).run();
          } else
            r.chain().focus().deleteRange(s).run();
          i.command(r);
        }
      }
    };
  },
  addProseMirrorPlugins() {
    return [
      Rf({
        editor: this.editor,
        ...this.options.suggestion,
        items: ({ query: r }) => {
          const s = r.toLowerCase().trim(), i = cb({
            translations: e,
            aiBlockConfig: t,
            imageUploadConfig: n
          }).filter((o) => {
            const l = o.title.toLowerCase();
            return s ? l.includes(s) : !0;
          });
          return i.length > 0 ? i : [];
        },
        render: () => {
          let r = null, s = null, i = null;
          const o = () => {
            const a = window.getSelection();
            if (a && a.rangeCount > 0) {
              const c = a.getRangeAt(0), { startContainer: u, startOffset: d } = c;
              if (u.nodeType === Node.TEXT_NODE) {
                const p = (u.textContent || "").lastIndexOf("/", d);
                if (p !== -1) {
                  const m = document.createRange();
                  return m.setStart(u, p), m.setEnd(u, p + 1), m.getBoundingClientRect();
                }
              }
              return c.getBoundingClientRect();
            }
            return document.body.getBoundingClientRect();
          }, l = ({
            content: a,
            anchorRect: c
          }) => {
            const u = {
              position: "absolute",
              top: c.bottom + window.scrollY,
              left: c.left + window.scrollX,
              width: 0,
              height: 0
            };
            return /* @__PURE__ */ w(Vu, { open: !0, modal: !1, children: [
              /* @__PURE__ */ f("div", { style: u }),
              /* @__PURE__ */ f(Uu, { asChild: !0, children: /* @__PURE__ */ f("div", { style: u }) }),
              /* @__PURE__ */ f(
                Wu,
                {
                  side: "bottom",
                  align: "start",
                  sideOffset: 15,
                  collisionPadding: 10,
                  style: { zIndex: 9999 },
                  onOpenAutoFocus: (d) => {
                    d.preventDefault();
                  },
                  onCloseAutoFocus: (d) => {
                    d.preventDefault();
                  },
                  children: /* @__PURE__ */ f(
                    "div",
                    {
                      ref: (d) => {
                        d && a.parentNode !== d && d.appendChild(a);
                      }
                    }
                  )
                }
              )
            ] });
          };
          return {
            onStart: (a) => {
              if (a.items.length === 0) return;
              const c = ks({
                aiBlockConfig: t,
                translations: e,
                imageUploadConfig: n
              });
              let u = c;
              if (a.query && a.query.trim()) {
                const p = a.query.toLowerCase().trim();
                u = c.map((m) => ({
                  ...m,
                  commands: m.commands.filter((g) => g.title.toLowerCase().includes(p))
                })).filter((m) => m.commands.length > 0);
              }
              r = new Lf(Oa, {
                props: {
                  items: a.items,
                  groups: u,
                  command: a.command
                },
                editor: a.editor
              });
              const h = (() => {
                if (a.clientRect) {
                  const p = a.clientRect();
                  if (p && p.width && p.height) return p;
                }
                return o();
              })();
              i = document.createElement("div"), document.body.appendChild(i), s = Mf.createRoot(i), s.render(
                /* @__PURE__ */ f(
                  l,
                  {
                    content: r.element,
                    anchorRect: h,
                    editor: a.editor
                  }
                )
              );
            },
            onUpdate: (a) => {
              if (!r || !i || !s) return;
              const c = ks({
                aiBlockConfig: t,
                translations: e,
                imageUploadConfig: n
              });
              let u = c;
              if (a.query && a.query.trim()) {
                const d = a.query.toLowerCase().trim();
                u = c.map((h) => ({
                  ...h,
                  commands: h.commands.filter((p) => p.title.toLowerCase().includes(d))
                })).filter((h) => h.commands.length > 0);
              }
              if (r.updateProps({
                items: a.items,
                groups: u
              }), a.items.length === 0)
                i && (i.style.display = "none");
              else {
                i && (i.style.display = "");
                const h = (() => {
                  if (a.clientRect) {
                    const p = a.clientRect();
                    if (p && p.width && p.height) return p;
                  }
                  return o();
                })();
                s.render(
                  /* @__PURE__ */ f(
                    l,
                    {
                      content: r.element,
                      anchorRect: h,
                      editor: a.editor
                    }
                  )
                );
              }
            },
            onKeyDown: (a) => {
              if (a.event.key === "Escape")
                return s && i && (s.unmount(), i.remove()), !0;
              const c = r?.ref;
              return c && typeof c == "object" && "onKeyDown" in c && typeof c.onKeyDown == "function" && c.onKeyDown(a) || !1;
            },
            onExit() {
              s && i && (s.unmount(), i.remove()), r?.destroy();
            }
          };
        }
      })
    ];
  }
}), ub = ({
  node: t,
  deleteNode: e,
  updateAttributes: n
}) => {
  const r = ot(), [s, i] = K(t.attrs.isOpen ?? !1), o = t.attrs.data;
  if (!o) return null;
  const l = () => {
    const d = !s;
    i(d), n({ isOpen: d });
  }, a = [
    {
      label: r.actions.delete,
      icon: Nr,
      critical: !0,
      onClick: () => e()
    }
  ], c = (d) => o.users.find((h) => h.id === d), u = (d) => {
    try {
      const h = new Date(d);
      return Nl(h, "HH:mm");
    } catch (h) {
      return console.error(h), d;
    }
  };
  return /* @__PURE__ */ w(kn, { contentEditable: !1, children: [
    /* @__PURE__ */ w(
      "div",
      {
        className: "editor-transcript mb-4 flex w-full flex-col gap-4 rounded-md border border-solid border-f1-border-secondary p-3",
        onClick: (d) => d.stopPropagation(),
        children: [
          /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-2", children: [
            /* @__PURE__ */ f("div", { className: "flex flex-row items-center gap-2", children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-1", children: [
              /* @__PURE__ */ f("div", { className: "flex flex-row items-center gap-3", children: /* @__PURE__ */ f("p", { className: "text-f1-text-primary text-lg font-semibold", children: o.title }) }),
              /* @__PURE__ */ f("p", { className: "text-f1-text-secondary text-sm", children: o.messages.length })
            ] }) }),
            /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-1", children: [
              /* @__PURE__ */ f(
                $,
                {
                  onClick: l,
                  variant: "outline",
                  hideLabel: !0,
                  label: s ? r.actions.collapse : r.actions.expand,
                  icon: s ? Sl : Qs,
                  size: "sm"
                }
              ),
              /* @__PURE__ */ f(sn, { items: a, size: "sm" })
            ] })
          ] }),
          s && /* @__PURE__ */ f("div", { className: "scrollbar-macos text-f1-text-primary flex max-h-[500px] flex-col gap-4 overflow-y-auto", children: o.messages.map((d, h) => {
            const p = c(d.userId);
            return /* @__PURE__ */ w("div", { className: "flex flex-row gap-3", children: [
              p?.imageUrl && /* @__PURE__ */ f(
                mt,
                {
                  size: "xs",
                  src: p.imageUrl,
                  firstName: p.fullname,
                  lastName: ""
                }
              ),
              /* @__PURE__ */ w("div", { className: "flex flex-col", children: [
                /* @__PURE__ */ w("div", { className: "flex items-baseline gap-2", children: [
                  /* @__PURE__ */ f("span", { className: "text-f1-text-primary font-medium", children: p?.fullname || "Unknown User" }),
                  /* @__PURE__ */ f("span", { className: "text-f1-text-tertiary text-xs", children: u(d.dateTime) })
                ] }),
                /* @__PURE__ */ f("p", { className: "text-f1-text-secondary", children: d.text })
              ] })
            ] }, h);
          }) })
        ]
      }
    ),
    /* @__PURE__ */ f(ri, { style: { display: "none" } })
  ] });
}, fb = Fe.create({
  name: "transcript",
  group: "block",
  atom: !0,
  selectable: !0,
  draggable: !0,
  addOptions() {
    return {
      currentConfig: null
    };
  },
  addAttributes() {
    return {
      data: {
        default: null,
        parseHTML: (t) => {
          const e = t.getAttribute("data-transcript");
          return e ? JSON.parse(e) : null;
        },
        renderHTML: (t) => t.data ? {
          "data-transcript": JSON.stringify(t.data)
        } : {}
      },
      config: {
        default: null
      },
      isOpen: {
        default: !1
      }
    };
  },
  parseHTML() {
    return [
      {
        tag: "div[data-transcript]"
      }
    ];
  },
  renderHTML({ HTMLAttributes: t, node: e }) {
    const n = e.attrs.data;
    return n ? [
      "div",
      {
        ...t,
        class: "transcript-block",
        "data-transcript": JSON.stringify(n)
      },
      ["div", { class: "transcript-content" }, `Transcript: ${n.title}`]
    ] : ["div"];
  },
  addNodeView() {
    return vn(ub);
  },
  addCommands() {
    return {
      insertTranscript: (t) => ({ commands: e }) => e.insertContent({
        type: this.name,
        attrs: { data: t }
      })
    };
  }
}), hb = fb, Fa = Et({
  base: "flex w-full flex-col rounded-lg p-[1px]",
  variants: {
    variant: {
      ai: "bg-gradient-to-l from-[#A1ADE51F] via-[#E519431F] to-[#E556191F]",
      critical: "bg-f1-background-critical",
      positive: "bg-f1-background-positive",
      info: "bg-f1-background-info",
      warning: "bg-f1-background-warning"
    }
  },
  defaultVariants: {
    variant: "ai"
  }
}), po = {
  positive: Js,
  warning: Xd,
  info: hl
}, mo = {
  positive: "text-f1-foreground-positive font-medium",
  warning: "text-f1-foreground-warning font-medium",
  info: "text-f1-foreground-info font-medium"
}, pb = F(
  function({ title: e, onClose: n, children: r, actions: s = [], variant: i }, o) {
    if (s.length > 2)
      throw new Error(
        `F0Callout: Maximum of 2 actions allowed, but ${s.length} actions were provided.`
      );
    const l = s.length > 0;
    return /* @__PURE__ */ w(
      "div",
      {
        ref: o,
        className: Fa({ variant: i }),
        "data-testid": "sdm-callout",
        children: [
          /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between px-4 py-2", children: [
            /* @__PURE__ */ w(
              "div",
              {
                className: _(
                  "flex flex-row items-center gap-2",
                  mo[i]
                ),
                children: [
                  po[i] && /* @__PURE__ */ f(G, { icon: po[i], size: "sm", "aria-hidden": !0 }),
                  /* @__PURE__ */ f(
                    pt,
                    {
                      className: mo[i] || "font-medium",
                      children: e
                    }
                  )
                ]
              }
            ),
            n && /* @__PURE__ */ f(
              $,
              {
                variant: "ghost",
                icon: Sr,
                size: "sm",
                hideLabel: !0,
                onClick: n,
                label: "Close"
              }
            )
          ] }),
          /* @__PURE__ */ w("div", { className: "flex flex-col gap-[1px]", children: [
            /* @__PURE__ */ f(
              "div",
              {
                className: _(
                  "bg-f1-background px-4 py-3",
                  l ? "rounded-t-[13.25px]" : "rounded-[13.25px]"
                ),
                children: r
              }
            ),
            l && /* @__PURE__ */ f("div", { className: "flex flex-row items-center justify-between gap-3 rounded-b-[13.25px] bg-f1-background px-4 py-3", children: s.map((a, c) => /* @__PURE__ */ f("div", { children: /* @__PURE__ */ f(
              $,
              {
                label: a.label,
                onClick: a.onClick,
                variant: "outline",
                icon: a.icon
              }
            ) }, c)) })
          ] })
        ]
      }
    );
  }
), mb = ({
  compact: t,
  variant: e = "ai"
}) => /* @__PURE__ */ w(
  "div",
  {
    className: Fa({ variant: e }),
    "aria-busy": "true",
    "aria-live": "polite",
    children: [
      /* @__PURE__ */ f("div", { className: "flex flex-row items-center justify-between px-4 py-2", children: /* @__PURE__ */ f(A, { className: "h-5 w-32 rounded-md" }) }),
      /* @__PURE__ */ w("div", { className: "flex flex-col gap-[1px]", children: [
        /* @__PURE__ */ f(
          "div",
          {
            className: _(
              "rounded-t-[13.25px] bg-f1-background px-4 py-3",
              t && "rounded-[13.25px]"
            ),
            children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-2", children: [
              /* @__PURE__ */ f(A, { className: "h-4 w-full rounded-md" }),
              /* @__PURE__ */ f(A, { className: "h-4 w-3/4 rounded-md" }),
              /* @__PURE__ */ f(A, { className: "h-4 w-1/2 rounded-md" })
            ] })
          }
        ),
        !t && /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-3 rounded-b-[13.25px] bg-f1-background px-4 py-3", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-24 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-28 rounded-md" })
        ] })
      ] })
    ]
  }
), za = F(
  (t, e) => /* @__PURE__ */ f(pb, { ref: e, ...t })
), gb = ({ compact: t, variant: e }) => /* @__PURE__ */ f(mb, { compact: t, variant: e });
za.displayName = "F0Callout";
const qv = Oe(
  Ge(za),
  gb
);
function bb({
  title: t,
  isActive: e = !1,
  onClick: n
}) {
  return /* @__PURE__ */ w(
    "button",
    {
      type: "button",
      className: _(
        "flex w-full flex-row items-center gap-[6px] rounded-md p-[6px] pr-2 text-left transition-colors",
        e && "bg-f1-background-tertiary",
        n && "cursor-pointer hover:bg-f1-background-hover",
        vr("focus-visible:ring-inset")
      ),
      onClick: n,
      disabled: !n,
      "aria-label": `${t}${e ? " (selected)" : ""}`,
      "aria-pressed": n ? e : void 0,
      children: [
        /* @__PURE__ */ f(G, { icon: Gu, size: "md", color: "selected" }),
        /* @__PURE__ */ f(
          pt,
          {
            lines: 1,
            className: "text-[13px] font-semibold leading-5 text-f1-foreground-selected",
            children: t
          }
        )
      ]
    }
  );
}
function wb({
  author: t,
  timestamp: e,
  onClick: n,
  isActive: r
}) {
  const { locale: s } = Zd(), i = Ku(s), o = Nl(e, "PPPp", { locale: i }), l = `${t.firstName} ${t.lastName}`;
  return /* @__PURE__ */ w(
    "button",
    {
      type: "button",
      className: _(
        "flex w-full flex-col gap-[6px] rounded-md px-2 py-[6px] text-left transition-colors",
        r && "bg-f1-background-tertiary",
        n && "cursor-pointer hover:bg-f1-background-hover",
        vr("focus-visible:ring-inset")
      ),
      onClick: n,
      disabled: !n,
      "aria-label": `Version ${o} by ${l}${r ? " (selected)" : ""}`,
      "aria-pressed": n ? r : void 0,
      children: [
        /* @__PURE__ */ f(pt, { lines: 1, className: "font-medium text-f1-foreground", children: o }),
        /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-[6px]", children: [
          /* @__PURE__ */ f(
            mt,
            {
              firstName: t.firstName,
              lastName: t.lastName,
              src: t.src,
              size: "xs"
            }
          ),
          /* @__PURE__ */ f(
            pt,
            {
              lines: 1,
              className: "font-medium text-f1-foreground-secondary",
              children: l
            }
          )
        ] })
      ]
    }
  );
}
function xb({
  title: t,
  versions: e,
  currentVersion: n,
  activeVersionId: r
}) {
  return /* @__PURE__ */ w(
    "nav",
    {
      className: "flex h-full w-full min-w-[320px] max-w-[380px] flex-col overflow-hidden px-3 pb-3 pt-[6px]",
      "aria-label": t,
      children: [
        /* @__PURE__ */ f(
          pt,
          {
            tag: "h2",
            lines: 1,
            className: "flex-shrink-0 px-2 pb-3 pt-4 text-lg font-semibold text-f1-foreground",
            children: t
          }
        ),
        /* @__PURE__ */ f(yn, { className: "h-full flex-1", children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-1", children: [
          n && /* @__PURE__ */ f(
            bb,
            {
              title: n.title,
              onClick: n.onClick,
              isActive: r === "current"
            }
          ),
          e.map((s) => /* @__PURE__ */ f(
            wb,
            {
              author: s.author,
              timestamp: s.timestamp,
              onClick: s.onClick,
              isActive: r === s.id
            },
            s.id
          ))
        ] }) })
      ]
    }
  );
}
const Yv = Ge(
  Y("F0VersionHistory", xb)
), Pa = F(
  ({ height: t, itemCount: e, itemSize: n, className: r, renderer: s }, i) => {
    const o = se.useRef(null);
    se.useImperativeHandle(
      i,
      () => o.current,
      []
    );
    const l = qu({
      count: e,
      getScrollElement: () => o.current,
      estimateSize: typeof n == "number" ? () => n : n,
      overscan: 5
    });
    return /* @__PURE__ */ f(
      "div",
      {
        ref: o,
        className: _("scrollbar-macos w-full overflow-auto", r),
        style: {
          height: `${t}px`
        },
        children: /* @__PURE__ */ f(
          "div",
          {
            style: {
              height: `${l.getTotalSize()}px`,
              width: "100%",
              position: "relative"
            },
            children: l.getVirtualItems().map((a) => /* @__PURE__ */ f(
              "div",
              {
                style: {
                  position: "absolute",
                  top: 0,
                  left: 0,
                  width: "100%",
                  height: `${a.size}px`,
                  transform: `translateY(${a.start}px)`
                },
                children: a ? s(a) : /* @__PURE__ */ f(ae, {})
              },
              a.key
            ))
          }
        )
      }
    );
  }
);
Pa.displayName = "VirtualList";
const Cs = Y("VirtualList", Pa), Ba = ({
  text: t,
  search: e,
  searchKeys: n = [],
  semiBold: r = !1
}) => {
  if (!e)
    return /* @__PURE__ */ f("span", { className: _("line-clamp-1", r ? "font-semibold" : ""), children: t });
  if (t.toLowerCase().indexOf(e.toLowerCase()) === -1)
    if (n.find(
      (o) => o.toLowerCase().indexOf(e.toLowerCase().trim()) >= 0
    ))
      e = t.split(" ")[0];
    else
      return /* @__PURE__ */ f("span", { className: _("line-clamp-1", r ? "font-semibold" : ""), children: t });
  const s = new RegExp(`(${e})`, "gi"), i = t.split(s);
  return /* @__PURE__ */ f("span", { className: _("line-clamp-1", r ? "font-semibold" : ""), children: i.map(
    (o, l) => o.toLowerCase() === e.toLowerCase() ? /* @__PURE__ */ f(
      "span",
      {
        className: "truncate font-medium",
        style: {
          fontWeight: "bold"
        },
        children: o
      },
      l
    ) : o
  ) });
};
function _r(t, e) {
  const r = Array.from(
    document.querySelectorAll('[data-avatarname-navigator-element="true"]')
  ), s = r.indexOf(t);
  s >= 0 && s < r.length - 1 ? r[s + 1].focus() : r.length > 0 && e?.();
}
function Ar(t, e) {
  const r = Array.from(
    document.querySelectorAll('[data-avatarname-navigator-element="true"]')
  ), s = r.indexOf(t);
  s > 0 ? r[s - 1].focus() : r.length > 0 && e?.();
}
const yb = ({
  entity: t,
  selected: e,
  onSelect: n,
  onRemove: r,
  marginLeft: s,
  search: i,
  goToFirst: o,
  goToLast: l,
  singleSelector: a = !1,
  disabled: c = !1,
  hiddenAvatar: u = !1
}) => {
  const d = t.name.split(" "), h = d[0] || "", p = d.slice(1).join(" "), m = (b) => {
    b.preventDefault(), b.stopPropagation(), !c && (e ? r(t) : n(t));
  }, g = (b) => {
    if (b.key === "Enter" || b.key === " ") {
      if (b.preventDefault(), c) return;
      e ? e && r(t) : n(t);
    } else b.key === "ArrowDown" ? _r(b.currentTarget, o) : b.key === "ArrowUp" && Ar(b.currentTarget, l);
  };
  return /* @__PURE__ */ f("div", { className: "w-full pl-1 pr-1", children: /* @__PURE__ */ w(
    "label",
    {
      "aria-label": t.name,
      className: _(
        s,
        "flex flex-row flex-wrap items-center gap-2 rounded-[10px] border px-2 py-1.5 hover:cursor-pointer",
        "focus-within:outline focus-within:outline-1 focus-within:-outline-offset-1 focus-within:outline-f1-border-selected-bold hover:bg-f1-background-hover",
        e && a ? "bg-f1-background-selected-bold/10 transition-colors dark:bg-f1-background-selected-bold/20" : ""
      ),
      children: [
        !u && /* @__PURE__ */ f(
          mt,
          {
            src: t.avatar,
            firstName: h,
            lastName: p,
            size: "xs",
            deactivated: t.deactivated
          }
        ),
        /* @__PURE__ */ f("div", { className: "flex flex-1 flex-col", children: /* @__PURE__ */ f(
          "div",
          {
            className: _(
              "flex flex-1 flex-row items-center gap-2 break-all",
              t.deactivated ? "text-f1-foreground/[0.61]" : void 0
            ),
            children: /* @__PURE__ */ f(
              Ba,
              {
                text: t.name,
                search: i,
                searchKeys: t.searchKeys
              }
            )
          }
        ) }),
        /* @__PURE__ */ f(
          Il,
          {
            "data-avatarname-navigator-element": "true",
            checked: e,
            disabled: c,
            onClick: m,
            onKeyDown: g,
            className: _(
              "pointer-events-none ml-auto",
              a ? "opacity-0" : ""
            )
          }
        ),
        a && e && /* @__PURE__ */ f(
          G,
          {
            className: "text-f1-icon-selected",
            icon: Js,
            size: "md"
          }
        )
      ]
    }
  ) });
}, qn = ({
  groupView: t,
  expanded: e,
  search: n,
  entity: r,
  selected: s,
  partialSelected: i,
  onSelect: o,
  onRemove: l,
  onExpand: a,
  goToFirst: c,
  goToLast: u,
  isChild: d = !1,
  hideLine: h = !1,
  showGroupIcon: p = !1,
  singleSelector: m = !1,
  disabled: g = !1,
  hiddenAvatar: b = !1
}) => {
  const [x, v] = K(!1);
  if (!t)
    return /* @__PURE__ */ f(
      yb,
      {
        marginLeft: d ? "ml-6" : "ml-0",
        entity: r,
        search: n,
        selected: s,
        onSelect: o,
        onRemove: l,
        singleSelector: m,
        goToFirst: c,
        goToLast: u,
        disabled: g,
        hiddenAvatar: b
      }
    );
  const S = (k) => {
    if (k.key === " ")
      k.preventDefault(), a(!e);
    else if (k.key === "Enter" && m)
      a(!e);
    else if (k.key === "Enter") {
      if (g) return;
      !s || i ? o(r) : s && l(r);
    } else k.key === "ArrowDown" ? _r(k.currentTarget, c) : k.key === "ArrowUp" && Ar(k.currentTarget, u);
  }, E = () => {
    if (x)
      a(!e), v(!1);
    else {
      if (g || m) return;
      s ? l(r) : o(r);
    }
  };
  if (!r.subItems?.length) return null;
  const I = s || i;
  return /* @__PURE__ */ w(ae, { children: [
    /* @__PURE__ */ w("div", { className: "flex w-full flex-row flex-wrap items-center gap-0 rounded-md border pl-2.5 pr-1", children: [
      /* @__PURE__ */ f(
        $,
        {
          hideLabel: !0,
          icon: e ? Yu : Ju,
          onClick: () => a(!e),
          label: e ? "Collapse" : "Expand",
          size: "sm",
          variant: "ghost",
          type: "button"
        }
      ),
      /* @__PURE__ */ w(
        "label",
        {
          "aria-label": r.name,
          onPointerDown: () => {
            v(!0);
          },
          className: "flex flex-1 flex-row items-center gap-2 rounded-[10px] border px-2 py-1.5 focus-within:outline focus-within:outline-1 focus-within:-outline-offset-1 focus-within:outline-f1-border-selected-bold hover:cursor-pointer hover:bg-f1-background-hover",
          children: [
            p && /* @__PURE__ */ f(
              G,
              {
                icon: Xu,
                className: "rounded-xs bg-f1-foreground-secondary text-f1-foreground-inverse"
              }
            ),
            /* @__PURE__ */ w("div", { className: "flex flex-grow flex-row items-center gap-2 break-all", children: [
              /* @__PURE__ */ f(
                Ba,
                {
                  semiBold: !0,
                  text: r.name,
                  search: n,
                  searchKeys: r.searchKeys
                }
              ),
              /* @__PURE__ */ f(pl, { value: r.subItems?.length ?? 0 })
            ] }),
            /* @__PURE__ */ f(
              Il,
              {
                checked: I,
                disabled: g,
                onClick: E,
                onKeyDown: S,
                indeterminate: i,
                onPointerDown: (k) => {
                  k.stopPropagation(), v(!1);
                },
                "data-avatarname-navigator-element": "true",
                className: _("ml-auto", m ? "opacity-0" : "")
              }
            )
          ]
        }
      )
    ] }),
    !h && !e && /* @__PURE__ */ f("div", { className: "h-[1px] w-full bg-f1-border-secondary" })
  ] });
};
qn.displayName = "EntitySelectListItem";
const go = ({
  label: t,
  onCreate: e,
  goToFirst: n,
  goToLast: r
}) => /* @__PURE__ */ f("div", { className: "w-full pl-1 pr-1", onKeyDown: (i) => {
  i.key === "ArrowDown" || i.key === "Tab" ? _r(i.currentTarget, n) : i.key === "ArrowUp" && Ar(i.currentTarget, r);
}, children: /* @__PURE__ */ w(
  "label",
  {
    "aria-label": t,
    className: _(
      "flex flex-row flex-wrap items-center gap-1.5 rounded-[10px] border px-1.5 py-1.5 hover:cursor-pointer",
      "focus-within:outline focus-within:outline-1 focus-within:-outline-offset-1 focus-within:outline-f1-border-selected-bold hover:bg-f1-background-hover",
      "select-none"
    ),
    children: [
      /* @__PURE__ */ f(
        $,
        {
          hideLabel: !0,
          label: t,
          onClick: () => e(),
          icon: _l,
          size: "sm",
          variant: "outline"
        }
      ),
      /* @__PURE__ */ f("div", { className: "flex flex-1 flex-col", children: /* @__PURE__ */ f("div", { className: "flex flex-1 flex-row items-center gap-2 break-all", children: /* @__PURE__ */ f("span", { className: _("line-clamp-1"), children: t }) }) })
    ]
  }
) }), Qt = ({ primaryAction: t, secondaryActions: e }) => {
  if (!e || e.length === 0)
    return /* @__PURE__ */ f(
      $,
      {
        disabled: t.disabled,
        variant: t.variant,
        onClick: t.onClick,
        label: t.label,
        icon: t.icon,
        size: "sm"
      }
    );
  const n = [t, ...e ?? []], r = n.map((o) => ({
    label: o.label,
    value: o.label,
    icon: o.icon,
    critical: o.variant === "critical"
  })) || [], s = (o) => {
    const l = n.find((a) => a.label === o);
    l && !l.disabled && l.onClick?.();
  }, i = n.every((o) => o.disabled);
  return /* @__PURE__ */ f(
    Al,
    {
      items: r,
      value: t.label,
      disabled: i,
      onClick: s,
      variant: "outline",
      size: "sm"
    }
  );
}, vb = ({
  actions: t,
  selectAllLabel: e,
  clearLabel: n,
  disabled: r,
  allVisibleSelected: s,
  anyVisibleSelected: i,
  loading: o,
  singleSelector: l,
  onSelectAll: a,
  onClear: c
}) => {
  const u = !l && (e || n), d = t && t.length > 0;
  if (!(!o && (!l && u || d))) return null;
  let p, m, g = a ? {
    label: e || "",
    onClick: a,
    variant: "outline",
    disabled: r || s
  } : void 0, b = c ? {
    label: n || "",
    onClick: c,
    variant: "ghost",
    disabled: r || !i
  } : void 0;
  return g || (g = b, b = void 0), d && u ? (p = /* @__PURE__ */ f(
    Qt,
    {
      primaryAction: g,
      secondaryActions: b ? [b] : []
    }
  ), m = /* @__PURE__ */ f(
    Qt,
    {
      primaryAction: t[0],
      secondaryActions: t.slice(1)
    }
  )) : d ? p = /* @__PURE__ */ f(
    Qt,
    {
      primaryAction: t[0],
      secondaryActions: t.slice(1)
    }
  ) : u && (p = /* @__PURE__ */ f(Qt, { primaryAction: g, secondaryActions: [] }), b && (m = /* @__PURE__ */ f(Qt, { primaryAction: b, secondaryActions: [] }))), /* @__PURE__ */ f("footer", { className: "rounded-bl-xl border-0 border-r-[1px] border-t-[1px] border-solid border-f1-border-secondary bg-f1-background/30 backdrop-blur-2xl", children: /* @__PURE__ */ w("div", { className: "flex flex-1 justify-between p-2", children: [
    p,
    m
  ] }) });
}, kb = ({
  search: t,
  onSearch: e,
  searchPlaceholder: n,
  disabled: r = !1,
  goToFirst: s,
  goToLast: i
}) => /* @__PURE__ */ w("div", { className: "flex justify-between gap-1 rounded-[10px] border-[1px] border-solid border-f1-border px-2 py-[3px] transition-all focus-within:border-f1-border-hover hover:border-f1-border-hover", children: [
  /* @__PURE__ */ f(G, { icon: nh, size: "md" }),
  /* @__PURE__ */ f(
    "input",
    {
      disabled: r,
      onKeyDown: (l) => {
        l.key === "ArrowDown" ? (l.preventDefault(), l.stopPropagation(), _r(l.currentTarget, s)) : l.key === "ArrowUp" ? (l.preventDefault(), l.stopPropagation(), Ar(l.currentTarget, i)) : l.key === "Enter" && (l.preventDefault(), l.stopPropagation());
      },
      type: "text",
      className: "w-full border-none bg-transparent focus:outline-none",
      placeholder: n,
      value: t,
      onChange: (l) => e(l.target.value)
    }
  ),
  t && /* @__PURE__ */ f(
    G,
    {
      icon: Zu,
      size: "md",
      onClick: () => e(""),
      className: "cursor-pointer text-f1-icon-secondary"
    }
  )
] }), Yr = 384, Jr = 36, Cb = 37, bo = 1, wo = 200, xo = '[data-avatarname-navigator-element="true"]', Sb = ({
  groupView: t,
  entities: e,
  groups: n,
  selectedGroup: r,
  search: s,
  onSelect: i,
  onRemove: o,
  onSubItemRemove: l,
  onSubItemSelect: a,
  onClear: c,
  onSelectAll: u,
  onSearch: d,
  selectedEntities: h = [],
  onGroupChange: p,
  onToggleExpand: m,
  searchPlaceholder: g,
  selectAllLabel: b,
  clearLabel: x,
  notFoundTitle: v,
  notFoundSubtitle: S,
  className: E,
  actions: I,
  onCreate: k,
  onCreateLabel: T,
  singleSelector: C = !1,
  loading: D = !1,
  disabled: y = !1,
  hiddenAvatar: N = !1
}) => {
  const M = se.useRef(null), z = we(
    () => t ? e.reduce(
      (R, Q) => R + (Q.subItems?.length ?? 0),
      0
    ) : e.length,
    [e, t]
  ), P = ne(() => {
    setTimeout(() => {
      M.current?.scrollTo({ top: 0 });
    }, bo), setTimeout(() => {
      Array.from(
        document.querySelectorAll(xo)
      )[0]?.focus();
    }, wo);
  }, []), V = ne(() => {
    setTimeout(() => {
      M.current?.scrollTo({ top: M.current?.scrollHeight });
    }, bo), setTimeout(() => {
      const R = Array.from(
        document.querySelectorAll(xo)
      );
      R[R.length - 1]?.focus();
    }, wo);
  }, []), J = we(
    () => new Map(h.map((R) => [R.id, R])),
    [h]
  ), L = ne(
    (R) => {
      const Q = J.get(R.id);
      if (!t)
        return {
          selected: !!Q,
          partialSelected: !!Q
        };
      const ie = (R.subItems ?? []).filter(
        (fe) => Q?.subItems?.some(
          (et) => et.subId === fe.subId
        )
      ), ee = (R.subItems?.length ?? 0) === ie.length, ze = !ee && ie.length > 0;
      return { selected: ee, partialSelected: ze };
    },
    [t, J]
  ), de = ne(
    (R) => {
      if (R.index === 0 && k)
        return /* @__PURE__ */ f(
          go,
          {
            label: T ?? "",
            onCreate: () => k?.(s),
            goToFirst: P,
            goToLast: V
          }
        );
      const Q = k ? R.index - 1 : R.index, ie = e[Q], { selected: ee, partialSelected: ze } = L(ie);
      return /* @__PURE__ */ f(
        qn,
        {
          expanded: ie.expanded ?? !1,
          onExpand: () => m(ie, !0),
          search: s,
          groupView: t,
          entity: ie,
          onSelect: i,
          onRemove: o,
          selected: ee,
          partialSelected: ze,
          showGroupIcon: Nb(n, r),
          singleSelector: C,
          goToFirst: P,
          goToLast: V,
          disabled: y,
          hiddenAvatar: N
        },
        ie.id
      );
    },
    [
      k,
      T,
      y,
      e,
      L,
      P,
      V,
      t,
      n,
      N,
      o,
      i,
      m,
      s,
      r,
      C
    ]
  ), ye = we(() => t ? e.flatMap((R) => {
    const Q = Fn(
      h ?? [],
      R.id
    );
    return [
      {
        parent: null,
        subItem: {
          subId: R.id,
          subName: R.name,
          subAvatar: R.avatar,
          expanded: R.expanded ?? Q?.expanded ?? !1,
          subItems: R.subItems,
          subSearchKeys: R.searchKeys
        }
      },
      ...(R.subItems ?? []).map((ie) => ({
        parent: {
          ...R,
          expanded: R.expanded ?? Q?.expanded ?? !1
        },
        subItem: ie
      }))
    ];
  }).filter(
    (R) => (!R.parent || R.parent.expanded) && (!!R.parent || !!R.subItem.subItems && R.subItem.subItems.length > 0)
  ) : e.map((R) => ({
    parent: null,
    subItem: {
      subId: R.id,
      subName: R.name,
      subAvatar: R.avatar,
      subSearchKeys: R.searchKeys
    }
  })), [t, e, h]), H = ne(
    (R) => {
      if (R.index === 0 && k)
        return /* @__PURE__ */ f(
          go,
          {
            label: T ?? "",
            onCreate: () => k?.(s),
            goToFirst: P,
            goToLast: V
          }
        );
      const Q = k ? R.index - 1 : R.index, ie = ye[Q].parent, ee = ye[Q].subItem;
      if (!ie) {
        const fe = {
          id: ee.subId,
          name: ee.subName,
          avatar: ee.subAvatar,
          subItems: ee.subItems,
          expanded: ee.expanded,
          searchKeys: ee.subSearchKeys
        }, et = Fn(
          h,
          fe.id
        ), dt = (fe?.subItems ?? []).filter(
          (Zt) => et?.subItems?.some(
            (Pd) => Pd.subId === Zt.subId
          )
        ), Tn = (fe.subItems?.length ?? 0) === dt.length, zd = !Tn && dt.length > 0;
        return /* @__PURE__ */ f(
          qn,
          {
            groupView: !0,
            expanded: fe.expanded ?? !1,
            onExpand: (Zt) => m(fe, Zt),
            search: s,
            entity: fe,
            onSelect: i,
            onRemove: o,
            selected: Tn,
            partialSelected: zd,
            showGroupIcon: n.find((Zt) => Zt.value === r)?.groupType === "team",
            singleSelector: C,
            goToFirst: P,
            goToLast: V,
            hideLine: Q === ye.length - 1,
            disabled: y,
            hiddenAvatar: N
          }
        );
      }
      let ze = !!Fn(h, ee.subId);
      if (!ze) {
        const fe = Fn(
          h,
          ie.id
        );
        ze = !!(ie?.subItems ?? []).filter(
          (dt) => fe?.subItems?.some(
            (Tn) => Tn.subId === dt.subId
          )
        ).find((dt) => dt.subId === ee.subId);
      }
      return /* @__PURE__ */ f(
        qn,
        {
          expanded: !1,
          onExpand: () => null,
          search: s,
          groupView: !1,
          entity: {
            id: ee.subId,
            name: ee.subName,
            avatar: ee.subAvatar,
            searchKeys: ee.subSearchKeys
          },
          onSelect: () => {
            a(ie, ee);
          },
          onRemove: () => l(ie, ee),
          selected: !!ze,
          partialSelected: !1,
          singleSelector: C,
          goToFirst: P,
          goToLast: V,
          isChild: !0,
          hiddenAvatar: N
        }
      );
    },
    [
      ye,
      h,
      s,
      C,
      P,
      V,
      i,
      o,
      n,
      y,
      m,
      r,
      a,
      l,
      N,
      k,
      T
    ]
  ), [An, Rd] = we(() => {
    if (!e.length)
      return [!1, !1];
    let R = 0, Q = 0;
    if (!t)
      R = e.length, Q = e.reduce(
        (ze, { id: fe }) => ze + (J.has(fe) ? 1 : 0),
        0
      );
    else {
      const ze = new Set(
        [...J.values()].flatMap(
          (fe) => fe.subItems?.map((et) => et.subId) ?? []
        )
      );
      e.forEach((fe) => {
        const et = fe.subItems ?? [];
        R += et.length, Q += et.filter(
          (dt) => ze.has(dt.subId)
        ).length;
      });
    }
    const ie = R > 0 && Q === R, ee = Q > 0;
    return [ie, ee];
  }, [e, J, t]), Ld = ye.length, Md = !C && (b || x), Od = I && I.length > 0, Fd = !D && (!C && Md || Od);
  return /* @__PURE__ */ w(
    "div",
    {
      className: _(
        "flex w-full flex-col rounded-l-xl border-0",
        C || D ? "rounded-r-xl" : "",
        E
      ),
      children: [
        /* @__PURE__ */ w(
          "header",
          {
            className: _(
              "flex h-[48px] justify-between gap-2 rounded-tl-xl border-0 border-b-[1px] border-r-[1px] border-solid border-f1-border-secondary bg-f1-background/30 p-2 backdrop-blur-2xl",
              C || D ? "rounded-t-xl border-r-0" : ""
            ),
            children: [
              /* @__PURE__ */ f("div", { className: "flex-1", children: /* @__PURE__ */ f(
                kb,
                {
                  search: s,
                  onSearch: d,
                  searchPlaceholder: g,
                  goToFirst: P,
                  goToLast: V
                }
              ) }),
              n && n.length > 1 && /* @__PURE__ */ f("div", { className: "flex-1", children: /* @__PURE__ */ f(
                fs,
                {
                  label: "Group",
                  hideLabel: !0,
                  disabled: D,
                  onChange: p,
                  options: n,
                  value: r,
                  className: _(
                    "h-8 rounded-[10px] bg-transparent py-[5px]",
                    r === "all" ? "text-f1-foreground-secondary" : ""
                  )
                }
              ) })
            ]
          }
        ),
        /* @__PURE__ */ w(
          "section",
          {
            className: _(
              "flex-grow-1 flex h-96 flex-col justify-start gap-1 border-0 border-r-[1px] border-solid border-f1-border-secondary bg-f1-background",
              Fd ? "" : "rounded-b-xl border-r-0"
            ),
            children: [
              D && /* @__PURE__ */ f("div", { className: "flex h-full w-full flex-row items-center justify-center", children: /* @__PURE__ */ f(Cl, {}) }),
              !D && !z && /* @__PURE__ */ w(
                "div",
                {
                  className: "absolute flex w-full flex-col items-center justify-center gap-0.5 p-5",
                  style: {
                    height: Yr
                  },
                  children: [
                    /* @__PURE__ */ f("span", { className: "text-lg font-medium", children: v }),
                    /* @__PURE__ */ f("span", { className: "text-center text-f1-foreground-secondary", children: S })
                  ]
                }
              ),
              !D && (!!z || k) && /* @__PURE__ */ f("div", { className: "h-full", children: t ? /* @__PURE__ */ f(
                Cs,
                {
                  height: Yr,
                  itemCount: Ld + (k ? 1 : 0),
                  itemSize: (R) => {
                    if (R === 0 && k) return Jr;
                    const Q = k ? R - 1 : R;
                    return ye[Q]?.parent === null ? Cb : Jr;
                  },
                  renderer: H,
                  ref: M
                }
              ) : /* @__PURE__ */ f(
                Cs,
                {
                  height: Yr,
                  itemCount: e.length + (k ? 1 : 0),
                  itemSize: Jr,
                  renderer: de,
                  ref: M
                }
              ) })
            ]
          }
        ),
        /* @__PURE__ */ f(
          vb,
          {
            onSelectAll: u,
            onClear: c,
            singleSelector: C,
            totalFilteredEntities: z,
            allVisibleSelected: An,
            anyVisibleSelected: Rd,
            selectAllLabel: b,
            clearLabel: x,
            disabled: y,
            actions: I
          }
        )
      ]
    }
  );
}, Fn = (t, e) => t.find((n) => n.id === e), Nb = (t, e) => t.find((n) => n.value === e)?.groupType === "team", Ib = ({
  entity: t,
  onRemove: e,
  disabled: n = !1,
  deactivated: r = !1,
  hiddenAvatar: s = !1
}) => /* @__PURE__ */ f("div", { className: "pr-2 pt-1.5", children: /* @__PURE__ */ f(
  Qd,
  {
    className: _(
      "max-w-54 w-fit gap-1 text-ellipsis break-all border-[1px] border-solid border-f1-border-secondary py-[1px] pl-[1px]",
      "rounded-full",
      s ? "pl-2" : "pl-0"
    ),
    left: !s && /* @__PURE__ */ f(
      eu,
      {
        src: t.subAvatar,
        name: t.subName,
        size: "xs",
        type: "rounded",
        icon: r ? { icon: tu, color: "secondary" } : void 0
      }
    ),
    right: !n && /* @__PURE__ */ f(
      G,
      {
        icon: Sr,
        size: "sm",
        className: "cursor-pointer text-f1-icon-secondary",
        onClick: () => e?.(t)
      }
    ),
    text: t.subName,
    deactivated: r
  }
) }), _b = ({
  groupView: t,
  onSubItemRemove: e,
  onRemove: n,
  selectedEntities: r,
  selectedLabel: s,
  disabled: i = !1,
  hiddenAvatar: o = !1
}) => {
  const l = we(() => {
    const c = t ? r.flatMap(
      (d) => (d.subItems ?? []).map((h) => ({
        parent: d,
        subItem: h
      }))
    ) : r.map((d) => ({
      parent: null,
      subItem: {
        subId: d.id,
        subName: d.name,
        subAvatar: d.avatar,
        subDeactivated: d.deactivated
      }
    })), u = /* @__PURE__ */ new Set();
    return c.filter((d) => {
      const h = d.subItem.subId;
      return u.has(h) ? !1 : (u.add(h), !0);
    });
  }, [t, r]), a = l.length;
  return /* @__PURE__ */ w("div", { className: "w-full flex-col rounded-r-xl", children: [
    /* @__PURE__ */ f("div", { className: "flex h-[48px] rounded-tr-xl border-0 border-b-[1px] border-solid border-f1-border-secondary bg-f1-background/30 p-3 backdrop-blur-2xl", children: s && /* @__PURE__ */ w("span", { className: "my-auto text-f1-foreground-secondary", children: [
      a,
      " ",
      s
    ] }) }),
    /* @__PURE__ */ f("div", { className: "flex flex-col gap-3 rounded-br-xl bg-f1-background pb-0 pl-2", children: /* @__PURE__ */ f(
      Cs,
      {
        height: 425,
        itemCount: a,
        itemSize: 30,
        className: "overflow-x-hidden",
        renderer: (c) => {
          const u = l[c.index];
          return u ? /* @__PURE__ */ f(
            Ib,
            {
              deactivated: u.subItem.subDeactivated,
              entity: u.subItem,
              disabled: i,
              hiddenAvatar: o,
              onRemove: () => u.parent ? e?.(u.parent, u.subItem) : n({
                id: u.subItem.subId,
                name: u.subItem.subName,
                avatar: u.subItem.subAvatar
              })
            }
          ) : /* @__PURE__ */ f(ae, {});
        }
      }
    ) })
  ] });
}, Ab = 500, yo = 520, vo = 210, ko = ({
  groupView: t,
  onRemove: e,
  onSubItemRemove: n,
  selectedEntities: r,
  selectedLabel: s,
  width: i,
  singleSelector: o = !1,
  loading: l = !1,
  hiddenAvatar: a = !1,
  actions: c,
  onCreate: u,
  onCreateLabel: d,
  ...h
}) => {
  const p = (i ?? yo) < Ab, m = !l && !o && !p, g = i ?? yo, b = m ? g - vo : g;
  return /* @__PURE__ */ w(
    "div",
    {
      className: "relative overflow-hidden",
      style: {
        height: o && (!c || c.length === 0) ? "435px" : "473px",
        width: g
      },
      children: [
        /* @__PURE__ */ f(
          "div",
          {
            className: "absolute right-0 top-0",
            style: {
              width: vo + "px"
            },
            children: /* @__PURE__ */ f(
              _b,
              {
                groupView: t,
                onRemove: e,
                onSubItemRemove: n,
                selectedEntities: r ?? [],
                selectedLabel: s,
                disabled: h.disabled,
                hiddenAvatar: a
              }
            )
          }
        ),
        /* @__PURE__ */ f(
          "div",
          {
            className: "absolute left-0",
            style: { width: b + 1 + "px" },
            children: /* @__PURE__ */ f(
              Sb,
              {
                ...h,
                groupView: t,
                onRemove: e,
                onSubItemRemove: n,
                selectedEntities: r,
                singleSelector: o,
                loading: l,
                disabled: h.disabled,
                hiddenAvatar: a,
                actions: c,
                onCreate: u,
                onCreateLabel: d
              }
            )
          }
        )
      ]
    }
  );
}, Tb = ({
  placeholder: t,
  selected: e,
  selectedEntities: n,
  disabled: r = !1,
  hiddenAvatar: s = !1,
  label: i,
  labelIcon: o,
  icon: l,
  error: a,
  status: c,
  hint: u,
  onClickContent: d,
  hideLabel: h = !1,
  maxLength: p,
  loading: m = !1,
  required: g = !1,
  readonly: b = !1,
  append: x,
  size: v = "sm",
  open: S
}) => {
  const E = we(
    () => n.some(
      (C) => C.subItems && C.subItems.length > 0
    ),
    [n]
  ), I = we(() => E ? n.flatMap(
    (C) => (C.subItems ?? []).map((D) => ({
      parent: C,
      subItem: D
    }))
  ) : n.map((C) => ({
    parent: null,
    subItem: {
      subId: C.id,
      subName: C.name,
      subAvatar: C.avatar,
      subDeactivated: C.deactivated
    }
  })), [E, n]), k = I.length === 0 ? void 0 : I.length === 1 ? I[0].subItem.subName : I.length + " " + e, T = I.length === 1 ? I[0].subItem.subName : void 0;
  return /* @__PURE__ */ f(
    Qu,
    {
      onClickContent: d,
      role: "combobox",
      label: i,
      labelIcon: o,
      "aria-expanded": !1,
      "aria-controls": "listbox",
      icon: l && !k ? l : void 0,
      error: a,
      status: c,
      hint: u,
      hideLabel: h,
      maxLength: p,
      clearable: !1,
      value: k,
      disabled: r,
      loading: m,
      required: g,
      readonly: b,
      size: v,
      avatar: s || !T ? void 0 : {
        type: "person",
        firstName: T,
        lastName: "",
        src: I[0].subItem.subAvatar,
        deactivated: I[0].subItem.subDeactivated
      },
      append: x ?? /* @__PURE__ */ f(ae, { children: /* @__PURE__ */ f(ef, { open: S, disabled: r, size: v }) }),
      children: /* @__PURE__ */ f(
        "span",
        {
          role: "button",
          className: _(
            "my-auto flex items-center pr-1",
            t && "text-f1-foreground-secondary",
            k && "text-f1-foreground",
            I.length === 1 && !s || l && !k ? "pl-8" : "pl-2"
          ),
          children: /* @__PURE__ */ f(
            pt,
            {
              tag: "span",
              className: I.length === 1 && I[0].subItem.subDeactivated ? "text-f1-foreground-disabled" : void 0,
              children: I.length === 0 ? t ?? "" : I.length === 1 ? I[0].subItem.subName : `${I.length} ${e}`
            }
          )
        }
      )
    }
  );
}, Jv = (t) => {
  const [e, n] = K(
    (t.alwaysOpen || t.defaultOpen) ?? !1
  ), r = (k) => {
    n(k), t.onOpenChange?.(k);
  };
  ue(() => {
    t.defaultOpen && e && t.onOpenChange?.(!0);
  }, [t.defaultOpen]);
  const [s, i] = K(t.entities), [o, l] = K(""), [a, c] = nu("", 300), u = we(
    () => t.entities.some(
      (k) => k.subItems && k.subItems.length > 0
    ),
    [t.entities]
  );
  function d(k) {
    if (t.singleSelector) {
      t.onSelect(k), n(!1);
      return;
    }
    const T = t.selectedEntities ?? [], C = s.find((z) => z.id === k.id);
    if (!C)
      return;
    const D = new Set(
      (C.subItems ?? []).map((z) => z.subId)
    ), y = /* @__PURE__ */ new Set([C.id]);
    s.forEach((z) => {
      z.id !== C.id && (z.subItems ?? []).some(
        (V) => D.has(V.subId)
      ) && y.add(z.id);
    });
    const N = [...T];
    function M(z) {
      const P = N.findIndex((V) => V.id === z.id);
      P >= 0 ? N[P] = z : N.push(z);
    }
    y.forEach((z) => {
      const P = s.find((L) => L.id === z);
      if (!P) return;
      const V = P.subItems?.filter(
        (L) => D.has(L.subId)
      ) ?? [], J = N.findIndex((L) => L.id === z);
      if (J >= 0) {
        const L = N[J].subItems ?? [], de = new Set(L.map((H) => H.subId)), ye = [
          ...L,
          ...V.filter((H) => !de.has(H.subId))
        ];
        M({
          ...P,
          subItems: ye
        });
      } else
        M({
          ...P,
          subItems: V
        });
    }), t.onSelect(N);
  }
  function h(k, T) {
    if (t.singleSelector)
      t.onSelect({ ...k, subItems: [{ ...T }] }), n(!1);
    else {
      const C = t.selectedEntities ?? [], D = new Set(C.map((M) => M.id)), y = new Map(
        C.map((M) => [M.id, M.subItems ?? []])
      );
      D.add(k.id), t.entities.forEach((M) => {
        M.subItems?.some(
          (P) => P.subId === T.subId
        ) && D.add(M.id);
      });
      const N = [];
      t.entities.forEach((M) => {
        if (D.has(M.id)) {
          let P = [...y.get(M.id) ?? []];
          M.subItems?.some(
            (L) => L.subId === T.subId
          ) && (P.some(
            (de) => de.subId === T.subId
          ) || P.push(T));
          const J = new Set(
            (M.subItems ?? []).map((L) => L.subId)
          );
          P = P.filter(
            (L) => J.has(L.subId)
          ), N.push({
            ...M,
            subItems: P
          });
        }
      }), t.onSelect(N);
    }
  }
  function p(k) {
    if (t.singleSelector) {
      t.onSelect(null);
      return;
    }
    let T = [];
    const C = t.selectedEntities ?? [];
    if (u) {
      const D = s.find(
        (N) => N.id === k.id
      );
      if (!D)
        return;
      const y = new Set(
        (D.subItems ?? []).map((N) => N.subId)
      );
      for (const N of C) {
        const M = (N.subItems ?? []).filter(
          (z) => !y.has(z.subId)
        );
        M.length > 0 && T.push({
          ...N,
          subItems: M
        });
      }
    } else
      T = (C ?? []).filter((D) => D.id !== k.id);
    t.onSelect(T);
  }
  function m(k, T) {
    if (t.singleSelector) {
      t.onSelect(null);
      return;
    }
    const C = t.selectedEntities ?? [], D = T.subId, y = [];
    for (const N of C) {
      const M = N.subItems?.filter((z) => z.subId !== D) ?? [];
      M.length > 0 && y.push({
        ...N,
        subItems: M
      });
    }
    t.onSelect(y);
  }
  function g() {
    if (t.singleSelector) {
      t.onSelect(null);
      return;
    }
    const k = t.selectedEntities ?? [];
    let T = [];
    if (u) {
      const C = new Set(
        s.flatMap(
          (D) => (D.subItems ?? []).map((y) => y.subId)
        )
      );
      for (const D of k) {
        const y = (D.subItems ?? []).filter(
          (N) => !C.has(N.subId)
        );
        y.length > 0 && T.push({
          ...D,
          subItems: y
        });
      }
    } else {
      const C = new Set(
        s.map((D) => D.id)
      );
      T = (k ?? []).filter((D) => !C.has(D.id));
    }
    t.onSelect(T);
  }
  function b() {
    const k = [...t.selectedEntities ?? []];
    s.forEach((T) => {
      const C = k.find((D) => D.id === T.id);
      if (!C)
        k.push({
          ...T,
          subItems: T.subItems || []
        });
      else {
        const D = Array.from(
          /* @__PURE__ */ new Set([
            ...C.subItems ?? [],
            ...T.subItems ?? []
          ])
        );
        C.subItems = D;
      }
    }), t.singleSelector || t.onSelect(k);
  }
  const x = (k) => {
    l(k), c(k);
  }, v = (k, T) => {
    t.onItemExpandedChange(k.id, T), i(
      s.map(
        (C) => C.id === k.id ? { ...C, expanded: !k.expanded } : C
      )
    );
  };
  ue(() => {
    if (!a) {
      i(t.entities);
      return;
    }
    if (u && !t.applySearchToGroup) {
      const T = t.entities.map((C) => {
        const D = Eb(C, a), y = C.subItems?.map((N) => ({
          ...N,
          score: lr(
            a,
            N.subSearchKeys ?? [N.subName]
          )
        })).filter((N) => N.score < 1 / 0).sort((N, M) => N.score - M.score);
        return {
          ...C,
          score: D,
          expanded: C.expanded ?? (y?.length ?? 0) > 0,
          subItems: y
        };
      }).filter((C) => C.score < 1 / 0).sort((C, D) => C.score - D.score);
      i(T);
    } else {
      const k = t.entities.map((T) => {
        const C = lr(
          a,
          T.searchKeys ?? [T.name]
        );
        return { ...T, score: C };
      }).filter((T) => T.score < 1 / 0).sort((T, C) => T.score - C.score);
      i(k);
    }
  }, [
    a,
    t.entities,
    t.applySearchToGroup,
    u,
    i
  ]);
  const S = ge(null), [E, I] = K(0);
  return qf(() => {
    const k = () => {
      S.current && I(S.current.offsetWidth);
    };
    return k(), window.addEventListener("resize", k), () => window.removeEventListener("resize", k);
  }, []), t.alwaysOpen ? /* @__PURE__ */ f(
    "div",
    {
      ref: S,
      className: _(
        "scrollbar-macos relative overflow-auto rounded-xl border-[1px] border-solid border-f1-border-secondary bg-transparent p-0",
        t.width ? "w-fit" : "w-full"
      ),
      children: /* @__PURE__ */ f(
        ko,
        {
          groupView: u,
          entities: s,
          groups: t.groups,
          onGroupChange: t.onGroupChange,
          selectedGroup: t.selectedGroup,
          onSelect: d,
          onRemove: p,
          onSubItemRemove: m,
          onSubItemSelect: h,
          onClear: g,
          onSelectAll: b,
          selectedEntities: t.selectedEntities ?? [],
          search: o,
          onSearch: x,
          onToggleExpand: v,
          searchPlaceholder: t.searchPlaceholder,
          selectAllLabel: t.selectAllLabel,
          clearLabel: t.clearLabel,
          selectedLabel: t.selectedLabel,
          singleSelector: t.singleSelector,
          loading: t.loading,
          notFoundTitle: t.notFoundTitle,
          notFoundSubtitle: t.notFoundSubtitle,
          width: t.width ?? E - 2,
          disabled: t.disabled,
          hiddenAvatar: t.hiddenAvatar,
          onCreate: t.onCreate,
          onCreateLabel: t.onCreateLabel
        }
      )
    }
  ) : /* @__PURE__ */ w(tf, { ...t, onOpenChange: r, open: e, children: [
    /* @__PURE__ */ f(
      nf,
      {
        className: "w-full",
        disabled: t.disabled,
        "aria-label": t.label || t.placeholder,
        children: t.children ? t.children : /* @__PURE__ */ f(
          Tb,
          {
            selected: t.selectedItemsCopy,
            selectedEntities: t.selectedEntities ?? [],
            hiddenAvatar: t.hiddenAvatar,
            label: t.label,
            labelIcon: t.labelIcon,
            icon: t.icon,
            error: t.error,
            status: t.status,
            hint: t.hint,
            hideLabel: t.hideLabel,
            maxLength: t.maxLength,
            value: t.value?.toString() ?? void 0,
            disabled: t.disabled,
            placeholder: t.placeholder,
            loading: t.alwaysOpen ? t.loading : !1,
            required: t.required,
            readonly: t.readonly,
            append: t.append,
            size: t.size,
            open: e
          }
        )
      }
    ),
    /* @__PURE__ */ f(
      rf,
      {
        className: _(
          "scrollbar-macos relative w-full overflow-auto rounded-xl border-[1px] border-solid border-f1-border-secondary bg-transparent p-0"
        ),
        children: /* @__PURE__ */ f(
          ko,
          {
            groupView: u,
            entities: s,
            groups: t.groups,
            onGroupChange: t.onGroupChange,
            selectedGroup: t.selectedGroup,
            onSelect: d,
            onRemove: p,
            onSubItemRemove: m,
            onSubItemSelect: h,
            onClear: g,
            onSelectAll: b,
            selectedEntities: t.selectedEntities ?? [],
            search: o,
            onSearch: x,
            onToggleExpand: v,
            searchPlaceholder: t.searchPlaceholder,
            selectAllLabel: t.selectAllLabel,
            clearLabel: t.clearLabel,
            selectedLabel: t.selectedLabel,
            singleSelector: t.singleSelector,
            loading: t.loading,
            notFoundTitle: t.notFoundTitle,
            notFoundSubtitle: t.notFoundSubtitle,
            width: t.width,
            disabled: t.disabled,
            hiddenAvatar: t.hiddenAvatar,
            actions: t.actions,
            onCreate: t.onCreate,
            onCreateLabel: t.onCreateLabel
          }
        )
      }
    )
  ] });
};
function Ss(t) {
  return t.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().trim();
}
function Co(t) {
  return Ss(t).split(/\s+/).filter((e) => e.length > 0);
}
function lr(t = "", e) {
  const n = Co(t);
  if (n.length === 0)
    return 1 / 0;
  for (const r of e) {
    const s = Ss(r), i = Co(r), o = Ss(t);
    if (s.includes(o) || n.every(
      (a) => i.some((c) => c.includes(a))
    ))
      return 1;
  }
  return 1 / 0;
}
function Eb(t, e) {
  const n = lr(e, t.searchKeys ?? [t.name]);
  let r = 1 / 0;
  return t.subItems?.length && (r = t.subItems.reduce((s, i) => {
    const o = lr(
      e,
      i.subSearchKeys ?? [i.subName]
    );
    return Math.min(s, o);
  }, 1 / 0)), Math.min(n, r);
}
var Xr = "rovingFocusGroup.onEntryFocus", Db = { bubbles: !1, cancelable: !0 }, Tr = "RovingFocusGroup", [Ns, $a, Rb] = sf(Tr), [Lb, Ha] = ml(
  Tr,
  [Rb]
), [Mb, Ob] = Lb(Tr), ja = le.forwardRef(
  (t, e) => /* @__PURE__ */ f(Ns.Provider, { scope: t.__scopeRovingFocusGroup, children: /* @__PURE__ */ f(Ns.Slot, { scope: t.__scopeRovingFocusGroup, children: /* @__PURE__ */ f(Fb, { ...t, ref: e }) }) })
);
ja.displayName = Tr;
var Fb = le.forwardRef((t, e) => {
  const {
    __scopeRovingFocusGroup: n,
    orientation: r,
    loop: s = !1,
    dir: i,
    currentTabStopId: o,
    defaultCurrentTabStopId: l,
    onCurrentTabStopIdChange: a,
    onEntryFocus: c,
    preventScrollOnEntryFocus: u = !1,
    ...d
  } = t, h = le.useRef(null), p = ru(e, h), m = Tl(i), [g = null, b] = ei({
    prop: o,
    defaultProp: l,
    onChange: a
  }), [x, v] = le.useState(!1), S = su(c), E = $a(n), I = le.useRef(!1), [k, T] = le.useState(0);
  return le.useEffect(() => {
    const C = h.current;
    if (C)
      return C.addEventListener(Xr, S), () => C.removeEventListener(Xr, S);
  }, [S]), /* @__PURE__ */ f(
    Mb,
    {
      scope: n,
      orientation: r,
      dir: m,
      loop: s,
      currentTabStopId: g,
      onItemFocus: le.useCallback(
        (C) => b(C),
        [b]
      ),
      onItemShiftTab: le.useCallback(() => v(!0), []),
      onFocusableItemAdd: le.useCallback(
        () => T((C) => C + 1),
        []
      ),
      onFocusableItemRemove: le.useCallback(
        () => T((C) => C - 1),
        []
      ),
      children: /* @__PURE__ */ f(
        rr.div,
        {
          tabIndex: x || k === 0 ? -1 : 0,
          "data-orientation": r,
          ...d,
          ref: p,
          style: { outline: "none", ...t.style },
          onMouseDown: Ft(t.onMouseDown, () => {
            I.current = !0;
          }),
          onFocus: Ft(t.onFocus, (C) => {
            const D = !I.current;
            if (C.target === C.currentTarget && D && !x) {
              const y = new CustomEvent(Xr, Db);
              if (C.currentTarget.dispatchEvent(y), !y.defaultPrevented) {
                const N = E().filter((J) => J.focusable), M = N.find((J) => J.active), z = N.find((J) => J.id === g), V = [M, z, ...N].filter(
                  Boolean
                ).map((J) => J.ref.current);
                Wa(V, u);
              }
            }
            I.current = !1;
          }),
          onBlur: Ft(t.onBlur, () => v(!1))
        }
      )
    }
  );
}), Va = "RovingFocusGroupItem", Ua = le.forwardRef(
  (t, e) => {
    const {
      __scopeRovingFocusGroup: n,
      focusable: r = !0,
      active: s = !1,
      tabStopId: i,
      ...o
    } = t, l = of(), a = i || l, c = Ob(Va, n), u = c.currentTabStopId === a, d = $a(n), { onFocusableItemAdd: h, onFocusableItemRemove: p } = c;
    return le.useEffect(() => {
      if (r)
        return h(), () => p();
    }, [r, h, p]), /* @__PURE__ */ f(
      Ns.ItemSlot,
      {
        scope: n,
        id: a,
        focusable: r,
        active: s,
        children: /* @__PURE__ */ f(
          rr.span,
          {
            tabIndex: u ? 0 : -1,
            "data-orientation": c.orientation,
            ...o,
            ref: e,
            onMouseDown: Ft(t.onMouseDown, (m) => {
              r ? c.onItemFocus(a) : m.preventDefault();
            }),
            onFocus: Ft(t.onFocus, () => c.onItemFocus(a)),
            onKeyDown: Ft(t.onKeyDown, (m) => {
              if (m.key === "Tab" && m.shiftKey) {
                c.onItemShiftTab();
                return;
              }
              if (m.target !== m.currentTarget) return;
              const g = Bb(m, c.orientation, c.dir);
              if (g !== void 0) {
                if (m.metaKey || m.ctrlKey || m.altKey || m.shiftKey) return;
                m.preventDefault();
                let x = d().filter((v) => v.focusable).map((v) => v.ref.current);
                if (g === "last") x.reverse();
                else if (g === "prev" || g === "next") {
                  g === "prev" && x.reverse();
                  const v = x.indexOf(m.currentTarget);
                  x = c.loop ? $b(x, v + 1) : x.slice(v + 1);
                }
                setTimeout(() => Wa(x));
              }
            })
          }
        )
      }
    );
  }
);
Ua.displayName = Va;
var zb = {
  ArrowLeft: "prev",
  ArrowUp: "prev",
  ArrowRight: "next",
  ArrowDown: "next",
  PageUp: "first",
  Home: "first",
  PageDown: "last",
  End: "last"
};
function Pb(t, e) {
  return e !== "rtl" ? t : t === "ArrowLeft" ? "ArrowRight" : t === "ArrowRight" ? "ArrowLeft" : t;
}
function Bb(t, e, n) {
  const r = Pb(t.key, n);
  if (!(e === "vertical" && ["ArrowLeft", "ArrowRight"].includes(r)) && !(e === "horizontal" && ["ArrowUp", "ArrowDown"].includes(r)))
    return zb[r];
}
function Wa(t, e = !1) {
  const n = document.activeElement;
  for (const r of t)
    if (r === n || (r.focus({ preventScroll: e }), document.activeElement !== n)) return;
}
function $b(t, e) {
  return t.map((n, r) => t[(e + r) % t.length]);
}
var Hb = ja, jb = Ua, qt = "ToggleGroup", [Ga] = ml(qt, [
  Ha
]), Ka = Ha(), ci = se.forwardRef((t, e) => {
  const { type: n, ...r } = t;
  if (n === "single")
    return /* @__PURE__ */ f(Vb, { ...r, ref: e });
  if (n === "multiple")
    return /* @__PURE__ */ f(Ub, { ...r, ref: e });
  throw new Error(`Missing prop \`type\` expected on \`${qt}\``);
});
ci.displayName = qt;
var [qa, Ya] = Ga(qt), Vb = se.forwardRef((t, e) => {
  const {
    value: n,
    defaultValue: r,
    onValueChange: s = () => {
    },
    ...i
  } = t, [o, l] = ei({
    prop: n,
    defaultProp: r,
    onChange: s
  });
  return /* @__PURE__ */ f(
    qa,
    {
      scope: t.__scopeToggleGroup,
      type: "single",
      value: o ? [o] : [],
      onItemActivate: l,
      onItemDeactivate: se.useCallback(() => l(""), [l]),
      children: /* @__PURE__ */ f(Ja, { ...i, ref: e })
    }
  );
}), Ub = se.forwardRef((t, e) => {
  const {
    value: n,
    defaultValue: r,
    onValueChange: s = () => {
    },
    ...i
  } = t, [o = [], l] = ei({
    prop: n,
    defaultProp: r,
    onChange: s
  }), a = se.useCallback(
    (u) => l((d = []) => [...d, u]),
    [l]
  ), c = se.useCallback(
    (u) => l((d = []) => d.filter((h) => h !== u)),
    [l]
  );
  return /* @__PURE__ */ f(
    qa,
    {
      scope: t.__scopeToggleGroup,
      type: "multiple",
      value: o,
      onItemActivate: a,
      onItemDeactivate: c,
      children: /* @__PURE__ */ f(Ja, { ...i, ref: e })
    }
  );
});
ci.displayName = qt;
var [Wb, Gb] = Ga(qt), Ja = se.forwardRef(
  (t, e) => {
    const {
      __scopeToggleGroup: n,
      disabled: r = !1,
      rovingFocus: s = !0,
      orientation: i,
      dir: o,
      loop: l = !0,
      ...a
    } = t, c = Ka(n), u = Tl(o), d = { role: "group", dir: u, ...a };
    return /* @__PURE__ */ f(Wb, { scope: n, rovingFocus: s, disabled: r, children: s ? /* @__PURE__ */ f(
      Hb,
      {
        asChild: !0,
        ...c,
        orientation: i,
        dir: u,
        loop: l,
        children: /* @__PURE__ */ f(rr.div, { ...d, ref: e })
      }
    ) : /* @__PURE__ */ f(rr.div, { ...d, ref: e }) });
  }
), ar = "ToggleGroupItem", Xa = se.forwardRef(
  (t, e) => {
    const n = Ya(ar, t.__scopeToggleGroup), r = Gb(ar, t.__scopeToggleGroup), s = Ka(t.__scopeToggleGroup), i = n.value.includes(t.value), o = r.disabled || t.disabled, l = { ...t, pressed: i, disabled: o }, a = se.useRef(null);
    return r.rovingFocus ? /* @__PURE__ */ f(
      jb,
      {
        asChild: !0,
        ...s,
        focusable: !o,
        active: i,
        ref: a,
        children: /* @__PURE__ */ f(So, { ...l, ref: e })
      }
    ) : /* @__PURE__ */ f(So, { ...l, ref: e });
  }
);
Xa.displayName = ar;
var So = se.forwardRef(
  (t, e) => {
    const { __scopeToggleGroup: n, value: r, ...s } = t, i = Ya(ar, n), o = { role: "radio", "aria-checked": t.pressed, "aria-pressed": void 0 }, l = i.type === "single" ? o : void 0;
    return /* @__PURE__ */ f(
      lf,
      {
        ...l,
        ...s,
        ref: e,
        onPressedChange: (a) => {
          a ? i.onItemActivate(r) : i.onItemDeactivate(r);
        }
      }
    );
  }
), Za = ci, Qa = Xa;
const ec = Et({
  base: _(
    "inline-flex items-center justify-center rounded-sm text-sm font-medium transition-colors hover:bg-f1-background-secondary hover:text-f1-foreground-secondary disabled:pointer-events-none disabled:opacity-50 data-[state=on]:bg-f1-background-secondary data-[state=on]:text-f1-foreground",
    vr()
  ),
  variants: {
    variant: {
      default: "bg-transparent",
      outline: "border border-f1-border bg-transparent hover:bg-f1-background-secondary hover:text-f1-foreground"
    },
    size: {
      default: "h-10 px-3",
      sm: "h-9 px-2.5",
      lg: "h-11 px-5"
    }
  },
  defaultVariants: {
    variant: "default",
    size: "default"
  }
}), Kb = le.forwardRef(({ className: t, variant: e, size: n, ...r }, s) => /* @__PURE__ */ f(
  El,
  {
    ref: s,
    className: _(ec({ variant: e, size: n, className: t })),
    ...r
  }
));
Kb.displayName = El.displayName;
const tc = le.createContext({
  size: "default",
  variant: "default"
}), nc = le.forwardRef(({ className: t, variant: e, size: n, children: r, ...s }, i) => /* @__PURE__ */ f(
  Za,
  {
    ref: i,
    className: _("flex items-center justify-center gap-1.5", t),
    ...s,
    children: /* @__PURE__ */ f(tc.Provider, { value: { variant: e, size: n }, children: r })
  }
));
nc.displayName = Za.displayName;
const rc = le.forwardRef(({ className: t, children: e, variant: n, size: r, ...s }, i) => {
  const o = le.useContext(tc);
  return /* @__PURE__ */ f(
    Qa,
    {
      ref: i,
      className: _(
        ec({
          variant: o.variant || n,
          size: o.size || r
        }),
        t
      ),
      ...s,
      children: e
    }
  );
});
rc.displayName = Qa.displayName;
const qb = ({
  id: t,
  createdAt: e,
  title: n,
  description: r,
  icon: s,
  category: i,
  isUnread: o = !1,
  onClick: l,
  onVisible: a
}) => {
  const { ref: c } = iu({
    threshold: 0.1,
    onChange(h) {
      h && a?.(t);
    }
  }), u = Ul(e, {
    yesterdayRelative: !1
  });
  return /* @__PURE__ */ w(
    "div",
    {
      ref: c,
      className: "flex w-full cursor-pointer flex-row gap-2 rounded-lg p-2 pr-3 hover:bg-f1-background-hover focus:border-f1-border-secondary focus:outline focus:outline-1 focus:outline-offset-1 focus:outline-f1-border-selected-bold",
      onClick: () => {
        l(t);
      },
      children: [
        /* @__PURE__ */ f(ds, { icon: s ?? af }),
        /* @__PURE__ */ w("div", { className: "flex-1", children: [
          /* @__PURE__ */ f(
            "p",
            {
              className: "line-clamp-2 font-medium text-f1-foreground",
              title: n,
              children: n
            }
          ),
          /* @__PURE__ */ f(
            "p",
            {
              className: "line-clamp-2 text-f1-foreground-secondary",
              title: r,
              children: r
            }
          ),
          /* @__PURE__ */ f("div", { className: "mt-1.5 flex flex-row", children: /* @__PURE__ */ f("p", { className: "text-f1-foreground-secondary", children: `${i} · ${u}` }) })
        ] }),
        /* @__PURE__ */ f("div", { className: "ml-1", children: o && /* @__PURE__ */ f("div", { className: "mt-1.5 size-2 rounded-full bg-f1-icon-accent" }) })
      ]
    }
  );
}, Yb = () => /* @__PURE__ */ w("div", { className: "flex w-full flex-row gap-2 rounded-lg p-2 pr-3", children: [
  /* @__PURE__ */ f(A, { className: "size-9 rounded-md" }),
  /* @__PURE__ */ w("div", { className: "flex-1", children: [
    /* @__PURE__ */ f(A, { className: "mb-1 h-5 w-full" }),
    /* @__PURE__ */ f(A, { className: "mb-1 h-4 w-full" }),
    /* @__PURE__ */ f(A, { className: "mb-1 h-4 w-full" }),
    /* @__PURE__ */ f(A, { className: "mt-1.5 h-4 w-1/3" })
  ] })
] }), di = Y(
  "ActivityItem",
  Oe(qb, Yb)
), sc = ({
  title: t,
  children: e
}) => /* @__PURE__ */ w("div", { children: [
  /* @__PURE__ */ f("div", { className: "pb-2 pl-2 pt-1", children: /* @__PURE__ */ f("p", { className: "text-sm font-medium text-f1-foreground-secondary", children: t }) }),
  /* @__PURE__ */ f("div", { className: "flex flex-col gap-1", children: e })
] }), Jb = ({
  title: t,
  items: e,
  onClickItem: n,
  onItemVisible: r
}) => /* @__PURE__ */ f(sc, { title: t, children: e.map((s) => /* @__PURE__ */ f(
  di,
  {
    ...s,
    onClick: () => n(s.id),
    onVisible: r
  },
  s.id
)) }), Xb = ({
  title: t,
  numItems: e
}) => /* @__PURE__ */ f(sc, { title: t, children: Array.from({ length: e }).map((n, r) => /* @__PURE__ */ f(di.Skeleton, {}, r)) }), Yn = Oe(Jb, Xb), Zb = 3, Qb = ["today", "yesterday", "lastWeek", "lastMonth"], ew = (t) => Zf(t, ([e]) => {
  const n = Qb.indexOf(e);
  return n === -1 ? -Number(e) : n - 4e3;
}), Is = () => /* @__PURE__ */ f("div", { className: "-mx-2 h-px bg-f1-background-secondary" }), tw = ({
  items: t,
  loadingMoreItems: e = !1,
  onClickItem: n,
  onEndReached: r,
  onEndReachedItemsThreshold: s = 5
}) => {
  const i = ot(), o = Of(t, "createdAt"), l = Object.values(o).slice().flatMap((u) => u.map((d) => d.id)).slice(-s), a = Xf((u) => {
    l.includes(u) && r?.();
  }, 1e3), c = ew(
    Object.entries(o).filter(([u, d]) => !!d.length)
  );
  return /* @__PURE__ */ w("div", { className: "flex flex-col gap-2 p-2", children: [
    c.map(([u, d], h) => /* @__PURE__ */ w(se.Fragment, { children: [
      /* @__PURE__ */ f(
        Yn,
        {
          title: u in i.date.groups ? i.date.groups[u] : u,
          items: d,
          onClickItem: n,
          onItemVisible: a
        }
      ),
      h !== c.length - 1 && /* @__PURE__ */ f(Is, {})
    ] }, u)),
    e && new Array(Zb).fill(null).map((u, d) => /* @__PURE__ */ f(di.Skeleton, {}, d))
  ] });
}, nw = () => {
  const t = ot();
  return /* @__PURE__ */ w("div", { className: "flex flex-col gap-2 p-2", children: [
    /* @__PURE__ */ f(Yn.Skeleton, { title: t.date.groups.today, numItems: 1 }),
    /* @__PURE__ */ f(Is, {}),
    /* @__PURE__ */ f(
      Yn.Skeleton,
      {
        title: t.date.groups.yesterday,
        numItems: 3
      }
    ),
    /* @__PURE__ */ f(Is, {}),
    /* @__PURE__ */ f(
      Yn.Skeleton,
      {
        title: t.date.groups.lastMonth,
        numItems: 5
      }
    )
  ] });
}, Xv = Y(
  "ActivityItemList",
  Oe(tw, nw)
), rw = {
  birthday: "🎂",
  anniversary: "🎉",
  "first-day": "💼"
}, sw = {
  viridian: "bg-[hsl(theme(colors.viridian.50)_/_0.2)]",
  malibu: "bg-[hsl(theme(colors.malibu.50)_/_0.2)]",
  yellow: "bg-[hsl(theme(colors.yellow.50)_/_0.2)]",
  purple: "bg-[hsl(theme(colors.purple.50)_/_0.2)]",
  lilac: "bg-[hsl(theme(colors.lilac.50)_/_0.2)]",
  barbie: "bg-[hsl(theme(colors.barbie.50)_/_0.2)]",
  smoke: "bg-[hsl(theme(colors.smoke.50)_/_0.2)]",
  army: "bg-[hsl(theme(colors.army.50)_/_0.2)]",
  flubber: "bg-[hsl(theme(colors.flubber.50)_/_0.2)]",
  indigo: "bg-[hsl(theme(colors.indigo.50)_/_0.2)]",
  camel: "bg-[hsl(theme(colors.camel.50)_/_0.2)]"
};
function iw({
  firstName: t,
  lastName: e,
  src: n,
  canReact: r,
  lastEmojiReaction: s,
  onReactionSelect: i,
  pickerRef: o
}) {
  return /* @__PURE__ */ w(
    "div",
    {
      className: _(
        "relative h-32 w-full overflow-hidden rounded-md bg-f1-background",
        n ? "" : sw[ou(
          [t, e].join("")
        )]
      ),
      children: [
        n && /* @__PURE__ */ f(
          "div",
          {
            className: "absolute inset-0 bg-cover bg-center bg-no-repeat opacity-10",
            style: { backgroundImage: `url("${n}")` }
          }
        ),
        /* @__PURE__ */ f("div", { className: "relative flex h-full w-full items-center justify-center overflow-hidden rounded-md backdrop-blur", children: /* @__PURE__ */ w("div", { className: "relative h-fit w-fit", children: [
          /* @__PURE__ */ f(
            "div",
            {
              style: r ? {
                clipPath: "path('M69.6933 48.707C71.1842 44.7556 72 40.4731 72 36C72 16.1177 55.8823 0 36 0C16.1177 0 0 16.1177 0 36C0 55.8823 16.1177 72 36 72C40.4731 72 44.7556 71.1842 48.707 69.6933C48.6283 69.4953 48.5557 69.2942 48.4894 69.0902C48 67.5838 48 65.7226 48 62C48 58.2774 48 56.4162 48.4894 54.9098C49.4786 51.8655 51.8655 49.4786 54.9098 48.4894C56.4162 48 58.2774 48 62 48C65.7226 48 67.5838 48 69.0902 48.4894C69.2942 48.5557 69.4953 48.6283 69.6933 48.707')"
              } : {},
              children: /* @__PURE__ */ f(
                mt,
                {
                  src: n,
                  firstName: t,
                  lastName: e,
                  size: "2xl"
                }
              )
            }
          ),
          r && /* @__PURE__ */ f(
            "div",
            {
              ref: o,
              className: _(
                "absolute -right-0.5",
                n ? "bottom-0.5" : "-bottom-[3px]"
              ),
              children: /* @__PURE__ */ f(
                Wl,
                {
                  lastEmojiReaction: s,
                  onSelect: i,
                  size: "sm",
                  variant: "neutral"
                }
              )
            }
          )
        ] }) })
      ]
    }
  );
}
function ow(t) {
  const e = ge(null), n = ge(), r = ne(() => {
    const i = e.current;
    if (!i) return;
    const o = lu.create(i, {
      resize: !0,
      useWorker: !1
    }), l = ["9D76F3", "3FC495", "E61D46", "F6AF3D"], a = 0.1;
    n.current = setInterval(() => {
      o({
        particleCount: 1,
        angle: 90,
        spread: 45,
        origin: {
          x: a + Math.random() * (1 - a * 2),
          y: -0.1
        },
        gravity: 0.65,
        scalar: 1,
        ticks: 80,
        startVelocity: 1,
        disableForReducedMotion: t,
        colors: [
          l[Math.floor(Math.random() * l.length)]
        ]
      });
    }, 100);
  }, [t]), s = ne(() => {
    clearInterval(n.current);
  }, []);
  return { canvasRef: e, handleMouseEnter: r, handleMouseLeave: s };
}
const lw = ({
  link: t,
  firstName: e,
  lastName: n,
  src: r,
  onClick: s,
  canReact: i = !0,
  lastEmojiReaction: o,
  onReactionSelect: l,
  type: a,
  typeLabel: c,
  date: u
}) => {
  const [d, h] = K(o), p = ge(null);
  ue(() => {
    h(o);
  }, [o]);
  const m = (E) => {
    h(E), l?.(E);
  }, g = au(), { canvasRef: b, handleMouseEnter: x, handleMouseLeave: v } = ow(g), S = Xs({
    emoji: rw[a],
    size: "sm"
  });
  return /* @__PURE__ */ w(
    Zs,
    {
      href: t,
      onClick: s,
      className: _(
        "relative flex flex-col rounded-xl border border-solid border-f1-border-secondary bg-f1-background-inverse-secondary no-underline transition-shadow hover:shadow",
        vr()
      ),
      onMouseEnter: g ? void 0 : x,
      onMouseLeave: g ? void 0 : v,
      children: [
        /* @__PURE__ */ f(
          "canvas",
          {
            ref: b,
            className: "pointer-events-none absolute inset-0 z-50 h-full w-full"
          }
        ),
        /* @__PURE__ */ f("div", { className: "basis-2/3 px-1 pt-1", children: /* @__PURE__ */ f(
          iw,
          {
            firstName: e,
            lastName: n,
            src: r,
            canReact: i,
            lastEmojiReaction: d,
            onReactionSelect: m,
            pickerRef: p
          }
        ) }),
        /* @__PURE__ */ w("div", { className: "flex basis-1/3 flex-row justify-between gap-2 p-3", children: [
          /* @__PURE__ */ w("div", { className: "flex min-w-0 flex-1 flex-col", children: [
            /* @__PURE__ */ w("div", { className: "truncate font-medium text-f1-foreground", children: [
              e,
              " ",
              n
            ] }),
            /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-1.5 text-f1-foreground-secondary", children: [
              /* @__PURE__ */ f("span", { className: "truncate", children: c }),
              /* @__PURE__ */ f("span", { className: "shrink-0 leading-none", children: S })
            ] })
          ] }),
          /* @__PURE__ */ f("div", { className: "shrink-0", children: /* @__PURE__ */ f(hs, { date: u }) })
        ] })
      ]
    }
  );
}, aw = () => /* @__PURE__ */ w(
  "div",
  {
    className: "bg-f1-background-inverse-secondar flex flex-col rounded-xl border border-solid border-f1-border-secondary",
    role: "status",
    "aria-busy": "true",
    "aria-live": "polite",
    children: [
      /* @__PURE__ */ f("div", { className: "basis-2/3 px-1 pt-1", children: /* @__PURE__ */ f(A, { className: "h-32 w-full rounded-lg" }) }),
      /* @__PURE__ */ f("div", { className: "flex basis-1/3 flex-row justify-between gap-2 p-3", children: /* @__PURE__ */ f("div", { className: "flex min-w-0 flex-1 flex-col", children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-2 py-1", children: [
        /* @__PURE__ */ f(A, { className: "h-3 w-2/3" }),
        /* @__PURE__ */ f(A, { className: "h-3 w-1/3" })
      ] }) }) })
    ]
  }
), Zv = Oe(lw, aw), Qv = ({
  title: t,
  subtitle: e,
  buttonLabel: n,
  onClick: r
}) => /* @__PURE__ */ w("div", { className: "flex w-full flex-col items-start justify-between gap-4 rounded-md bg-gradient-to-r from-[#F5A51C]/30 via-[#E51943]/30 to-[#5596F6]/30 px-3 py-3 ring-1 ring-inset ring-f1-border-secondary sm:flex-row sm:items-center sm:px-4", children: [
  /* @__PURE__ */ w("div", { className: "flex flex-col items-start gap-3 sm:flex-row sm:items-center", children: [
    /* @__PURE__ */ f(gl, { module: "kudos", size: "lg" }),
    /* @__PURE__ */ w("div", { className: "flex flex-col", children: [
      /* @__PURE__ */ f("span", { className: "font-medium text-f1-foreground", children: t }),
      /* @__PURE__ */ f("span", { className: "text-f1-foreground-secondary", children: e })
    ] })
  ] }),
  /* @__PURE__ */ f("div", { className: "w-full sm:w-fit [&>div]:w-full", children: /* @__PURE__ */ f($, { label: n, variant: "outline", onClick: r }) })
] });
function cw({
  emoji: t,
  initialCount: e,
  hasReacted: n = !1,
  users: r,
  onInteraction: s
}) {
  const [i, o] = K(n), [l, a] = K(e), c = ge(null), { fireEmojiConfetti: u } = cu(), d = (m, g) => {
    m.stopPropagation(), a(l + (i ? -1 : 1)), o(!i), s?.(g), i || u(g, c);
  }, h = r?.map((m) => m.name).join(", ") || "", p = /* @__PURE__ */ f(
    du,
    {
      ref: c,
      variant: "outline",
      size: "md",
      compact: !0,
      onClick: (m) => {
        d(m, t);
      },
      className: _(
        "flex items-center gap-1 px-[3px] font-medium leading-tight shadow-none transition-all active:scale-90 motion-reduce:transition-none motion-reduce:active:scale-100",
        i && "border-f1-border-selected bg-f1-background-selected hover:border-f1-border-selected-bold"
      ),
      "aria-label": uu(t),
      prepend: /* @__PURE__ */ f(Xs, { emoji: t, size: "md" }),
      children: /* @__PURE__ */ f(
        cf,
        {
          value: l,
          spinTiming: {
            duration: 200,
            easing: "cubic-bezier(0.175, 0.885, 0.32, 1.275)"
          },
          className: _(
            "tabular-nums",
            i ? "text-f1-foreground-selected" : "text-f1-foreground"
          )
        }
      )
    }
  );
  return h ? /* @__PURE__ */ f(kr, { label: h, children: p }) : p;
}
function dw({ items: t, onInteraction: e, locale: n, action: r }) {
  return /* @__PURE__ */ w("div", { className: "flex flex-wrap gap-2", children: [
    r && /* @__PURE__ */ f(
      $,
      {
        label: r.label,
        icon: r.icon,
        onClick: r.onClick,
        variant: "outline",
        hideLabel: !0
      }
    ),
    /* @__PURE__ */ f(Wl, { onSelect: e, locale: n }),
    t.map((s) => /* @__PURE__ */ f(
      cw,
      {
        emoji: s.emoji,
        initialCount: s.initialCount,
        hasReacted: s.hasReacted,
        users: s.users,
        onInteraction: e
      },
      s.emoji
    ))
  ] });
}
const uw = Y("Reactions", dw), fw = ({
  content: t,
  collapsed: e
}) => /* @__PURE__ */ f(
  Ea,
  {
    content: t,
    className: _(
      "FactorialOneTextEditor",
      e && "line-clamp-5 break-words"
    )
  }
), hw = () => /* @__PURE__ */ w("div", { className: "flex flex-col justify-around gap-3 py-2", children: [
  /* @__PURE__ */ f(A, { className: "h-2.5 w-1/2 rounded-2xs" }),
  /* @__PURE__ */ f(A, { className: "h-2.5 w-2/3 rounded-2xs" })
] }), ic = Oe(
  fw,
  hw
), No = ({ tags: t, right: e }) => /* @__PURE__ */ f(
  "div",
  {
    className: _(
      "flex flex-1 flex-row items-center gap-1.5",
      e && "justify-end"
    ),
    children: t.map((n) => {
      const r = /* @__PURE__ */ f("div", { children: /* @__PURE__ */ f(
        jt,
        {
          icon: n.icon,
          text: n.label ?? (n.description || ""),
          onlyIcon: !0,
          additionalAccessibleText: `${n.label}: ${n.description}`
        }
      ) });
      return n.label || n.description ? /* @__PURE__ */ f(
        kr,
        {
          label: n.label,
          description: n.description,
          children: r
        },
        n.label ?? n.description
      ) : r;
    })
  }
), _s = F(
  function({
    label: e,
    title: n,
    subtitle: r,
    description: s,
    color: i,
    isPending: o,
    leftTags: l,
    rightTags: a,
    fromDate: c,
    toDate: u,
    noBackground: d
  }, h) {
    return /* @__PURE__ */ w(
      "div",
      {
        ref: h,
        className: "relative flex flex-row items-stretch gap-2.5 overflow-hidden rounded-sm p-2",
        children: [
          !d && /* @__PURE__ */ w(ae, { children: [
            /* @__PURE__ */ f(
              "div",
              {
                className: "absolute bottom-0 left-0 right-0 top-0 opacity-5",
                style: {
                  background: `${i}`
                }
              }
            ),
            /* @__PURE__ */ f(
              "div",
              {
                className: "absolute bottom-0 left-0 right-0 top-0 opacity-5",
                style: {
                  background: `linear-gradient(to right, ${i}, transparent)`
                }
              }
            )
          ] }),
          /* @__PURE__ */ f(
            "div",
            {
              className: "min-h-10 min-w-1 rounded-2xs",
              style: o ? {
                backgroundImage: `repeating-linear-gradient(
              135deg,
              ${i} 0px,
              ${i} 4px, 
              transparent 4px, 
              transparent 5.5px
            )`
              } : {
                backgroundColor: i
              }
            }
          ),
          /* @__PURE__ */ w("div", { className: "z-10 flex flex-1 flex-col gap-2", children: [
            /* @__PURE__ */ w("div", { className: "flex flex-row items-start gap-2.5", children: [
              /* @__PURE__ */ w("div", { className: "flex flex-1 flex-col gap-0.5", children: [
                !!e && /* @__PURE__ */ f("p", { className: "line-clamp-1 text-sm text-f1-foreground-secondary", children: e }),
                /* @__PURE__ */ w("p", { className: "line-clamp-3 font-medium text-f1-foreground", children: [
                  n,
                  !!r && /* @__PURE__ */ f("span", { className: "pl-1 font-normal text-f1-foreground-secondary", children: `· ${r}` })
                ] }),
                /* @__PURE__ */ f("p", { className: "text-f1-foreground-secondary", children: s })
              ] }),
              /* @__PURE__ */ w("div", { className: "flex flex-row items-center", children: [
                c && /* @__PURE__ */ w(ae, { children: [
                  /* @__PURE__ */ f(hs, { date: c }),
                  /* @__PURE__ */ f(
                    G,
                    {
                      icon: bl,
                      size: "sm",
                      className: "text-f1-foreground-tertiary"
                    }
                  )
                ] }),
                u && /* @__PURE__ */ f(hs, { date: u })
              ] })
            ] }),
            (l || a) && /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between", children: [
              l && /* @__PURE__ */ f(No, { tags: l }),
              a && /* @__PURE__ */ f(No, { tags: a, right: !0 })
            ] })
          ] })
        ]
      }
    );
  }
), pw = /* @__PURE__ */ new Set(["avi", "mkv", "mov", "mpeg", "mp4", "webm", "wmv"]), oc = (t) => {
  if (!t) return !1;
  if (t.indexOf("//s3.") >= 0)
    return t.indexOf("response-content-type=video") >= 0;
  const n = (t?.split(".")).at(-1);
  return !!n && pw.has(n);
}, mw = ({
  title: t,
  mediaUrl: e,
  place: n,
  date: r
}) => {
  let s = Ff(r);
  const i = (o) => {
    o.stopPropagation();
  };
  return n && (s = `${s} · ${n}`), /* @__PURE__ */ w("div", { className: "flex w-full flex-col gap-1 rounded-xl border border-solid border-f1-border-secondary bg-f1-background-inverse-secondary p-1 shadow", children: [
    e && /* @__PURE__ */ f("div", { className: "relative aspect-video w-full overflow-hidden rounded-md", children: oc(e) ? /* @__PURE__ */ f(
      "video",
      {
        controls: !0,
        className: "aspect-video h-full w-full bg-f1-background-secondary object-cover",
        onClick: i,
        children: /* @__PURE__ */ f("source", { src: e })
      }
    ) : /* @__PURE__ */ w(ae, { children: [
      /* @__PURE__ */ f(
        "img",
        {
          src: e,
          role: "presentation",
          loading: "lazy",
          className: "aspect-video h-full w-full object-cover"
        }
      ),
      /* @__PURE__ */ f(A, { className: "absolute inset-0 h-full w-full" })
    ] }) }),
    /* @__PURE__ */ f(
      _s,
      {
        title: t,
        description: s,
        color: fu.special.highlight,
        isPending: !1,
        toDate: r,
        noBackground: !0
      }
    )
  ] });
}, gw = () => /* @__PURE__ */ w(
  "div",
  {
    className: "flex w-full flex-col gap-1 rounded-xl border border-solid border-f1-border-secondary bg-f1-background-inverse-secondary p-1",
    role: "status",
    "aria-busy": "true",
    "aria-live": "polite",
    children: [
      /* @__PURE__ */ f("div", { children: /* @__PURE__ */ f(A, { className: "aspect-video h-full w-full rounded-lg" }) }),
      /* @__PURE__ */ w("div", { className: "flex h-full flex-row gap-3 p-2", children: [
        /* @__PURE__ */ f(A, { className: "w-1 shrink-0 self-stretch rounded-full" }),
        /* @__PURE__ */ w("div", { className: "flex grow flex-col gap-1.5 py-1", children: [
          /* @__PURE__ */ f(A, { className: "mt-px h-3 w-1/2" }),
          /* @__PURE__ */ f(A, { className: "mb-px h-3 w-1/4" })
        ] })
      ] })
    ]
  }
), lc = Oe(mw, gw), bw = ({
  id: t,
  author: e,
  group: n,
  createdAt: r,
  title: s,
  description: i,
  onClick: o,
  mediaUrl: l,
  event: a,
  counters: c,
  reactions: u,
  inLabel: d,
  comment: h,
  actions: p,
  dropdownItems: m,
  noReactionsButton: g = !1
}) => {
  const b = [c.views, c.comments].filter(Boolean).join(" · "), x = !0, v = Ul(r), S = () => {
    o(t);
  }, E = (k) => {
    k.stopPropagation();
  }, I = e ? `${e.firstName} ${e.lastName}` : void 0;
  return /* @__PURE__ */ w(
    "div",
    {
      className: "flex w-full cursor-pointer flex-row gap-3 rounded-xl border border-solid border-transparent p-3 pt-2 hover:bg-f1-background-hover focus:border-f1-border-secondary focus:outline focus:outline-1 focus:outline-offset-1 focus:outline-f1-border-selected-bold md:pb-4 md:pt-3",
      onClick: S,
      id: `community-post-${t}`,
      children: [
        /* @__PURE__ */ f("div", { className: "hidden md:block", children: e ? /* @__PURE__ */ f(
          En,
          {
            href: e.url || "#",
            title: I,
            stopPropagation: !0,
            children: /* @__PURE__ */ f(
              mt,
              {
                firstName: e.firstName,
                lastName: e.lastName,
                src: e.avatarUrl
              }
            )
          }
        ) : /* @__PURE__ */ f(ds, { icon: Ri }) }),
        /* @__PURE__ */ w("div", { className: "flex min-w-0 flex-1 flex-col gap-3", children: [
          /* @__PURE__ */ w("div", { className: "flex min-w-0 flex-col gap-2", children: [
            /* @__PURE__ */ w("div", { className: "flex flex-row justify-between", children: [
              /* @__PURE__ */ w("div", { className: "flex min-w-0 flex-1 flex-row flex-wrap items-center gap-1", children: [
                e ? /* @__PURE__ */ w(ae, { children: [
                  /* @__PURE__ */ f(
                    En,
                    {
                      href: e.url,
                      className: "block md:hidden",
                      title: I,
                      stopPropagation: !0,
                      children: /* @__PURE__ */ f("span", { className: "flex items-center", children: /* @__PURE__ */ f(
                        mt,
                        {
                          firstName: e.firstName,
                          lastName: e.lastName,
                          src: e.avatarUrl,
                          size: "xs"
                        }
                      ) })
                    }
                  ),
                  /* @__PURE__ */ f(
                    En,
                    {
                      href: e.url,
                      title: I,
                      className: "font-medium text-f1-foreground no-underline visited:text-f1-foreground",
                      stopPropagation: !0,
                      children: I
                    }
                  )
                ] }) : /* @__PURE__ */ f("div", { className: "block md:hidden", children: /* @__PURE__ */ f(ds, { icon: Ri, size: "sm" }) }),
                /* @__PURE__ */ f(
                  "span",
                  {
                    className: _(
                      "text-f1-foreground-secondary",
                      !e && "capitalize"
                    ),
                    children: d
                  }
                ),
                /* @__PURE__ */ f(
                  En,
                  {
                    onClick: n.onClick,
                    title: n.title,
                    className: "font-medium text-f1-foreground no-underline visited:text-f1-foreground",
                    stopPropagation: !0,
                    href: "#",
                    children: n.title
                  }
                )
              ] }),
              /* @__PURE__ */ w("div", { className: "flex flex-row gap-2", children: [
                /* @__PURE__ */ w("div", { className: "hidden flex-row gap-2 md:flex", children: [
                  p?.map((k) => /* @__PURE__ */ f(
                    $,
                    {
                      hideLabel: !k.label,
                      ...k.icon && { icon: k.icon },
                      variant: "outline",
                      size: "md",
                      onClick: k.onClick,
                      label: k.label ?? "",
                      title: k.label ?? ""
                    },
                    k.label
                  )),
                  m?.length && /* @__PURE__ */ f(
                    sn,
                    {
                      items: m,
                      icon: Li,
                      size: "sm"
                    }
                  )
                ] }),
                /* @__PURE__ */ f("div", { className: "md:hidden", children: /* @__PURE__ */ f(
                  sn,
                  {
                    items: [
                      {
                        label: h.label,
                        onClick: h.onClick
                      },
                      ...m ?? []
                    ],
                    icon: Li,
                    size: "sm"
                  }
                ) })
              ] })
            ] }),
            /* @__PURE__ */ f("span", { className: "-mt-3 text-sm text-f1-foreground-secondary", children: v }),
            /* @__PURE__ */ w("div", { className: "flex min-w-0 flex-col gap-1 text-f1-foreground", children: [
              /* @__PURE__ */ f(
                "p",
                {
                  className: _(
                    "text-xl font-semibold",
                    "line-clamp-2 break-words"
                  ),
                  children: s
                }
              ),
              i && /* @__PURE__ */ f(ic, { content: i, collapsed: x })
            ] })
          ] }),
          l && !a && /* @__PURE__ */ f("div", { className: "relative aspect-video overflow-hidden rounded-xl md:max-w-[480px]", children: oc(l) ? /* @__PURE__ */ f(
            "video",
            {
              controls: !0,
              className: "aspect-video h-full w-full bg-f1-background-secondary object-cover",
              onClick: E,
              children: /* @__PURE__ */ f("source", { src: l })
            }
          ) : /* @__PURE__ */ w(ae, { children: [
            /* @__PURE__ */ f(
              "img",
              {
                src: l,
                role: "presentation",
                loading: "lazy",
                className: "aspect-video h-full w-full object-cover"
              }
            ),
            /* @__PURE__ */ f(A, { className: "absolute inset-0 -z-10 h-full w-full" })
          ] }) }),
          a && /* @__PURE__ */ f("div", { className: "w-full md:max-w-[480px]", children: /* @__PURE__ */ f(lc, { ...a }) }),
          /* @__PURE__ */ f("p", { className: "text-f1-foreground-secondary", children: b }),
          !g && /* @__PURE__ */ f(
            uw,
            {
              items: u?.items ?? [],
              onInteraction: u?.onInteraction,
              action: {
                label: h.label,
                onClick: h.onClick,
                icon: df
              }
            }
          )
        ] })
      ]
    }
  );
}, ww = ({
  withEvent: t,
  withImage: e
}) => /* @__PURE__ */ w("div", { className: "flex w-full cursor-wait flex-row gap-3 rounded-xl p-3 pt-2 md:pb-4 md:pt-3", children: [
  /* @__PURE__ */ f("div", { className: "hidden md:block", children: /* @__PURE__ */ f(A, { className: "aspect-square w-8 rounded-full" }) }),
  /* @__PURE__ */ w("div", { className: "w-full", children: [
    /* @__PURE__ */ w("div", { className: "flex h-6 flex-row items-center gap-2", children: [
      /* @__PURE__ */ f("div", { className: "md:hidden", children: /* @__PURE__ */ f(A, { className: "aspect-square w-4 rounded-full" }) }),
      /* @__PURE__ */ f(A, { className: "h-2.5 w-14 rounded-2xs" }),
      /* @__PURE__ */ f(A, { className: "h-2.5 w-18 rounded-2xs" })
    ] }),
    /* @__PURE__ */ f(A, { className: "mt-3.5 h-3.5 w-1/5 rounded-2xs" }),
    /* @__PURE__ */ f("div", { className: "mt-3", children: /* @__PURE__ */ f(ic.Skeleton, {}) }),
    e && !t && /* @__PURE__ */ f("div", { className: "mt-3 aspect-video w-full overflow-hidden rounded-xl md:w-2/3", children: /* @__PURE__ */ f(A, { className: "h-full w-full rounded-2xs" }) }),
    t && /* @__PURE__ */ f("div", { className: "mt-3 w-full md:w-2/3", children: /* @__PURE__ */ f(lc.Skeleton, {}) }),
    /* @__PURE__ */ w("div", { className: "mt-3 flex flex-row items-center gap-1 py-1", children: [
      /* @__PURE__ */ f(A, { className: "h-2.5 w-14 rounded-2xs" }),
      /* @__PURE__ */ f(A, { className: "h-2.5 w-14 rounded-2xs" })
    ] })
  ] })
] }), ek = Oe(
  bw,
  ww
), xw = [
  "Monday",
  "Tuesday",
  "Wednesday",
  "Thursday",
  "Friday",
  "Saturday",
  "Sunday"
], yw = F(
  function({ daysOfTheWeek: e = xw, activatedDays: n = [] }, r) {
    const s = n.map(
      (i) => `${i}-${e[i]}`
    );
    return /* @__PURE__ */ f(
      nc,
      {
        type: "multiple",
        value: s,
        disabled: !0,
        className: "flex justify-start",
        ref: r,
        children: e.map((i, o) => /* @__PURE__ */ f(
          rc,
          {
            "aria-label": i,
            value: `${o}-${i}`,
            className: "h-6 w-6 shrink-0 grow-0 basis-6 p-0 text-sm font-medium disabled:select-none disabled:bg-f1-background-tertiary disabled:text-f1-foreground-secondary disabled:opacity-100 disabled:data-[state=on]:border disabled:data-[state=on]:border-solid disabled:data-[state=on]:border-f1-border-selected disabled:data-[state=on]:bg-f1-background-selected disabled:data-[state=on]:text-f1-foreground-selected",
            children: i[0]
          },
          o
        ))
      }
    );
  }
), vw = 750, kw = ({ text: t, children: e }) => {
  const [n, r] = K(!1);
  ue(() => {
    if (n) {
      const i = setTimeout(() => r(!1), vw);
      return () => clearTimeout(i);
    }
  }, [n]);
  const s = async () => {
    try {
      await navigator.clipboard.writeText(t), r(!0);
    } catch {
    }
  };
  return /* @__PURE__ */ w(
    "button",
    {
      type: "button",
      "aria-label": n ? "Copied!" : `Copy ${t}`,
      className: _(
        "group flex items-center gap-1.5 rounded p-1.5",
        "focus-visible:outline focus-visible:outline-2 focus-visible:outline-f1-border-selected-bold",
        "transition-colors duration-300 hover:bg-f1-background-hover active:bg-f1-background-secondary-hover",
        n ? "hover:bg-f1-background-positive focus-visible:bg-f1-background-positive" : void 0
      ),
      onClick: s,
      children: [
        e,
        /* @__PURE__ */ f("div", { className: "relative h-5 w-5", children: /* @__PURE__ */ w(hu, { mode: "wait", children: [
          !n && /* @__PURE__ */ f(
            us.div,
            {
              initial: { opacity: 0, scale: 0.8 },
              animate: { opacity: 1, scale: 1 },
              exit: { opacity: 0, scale: 0.8 },
              transition: { duration: 0.1 },
              className: "absolute inset-0",
              children: /* @__PURE__ */ f(
                G,
                {
                  icon: uf,
                  size: "md",
                  "aria-hidden": !0,
                  color: "default",
                  className: _(
                    "opacity-0 transition-opacity duration-300",
                    !n && "group-hover:opacity-100 group-focus-visible:opacity-100"
                  )
                }
              )
            },
            "copy-icon"
          ),
          n && /* @__PURE__ */ f(
            us.div,
            {
              initial: { opacity: 0, scale: 0.8 },
              animate: { opacity: 1, scale: 1 },
              exit: { opacity: 0, scale: 0.8 },
              transition: { duration: 0.1 },
              className: "absolute inset-0",
              children: /* @__PURE__ */ f(
                G,
                {
                  icon: Js,
                  size: "md",
                  "aria-hidden": !0,
                  color: "positive",
                  className: _(
                    "text-f1-icon-positive opacity-0 transition-opacity duration-300",
                    n && "group-hover:opacity-100 group-focus-visible:opacity-100"
                  )
                }
              )
            },
            "check-icon"
          )
        ] }) })
      ]
    }
  );
}, ac = Kl(
  ({ children: t, className: e, ...n }) => /* @__PURE__ */ w(
    Zs,
    {
      ...n,
      className: _(
        "text-inherit group flex items-center gap-1.5 rounded p-1.5 text-f1-foreground",
        "no-underline hover:bg-f1-background-hover focus-visible:outline focus-visible:outline-2 focus-visible:outline-f1-border-selected-bold active:bg-f1-background-secondary-hover",
        e
      ),
      children: [
        t,
        /* @__PURE__ */ f("div", { className: "grid", children: /* @__PURE__ */ f(G, { "aria-hidden": !0, icon: bl, size: "md" }) })
      ]
    }
  )
);
ac.displayName = "NavigateAction";
const cc = Kl(
  ({ children: t, className: e, href: n, ...r }) => /* @__PURE__ */ w(
    Zs,
    {
      ...r,
      target: "_blank",
      href: n,
      rel: "noopener noreferrer",
      className: _(
        "text-inherit group flex items-center gap-1.5 rounded p-1.5 text-f1-foreground",
        "no-underline hover:bg-f1-background-hover focus-visible:outline focus-visible:outline-2 focus-visible:outline-f1-border-selected-bold active:bg-f1-background-secondary-hover",
        e
      ),
      children: [
        t,
        /* @__PURE__ */ f("div", { className: "grid opacity-0 transition-opacity duration-300 group-hover:opacity-100 group-focus-visible:opacity-100 group-active:opacity-100", children: /* @__PURE__ */ f(
          G,
          {
            "aria-hidden": !0,
            icon: ff,
            size: "md",
            color: "default"
          }
        ) })
      ]
    }
  )
);
cc.displayName = "OpenLinkAction";
const Cn = F(
  (t, e) => {
    const {
      text: n,
      leftIcon: r,
      className: s,
      action: i = { type: "noop" }
    } = t;
    return /* @__PURE__ */ f(
      "li",
      {
        className: "flex rounded font-medium text-f1-foreground *:flex-1",
        ref: e,
        children: /* @__PURE__ */ w(
          Cw,
          {
            action: i,
            className: _("flex items-center gap-1.5 p-1.5", s),
            children: [
              r && (typeof r == "function" ? r({}) : /* @__PURE__ */ f(G, { icon: r, size: "md", "aria-hidden": "true" })),
              /* @__PURE__ */ f("div", { className: "line-clamp-5 flex-1 whitespace-pre-line text-left", children: n })
            ]
          }
        )
      }
    );
  }
);
Cn.displayName = "ItemContainer";
const Cw = ({
  children: t,
  action: e,
  ...n
}) => {
  const r = e.type;
  switch (r) {
    case "copy":
      return /* @__PURE__ */ f(kw, { ...e, ...n, children: t });
    case "navigate":
      return /* @__PURE__ */ f(ac, { ...e, ...n, children: t });
    case "open-link":
      return /* @__PURE__ */ f(cc, { ...e, ...n, children: t });
    case "noop":
      return /* @__PURE__ */ f("div", { ...n, children: t });
    default:
      return r;
  }
}, Er = (t, e) => t && t.type === "copy" ? { type: "copy", text: t.text ?? e } : t, dc = F(
  ({ text: t, icon: e, action: n }, r) => /* @__PURE__ */ f(
    Cn,
    {
      ref: r,
      text: t,
      leftIcon: e,
      action: Er(n, t)
    }
  )
);
dc.displayName = "DataList.Item";
const Sw = Y("DataList.Item", dc), uc = F(
  ({ action: t, avatarUrl: e, firstName: n, lastName: r }, s) => {
    const i = `${n} ${r}`;
    return /* @__PURE__ */ f(
      Cn,
      {
        ref: s,
        leftIcon: () => /* @__PURE__ */ f(
          mt,
          {
            size: "xs",
            src: e,
            firstName: n,
            lastName: r
          }
        ),
        text: i,
        action: Er(t, i)
      }
    );
  }
);
uc.displayName = "PersonItem";
const Nw = Y("PersonItem", uc), fc = F(
  ({ avatarUrl: t, name: e, action: n }, r) => /* @__PURE__ */ f(
    Cn,
    {
      ref: r,
      leftIcon: () => /* @__PURE__ */ f(pu, { name: e, size: "xs", src: t }),
      text: e,
      action: Er(n, e)
    }
  )
);
fc.displayName = "CompanyItem";
const Iw = Y("CompanyItem", fc), hc = F(
  ({ action: t, name: e }, n) => /* @__PURE__ */ f(
    Cn,
    {
      ref: n,
      leftIcon: () => /* @__PURE__ */ f(mu, { name: e, size: "xs" }),
      text: e,
      action: Er(t, e)
    }
  )
);
hc.displayName = "TeamItem";
const _w = Y("TeamItem", hc), pc = F(
  ({ ...t }, e) => /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(wl, { ...t }) })
);
pc.displayName = "DotTagItem";
const Aw = Y("DotTagItem", pc), mc = F(
  ({ ...t }, e) => /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(xl, { ...t }) })
);
mc.displayName = "AlertTagItem";
const Tw = Y("AlertTagItem", mc), gc = F(
  ({ ...t }, e) => /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(gu, { ...t }) })
);
gc.displayName = "BalanceTagItem";
const Ew = Y(
  "BalanceTagItem",
  gc
), bc = F(
  ({ ...t }, e) => /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(yl, { ...t }) })
);
bc.displayName = "StatusTagItem";
const Dw = Y(
  "StatusTagItem",
  bc
), wc = F(
  ({ ...t }, e) => /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(jt, { ...t }) })
);
wc.displayName = "RawTagItem";
const Rw = Y("RawTagItem", wc);
function Lw(t, e) {
  return /* @__PURE__ */ f("li", { ref: e, className: "flex items-start pt-1", children: /* @__PURE__ */ f(hf, { ...t }) });
}
const xc = F(Lw);
xc.displayName = "TagListItem";
const Mw = Y("TagListItem", xc), yc = F(
  ({ children: t, label: e, isHorizontal: n = !1 }, r) => /* @__PURE__ */ w(
    "div",
    {
      className: _(
        n ? "flex min-h-12 flex-1 flex-col py-1.5 pl-3 pr-1.5 xs:flex-row" : "min-w-32 md:max-w-80"
      ),
      children: [
        e && /* @__PURE__ */ f(
          "p",
          {
            className: _(
              "px-1.5 text-f1-foreground-secondary",
              n ? "mt-2 w-44 xs:px-0" : "mb-0.5"
            ),
            children: e
          }
        ),
        /* @__PURE__ */ f("ul", { className: "flex flex-col justify-center gap-0.5", ref: r, children: t })
      ]
    }
  )
);
yc.displayName = "DataList";
const Ow = Y("DataList", yc), Pe = Object.assign(Ow, {
  Item: Sw,
  CompanyItem: Iw,
  PersonItem: Nw,
  TeamItem: _w,
  DotTagItem: Aw,
  AlertTagItem: Tw,
  BalanceTagItem: Ew,
  StatusTagItem: Dw,
  RawTagItem: Rw,
  TagListItem: Mw
}), Fw = ({ content: t }) => /* @__PURE__ */ w(ae, { children: [
  t.type === "weekdays" && /* @__PURE__ */ f("li", { className: "list-none px-1.5 py-1", children: /* @__PURE__ */ f(yw, { ...t }) }),
  t.type === "person" && /* @__PURE__ */ f(Pe.PersonItem, { ...t }),
  t.type === "item" && /* @__PURE__ */ f(Pe.Item, { ...t }),
  t.type === "team" && /* @__PURE__ */ f(Pe.TeamItem, { ...t }),
  t.type === "company" && /* @__PURE__ */ f(Pe.CompanyItem, { ...t }),
  t.type === "dot-tag" && /* @__PURE__ */ f(Pe.DotTagItem, { ...t }),
  t.type === "alert-tag" && /* @__PURE__ */ f(Pe.AlertTagItem, { ...t }),
  t.type === "balance-tag" && /* @__PURE__ */ f(Pe.BalanceTagItem, { ...t }),
  t.type === "status-tag" && /* @__PURE__ */ f(Pe.StatusTagItem, { ...t }),
  t.type === "raw-tag" && /* @__PURE__ */ f(Pe.RawTagItem, { ...t }),
  t.type === "tag-list" && /* @__PURE__ */ f(Pe.TagListItem, { ...t.tagList }),
  t.type === "avatar-list" && /* @__PURE__ */ f("li", { className: "w-fit list-none px-1.5 py-1", children: /* @__PURE__ */ f(ti, { ...t.avatarList }) })
] }), zw = F(
  function({ title: e, content: n, isHorizontal: r = !1, spacingAtTheBottom: s }, i) {
    const o = Array.isArray(n) ? n : [n];
    return /* @__PURE__ */ f(
      "div",
      {
        ref: i,
        className: _(
          "flex flex-col gap-0.5",
          s && !r && "pb-3",
          r && "xs:[&_ul>li]:p-0 [&_ul]:flex-1"
        ),
        children: /* @__PURE__ */ f(Pe, { label: e, isHorizontal: r, children: o.map((l, a) => /* @__PURE__ */ f(Fw, { content: l }, a)) })
      }
    );
  }
), Pw = Ge(
  Y("DetailsItem", zw)
), Bw = F(
  function({ title: e, tableView: n = !1, details: r, dataTestId: s }, i) {
    return /* @__PURE__ */ f(vl, { dataTestId: s, children: /* @__PURE__ */ w("div", { ref: i, className: "flex flex-col gap-4", children: [
      !!e && /* @__PURE__ */ f("p", { className: "mb-1 pl-1.5 text-sm font-semibold text-f1-foreground-secondary", children: e.toLocaleUpperCase() }),
      /* @__PURE__ */ f(
        "div",
        {
          className: _(
            "flex flex-col",
            n ? "rounded-md border border-solid border-f1-border-secondary" : "gap-3"
          ),
          children: r?.map((o, l) => /* @__PURE__ */ w(se.Fragment, { children: [
            /* @__PURE__ */ f(
              Pw,
              {
                title: o.title,
                content: o.content,
                spacingAtTheBottom: o.spacingAtTheBottom,
                isHorizontal: n
              },
              o.title
            ),
            n && l !== r.length - 1 && /* @__PURE__ */ f("div", { className: "h-[1px] w-full bg-f1-border-secondary" })
          ] }, o.title))
        }
      )
    ] }) });
  }
), tk = Y(
  "DetailsItemsList",
  Bw
), vc = se.forwardRef(({ person: t, onClick: e, ...n }, r) => {
  const s = () => {
    e();
  };
  return /* @__PURE__ */ w(
    "div",
    {
      ref: r,
      className: _(
        "flex w-full flex-row flex-wrap items-center gap-2 rounded-md border p-2 hover:bg-f1-background-hover focus:outline focus:outline-1 focus:outline-offset-1 focus:outline-f1-border-selected-bold",
        n.withPointerCursor && "cursor-pointer"
      ),
      onClick: s,
      children: [
        /* @__PURE__ */ f(
          mt,
          {
            firstName: t.firstName,
            lastName: t.lastName,
            src: t.avatarUrl,
            badge: t.avatarBadge
          }
        ),
        /* @__PURE__ */ w("div", { className: "flex flex-1 flex-col", children: [
          /* @__PURE__ */ w("div", { className: "flex flex-1 flex-row items-center gap-1", children: [
            /* @__PURE__ */ f("span", { className: "truncate font-medium", children: `${t.firstName} ${t.lastName}` }),
            n.info && /* @__PURE__ */ f(kr, { label: n.info, children: /* @__PURE__ */ f(
              G,
              {
                icon: hl,
                size: "sm",
                className: "text-f1-icon-secondary"
              }
            ) })
          ] }),
          "bottomTags" in n && /* @__PURE__ */ f("div", { className: "-ml-1.5 flex flex-row items-center [&>div]:-mr-1", children: n.bottomTags.map((i, o) => /* @__PURE__ */ w(ae, { children: [
            /* @__PURE__ */ f(jt, { ...i }, i.text),
            o < n.bottomTags.length - 1 && /* @__PURE__ */ f("span", { children: "·" })
          ] })) }),
          "description" in n && n.description && /* @__PURE__ */ f("p", { className: "truncate text-f1-foreground-secondary", children: n.description })
        ] }),
        /* @__PURE__ */ w("div", { className: "flex flex-row items-center justify-between gap-2", children: [
          "rightTag" in n && n.rightTag && /* @__PURE__ */ f(wl, { ...n.rightTag }),
          "actions" in n && /* @__PURE__ */ w("div", { className: "flex flex-1 flex-row items-center justify-end gap-2", children: [
            n.actions?.primary && /* @__PURE__ */ f(
              $,
              {
                variant: "outline",
                onClick: n.actions.primary.onClick,
                label: n.actions.primary.label,
                icon: n.actions.primary.icon
              }
            ),
            n.actions?.secondary && /* @__PURE__ */ f(
              $,
              {
                variant: "outline",
                onClick: n.actions.secondary.onClick,
                label: "Secondary",
                icon: n.actions.secondary.icon,
                hideLabel: !0
              }
            )
          ] })
        ] })
      ]
    }
  );
}), $w = () => /* @__PURE__ */ w("div", { className: "flex w-full flex-row flex-wrap items-center gap-2 rounded-md border p-2 hover:bg-f1-background-hover focus:outline focus:outline-1 focus:outline-offset-1 focus:outline-f1-border-selected-bold", children: [
  /* @__PURE__ */ f(A, { className: "aspect-square w-8 rounded-full" }),
  /* @__PURE__ */ w("div", { className: "flex flex-1 flex-col gap-0.5", children: [
    /* @__PURE__ */ f(A, { className: "h-4" }),
    /* @__PURE__ */ f(A, { className: "h-4" })
  ] })
] });
vc.displayName = "OnePersonListItem";
const nk = Ge(
  Y(
    "OnePersonListItem",
    Oe(vc, $w)
  )
), Hw = {
  waiting: "neutral",
  pending: "neutral",
  approved: "positive",
  rejected: "critical"
}, Io = {
  approved: {
    icon: pf,
    type: "positive",
    size: "sm"
  },
  rejected: {
    icon: Sr,
    type: "critical",
    size: "sm"
  }
}, jw = {
  icon: mf,
  type: "neutral",
  size: "sm"
}, Vw = {
  positive: 4,
  highlight: 3,
  critical: 2,
  warning: 1,
  neutral: 0
}, Uw = (t) => t in Io ? Io[t] : jw;
function _o(t) {
  return Vw[t ?? "neutral"] ?? 0;
}
const Ww = ({
  title: t,
  approvalsRequired: e = 1,
  status: n,
  approvers: r
}) => {
  const s = ot(), i = e === 1 ? s.approvals.requiredNumbers.one : s.approvals.requiredNumbers.other.replace(
    "{{count}}",
    e.toString()
  ), o = s.approvals.statuses[n], l = we(() => r.map((a) => {
    const c = Uw(a.status);
    return {
      firstName: a.firstName,
      lastName: a.lastName,
      src: a.avatar,
      badge: c
    };
  }).sort(
    (a, c) => _o(c.badge?.type) - _o(a.badge?.type)
  ), [r]);
  return /* @__PURE__ */ w("div", { className: "flex flex-col gap-3 pb-5 pl-4 pr-3 pt-3", children: [
    /* @__PURE__ */ w("div", { className: "flex flex-row items-start justify-between", children: [
      /* @__PURE__ */ w("div", { children: [
        /* @__PURE__ */ f("p", { className: "font-medium text-f1-foreground", children: t }),
        /* @__PURE__ */ f("p", { className: "text-f1-foreground-secondary", children: i })
      ] }),
      /* @__PURE__ */ f(yl, { text: o, variant: Hw[n] })
    ] }),
    /* @__PURE__ */ f("div", { className: "w-full", children: /* @__PURE__ */ f(ti, { avatars: l, layout: "fill", type: "person", size: "md" }) })
  ] });
}, Gw = ({ steps: t }) => {
  const n = ot().approvals.history, r = t.findIndex((s) => s.status === "pending");
  return /* @__PURE__ */ w("div", { className: "flex w-full flex-col gap-4", children: [
    /* @__PURE__ */ f("h2", { className: "mb-2 text-lg font-semibold text-f1-foreground", children: n }),
    /* @__PURE__ */ w("div", { className: "flex flex-row gap-4", children: [
      /* @__PURE__ */ f("div", { className: "mt-3.5 flex flex-col items-center", children: t.map((s, i) => /* @__PURE__ */ w("div", { className: "flex flex-col items-center", children: [
        /* @__PURE__ */ f(
          "div",
          {
            className: _(
              "flex size-5 items-center justify-center rounded-xs text-sm font-medium",
              i < r ? "bg-f1-background-selected-bold text-f1-foreground-inverse" : "border border-solid border-f1-border-secondary bg-f1-background-secondary text-f1-foreground"
            ),
            children: /* @__PURE__ */ f("span", { children: i + 1 })
          }
        ),
        i !== t.length - 1 && /* @__PURE__ */ f("div", { className: "h-[96px] w-px bg-f1-border-secondary" })
      ] }, s.title)) }),
      /* @__PURE__ */ f("div", { className: "flex w-full flex-col rounded-xl border border-solid border-f1-border-secondary", children: t.map((s, i) => /* @__PURE__ */ w(ae, { children: [
        /* @__PURE__ */ f(
          Ww,
          {
            title: s.title,
            approvalsRequired: s.approvalsRequired,
            status: s.status,
            approvers: s.approvers
          },
          s.title
        ),
        i !== t.length - 1 && /* @__PURE__ */ f("div", { className: "h-px w-full bg-f1-border-secondary" })
      ] })) })
    ] })
  ] });
}, rk = Ge(
  Y("OneApprovalHistory", Gw)
), je = () => /* @__PURE__ */ new Map(), As = (t) => {
  const e = je();
  return t.forEach((n, r) => {
    e.set(r, n);
  }), e;
}, at = (t, e, n) => {
  let r = t.get(e);
  return r === void 0 && t.set(e, r = n()), r;
}, Kw = (t, e) => {
  const n = [];
  for (const [r, s] of t)
    n.push(e(s, r));
  return n;
}, qw = (t, e) => {
  for (const [n, r] of t)
    if (e(r, n))
      return !0;
  return !1;
}, It = () => /* @__PURE__ */ new Set(), Zr = (t) => t[t.length - 1], Yw = (t, e) => {
  for (let n = 0; n < e.length; n++)
    t.push(e[n]);
}, _t = Array.from, Jw = (t, e) => {
  for (let n = 0; n < t.length; n++)
    if (e(t[n], n, t))
      return !0;
  return !1;
}, Ts = Array.isArray;
class kc {
  constructor() {
    this._observers = je();
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  on(e, n) {
    return at(
      this._observers,
      /** @type {string} */
      e,
      It
    ).add(n), n;
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  once(e, n) {
    const r = (...s) => {
      this.off(
        e,
        /** @type {any} */
        r
      ), n(...s);
    };
    this.on(
      e,
      /** @type {any} */
      r
    );
  }
  /**
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name
   * @param {EVENTS[NAME]} f
   */
  off(e, n) {
    const r = this._observers.get(e);
    r !== void 0 && (r.delete(n), r.size === 0 && this._observers.delete(e));
  }
  /**
   * Emit a named event. All registered event listeners that listen to the
   * specified name will receive the event.
   *
   * @todo This should catch exceptions
   *
   * @template {keyof EVENTS & string} NAME
   * @param {NAME} name The event name.
   * @param {Parameters<EVENTS[NAME]>} args The arguments that are applied to the event listener.
   */
  emit(e, n) {
    return _t((this._observers.get(e) || je()).values()).forEach((r) => r(...n));
  }
  destroy() {
    this._observers = je();
  }
}
const it = Math.floor, Jn = Math.abs, Vt = (t, e) => t < e ? t : e, gt = (t, e) => t > e ? t : e, Cc = (t) => t !== 0 ? t < 0 : 1 / t < 0, Ao = 1, To = 2, Qr = 4, es = 8, cn = 32, rt = 64, xe = 128, Xw = 1 << 29, Dr = 31, Es = 63, St = 127, Zw = 2147483647, Sc = Number.MAX_SAFE_INTEGER, Qw = Number.isInteger || ((t) => typeof t == "number" && isFinite(t) && it(t) === t), ex = String.fromCharCode, tx = (t) => t.toLowerCase(), nx = /^\s*/g, rx = (t) => t.replace(nx, ""), sx = /([A-Z])/g, Eo = (t, e) => rx(t.replace(sx, (n) => `${e}${tx(n)}`)), ix = (t) => {
  const e = unescape(encodeURIComponent(t)), n = e.length, r = new Uint8Array(n);
  for (let s = 0; s < n; s++)
    r[s] = /** @type {number} */
    e.codePointAt(s);
  return r;
}, dn = (
  /** @type {TextEncoder} */
  typeof TextEncoder < "u" ? new TextEncoder() : null
), ox = (t) => dn.encode(t), lx = dn ? ox : ix;
let nn = typeof TextDecoder > "u" ? null : new TextDecoder("utf-8", { fatal: !0, ignoreBOM: !0 });
nn && nn.decode(new Uint8Array()).length === 1 && (nn = null);
class Sn {
  constructor() {
    this.cpos = 0, this.cbuf = new Uint8Array(100), this.bufs = [];
  }
}
const Nn = () => new Sn(), ax = (t) => {
  const e = Nn();
  return t(e), He(e);
}, cx = (t) => {
  let e = t.cpos;
  for (let n = 0; n < t.bufs.length; n++)
    e += t.bufs[n].length;
  return e;
}, He = (t) => {
  const e = new Uint8Array(cx(t));
  let n = 0;
  for (let r = 0; r < t.bufs.length; r++) {
    const s = t.bufs[r];
    e.set(s, n), n += s.length;
  }
  return e.set(new Uint8Array(t.cbuf.buffer, 0, t.cpos), n), e;
}, dx = (t, e) => {
  const n = t.cbuf.length;
  n - t.cpos < e && (t.bufs.push(new Uint8Array(t.cbuf.buffer, 0, t.cpos)), t.cbuf = new Uint8Array(gt(n, e) * 2), t.cpos = 0);
}, oe = (t, e) => {
  const n = t.cbuf.length;
  t.cpos === n && (t.bufs.push(t.cbuf), t.cbuf = new Uint8Array(n * 2), t.cpos = 0), t.cbuf[t.cpos++] = e;
}, Ds = oe, B = (t, e) => {
  for (; e > St; )
    oe(t, xe | St & e), e = it(e / 128);
  oe(t, St & e);
}, ui = (t, e) => {
  const n = Cc(e);
  for (n && (e = -e), oe(t, (e > Es ? xe : 0) | (n ? rt : 0) | Es & e), e = it(e / 64); e > 0; )
    oe(t, (e > St ? xe : 0) | St & e), e = it(e / 128);
}, Rs = new Uint8Array(3e4), ux = Rs.length / 3, fx = (t, e) => {
  if (e.length < ux) {
    const n = dn.encodeInto(e, Rs).written || 0;
    B(t, n);
    for (let r = 0; r < n; r++)
      oe(t, Rs[r]);
  } else
    Ie(t, lx(e));
}, hx = (t, e) => {
  const n = unescape(encodeURIComponent(e)), r = n.length;
  B(t, r);
  for (let s = 0; s < r; s++)
    oe(
      t,
      /** @type {number} */
      n.codePointAt(s)
    );
}, zt = dn && /** @type {any} */
dn.encodeInto ? fx : hx, Rr = (t, e) => {
  const n = t.cbuf.length, r = t.cpos, s = Vt(n - r, e.length), i = e.length - s;
  t.cbuf.set(e.subarray(0, s), r), t.cpos += s, i > 0 && (t.bufs.push(t.cbuf), t.cbuf = new Uint8Array(gt(n * 2, i)), t.cbuf.set(e.subarray(s)), t.cpos = i);
}, Ie = (t, e) => {
  B(t, e.byteLength), Rr(t, e);
}, fi = (t, e) => {
  dx(t, e);
  const n = new DataView(t.cbuf.buffer, t.cpos, e);
  return t.cpos += e, n;
}, px = (t, e) => fi(t, 4).setFloat32(0, e, !1), mx = (t, e) => fi(t, 8).setFloat64(0, e, !1), gx = (t, e) => (
  /** @type {any} */
  fi(t, 8).setBigInt64(0, e, !1)
), Do = new DataView(new ArrayBuffer(4)), bx = (t) => (Do.setFloat32(0, t), Do.getFloat32(0) === t), Ut = (t, e) => {
  switch (typeof e) {
    case "string":
      oe(t, 119), zt(t, e);
      break;
    case "number":
      Qw(e) && Jn(e) <= Zw ? (oe(t, 125), ui(t, e)) : bx(e) ? (oe(t, 124), px(t, e)) : (oe(t, 123), mx(t, e));
      break;
    case "bigint":
      oe(t, 122), gx(t, e);
      break;
    case "object":
      if (e === null)
        oe(t, 126);
      else if (Ts(e)) {
        oe(t, 117), B(t, e.length);
        for (let n = 0; n < e.length; n++)
          Ut(t, e[n]);
      } else if (e instanceof Uint8Array)
        oe(t, 116), Ie(t, e);
      else {
        oe(t, 118);
        const n = Object.keys(e);
        B(t, n.length);
        for (let r = 0; r < n.length; r++) {
          const s = n[r];
          zt(t, s), Ut(t, e[s]);
        }
      }
      break;
    case "boolean":
      oe(t, e ? 120 : 121);
      break;
    default:
      oe(t, 127);
  }
};
class Ro extends Sn {
  /**
   * @param {function(Encoder, T):void} writer
   */
  constructor(e) {
    super(), this.w = e, this.s = null, this.count = 0;
  }
  /**
   * @param {T} v
   */
  write(e) {
    this.s === e ? this.count++ : (this.count > 0 && B(this, this.count - 1), this.count = 1, this.w(this, e), this.s = e);
  }
}
const Lo = (t) => {
  t.count > 0 && (ui(t.encoder, t.count === 1 ? t.s : -t.s), t.count > 1 && B(t.encoder, t.count - 2));
};
class Xn {
  constructor() {
    this.encoder = new Sn(), this.s = 0, this.count = 0;
  }
  /**
   * @param {number} v
   */
  write(e) {
    this.s === e ? this.count++ : (Lo(this), this.count = 1, this.s = e);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return Lo(this), He(this.encoder);
  }
}
const Mo = (t) => {
  if (t.count > 0) {
    const e = t.diff * 2 + (t.count === 1 ? 0 : 1);
    ui(t.encoder, e), t.count > 1 && B(t.encoder, t.count - 2);
  }
};
class ts {
  constructor() {
    this.encoder = new Sn(), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @param {number} v
   */
  write(e) {
    this.diff === e - this.s ? (this.s = e, this.count++) : (Mo(this), this.count = 1, this.diff = e - this.s, this.s = e);
  }
  /**
   * Flush the encoded state and transform this to a Uint8Array.
   *
   * Note that this should only be called once.
   */
  toUint8Array() {
    return Mo(this), He(this.encoder);
  }
}
class wx {
  constructor() {
    this.sarr = [], this.s = "", this.lensE = new Xn();
  }
  /**
   * @param {string} string
   */
  write(e) {
    this.s += e, this.s.length > 19 && (this.sarr.push(this.s), this.s = ""), this.lensE.write(e.length);
  }
  toUint8Array() {
    const e = new Sn();
    return this.sarr.push(this.s), this.s = "", zt(e, this.sarr.join("")), Rr(e, this.lensE.toUint8Array()), He(e);
  }
}
const bt = (t) => new Error(t), Ve = () => {
  throw bt("Method unimplemented");
}, Se = () => {
  throw bt("Unexpected case");
}, Nc = bt("Unexpected end of array"), Ic = bt("Integer out of Range");
class Lr {
  /**
   * @param {Uint8Array} uint8Array Binary data to decode
   */
  constructor(e) {
    this.arr = e, this.pos = 0;
  }
}
const hi = (t) => new Lr(t), xx = (t) => t.pos !== t.arr.length, yx = (t, e) => {
  const n = new Uint8Array(t.arr.buffer, t.pos + t.arr.byteOffset, e);
  return t.pos += e, n;
}, Te = (t) => yx(t, q(t)), un = (t) => t.arr[t.pos++], q = (t) => {
  let e = 0, n = 1;
  const r = t.arr.length;
  for (; t.pos < r; ) {
    const s = t.arr[t.pos++];
    if (e = e + (s & St) * n, n *= 128, s < xe)
      return e;
    if (e > Sc)
      throw Ic;
  }
  throw Nc;
}, pi = (t) => {
  let e = t.arr[t.pos++], n = e & Es, r = 64;
  const s = (e & rt) > 0 ? -1 : 1;
  if ((e & xe) === 0)
    return s * n;
  const i = t.arr.length;
  for (; t.pos < i; ) {
    if (e = t.arr[t.pos++], n = n + (e & St) * r, r *= 128, e < xe)
      return s * n;
    if (n > Sc)
      throw Ic;
  }
  throw Nc;
}, vx = (t) => {
  let e = q(t);
  if (e === 0)
    return "";
  {
    let n = String.fromCodePoint(un(t));
    if (--e < 100)
      for (; e--; )
        n += String.fromCodePoint(un(t));
    else
      for (; e > 0; ) {
        const r = e < 1e4 ? e : 1e4, s = t.arr.subarray(t.pos, t.pos + r);
        t.pos += r, n += String.fromCodePoint.apply(
          null,
          /** @type {any} */
          s
        ), e -= r;
      }
    return decodeURIComponent(escape(n));
  }
}, kx = (t) => (
  /** @type any */
  nn.decode(Te(t))
), Ls = nn ? kx : vx, mi = (t, e) => {
  const n = new DataView(t.arr.buffer, t.arr.byteOffset + t.pos, e);
  return t.pos += e, n;
}, Cx = (t) => mi(t, 4).getFloat32(0, !1), Sx = (t) => mi(t, 8).getFloat64(0, !1), Nx = (t) => (
  /** @type {any} */
  mi(t, 8).getBigInt64(0, !1)
), Ix = [
  (t) => {
  },
  // CASE 127: undefined
  (t) => null,
  // CASE 126: null
  pi,
  // CASE 125: integer
  Cx,
  // CASE 124: float32
  Sx,
  // CASE 123: float64
  Nx,
  // CASE 122: bigint
  (t) => !1,
  // CASE 121: boolean (false)
  (t) => !0,
  // CASE 120: boolean (true)
  Ls,
  // CASE 119: string
  (t) => {
    const e = q(t), n = {};
    for (let r = 0; r < e; r++) {
      const s = Ls(t);
      n[s] = cr(t);
    }
    return n;
  },
  (t) => {
    const e = q(t), n = [];
    for (let r = 0; r < e; r++)
      n.push(cr(t));
    return n;
  },
  Te
  // CASE 116: Uint8Array
], cr = (t) => Ix[127 - un(t)](t);
class Oo extends Lr {
  /**
   * @param {Uint8Array} uint8Array
   * @param {function(Decoder):T} reader
   */
  constructor(e, n) {
    super(e), this.reader = n, this.s = null, this.count = 0;
  }
  read() {
    return this.count === 0 && (this.s = this.reader(this), xx(this) ? this.count = q(this) + 1 : this.count = -1), this.count--, /** @type {T} */
    this.s;
  }
}
class Zn extends Lr {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    super(e), this.s = 0, this.count = 0;
  }
  read() {
    if (this.count === 0) {
      this.s = pi(this);
      const e = Cc(this.s);
      this.count = 1, e && (this.s = -this.s, this.count = q(this) + 2);
    }
    return this.count--, /** @type {number} */
    this.s;
  }
}
class ns extends Lr {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    super(e), this.s = 0, this.count = 0, this.diff = 0;
  }
  /**
   * @return {number}
   */
  read() {
    if (this.count === 0) {
      const e = pi(this), n = e & 1;
      this.diff = it(e / 2), this.count = 1, n && (this.count = q(this) + 2);
    }
    return this.s += this.diff, this.count--, this.s;
  }
}
class _x {
  /**
   * @param {Uint8Array} uint8Array
   */
  constructor(e) {
    this.decoder = new Zn(e), this.str = Ls(this.decoder), this.spos = 0;
  }
  /**
   * @return {string}
   */
  read() {
    const e = this.spos + this.decoder.read(), n = this.str.slice(this.spos, e);
    return this.spos = e, n;
  }
}
const Ax = crypto.getRandomValues.bind(crypto), Tx = Math.random, _c = () => Ax(new Uint32Array(1))[0], Ex = (t) => t[it(Tx() * t.length)], Dx = "10000000-1000-4000-8000" + -1e11, Rx = () => Dx.replace(
  /[018]/g,
  /** @param {number} c */
  (t) => (t ^ _c() & 15 >> t / 4).toString(16)
), Lx = Date.now, Fo = (t) => (
  /** @type {Promise<T>} */
  new Promise(t)
);
Promise.all.bind(Promise);
const zo = (t) => t === void 0 ? null : t;
class Mx {
  constructor() {
    this.map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {string} key
   * @param {any} newValue
   */
  setItem(e, n) {
    this.map.set(e, n);
  }
  /**
   * @param {string} key
   */
  getItem(e) {
    return this.map.get(e);
  }
}
let Ac = new Mx(), Ox = !0;
try {
  typeof localStorage < "u" && localStorage && (Ac = localStorage, Ox = !1);
} catch {
}
const Fx = Ac, zx = Object.assign, Tc = Object.keys, Px = (t, e) => {
  for (const n in t)
    e(t[n], n);
}, Po = (t) => Tc(t).length, Bx = (t) => {
  for (const e in t)
    return !1;
  return !0;
}, Ec = (t, e) => {
  for (const n in t)
    if (!e(t[n], n))
      return !1;
  return !0;
}, $x = (t, e) => Object.prototype.hasOwnProperty.call(t, e), Hx = (t, e) => t === e || Po(t) === Po(e) && Ec(t, (n, r) => (n !== void 0 || $x(e, r)) && e[r] === n), jx = Object.freeze, Dc = (t) => {
  for (const e in t) {
    const n = t[e];
    (typeof n == "object" || typeof n == "function") && Dc(t[e]);
  }
  return jx(t);
}, gi = (t, e, n = 0) => {
  try {
    for (; n < t.length; n++)
      t[n](...e);
  } finally {
    n < t.length && gi(t, e, n + 1);
  }
}, Vx = (t, e) => e.includes(t), Wt = typeof process < "u" && process.release && /node|io\.js/.test(process.release.name) && Object.prototype.toString.call(typeof process < "u" ? process : 0) === "[object process]", Rc = typeof window < "u" && typeof document < "u" && !Wt;
let Ye;
const Ux = () => {
  if (Ye === void 0)
    if (Wt) {
      Ye = je();
      const t = process.argv;
      let e = null;
      for (let n = 0; n < t.length; n++) {
        const r = t[n];
        r[0] === "-" ? (e !== null && Ye.set(e, ""), e = r) : e !== null && (Ye.set(e, r), e = null);
      }
      e !== null && Ye.set(e, "");
    } else typeof location == "object" ? (Ye = je(), (location.search || "?").slice(1).split("&").forEach((t) => {
      if (t.length !== 0) {
        const [e, n] = t.split("=");
        Ye.set(`--${Eo(e, "-")}`, n), Ye.set(`-${Eo(e, "-")}`, n);
      }
    })) : Ye = je();
  return Ye;
}, Ms = (t) => Ux().has(t), dr = (t) => zo(Wt ? process.env[t.toUpperCase().replaceAll("-", "_")] : Fx.getItem(t)), Lc = (t) => Ms("--" + t) || dr(t) !== null;
Lc("production");
const Wx = Wt && Vx(process.env.FORCE_COLOR, ["true", "1", "2"]), Gx = Wx || !Ms("--no-colors") && // @todo deprecate --no-colors
!Lc("no-color") && (!Wt || process.stdout.isTTY) && (!Wt || Ms("--color") || dr("COLORTERM") !== null || (dr("TERM") || "").includes("color")), Kx = (t) => {
  let e = "";
  for (let n = 0; n < t.byteLength; n++)
    e += ex(t[n]);
  return btoa(e);
}, qx = (t) => Buffer.from(t.buffer, t.byteOffset, t.byteLength).toString("base64"), Yx = Rc ? Kx : qx, Jx = (t) => ax((e) => Ut(e, t));
class Xx {
  /**
   * @param {L} left
   * @param {R} right
   */
  constructor(e, n) {
    this.left = e, this.right = n;
  }
}
const tt = (t, e) => new Xx(t, e), Zx = (
  /** @type {Document} */
  typeof document < "u" ? document : {}
);
typeof DOMParser < "u" && new DOMParser();
const Qx = (t) => Kw(t, (e, n) => `${n}:${e};`).join(""), e0 = (t) => class {
  /**
   * @param {number} timeoutId
   */
  constructor(n) {
    this._ = n;
  }
  destroy() {
    t(this._);
  }
}, t0 = e0(clearTimeout), Mc = (t, e) => new t0(setTimeout(e, t)), ct = Symbol, Oc = ct(), Fc = ct(), n0 = ct(), r0 = ct(), s0 = ct(), zc = ct(), i0 = ct(), bi = ct(), o0 = ct(), l0 = (t) => {
  t.length === 1 && t[0]?.constructor === Function && (t = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  t[0]());
  const e = [], n = [];
  let r = 0;
  for (; r < t.length; r++) {
    const s = t[r];
    if (s === void 0)
      break;
    if (s.constructor === String || s.constructor === Number)
      e.push(s);
    else if (s.constructor === Object)
      break;
  }
  for (r > 0 && n.push(e.join("")); r < t.length; r++) {
    const s = t[r];
    s instanceof Symbol || n.push(s);
  }
  return n;
}, a0 = {
  [Oc]: tt("font-weight", "bold"),
  [Fc]: tt("font-weight", "normal"),
  [n0]: tt("color", "blue"),
  [s0]: tt("color", "green"),
  [r0]: tt("color", "grey"),
  [zc]: tt("color", "red"),
  [i0]: tt("color", "purple"),
  [bi]: tt("color", "orange"),
  // not well supported in chrome when debugging node with inspector - TODO: deprecate
  [o0]: tt("color", "black")
}, c0 = (t) => {
  t.length === 1 && t[0]?.constructor === Function && (t = /** @type {Array<string|Symbol|Object|number>} */
  /** @type {[function]} */
  t[0]());
  const e = [], n = [], r = je();
  let s = [], i = 0;
  for (; i < t.length; i++) {
    const o = t[i], l = a0[o];
    if (l !== void 0)
      r.set(l.left, l.right);
    else {
      if (o === void 0)
        break;
      if (o.constructor === String || o.constructor === Number) {
        const a = Qx(r);
        i > 0 || a.length > 0 ? (e.push("%c" + o), n.push(a)) : e.push(o);
      } else
        break;
    }
  }
  for (i > 0 && (s = n, s.unshift(e.join(""))); i < t.length; i++) {
    const o = t[i];
    o instanceof Symbol || s.push(o);
  }
  return s;
}, Pc = Gx ? c0 : l0, d0 = (...t) => {
  console.log(...Pc(t)), $c.forEach((e) => e.print(t));
}, Bc = (...t) => {
  console.warn(...Pc(t)), t.unshift(bi), $c.forEach((e) => e.print(t));
}, $c = It(), Hc = (t) => ({
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return this;
  },
  // @ts-ignore
  next: t
}), u0 = (t, e) => Hc(() => {
  let n;
  do
    n = t.next();
  while (!n.done && !e(n.value));
  return n;
}), rs = (t, e) => Hc(() => {
  const { done: n, value: r } = t.next();
  return { done: n, value: n ? void 0 : e(r) };
});
class wi {
  /**
   * @param {number} clock
   * @param {number} len
   */
  constructor(e, n) {
    this.clock = e, this.len = n;
  }
}
class Yt {
  constructor() {
    this.clients = /* @__PURE__ */ new Map();
  }
}
const wt = (t, e, n) => e.clients.forEach((r, s) => {
  const i = (
    /** @type {Array<GC|Item>} */
    t.doc.store.clients.get(s)
  );
  if (i != null) {
    const o = i[i.length - 1], l = o.id.clock + o.length;
    for (let a = 0, c = r[a]; a < r.length && c.clock < l; c = r[++a])
      Yc(t, i, c.clock, c.len, n);
  }
}), f0 = (t, e) => {
  let n = 0, r = t.length - 1;
  for (; n <= r; ) {
    const s = it((n + r) / 2), i = t[s], o = i.clock;
    if (o <= e) {
      if (e < o + i.len)
        return s;
      n = s + 1;
    } else
      r = s - 1;
  }
  return null;
}, Jt = (t, e) => {
  const n = t.clients.get(e.client);
  return n !== void 0 && f0(n, e.clock) !== null;
}, xi = (t) => {
  t.clients.forEach((e) => {
    e.sort((s, i) => s.clock - i.clock);
    let n, r;
    for (n = 1, r = 1; n < e.length; n++) {
      const s = e[r - 1], i = e[n];
      s.clock + s.len >= i.clock ? s.len = gt(s.len, i.clock + i.len - s.clock) : (r < n && (e[r] = i), r++);
    }
    e.length = r;
  });
}, Os = (t) => {
  const e = new Yt();
  for (let n = 0; n < t.length; n++)
    t[n].clients.forEach((r, s) => {
      if (!e.clients.has(s)) {
        const i = r.slice();
        for (let o = n + 1; o < t.length; o++)
          Yw(i, t[o].clients.get(s) || []);
        e.clients.set(s, i);
      }
    });
  return xi(e), e;
}, fn = (t, e, n, r) => {
  at(t.clients, e, () => (
    /** @type {Array<DeleteItem>} */
    []
  )).push(new wi(n, r));
}, jc = () => new Yt(), h0 = (t) => {
  const e = jc();
  return t.clients.forEach((n, r) => {
    const s = [];
    for (let i = 0; i < n.length; i++) {
      const o = n[i];
      if (o.deleted) {
        const l = o.id.clock;
        let a = o.length;
        if (i + 1 < n.length)
          for (let c = n[i + 1]; i + 1 < n.length && c.deleted; c = n[++i + 1])
            a += c.length;
        s.push(new wi(l, a));
      }
    }
    s.length > 0 && e.clients.set(r, s);
  }), e;
}, yi = (t, e) => {
  B(t.restEncoder, e.clients.size), _t(e.clients.entries()).sort((n, r) => r[0] - n[0]).forEach(([n, r]) => {
    t.resetDsCurVal(), B(t.restEncoder, n);
    const s = r.length;
    B(t.restEncoder, s);
    for (let i = 0; i < s; i++) {
      const o = r[i];
      t.writeDsClock(o.clock), t.writeDsLen(o.len);
    }
  });
}, p0 = (t) => {
  const e = new Yt(), n = q(t.restDecoder);
  for (let r = 0; r < n; r++) {
    t.resetDsCurVal();
    const s = q(t.restDecoder), i = q(t.restDecoder);
    if (i > 0) {
      const o = at(e.clients, s, () => (
        /** @type {Array<DeleteItem>} */
        []
      ));
      for (let l = 0; l < i; l++)
        o.push(new wi(t.readDsClock(), t.readDsLen()));
    }
  }
  return e;
}, Bo = (t, e, n) => {
  const r = new Yt(), s = q(t.restDecoder);
  for (let i = 0; i < s; i++) {
    t.resetDsCurVal();
    const o = q(t.restDecoder), l = q(t.restDecoder), a = n.clients.get(o) || [], c = te(n, o);
    for (let u = 0; u < l; u++) {
      const d = t.readDsClock(), h = d + t.readDsLen();
      if (d < c) {
        c < h && fn(r, o, c, h - c);
        let p = Ue(a, d), m = a[p];
        for (!m.deleted && m.id.clock < d && (a.splice(p + 1, 0, br(e, m, d - m.id.clock)), p++); p < a.length && (m = a[p++], m.id.clock < h); )
          m.deleted || (h < m.id.clock + m.length && a.splice(p, 0, br(e, m, h - m.id.clock)), m.delete(e));
      } else
        fn(r, o, d, h - d);
    }
  }
  if (r.clients.size > 0) {
    const i = new Mr();
    return B(i.restEncoder, 0), yi(i, r), i.toUint8Array();
  }
  return null;
}, Vc = _c;
class Rt extends kc {
  /**
   * @param {DocOpts} opts configuration
   */
  constructor({ guid: e = Rx(), collectionid: n = null, gc: r = !0, gcFilter: s = () => !0, meta: i = null, autoLoad: o = !1, shouldLoad: l = !0 } = {}) {
    super(), this.gc = r, this.gcFilter = s, this.clientID = Vc(), this.guid = e, this.collectionid = n, this.share = /* @__PURE__ */ new Map(), this.store = new Kc(), this._transaction = null, this._transactionCleanups = [], this.subdocs = /* @__PURE__ */ new Set(), this._item = null, this.shouldLoad = l, this.autoLoad = o, this.meta = i, this.isLoaded = !1, this.isSynced = !1, this.isDestroyed = !1, this.whenLoaded = Fo((c) => {
      this.on("load", () => {
        this.isLoaded = !0, c(this);
      });
    });
    const a = () => Fo((c) => {
      const u = (d) => {
        (d === void 0 || d === !0) && (this.off("sync", u), c());
      };
      this.on("sync", u);
    });
    this.on("sync", (c) => {
      c === !1 && this.isSynced && (this.whenSynced = a()), this.isSynced = c === void 0 || c === !0, this.isSynced && !this.isLoaded && this.emit("load", [this]);
    }), this.whenSynced = a();
  }
  /**
   * Notify the parent document that you request to load data into this subdocument (if it is a subdocument).
   *
   * `load()` might be used in the future to request any provider to load the most current data.
   *
   * It is safe to call `load()` multiple times.
   */
  load() {
    const e = this._item;
    e !== null && !this.shouldLoad && j(
      /** @type {any} */
      e.parent.doc,
      (n) => {
        n.subdocsLoaded.add(this);
      },
      null,
      !0
    ), this.shouldLoad = !0;
  }
  getSubdocs() {
    return this.subdocs;
  }
  getSubdocGuids() {
    return new Set(_t(this.subdocs).map((e) => e.guid));
  }
  /**
   * Changes that happen inside of a transaction are bundled. This means that
   * the observer fires _after_ the transaction is finished and that all changes
   * that happened inside of the transaction are sent as one message to the
   * other peers.
   *
   * @template T
   * @param {function(Transaction):T} f The function that should be executed as a transaction
   * @param {any} [origin] Origin of who started the transaction. Will be stored on transaction.origin
   * @return T
   *
   * @public
   */
  transact(e, n = null) {
    return j(this, e, n);
  }
  /**
   * Define a shared data type.
   *
   * Multiple calls of `ydoc.get(name, TypeConstructor)` yield the same result
   * and do not overwrite each other. I.e.
   * `ydoc.get(name, Y.Array) === ydoc.get(name, Y.Array)`
   *
   * After this method is called, the type is also available on `ydoc.share.get(name)`.
   *
   * *Best Practices:*
   * Define all types right after the Y.Doc instance is created and store them in a separate object.
   * Also use the typed methods `getText(name)`, `getArray(name)`, ..
   *
   * @template {typeof AbstractType<any>} Type
   * @example
   *   const ydoc = new Y.Doc(..)
   *   const appState = {
   *     document: ydoc.getText('document')
   *     comments: ydoc.getArray('comments')
   *   }
   *
   * @param {string} name
   * @param {Type} TypeConstructor The constructor of the type definition. E.g. Y.Text, Y.Array, Y.Map, ...
   * @return {InstanceType<Type>} The created type. Constructed with TypeConstructor
   *
   * @public
   */
  get(e, n = (
    /** @type {any} */
    ce
  )) {
    const r = at(this.share, e, () => {
      const i = new n();
      return i._integrate(this, null), i;
    }), s = r.constructor;
    if (n !== ce && s !== n)
      if (s === ce) {
        const i = new n();
        i._map = r._map, r._map.forEach(
          /** @param {Item?} n */
          (o) => {
            for (; o !== null; o = o.left)
              o.parent = i;
          }
        ), i._start = r._start;
        for (let o = i._start; o !== null; o = o.right)
          o.parent = i;
        return i._length = r._length, this.share.set(e, i), i._integrate(this, null), /** @type {InstanceType<Type>} */
        i;
      } else
        throw new Error(`Type with the name ${e} has already been defined with a different constructor`);
    return (
      /** @type {InstanceType<Type>} */
      r
    );
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YArray<T>}
   *
   * @public
   */
  getArray(e = "") {
    return (
      /** @type {YArray<T>} */
      this.get(e, $t)
    );
  }
  /**
   * @param {string} [name]
   * @return {YText}
   *
   * @public
   */
  getText(e = "") {
    return this.get(e, xt);
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YMap<T>}
   *
   * @public
   */
  getMap(e = "") {
    return (
      /** @type {YMap<T>} */
      this.get(e, Gt)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlElement}
   *
   * @public
   */
  getXmlElement(e = "") {
    return (
      /** @type {YXmlElement<{[key:string]:string}>} */
      this.get(e, pe)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlFragment}
   *
   * @public
   */
  getXmlFragment(e = "") {
    return this.get(e, At);
  }
  /**
   * Converts the entire document into a js object, recursively traversing each yjs type
   * Doesn't log types that have not been defined (using ydoc.getType(..)).
   *
   * @deprecated Do not use this method and rather call toJSON directly on the shared types.
   *
   * @return {Object<string, any>}
   */
  toJSON() {
    const e = {};
    return this.share.forEach((n, r) => {
      e[r] = n.toJSON();
    }), e;
  }
  /**
   * Emit `destroy` event and unregister all event handlers.
   */
  destroy() {
    this.isDestroyed = !0, _t(this.subdocs).forEach((n) => n.destroy());
    const e = this._item;
    if (e !== null) {
      this._item = null;
      const n = (
        /** @type {ContentDoc} */
        e.content
      );
      n.doc = new Rt({ guid: this.guid, ...n.opts, shouldLoad: !1 }), n.doc._item = e, j(
        /** @type {any} */
        e.parent.doc,
        (r) => {
          const s = n.doc;
          e.deleted || r.subdocsAdded.add(s), r.subdocsRemoved.add(this);
        },
        null,
        !0
      );
    }
    this.emit("destroyed", [!0]), this.emit("destroy", [this]), super.destroy();
  }
}
class m0 {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(e) {
    this.dsCurrVal = 0, this.restDecoder = e;
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @return {number}
   */
  readDsClock() {
    return this.dsCurrVal += q(this.restDecoder), this.dsCurrVal;
  }
  /**
   * @return {number}
   */
  readDsLen() {
    const e = q(this.restDecoder) + 1;
    return this.dsCurrVal += e, e;
  }
}
class ur extends m0 {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(e) {
    super(e), this.keys = [], q(e), this.keyClockDecoder = new ns(Te(e)), this.clientDecoder = new Zn(Te(e)), this.leftClockDecoder = new ns(Te(e)), this.rightClockDecoder = new ns(Te(e)), this.infoDecoder = new Oo(Te(e), un), this.stringDecoder = new _x(Te(e)), this.parentInfoDecoder = new Oo(Te(e), un), this.typeRefDecoder = new Zn(Te(e)), this.lenDecoder = new Zn(Te(e));
  }
  /**
   * @return {ID}
   */
  readLeftID() {
    return new Pt(this.clientDecoder.read(), this.leftClockDecoder.read());
  }
  /**
   * @return {ID}
   */
  readRightID() {
    return new Pt(this.clientDecoder.read(), this.rightClockDecoder.read());
  }
  /**
   * Read the next client id.
   * Use this in favor of readID whenever possible to reduce the number of objects created.
   */
  readClient() {
    return this.clientDecoder.read();
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readInfo() {
    return (
      /** @type {number} */
      this.infoDecoder.read()
    );
  }
  /**
   * @return {string}
   */
  readString() {
    return this.stringDecoder.read();
  }
  /**
   * @return {boolean}
   */
  readParentInfo() {
    return this.parentInfoDecoder.read() === 1;
  }
  /**
   * @return {number} An unsigned 8-bit integer
   */
  readTypeRef() {
    return this.typeRefDecoder.read();
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @return {number}
   */
  readLen() {
    return this.lenDecoder.read();
  }
  /**
   * @return {any}
   */
  readAny() {
    return cr(this.restDecoder);
  }
  /**
   * @return {Uint8Array}
   */
  readBuf() {
    return Te(this.restDecoder);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @return {any}
   */
  readJSON() {
    return cr(this.restDecoder);
  }
  /**
   * @return {string}
   */
  readKey() {
    const e = this.keyClockDecoder.read();
    if (e < this.keys.length)
      return this.keys[e];
    {
      const n = this.stringDecoder.read();
      return this.keys.push(n), n;
    }
  }
}
class g0 {
  constructor() {
    this.restEncoder = Nn();
  }
  toUint8Array() {
    return He(this.restEncoder);
  }
  resetDsCurVal() {
  }
  /**
   * @param {number} clock
   */
  writeDsClock(e) {
    B(this.restEncoder, e);
  }
  /**
   * @param {number} len
   */
  writeDsLen(e) {
    B(this.restEncoder, e);
  }
}
class b0 extends g0 {
  /**
   * @param {ID} id
   */
  writeLeftID(e) {
    B(this.restEncoder, e.client), B(this.restEncoder, e.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(e) {
    B(this.restEncoder, e.client), B(this.restEncoder, e.clock);
  }
  /**
   * Use writeClient and writeClock instead of writeID if possible.
   * @param {number} client
   */
  writeClient(e) {
    B(this.restEncoder, e);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(e) {
    Ds(this.restEncoder, e);
  }
  /**
   * @param {string} s
   */
  writeString(e) {
    zt(this.restEncoder, e);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(e) {
    B(this.restEncoder, e ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(e) {
    B(this.restEncoder, e);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(e) {
    B(this.restEncoder, e);
  }
  /**
   * @param {any} any
   */
  writeAny(e) {
    Ut(this.restEncoder, e);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(e) {
    Ie(this.restEncoder, e);
  }
  /**
   * @param {any} embed
   */
  writeJSON(e) {
    zt(this.restEncoder, JSON.stringify(e));
  }
  /**
   * @param {string} key
   */
  writeKey(e) {
    zt(this.restEncoder, e);
  }
}
class w0 {
  constructor() {
    this.restEncoder = Nn(), this.dsCurrVal = 0;
  }
  toUint8Array() {
    return He(this.restEncoder);
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @param {number} clock
   */
  writeDsClock(e) {
    const n = e - this.dsCurrVal;
    this.dsCurrVal = e, B(this.restEncoder, n);
  }
  /**
   * @param {number} len
   */
  writeDsLen(e) {
    e === 0 && Se(), B(this.restEncoder, e - 1), this.dsCurrVal += e;
  }
}
class Mr extends w0 {
  constructor() {
    super(), this.keyMap = /* @__PURE__ */ new Map(), this.keyClock = 0, this.keyClockEncoder = new ts(), this.clientEncoder = new Xn(), this.leftClockEncoder = new ts(), this.rightClockEncoder = new ts(), this.infoEncoder = new Ro(Ds), this.stringEncoder = new wx(), this.parentInfoEncoder = new Ro(Ds), this.typeRefEncoder = new Xn(), this.lenEncoder = new Xn();
  }
  toUint8Array() {
    const e = Nn();
    return B(e, 0), Ie(e, this.keyClockEncoder.toUint8Array()), Ie(e, this.clientEncoder.toUint8Array()), Ie(e, this.leftClockEncoder.toUint8Array()), Ie(e, this.rightClockEncoder.toUint8Array()), Ie(e, He(this.infoEncoder)), Ie(e, this.stringEncoder.toUint8Array()), Ie(e, He(this.parentInfoEncoder)), Ie(e, this.typeRefEncoder.toUint8Array()), Ie(e, this.lenEncoder.toUint8Array()), Rr(e, He(this.restEncoder)), He(e);
  }
  /**
   * @param {ID} id
   */
  writeLeftID(e) {
    this.clientEncoder.write(e.client), this.leftClockEncoder.write(e.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(e) {
    this.clientEncoder.write(e.client), this.rightClockEncoder.write(e.clock);
  }
  /**
   * @param {number} client
   */
  writeClient(e) {
    this.clientEncoder.write(e);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(e) {
    this.infoEncoder.write(e);
  }
  /**
   * @param {string} s
   */
  writeString(e) {
    this.stringEncoder.write(e);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(e) {
    this.parentInfoEncoder.write(e ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(e) {
    this.typeRefEncoder.write(e);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(e) {
    this.lenEncoder.write(e);
  }
  /**
   * @param {any} any
   */
  writeAny(e) {
    Ut(this.restEncoder, e);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(e) {
    Ie(this.restEncoder, e);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @param {any} embed
   */
  writeJSON(e) {
    Ut(this.restEncoder, e);
  }
  /**
   * Property keys are often reused. For example, in y-prosemirror the key `bold` might
   * occur very often. For a 3d application, the key `position` might occur very often.
   *
   * We cache these keys in a Map and refer to them via a unique number.
   *
   * @param {string} key
   */
  writeKey(e) {
    const n = this.keyMap.get(e);
    n === void 0 ? (this.keyClockEncoder.write(this.keyClock++), this.stringEncoder.write(e)) : this.keyClockEncoder.write(n);
  }
}
const x0 = (t, e, n, r) => {
  r = gt(r, e[0].id.clock);
  const s = Ue(e, r);
  B(t.restEncoder, e.length - s), t.writeClient(n), B(t.restEncoder, r);
  const i = e[s];
  i.write(t, r - i.id.clock);
  for (let o = s + 1; o < e.length; o++)
    e[o].write(t, 0);
}, Uc = (t, e, n) => {
  const r = /* @__PURE__ */ new Map();
  n.forEach((s, i) => {
    te(e, i) > s && r.set(i, s);
  }), Or(e).forEach((s, i) => {
    n.has(i) || r.set(i, 0);
  }), B(t.restEncoder, r.size), _t(r.entries()).sort((s, i) => i[0] - s[0]).forEach(([s, i]) => {
    x0(
      t,
      /** @type {Array<GC|Item>} */
      e.clients.get(s),
      s,
      i
    );
  });
}, y0 = (t, e) => {
  const n = je(), r = q(t.restDecoder);
  for (let s = 0; s < r; s++) {
    const i = q(t.restDecoder), o = new Array(i), l = t.readClient();
    let a = q(t.restDecoder);
    n.set(l, { i: 0, refs: o });
    for (let c = 0; c < i; c++) {
      const u = t.readInfo();
      switch (Dr & u) {
        case 0: {
          const d = t.readLen();
          o[c] = new De(O(l, a), d), a += d;
          break;
        }
        case 10: {
          const d = q(t.restDecoder);
          o[c] = new Be(O(l, a), d), a += d;
          break;
        }
        default: {
          const d = (u & (rt | xe)) === 0, h = new U(
            O(l, a),
            null,
            // left
            (u & xe) === xe ? t.readLeftID() : null,
            // origin
            null,
            // right
            (u & rt) === rt ? t.readRightID() : null,
            // right origin
            d ? t.readParentInfo() ? e.get(t.readString()) : t.readLeftID() : null,
            // parent
            d && (u & cn) === cn ? t.readString() : null,
            // parentSub
            bd(t, u)
            // item content
          );
          o[c] = h, a += h.length;
        }
      }
    }
  }
  return n;
}, v0 = (t, e, n) => {
  const r = [];
  let s = _t(n.keys()).sort((p, m) => p - m);
  if (s.length === 0)
    return null;
  const i = () => {
    if (s.length === 0)
      return null;
    let p = (
      /** @type {{i:number,refs:Array<GC|Item>}} */
      n.get(s[s.length - 1])
    );
    for (; p.refs.length === p.i; )
      if (s.pop(), s.length > 0)
        p = /** @type {{i:number,refs:Array<GC|Item>}} */
        n.get(s[s.length - 1]);
      else
        return null;
    return p;
  };
  let o = i();
  if (o === null)
    return null;
  const l = new Kc(), a = /* @__PURE__ */ new Map(), c = (p, m) => {
    const g = a.get(p);
    (g == null || g > m) && a.set(p, m);
  };
  let u = (
    /** @type {any} */
    o.refs[
      /** @type {any} */
      o.i++
    ]
  );
  const d = /* @__PURE__ */ new Map(), h = () => {
    for (const p of r) {
      const m = p.id.client, g = n.get(m);
      g ? (g.i--, l.clients.set(m, g.refs.slice(g.i)), n.delete(m), g.i = 0, g.refs = []) : l.clients.set(m, [p]), s = s.filter((b) => b !== m);
    }
    r.length = 0;
  };
  for (; ; ) {
    if (u.constructor !== Be) {
      const m = at(d, u.id.client, () => te(e, u.id.client)) - u.id.clock;
      if (m < 0)
        r.push(u), c(u.id.client, u.id.clock - 1), h();
      else {
        const g = u.getMissing(t, e);
        if (g !== null) {
          r.push(u);
          const b = n.get(
            /** @type {number} */
            g
          ) || { refs: [], i: 0 };
          if (b.refs.length === b.i)
            c(
              /** @type {number} */
              g,
              te(e, g)
            ), h();
          else {
            u = b.refs[b.i++];
            continue;
          }
        } else (m === 0 || m < u.length) && (u.integrate(t, m), d.set(u.id.client, u.id.clock + u.length));
      }
    }
    if (r.length > 0)
      u = /** @type {GC|Item} */
      r.pop();
    else if (o !== null && o.i < o.refs.length)
      u = /** @type {GC|Item} */
      o.refs[o.i++];
    else {
      if (o = i(), o === null)
        break;
      u = /** @type {GC|Item} */
      o.refs[o.i++];
    }
  }
  if (l.clients.size > 0) {
    const p = new Mr();
    return Uc(p, l, /* @__PURE__ */ new Map()), B(p.restEncoder, 0), { missing: a, update: p.toUint8Array() };
  }
  return null;
}, k0 = (t, e) => Uc(t, e.doc.store, e.beforeState), C0 = (t, e, n, r = new ur(t)) => j(e, (s) => {
  s.local = !1;
  let i = !1;
  const o = s.doc, l = o.store, a = y0(r, o), c = v0(s, l, a), u = l.pendingStructs;
  if (u) {
    for (const [h, p] of u.missing)
      if (p < te(l, h)) {
        i = !0;
        break;
      }
    if (c) {
      for (const [h, p] of c.missing) {
        const m = u.missing.get(h);
        (m == null || m > p) && u.missing.set(h, p);
      }
      u.update = qo([u.update, c.update]);
    }
  } else
    l.pendingStructs = c;
  const d = Bo(r, s, l);
  if (l.pendingDs) {
    const h = new ur(hi(l.pendingDs));
    q(h.restDecoder);
    const p = Bo(h, s, l);
    d && p ? l.pendingDs = qo([d, p]) : l.pendingDs = d || p;
  } else
    l.pendingDs = d;
  if (i) {
    const h = (
      /** @type {{update: Uint8Array}} */
      l.pendingStructs.update
    );
    l.pendingStructs = null, Fs(s.doc, h);
  }
}, n, !1), Fs = (t, e, n, r = ur) => {
  const s = hi(e);
  C0(s, t, n, new r(s));
};
class S0 {
  constructor() {
    this.l = [];
  }
}
const $o = () => new S0(), Ho = (t, e) => t.l.push(e), jo = (t, e) => {
  const n = t.l, r = n.length;
  t.l = n.filter((s) => e !== s), r === t.l.length && console.error("[yjs] Tried to remove event handler that doesn't exist.");
}, Wc = (t, e, n) => gi(t.l, [e, n]);
class Pt {
  /**
   * @param {number} client client id
   * @param {number} clock unique per client id, continuous number
   */
  constructor(e, n) {
    this.client = e, this.clock = n;
  }
}
const zn = (t, e) => t === e || t !== null && e !== null && t.client === e.client && t.clock === e.clock, O = (t, e) => new Pt(t, e), hn = (t) => {
  for (const [e, n] of t.doc.share.entries())
    if (n === t)
      return e;
  throw Se();
}, pn = (t, e) => {
  for (; e !== null; ) {
    if (e.parent === t)
      return !0;
    e = /** @type {AbstractType<any>} */
    e.parent._item;
  }
  return !1;
};
class fr {
  /**
   * @param {ID|null} type
   * @param {string|null} tname
   * @param {ID|null} item
   * @param {number} assoc
   */
  constructor(e, n, r, s = 0) {
    this.type = e, this.tname = n, this.item = r, this.assoc = s;
  }
}
class N0 {
  /**
   * @param {AbstractType<any>} type
   * @param {number} index
   * @param {number} [assoc]
   */
  constructor(e, n, r = 0) {
    this.type = e, this.index = n, this.assoc = r;
  }
}
const I0 = (t, e, n = 0) => new N0(t, e, n), Pn = (t, e, n) => {
  let r = null, s = null;
  return t._item === null ? s = hn(t) : r = O(t._item.id.client, t._item.id.clock), new fr(r, s, e, n);
}, ss = (t, e, n = 0) => {
  let r = t._start;
  if (n < 0) {
    if (e === 0)
      return Pn(t, null, n);
    e--;
  }
  for (; r !== null; ) {
    if (!r.deleted && r.countable) {
      if (r.length > e)
        return Pn(t, O(r.id.client, r.id.clock + e), n);
      e -= r.length;
    }
    if (r.right === null && n < 0)
      return Pn(t, r.lastId, n);
    r = r.right;
  }
  return Pn(t, null, n);
}, _0 = (t, e) => {
  const n = Bt(t, e), r = e.clock - n.id.clock;
  return {
    item: n,
    diff: r
  };
}, A0 = (t, e, n = !0) => {
  const r = e.store, s = t.item, i = t.type, o = t.tname, l = t.assoc;
  let a = null, c = 0;
  if (s !== null) {
    if (te(r, s.client) <= s.clock)
      return null;
    const u = n ? $s(r, s) : _0(r, s), d = u.item;
    if (!(d instanceof U))
      return null;
    if (a = /** @type {AbstractType<any>} */
    d.parent, a._item === null || !a._item.deleted) {
      c = d.deleted || !d.countable ? 0 : u.diff + (l >= 0 ? 0 : 1);
      let h = d.left;
      for (; h !== null; )
        !h.deleted && h.countable && (c += h.length), h = h.left;
    }
  } else {
    if (o !== null)
      a = e.get(o);
    else if (i !== null) {
      if (te(r, i.client) <= i.clock)
        return null;
      const { item: u } = n ? $s(r, i) : { item: Bt(r, i) };
      if (u instanceof U && u.content instanceof qe)
        a = u.content.type;
      else
        return null;
    } else
      throw Se();
    l >= 0 ? c = a._length : c = 0;
  }
  return I0(a, c, t.assoc);
};
class vi {
  /**
   * @param {DeleteSet} ds
   * @param {Map<number,number>} sv state map
   */
  constructor(e, n) {
    this.ds = e, this.sv = n;
  }
}
const Gc = (t, e) => new vi(t, e), is = (t) => Gc(h0(t.store), Or(t.store)), vt = (t, e) => e === void 0 ? !t.deleted : e.sv.has(t.id.client) && (e.sv.get(t.id.client) || 0) > t.id.clock && !Jt(e.ds, t.id), zs = (t, e) => {
  const n = at(t.meta, zs, It), r = t.doc.store;
  n.has(e) || (e.sv.forEach((s, i) => {
    s < te(r, i) && ve(t, O(i, s));
  }), wt(t, e.ds, (s) => {
  }), n.add(e));
};
class Kc {
  constructor() {
    this.clients = /* @__PURE__ */ new Map(), this.pendingStructs = null, this.pendingDs = null;
  }
}
const Or = (t) => {
  const e = /* @__PURE__ */ new Map();
  return t.clients.forEach((n, r) => {
    const s = n[n.length - 1];
    e.set(r, s.id.clock + s.length);
  }), e;
}, te = (t, e) => {
  const n = t.clients.get(e);
  if (n === void 0)
    return 0;
  const r = n[n.length - 1];
  return r.id.clock + r.length;
}, qc = (t, e) => {
  let n = t.clients.get(e.id.client);
  if (n === void 0)
    n = [], t.clients.set(e.id.client, n);
  else {
    const r = n[n.length - 1];
    if (r.id.clock + r.length !== e.id.clock)
      throw Se();
  }
  n.push(e);
}, Ue = (t, e) => {
  let n = 0, r = t.length - 1, s = t[r], i = s.id.clock;
  if (i === e)
    return r;
  let o = it(e / (i + s.length - 1) * r);
  for (; n <= r; ) {
    if (s = t[o], i = s.id.clock, i <= e) {
      if (e < i + s.length)
        return o;
      n = o + 1;
    } else
      r = o - 1;
    o = it((n + r) / 2);
  }
  throw Se();
}, T0 = (t, e) => {
  const n = t.clients.get(e.client);
  return n[Ue(n, e.clock)];
}, Bt = (
  /** @type {function(StructStore,ID):Item} */
  T0
), Ps = (t, e, n) => {
  const r = Ue(e, n), s = e[r];
  return s.id.clock < n && s instanceof U ? (e.splice(r + 1, 0, br(t, s, n - s.id.clock)), r + 1) : r;
}, ve = (t, e) => {
  const n = (
    /** @type {Array<Item>} */
    t.doc.store.clients.get(e.client)
  );
  return n[Ps(t, n, e.clock)];
}, Vo = (t, e, n) => {
  const r = e.clients.get(n.client), s = Ue(r, n.clock), i = r[s];
  return n.clock !== i.id.clock + i.length - 1 && i.constructor !== De && r.splice(s + 1, 0, br(t, i, n.clock - i.id.clock + 1)), i;
}, E0 = (t, e, n) => {
  const r = (
    /** @type {Array<GC|Item>} */
    t.clients.get(e.id.client)
  );
  r[Ue(r, e.id.clock)] = n;
}, Yc = (t, e, n, r, s) => {
  if (r === 0)
    return;
  const i = n + r;
  let o = Ps(t, e, n), l;
  do
    l = e[o++], i < l.id.clock + l.length && Ps(t, e, i), s(l);
  while (o < e.length && e[o].id.clock < i);
};
class D0 {
  /**
   * @param {Doc} doc
   * @param {any} origin
   * @param {boolean} local
   */
  constructor(e, n, r) {
    this.doc = e, this.deleteSet = new Yt(), this.beforeState = Or(e.store), this.afterState = /* @__PURE__ */ new Map(), this.changed = /* @__PURE__ */ new Map(), this.changedParentTypes = /* @__PURE__ */ new Map(), this._mergeStructs = [], this.origin = n, this.meta = /* @__PURE__ */ new Map(), this.local = r, this.subdocsAdded = /* @__PURE__ */ new Set(), this.subdocsRemoved = /* @__PURE__ */ new Set(), this.subdocsLoaded = /* @__PURE__ */ new Set(), this._needFormattingCleanup = !1;
  }
}
const Uo = (t, e) => e.deleteSet.clients.size === 0 && !qw(e.afterState, (n, r) => e.beforeState.get(r) !== n) ? !1 : (xi(e.deleteSet), k0(t, e), yi(t, e.deleteSet), !0), Wo = (t, e, n) => {
  const r = e._item;
  (r === null || r.id.clock < (t.beforeState.get(r.id.client) || 0) && !r.deleted) && at(t.changed, e, It).add(n);
}, Qn = (t, e) => {
  let n = t[e], r = t[e - 1], s = e;
  for (; s > 0; n = r, r = t[--s - 1]) {
    if (r.deleted === n.deleted && r.constructor === n.constructor && r.mergeWith(n)) {
      n instanceof U && n.parentSub !== null && /** @type {AbstractType<any>} */
      n.parent._map.get(n.parentSub) === n && n.parent._map.set(
        n.parentSub,
        /** @type {Item} */
        r
      );
      continue;
    }
    break;
  }
  const i = e - s;
  return i && t.splice(e + 1 - i, i), i;
}, R0 = (t, e, n) => {
  for (const [r, s] of t.clients.entries()) {
    const i = (
      /** @type {Array<GC|Item>} */
      e.clients.get(r)
    );
    for (let o = s.length - 1; o >= 0; o--) {
      const l = s[o], a = l.clock + l.len;
      for (let c = Ue(i, l.clock), u = i[c]; c < i.length && u.id.clock < a; u = i[++c]) {
        const d = i[c];
        if (l.clock + l.len <= d.id.clock)
          break;
        d instanceof U && d.deleted && !d.keep && n(d) && d.gc(e, !1);
      }
    }
  }
}, L0 = (t, e) => {
  t.clients.forEach((n, r) => {
    const s = (
      /** @type {Array<GC|Item>} */
      e.clients.get(r)
    );
    for (let i = n.length - 1; i >= 0; i--) {
      const o = n[i], l = Vt(s.length - 1, 1 + Ue(s, o.clock + o.len - 1));
      for (let a = l, c = s[a]; a > 0 && c.id.clock >= o.clock; c = s[a])
        a -= 1 + Qn(s, a);
    }
  });
}, Jc = (t, e) => {
  if (e < t.length) {
    const n = t[e], r = n.doc, s = r.store, i = n.deleteSet, o = n._mergeStructs;
    try {
      xi(i), n.afterState = Or(n.doc.store), r.emit("beforeObserverCalls", [n, r]);
      const l = [];
      n.changed.forEach(
        (a, c) => l.push(() => {
          (c._item === null || !c._item.deleted) && c._callObserver(n, a);
        })
      ), l.push(() => {
        n.changedParentTypes.forEach((a, c) => {
          c._dEH.l.length > 0 && (c._item === null || !c._item.deleted) && (a = a.filter(
            (u) => u.target._item === null || !u.target._item.deleted
          ), a.forEach((u) => {
            u.currentTarget = c, u._path = null;
          }), a.sort((u, d) => u.path.length - d.path.length), Wc(c._dEH, a, n));
        });
      }), l.push(() => r.emit("afterTransaction", [n, r])), gi(l, []), n._needFormattingCleanup && Q0(n);
    } finally {
      r.gc && R0(i, s, r.gcFilter), L0(i, s), n.afterState.forEach((u, d) => {
        const h = n.beforeState.get(d) || 0;
        if (h !== u) {
          const p = (
            /** @type {Array<GC|Item>} */
            s.clients.get(d)
          ), m = gt(Ue(p, h), 1);
          for (let g = p.length - 1; g >= m; )
            g -= 1 + Qn(p, g);
        }
      });
      for (let u = o.length - 1; u >= 0; u--) {
        const { client: d, clock: h } = o[u].id, p = (
          /** @type {Array<GC|Item>} */
          s.clients.get(d)
        ), m = Ue(p, h);
        m + 1 < p.length && Qn(p, m + 1) > 1 || m > 0 && Qn(p, m);
      }
      if (!n.local && n.afterState.get(r.clientID) !== n.beforeState.get(r.clientID) && (d0(bi, Oc, "[yjs] ", Fc, zc, "Changed the client-id because another client seems to be using it."), r.clientID = Vc()), r.emit("afterTransactionCleanup", [n, r]), r._observers.has("update")) {
        const u = new b0();
        Uo(u, n) && r.emit("update", [u.toUint8Array(), n.origin, r, n]);
      }
      if (r._observers.has("updateV2")) {
        const u = new Mr();
        Uo(u, n) && r.emit("updateV2", [u.toUint8Array(), n.origin, r, n]);
      }
      const { subdocsAdded: l, subdocsLoaded: a, subdocsRemoved: c } = n;
      (l.size > 0 || c.size > 0 || a.size > 0) && (l.forEach((u) => {
        u.clientID = r.clientID, u.collectionid == null && (u.collectionid = r.collectionid), r.subdocs.add(u);
      }), c.forEach((u) => r.subdocs.delete(u)), r.emit("subdocs", [{ loaded: a, added: l, removed: c }, r, n]), c.forEach((u) => u.destroy())), t.length <= e + 1 ? (r._transactionCleanups = [], r.emit("afterAllTransactions", [r, t])) : Jc(t, e + 1);
    }
  }
}, j = (t, e, n = null, r = !0) => {
  const s = t._transactionCleanups;
  let i = !1, o = null;
  t._transaction === null && (i = !0, t._transaction = new D0(t, n, r), s.push(t._transaction), s.length === 1 && t.emit("beforeAllTransactions", [t]), t.emit("beforeTransaction", [t._transaction, t]));
  try {
    o = e(t._transaction);
  } finally {
    if (i) {
      const l = t._transaction === s[0];
      t._transaction = null, l && Jc(s, 0);
    }
  }
  return o;
};
class M0 {
  /**
   * @param {DeleteSet} deletions
   * @param {DeleteSet} insertions
   */
  constructor(e, n) {
    this.insertions = n, this.deletions = e, this.meta = /* @__PURE__ */ new Map();
  }
}
const Go = (t, e, n) => {
  wt(t, n.deletions, (r) => {
    r instanceof U && e.scope.some((s) => s === t.doc || pn(
      /** @type {AbstractType<any>} */
      s,
      r
    )) && Ii(r, !1);
  });
}, Ko = (t, e, n) => {
  let r = null;
  const s = t.doc, i = t.scope;
  j(s, (l) => {
    for (; e.length > 0 && t.currStackItem === null; ) {
      const a = s.store, c = (
        /** @type {StackItem} */
        e.pop()
      ), u = /* @__PURE__ */ new Set(), d = [];
      let h = !1;
      wt(l, c.insertions, (p) => {
        if (p instanceof U) {
          if (p.redone !== null) {
            let { item: m, diff: g } = $s(a, p.id);
            g > 0 && (m = ve(l, O(m.id.client, m.id.clock + g))), p = m;
          }
          !p.deleted && i.some((m) => m === l.doc || pn(
            /** @type {AbstractType<any>} */
            m,
            /** @type {Item} */
            p
          )) && d.push(p);
        }
      }), wt(l, c.deletions, (p) => {
        p instanceof U && i.some((m) => m === l.doc || pn(
          /** @type {AbstractType<any>} */
          m,
          p
        )) && // Never redo structs in stackItem.insertions because they were created and deleted in the same capture interval.
        !Jt(c.insertions, p.id) && u.add(p);
      }), u.forEach((p) => {
        h = gd(l, p, u, c.insertions, t.ignoreRemoteMapChanges, t) !== null || h;
      });
      for (let p = d.length - 1; p >= 0; p--) {
        const m = d[p];
        t.deleteFilter(m) && (m.delete(l), h = !0);
      }
      t.currStackItem = h ? c : null;
    }
    l.changed.forEach((a, c) => {
      a.has(null) && c._searchMarker && (c._searchMarker.length = 0);
    }), r = l;
  }, t);
  const o = t.currStackItem;
  if (o != null) {
    const l = r.changedParentTypes;
    t.emit("stack-item-popped", [{ stackItem: o, type: n, changedParentTypes: l, origin: t }, t]), t.currStackItem = null;
  }
  return o;
};
class Xc extends kc {
  /**
   * @param {Doc|AbstractType<any>|Array<AbstractType<any>>} typeScope Limits the scope of the UndoManager. If this is set to a ydoc instance, all changes on that ydoc will be undone. If set to a specific type, only changes on that type or its children will be undone. Also accepts an array of types.
   * @param {UndoManagerOptions} options
   */
  constructor(e, {
    captureTimeout: n = 500,
    captureTransaction: r = (a) => !0,
    deleteFilter: s = () => !0,
    trackedOrigins: i = /* @__PURE__ */ new Set([null]),
    ignoreRemoteMapChanges: o = !1,
    doc: l = (
      /** @type {Doc} */
      Ts(e) ? e[0].doc : e instanceof Rt ? e : e.doc
    )
  } = {}) {
    super(), this.scope = [], this.doc = l, this.addToScope(e), this.deleteFilter = s, i.add(this), this.trackedOrigins = i, this.captureTransaction = r, this.undoStack = [], this.redoStack = [], this.undoing = !1, this.redoing = !1, this.currStackItem = null, this.lastChange = 0, this.ignoreRemoteMapChanges = o, this.captureTimeout = n, this.afterTransactionHandler = (a) => {
      if (!this.captureTransaction(a) || !this.scope.some((b) => a.changedParentTypes.has(
        /** @type {AbstractType<any>} */
        b
      ) || b === this.doc) || !this.trackedOrigins.has(a.origin) && (!a.origin || !this.trackedOrigins.has(a.origin.constructor)))
        return;
      const c = this.undoing, u = this.redoing, d = c ? this.redoStack : this.undoStack;
      c ? this.stopCapturing() : u || this.clear(!1, !0);
      const h = new Yt();
      a.afterState.forEach((b, x) => {
        const v = a.beforeState.get(x) || 0, S = b - v;
        S > 0 && fn(h, x, v, S);
      });
      const p = Lx();
      let m = !1;
      if (this.lastChange > 0 && p - this.lastChange < this.captureTimeout && d.length > 0 && !c && !u) {
        const b = d[d.length - 1];
        b.deletions = Os([b.deletions, a.deleteSet]), b.insertions = Os([b.insertions, h]);
      } else
        d.push(new M0(a.deleteSet, h)), m = !0;
      !c && !u && (this.lastChange = p), wt(
        a,
        a.deleteSet,
        /** @param {Item|GC} item */
        (b) => {
          b instanceof U && this.scope.some((x) => x === a.doc || pn(
            /** @type {AbstractType<any>} */
            x,
            b
          )) && Ii(b, !0);
        }
      );
      const g = [{ stackItem: d[d.length - 1], origin: a.origin, type: c ? "redo" : "undo", changedParentTypes: a.changedParentTypes }, this];
      m ? this.emit("stack-item-added", g) : this.emit("stack-item-updated", g);
    }, this.doc.on("afterTransaction", this.afterTransactionHandler), this.doc.on("destroy", () => {
      this.destroy();
    });
  }
  /**
   * Extend the scope.
   *
   * @param {Array<AbstractType<any> | Doc> | AbstractType<any> | Doc} ytypes
   */
  addToScope(e) {
    const n = new Set(this.scope);
    e = Ts(e) ? e : [e], e.forEach((r) => {
      n.has(r) || (n.add(r), (r instanceof ce ? r.doc !== this.doc : r !== this.doc) && Bc("[yjs#509] Not same Y.Doc"), this.scope.push(r));
    });
  }
  /**
   * @param {any} origin
   */
  addTrackedOrigin(e) {
    this.trackedOrigins.add(e);
  }
  /**
   * @param {any} origin
   */
  removeTrackedOrigin(e) {
    this.trackedOrigins.delete(e);
  }
  clear(e = !0, n = !0) {
    (e && this.canUndo() || n && this.canRedo()) && this.doc.transact((r) => {
      e && (this.undoStack.forEach((s) => Go(r, this, s)), this.undoStack = []), n && (this.redoStack.forEach((s) => Go(r, this, s)), this.redoStack = []), this.emit("stack-cleared", [{ undoStackCleared: e, redoStackCleared: n }]);
    });
  }
  /**
   * UndoManager merges Undo-StackItem if they are created within time-gap
   * smaller than `options.captureTimeout`. Call `um.stopCapturing()` so that the next
   * StackItem won't be merged.
   *
   *
   * @example
   *     // without stopCapturing
   *     ytext.insert(0, 'a')
   *     ytext.insert(1, 'b')
   *     um.undo()
   *     ytext.toString() // => '' (note that 'ab' was removed)
   *     // with stopCapturing
   *     ytext.insert(0, 'a')
   *     um.stopCapturing()
   *     ytext.insert(0, 'b')
   *     um.undo()
   *     ytext.toString() // => 'a' (note that only 'b' was removed)
   *
   */
  stopCapturing() {
    this.lastChange = 0;
  }
  /**
   * Undo last changes on type.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  undo() {
    this.undoing = !0;
    let e;
    try {
      e = Ko(this, this.undoStack, "undo");
    } finally {
      this.undoing = !1;
    }
    return e;
  }
  /**
   * Redo last undo operation.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  redo() {
    this.redoing = !0;
    let e;
    try {
      e = Ko(this, this.redoStack, "redo");
    } finally {
      this.redoing = !1;
    }
    return e;
  }
  /**
   * Are undo steps available?
   *
   * @return {boolean} `true` if undo is possible
   */
  canUndo() {
    return this.undoStack.length > 0;
  }
  /**
   * Are redo steps available?
   *
   * @return {boolean} `true` if redo is possible
   */
  canRedo() {
    return this.redoStack.length > 0;
  }
  destroy() {
    this.trackedOrigins.delete(this), this.doc.off("afterTransaction", this.afterTransactionHandler), super.destroy();
  }
}
function* O0(t) {
  const e = q(t.restDecoder);
  for (let n = 0; n < e; n++) {
    const r = q(t.restDecoder), s = t.readClient();
    let i = q(t.restDecoder);
    for (let o = 0; o < r; o++) {
      const l = t.readInfo();
      if (l === 10) {
        const a = q(t.restDecoder);
        yield new Be(O(s, i), a), i += a;
      } else if ((Dr & l) !== 0) {
        const a = (l & (rt | xe)) === 0, c = new U(
          O(s, i),
          null,
          // left
          (l & xe) === xe ? t.readLeftID() : null,
          // origin
          null,
          // right
          (l & rt) === rt ? t.readRightID() : null,
          // right origin
          // @ts-ignore Force writing a string here.
          a ? t.readParentInfo() ? t.readString() : t.readLeftID() : null,
          // parent
          a && (l & cn) === cn ? t.readString() : null,
          // parentSub
          bd(t, l)
          // item content
        );
        yield c, i += c.length;
      } else {
        const a = t.readLen();
        yield new De(O(s, i), a), i += a;
      }
    }
  }
}
class F0 {
  /**
   * @param {UpdateDecoderV1 | UpdateDecoderV2} decoder
   * @param {boolean} filterSkips
   */
  constructor(e, n) {
    this.gen = O0(e), this.curr = null, this.done = !1, this.filterSkips = n, this.next();
  }
  /**
   * @return {Item | GC | Skip |null}
   */
  next() {
    do
      this.curr = this.gen.next().value || null;
    while (this.filterSkips && this.curr !== null && this.curr.constructor === Be);
    return this.curr;
  }
}
class z0 {
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  constructor(e) {
    this.currClient = 0, this.startClock = 0, this.written = 0, this.encoder = e, this.clientStructs = [];
  }
}
const P0 = (t, e) => {
  if (t.constructor === De) {
    const { client: n, clock: r } = t.id;
    return new De(O(n, r + e), t.length - e);
  } else if (t.constructor === Be) {
    const { client: n, clock: r } = t.id;
    return new Be(O(n, r + e), t.length - e);
  } else {
    const n = (
      /** @type {Item} */
      t
    ), { client: r, clock: s } = n.id;
    return new U(
      O(r, s + e),
      null,
      O(r, s + e - 1),
      null,
      n.rightOrigin,
      n.parent,
      n.parentSub,
      n.content.splice(e)
    );
  }
}, qo = (t, e = ur, n = Mr) => {
  if (t.length === 1)
    return t[0];
  const r = t.map((u) => new e(hi(u)));
  let s = r.map((u) => new F0(u, !0)), i = null;
  const o = new n(), l = new z0(o);
  for (; s = s.filter((h) => h.curr !== null), s.sort(
    /** @type {function(any,any):number} */
    (h, p) => {
      if (h.curr.id.client === p.curr.id.client) {
        const m = h.curr.id.clock - p.curr.id.clock;
        return m === 0 ? h.curr.constructor === p.curr.constructor ? 0 : h.curr.constructor === Be ? 1 : -1 : m;
      } else
        return p.curr.id.client - h.curr.id.client;
    }
  ), s.length !== 0; ) {
    const u = s[0], d = (
      /** @type {Item | GC} */
      u.curr.id.client
    );
    if (i !== null) {
      let h = (
        /** @type {Item | GC | null} */
        u.curr
      ), p = !1;
      for (; h !== null && h.id.clock + h.length <= i.struct.id.clock + i.struct.length && h.id.client >= i.struct.id.client; )
        h = u.next(), p = !0;
      if (h === null || // current decoder is empty
      h.id.client !== d || // check whether there is another decoder that has has updates from `firstClient`
      p && h.id.clock > i.struct.id.clock + i.struct.length)
        continue;
      if (d !== i.struct.id.client)
        en(l, i.struct, i.offset), i = { struct: h, offset: 0 }, u.next();
      else if (i.struct.id.clock + i.struct.length < h.id.clock)
        if (i.struct.constructor === Be)
          i.struct.length = h.id.clock + h.length - i.struct.id.clock;
        else {
          en(l, i.struct, i.offset);
          const m = h.id.clock - i.struct.id.clock - i.struct.length;
          i = { struct: new Be(O(d, i.struct.id.clock + i.struct.length), m), offset: 0 };
        }
      else {
        const m = i.struct.id.clock + i.struct.length - h.id.clock;
        m > 0 && (i.struct.constructor === Be ? i.struct.length -= m : h = P0(h, m)), i.struct.mergeWith(
          /** @type {any} */
          h
        ) || (en(l, i.struct, i.offset), i = { struct: h, offset: 0 }, u.next());
      }
    } else
      i = { struct: (
        /** @type {Item | GC} */
        u.curr
      ), offset: 0 }, u.next();
    for (let h = u.curr; h !== null && h.id.client === d && h.id.clock === i.struct.id.clock + i.struct.length && h.constructor !== Be; h = u.next())
      en(l, i.struct, i.offset), i = { struct: h, offset: 0 };
  }
  i !== null && (en(l, i.struct, i.offset), i = null), B0(l);
  const a = r.map((u) => p0(u)), c = Os(a);
  return yi(o, c), o.toUint8Array();
}, Zc = (t) => {
  t.written > 0 && (t.clientStructs.push({ written: t.written, restEncoder: He(t.encoder.restEncoder) }), t.encoder.restEncoder = Nn(), t.written = 0);
}, en = (t, e, n) => {
  t.written > 0 && t.currClient !== e.id.client && Zc(t), t.written === 0 && (t.currClient = e.id.client, t.encoder.writeClient(e.id.client), B(t.encoder.restEncoder, e.id.clock + n)), e.write(t.encoder, n), t.written++;
}, B0 = (t) => {
  Zc(t);
  const e = t.encoder.restEncoder;
  B(e, t.clientStructs.length);
  for (let n = 0; n < t.clientStructs.length; n++) {
    const r = t.clientStructs[n];
    B(e, r.written), Rr(e, r.restEncoder);
  }
}, Yo = "You must not compute changes after the event-handler fired.";
class Fr {
  /**
   * @param {T} target The changed type.
   * @param {Transaction} transaction
   */
  constructor(e, n) {
    this.target = e, this.currentTarget = e, this.transaction = n, this._changes = null, this._keys = null, this._delta = null, this._path = null;
  }
  /**
   * Computes the path from `y` to the changed type.
   *
   * @todo v14 should standardize on path: Array<{parent, index}> because that is easier to work with.
   *
   * The following property holds:
   * @example
   *   let type = y
   *   event.path.forEach(dir => {
   *     type = type.get(dir)
   *   })
   *   type === event.target // => true
   */
  get path() {
    return this._path || (this._path = $0(this.currentTarget, this.target));
  }
  /**
   * Check if a struct is deleted by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  deletes(e) {
    return Jt(this.transaction.deleteSet, e.id);
  }
  /**
   * @type {Map<string, { action: 'add' | 'update' | 'delete', oldValue: any, newValue: any }>}
   */
  get keys() {
    if (this._keys === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw bt(Yo);
      const e = /* @__PURE__ */ new Map(), n = this.target;
      /** @type Set<string|null> */
      this.transaction.changed.get(n).forEach((s) => {
        if (s !== null) {
          const i = (
            /** @type {Item} */
            n._map.get(s)
          );
          let o, l;
          if (this.adds(i)) {
            let a = i.left;
            for (; a !== null && this.adds(a); )
              a = a.left;
            if (this.deletes(i))
              if (a !== null && this.deletes(a))
                o = "delete", l = Zr(a.content.getContent());
              else
                return;
            else
              a !== null && this.deletes(a) ? (o = "update", l = Zr(a.content.getContent())) : (o = "add", l = void 0);
          } else if (this.deletes(i))
            o = "delete", l = Zr(
              /** @type {Item} */
              i.content.getContent()
            );
          else
            return;
          e.set(s, { action: o, oldValue: l });
        }
      }), this._keys = e;
    }
    return this._keys;
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {Array<{insert?: string | Array<any> | object | AbstractType<any>, retain?: number, delete?: number, attributes?: Object<string, any>}>}
   */
  get delta() {
    return this.changes.delta;
  }
  /**
   * Check if a struct is added by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  adds(e) {
    return e.id.clock >= (this.transaction.beforeState.get(e.id.client) || 0);
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    let e = this._changes;
    if (e === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw bt(Yo);
      const n = this.target, r = It(), s = It(), i = [];
      if (e = {
        added: r,
        deleted: s,
        delta: i,
        keys: this.keys
      }, /** @type Set<string|null> */
      this.transaction.changed.get(n).has(null)) {
        let l = null;
        const a = () => {
          l && i.push(l);
        };
        for (let c = n._start; c !== null; c = c.right)
          c.deleted ? this.deletes(c) && !this.adds(c) && ((l === null || l.delete === void 0) && (a(), l = { delete: 0 }), l.delete += c.length, s.add(c)) : this.adds(c) ? ((l === null || l.insert === void 0) && (a(), l = { insert: [] }), l.insert = l.insert.concat(c.content.getContent()), r.add(c)) : ((l === null || l.retain === void 0) && (a(), l = { retain: 0 }), l.retain += c.length);
        l !== null && l.retain === void 0 && a();
      }
      this._changes = e;
    }
    return (
      /** @type {any} */
      e
    );
  }
}
const $0 = (t, e) => {
  const n = [];
  for (; e._item !== null && e !== t; ) {
    if (e._item.parentSub !== null)
      n.unshift(e._item.parentSub);
    else {
      let r = 0, s = (
        /** @type {AbstractType<any>} */
        e._item.parent._start
      );
      for (; s !== e._item && s !== null; )
        !s.deleted && s.countable && (r += s.length), s = s.right;
      n.unshift(r);
    }
    e = /** @type {AbstractType<any>} */
    e._item.parent;
  }
  return n;
}, me = () => {
  Bc("Invalid access: Add Yjs type to a document before reading data.");
}, Qc = 80;
let ki = 0;
class H0 {
  /**
   * @param {Item} p
   * @param {number} index
   */
  constructor(e, n) {
    e.marker = !0, this.p = e, this.index = n, this.timestamp = ki++;
  }
}
const j0 = (t) => {
  t.timestamp = ki++;
}, ed = (t, e, n) => {
  t.p.marker = !1, t.p = e, e.marker = !0, t.index = n, t.timestamp = ki++;
}, V0 = (t, e, n) => {
  if (t.length >= Qc) {
    const r = t.reduce((s, i) => s.timestamp < i.timestamp ? s : i);
    return ed(r, e, n), r;
  } else {
    const r = new H0(e, n);
    return t.push(r), r;
  }
}, zr = (t, e) => {
  if (t._start === null || e === 0 || t._searchMarker === null)
    return null;
  const n = t._searchMarker.length === 0 ? null : t._searchMarker.reduce((i, o) => Jn(e - i.index) < Jn(e - o.index) ? i : o);
  let r = t._start, s = 0;
  for (n !== null && (r = n.p, s = n.index, j0(n)); r.right !== null && s < e; ) {
    if (!r.deleted && r.countable) {
      if (e < s + r.length)
        break;
      s += r.length;
    }
    r = r.right;
  }
  for (; r.left !== null && s > e; )
    r = r.left, !r.deleted && r.countable && (s -= r.length);
  for (; r.left !== null && r.left.id.client === r.id.client && r.left.id.clock + r.left.length === r.id.clock; )
    r = r.left, !r.deleted && r.countable && (s -= r.length);
  return n !== null && Jn(n.index - s) < /** @type {YText|YArray<any>} */
  r.parent.length / Qc ? (ed(n, r, s), n) : V0(t._searchMarker, r, s);
}, mn = (t, e, n) => {
  for (let r = t.length - 1; r >= 0; r--) {
    const s = t[r];
    if (n > 0) {
      let i = s.p;
      for (i.marker = !1; i && (i.deleted || !i.countable); )
        i = i.left, i && !i.deleted && i.countable && (s.index -= i.length);
      if (i === null || i.marker === !0) {
        t.splice(r, 1);
        continue;
      }
      s.p = i, i.marker = !0;
    }
    (e < s.index || n > 0 && e === s.index) && (s.index = gt(e, s.index + n));
  }
}, Pr = (t, e, n) => {
  const r = t, s = e.changedParentTypes;
  for (; at(s, t, () => []).push(n), t._item !== null; )
    t = /** @type {AbstractType<any>} */
    t._item.parent;
  Wc(r._eH, n, e);
};
class ce {
  constructor() {
    this._item = null, this._map = /* @__PURE__ */ new Map(), this._start = null, this.doc = null, this._length = 0, this._eH = $o(), this._dEH = $o(), this._searchMarker = null;
  }
  /**
   * @return {AbstractType<any>|null}
   */
  get parent() {
    return this._item ? (
      /** @type {AbstractType<any>} */
      this._item.parent
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item|null} item
   */
  _integrate(e, n) {
    this.doc = e, this._item = n;
  }
  /**
   * @return {AbstractType<EventType>}
   */
  _copy() {
    throw Ve();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {AbstractType<EventType>}
   */
  clone() {
    throw Ve();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} _encoder
   */
  _write(e) {
  }
  /**
   * The first non-deleted item
   */
  get _first() {
    let e = this._start;
    for (; e !== null && e.deleted; )
      e = e.right;
    return e;
  }
  /**
   * Creates YEvent and calls all type observers.
   * Must be implemented by each type.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} _parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    !e.local && this._searchMarker && (this._searchMarker.length = 0);
  }
  /**
   * Observe all events that are created on this type.
   *
   * @param {function(EventType, Transaction):void} f Observer function
   */
  observe(e) {
    Ho(this._eH, e);
  }
  /**
   * Observe all events that are created by this type and its children.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  observeDeep(e) {
    Ho(this._dEH, e);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(EventType,Transaction):void} f Observer function
   */
  unobserve(e) {
    jo(this._eH, e);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  unobserveDeep(e) {
    jo(this._dEH, e);
  }
  /**
   * @abstract
   * @return {any}
   */
  toJSON() {
  }
}
const td = (t, e, n) => {
  t.doc ?? me(), e < 0 && (e = t._length + e), n < 0 && (n = t._length + n);
  let r = n - e;
  const s = [];
  let i = t._start;
  for (; i !== null && r > 0; ) {
    if (i.countable && !i.deleted) {
      const o = i.content.getContent();
      if (o.length <= e)
        e -= o.length;
      else {
        for (let l = e; l < o.length && r > 0; l++)
          s.push(o[l]), r--;
        e = 0;
      }
    }
    i = i.right;
  }
  return s;
}, nd = (t) => {
  t.doc ?? me();
  const e = [];
  let n = t._start;
  for (; n !== null; ) {
    if (n.countable && !n.deleted) {
      const r = n.content.getContent();
      for (let s = 0; s < r.length; s++)
        e.push(r[s]);
    }
    n = n.right;
  }
  return e;
}, rd = (t, e) => {
  const n = [];
  let r = t._start;
  for (; r !== null; ) {
    if (r.countable && vt(r, e)) {
      const s = r.content.getContent();
      for (let i = 0; i < s.length; i++)
        n.push(s[i]);
    }
    r = r.right;
  }
  return n;
}, gn = (t, e) => {
  let n = 0, r = t._start;
  for (t.doc ?? me(); r !== null; ) {
    if (r.countable && !r.deleted) {
      const s = r.content.getContent();
      for (let i = 0; i < s.length; i++)
        e(s[i], n++, t);
    }
    r = r.right;
  }
}, sd = (t, e) => {
  const n = [];
  return gn(t, (r, s) => {
    n.push(e(r, s, t));
  }), n;
}, U0 = (t) => {
  let e = t._start, n = null, r = 0;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next: () => {
      if (n === null) {
        for (; e !== null && e.deleted; )
          e = e.right;
        if (e === null)
          return {
            done: !0,
            value: void 0
          };
        n = e.content.getContent(), r = 0, e = e.right;
      }
      const s = n[r++];
      return n.length <= r && (n = null), {
        done: !1,
        value: s
      };
    }
  };
}, id = (t, e) => {
  t.doc ?? me();
  const n = zr(t, e);
  let r = t._start;
  for (n !== null && (r = n.p, e -= n.index); r !== null; r = r.right)
    if (!r.deleted && r.countable) {
      if (e < r.length)
        return r.content.getContent()[e];
      e -= r.length;
    }
}, hr = (t, e, n, r) => {
  let s = n;
  const i = t.doc, o = i.clientID, l = i.store, a = n === null ? e._start : n.right;
  let c = [];
  const u = () => {
    c.length > 0 && (s = new U(O(o, te(l, o)), s, s && s.lastId, a, a && a.id, e, null, new Tt(c)), s.integrate(t, 0), c = []);
  };
  r.forEach((d) => {
    if (d === null)
      c.push(d);
    else
      switch (d.constructor) {
        case Number:
        case Object:
        case Boolean:
        case Array:
        case String:
          c.push(d);
          break;
        default:
          switch (u(), d.constructor) {
            case Uint8Array:
            case ArrayBuffer:
              s = new U(O(o, te(l, o)), s, s && s.lastId, a, a && a.id, e, null, new In(new Uint8Array(
                /** @type {Uint8Array} */
                d
              ))), s.integrate(t, 0);
              break;
            case Rt:
              s = new U(O(o, te(l, o)), s, s && s.lastId, a, a && a.id, e, null, new _n(
                /** @type {Doc} */
                d
              )), s.integrate(t, 0);
              break;
            default:
              if (d instanceof ce)
                s = new U(O(o, te(l, o)), s, s && s.lastId, a, a && a.id, e, null, new qe(d)), s.integrate(t, 0);
              else
                throw new Error("Unexpected content type in insert operation");
          }
      }
  }), u();
}, od = () => bt("Length exceeded!"), ld = (t, e, n, r) => {
  if (n > e._length)
    throw od();
  if (n === 0)
    return e._searchMarker && mn(e._searchMarker, n, r.length), hr(t, e, null, r);
  const s = n, i = zr(e, n);
  let o = e._start;
  for (i !== null && (o = i.p, n -= i.index, n === 0 && (o = o.prev, n += o && o.countable && !o.deleted ? o.length : 0)); o !== null; o = o.right)
    if (!o.deleted && o.countable) {
      if (n <= o.length) {
        n < o.length && ve(t, O(o.id.client, o.id.clock + n));
        break;
      }
      n -= o.length;
    }
  return e._searchMarker && mn(e._searchMarker, s, r.length), hr(t, e, o, r);
}, W0 = (t, e, n) => {
  let s = (e._searchMarker || []).reduce((i, o) => o.index > i.index ? o : i, { index: 0, p: e._start }).p;
  if (s)
    for (; s.right; )
      s = s.right;
  return hr(t, e, s, n);
}, ad = (t, e, n, r) => {
  if (r === 0)
    return;
  const s = n, i = r, o = zr(e, n);
  let l = e._start;
  for (o !== null && (l = o.p, n -= o.index); l !== null && n > 0; l = l.right)
    !l.deleted && l.countable && (n < l.length && ve(t, O(l.id.client, l.id.clock + n)), n -= l.length);
  for (; r > 0 && l !== null; )
    l.deleted || (r < l.length && ve(t, O(l.id.client, l.id.clock + r)), l.delete(t), r -= l.length), l = l.right;
  if (r > 0)
    throw od();
  e._searchMarker && mn(
    e._searchMarker,
    s,
    -i + r
    /* in case we remove the above exception */
  );
}, pr = (t, e, n) => {
  const r = e._map.get(n);
  r !== void 0 && r.delete(t);
}, Ci = (t, e, n, r) => {
  const s = e._map.get(n) || null, i = t.doc, o = i.clientID;
  let l;
  if (r == null)
    l = new Tt([r]);
  else
    switch (r.constructor) {
      case Number:
      case Object:
      case Boolean:
      case Array:
      case String:
      case Date:
      case BigInt:
        l = new Tt([r]);
        break;
      case Uint8Array:
        l = new In(
          /** @type {Uint8Array} */
          r
        );
        break;
      case Rt:
        l = new _n(
          /** @type {Doc} */
          r
        );
        break;
      default:
        if (r instanceof ce)
          l = new qe(r);
        else
          throw new Error("Unexpected content type");
    }
  new U(O(o, te(i.store, o)), s, s && s.lastId, null, null, e, n, l).integrate(t, 0);
}, Si = (t, e) => {
  t.doc ?? me();
  const n = t._map.get(e);
  return n !== void 0 && !n.deleted ? n.content.getContent()[n.length - 1] : void 0;
}, cd = (t) => {
  const e = {};
  return t.doc ?? me(), t._map.forEach((n, r) => {
    n.deleted || (e[r] = n.content.getContent()[n.length - 1]);
  }), e;
}, dd = (t, e) => {
  t.doc ?? me();
  const n = t._map.get(e);
  return n !== void 0 && !n.deleted;
}, G0 = (t, e) => {
  const n = {};
  return t._map.forEach((r, s) => {
    let i = r;
    for (; i !== null && (!e.sv.has(i.id.client) || i.id.clock >= (e.sv.get(i.id.client) || 0)); )
      i = i.left;
    i !== null && vt(i, e) && (n[s] = i.content.getContent()[i.length - 1]);
  }), n;
}, Bn = (t) => (t.doc ?? me(), u0(
  t._map.entries(),
  /** @param {any} entry */
  (e) => !e[1].deleted
));
class K0 extends Fr {
}
class $t extends ce {
  constructor() {
    super(), this._prelimContent = [], this._searchMarker = [];
  }
  /**
   * Construct a new YArray containing the specified items.
   * @template {Object<string,any>|Array<any>|number|null|string|Uint8Array} T
   * @param {Array<T>} items
   * @return {YArray<T>}
   */
  static from(e) {
    const n = new $t();
    return n.push(e), n;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  /**
   * @return {YArray<T>}
   */
  _copy() {
    return new $t();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YArray<T>}
   */
  clone() {
    const e = new $t();
    return e.insert(0, this.toArray().map(
      (n) => n instanceof ce ? (
        /** @type {typeof el} */
        n.clone()
      ) : n
    )), e;
  }
  get length() {
    return this.doc ?? me(), this._length;
  }
  /**
   * Creates YArrayEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    super._callObserver(e, n), Pr(this, e, new K0(this, e));
  }
  /**
   * Inserts new content at an index.
   *
   * Important: This function expects an array of content. Not just a content
   * object. The reason for this "weirdness" is that inserting several elements
   * is very efficient when it is done as a single operation.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  yarray.insert(0, ['a'])
   *  // Insert numbers 1, 2 at position 1
   *  yarray.insert(1, [1, 2])
   *
   * @param {number} index The index to insert content at.
   * @param {Array<T>} content The array of content
   */
  insert(e, n) {
    this.doc !== null ? j(this.doc, (r) => {
      ld(
        r,
        this,
        e,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.splice(e, 0, ...n);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<T>} content Array of content to append.
   *
   * @todo Use the following implementation in all types.
   */
  push(e) {
    this.doc !== null ? j(this.doc, (n) => {
      W0(
        n,
        this,
        /** @type {any} */
        e
      );
    }) : this._prelimContent.push(...e);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<T>} content Array of content to prepend.
   */
  unshift(e) {
    this.insert(0, e);
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} length The number of elements to remove. Defaults to 1.
   */
  delete(e, n = 1) {
    this.doc !== null ? j(this.doc, (r) => {
      ad(r, this, e, n);
    }) : this._prelimContent.splice(e, n);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {T}
   */
  get(e) {
    return id(this, e);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<T>}
   */
  toArray() {
    return nd(this);
  }
  /**
   * Returns a portion of this YArray into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<T>}
   */
  slice(e = 0, n = this.length) {
    return td(this, e, n);
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Array<any>}
   */
  toJSON() {
    return this.map((e) => e instanceof ce ? e.toJSON() : e);
  }
  /**
   * Returns an Array with the result of calling a provided function on every
   * element of this YArray.
   *
   * @template M
   * @param {function(T,number,YArray<T>):M} f Function that produces an element of the new Array
   * @return {Array<M>} A new array with each element being the result of the
   *                 callback function
   */
  map(e) {
    return sd(
      this,
      /** @type {any} */
      e
    );
  }
  /**
   * Executes a provided function once on every element of this YArray.
   *
   * @param {function(T,number,YArray<T>):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    gn(this, e);
  }
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return U0(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(w1);
  }
}
const q0 = (t) => new $t();
class Y0 extends Fr {
  /**
   * @param {YMap<T>} ymap The YArray that changed.
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed.
   */
  constructor(e, n, r) {
    super(e, n), this.keysChanged = r;
  }
}
class Gt extends ce {
  /**
   *
   * @param {Iterable<readonly [string, any]>=} entries - an optional iterable to initialize the YMap
   */
  constructor(e) {
    super(), this._prelimContent = null, e === void 0 ? this._prelimContent = /* @__PURE__ */ new Map() : this._prelimContent = new Map(e);
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this._prelimContent.forEach((r, s) => {
      this.set(s, r);
    }), this._prelimContent = null;
  }
  /**
   * @return {YMap<MapType>}
   */
  _copy() {
    return new Gt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YMap<MapType>}
   */
  clone() {
    const e = new Gt();
    return this.forEach((n, r) => {
      e.set(r, n instanceof ce ? (
        /** @type {typeof value} */
        n.clone()
      ) : n);
    }), e;
  }
  /**
   * Creates YMapEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    Pr(this, e, new Y0(this, e, n));
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Object<string,any>}
   */
  toJSON() {
    this.doc ?? me();
    const e = {};
    return this._map.forEach((n, r) => {
      if (!n.deleted) {
        const s = n.content.getContent()[n.length - 1];
        e[r] = s instanceof ce ? s.toJSON() : s;
      }
    }), e;
  }
  /**
   * Returns the size of the YMap (count of key/value pairs)
   *
   * @return {number}
   */
  get size() {
    return [...Bn(this)].length;
  }
  /**
   * Returns the keys for each element in the YMap Type.
   *
   * @return {IterableIterator<string>}
   */
  keys() {
    return rs(
      Bn(this),
      /** @param {any} v */
      (e) => e[0]
    );
  }
  /**
   * Returns the values for each element in the YMap Type.
   *
   * @return {IterableIterator<MapType>}
   */
  values() {
    return rs(
      Bn(this),
      /** @param {any} v */
      (e) => e[1].content.getContent()[e[1].length - 1]
    );
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  entries() {
    return rs(
      Bn(this),
      /** @param {any} v */
      (e) => (
        /** @type {any} */
        [e[0], e[1].content.getContent()[e[1].length - 1]]
      )
    );
  }
  /**
   * Executes a provided function on once on every key-value pair.
   *
   * @param {function(MapType,string,YMap<MapType>):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    this.doc ?? me(), this._map.forEach((n, r) => {
      n.deleted || e(n.content.getContent()[n.length - 1], r, this);
    });
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * Remove a specified element from this YMap.
   *
   * @param {string} key The key of the element to remove.
   */
  delete(e) {
    this.doc !== null ? j(this.doc, (n) => {
      pr(n, this, e);
    }) : this._prelimContent.delete(e);
  }
  /**
   * Adds or updates an element with a specified key and value.
   * @template {MapType} VAL
   *
   * @param {string} key The key of the element to add to this YMap
   * @param {VAL} value The value of the element to add
   * @return {VAL}
   */
  set(e, n) {
    return this.doc !== null ? j(this.doc, (r) => {
      Ci(
        r,
        this,
        e,
        /** @type {any} */
        n
      );
    }) : this._prelimContent.set(e, n), n;
  }
  /**
   * Returns a specified element from this YMap.
   *
   * @param {string} key
   * @return {MapType|undefined}
   */
  get(e) {
    return (
      /** @type {any} */
      Si(this, e)
    );
  }
  /**
   * Returns a boolean indicating whether the specified key exists or not.
   *
   * @param {string} key The key to test.
   * @return {boolean}
   */
  has(e) {
    return dd(this, e);
  }
  /**
   * Removes all elements from this YMap.
   */
  clear() {
    this.doc !== null ? j(this.doc, (e) => {
      this.forEach(function(n, r, s) {
        pr(e, s, r);
      });
    }) : this._prelimContent.clear();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(x1);
  }
}
const J0 = (t) => new Gt(), ht = (t, e) => t === e || typeof t == "object" && typeof e == "object" && t && e && Hx(t, e);
class Bs {
  /**
   * @param {Item|null} left
   * @param {Item|null} right
   * @param {number} index
   * @param {Map<string,any>} currentAttributes
   */
  constructor(e, n, r, s) {
    this.left = e, this.right = n, this.index = r, this.currentAttributes = s;
  }
  /**
   * Only call this if you know that this.right is defined
   */
  forward() {
    this.right === null && Se(), this.right.content.constructor === re ? this.right.deleted || Xt(
      this.currentAttributes,
      /** @type {ContentFormat} */
      this.right.content
    ) : this.right.deleted || (this.index += this.right.length), this.left = this.right, this.right = this.right.right;
  }
}
const Jo = (t, e, n) => {
  for (; e.right !== null && n > 0; )
    e.right.content.constructor === re ? e.right.deleted || Xt(
      e.currentAttributes,
      /** @type {ContentFormat} */
      e.right.content
    ) : e.right.deleted || (n < e.right.length && ve(t, O(e.right.id.client, e.right.id.clock + n)), e.index += e.right.length, n -= e.right.length), e.left = e.right, e.right = e.right.right;
  return e;
}, $n = (t, e, n, r) => {
  const s = /* @__PURE__ */ new Map(), i = r ? zr(e, n) : null;
  if (i) {
    const o = new Bs(i.p.left, i.p, i.index, s);
    return Jo(t, o, n - i.index);
  } else {
    const o = new Bs(null, e._start, 0, s);
    return Jo(t, o, n);
  }
}, ud = (t, e, n, r) => {
  for (; n.right !== null && (n.right.deleted === !0 || n.right.content.constructor === re && ht(
    r.get(
      /** @type {ContentFormat} */
      n.right.content.key
    ),
    /** @type {ContentFormat} */
    n.right.content.value
  )); )
    n.right.deleted || r.delete(
      /** @type {ContentFormat} */
      n.right.content.key
    ), n.forward();
  const s = t.doc, i = s.clientID;
  r.forEach((o, l) => {
    const a = n.left, c = n.right, u = new U(O(i, te(s.store, i)), a, a && a.lastId, c, c && c.id, e, null, new re(l, o));
    u.integrate(t, 0), n.right = u, n.forward();
  });
}, Xt = (t, e) => {
  const { key: n, value: r } = e;
  r === null ? t.delete(n) : t.set(n, r);
}, fd = (t, e) => {
  for (; t.right !== null; ) {
    if (!(t.right.deleted || t.right.content.constructor === re && ht(
      e[
        /** @type {ContentFormat} */
        t.right.content.key
      ] ?? null,
      /** @type {ContentFormat} */
      t.right.content.value
    ))) break;
    t.forward();
  }
}, hd = (t, e, n, r) => {
  const s = t.doc, i = s.clientID, o = /* @__PURE__ */ new Map();
  for (const l in r) {
    const a = r[l], c = n.currentAttributes.get(l) ?? null;
    if (!ht(c, a)) {
      o.set(l, c);
      const { left: u, right: d } = n;
      n.right = new U(O(i, te(s.store, i)), u, u && u.lastId, d, d && d.id, e, null, new re(l, a)), n.right.integrate(t, 0), n.forward();
    }
  }
  return o;
}, os = (t, e, n, r, s) => {
  n.currentAttributes.forEach((h, p) => {
    s[p] === void 0 && (s[p] = null);
  });
  const i = t.doc, o = i.clientID;
  fd(n, s);
  const l = hd(t, e, n, s), a = r.constructor === String ? new We(
    /** @type {string} */
    r
  ) : r instanceof ce ? new qe(r) : new Lt(r);
  let { left: c, right: u, index: d } = n;
  e._searchMarker && mn(e._searchMarker, n.index, a.getLength()), u = new U(O(o, te(i.store, o)), c, c && c.lastId, u, u && u.id, e, null, a), u.integrate(t, 0), n.right = u, n.index = d, n.forward(), ud(t, e, n, l);
}, Xo = (t, e, n, r, s) => {
  const i = t.doc, o = i.clientID;
  fd(n, s);
  const l = hd(t, e, n, s);
  e: for (; n.right !== null && (r > 0 || l.size > 0 && (n.right.deleted || n.right.content.constructor === re)); ) {
    if (!n.right.deleted)
      switch (n.right.content.constructor) {
        case re: {
          const { key: a, value: c } = (
            /** @type {ContentFormat} */
            n.right.content
          ), u = s[a];
          if (u !== void 0) {
            if (ht(u, c))
              l.delete(a);
            else {
              if (r === 0)
                break e;
              l.set(a, c);
            }
            n.right.delete(t);
          } else
            n.currentAttributes.set(a, c);
          break;
        }
        default:
          r < n.right.length && ve(t, O(n.right.id.client, n.right.id.clock + r)), r -= n.right.length;
          break;
      }
    n.forward();
  }
  if (r > 0) {
    let a = "";
    for (; r > 0; r--)
      a += `
`;
    n.right = new U(O(o, te(i.store, o)), n.left, n.left && n.left.lastId, n.right, n.right && n.right.id, e, null, new We(a)), n.right.integrate(t, 0), n.forward();
  }
  ud(t, e, n, l);
}, pd = (t, e, n, r, s) => {
  let i = e;
  const o = je();
  for (; i && (!i.countable || i.deleted); ) {
    if (!i.deleted && i.content.constructor === re) {
      const c = (
        /** @type {ContentFormat} */
        i.content
      );
      o.set(c.key, c);
    }
    i = i.right;
  }
  let l = 0, a = !1;
  for (; e !== i; ) {
    if (n === e && (a = !0), !e.deleted) {
      const c = e.content;
      if (c.constructor === re) {
        const { key: u, value: d } = (
          /** @type {ContentFormat} */
          c
        ), h = r.get(u) ?? null;
        (o.get(u) !== c || h === d) && (e.delete(t), l++, !a && (s.get(u) ?? null) === d && h !== d && (h === null ? s.delete(u) : s.set(u, h))), !a && !e.deleted && Xt(
          s,
          /** @type {ContentFormat} */
          c
        );
      }
    }
    e = /** @type {Item} */
    e.right;
  }
  return l;
}, X0 = (t, e) => {
  for (; e && e.right && (e.right.deleted || !e.right.countable); )
    e = e.right;
  const n = /* @__PURE__ */ new Set();
  for (; e && (e.deleted || !e.countable); ) {
    if (!e.deleted && e.content.constructor === re) {
      const r = (
        /** @type {ContentFormat} */
        e.content.key
      );
      n.has(r) ? e.delete(t) : n.add(r);
    }
    e = e.left;
  }
}, Z0 = (t) => {
  let e = 0;
  return j(
    /** @type {Doc} */
    t.doc,
    (n) => {
      let r = (
        /** @type {Item} */
        t._start
      ), s = t._start, i = je();
      const o = As(i);
      for (; s; )
        s.deleted === !1 && (s.content.constructor === re ? Xt(
          o,
          /** @type {ContentFormat} */
          s.content
        ) : (e += pd(n, r, s, i, o), i = As(o), r = s)), s = s.right;
    }
  ), e;
}, Q0 = (t) => {
  const e = /* @__PURE__ */ new Set(), n = t.doc;
  for (const [r, s] of t.afterState.entries()) {
    const i = t.beforeState.get(r) || 0;
    s !== i && Yc(
      t,
      /** @type {Array<Item|GC>} */
      n.store.clients.get(r),
      i,
      s,
      (o) => {
        !o.deleted && /** @type {Item} */
        o.content.constructor === re && o.constructor !== De && e.add(
          /** @type {any} */
          o.parent
        );
      }
    );
  }
  j(n, (r) => {
    wt(t, t.deleteSet, (s) => {
      if (s instanceof De || !/** @type {YText} */
      s.parent._hasFormatting || e.has(
        /** @type {YText} */
        s.parent
      ))
        return;
      const i = (
        /** @type {YText} */
        s.parent
      );
      s.content.constructor === re ? e.add(i) : X0(r, s);
    });
    for (const s of e)
      Z0(s);
  });
}, Zo = (t, e, n) => {
  const r = n, s = As(e.currentAttributes), i = e.right;
  for (; n > 0 && e.right !== null; ) {
    if (e.right.deleted === !1)
      switch (e.right.content.constructor) {
        case qe:
        case Lt:
        case We:
          n < e.right.length && ve(t, O(e.right.id.client, e.right.id.clock + n)), n -= e.right.length, e.right.delete(t);
          break;
      }
    e.forward();
  }
  i && pd(t, i, e.right, s, e.currentAttributes);
  const o = (
    /** @type {AbstractType<any>} */
    /** @type {Item} */
    (e.left || e.right).parent
  );
  return o._searchMarker && mn(o._searchMarker, e.index, -r + n), e;
};
class e1 extends Fr {
  /**
   * @param {YText} ytext
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed
   */
  constructor(e, n, r) {
    super(e, n), this.childListChanged = !1, this.keysChanged = /* @__PURE__ */ new Set(), r.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.keysChanged.add(s);
    });
  }
  /**
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    if (this._changes === null) {
      const e = {
        keys: this.keys,
        delta: this.delta,
        added: /* @__PURE__ */ new Set(),
        deleted: /* @__PURE__ */ new Set()
      };
      this._changes = e;
    }
    return (
      /** @type {any} */
      this._changes
    );
  }
  /**
   * Compute the changes in the delta format.
   * A {@link https://quilljs.com/docs/delta/|Quill Delta}) that represents the changes on the document.
   *
   * @type {Array<{insert?:string|object|AbstractType<any>, delete?:number, retain?:number, attributes?: Object<string,any>}>}
   *
   * @public
   */
  get delta() {
    if (this._delta === null) {
      const e = (
        /** @type {Doc} */
        this.target.doc
      ), n = [];
      j(e, (r) => {
        const s = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
        let o = this.target._start, l = null;
        const a = {};
        let c = "", u = 0, d = 0;
        const h = () => {
          if (l !== null) {
            let p = null;
            switch (l) {
              case "delete":
                d > 0 && (p = { delete: d }), d = 0;
                break;
              case "insert":
                (typeof c == "object" || c.length > 0) && (p = { insert: c }, s.size > 0 && (p.attributes = {}, s.forEach((m, g) => {
                  m !== null && (p.attributes[g] = m);
                }))), c = "";
                break;
              case "retain":
                u > 0 && (p = { retain: u }, Bx(a) || (p.attributes = zx({}, a))), u = 0;
                break;
            }
            p && n.push(p), l = null;
          }
        };
        for (; o !== null; ) {
          switch (o.content.constructor) {
            case qe:
            case Lt:
              this.adds(o) ? this.deletes(o) || (h(), l = "insert", c = o.content.getContent()[0], h()) : this.deletes(o) ? (l !== "delete" && (h(), l = "delete"), d += 1) : o.deleted || (l !== "retain" && (h(), l = "retain"), u += 1);
              break;
            case We:
              this.adds(o) ? this.deletes(o) || (l !== "insert" && (h(), l = "insert"), c += /** @type {ContentString} */
              o.content.str) : this.deletes(o) ? (l !== "delete" && (h(), l = "delete"), d += o.length) : o.deleted || (l !== "retain" && (h(), l = "retain"), u += o.length);
              break;
            case re: {
              const { key: p, value: m } = (
                /** @type {ContentFormat} */
                o.content
              );
              if (this.adds(o)) {
                if (!this.deletes(o)) {
                  const g = s.get(p) ?? null;
                  ht(g, m) ? m !== null && o.delete(r) : (l === "retain" && h(), ht(m, i.get(p) ?? null) ? delete a[p] : a[p] = m);
                }
              } else if (this.deletes(o)) {
                i.set(p, m);
                const g = s.get(p) ?? null;
                ht(g, m) || (l === "retain" && h(), a[p] = g);
              } else if (!o.deleted) {
                i.set(p, m);
                const g = a[p];
                g !== void 0 && (ht(g, m) ? g !== null && o.delete(r) : (l === "retain" && h(), m === null ? delete a[p] : a[p] = m));
              }
              o.deleted || (l === "insert" && h(), Xt(
                s,
                /** @type {ContentFormat} */
                o.content
              ));
              break;
            }
          }
          o = o.right;
        }
        for (h(); n.length > 0; ) {
          const p = n[n.length - 1];
          if (p.retain !== void 0 && p.attributes === void 0)
            n.pop();
          else
            break;
        }
      }), this._delta = n;
    }
    return (
      /** @type {any} */
      this._delta
    );
  }
}
class xt extends ce {
  /**
   * @param {String} [string] The initial value of the YText.
   */
  constructor(e) {
    super(), this._pending = e !== void 0 ? [() => this.insert(0, e)] : [], this._searchMarker = [], this._hasFormatting = !1;
  }
  /**
   * Number of characters of this text type.
   *
   * @type {number}
   */
  get length() {
    return this.doc ?? me(), this._length;
  }
  /**
   * @param {Doc} y
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n);
    try {
      this._pending.forEach((r) => r());
    } catch (r) {
      console.error(r);
    }
    this._pending = null;
  }
  _copy() {
    return new xt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YText}
   */
  clone() {
    const e = new xt();
    return e.applyDelta(this.toDelta()), e;
  }
  /**
   * Creates YTextEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    super._callObserver(e, n);
    const r = new e1(this, e, n);
    Pr(this, e, r), !e.local && this._hasFormatting && (e._needFormattingCleanup = !0);
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @public
   */
  toString() {
    this.doc ?? me();
    let e = "", n = this._start;
    for (; n !== null; )
      !n.deleted && n.countable && n.content.constructor === We && (e += /** @type {ContentString} */
      n.content.str), n = n.right;
    return e;
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @return {string}
   * @public
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Apply a {@link Delta} on this shared YText type.
   *
   * @param {Array<any>} delta The changes to apply on this element.
   * @param {object}  opts
   * @param {boolean} [opts.sanitize] Sanitize input delta. Removes ending newlines if set to true.
   *
   *
   * @public
   */
  applyDelta(e, { sanitize: n = !0 } = {}) {
    this.doc !== null ? j(this.doc, (r) => {
      const s = new Bs(null, this._start, 0, /* @__PURE__ */ new Map());
      for (let i = 0; i < e.length; i++) {
        const o = e[i];
        if (o.insert !== void 0) {
          const l = !n && typeof o.insert == "string" && i === e.length - 1 && s.right === null && o.insert.slice(-1) === `
` ? o.insert.slice(0, -1) : o.insert;
          (typeof l != "string" || l.length > 0) && os(r, this, s, l, o.attributes || {});
        } else o.retain !== void 0 ? Xo(r, this, s, o.retain, o.attributes || {}) : o.delete !== void 0 && Zo(r, s, o.delete);
      }
    }) : this._pending.push(() => this.applyDelta(e));
  }
  /**
   * Returns the Delta representation of this YText type.
   *
   * @param {Snapshot} [snapshot]
   * @param {Snapshot} [prevSnapshot]
   * @param {function('removed' | 'added', ID):any} [computeYChange]
   * @return {any} The Delta representation of this type.
   *
   * @public
   */
  toDelta(e, n, r) {
    this.doc ?? me();
    const s = [], i = /* @__PURE__ */ new Map(), o = (
      /** @type {Doc} */
      this.doc
    );
    let l = "", a = this._start;
    function c() {
      if (l.length > 0) {
        const d = {};
        let h = !1;
        i.forEach((m, g) => {
          h = !0, d[g] = m;
        });
        const p = { insert: l };
        h && (p.attributes = d), s.push(p), l = "";
      }
    }
    const u = () => {
      for (; a !== null; ) {
        if (vt(a, e) || n !== void 0 && vt(a, n))
          switch (a.content.constructor) {
            case We: {
              const d = i.get("ychange");
              e !== void 0 && !vt(a, e) ? (d === void 0 || d.user !== a.id.client || d.type !== "removed") && (c(), i.set("ychange", r ? r("removed", a.id) : { type: "removed" })) : n !== void 0 && !vt(a, n) ? (d === void 0 || d.user !== a.id.client || d.type !== "added") && (c(), i.set("ychange", r ? r("added", a.id) : { type: "added" })) : d !== void 0 && (c(), i.delete("ychange")), l += /** @type {ContentString} */
              a.content.str;
              break;
            }
            case qe:
            case Lt: {
              c();
              const d = {
                insert: a.content.getContent()[0]
              };
              if (i.size > 0) {
                const h = (
                  /** @type {Object<string,any>} */
                  {}
                );
                d.attributes = h, i.forEach((p, m) => {
                  h[m] = p;
                });
              }
              s.push(d);
              break;
            }
            case re:
              vt(a, e) && (c(), Xt(
                i,
                /** @type {ContentFormat} */
                a.content
              ));
              break;
          }
        a = a.right;
      }
      c();
    };
    return e || n ? j(o, (d) => {
      e && zs(d, e), n && zs(d, n), u();
    }, "cleanup") : u(), s;
  }
  /**
   * Insert text at a given index.
   *
   * @param {number} index The index at which to start inserting.
   * @param {String} text The text to insert at the specified position.
   * @param {TextAttributes} [attributes] Optionally define some formatting
   *                                    information to apply on the inserted
   *                                    Text.
   * @public
   */
  insert(e, n, r) {
    if (n.length <= 0)
      return;
    const s = this.doc;
    s !== null ? j(s, (i) => {
      const o = $n(i, this, e, !r);
      r || (r = {}, o.currentAttributes.forEach((l, a) => {
        r[a] = l;
      })), os(i, this, o, n, r);
    }) : this._pending.push(() => this.insert(e, n, r));
  }
  /**
   * Inserts an embed at a index.
   *
   * @param {number} index The index to insert the embed at.
   * @param {Object | AbstractType<any>} embed The Object that represents the embed.
   * @param {TextAttributes} [attributes] Attribute information to apply on the
   *                                    embed
   *
   * @public
   */
  insertEmbed(e, n, r) {
    const s = this.doc;
    s !== null ? j(s, (i) => {
      const o = $n(i, this, e, !r);
      os(i, this, o, n, r || {});
    }) : this._pending.push(() => this.insertEmbed(e, n, r || {}));
  }
  /**
   * Deletes text starting from an index.
   *
   * @param {number} index Index at which to start deleting.
   * @param {number} length The number of characters to remove. Defaults to 1.
   *
   * @public
   */
  delete(e, n) {
    if (n === 0)
      return;
    const r = this.doc;
    r !== null ? j(r, (s) => {
      Zo(s, $n(s, this, e, !0), n);
    }) : this._pending.push(() => this.delete(e, n));
  }
  /**
   * Assigns properties to a range of text.
   *
   * @param {number} index The position where to start formatting.
   * @param {number} length The amount of characters to assign properties to.
   * @param {TextAttributes} attributes Attribute information to apply on the
   *                                    text.
   *
   * @public
   */
  format(e, n, r) {
    if (n === 0)
      return;
    const s = this.doc;
    s !== null ? j(s, (i) => {
      const o = $n(i, this, e, !1);
      o.right !== null && Xo(i, this, o, n, r);
    }) : this._pending.push(() => this.format(e, n, r));
  }
  /**
   * Removes an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(e) {
    this.doc !== null ? j(this.doc, (n) => {
      pr(n, this, e);
    }) : this._pending.push(() => this.removeAttribute(e));
  }
  /**
   * Sets or updates an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be set.
   * @param {any} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(e, n) {
    this.doc !== null ? j(this.doc, (r) => {
      Ci(r, this, e, n);
    }) : this._pending.push(() => this.setAttribute(e, n));
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {any} The queried attribute value.
   *
   * @public
   */
  getAttribute(e) {
    return (
      /** @type {any} */
      Si(this, e)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @return {Object<string, any>} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes() {
    return cd(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(y1);
  }
}
const t1 = (t) => new xt();
class ls {
  /**
   * @param {YXmlFragment | YXmlElement} root
   * @param {function(AbstractType<any>):boolean} [f]
   */
  constructor(e, n = () => !0) {
    this._filter = n, this._root = e, this._currentNode = /** @type {Item} */
    e._start, this._firstCall = !0, e.doc ?? me();
  }
  [Symbol.iterator]() {
    return this;
  }
  /**
   * Get the next node.
   *
   * @return {IteratorResult<YXmlElement|YXmlText|YXmlHook>} The next node.
   *
   * @public
   */
  next() {
    let e = this._currentNode, n = e && e.content && /** @type {any} */
    e.content.type;
    if (e !== null && (!this._firstCall || e.deleted || !this._filter(n)))
      do
        if (n = /** @type {any} */
        e.content.type, !e.deleted && (n.constructor === pe || n.constructor === At) && n._start !== null)
          e = n._start;
        else
          for (; e !== null; ) {
            const r = e.next;
            if (r !== null) {
              e = r;
              break;
            } else e.parent === this._root ? e = null : e = /** @type {AbstractType<any>} */
            e.parent._item;
          }
      while (e !== null && (e.deleted || !this._filter(
        /** @type {ContentType} */
        e.content.type
      )));
    return this._firstCall = !1, e === null ? { value: void 0, done: !0 } : (this._currentNode = e, { value: (
      /** @type {any} */
      e.content.type
    ), done: !1 });
  }
}
class At extends ce {
  constructor() {
    super(), this._prelimContent = [];
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get firstChild() {
    const e = this._first;
    return e ? e.content.getContent()[0] : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  _copy() {
    return new At();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlFragment}
   */
  clone() {
    const e = new At();
    return e.insert(0, this.toArray().map((n) => n instanceof ce ? n.clone() : n)), e;
  }
  get length() {
    return this.doc ?? me(), this._prelimContent === null ? this._length : this._prelimContent.length;
  }
  /**
   * Create a subtree of childNodes.
   *
   * @example
   * const walker = elem.createTreeWalker(dom => dom.nodeName === 'div')
   * for (let node in walker) {
   *   // `node` is a div node
   *   nop(node)
   * }
   *
   * @param {function(AbstractType<any>):boolean} filter Function that is called on each child element and
   *                          returns a Boolean indicating whether the child
   *                          is to be included in the subtree.
   * @return {YXmlTreeWalker} A subtree and a position within it.
   *
   * @public
   */
  createTreeWalker(e) {
    return new ls(this, e);
  }
  /**
   * Returns the first YXmlElement that matches the query.
   * Similar to DOM's {@link querySelector}.
   *
   * Query support:
   *   - tagname
   * TODO:
   *   - id
   *   - attribute
   *
   * @param {CSS_Selector} query The query on the children.
   * @return {YXmlElement|YXmlText|YXmlHook|null} The first element that matches the query or null.
   *
   * @public
   */
  querySelector(e) {
    e = e.toUpperCase();
    const r = new ls(this, (s) => s.nodeName && s.nodeName.toUpperCase() === e).next();
    return r.done ? null : r.value;
  }
  /**
   * Returns all YXmlElements that match the query.
   * Similar to Dom's {@link querySelectorAll}.
   *
   * @todo Does not yet support all queries. Currently only query by tagName.
   *
   * @param {CSS_Selector} query The query on the children
   * @return {Array<YXmlElement|YXmlText|YXmlHook|null>} The elements that match this query.
   *
   * @public
   */
  querySelectorAll(e) {
    return e = e.toUpperCase(), _t(new ls(this, (n) => n.nodeName && n.nodeName.toUpperCase() === e));
  }
  /**
   * Creates YXmlEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(e, n) {
    Pr(this, e, new s1(this, n, e));
  }
  /**
   * Get the string representation of all the children of this YXmlFragment.
   *
   * @return {string} The string representation of all children.
   */
  toString() {
    return sd(this, (e) => e.toString()).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, r) {
    const s = e.createDocumentFragment();
    return r !== void 0 && r._createAssociation(s, this), gn(this, (i) => {
      s.insertBefore(i.toDOM(e, n, r), null);
    }), s;
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {number} index The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insert(e, n) {
    this.doc !== null ? j(this.doc, (r) => {
      ld(r, this, e, n);
    }) : this._prelimContent.splice(e, 0, ...n);
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {null|Item|YXmlElement|YXmlText} ref The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insertAfter(e, n) {
    if (this.doc !== null)
      j(this.doc, (r) => {
        const s = e && e instanceof ce ? e._item : e;
        hr(r, this, s, n);
      });
    else {
      const r = (
        /** @type {Array<any>} */
        this._prelimContent
      ), s = e === null ? 0 : r.findIndex((i) => i === e) + 1;
      if (s === 0 && e !== null)
        throw bt("Reference item not found");
      r.splice(s, 0, ...n);
    }
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} [length=1] The number of elements to remove. Defaults to 1.
   */
  delete(e, n = 1) {
    this.doc !== null ? j(this.doc, (r) => {
      ad(r, this, e, n);
    }) : this._prelimContent.splice(e, n);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<YXmlElement|YXmlText|YXmlHook>}
   */
  toArray() {
    return nd(this);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to append.
   */
  push(e) {
    this.insert(this.length, e);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to prepend.
   */
  unshift(e) {
    this.insert(0, e);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {YXmlElement|YXmlText}
   */
  get(e) {
    return id(this, e);
  }
  /**
   * Returns a portion of this YXmlFragment into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<YXmlElement|YXmlText>}
   */
  slice(e = 0, n = this.length) {
    return td(this, e, n);
  }
  /**
   * Executes a provided function on once on every child element.
   *
   * @param {function(YXmlElement|YXmlText,number, typeof self):void} f A function to execute on every element of this YArray.
   */
  forEach(e) {
    gn(this, e);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(k1);
  }
}
const n1 = (t) => new At();
class pe extends At {
  constructor(e = "UNDEFINED") {
    super(), this.nodeName = e, this._prelimAttrs = /* @__PURE__ */ new Map();
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const e = this._item ? this._item.next : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const e = this._item ? this._item.prev : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(e, n) {
    super._integrate(e, n), /** @type {Map<string, any>} */
    this._prelimAttrs.forEach((r, s) => {
      this.setAttribute(s, r);
    }), this._prelimAttrs = null;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   *
   * @return {YXmlElement}
   */
  _copy() {
    return new pe(this.nodeName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlElement<KV>}
   */
  clone() {
    const e = new pe(this.nodeName), n = this.getAttributes();
    return Px(n, (r, s) => {
      typeof r == "string" && e.setAttribute(s, r);
    }), e.insert(0, this.toArray().map((r) => r instanceof ce ? r.clone() : r)), e;
  }
  /**
   * Returns the XML serialization of this YXmlElement.
   * The attributes are ordered by attribute-name, so you can easily use this
   * method to compare YXmlElements
   *
   * @return {string} The string representation of this type.
   *
   * @public
   */
  toString() {
    const e = this.getAttributes(), n = [], r = [];
    for (const l in e)
      r.push(l);
    r.sort();
    const s = r.length;
    for (let l = 0; l < s; l++) {
      const a = r[l];
      n.push(a + '="' + e[a] + '"');
    }
    const i = this.nodeName.toLocaleLowerCase(), o = n.length > 0 ? " " + n.join(" ") : "";
    return `<${i}${o}>${super.toString()}</${i}>`;
  }
  /**
   * Removes an attribute from this YXmlElement.
   *
   * @param {string} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(e) {
    this.doc !== null ? j(this.doc, (n) => {
      pr(n, this, e);
    }) : this._prelimAttrs.delete(e);
  }
  /**
   * Sets or updates an attribute.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that is to be set.
   * @param {KV[KEY]} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(e, n) {
    this.doc !== null ? j(this.doc, (r) => {
      Ci(r, this, e, n);
    }) : this._prelimAttrs.set(e, n);
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {KV[KEY]|undefined} The queried attribute value.
   *
   * @public
   */
  getAttribute(e) {
    return (
      /** @type {any} */
      Si(this, e)
    );
  }
  /**
   * Returns whether an attribute exists
   *
   * @param {string} attributeName The attribute name to check for existence.
   * @return {boolean} whether the attribute exists.
   *
   * @public
   */
  hasAttribute(e) {
    return (
      /** @type {any} */
      dd(this, e)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @param {Snapshot} [snapshot]
   * @return {{ [Key in Extract<keyof KV,string>]?: KV[Key]}} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes(e) {
    return (
      /** @type {any} */
      e ? G0(this, e) : cd(this)
    );
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, r) {
    const s = e.createElement(this.nodeName), i = this.getAttributes();
    for (const o in i) {
      const l = i[o];
      typeof l == "string" && s.setAttribute(o, l);
    }
    return gn(this, (o) => {
      s.appendChild(o.toDOM(e, n, r));
    }), r !== void 0 && r._createAssociation(s, this), s;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(v1), e.writeKey(this.nodeName);
  }
}
const r1 = (t) => new pe(t.readKey());
class s1 extends Fr {
  /**
   * @param {YXmlElement|YXmlText|YXmlFragment} target The target on which the event is created.
   * @param {Set<string|null>} subs The set of changed attributes. `null` is included if the
   *                   child list changed.
   * @param {Transaction} transaction The transaction instance with which the
   *                                  change was created.
   */
  constructor(e, n, r) {
    super(e, r), this.childListChanged = !1, this.attributesChanged = /* @__PURE__ */ new Set(), n.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.attributesChanged.add(s);
    });
  }
}
class mr extends Gt {
  /**
   * @param {string} hookName nodeName of the Dom Node.
   */
  constructor(e) {
    super(), this.hookName = e;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   */
  _copy() {
    return new mr(this.hookName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlHook}
   */
  clone() {
    const e = new mr(this.hookName);
    return this.forEach((n, r) => {
      e.set(r, n);
    }), e;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object.<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type
   * @return {Element} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n = {}, r) {
    const s = n[this.hookName];
    let i;
    return s !== void 0 ? i = s.createDom(this) : i = document.createElement(this.hookName), i.setAttribute("data-yjs-hook", this.hookName), r !== void 0 && r._createAssociation(i, this), i;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(e) {
    e.writeTypeRef(C1), e.writeKey(this.hookName);
  }
}
const i1 = (t) => new mr(t.readKey());
class Ce extends xt {
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const e = this._item ? this._item.next : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const e = this._item ? this._item.prev : null;
    return e ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      e.content.type
    ) : null;
  }
  _copy() {
    return new Ce();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlText}
   */
  clone() {
    const e = new Ce();
    return e.applyDelta(this.toDelta()), e;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlText.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Text} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(e = document, n, r) {
    const s = e.createTextNode(this.toString());
    return r !== void 0 && r._createAssociation(s, this), s;
  }
  toString() {
    return this.toDelta().map((e) => {
      const n = [];
      for (const s in e.attributes) {
        const i = [];
        for (const o in e.attributes[s])
          i.push({ key: o, value: e.attributes[s][o] });
        i.sort((o, l) => o.key < l.key ? -1 : 1), n.push({ nodeName: s, attrs: i });
      }
      n.sort((s, i) => s.nodeName < i.nodeName ? -1 : 1);
      let r = "";
      for (let s = 0; s < n.length; s++) {
        const i = n[s];
        r += `<${i.nodeName}`;
        for (let o = 0; o < i.attrs.length; o++) {
          const l = i.attrs[o];
          r += ` ${l.key}="${l.value}"`;
        }
        r += ">";
      }
      r += e.insert;
      for (let s = n.length - 1; s >= 0; s--)
        r += `</${n[s].nodeName}>`;
      return r;
    }).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(e) {
    e.writeTypeRef(S1);
  }
}
const o1 = (t) => new Ce();
class Ni {
  /**
   * @param {ID} id
   * @param {number} length
   */
  constructor(e, n) {
    this.id = e, this.length = n;
  }
  /**
   * @type {boolean}
   */
  get deleted() {
    throw Ve();
  }
  /**
   * Merge this struct with the item to the right.
   * This method is already assuming that `this.id.clock + this.length === this.id.clock`.
   * Also this method does *not* remove right from StructStore!
   * @param {AbstractStruct} right
   * @return {boolean} whether this merged with right
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   * @param {number} encodingRef
   */
  write(e, n, r) {
    throw Ve();
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    throw Ve();
  }
}
const l1 = 0;
class De extends Ni {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {GC} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.constructor !== e.constructor ? !1 : (this.length += e.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    n > 0 && (this.id.clock += n, this.length -= n), qc(e.doc.store, this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeInfo(l1), e.writeLen(this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    return null;
  }
}
class In {
  /**
   * @param {Uint8Array} content
   */
  constructor(e) {
    this.content = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.content];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentBinary}
   */
  copy() {
    return new In(this.content);
  }
  /**
   * @param {number} offset
   * @return {ContentBinary}
   */
  splice(e) {
    throw Ve();
  }
  /**
   * @param {ContentBinary} right
   * @return {boolean}
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeBuf(this.content);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 3;
  }
}
const a1 = (t) => new In(t.readBuf());
class bn {
  /**
   * @param {number} len
   */
  constructor(e) {
    this.len = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.len;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentDeleted}
   */
  copy() {
    return new bn(this.len);
  }
  /**
   * @param {number} offset
   * @return {ContentDeleted}
   */
  splice(e) {
    const n = new bn(this.len - e);
    return this.len = e, n;
  }
  /**
   * @param {ContentDeleted} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.len += e.len, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    fn(e.deleteSet, n.id.client, n.id.clock, this.len), n.markDeleted();
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeLen(this.len - n);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 1;
  }
}
const c1 = (t) => new bn(t.readLen()), md = (t, e) => new Rt({ guid: t, ...e, shouldLoad: e.shouldLoad || e.autoLoad || !1 });
class _n {
  /**
   * @param {Doc} doc
   */
  constructor(e) {
    e._item && console.error("This document was already integrated as a sub-document. You should create a second instance instead with the same guid."), this.doc = e;
    const n = {};
    this.opts = n, e.gc || (n.gc = !1), e.autoLoad && (n.autoLoad = !0), e.meta !== null && (n.meta = e.meta);
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.doc];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentDoc}
   */
  copy() {
    return new _n(md(this.doc.guid, this.opts));
  }
  /**
   * @param {number} offset
   * @return {ContentDoc}
   */
  splice(e) {
    throw Ve();
  }
  /**
   * @param {ContentDoc} right
   * @return {boolean}
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    this.doc._item = n, e.subdocsAdded.add(this.doc), this.doc.shouldLoad && e.subdocsLoaded.add(this.doc);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
    e.subdocsAdded.has(this.doc) ? e.subdocsAdded.delete(this.doc) : e.subdocsRemoved.add(this.doc);
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeString(this.doc.guid), e.writeAny(this.opts);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 9;
  }
}
const d1 = (t) => new _n(md(t.readString(), t.readAny()));
class Lt {
  /**
   * @param {Object} embed
   */
  constructor(e) {
    this.embed = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.embed];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentEmbed}
   */
  copy() {
    return new Lt(this.embed);
  }
  /**
   * @param {number} offset
   * @return {ContentEmbed}
   */
  splice(e) {
    throw Ve();
  }
  /**
   * @param {ContentEmbed} right
   * @return {boolean}
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeJSON(this.embed);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 5;
  }
}
const u1 = (t) => new Lt(t.readJSON());
class re {
  /**
   * @param {string} key
   * @param {Object} value
   */
  constructor(e, n) {
    this.key = e, this.value = n;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentFormat}
   */
  copy() {
    return new re(this.key, this.value);
  }
  /**
   * @param {number} _offset
   * @return {ContentFormat}
   */
  splice(e) {
    throw Ve();
  }
  /**
   * @param {ContentFormat} _right
   * @return {boolean}
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {Transaction} _transaction
   * @param {Item} item
   */
  integrate(e, n) {
    const r = (
      /** @type {YText} */
      n.parent
    );
    r._searchMarker = null, r._hasFormatting = !0;
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeKey(this.key), e.writeJSON(this.value);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 6;
  }
}
const f1 = (t) => new re(t.readKey(), t.readJSON());
class gr {
  /**
   * @param {Array<any>} arr
   */
  constructor(e) {
    this.arr = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentJSON}
   */
  copy() {
    return new gr(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentJSON}
   */
  splice(e) {
    const n = new gr(this.arr.slice(e));
    return this.arr = this.arr.slice(0, e), n;
  }
  /**
   * @param {ContentJSON} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.arr = this.arr.concat(e.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    const r = this.arr.length;
    e.writeLen(r - n);
    for (let s = n; s < r; s++) {
      const i = this.arr[s];
      e.writeString(i === void 0 ? "undefined" : JSON.stringify(i));
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 2;
  }
}
const h1 = (t) => {
  const e = t.readLen(), n = [];
  for (let r = 0; r < e; r++) {
    const s = t.readString();
    s === "undefined" ? n.push(void 0) : n.push(JSON.parse(s));
  }
  return new gr(n);
}, p1 = dr("node_env") === "development";
class Tt {
  /**
   * @param {Array<any>} arr
   */
  constructor(e) {
    this.arr = e, p1 && Dc(e);
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentAny}
   */
  copy() {
    return new Tt(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentAny}
   */
  splice(e) {
    const n = new Tt(this.arr.slice(e));
    return this.arr = this.arr.slice(0, e), n;
  }
  /**
   * @param {ContentAny} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.arr = this.arr.concat(e.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    const r = this.arr.length;
    e.writeLen(r - n);
    for (let s = n; s < r; s++) {
      const i = this.arr[s];
      e.writeAny(i);
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 8;
  }
}
const m1 = (t) => {
  const e = t.readLen(), n = [];
  for (let r = 0; r < e; r++)
    n.push(t.readAny());
  return new Tt(n);
};
class We {
  /**
   * @param {string} str
   */
  constructor(e) {
    this.str = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.str.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.str.split("");
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentString}
   */
  copy() {
    return new We(this.str);
  }
  /**
   * @param {number} offset
   * @return {ContentString}
   */
  splice(e) {
    const n = new We(this.str.slice(e));
    this.str = this.str.slice(0, e);
    const r = this.str.charCodeAt(e - 1);
    return r >= 55296 && r <= 56319 && (this.str = this.str.slice(0, e - 1) + "�", n.str = "�" + n.str.slice(1)), n;
  }
  /**
   * @param {ContentString} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.str += e.str, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeString(n === 0 ? this.str : this.str.slice(n));
  }
  /**
   * @return {number}
   */
  getRef() {
    return 4;
  }
}
const g1 = (t) => new We(t.readString()), b1 = [
  q0,
  J0,
  t1,
  r1,
  n1,
  i1,
  o1
], w1 = 0, x1 = 1, y1 = 2, v1 = 3, k1 = 4, C1 = 5, S1 = 6;
class qe {
  /**
   * @param {AbstractType<any>} type
   */
  constructor(e) {
    this.type = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.type];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentType}
   */
  copy() {
    return new qe(this.type._copy());
  }
  /**
   * @param {number} offset
   * @return {ContentType}
   */
  splice(e) {
    throw Ve();
  }
  /**
   * @param {ContentType} right
   * @return {boolean}
   */
  mergeWith(e) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(e, n) {
    this.type._integrate(e.doc, n);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(e) {
    let n = this.type._start;
    for (; n !== null; )
      n.deleted ? n.id.clock < (e.beforeState.get(n.id.client) || 0) && e._mergeStructs.push(n) : n.delete(e), n = n.right;
    this.type._map.forEach((r) => {
      r.deleted ? r.id.clock < (e.beforeState.get(r.id.client) || 0) && e._mergeStructs.push(r) : r.delete(e);
    }), e.changed.delete(this.type);
  }
  /**
   * @param {StructStore} store
   */
  gc(e) {
    let n = this.type._start;
    for (; n !== null; )
      n.gc(e, !0), n = n.right;
    this.type._start = null, this.type._map.forEach(
      /** @param {Item | null} item */
      (r) => {
        for (; r !== null; )
          r.gc(e, !0), r = r.left;
      }
    ), this.type._map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    this.type._write(e);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 7;
  }
}
const N1 = (t) => new qe(b1[t.readTypeRef()](t)), $s = (t, e) => {
  let n = e, r = 0, s;
  do
    r > 0 && (n = O(n.client, n.clock + r)), s = Bt(t, n), r = n.clock - s.id.clock, n = s.redone;
  while (n !== null && s instanceof U);
  return {
    item: s,
    diff: r
  };
}, Ii = (t, e) => {
  for (; t !== null && t.keep !== e; )
    t.keep = e, t = /** @type {AbstractType<any>} */
    t.parent._item;
}, br = (t, e, n) => {
  const { client: r, clock: s } = e.id, i = new U(
    O(r, s + n),
    e,
    O(r, s + n - 1),
    e.right,
    e.rightOrigin,
    e.parent,
    e.parentSub,
    e.content.splice(n)
  );
  return e.deleted && i.markDeleted(), e.keep && (i.keep = !0), e.redone !== null && (i.redone = O(e.redone.client, e.redone.clock + n)), e.right = i, i.right !== null && (i.right.left = i), t._mergeStructs.push(i), i.parentSub !== null && i.right === null && i.parent._map.set(i.parentSub, i), e.length = n, i;
}, Qo = (t, e) => Jw(
  t,
  /** @param {StackItem} s */
  (n) => Jt(n.deletions, e)
), gd = (t, e, n, r, s, i) => {
  const o = t.doc, l = o.store, a = o.clientID, c = e.redone;
  if (c !== null)
    return ve(t, c);
  let u = (
    /** @type {AbstractType<any>} */
    e.parent._item
  ), d = null, h;
  if (u !== null && u.deleted === !0) {
    if (u.redone === null && (!n.has(u) || gd(t, u, n, r, s, i) === null))
      return null;
    for (; u.redone !== null; )
      u = ve(t, u.redone);
  }
  const p = u === null ? (
    /** @type {AbstractType<any>} */
    e.parent
  ) : (
    /** @type {ContentType} */
    u.content.type
  );
  if (e.parentSub === null) {
    for (d = e.left, h = e; d !== null; ) {
      let x = d;
      for (; x !== null && /** @type {AbstractType<any>} */
      x.parent._item !== u; )
        x = x.redone === null ? null : ve(t, x.redone);
      if (x !== null && /** @type {AbstractType<any>} */
      x.parent._item === u) {
        d = x;
        break;
      }
      d = d.left;
    }
    for (; h !== null; ) {
      let x = h;
      for (; x !== null && /** @type {AbstractType<any>} */
      x.parent._item !== u; )
        x = x.redone === null ? null : ve(t, x.redone);
      if (x !== null && /** @type {AbstractType<any>} */
      x.parent._item === u) {
        h = x;
        break;
      }
      h = h.right;
    }
  } else if (h = null, e.right && !s) {
    for (d = e; d !== null && d.right !== null && (d.right.redone || Jt(r, d.right.id) || Qo(i.undoStack, d.right.id) || Qo(i.redoStack, d.right.id)); )
      for (d = d.right; d.redone; ) d = ve(t, d.redone);
    if (d && d.right !== null)
      return null;
  } else
    d = p._map.get(e.parentSub) || null;
  const m = te(l, a), g = O(a, m), b = new U(
    g,
    d,
    d && d.lastId,
    h,
    h && h.id,
    p,
    e.parentSub,
    e.content.copy()
  );
  return e.redone = g, Ii(b, !0), b.integrate(t, 0), b;
};
let U = class Hs extends Ni {
  /**
   * @param {ID} id
   * @param {Item | null} left
   * @param {ID | null} origin
   * @param {Item | null} right
   * @param {ID | null} rightOrigin
   * @param {AbstractType<any>|ID|null} parent Is a type if integrated, is null if it is possible to copy parent from left or right, is ID before integration to search for it.
   * @param {string | null} parentSub
   * @param {AbstractContent} content
   */
  constructor(e, n, r, s, i, o, l, a) {
    super(e, a.getLength()), this.origin = r, this.left = n, this.right = s, this.rightOrigin = i, this.parent = o, this.parentSub = l, this.redone = null, this.content = a, this.info = this.content.isCountable() ? To : 0;
  }
  /**
   * This is used to mark the item as an indexed fast-search marker
   *
   * @type {boolean}
   */
  set marker(e) {
    (this.info & es) > 0 !== e && (this.info ^= es);
  }
  get marker() {
    return (this.info & es) > 0;
  }
  /**
   * If true, do not garbage collect this Item.
   */
  get keep() {
    return (this.info & Ao) > 0;
  }
  set keep(e) {
    this.keep !== e && (this.info ^= Ao);
  }
  get countable() {
    return (this.info & To) > 0;
  }
  /**
   * Whether this item was deleted or not.
   * @type {Boolean}
   */
  get deleted() {
    return (this.info & Qr) > 0;
  }
  set deleted(e) {
    this.deleted !== e && (this.info ^= Qr);
  }
  markDeleted() {
    this.info |= Qr;
  }
  /**
   * Return the creator clientID of the missing op or define missing items and return null.
   *
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    if (this.origin && this.origin.client !== this.id.client && this.origin.clock >= te(n, this.origin.client))
      return this.origin.client;
    if (this.rightOrigin && this.rightOrigin.client !== this.id.client && this.rightOrigin.clock >= te(n, this.rightOrigin.client))
      return this.rightOrigin.client;
    if (this.parent && this.parent.constructor === Pt && this.id.client !== this.parent.client && this.parent.clock >= te(n, this.parent.client))
      return this.parent.client;
    if (this.origin && (this.left = Vo(e, n, this.origin), this.origin = this.left.lastId), this.rightOrigin && (this.right = ve(e, this.rightOrigin), this.rightOrigin = this.right.id), this.left && this.left.constructor === De || this.right && this.right.constructor === De)
      this.parent = null;
    else if (!this.parent)
      this.left && this.left.constructor === Hs ? (this.parent = this.left.parent, this.parentSub = this.left.parentSub) : this.right && this.right.constructor === Hs && (this.parent = this.right.parent, this.parentSub = this.right.parentSub);
    else if (this.parent.constructor === Pt) {
      const r = Bt(n, this.parent);
      r.constructor === De ? this.parent = null : this.parent = /** @type {ContentType} */
      r.content.type;
    }
    return null;
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    if (n > 0 && (this.id.clock += n, this.left = Vo(e, e.doc.store, O(this.id.client, this.id.clock - 1)), this.origin = this.left.lastId, this.content = this.content.splice(n), this.length -= n), this.parent) {
      if (!this.left && (!this.right || this.right.left !== null) || this.left && this.left.right !== this.right) {
        let r = this.left, s;
        if (r !== null)
          s = r.right;
        else if (this.parentSub !== null)
          for (s = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; s !== null && s.left !== null; )
            s = s.left;
        else
          s = /** @type {AbstractType<any>} */
          this.parent._start;
        const i = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Set();
        for (; s !== null && s !== this.right; ) {
          if (o.add(s), i.add(s), zn(this.origin, s.origin)) {
            if (s.id.client < this.id.client)
              r = s, i.clear();
            else if (zn(this.rightOrigin, s.rightOrigin))
              break;
          } else if (s.origin !== null && o.has(Bt(e.doc.store, s.origin)))
            i.has(Bt(e.doc.store, s.origin)) || (r = s, i.clear());
          else
            break;
          s = s.right;
        }
        this.left = r;
      }
      if (this.left !== null) {
        const r = this.left.right;
        this.right = r, this.left.right = this;
      } else {
        let r;
        if (this.parentSub !== null)
          for (r = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; r !== null && r.left !== null; )
            r = r.left;
        else
          r = /** @type {AbstractType<any>} */
          this.parent._start, this.parent._start = this;
        this.right = r;
      }
      this.right !== null ? this.right.left = this : this.parentSub !== null && (this.parent._map.set(this.parentSub, this), this.left !== null && this.left.delete(e)), this.parentSub === null && this.countable && !this.deleted && (this.parent._length += this.length), qc(e.doc.store, this), this.content.integrate(e, this), Wo(
        e,
        /** @type {AbstractType<any>} */
        this.parent,
        this.parentSub
      ), /** @type {AbstractType<any>} */
      (this.parent._item !== null && /** @type {AbstractType<any>} */
      this.parent._item.deleted || this.parentSub !== null && this.right !== null) && this.delete(e);
    } else
      new De(this.id, this.length).integrate(e, 0);
  }
  /**
   * Returns the next non-deleted item
   */
  get next() {
    let e = this.right;
    for (; e !== null && e.deleted; )
      e = e.right;
    return e;
  }
  /**
   * Returns the previous non-deleted item
   */
  get prev() {
    let e = this.left;
    for (; e !== null && e.deleted; )
      e = e.left;
    return e;
  }
  /**
   * Computes the last content address of this Item.
   */
  get lastId() {
    return this.length === 1 ? this.id : O(this.id.client, this.id.clock + this.length - 1);
  }
  /**
   * Try to merge two items
   *
   * @param {Item} right
   * @return {boolean}
   */
  mergeWith(e) {
    if (this.constructor === e.constructor && zn(e.origin, this.lastId) && this.right === e && zn(this.rightOrigin, e.rightOrigin) && this.id.client === e.id.client && this.id.clock + this.length === e.id.clock && this.deleted === e.deleted && this.redone === null && e.redone === null && this.content.constructor === e.content.constructor && this.content.mergeWith(e.content)) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent._searchMarker
      );
      return n && n.forEach((r) => {
        r.p === e && (r.p = this, !this.deleted && this.countable && (r.index -= this.length));
      }), e.keep && (this.keep = !0), this.right = e.right, this.right !== null && (this.right.left = this), this.length += e.length, !0;
    }
    return !1;
  }
  /**
   * Mark this Item as deleted.
   *
   * @param {Transaction} transaction
   */
  delete(e) {
    if (!this.deleted) {
      const n = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      this.countable && this.parentSub === null && (n._length -= this.length), this.markDeleted(), fn(e.deleteSet, this.id.client, this.id.clock, this.length), Wo(e, n, this.parentSub), this.content.delete(e);
    }
  }
  /**
   * @param {StructStore} store
   * @param {boolean} parentGCd
   */
  gc(e, n) {
    if (!this.deleted)
      throw Se();
    this.content.gc(e), n ? E0(e, this, new De(this.id, this.length)) : this.content = new bn(this.length);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   */
  write(e, n) {
    const r = n > 0 ? O(this.id.client, this.id.clock + n - 1) : this.origin, s = this.rightOrigin, i = this.parentSub, o = this.content.getRef() & Dr | (r === null ? 0 : xe) | // origin is defined
    (s === null ? 0 : rt) | // right origin is defined
    (i === null ? 0 : cn);
    if (e.writeInfo(o), r !== null && e.writeLeftID(r), s !== null && e.writeRightID(s), r === null && s === null) {
      const l = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      if (l._item !== void 0) {
        const a = l._item;
        if (a === null) {
          const c = hn(l);
          e.writeParentInfo(!0), e.writeString(c);
        } else
          e.writeParentInfo(!1), e.writeLeftID(a.id);
      } else l.constructor === String ? (e.writeParentInfo(!0), e.writeString(l)) : l.constructor === Pt ? (e.writeParentInfo(!1), e.writeLeftID(l)) : Se();
      i !== null && e.writeString(i);
    }
    this.content.write(e, n);
  }
};
const bd = (t, e) => I1[e & Dr](t), I1 = [
  () => {
    Se();
  },
  // GC is not ItemContent
  c1,
  // 1
  h1,
  // 2
  a1,
  // 3
  g1,
  // 4
  u1,
  // 5
  f1,
  // 6
  N1,
  // 7
  m1,
  // 8
  d1,
  // 9
  () => {
    Se();
  }
  // 10 - Skip is not ItemContent
], _1 = 10;
class Be extends Ni {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {Skip} right
   * @return {boolean}
   */
  mergeWith(e) {
    return this.constructor !== e.constructor ? !1 : (this.length += e.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(e, n) {
    Se();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(e, n) {
    e.writeInfo(_1), B(e.restEncoder, this.length - n);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(e, n) {
    return null;
  }
}
const wd = (
  /** @type {any} */
  typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : {}
), xd = "__ $YJS$ __";
wd[xd] === !0 && console.error("Yjs was already imported. This breaks constructor checks and will lead to issues! - https://github.com/yjs/yjs/issues/438");
wd[xd] = !0;
const A1 = () => {
  let t = !0;
  return (e, n) => {
    if (t) {
      t = !1;
      try {
        e();
      } finally {
        t = !0;
      }
    } else n !== void 0 && n();
  };
}, T1 = /[\uD800-\uDBFF]/, E1 = /[\uDC00-\uDFFF]/, D1 = (t, e) => {
  let n = 0, r = 0;
  for (; n < t.length && n < e.length && t[n] === e[n]; )
    n++;
  for (n > 0 && T1.test(t[n - 1]) && n--; r + n < t.length && r + n < e.length && t[t.length - r - 1] === e[e.length - r - 1]; )
    r++;
  return r > 0 && E1.test(t[t.length - r]) && r--, {
    index: n,
    remove: t.length - n - r,
    insert: e.slice(n, e.length - r)
  };
}, R1 = D1, X = new Ne("y-sync"), st = new Ne("y-undo");
new Ne("yjs-cursor");
const Je = (t, e) => t >>> e | t << 32 - e, L1 = (t) => Je(t, 2) ^ Je(t, 13) ^ Je(t, 22), M1 = (t) => Je(t, 6) ^ Je(t, 11) ^ Je(t, 25), O1 = (t) => Je(t, 7) ^ Je(t, 18) ^ t >>> 3, F1 = (t) => Je(t, 17) ^ Je(t, 19) ^ t >>> 10, z1 = new Uint32Array([
  1116352408,
  1899447441,
  3049323471,
  3921009573,
  961987163,
  1508970993,
  2453635748,
  2870763221,
  3624381080,
  310598401,
  607225278,
  1426881987,
  1925078388,
  2162078206,
  2614888103,
  3248222580,
  3835390401,
  4022224774,
  264347078,
  604807628,
  770255983,
  1249150122,
  1555081692,
  1996064986,
  2554220882,
  2821834349,
  2952996808,
  3210313671,
  3336571891,
  3584528711,
  113926993,
  338241895,
  666307205,
  773529912,
  1294757372,
  1396182291,
  1695183700,
  1986661051,
  2177026350,
  2456956037,
  2730485921,
  2820302411,
  3259730800,
  3345764771,
  3516065817,
  3600352804,
  4094571909,
  275423344,
  430227734,
  506948616,
  659060556,
  883997877,
  958139571,
  1322822218,
  1537002063,
  1747873779,
  1955562222,
  2024104815,
  2227730452,
  2361852424,
  2428436474,
  2756734187,
  3204031479,
  3329325298
]), P1 = new Uint32Array([
  1779033703,
  3144134277,
  1013904242,
  2773480762,
  1359893119,
  2600822924,
  528734635,
  1541459225
]);
class B1 {
  constructor() {
    const e = new ArrayBuffer(320);
    this._H = new Uint32Array(e, 0, 8), this._H.set(P1), this._W = new Uint32Array(e, 64, 64);
  }
  _updateHash() {
    const e = this._H, n = this._W;
    for (let d = 16; d < 64; d++)
      n[d] = F1(n[d - 2]) + n[d - 7] + O1(n[d - 15]) + n[d - 16];
    let r = e[0], s = e[1], i = e[2], o = e[3], l = e[4], a = e[5], c = e[6], u = e[7];
    for (let d = 0, h, p; d < 64; d++)
      h = u + M1(l) + (l & a ^ ~l & c) + z1[d] + n[d] >>> 0, p = L1(r) + (r & s ^ r & i ^ s & i) >>> 0, u = c, c = a, a = l, l = o + h >>> 0, o = i, i = s, s = r, r = h + p >>> 0;
    e[0] += r, e[1] += s, e[2] += i, e[3] += o, e[4] += l, e[5] += a, e[6] += c, e[7] += u;
  }
  /**
   * Returns a 32-byte hash.
   *
   * @param {Uint8Array} data
   */
  digest(e) {
    let n = 0;
    for (; n + 56 <= e.length; ) {
      let o = 0;
      for (; o < 16 && n + 3 < e.length; o++)
        this._W[o] = e[n++] << 24 | e[n++] << 16 | e[n++] << 8 | e[n++];
      if (n % 64 !== 0) {
        for (this._W.fill(0, o, 16); n < e.length; )
          this._W[o] |= e[n] << (3 - n % 4) * 8, n++;
        this._W[o] |= xe << (3 - n % 4) * 8;
      }
      this._updateHash();
    }
    const r = n % 64 !== 0;
    this._W.fill(0, 0, 16);
    let s = 0;
    for (; n < e.length; s++)
      for (let o = 3; o >= 0 && n < e.length; o--)
        this._W[s] |= e[n++] << o * 8;
    r || (this._W[s - (n % 4 === 0 ? 0 : 1)] |= xe << (3 - n % 4) * 8), this._W[14] = e.byteLength / Xw, this._W[15] = e.byteLength * 8, this._updateHash();
    const i = new Uint8Array(32);
    for (let o = 0; o < this._H.length; o++)
      for (let l = 0; l < 4; l++)
        i[o * 4 + l] = this._H[o] >>> (3 - l) * 8;
    return i;
  }
}
const $1 = (t) => new B1().digest(t), H1 = (t) => {
  for (let n = 6; n < t.length; n++)
    t[n % 6] = t[n % 6] ^ t[n];
  return t.slice(0, 6);
}, j1 = (t) => Yx(H1($1(Jx(t)))), wr = (t, e) => e === void 0 ? !t.deleted : e.sv.has(t.id.client) && /** @type {number} */
e.sv.get(t.id.client) > t.id.clock && !Jt(e.ds, t.id), V1 = [{ light: "#ecd44433", dark: "#ecd444" }], U1 = (t, e, n) => {
  if (!t.has(n)) {
    if (t.size < e.length) {
      const r = It();
      t.forEach((s) => r.add(s)), e = e.filter((s) => !r.has(s));
    }
    t.set(n, Ex(e));
  }
  return (
    /** @type {ColorDef} */
    t.get(n)
  );
}, W1 = (t, {
  colors: e = V1,
  colorMapping: n = /* @__PURE__ */ new Map(),
  permanentUserData: r = null,
  onFirstRender: s = () => {
  },
  mapping: i
} = {}) => {
  let o = !1;
  const l = new K1(t, i), a = new Xe({
    props: {
      editable: (c) => {
        const u = X.getState(c);
        return u.snapshot == null && u.prevSnapshot == null;
      }
    },
    key: X,
    state: {
      /**
       * @returns {any}
       */
      init: (c, u) => ({
        type: t,
        doc: t.doc,
        binding: l,
        snapshot: null,
        prevSnapshot: null,
        isChangeOrigin: !1,
        isUndoRedoOperation: !1,
        addToHistory: !0,
        colors: e,
        colorMapping: n,
        permanentUserData: r
      }),
      apply: (c, u) => {
        const d = c.getMeta(X);
        if (d !== void 0) {
          u = Object.assign({}, u);
          for (const h in d)
            u[h] = d[h];
        }
        return u.addToHistory = c.getMeta("addToHistory") !== !1, u.isChangeOrigin = d !== void 0 && !!d.isChangeOrigin, u.isUndoRedoOperation = d !== void 0 && !!d.isChangeOrigin && !!d.isUndoRedoOperation, l.prosemirrorView !== null && d !== void 0 && (d.snapshot != null || d.prevSnapshot != null) && Mc(0, () => {
          l.prosemirrorView != null && (d.restore == null ? l._renderSnapshot(
            d.snapshot,
            d.prevSnapshot,
            u
          ) : (l._renderSnapshot(
            d.snapshot,
            d.snapshot,
            u
          ), delete u.restore, delete u.snapshot, delete u.prevSnapshot, l.mux(() => {
            l._prosemirrorChanged(
              l.prosemirrorView.state.doc
            );
          })));
        }), u;
      }
    },
    view: (c) => (l.initView(c), i == null && l._forceRerender(), s(), {
      update: () => {
        const u = a.getState(c.state);
        if (u.snapshot == null && u.prevSnapshot == null && // If the content doesn't change initially, we don't render anything to Yjs
        // If the content was cleared by a user action, we want to catch the change and
        // represent it in Yjs
        (o || c.state.doc.content.findDiffStart(
          c.state.doc.type.createAndFill().content
        ) !== null)) {
          if (o = !0, u.addToHistory === !1 && !u.isChangeOrigin) {
            const d = st.getState(c.state), h = d && d.undoManager;
            h && h.stopCapturing();
          }
          l.mux(() => {
            u.doc.transact((d) => {
              d.meta.set("addToHistory", u.addToHistory), l._prosemirrorChanged(c.state.doc);
            }, X);
          });
        }
      },
      destroy: () => {
        l.destroy();
      }
    })
  });
  return a;
}, G1 = (t, e, n) => {
  if (e !== null && e.anchor !== null && e.head !== null)
    if (e.type === "all")
      t.setSelection(new zf(t.doc));
    else if (e.type === "node") {
      const r = tr(
        n.doc,
        n.type,
        e.anchor,
        n.mapping
      );
      t.setSelection(Ml.create(t.doc, r));
    } else {
      const r = tr(
        n.doc,
        n.type,
        e.anchor,
        n.mapping
      ), s = tr(
        n.doc,
        n.type,
        e.head,
        n.mapping
      );
      if (r !== null && s !== null) {
        const i = $e.between(t.doc.resolve(r), t.doc.resolve(s));
        t.setSelection(i);
      }
    }
}, js = (t, e) => ({
  type: (
    /** @type {any} */
    e.selection.jsonID
  ),
  anchor: Gs(
    e.selection.anchor,
    t.type,
    t.mapping
  ),
  head: Gs(
    e.selection.head,
    t.type,
    t.mapping
  )
});
class K1 {
  /**
   * @param {Y.XmlFragment} yXmlFragment The bind source
   * @param {ProsemirrorMapping} mapping
   */
  constructor(e, n = /* @__PURE__ */ new Map()) {
    this.type = e, this.prosemirrorView = null, this.mux = A1(), this.mapping = n, this.isOMark = /* @__PURE__ */ new Map(), this._observeFunction = this._typeChanged.bind(this), this.doc = e.doc, this.beforeTransactionSelection = null, this.beforeAllTransactions = () => {
      this.beforeTransactionSelection === null && this.prosemirrorView != null && (this.beforeTransactionSelection = js(
        this,
        this.prosemirrorView.state
      ));
    }, this.afterAllTransactions = () => {
      this.beforeTransactionSelection = null;
    }, this._domSelectionInView = null;
  }
  /**
   * Create a transaction for changing the prosemirror state.
   *
   * @returns
   */
  get _tr() {
    return this.prosemirrorView.state.tr.setMeta("addToHistory", !1);
  }
  _isLocalCursorInView() {
    return this.prosemirrorView.hasFocus() ? (Rc && this._domSelectionInView === null && (Mc(0, () => {
      this._domSelectionInView = null;
    }), this._domSelectionInView = this._isDomSelectionInView()), this._domSelectionInView) : !1;
  }
  _isDomSelectionInView() {
    const e = this.prosemirrorView._root.getSelection();
    if (e == null || e.anchorNode == null) return !1;
    const n = this.prosemirrorView._root.createRange();
    n.setStart(e.anchorNode, e.anchorOffset), n.setEnd(e.focusNode, e.focusOffset), n.getClientRects().length === 0 && n.startContainer && n.collapsed && n.selectNodeContents(n.startContainer);
    const s = n.getBoundingClientRect(), i = Zx.documentElement;
    return s.bottom >= 0 && s.right >= 0 && s.left <= (window.innerWidth || i.clientWidth || 0) && s.top <= (window.innerHeight || i.clientHeight || 0);
  }
  /**
   * @param {Y.Snapshot} snapshot
   * @param {Y.Snapshot} prevSnapshot
   */
  renderSnapshot(e, n) {
    n || (n = Gc(jc(), /* @__PURE__ */ new Map())), this.prosemirrorView.dispatch(
      this._tr.setMeta(X, { snapshot: e, prevSnapshot: n })
    );
  }
  unrenderSnapshot() {
    this.mapping.clear(), this.mux(() => {
      const e = this.type.toArray().map(
        (r) => er(
          /** @type {Y.XmlElement} */
          r,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((r) => r !== null), n = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new Re(ke.from(e), 0, 0)
      );
      n.setMeta(X, { snapshot: null, prevSnapshot: null }), this.prosemirrorView.dispatch(n);
    });
  }
  _forceRerender() {
    this.mapping.clear(), this.mux(() => {
      const e = this.beforeTransactionSelection !== null ? null : this.prosemirrorView.state.selection, n = this.type.toArray().map(
        (s) => er(
          /** @type {Y.XmlElement} */
          s,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((s) => s !== null), r = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new Re(ke.from(n), 0, 0)
      );
      if (e) {
        const s = Vt(gt(e.anchor, 0), r.doc.content.size), i = Vt(gt(e.head, 0), r.doc.content.size);
        r.setSelection($e.create(r.doc, s, i));
      }
      this.prosemirrorView.dispatch(
        r.setMeta(X, { isChangeOrigin: !0, binding: this })
      );
    });
  }
  /**
   * @param {Y.Snapshot|Uint8Array} snapshot
   * @param {Y.Snapshot|Uint8Array} prevSnapshot
   * @param {Object} pluginState
   */
  _renderSnapshot(e, n, r) {
    let s = this.doc, i = this.type;
    if (e || (e = is(this.doc)), e instanceof Uint8Array || n instanceof Uint8Array)
      if ((!(e instanceof Uint8Array) || !(n instanceof Uint8Array)) && Se(), s = new Rt({ gc: !1 }), Fs(s, n), n = is(s), Fs(s, e), e = is(s), i._item === null) {
        const o = Array.from(this.doc.share.keys()).find(
          (l) => this.doc.share.get(l) === this.type
        );
        i = s.getXmlFragment(o);
      } else {
        const o = s.store.clients.get(i._item.id.client) ?? [], l = Ue(
          o,
          i._item.id.clock
        );
        i = /** @type {Y.XmlFragment} */
        /** @type {Y.ContentType} */
        /** @type {Y.Item} */
        o[l].content.type;
      }
    this.mapping.clear(), this.mux(() => {
      s.transact((o) => {
        const l = r.permanentUserData;
        l && l.dss.forEach((d) => {
          wt(o, d, (h) => {
          });
        });
        const a = (d, h) => {
          const p = d === "added" ? l.getUserByClientId(h.client) : l.getUserByDeletedId(h);
          return {
            user: p,
            type: d,
            color: U1(
              r.colorMapping,
              r.colors,
              p
            )
          };
        }, c = rd(
          i,
          new vi(n.ds, e.sv)
        ).map((d) => !d._item.deleted || wr(d._item, e) || wr(d._item, n) ? er(
          d,
          this.prosemirrorView.state.schema,
          { mapping: /* @__PURE__ */ new Map(), isOMark: /* @__PURE__ */ new Map() },
          e,
          n,
          a
        ) : null).filter((d) => d !== null), u = this._tr.replace(
          0,
          this.prosemirrorView.state.doc.content.size,
          new Re(ke.from(c), 0, 0)
        );
        this.prosemirrorView.dispatch(
          u.setMeta(X, { isChangeOrigin: !0 })
        );
      }, X);
    });
  }
  /**
   * @param {Array<Y.YEvent<any>>} events
   * @param {Y.Transaction} transaction
   */
  _typeChanged(e, n) {
    if (this.prosemirrorView == null) return;
    const r = X.getState(this.prosemirrorView.state);
    if (e.length === 0 || r.snapshot != null || r.prevSnapshot != null) {
      this.renderSnapshot(r.snapshot, r.prevSnapshot);
      return;
    }
    this.mux(() => {
      const s = (l, a) => this.mapping.delete(a);
      wt(
        n,
        n.deleteSet,
        (l) => {
          if (l.constructor === U) {
            const a = (
              /** @type {Y.ContentType} */
              /** @type {Y.Item} */
              l.content.type
            );
            a && this.mapping.delete(a);
          }
        }
      ), n.changed.forEach(s), n.changedParentTypes.forEach(s);
      const i = this.type.toArray().map(
        (l) => yd(
          /** @type {Y.XmlElement | Y.XmlHook} */
          l,
          this.prosemirrorView.state.schema,
          this
        )
      ).filter((l) => l !== null);
      let o = this._tr.replace(
        0,
        this.prosemirrorView.state.doc.content.size,
        new Re(ke.from(i), 0, 0)
      );
      G1(o, this.beforeTransactionSelection, this), o = o.setMeta(X, { isChangeOrigin: !0, isUndoRedoOperation: n.origin instanceof Xc }), this.beforeTransactionSelection !== null && this._isLocalCursorInView() && o.scrollIntoView(), this.prosemirrorView.dispatch(o);
    });
  }
  /**
   * @param {import('prosemirror-model').Node} doc
   */
  _prosemirrorChanged(e) {
    this.doc.transact(() => {
      Us(this.doc, this.type, e, this), this.beforeTransactionSelection = js(
        this,
        this.prosemirrorView.state
      );
    }, X);
  }
  /**
   * View is ready to listen to changes. Register observers.
   * @param {any} prosemirrorView
   */
  initView(e) {
    this.prosemirrorView != null && this.destroy(), this.prosemirrorView = e, this.doc.on("beforeAllTransactions", this.beforeAllTransactions), this.doc.on("afterAllTransactions", this.afterAllTransactions), this.type.observeDeep(this._observeFunction);
  }
  destroy() {
    this.prosemirrorView != null && (this.prosemirrorView = null, this.type.unobserveDeep(this._observeFunction), this.doc.off("beforeAllTransactions", this.beforeAllTransactions), this.doc.off("afterAllTransactions", this.afterAllTransactions));
  }
}
const yd = (t, e, n, r, s, i) => {
  const o = (
    /** @type {PModel.Node} */
    n.mapping.get(t)
  );
  if (o === void 0) {
    if (t instanceof pe)
      return er(
        t,
        e,
        n,
        r,
        s,
        i
      );
    throw Ve();
  }
  return o;
}, er = (t, e, n, r, s, i) => {
  const o = [], l = (a) => {
    if (a instanceof pe) {
      const c = yd(
        a,
        e,
        n,
        r,
        s,
        i
      );
      c !== null && o.push(c);
    } else {
      const c = (
        /** @type {Y.ContentType} */
        a._item.right?.content?.type
      );
      c instanceof xt && !c._item.deleted && c._item.id.client === c.doc.clientID && (a.applyDelta([
        { retain: a.length },
        ...c.toDelta()
      ]), c.doc.transact((d) => {
        c._item.delete(d);
      }));
      const u = q1(
        a,
        e,
        n,
        r,
        s,
        i
      );
      u !== null && u.forEach((d) => {
        d !== null && o.push(d);
      });
    }
  };
  r === void 0 || s === void 0 ? t.toArray().forEach(l) : rd(t, new vi(s.ds, r.sv)).forEach(l);
  try {
    const a = t.getAttributes(r);
    r !== void 0 && (wr(
      /** @type {Y.Item} */
      t._item,
      r
    ) ? wr(
      /** @type {Y.Item} */
      t._item,
      s
    ) || (a.ychange = i ? i(
      "added",
      /** @type {Y.Item} */
      t._item.id
    ) : { type: "added" }) : a.ychange = i ? i(
      "removed",
      /** @type {Y.Item} */
      t._item.id
    ) : { type: "removed" });
    const c = e.node(t.nodeName, a, o);
    return n.mapping.set(t, c), c;
  } catch {
    return t.doc.transact((c) => {
      t._item.delete(c);
    }, X), n.mapping.delete(t), null;
  }
}, q1 = (t, e, n, r, s, i) => {
  const o = [], l = t.toDelta(r, s, i);
  try {
    for (let a = 0; a < l.length; a++) {
      const c = l[a];
      o.push(e.text(c.insert, ey(c.attributes, e)));
    }
  } catch {
    return t.doc.transact((c) => {
      t._item.delete(c);
    }, X), null;
  }
  return o;
}, Y1 = (t, e) => {
  const n = new Ce(), r = t.map((s) => ({
    // @ts-ignore
    insert: s.text,
    attributes: kd(s.marks, e)
  }));
  return n.applyDelta(r), e.mapping.set(n, t), n;
}, J1 = (t, e) => {
  const n = new pe(t.type.name);
  for (const r in t.attrs) {
    const s = t.attrs[r];
    s !== null && r !== "ychange" && n.setAttribute(r, s);
  }
  return n.insert(
    0,
    Br(t).map(
      (r) => Vs(r, e)
    )
  ), e.mapping.set(n, t), n;
}, Vs = (t, e) => t instanceof Array ? Y1(t, e) : J1(t, e), el = (t) => typeof t == "object" && t !== null, _i = (t, e) => {
  const n = Object.keys(t).filter((s) => t[s] !== null);
  let r = n.length === (e == null ? 0 : Object.keys(e).filter((s) => e[s] !== null).length);
  for (let s = 0; s < n.length && r; s++) {
    const i = n[s], o = t[i], l = e[i];
    r = i === "ychange" || o === l || el(o) && el(l) && _i(o, l);
  }
  return r;
}, Br = (t) => {
  const e = t.content.content, n = [];
  for (let r = 0; r < e.length; r++) {
    const s = e[r];
    if (s.isText) {
      const i = [];
      for (let o = e[r]; r < e.length && o.isText; o = e[++r])
        i.push(o);
      r--, n.push(i);
    } else
      n.push(s);
  }
  return n;
}, vd = (t, e) => {
  const n = t.toDelta();
  return n.length === e.length && n.every(
    /** @type {(d:any,i:number) => boolean} */
    (r, s) => r.insert === /** @type {any} */
    e[s].text && Tc(r.attributes || {}).length === e[s].marks.length && Ec(r.attributes, (i, o) => {
      const l = Ai(o), a = e[s].marks;
      return _i(i, a.find(
        /** @param {any} mark */
        (c) => c.type.name === l
      )?.attrs);
    })
  );
}, wn = (t, e) => {
  if (t instanceof pe && !(e instanceof Array) && Ws(t, e)) {
    const n = Br(e);
    return t._length === n.length && _i(t.getAttributes(), e.attrs) && t.toArray().every(
      (r, s) => wn(r, n[s])
    );
  }
  return t instanceof Ce && e instanceof Array && vd(t, e);
}, xr = (t, e) => t === e || t instanceof Array && e instanceof Array && t.length === e.length && t.every(
  (n, r) => e[r] === n
), tl = (t, e, n) => {
  const r = t.toArray(), s = Br(e), i = s.length, o = r.length, l = Vt(o, i);
  let a = 0, c = 0, u = !1;
  for (; a < l; a++) {
    const d = r[a], h = s[a];
    if (xr(n.mapping.get(d), h))
      u = !0;
    else if (!wn(d, h))
      break;
  }
  for (; a + c < l; c++) {
    const d = r[o - c - 1], h = s[i - c - 1];
    if (xr(n.mapping.get(d), h))
      u = !0;
    else if (!wn(d, h))
      break;
  }
  return {
    equalityFactor: a + c,
    foundMappedChild: u
  };
}, X1 = (t) => {
  let e = "", n = t._start;
  const r = {};
  for (; n !== null; )
    n.deleted || (n.countable && n.content instanceof We ? e += n.content.str : n.content instanceof re && (r[n.content.key] = null)), n = n.right;
  return {
    str: e,
    nAttrs: r
  };
}, Z1 = (t, e, n) => {
  n.mapping.set(t, e);
  const { nAttrs: r, str: s } = X1(t), i = e.map((c) => ({
    insert: (
      /** @type {any} */
      c.text
    ),
    attributes: Object.assign({}, r, kd(c.marks, n))
  })), { insert: o, remove: l, index: a } = R1(
    s,
    i.map((c) => c.insert).join("")
  );
  t.delete(a, l), t.insert(a, o), t.applyDelta(
    i.map((c) => ({ retain: c.insert.length, attributes: c.attributes }))
  );
}, Q1 = /(.*)(--[a-zA-Z0-9+/=]{8})$/, Ai = (t) => Q1.exec(t)?.[1] ?? t, ey = (t, e) => {
  const n = [];
  for (const r in t)
    n.push(e.mark(Ai(r), t[r]));
  return n;
}, kd = (t, e) => {
  const n = {};
  return t.forEach((r) => {
    if (r.type.name !== "ychange") {
      const s = at(e.isOMark, r.type, () => !r.type.excludes(r.type));
      n[s ? `${r.type.name}--${j1(r.toJSON())}` : r.type.name] = r.attrs;
    }
  }), n;
}, Us = (t, e, n, r) => {
  if (e instanceof pe && e.nodeName !== n.type.name)
    throw new Error("node name mismatch!");
  if (r.mapping.set(e, n), e instanceof pe) {
    const d = e.getAttributes(), h = n.attrs;
    for (const p in h)
      h[p] !== null ? d[p] !== h[p] && p !== "ychange" && e.setAttribute(p, h[p]) : e.removeAttribute(p);
    for (const p in d)
      h[p] === void 0 && e.removeAttribute(p);
  }
  const s = Br(n), i = s.length, o = e.toArray(), l = o.length, a = Vt(i, l);
  let c = 0, u = 0;
  for (; c < a; c++) {
    const d = o[c], h = s[c];
    if (!xr(r.mapping.get(d), h))
      if (wn(d, h))
        r.mapping.set(d, h);
      else
        break;
  }
  for (; u + c + 1 < a; u++) {
    const d = o[l - u - 1], h = s[i - u - 1];
    if (!xr(r.mapping.get(d), h))
      if (wn(d, h))
        r.mapping.set(d, h);
      else
        break;
  }
  t.transact(() => {
    for (; l - c - u > 0 && i - c - u > 0; ) {
      const h = o[c], p = s[c], m = o[l - u - 1], g = s[i - u - 1];
      if (h instanceof Ce && p instanceof Array)
        vd(h, p) || Z1(h, p, r), c += 1;
      else {
        let b = h instanceof pe && Ws(h, p), x = m instanceof pe && Ws(m, g);
        if (b && x) {
          const v = tl(
            /** @type {Y.XmlElement} */
            h,
            /** @type {PModel.Node} */
            p,
            r
          ), S = tl(
            /** @type {Y.XmlElement} */
            m,
            /** @type {PModel.Node} */
            g,
            r
          );
          v.foundMappedChild && !S.foundMappedChild ? x = !1 : !v.foundMappedChild && S.foundMappedChild || v.equalityFactor < S.equalityFactor ? b = !1 : x = !1;
        }
        b ? (Us(
          t,
          /** @type {Y.XmlFragment} */
          h,
          /** @type {PModel.Node} */
          p,
          r
        ), c += 1) : x ? (Us(
          t,
          /** @type {Y.XmlFragment} */
          m,
          /** @type {PModel.Node} */
          g,
          r
        ), u += 1) : (r.mapping.delete(e.get(c)), e.delete(c, 1), e.insert(c, [
          Vs(p, r)
        ]), c += 1);
      }
    }
    const d = l - c - u;
    if (l === 1 && i === 0 && o[0] instanceof Ce ? (r.mapping.delete(o[0]), o[0].delete(0, o[0].length)) : d > 0 && (e.slice(c, c + d).forEach((h) => r.mapping.delete(h)), e.delete(c, d)), c + u < i) {
      const h = [];
      for (let p = c; p < i - u; p++)
        h.push(Vs(s[p], r));
      e.insert(c, h);
    }
  }, X);
}, Ws = (t, e) => !(e instanceof Array) && t.nodeName === e.type.name, Gs = (t, e, n) => {
  if (t === 0)
    return ss(e, 0, e.length === 0 ? -1 : 0);
  let r = e._first === null ? null : (
    /** @type {Y.ContentType} */
    e._first.content.type
  );
  for (; r !== null && e !== r; ) {
    if (r instanceof Ce) {
      if (r._length >= t)
        return ss(r, t, e.length === 0 ? -1 : 0);
      if (t -= r._length, r._item !== null && r._item.next !== null)
        r = /** @type {Y.ContentType} */
        r._item.next.content.type;
      else {
        do
          r = r._item === null ? null : r._item.parent, t--;
        while (r !== e && r !== null && r._item !== null && r._item.next === null);
        r !== null && r !== e && (r = r._item === null ? null : (
          /** @type {Y.ContentType} */
          /** @type Y.Item */
          r._item.next.content.type
        ));
      }
    } else {
      const s = (
        /** @type {any} */
        (n.get(r) || { nodeSize: 0 }).nodeSize
      );
      if (r._first !== null && t < s)
        r = /** @type {Y.ContentType} */
        r._first.content.type, t--;
      else {
        if (t === 1 && r._length === 0 && s > 1)
          return new fr(r._item === null ? null : r._item.id, r._item === null ? hn(r) : null, null);
        if (t -= s, r._item !== null && r._item.next !== null)
          r = /** @type {Y.ContentType} */
          r._item.next.content.type;
        else {
          if (t === 0)
            return r = r._item === null ? r : r._item.parent, new fr(r._item === null ? null : r._item.id, r._item === null ? hn(r) : null, null);
          do
            r = /** @type {Y.Item} */
            r._item.parent, t--;
          while (r !== e && /** @type {Y.Item} */
          r._item.next === null);
          r !== e && (r = /** @type {Y.ContentType} */
          /** @type {Y.Item} */
          /** @type {Y.Item} */
          r._item.next.content.type);
        }
      }
    }
    if (r === null)
      throw Se();
    if (t === 0 && r.constructor !== Ce && r !== e)
      return ty(r._item.parent, r._item);
  }
  return ss(e, e._length, e.length === 0 ? -1 : 0);
}, ty = (t, e) => {
  let n = null, r = null;
  return t._item === null ? r = hn(t) : n = O(t._item.id.client, t._item.id.clock), new fr(n, r, e.id);
}, tr = (t, e, n, r) => {
  const s = A0(n, t);
  if (s === null || s.type !== e && !pn(e, s.type._item))
    return null;
  let i = s.type, o = 0;
  if (i.constructor === Ce)
    o = s.index;
  else if (i._item === null || !i._item.deleted) {
    let l = i._first, a = 0;
    for (; a < i._length && a < s.index && l !== null; ) {
      if (!l.deleted) {
        const c = (
          /** @type {Y.ContentType} */
          l.content.type
        );
        a++, c instanceof Ce ? o += c._length : o += /** @type {any} */
        r.get(c).nodeSize;
      }
      l = /** @type {Y.Item} */
      l.right;
    }
    o += 1;
  }
  for (; i !== e && i._item !== null; ) {
    const l = i._item.parent;
    if (l._item === null || !l._item.deleted) {
      o += 1;
      let a = (
        /** @type {Y.AbstractType} */
        l._first
      );
      for (; a !== null; ) {
        const c = (
          /** @type {Y.ContentType} */
          a.content.type
        );
        if (c === i)
          break;
        a.deleted || (c instanceof Ce ? o += c._length : o += /** @type {any} */
        r.get(c).nodeSize), a = a.right;
      }
    }
    i = /** @type {Y.AbstractType} */
    l;
  }
  return o - 1;
};
function ny(t) {
  const e = t.toArray(), n = (r) => {
    let s;
    if (r instanceof Ce)
      s = r.toDelta().map(
        /** @param {any} d */
        (o) => {
          const l = {
            type: "text",
            text: o.insert
          };
          return o.attributes && (l.marks = Object.keys(o.attributes).map((a) => {
            const c = o.attributes[a], d = {
              type: Ai(a)
            };
            return Object.keys(c) && (d.attrs = c), d;
          })), l;
        }
      );
    else if (r instanceof pe) {
      s = {
        type: r.nodeName
      };
      const i = r.getAttributes();
      Object.keys(i).length && (s.attrs = i);
      const o = r.toArray();
      o.length && (s.content = o.map(n).flat());
    } else
      Se();
    return s;
  };
  return {
    type: "doc",
    content: e.map(n)
  };
}
const ry = (t) => st.getState(t)?.undoManager?.undo() != null, sy = (t) => st.getState(t)?.undoManager?.redo() != null, iy = /* @__PURE__ */ new Set(["paragraph"]), oy = (t, e) => !(t instanceof U) || !(t.content instanceof qe) || !(t.content.type instanceof xt || t.content.type instanceof pe && e.has(t.content.type.nodeName)) || t.content.type._length === 0, ly = ({ protectedNodes: t = iy, trackedOrigins: e = [], undoManager: n = null } = {}) => new Xe({
  key: st,
  state: {
    init: (r, s) => {
      const i = X.getState(s), o = n || new Xc(i.type, {
        trackedOrigins: new Set([X].concat(e)),
        deleteFilter: (l) => oy(l, t),
        captureTransaction: (l) => l.meta.get("addToHistory") !== !1
      });
      return {
        undoManager: o,
        prevSel: null,
        hasUndoOps: o.undoStack.length > 0,
        hasRedoOps: o.redoStack.length > 0
      };
    },
    apply: (r, s, i, o) => {
      const l = X.getState(o).binding, a = s.undoManager, c = a.undoStack.length > 0, u = a.redoStack.length > 0;
      return l ? {
        undoManager: a,
        prevSel: js(l, i),
        hasUndoOps: c,
        hasRedoOps: u
      } : c !== s.hasUndoOps || u !== s.hasRedoOps ? Object.assign({}, s, {
        hasUndoOps: a.undoStack.length > 0,
        hasRedoOps: a.redoStack.length > 0
      }) : s;
    }
  },
  view: (r) => {
    const s = X.getState(r.state), i = st.getState(r.state).undoManager;
    return i.on("stack-item-added", ({ stackItem: o }) => {
      const l = s.binding;
      l && o.meta.set(l, st.getState(r.state).prevSel);
    }), i.on("stack-item-popped", ({ stackItem: o }) => {
      const l = s.binding;
      l && (l.beforeTransactionSelection = o.meta.get(l) || l.beforeTransactionSelection);
    }), {
      destroy: () => {
        i.destroy();
      }
    };
  }
});
Dt.create({
  name: "collaboration",
  priority: 1e3,
  addOptions() {
    return {
      document: null,
      field: "default",
      fragment: null
    };
  },
  addStorage() {
    return {
      isDisabled: !1
    };
  },
  onCreate() {
    this.editor.extensionManager.extensions.find((t) => t.name === "history") && console.warn('[tiptap warn]: "@tiptap/extension-collaboration" comes with its own history support and is not compatible with "@tiptap/extension-history".');
  },
  addCommands() {
    return {
      undo: () => ({ tr: t, state: e, dispatch: n }) => (t.setMeta("preventDispatch", !0), st.getState(e).undoManager.undoStack.length === 0 ? !1 : n ? ry(e) : !0),
      redo: () => ({ tr: t, state: e, dispatch: n }) => (t.setMeta("preventDispatch", !0), st.getState(e).undoManager.redoStack.length === 0 ? !1 : n ? sy(e) : !0)
    };
  },
  addKeyboardShortcuts() {
    return {
      "Mod-z": () => this.editor.commands.undo(),
      "Mod-y": () => this.editor.commands.redo(),
      "Shift-Mod-z": () => this.editor.commands.redo()
    };
  },
  addProseMirrorPlugins() {
    var t;
    const e = this.options.fragment ? this.options.fragment : this.options.document.getXmlFragment(this.options.field), n = ly(this.options.yUndoOptions), r = n.spec.view;
    n.spec.view = (o) => {
      const { undoManager: l } = st.getState(o.state);
      l.restore && (l.restore(), l.restore = () => {
      });
      const a = r ? r(o) : void 0;
      return {
        destroy: () => {
          const c = l.trackedOrigins.has(l), u = l._observers;
          l.restore = () => {
            c && l.trackedOrigins.add(l), l.doc.on("afterTransaction", l.afterTransactionHandler), l._observers = u;
          }, a?.destroy && a.destroy();
        }
      };
    };
    const s = {
      ...this.options.ySyncOptions,
      onFirstRender: this.options.onFirstRender
    }, i = W1(e, s);
    return this.editor.options.enableContentCheck && ((t = e.doc) === null || t === void 0 || t.on("beforeTransaction", () => {
      try {
        const o = ny(e);
        if (o.content.length === 0)
          return;
        this.editor.schema.nodeFromJSON(o).check();
      } catch (o) {
        return this.editor.emit("contentError", {
          error: o,
          editor: this.editor,
          disableCollaboration: () => {
            var l;
            (l = e.doc) === null || l === void 0 || l.destroy(), this.storage.isDisabled = !0;
          }
        }), !1;
      }
    })), [
      i,
      n,
      // Only add the filterInvalidContent plugin if content checking is enabled
      this.editor.options.enableContentCheck && new Xe({
        key: new Ne("filterInvalidContent"),
        filterTransaction: () => {
          var o;
          return this.storage.isDisabled && ((o = e.doc) === null || o === void 0 || o.destroy()), !0;
        }
      })
    ].filter(Boolean);
  }
});
function ay(t) {
  return !!t.getMeta(X);
}
function nl(t) {
  if (!t.length)
    return on.empty;
  const e = [], n = t[0].$from.node(0);
  return t.forEach((r) => {
    const s = r.$from.pos, i = r.$from.nodeAfter;
    i && e.push(sr.node(s, s + i.nodeSize, {
      class: "ProseMirror-selectednoderange"
    }));
  }), on.create(n, e);
}
function $r(t, e, n) {
  const r = [], s = t.node(0);
  n = typeof n == "number" && n >= 0 ? n : t.sameParent(e) ? Math.max(0, t.sharedDepth(e.pos) - 1) : t.sharedDepth(e.pos);
  const i = new Pf(t, e, n), o = i.depth === 0 ? 0 : s.resolve(i.start).posAtIndex(0);
  return i.parent.forEach((l, a) => {
    const c = o + a, u = c + l.nodeSize;
    if (c < i.start || c >= i.end)
      return;
    const d = new Ll(s.resolve(c), s.resolve(u));
    r.push(d);
  }), r;
}
class Ti {
  constructor(e, n) {
    this.anchor = e, this.head = n;
  }
  map(e) {
    return new Ti(e.map(this.anchor), e.map(this.head));
  }
  resolve(e) {
    const n = e.resolve(this.anchor), r = e.resolve(this.head);
    return new he(n, r);
  }
}
class he extends Ee {
  constructor(e, n, r, s = 1) {
    const { doc: i } = e, o = e === n, l = e.pos === i.content.size && n.pos === i.content.size, a = o && !l ? i.resolve(n.pos + (s > 0 ? 1 : -1)) : n, c = o && l ? i.resolve(e.pos - (s > 0 ? 1 : -1)) : e, u = $r(c.min(a), c.max(a), r), d = a.pos >= e.pos ? u[0].$from : u[u.length - 1].$to, h = a.pos >= e.pos ? u[u.length - 1].$to : u[0].$from;
    super(d, h, u), this.depth = r;
  }
  // we can safely ignore this TypeScript error: https://github.com/Microsoft/TypeScript/issues/338
  // @ts-ignore
  get $to() {
    return this.ranges[this.ranges.length - 1].$to;
  }
  eq(e) {
    return e instanceof he && e.$from.pos === this.$from.pos && e.$to.pos === this.$to.pos;
  }
  map(e, n) {
    const r = e.resolve(n.map(this.anchor)), s = e.resolve(n.map(this.head));
    return new he(r, s);
  }
  toJSON() {
    return {
      type: "nodeRange",
      anchor: this.anchor,
      head: this.head
    };
  }
  get isForwards() {
    return this.head >= this.anchor;
  }
  get isBackwards() {
    return !this.isForwards;
  }
  extendBackwards() {
    const { doc: e } = this.$from;
    if (this.isForwards && this.ranges.length > 1) {
      const s = this.ranges.slice(0, -1), i = s[0].$from, o = s[s.length - 1].$to;
      return new he(i, o, this.depth);
    }
    const n = this.ranges[0], r = e.resolve(Math.max(0, n.$from.pos - 1));
    return new he(this.$anchor, r, this.depth);
  }
  extendForwards() {
    const { doc: e } = this.$from;
    if (this.isBackwards && this.ranges.length > 1) {
      const s = this.ranges.slice(1), i = s[0].$from, o = s[s.length - 1].$to;
      return new he(o, i, this.depth);
    }
    const n = this.ranges[this.ranges.length - 1], r = e.resolve(Math.min(e.content.size, n.$to.pos + 1));
    return new he(this.$anchor, r, this.depth);
  }
  static fromJSON(e, n) {
    return new he(e.resolve(n.anchor), e.resolve(n.head));
  }
  static create(e, n, r, s, i = 1) {
    return new this(e.resolve(n), e.resolve(r), s, i);
  }
  getBookmark() {
    return new Ti(this.anchor, this.head);
  }
}
he.prototype.visible = !1;
function Hn(t) {
  return t instanceof he;
}
Dt.create({
  name: "nodeRange",
  addOptions() {
    return {
      depth: void 0,
      key: "Mod"
    };
  },
  addKeyboardShortcuts() {
    return {
      // extend NodeRangeSelection upwards
      "Shift-ArrowUp": ({ editor: t }) => {
        const { depth: e } = this.options, { view: n, state: r } = t, { doc: s, selection: i, tr: o } = r, { anchor: l, head: a } = i;
        if (!Hn(i)) {
          const u = he.create(s, l, a, e, -1);
          return o.setSelection(u), n.dispatch(o), !0;
        }
        const c = i.extendBackwards();
        return o.setSelection(c), n.dispatch(o), !0;
      },
      // extend NodeRangeSelection downwards
      "Shift-ArrowDown": ({ editor: t }) => {
        const { depth: e } = this.options, { view: n, state: r } = t, { doc: s, selection: i, tr: o } = r, { anchor: l, head: a } = i;
        if (!Hn(i)) {
          const u = he.create(s, l, a, e);
          return o.setSelection(u), n.dispatch(o), !0;
        }
        const c = i.extendForwards();
        return o.setSelection(c), n.dispatch(o), !0;
      },
      // add `NodeRangeSelection` to all nodes
      "Mod-a": ({ editor: t }) => {
        const { depth: e } = this.options, { view: n, state: r } = t, { doc: s, tr: i } = r, o = he.create(s, 0, s.content.size, e);
        return i.setSelection(o), n.dispatch(i), !0;
      }
    };
  },
  onSelectionUpdate() {
    const { selection: t } = this.editor.state;
    Hn(t) && this.editor.view.dom.classList.add("ProseMirror-noderangeselection");
  },
  addProseMirrorPlugins() {
    let t = !1, e = !1;
    return [
      new Xe({
        key: new Ne("nodeRange"),
        props: {
          attributes: () => t ? {
            class: "ProseMirror-noderangeselection"
          } : { class: "" },
          handleDOMEvents: {
            mousedown: (n, r) => {
              const { key: s } = this.options, i = /Mac/.test(navigator.platform), o = !!r.shiftKey, l = !!r.ctrlKey, a = !!r.altKey, c = !!r.metaKey, u = i ? c : l;
              return (s == null || s === "Shift" && o || s === "Control" && l || s === "Alt" && a || s === "Meta" && c || s === "Mod" && u) && (e = !0), e && document.addEventListener("mouseup", () => {
                e = !1;
                const { state: d } = n, { doc: h, selection: p, tr: m } = d, { $anchor: g, $head: b } = p;
                if (g.sameParent(b))
                  return;
                const x = he.create(h, g.pos, b.pos, this.options.depth);
                m.setSelection(x), n.dispatch(m);
              }, { once: !0 }), !1;
            }
          },
          // when selecting some text we want to render some decorations
          // to preview a `NodeRangeSelection`
          decorations: (n) => {
            const { selection: r } = n, s = Hn(r);
            if (t = !1, !e)
              return s ? (t = !0, nl(r.ranges)) : null;
            const { $from: i, $to: o } = r;
            if (!s && i.sameParent(o))
              return null;
            const l = $r(i, o, this.options.depth);
            return l.length ? (t = !0, nl(l)) : null;
          }
        }
      })
    ];
  }
});
function cy(t) {
  let e = "";
  const n = getComputedStyle(t);
  for (let r = 0; r < n.length; r += 1)
    e += `${n[r]}:${n.getPropertyValue(n[r])};`;
  return e;
}
function dy(t) {
  const e = t.cloneNode(!0), n = [t, ...Array.from(t.getElementsByTagName("*"))], r = [e, ...Array.from(e.getElementsByTagName("*"))];
  return n.forEach((s, i) => {
    r[i].style.cssText = cy(s);
  }), e;
}
const Cd = (t) => {
  const { x: e, y: n, direction: r, editor: s } = t;
  let i = null, o = null, l = null, a = e;
  for (; o === null && a < window.innerWidth && a > 0; ) {
    const c = document.elementsFromPoint(a, n), u = c.findIndex((h) => h.classList.contains("ProseMirror")), d = c.slice(0, u);
    if (d.length > 0) {
      const h = d[0];
      if (i = h, l = s.view.posAtDOM(h, 0), l >= 0) {
        o = s.state.doc.nodeAt(Math.max(l - 1, 0)), o?.isText && (o = s.state.doc.nodeAt(Math.max(l - 1, 0))), o || (o = s.state.doc.nodeAt(Math.max(l, 0)));
        break;
      }
    }
    r === "left" ? a -= 1 : a += 1;
  }
  return { resultElement: i, resultNode: o, pos: l ?? null };
};
function jn(t, e) {
  return window.getComputedStyle(t)[e];
}
function uy(t = 0, e = 0, n = 0) {
  return Math.min(Math.max(t, e), n);
}
function fy(t, e, n) {
  const r = parseInt(jn(t.dom, "paddingLeft"), 10), s = parseInt(jn(t.dom, "paddingRight"), 10), i = parseInt(jn(t.dom, "borderLeftWidth"), 10), o = parseInt(jn(t.dom, "borderLeftWidth"), 10), l = t.dom.getBoundingClientRect();
  return {
    left: uy(e, l.left + r + i, l.right - s - o),
    top: n
  };
}
function Sd(t) {
  var e;
  (e = t.parentNode) === null || e === void 0 || e.removeChild(t);
}
function hy(t, e) {
  const { doc: n } = e.view.state, r = Cd({
    editor: e,
    x: t.clientX,
    y: t.clientY,
    direction: "right"
  });
  if (!r.resultNode || r.pos === null)
    return [];
  const s = t.clientX, i = fy(e.view, s, t.clientY), o = e.view.posAtCoords(i);
  if (!o)
    return [];
  const { pos: l } = o;
  if (!n.resolve(l).parent)
    return [];
  const c = n.resolve(r.pos), u = n.resolve(r.pos + 1);
  return $r(c, u, 0);
}
function py(t, e) {
  const { view: n } = e;
  if (!t.dataTransfer)
    return;
  const { empty: r, $from: s, $to: i } = n.state.selection, o = hy(t, e), l = $r(s, i, 0), a = l.some((b) => o.find((x) => x.$from === b.$from && x.$to === b.$to)), c = r || !a ? o : l;
  if (!c.length)
    return;
  const { tr: u } = n.state, d = document.createElement("div"), h = c[0].$from.pos, p = c[c.length - 1].$to.pos, m = he.create(n.state.doc, h, p), g = m.content();
  c.forEach((b) => {
    const x = n.nodeDOM(b.$from.pos), v = dy(x);
    d.append(v);
  }), d.style.position = "absolute", d.style.top = "-10000px", document.body.append(d), t.dataTransfer.clearData(), t.dataTransfer.setDragImage(d, 0, 0), n.dragging = { slice: g, move: !0 }, u.setSelection(m), n.dispatch(u), document.addEventListener("drop", () => Sd(d), { once: !0 });
}
const rl = (t, e) => {
  const n = t.resolve(e), { depth: r } = n;
  return r === 0 ? e : n.pos - n.parentOffset - 1;
}, sl = (t, e) => {
  const n = t.nodeAt(e), r = t.resolve(e);
  let { depth: s } = r, i = n;
  for (; s > 0; ) {
    const o = r.node(s);
    s -= 1, s === 0 && (i = o);
  }
  return i;
}, as = (t, e) => {
  const n = X.getState(t);
  return n ? Gs(e, n.type, n.binding.mapping) : null;
}, my = (t, e) => {
  const n = X.getState(t);
  return n ? tr(n.doc, n.type, e, n.binding.mapping) || 0 : -1;
}, il = (t, e) => {
  let n = e;
  for (; n && n.parentNode && n.parentNode !== t.dom; )
    n = n.parentNode;
  return n;
}, Nd = new Ne("dragHandle"), Id = ({ pluginKey: t = Nd, element: e, editor: n, tippyOptions: r, onNodeChange: s }) => {
  const i = document.createElement("div");
  let o = null, l = !1, a = null, c = -1, u;
  return e.addEventListener("dragstart", (d) => {
    py(d, n), setTimeout(() => {
      e && (e.style.pointerEvents = "none");
    }, 0);
  }), e.addEventListener("dragend", () => {
    e && (e.style.pointerEvents = "auto");
  }), new Xe({
    key: typeof t == "string" ? new Ne(t) : t,
    state: {
      init() {
        return { locked: !1 };
      },
      apply(d, h, p, m) {
        const g = d.getMeta("lockDragHandle"), b = d.getMeta("hideDragHandle");
        if (g !== void 0 && (l = g), b && o)
          return o.hide(), l = !1, a = null, c = -1, s?.({ editor: n, node: null, pos: -1 }), h;
        if (d.docChanged && c !== -1 && e && o)
          if (ay(d)) {
            const x = my(m, u);
            x !== c && (c = x);
          } else {
            const x = d.mapping.map(c);
            x !== c && (c = x, u = as(m, c));
          }
        return h;
      }
    },
    view: (d) => {
      var h;
      return e.draggable = !0, e.style.pointerEvents = "auto", (h = n.view.dom.parentElement) === null || h === void 0 || h.appendChild(i), i.appendChild(e), i.style.pointerEvents = "none", i.style.position = "absolute", i.style.top = "0", i.style.left = "0", {
        update(p, m) {
          if (!e)
            return;
          if (!n.isEditable) {
            o?.destroy(), o = null;
            return;
          }
          if (o || (o = Bf(d.dom, {
            getReferenceClientRect: null,
            interactive: !0,
            trigger: "manual",
            placement: "left-start",
            hideOnClick: !1,
            duration: 100,
            popperOptions: {
              modifiers: [
                { name: "flip", enabled: !1 },
                {
                  name: "preventOverflow",
                  options: {
                    rootBoundary: "document",
                    mainAxis: !1
                  }
                }
              ]
            },
            ...r,
            appendTo: i,
            content: e
          })), l ? e.draggable = !1 : e.draggable = !0, d.state.doc.eq(m.doc) || c === -1)
            return;
          let g = d.nodeDOM(c);
          if (g = il(d, g), g === d.dom || g?.nodeType !== 1)
            return;
          const b = d.posAtDOM(g, 0), x = sl(n.state.doc, b), v = rl(n.state.doc, b);
          a = x, c = v, u = as(d.state, c), s?.({ editor: n, node: a, pos: c }), o.setProps({
            getReferenceClientRect: () => g.getBoundingClientRect()
          });
        },
        // TODO: Kills even on hot reload
        destroy() {
          o?.destroy(), e && Sd(i);
        }
      };
    },
    props: {
      handleDOMEvents: {
        mouseleave(d, h) {
          return l || h.target && !i.contains(h.relatedTarget) && (o?.hide(), a = null, c = -1, s?.({ editor: n, node: null, pos: -1 })), !1;
        },
        mousemove(d, h) {
          if (!e || !o || l)
            return !1;
          const p = Cd({
            x: h.clientX,
            y: h.clientY,
            direction: "right",
            editor: n
          });
          if (!p.resultElement)
            return !1;
          let m = p.resultElement;
          if (m = il(d, m), m === d.dom || m?.nodeType !== 1)
            return !1;
          const g = d.posAtDOM(m, 0), b = sl(n.state.doc, g);
          if (b !== a) {
            const x = rl(n.state.doc, g);
            a = b, c = x, u = as(d.state, c), s?.({ editor: n, node: a, pos: c }), o.setProps({
              getReferenceClientRect: () => m.getBoundingClientRect()
            }), o.show();
          }
          return !1;
        }
      }
    }
  });
};
Dt.create({
  name: "dragHandle",
  addOptions() {
    return {
      render() {
        const t = document.createElement("div");
        return t.classList.add("drag-handle"), t;
      },
      tippyOptions: {},
      locked: !1,
      onNodeChange: () => null
    };
  },
  addCommands() {
    return {
      lockDragHandle: () => ({ editor: t }) => (this.options.locked = !0, t.commands.setMeta("lockDragHandle", this.options.locked)),
      unlockDragHandle: () => ({ editor: t }) => (this.options.locked = !1, t.commands.setMeta("lockDragHandle", this.options.locked)),
      toggleDragHandle: () => ({ editor: t }) => (this.options.locked = !this.options.locked, t.commands.setMeta("lockDragHandle", this.options.locked))
    };
  },
  addProseMirrorPlugins() {
    const t = this.options.render();
    return [
      Id({
        tippyOptions: this.options.tippyOptions,
        element: t,
        editor: this.editor,
        onNodeChange: this.options.onNodeChange
      })
    ];
  }
});
const gy = (t) => {
  const { className: e = "drag-handle", children: n, editor: r, pluginKey: s = Nd, onNodeChange: i, tippyOptions: o } = t, [l, a] = K(null), c = ge(null);
  return ue(() => l ? r.isDestroyed ? () => {
    c.current = null;
  } : (c.current || (c.current = Id({
    editor: r,
    element: l,
    pluginKey: s,
    tippyOptions: o,
    onNodeChange: i
  }), r.registerPlugin(c.current)), () => {
    r.unregisterPlugin(s), c.current = null;
  }) : () => {
    c.current = null;
  }, [l, r, i, s, o]), se.createElement("div", { className: e, ref: a }, n);
}, by = ({
  placeholder: t,
  translations: e,
  aiBlockConfig: n,
  imageUploadConfig: r
}) => [
  Ol,
  Fl,
  zl,
  Pl,
  Bl,
  $l,
  Hl,
  jl,
  Vl,
  Yl,
  Xl,
  Jl,
  wp,
  nb,
  hb,
  Vg.configure({
    currentConfig: n
  }),
  Zg,
  ab,
  ...r ? [Qg(r)] : [],
  Ug,
  // Automatically add unique IDs to all block nodes
  $f,
  Hf(t),
  jf(t),
  db({
    aiBlockConfig: n,
    translations: e,
    imageUploadConfig: r
  })
], wy = Et({
  base: "flex flex-row items-center justify-center gap-2 p-3",
  variants: {
    variant: {
      info: "bg-f1-background-info text-f1-foreground-info",
      warning: "bg-f1-background-warning text-f1-foreground-warning",
      critical: "bg-f1-background-critical text-f1-foreground-critical",
      neutral: "bg-f1-background-tertiary text-f1-foreground",
      positive: "bg-f1-background-positive text-f1-foreground-positive"
    }
  },
  defaultVariants: {
    variant: "info"
  }
}), ol = (t) => t.isVisible !== !1, xy = (t) => "isVisible" in t ? t.isVisible !== !1 : !0, yy = (t) => !!t && "items" in t, vy = (t) => !!t && "label" in t && !("items" in t), ky = ({
  primaryAction: t,
  secondaryActions: e = [],
  metadata: n,
  otherActions: r = [],
  banner: s
}) => {
  const i = e.filter(ol), o = r.filter(xy), l = t && ol(t), a = i.length > 0, c = o.length > 0, u = a || c || l;
  return /* @__PURE__ */ w("div", { className: "flex flex-col", children: [
    (n && n.length > 0 || u) && /* @__PURE__ */ w("div", { className: "flex flex-col items-start justify-between gap-2 sm:px-6 px-0 py-4 sm:flex-row sm:items-center", children: [
      n && n.length > 0 && /* @__PURE__ */ f(Vf, { items: n }),
      /* @__PURE__ */ w("div", { className: "flex flex-shrink-0 flex-row items-center gap-2", children: [
        c && /* @__PURE__ */ f(sn, { items: o }),
        i.map((d, h) => /* @__PURE__ */ f(
          $,
          {
            onClick: d.onClick,
            variant: d.variant || "outline",
            label: d.label,
            icon: d.icon,
            hideLabel: d.hideLabel,
            disabled: d.disabled,
            tooltip: d.tooltip
          },
          h
        )),
        l && (a || c) && /* @__PURE__ */ f("div", { className: "mx-1 h-4 w-px bg-f1-background-secondary-hover" }),
        l && vy(t) && /* @__PURE__ */ f(
          $,
          {
            label: t.label,
            onClick: t.onClick,
            variant: "default",
            icon: t.icon,
            disabled: t.disabled,
            tooltip: t.tooltip
          }
        ),
        l && yy(t) && /* @__PURE__ */ f(
          Al,
          {
            items: t.items,
            onClick: t.onClick,
            variant: "default",
            value: t.value,
            disabled: t.disabled,
            tooltip: t.tooltip
          }
        )
      ] })
    ] }),
    s && /* @__PURE__ */ w("div", { className: wy({ variant: s.variant }), children: [
      /* @__PURE__ */ f(G, { icon: s.icon }),
      /* @__PURE__ */ f(pt, { children: s.title })
    ] })
  ] });
}, Cy = ({
  value: t,
  onChange: e,
  placeholder: n,
  disabled: r = !1
}) => /* @__PURE__ */ f("div", { className: "mx-auto flex w-full max-w-[824px] flex-col pb-4 pt-5 transition-all duration-300 sm:px-14 px-0", children: /* @__PURE__ */ f(
  "textarea",
  {
    ref: (o) => {
      o && (o.style.height = "auto", o.style.height = `${o.scrollHeight}px`);
    },
    disabled: r,
    value: t,
    onChange: (o) => {
      const l = o.target.value.replace(/[\r\n]/g, "");
      e?.(l), o.target.style.height = "auto", o.target.style.height = `${o.target.scrollHeight}px`;
    },
    onKeyDown: (o) => {
      o.key === "Enter" && o.preventDefault();
    },
    placeholder: n,
    className: "resize-none overflow-hidden font-semibold text-f1-foreground placeholder-f1-foreground-tertiary sm:text-[39px] sm:leading-[49px] text-[34px] leading-[42px]",
    rows: 1
  }
) }), Sy = F(function({
  onChange: e,
  placeholder: n,
  initialEditorState: r,
  readonly: s = !1,
  aiBlockConfig: i,
  imageUploadConfig: o,
  onTitleChange: l,
  primaryAction: a,
  secondaryActions: c,
  otherActions: u,
  metadata: d,
  banner: h,
  showBubbleMenu: p = !1,
  titlePlaceholder: m,
  dataTestId: g
}, b) {
  const x = ot(), v = ge(null), S = ge(null), E = Yf(), [I] = K(() => r?.content || ""), [k, T] = K(r?.title || ""), [C, D] = K(null), y = (L) => {
    switch (L) {
      case "file-too-large":
        return x.imageUpload.errors.fileTooLarge;
      case "invalid-type":
        return x.imageUpload.errors.invalidType;
      case "upload-failed":
        return x.imageUpload.errors.uploadFailed;
      default:
        return x.imageUpload.errors.uploadFailed;
    }
  };
  ue(() => {
    l && l(k);
  }, [k, l]);
  const N = Uf({
    extensions: by({
      placeholder: n,
      translations: x,
      aiBlockConfig: i,
      imageUploadConfig: o ? {
        ...o,
        onError: (L) => {
          D(L);
        }
      } : void 0
    }),
    content: I,
    onUpdate: ({ editor: L }) => {
      e(
        L.isEmpty ? { json: null, html: null } : { json: L.getJSON(), html: L.getHTML() }
      );
    },
    editable: !s
  });
  Gl(b, () => ({
    clear: () => N?.commands.clearContent(),
    focus: () => N?.commands.focus(),
    setContent: (L) => N?.commands.setContent(L),
    insertAIBlock: () => {
      !N || !i || N.chain().focus().insertContentAt(N.state.doc.content.size, [
        {
          type: "aiBlock",
          attrs: {
            data: { content: null, selectedAction: void 0 },
            config: i,
            isCollapsed: !1
          }
        },
        { type: "paragraph" }
      ]).run();
    },
    insertTranscript: (L, de, ye) => {
      N && N.chain().focus().insertContentAt(N.state.doc.content.size, [
        {
          type: "transcript",
          attrs: {
            data: { title: L, users: de, messages: ye },
            isOpen: !1
          }
        },
        { type: "paragraph" }
      ]).run();
    },
    pushContent: (L) => {
      N && N.chain().focus().insertContentAt(N.state.doc.content.size, L).run();
    },
    insertImage: (L) => {
      !N || !o || Ma(N, L, {
        ...o,
        onError: (de) => {
          D(de);
        }
      });
    }
  }));
  const M = we(
    () => ({
      offset: [0, 5]
    }),
    []
  ), z = ne(
    ({ node: L, pos: de }) => {
      S.current = L ? { pos: de, nodeSize: L.nodeSize } : null;
    },
    []
  ), P = ne(() => {
    const L = S.current;
    if (!L || !N) return;
    const { pos: de, nodeSize: ye } = L, H = N.state.doc.nodeAt(de);
    if (H && H.content.size === 0)
      N.chain().focus().setTextSelection(de + 1).insertContent("/").run();
    else {
      const An = de + ye;
      N.chain().focus().insertContentAt(An, { type: "paragraph" }).setTextSelection(An + 1).insertContent("/").run();
    }
  }, [N]), V = a || c && c.length > 0 || d && d.length > 0 || u && u.length > 0 || h, J = l || k;
  return N ? /* @__PURE__ */ f(vl, { dataTestId: g, children: /* @__PURE__ */ w(
    "div",
    {
      className: "relative flex h-full w-full flex-col",
      ref: v,
      id: E,
      children: [
        V && /* @__PURE__ */ f(
          ky,
          {
            primaryAction: a,
            secondaryActions: c,
            metadata: d,
            otherActions: u,
            banner: h
          }
        ),
        C && /* @__PURE__ */ f("div", { className: "mx-auto flex w-full max-w-[824px] px-14 py-2", children: /* @__PURE__ */ w("div", { className: "flex w-max max-w-full items-center gap-4 rounded-md bg-f1-background-critical p-2 drop-shadow-sm", children: [
          /* @__PURE__ */ w("div", { className: "flex w-full flex-row items-center gap-2", children: [
            /* @__PURE__ */ f("div", { className: "flex-shrink-0", children: /* @__PURE__ */ f(Dl, { size: "sm", type: "critical" }) }),
            /* @__PURE__ */ f(
              "p",
              {
                className: "w-full max-w-xl flex-grow truncate text-ellipsis text-sm font-semibold text-f1-foreground-critical",
                title: y(C),
                children: y(C)
              }
            )
          ] }),
          /* @__PURE__ */ f("div", { className: "flex-shrink-0", children: /* @__PURE__ */ f(
            $,
            {
              variant: "outline",
              onClick: () => D(null),
              label: x.imageUpload.errors.dismiss,
              size: "sm"
            }
          ) })
        ] }) }),
        !s && !p && /* @__PURE__ */ f("div", { className: "absolute bottom-8 left-1/2 z-50 max-w-[calc(100%-48px)] -translate-x-1/2 rounded-lg border border-solid border-f1-border-secondary bg-f1-background p-2 shadow-md", children: /* @__PURE__ */ f(
          Wf,
          {
            editor: N,
            disableButtons: !1,
            showEmojiPicker: !1,
            plainHtmlMode: !1
          }
        ) }),
        /* @__PURE__ */ w(yn, { className: "h-full gap-6", children: [
          J && /* @__PURE__ */ f(
            Cy,
            {
              value: k,
              onChange: l ? T : void 0,
              placeholder: m,
              disabled: !l || s
            }
          ),
          /* @__PURE__ */ w(
            "div",
            {
              className: "notes-text-editor h-full",
              onClick: () => N.commands.focus(),
              children: [
                !s && /* @__PURE__ */ f(
                  gy,
                  {
                    editor: N,
                    tippyOptions: M,
                    onNodeChange: z,
                    children: /* @__PURE__ */ w("div", { className: "flex flex-row", children: [
                      /* @__PURE__ */ f(
                        bu,
                        {
                          compact: !0,
                          variant: "ghost",
                          size: "sm",
                          className: "text-f1-foreground-tertiary",
                          onClick: P,
                          label: "Add paragraph",
                          hideLabel: !0,
                          icon: _l
                        }
                      ),
                      /* @__PURE__ */ f(
                        "div",
                        {
                          className: "flex cursor-move items-center justify-center p-0.5 text-f1-icon-secondary",
                          draggable: !0,
                          "data-drag-handle": !0,
                          children: /* @__PURE__ */ f(G, { icon: gf, size: "xs" })
                        }
                      )
                    ] })
                  }
                ),
                /* @__PURE__ */ f(
                  Gf,
                  {
                    editor: N,
                    className: "pb-28 [&>div]:mx-auto [&>div]:w-full [&>div]:max-w-[824px] [&>div]:transition-[padding] [&>div]:duration-300 sm:[&>div]:px-14 [&>div]:px-0"
                  }
                )
              ]
            }
          )
        ] }),
        !s && /* @__PURE__ */ f(
          Kf,
          {
            editorId: E,
            editor: N,
            disableButtons: !1,
            isToolbarOpen: !p,
            isFullscreen: !1,
            plainHtmlMode: !1
          }
        )
      ]
    }
  ) }) : null;
}), ik = ({
  withHeader: t = !1,
  withTitle: e = !0,
  withToolbar: n = !0
}) => /* @__PURE__ */ w(
  "div",
  {
    className: "relative flex h-full w-full flex-col",
    "aria-busy": "true",
    "aria-live": "polite",
    children: [
      t && /* @__PURE__ */ w("div", { className: "flex items-center justify-between border-b border-f1-border px-6 py-3", children: [
        /* @__PURE__ */ w("div", { className: "flex items-center gap-3", children: [
          /* @__PURE__ */ f(A, { className: "h-6 w-20 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-6 w-24 rounded-md" })
        ] }),
        /* @__PURE__ */ w("div", { className: "flex items-center gap-2", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-16 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-12 rounded-md" })
        ] })
      ] }),
      n && /* @__PURE__ */ w("div", { className: "absolute bottom-8 left-1/2 z-50 flex -translate-x-1/2 flex-row items-center gap-[9px] rounded-lg bg-f1-background p-2 shadow-md", children: [
        /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
        /* @__PURE__ */ w("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" })
        ] }),
        /* @__PURE__ */ w("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" })
        ] }),
        /* @__PURE__ */ w("div", { className: "flex items-center gap-0.5", children: [
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" }),
          /* @__PURE__ */ f(A, { className: "h-8 w-8 rounded" })
        ] })
      ] }),
      /* @__PURE__ */ w(yn, { className: "h-full gap-6", children: [
        e && /* @__PURE__ */ f("div", { className: "mx-auto flex w-full max-w-[824px] flex-col px-14 pb-5 pt-5", children: /* @__PURE__ */ f(A, { className: "h-8 w-80 rounded-md" }) }),
        /* @__PURE__ */ f("div", { className: "h-full", children: /* @__PURE__ */ f("div", { className: "pb-28 [&>div]:mx-auto [&>div]:w-full [&>div]:max-w-[824px] [&>div]:px-14", children: /* @__PURE__ */ w("div", { className: "flex flex-col gap-2", children: [
          /* @__PURE__ */ f(A, { className: "h-5 w-full rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-5 w-4/5 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-5 w-3/5 rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-5 w-full rounded-md" }),
          /* @__PURE__ */ f(A, { className: "h-5 w-1/2 rounded-md" })
        ] }) }) })
      ] })
    ]
  }
), ok = Sy, Ei = {
  0: "gap-0",
  px: "gap-px",
  "0.5": "gap-0.5",
  1: "gap-1",
  "1.5": "gap-1.5",
  2: "gap-2",
  "2.5": "gap-2.5",
  3: "gap-3",
  4: "gap-4",
  5: "gap-5",
  6: "gap-6",
  7: "gap-7",
  8: "gap-8",
  9: "gap-9",
  10: "gap-10",
  11: "gap-11",
  12: "gap-12",
  14: "gap-14",
  16: "gap-16",
  sm: "gap-1",
  md: "gap-2",
  lg: "gap-3",
  xl: "gap-4"
}, Ny = Et({
  base: "grid grid-cols-1",
  variants: {
    tileSize: {
      // The amount of columns and autoflow when paginating is an issue if we
      // want to prevent orphan elments. Say, we have 10 elements, we can't just
      // render 3 rows of 3 elements and then an orphan one in the end.
      //
      // This makes sure that everything will look nice when using pages of 48
      // elements, it will always result in even rows.
      sm: "@12xl:grid-cols-16 @md:grid-cols-2 @2xl:grid-cols-3 @4xl:grid-cols-4 @8xl:grid-cols-6 @10xl:grid-cols-8 @11xl:grid-cols-12",
      md: "@lg:grid-cols-2 @4xl:grid-cols-3 @7xl:grid-cols-4 @9xl:grid-cols-6 @10xl:grid-cols-8 @12xl:grid-cols-12",
      lg: "@3xl:grid-cols-2 @7xl:grid-cols-3 @9xl:grid-cols-4 @10xl:grid-cols-6 @12xl:grid-cols-8"
    },
    gap: {
      ...Ei
    }
  },
  defaultVariants: {
    tileSize: "md",
    gap: "4"
  }
}), Iy = se.forwardRef(function({ className: e, gap: n, children: r, tileSize: s, ...i }, o) {
  return /* @__PURE__ */ f("div", { className: _("@container", "grow"), ref: o, ...i, children: /* @__PURE__ */ f(
    "div",
    {
      className: _(Ny({ gap: n, tileSize: s }), e),
      ref: o,
      ...i,
      children: r
    }
  ) });
}), _y = Et({
  base: "flex",
  variants: {
    overflow: {
      hidden: "overflow-hidden",
      auto: "overflow-auto"
    },
    paddingX: {
      none: "px-0",
      "p-2": "px-2",
      "p-4": "px-4",
      "p-8": "px-8",
      "p-12": "px-12",
      "p-16": "px-16"
    },
    maxWidth: {
      xs: "max-w-xs",
      sm: "max-w-sm",
      md: "max-w-md",
      xl: "max-w-xl",
      "screen-sm": "max-w-screen-sm",
      "screen-md": "max-w-screen-md",
      "screen-lg": "max-w-screen-lg",
      "screen-xl": "max-w-screen-xl",
      "screen-2xl": "max-w-screen-2xl"
    },
    height: {
      auto: "h-auto",
      full: "h-full"
    },
    width: {
      auto: "w-auto",
      full: "w-full"
    },
    paddingY: {
      none: "py-0",
      "p-2": "py-2",
      "p-4": "py-4",
      "p-8": "py-8",
      "p-12": "py-12",
      "p-16": "py-16"
    },
    basis: {
      0: "basis-0"
    },
    inline: {
      true: "inline-flex",
      false: "flex"
    },
    justifyContent: {
      center: "justify-center",
      end: "justify-end",
      "space-between": "justify-between",
      start: "justify-start",
      stretch: "justify-stretch"
    },
    alignItems: {
      center: "items-center",
      end: "items-end",
      "space-between": "items-between",
      start: "items-start",
      stretch: "items-stretch"
    },
    grow: {
      true: "flex-grow",
      false: "flex-grow-0"
    },
    shrink: {
      true: "flex-shrink",
      false: "flex-shrink-0"
    }
  },
  defaultVariants: {
    paddingX: "none",
    paddingY: "none",
    inline: !1
  }
}), _d = F(function({
  className: e,
  grow: n,
  basis: r,
  shrink: s,
  paddingX: i,
  paddingY: o,
  inline: l,
  overflow: a,
  maxWidth: c,
  justifyContent: u,
  alignItems: d,
  height: h,
  width: p,
  ...m
}, g) {
  return /* @__PURE__ */ f(
    "div",
    {
      className: _(
        _y({
          paddingX: i,
          basis: r,
          paddingY: o,
          grow: n,
          shrink: s,
          inline: l,
          overflow: a,
          maxWidth: c,
          justifyContent: u,
          alignItems: d,
          height: h,
          width: p
        }),
        e
      ),
      ref: g,
      ...m
    }
  );
}), Ay = Et({
  base: "flex-row",
  variants: {
    gap: {
      ...Ei
    },
    wrap: {
      true: "flex-wrap"
    }
  },
  defaultVariants: {
    wrap: !0
  }
}), Ty = se.forwardRef(function({ className: e, gap: n, wrap: r, ...s }, i) {
  return /* @__PURE__ */ f(
    _d,
    {
      className: _(Ay({ gap: n, wrap: r }), e),
      ref: i,
      ...s
    }
  );
}), Ey = Et({
  base: "flex-col",
  variants: {
    gap: {
      ...Ei
    }
  },
  defaultVariants: {}
}), Dy = F(function({ className: e, gap: n, children: r, ...s }, i) {
  return /* @__PURE__ */ f(
    _d,
    {
      className: _(Ey({ gap: n }), e),
      ref: i,
      ...s,
      children: r
    }
  );
}), lk = Cr(
  {
    name: "Stack",
    type: "layout"
  },
  Dy
), ak = Cr(
  {
    name: "Split",
    type: "layout"
  },
  Ty
), ck = Cr(
  {
    name: "AutoGrid",
    type: "layout"
  },
  Iy
), Ry = (t, e) => /* @__PURE__ */ w(
  "svg",
  {
    width: "366",
    height: "146",
    viewBox: "0 0 366 146",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    ref: e,
    ...t,
    children: [
      /* @__PURE__ */ f("g", { filter: "url(#filter0_b_378_17717)", children: /* @__PURE__ */ f(
        "rect",
        {
          y: "60",
          width: "40",
          height: "86",
          rx: "10",
          fill: "#F5A51C",
          fillOpacity: "0.1"
        }
      ) }),
      /* @__PURE__ */ f("g", { filter: "url(#filter1_b_378_17717)", children: /* @__PURE__ */ f(
        "rect",
        {
          x: "80",
          y: "33",
          width: "40",
          height: "113",
          rx: "10",
          fill: "#F5A51C",
          fillOpacity: "0.1"
        }
      ) }),
      /* @__PURE__ */ f("g", { filter: "url(#filter2_b_378_17717)", children: /* @__PURE__ */ f(
        "rect",
        {
          x: "162",
          y: "60",
          width: "40",
          height: "86",
          rx: "10",
          fill: "#F5A51C",
          fillOpacity: "0.1"
        }
      ) }),
      /* @__PURE__ */ f("g", { filter: "url(#filter3_b_378_17717)", children: /* @__PURE__ */ f(
        "rect",
        {
          x: "244",
          y: "38",
          width: "40",
          height: "108",
          rx: "10",
          fill: "#F5A51C",
          fillOpacity: "0.1"
        }
      ) }),
      /* @__PURE__ */ f("g", { filter: "url(#filter4_b_378_17717)", children: /* @__PURE__ */ f(
        "rect",
        {
          x: "326",
          width: "40",
          height: "146",
          rx: "10",
          fill: "#F5A51C",
          fillOpacity: "0.1"
        }
      ) }),
      /* @__PURE__ */ w("defs", { children: [
        /* @__PURE__ */ w(
          "filter",
          {
            id: "filter0_b_378_17717",
            x: "-40",
            y: "20",
            width: "120",
            height: "166",
            filterUnits: "userSpaceOnUse",
            colorInterpolationFilters: "sRGB",
            children: [
              /* @__PURE__ */ f("feFlood", { floodOpacity: "0", result: "BackgroundImageFix" }),
              /* @__PURE__ */ f("feGaussianBlur", { in: "BackgroundImageFix", stdDeviation: "20" }),
              /* @__PURE__ */ f(
                "feComposite",
                {
                  in2: "SourceAlpha",
                  operator: "in",
                  result: "effect1_backgroundBlur_378_17717"
                }
              ),
              /* @__PURE__ */ f(
                "feBlend",
                {
                  mode: "normal",
                  in: "SourceGraphic",
                  in2: "effect1_backgroundBlur_378_17717",
                  result: "shape"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ w(
          "filter",
          {
            id: "filter1_b_378_17717",
            x: "40",
            y: "-7",
            width: "120",
            height: "193",
            filterUnits: "userSpaceOnUse",
            colorInterpolationFilters: "sRGB",
            children: [
              /* @__PURE__ */ f("feFlood", { floodOpacity: "0", result: "BackgroundImageFix" }),
              /* @__PURE__ */ f("feGaussianBlur", { in: "BackgroundImageFix", stdDeviation: "20" }),
              /* @__PURE__ */ f(
                "feComposite",
                {
                  in2: "SourceAlpha",
                  operator: "in",
                  result: "effect1_backgroundBlur_378_17717"
                }
              ),
              /* @__PURE__ */ f(
                "feBlend",
                {
                  mode: "normal",
                  in: "SourceGraphic",
                  in2: "effect1_backgroundBlur_378_17717",
                  result: "shape"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ w(
          "filter",
          {
            id: "filter2_b_378_17717",
            x: "122",
            y: "20",
            width: "120",
            height: "166",
            filterUnits: "userSpaceOnUse",
            colorInterpolationFilters: "sRGB",
            children: [
              /* @__PURE__ */ f("feFlood", { floodOpacity: "0", result: "BackgroundImageFix" }),
              /* @__PURE__ */ f("feGaussianBlur", { in: "BackgroundImageFix", stdDeviation: "20" }),
              /* @__PURE__ */ f(
                "feComposite",
                {
                  in2: "SourceAlpha",
                  operator: "in",
                  result: "effect1_backgroundBlur_378_17717"
                }
              ),
              /* @__PURE__ */ f(
                "feBlend",
                {
                  mode: "normal",
                  in: "SourceGraphic",
                  in2: "effect1_backgroundBlur_378_17717",
                  result: "shape"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ w(
          "filter",
          {
            id: "filter3_b_378_17717",
            x: "204",
            y: "-2",
            width: "120",
            height: "188",
            filterUnits: "userSpaceOnUse",
            colorInterpolationFilters: "sRGB",
            children: [
              /* @__PURE__ */ f("feFlood", { floodOpacity: "0", result: "BackgroundImageFix" }),
              /* @__PURE__ */ f("feGaussianBlur", { in: "BackgroundImageFix", stdDeviation: "20" }),
              /* @__PURE__ */ f(
                "feComposite",
                {
                  in2: "SourceAlpha",
                  operator: "in",
                  result: "effect1_backgroundBlur_378_17717"
                }
              ),
              /* @__PURE__ */ f(
                "feBlend",
                {
                  mode: "normal",
                  in: "SourceGraphic",
                  in2: "effect1_backgroundBlur_378_17717",
                  result: "shape"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ w(
          "filter",
          {
            id: "filter4_b_378_17717",
            x: "286",
            y: "-40",
            width: "120",
            height: "226",
            filterUnits: "userSpaceOnUse",
            colorInterpolationFilters: "sRGB",
            children: [
              /* @__PURE__ */ f("feFlood", { floodOpacity: "0", result: "BackgroundImageFix" }),
              /* @__PURE__ */ f("feGaussianBlur", { in: "BackgroundImageFix", stdDeviation: "20" }),
              /* @__PURE__ */ f(
                "feComposite",
                {
                  in2: "SourceAlpha",
                  operator: "in",
                  result: "effect1_backgroundBlur_378_17717"
                }
              ),
              /* @__PURE__ */ f(
                "feBlend",
                {
                  mode: "normal",
                  in: "SourceGraphic",
                  in2: "effect1_backgroundBlur_378_17717",
                  result: "shape"
                }
              )
            ]
          }
        )
      ] })
    ]
  }
), Ly = F(Ry), My = (t, e) => /* @__PURE__ */ w(
  "svg",
  {
    width: "406",
    height: "179",
    viewBox: "0 0 406 179",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    ref: e,
    ...t,
    children: [
      /* @__PURE__ */ f(
        "path",
        {
          d: "M406 1L352.178 11.5985C343.237 13.359 334.653 16.5974 326.777 21.1805L270.327 54.031L208.727 80.0238C204.915 81.6326 200.986 82.9506 196.974 83.9662L146.837 96.6597C139.431 98.5348 132.323 101.436 125.72 105.279L80.2168 131.758C71.6933 136.718 62.3449 140.1 52.6208 141.742L1.12057e-05 150.623",
          stroke: "#E51943",
          strokeOpacity: "0.1",
          strokeWidth: "1.3",
          strokeLinejoin: "round"
        }
      ),
      /* @__PURE__ */ f(
        "path",
        {
          d: "M203 82.4405L270.327 54.031L338.673 14.2578L406 1V179H0V150.623L67.3266 139.26L135.673 99.4862L203 82.4405Z",
          fill: "url(#paint0_linear_3715_9085)"
        }
      ),
      /* @__PURE__ */ f("defs", { children: /* @__PURE__ */ w(
        "linearGradient",
        {
          id: "paint0_linear_3715_9085",
          x1: "203",
          y1: "179",
          x2: "203",
          y2: "1",
          gradientUnits: "userSpaceOnUse",
          children: [
            /* @__PURE__ */ f("stop", { stopColor: "#E51943", stopOpacity: "0" }),
            /* @__PURE__ */ f("stop", { offset: "1", stopColor: "#E51943", stopOpacity: "0.05" })
          ]
        }
      ) })
    ]
  }
), Oy = F(My), Fy = {
  "line-chart": "border-f1-border",
  "bar-chart": "border-f1-border-promote"
}, zy = {
  "line-chart": "min-h-48",
  "bar-chart": "min-h-64"
}, Py = {
  "line-chart": "from-f1-background-accent",
  "bar-chart": "from-f1-background-promote"
}, By = {
  "line-chart": "default",
  "bar-chart": "promote"
}, $y = F(
  function({ title: e, content: n, buttonLabel: r, buttonIcon: s, buttonAction: i, type: o }, l) {
    const a = Fy[o], c = zy[o], u = Py[o], d = By[o];
    return /* @__PURE__ */ w(
      wu,
      {
        className: _(
          "relative flex gap-4 overflow-hidden border-dashed",
          a
        ),
        ref: l,
        children: [
          /* @__PURE__ */ f(xu, { className: "-mt-0.5", children: /* @__PURE__ */ f(yu, { children: e }) }),
          /* @__PURE__ */ w(vu, { className: _("flex flex-col gap-4", c), children: [
            /* @__PURE__ */ w(
              "div",
              {
                className: _(
                  "absolute -top-12 bottom-0 left-0 right-0 flex flex-col justify-end bg-gradient-to-b to-transparent opacity-30",
                  u
                ),
                children: [
                  o === "bar-chart" && /* @__PURE__ */ f("div", { className: "absolute bottom-1 left-4 right-4", children: /* @__PURE__ */ f(Ly, { className: "w-full" }) }),
                  o === "line-chart" && /* @__PURE__ */ f(Oy, { className: "w-full" })
                ]
              }
            ),
            /* @__PURE__ */ w("div", { className: "relative flex min-h-28 flex-1 flex-col items-start gap-5", children: [
              /* @__PURE__ */ f("p", { className: "flex w-3/4 text-xl font-semibold", children: n }),
              r && /* @__PURE__ */ f(
                $,
                {
                  label: r,
                  icon: s,
                  variant: d,
                  onClick: i
                }
              )
            ] })
          ] })
        ]
      }
    );
  }
), dk = Ge(
  Y("ChartWidgetEmptyState", $y)
);
function Hy(t, e) {
  const n = ge(null), r = ge(null), [s, i] = K({
    visibleItems: [],
    overflowItems: []
  });
  ku({
    ref: n,
    onResize: () => {
      c();
    }
  });
  const o = ne(
    (u, d, h) => d < h - 1 ? u + e : u,
    [e]
  ), l = ne(() => {
    if (!r.current) return [];
    const u = r.current.children, d = [];
    for (let h = 0; h < u.length; h++) {
      const p = u[h].getBoundingClientRect().height;
      d.push(p);
    }
    return d;
  }, []), a = ne(
    (u, d) => {
      let h = 0, p = 0;
      for (let m = 0; m < u.length; m++) {
        const g = p + u[m];
        if (g > d + 30) break;
        p = g, p = o(
          p,
          m,
          u.length
        ), h++;
      }
      return h;
    },
    [o]
  ), c = ne(() => {
    if (!n.current || t.length === 0) return;
    const u = n.current.clientHeight, d = l(), h = a(
      d,
      u
    );
    i(h === 0 ? {
      visibleItems: [],
      overflowItems: t
    } : (p) => p.visibleItems.length === h && p.overflowItems.length === t.length - h ? p : {
      visibleItems: t.slice(0, h),
      overflowItems: t.slice(h)
    });
  }, [t, l, a]);
  return ue(() => {
    c();
  }, [c]), {
    containerRef: n,
    measurementContainerRef: r,
    visibleItems: s.visibleItems,
    overflowItems: s.overflowItems
  };
}
const Hr = function({
  items: e,
  renderListItem: n,
  className: r,
  gap: s = 0,
  minSize: i,
  onVisibleItemsChange: o
}) {
  const { containerRef: l, measurementContainerRef: a, visibleItems: c } = Hy(e, s);
  return ue(() => {
    o?.(c);
  }, [o, c]), /* @__PURE__ */ w(
    "div",
    {
      ref: l,
      className: _("relative flex h-full flex-col", r),
      style: {
        minHeight: `${i}px`
      },
      children: [
        /* @__PURE__ */ f(
          "div",
          {
            ref: a,
            "aria-hidden": "true",
            className: "pointer-events-none invisible absolute left-0 right-0 top-0 flex flex-col opacity-0",
            style: { gap: `${s}px` },
            "data-testid": "overflow-measurement-container",
            children: e.map((u, d) => /* @__PURE__ */ f("div", { "data-testid": "overflow-measurement-item", children: n(u, d, !1) }, `measure-${d}`))
          }
        ),
        /* @__PURE__ */ f(
          "div",
          {
            className: "flex flex-col",
            style: { gap: `${s}px` },
            "data-testid": "overflow-visible-container",
            children: c.map((u, d) => /* @__PURE__ */ f(
              "div",
              {
                className: "transition-all duration-150",
                "data-testid": "overflow-visible-item",
                children: n(u, d, !0)
              },
              `item-${d}`
            ))
          }
        )
      ]
    }
  );
};
Hr.displayName = "VerticalOverflowList";
const uk = ({
  events: t,
  showAllItems: e,
  gap: n = 8,
  minSize: r = 184
}) => t.length ? e ? /* @__PURE__ */ f("div", { className: "flex flex-col", children: t.map((s) => /* @__PURE__ */ f(_s, { ...s }, s.title)) }) : /* @__PURE__ */ f(
  Hr,
  {
    items: t,
    gap: n,
    minSize: r,
    renderListItem: (s) => /* @__PURE__ */ f(_s, { ...s }, s.title)
  }
) : null, jy = ({ onClick: t, children: e }) => {
  const n = "block rounded-lg border border-solid border-transparent p-[1px] -m-1";
  return t ? /* @__PURE__ */ f(
    "a",
    {
      className: _(
        n,
        "cursor-pointer focus:border-f1-background-selected-bold focus:outline-none"
      ),
      onClick: t,
      tabIndex: 0,
      children: e
    }
  ) : /* @__PURE__ */ f("div", { className: n, tabIndex: 1, children: e });
};
function fk({
  label: t,
  count: e,
  icon: n,
  iconClassName: r,
  onClick: s
}) {
  return /* @__PURE__ */ f(jy, { onClick: s, children: /* @__PURE__ */ w(
    "div",
    {
      className: _(
        "flex flex-col gap-0.5 rounded-md border border-solid border-f1-border-secondary px-3 py-2.5",
        s && "hover:border-f1-border-hover"
      ),
      children: [
        /* @__PURE__ */ w("div", { className: "flex flex-row items-center", children: [
          /* @__PURE__ */ f("p", { className: "line-clamp-1 flex-1 text-f1-foreground-secondary", children: t }),
          /* @__PURE__ */ f(G, { icon: n, size: "md", className: r })
        ] }),
        /* @__PURE__ */ f("p", { className: "line-clamp-1 flex-1 text-3xl font-semibold text-f1-foreground", children: e })
      ]
    }
  ) });
}
const Vy = F(
  function({ content: e, label: n, color: r, ...s }, i) {
    return /* @__PURE__ */ w("div", { className: "flex flex-col gap-1", ref: i, children: [
      /* @__PURE__ */ f("p", { className: "text-3xl font-semibold", children: e }),
      /* @__PURE__ */ w("div", { className: "flex items-center gap-1", children: [
        /* @__PURE__ */ f(
          "p",
          {
            className: "line-clamp-1 text-f1-foreground-secondary",
            title: n,
            children: n
          }
        ),
        "icon" in s && s.icon && /* @__PURE__ */ f("span", { className: _("flex", r), children: /* @__PURE__ */ f(G, { icon: s.icon }) }),
        "emoji" in s && s.emoji && /* @__PURE__ */ f("span", { className: _("flex", r), children: /* @__PURE__ */ f(Xs, { emoji: s.emoji, size: "md" }) })
      ] })
    ] }, n);
  }
), hk = F(
  function({ items: e }, n) {
    return /* @__PURE__ */ f(
      "div",
      {
        ref: n,
        className: "grid auto-cols-fr grid-flow-col items-end gap-x-3",
        children: e.map(({ label: r, content: s, color: i, ...o }) => /* @__PURE__ */ f(
          Vy,
          {
            label: r,
            content: s,
            color: i,
            ...o
          },
          `${r}-${s}`
        ))
      }
    );
  }
), Uy = ({
  onClick: t,
  withEmoji: e,
  withPointerCursor: n,
  children: r
}) => {
  const s = _(
    "flex flex-row items-center rounded-md border border-solid border-transparent p-2 text-f1-foreground",
    e ? "gap-2" : "gap-2.5",
    n ? "cursor-pointer" : "cursor-default",
    t ? "hover:bg-f1-background-tertiary focus:border-f1-background-selected-bold focus:outline-none" : void 0
  );
  return t ? /* @__PURE__ */ f("a", { className: s, onClick: t, children: r }) : /* @__PURE__ */ f("div", { className: s, children: r });
};
function pk({
  id: t,
  title: e,
  subtitle: n,
  avatars: r,
  remainingCount: s,
  withPointerCursor: i = !1,
  onClick: o,
  ...l
}) {
  return /* @__PURE__ */ w(
    Uy,
    {
      onClick: (c) => {
        c.preventDefault(), o?.(t);
      },
      withEmoji: "emoji" in l && !!l.emoji,
      withPointerCursor: i,
      children: [
        "alert" in l && l.alert && /* @__PURE__ */ f(Dl, { type: l.alert }),
        "emoji" in l && l.emoji && /* @__PURE__ */ f(Cu, { emoji: l.emoji }),
        /* @__PURE__ */ w("div", { className: "flex-1", children: [
          /* @__PURE__ */ f("p", { className: "line-clamp-1 font-medium", children: e }),
          /* @__PURE__ */ f("p", { className: "line-clamp-1 text-f1-foreground-secondary", children: n })
        ] }),
        /* @__PURE__ */ f("div", { className: "min-w-0 flex-1", children: /* @__PURE__ */ f(
          ti,
          {
            avatars: r,
            remainingCount: s,
            size: "emoji" in l && l.emoji ? "md" : "sm",
            type: "person"
          }
        ) })
      ]
    }
  );
}
const Wy = ({ onClick: t, className: e, children: n }) => t ? /* @__PURE__ */ f("a", { className: e, onClick: t, tabIndex: 0, children: n }) : /* @__PURE__ */ f("div", { className: e, tabIndex: -1, children: n });
function ll({
  id: t,
  title: e,
  subtitle: n,
  onClick: r,
  module: s
}) {
  const i = _(
    "flex flex-row gap-2 rounded-md border border-solid border-transparent p-2 text-f1-foreground",
    r ? "cursor-pointer hover:bg-f1-background-tertiary focus:border-f1-background-selected-bold focus:outline-none" : void 0
  );
  return /* @__PURE__ */ w(Wy, { onClick: (l) => {
    l.preventDefault(), r?.(t);
  }, className: i, children: [
    /* @__PURE__ */ f(gl, { module: s ?? "inbox", size: "md" }),
    /* @__PURE__ */ w("div", { className: "flex-1", children: [
      /* @__PURE__ */ f("p", { className: "line-clamp-1 font-medium", children: e }),
      /* @__PURE__ */ f("p", { className: "line-clamp-1 text-f1-foreground-secondary", children: n })
    ] })
  ] });
}
const Gy = ({ onClick: t, className: e, children: n }) => t ? /* @__PURE__ */ f("a", { className: e, onClick: t, tabIndex: 0, children: n }) : /* @__PURE__ */ f("div", { className: e, tabIndex: -1, children: n });
function Ks({
  id: t,
  title: e,
  alert: n,
  rawTag: r,
  count: s,
  icon: i,
  rightIcon: o,
  iconClassName: l = "text-f1-icon-secondary",
  rightIconClassName: a = "text-f1-icon-secondary",
  onClick: c
}) {
  const u = _(
    "flex flex-row items-start gap-1 rounded-md border border-solid border-transparent px-2 py-1.5 text-f1-foreground",
    c ? "cursor-pointer hover:bg-f1-background-tertiary focus:border-f1-background-selected-bold focus:outline-none" : void 0
  );
  return /* @__PURE__ */ w(Gy, { onClick: (h) => {
    h.preventDefault(), c?.(t);
  }, className: u, children: [
    /* @__PURE__ */ w("div", { className: "flex flex-1 flex-row items-start gap-1", children: [
      i && /* @__PURE__ */ f(
        G,
        {
          icon: i,
          size: "md",
          className: _("mt-0.5", l)
        }
      ),
      /* @__PURE__ */ f("p", { className: "mt-0.5 line-clamp-2 font-medium", children: e }),
      o && /* @__PURE__ */ f(
        G,
        {
          icon: o,
          size: "md",
          className: _("mt-0.5", a)
        }
      )
    ] }),
    /* @__PURE__ */ w("div", { className: "flex flex-row items-center gap-2", children: [
      n && /* @__PURE__ */ f(xl, { ...n }),
      r && /* @__PURE__ */ f(jt, { ...r }),
      !!s && /* @__PURE__ */ f(pl, { value: s })
    ] })
  ] });
}
function mk({
  items: t,
  minSize: e = 184,
  onClickItem: n,
  showAllItems: r,
  onVisibleItemsChange: s
}) {
  return r ? /* @__PURE__ */ f("div", { className: "flex flex-col gap-2", children: t.map((i) => /* @__PURE__ */ f(ll, { ...i, onClick: n }, i.id)) }) : /* @__PURE__ */ f(
    Hr,
    {
      items: t,
      minSize: e,
      renderListItem: (i) => /* @__PURE__ */ f(ll, { ...i, onClick: n }, i.id),
      onVisibleItemsChange: s,
      gap: 8
    }
  );
}
function gk({
  items: t,
  gap: e,
  minSize: n = 184,
  onClickItem: r,
  showAllItems: s
}) {
  return s ? /* @__PURE__ */ f("div", { className: "flex flex-col", style: { minHeight: `${n}px` }, children: t.map((i) => /* @__PURE__ */ f(Ks, { ...i, onClick: r }, i.id)) }) : /* @__PURE__ */ f(
    Hr,
    {
      items: t,
      gap: e,
      renderListItem: (i) => /* @__PURE__ */ f(Ks, { ...i, onClick: r }),
      minSize: n
    }
  );
}
const Ky = ({ title: t, info: e }) => /* @__PURE__ */ w("div", { className: "flex items-center justify-between", children: [
  /* @__PURE__ */ f("p", { className: "flex text-f1-foreground-secondary", children: t }),
  /* @__PURE__ */ f("div", { className: "max-w-32 flex-shrink-0 overflow-hidden text-ellipsis whitespace-nowrap text-right font-medium", children: e })
] }), bk = F(
  function({ title: e, titleValue: n, titleTooltip: r, list: s }, i) {
    return /* @__PURE__ */ w("div", { ref: i, className: "flex flex-col gap-2", children: [
      e && /* @__PURE__ */ w("div", { className: "flex items-center justify-between gap-2 font-medium", children: [
        /* @__PURE__ */ w("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ f("div", { children: e }),
          r && /* @__PURE__ */ f("div", { className: "flex h-4 w-4 items-center text-f1-foreground-tertiary hover:cursor-help", children: /* @__PURE__ */ f(
            kr,
            {
              label: r.label,
              description: r.description,
              children: /* @__PURE__ */ f(G, { icon: Su, size: "sm" })
            }
          ) })
        ] }),
        n && /* @__PURE__ */ f("div", { children: n })
      ] }),
      s.map((o) => /* @__PURE__ */ f(Ky, { title: o.title, info: o.info }, o.title))
    ] });
  }
);
function qy(t) {
  return F(t);
}
const Yy = ({ data: t, legend: e = !0, hideTooltip: n = !1 }, r) => {
  const s = t.reduce((i, o) => i + o.value, 0);
  return /* @__PURE__ */ w(Nu, { children: [
    /* @__PURE__ */ f("div", { className: "w-full", ref: r, children: /* @__PURE__ */ f("div", { className: "flex h-2 gap-1 overflow-hidden", children: t.map((i, o) => {
      const l = i.value / s * 100, a = i.color ? Mi(i.color) : Oi(o), c = (u) => {
        const d = u / s * 100;
        return d % 1 === 0 ? d.toFixed(0) : d.toFixed(1);
      };
      return l === 0 ? null : /* @__PURE__ */ w(Iu, { children: [
        /* @__PURE__ */ f(
          _u,
          {
            className: "h-full cursor-default overflow-hidden rounded-2xs",
            style: { width: `${l}%` },
            title: i.name,
            asChild: !0,
            children: /* @__PURE__ */ f(
              "div",
              {
                className: "h-full w-full",
                style: { backgroundColor: a },
                role: "img",
                title: i.name,
                tabIndex: 0
              }
            )
          }
        ),
        !n && /* @__PURE__ */ w(Au, { className: "flex items-center gap-1 text-sm", children: [
          /* @__PURE__ */ f(
            "div",
            {
              className: "h-2.5 w-2.5 shrink-0 translate-y-px rounded-full",
              style: { backgroundColor: a }
            }
          ),
          /* @__PURE__ */ f("span", { className: "pl-0.5 pr-2 text-f1-foreground-inverse-secondary dark:text-f1-foreground-secondary", children: i.name }),
          /* @__PURE__ */ w("span", { className: "font-mono font-medium tabular-nums text-f1-foreground-inverse dark:text-f1-foreground", children: [
            i.value,
            " (",
            c(i.value),
            "%)"
          ] })
        ] })
      ] }, i.name);
    }) }) }),
    e && /* @__PURE__ */ f(
      "div",
      {
        className: "mt-2 flex w-full flex-wrap gap-x-2.5 gap-y-0.5",
        role: "list",
        children: t.map((i, o) => {
          const l = i.color ? Mi(i.color) : Oi(o);
          return /* @__PURE__ */ w(
            "div",
            {
              className: "flex items-center gap-1.5",
              role: "listitem",
              children: [
                /* @__PURE__ */ f(
                  "div",
                  {
                    className: "h-2 w-2 shrink-0 rounded-full",
                    style: { backgroundColor: l }
                  }
                ),
                /* @__PURE__ */ f("span", { className: "text-f1-foreground", children: i.name })
              ]
            },
            i.name
          );
        })
      }
    )
  ] });
}, Jy = qy(Yy);
function wk({
  title: t,
  subtitle: e,
  data: n,
  helpText: r,
  legend: s = !1,
  hideTooltip: i = !1
}) {
  return /* @__PURE__ */ w("div", { children: [
    /* @__PURE__ */ w("div", { className: "flex items-baseline justify-between", children: [
      /* @__PURE__ */ f("span", { className: "text-3xl font-semibold", children: t }),
      /* @__PURE__ */ f("span", { className: "text-xl text-f1-foreground-secondary", children: e })
    ] }),
    /* @__PURE__ */ f("div", { className: "mt-2", children: /* @__PURE__ */ f(
      Jy,
      {
        data: n,
        legend: s,
        hideTooltip: i
      }
    ) }),
    !!r && /* @__PURE__ */ f("div", { className: s ? "mt-1" : "mt-2", children: /* @__PURE__ */ f(
      "span",
      {
        className: _(
          "text-f1-foreground",
          s ? "text-sm" : "text-base"
        ),
        children: r
      }
    ) })
  ] });
}
const Ad = (t, e) => ((e ?? 0) < -1 * (t ?? 0) ? -1 * t : e) ?? 0, Xy = ({
  data: t = [],
  labels: e,
  trackedMinutes: n,
  remainingMinutes: r,
  canSeeRemainingTime: s = !0
}) => {
  const o = t[t.length - 1]?.variant || "clocked-out", l = {
    "clocked-out": e.clockedOut,
    "clocked-in": e.clockedIn,
    break: e.onBreak
  }[o], a = (() => {
    if (!s || r === void 0) return;
    const u = Ad(
      n,
      r
    ), d = Math.abs(u), h = Math.floor(d / 60), p = Math.floor(d % 60), m = `${h.toString().padStart(2, "0")}:${p.toString().padStart(2, "0")}`;
    return r >= 0 ? `${e.remainingTime} ${m}` : `${e.overtime} ${m}`;
  })(), c = Ct[o];
  return {
    status: o,
    statusText: l,
    subtitle: a,
    statusColor: c
  };
}, cs = "--:--", Zy = (t, e) => {
  if (t && t > 0)
    return {
      value: t * 60 / e,
      color: Ct.empty
    };
  if (!e)
    return {
      value: 1,
      color: Ct.empty
    };
}, Qy = ({
  data: t = [],
  trackedMinutes: e,
  remainingMinutes: n = 0
}) => {
  const r = n < 0 && n < -1 * e, s = Ad(
    e,
    n
  ), i = t.reduce(
    (u, d) => u + (d.to.getTime() - d.from.getTime()) / 1e3,
    0
  ) + (s ?? 0) * 60, o = r || (s ?? 0) < 0 ? void 0 : Zy(
    s ?? 0,
    i
  );
  let l = (s ?? 0) < 0 ? Math.abs(s ?? 0) * 60 : 0, c = [
    ...[...t].reverse().reduce(
      (u, d) => {
        const h = (d.to.getTime() - d.from.getTime()) / 1e3, p = d.variant === "clocked-in" ? Math.min(h, l) : 0, g = (h - p) / i;
        return l -= p, d.variant === "clocked-in" && r ? [
          ...u,
          {
            value: p / i + g,
            color: Ct.overtime
          }
        ] : [
          ...u,
          {
            value: p / i,
            color: Ct.overtime
          },
          {
            value: g,
            color: Ct[d.variant]
          }
        ];
      },
      []
    ).reverse(),
    ...o ? [o] : []
  ];
  return c = c.filter((u) => u.value > 0), c.length || c.push({
    value: 1,
    color: Ct.empty
  }), c;
}, ev = ({
  data: t = [],
  remainingMinutes: e,
  trackedMinutes: n = 0
}) => {
  const r = t.find((h) => h.variant === "clocked-in")?.from, s = t.at(-1), i = r ? Vi(r) : cs, o = e === void 0 || e > 0 ? cs : s ? Vi(s.to) : cs, a = s?.variant === "break" ? s?.to.getTime() - s?.from.getTime() || 0 : n * 60 * 1e3, c = Math.floor(a / (1e3 * 60 * 60)), u = Math.floor(a % (1e3 * 60 * 60) / (1e3 * 60)), d = `${c.toString().padStart(2, "0")}:${u.toString().padStart(2, "0")}`;
  return {
    primaryLabel: i,
    secondaryLabel: o,
    time: d
  };
}, Ct = {
  "clocked-in": "hsl(var(--positive-50))",
  break: "hsl(var(--promote-50))",
  empty: "hsl(var(--neutral-10))",
  "clocked-out": "hsl(var(--neutral-10))",
  overtime: "hsl(var(--warning-50))"
};
function tv({
  data: t = [],
  trackedMinutes: e = 0,
  remainingMinutes: n
}) {
  const r = Qy({
    data: t,
    trackedMinutes: e,
    remainingMinutes: n
  }), { primaryLabel: s, secondaryLabel: i, time: o } = ev({
    data: t,
    trackedMinutes: e,
    remainingMinutes: n
  });
  return /* @__PURE__ */ w("div", { className: "relative h-40 w-40", children: [
    /* @__PURE__ */ f(Qf, { width: 156, height: 156, children: /* @__PURE__ */ f(
      eh,
      {
        data: r,
        cx: 74,
        cy: 74,
        innerRadius: 62,
        outerRadius: 74,
        startAngle: 225,
        endAngle: -45,
        paddingAngle: 2,
        cornerRadius: 4,
        dataKey: "value",
        strokeWidth: 0,
        isAnimationActive: !1,
        children: r.map((l, a) => /* @__PURE__ */ f(
          th,
          {
            fill: l.color,
            role: "presentation",
            "aria-label": `${l.value} minutes`
          },
          `cell-${a}`
        ))
      }
    ) }),
    /* @__PURE__ */ f("div", { className: "absolute inset-0 flex items-center justify-center", children: /* @__PURE__ */ f("span", { className: "text-3xl font-semibold tabular-nums text-f1-foreground", children: o }) }),
    /* @__PURE__ */ w("div", { className: "absolute bottom-3 flex w-full justify-between px-8 text-f1-foreground-secondary", children: [
      /* @__PURE__ */ f("span", { className: "text-sm font-medium opacity-60", children: s }),
      /* @__PURE__ */ f("span", { className: "text-sm font-medium opacity-60", children: i })
    ] })
  ] });
}
function nv({
  text: t,
  placeholder: e,
  icon: n,
  onClick: r
}) {
  return /* @__PURE__ */ w(
    "div",
    {
      className: "flex cursor-default flex-row items-center gap-1 rounded-xs px-1 py-0.5 hover:bg-f1-background-hover",
      onClick: r,
      children: [
        n && /* @__PURE__ */ f(G, { icon: n, className: "text-f1-icon" }),
        /* @__PURE__ */ f(
          "span",
          {
            className: _(
              "font-medium",
              t ? "text-f1-foreground" : "text-f1-foreground-secondary"
            ),
            children: t ?? e
          }
        ),
        /* @__PURE__ */ f(G, { icon: bf })
      ]
    }
  );
}
function xk({
  trackedMinutes: t,
  remainingMinutes: e,
  data: n = [],
  labels: r,
  locationId: s,
  locations: i,
  canShowLocation: o = !0,
  locationSelectorDisabled: l = !1,
  onClockIn: a,
  onClockOut: c,
  onBreak: u,
  breakTypes: d,
  onChangeBreakTypeId: h,
  canShowBreakButton: p = !0,
  canSeeGraph: m = !0,
  canSeeRemainingTime: g = !0,
  // onClickProjectSelector,
  onChangeLocationId: b,
  canShowProject: x = !0,
  projectSelectorElement: v,
  breakTypeName: S
}) {
  const { status: E, statusText: I, subtitle: k, statusColor: T } = Xy({
    data: n,
    labels: r,
    trackedMinutes: t,
    remainingMinutes: e,
    canSeeRemainingTime: g
  }), C = E === "clocked-out", D = d?.map((H) => ({
    value: H.id,
    label: H.duration ? `${H.name} · ${H.duration}` : H.name,
    description: H.description,
    tag: H.isPaid ? r.paid : r.unpaid
  })) ?? [], [y, N] = K(!1), M = () => {
    if (D.length > 1)
      y || N(!0);
    else {
      const H = D?.[0]?.value;
      u?.(H);
    }
  }, z = (H) => {
    h?.(H), N(!1), u?.(H);
  }, P = C && i.length && !l && o, V = i.find((H) => H.id === s), J = i.map((H) => ({
    value: H.id,
    label: H.name,
    icon: H.icon
  })), L = E === "break", [de, ye] = K(!1);
  return /* @__PURE__ */ f("div", { className: "@container", children: /* @__PURE__ */ w("div", { className: "flex-grow flex-col", children: [
    /* @__PURE__ */ w("div", { className: "flex flex-col-reverse items-center gap-2 @xs:flex-row", children: [
      /* @__PURE__ */ w("div", { className: "flex-1 space-y-4", children: [
        /* @__PURE__ */ w("div", { className: "flex flex-col items-center space-y-0.5 @xs:items-start", children: [
          /* @__PURE__ */ w("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ f("span", { className: "line-clamp-1 text-2xl font-semibold text-f1-foreground", children: I }),
            /* @__PURE__ */ w("div", { className: "relative aspect-square h-4", children: [
              /* @__PURE__ */ f(
                us.div,
                {
                  className: "absolute inset-0 rounded-full opacity-20",
                  style: {
                    backgroundColor: T
                  },
                  initial: { scale: 0.5, opacity: 0.6 },
                  animate: { scale: 1.6, opacity: 0 },
                  transition: {
                    duration: 1.5,
                    repeat: 1 / 0,
                    repeatDelay: 1
                  }
                }
              ),
              /* @__PURE__ */ f(
                "div",
                {
                  className: "absolute inset-[3px] rounded-full",
                  style: {
                    backgroundColor: T
                  }
                }
              )
            ] })
          ] }),
          k && /* @__PURE__ */ f("p", { className: "line-clamp-1 text-f1-foreground-secondary", children: k })
        ] }),
        /* @__PURE__ */ w("div", { className: "flex justify-center gap-2 @xs:justify-start", children: [
          E === "clocked-out" && /* @__PURE__ */ f("div", { className: "mr-3 @xs:mr-0", children: /* @__PURE__ */ f(
            $,
            {
              onClick: a,
              label: r.clockIn,
              icon: Fi
            }
          ) }),
          E === "clocked-in" && /* @__PURE__ */ w(ae, { children: [
            p && /* @__PURE__ */ f(ae, { children: D.length > 1 && h ? /* @__PURE__ */ f(
              fs,
              {
                label: r.break,
                hideLabel: !0,
                value: "",
                options: D,
                onChange: z,
                open: y,
                onOpenChange: N,
                children: /* @__PURE__ */ f("div", { "aria-label": "Select break type", children: /* @__PURE__ */ f(
                  $,
                  {
                    label: r.break,
                    variant: "neutral",
                    icon: zi,
                    hideLabel: !0
                  }
                ) })
              }
            ) : /* @__PURE__ */ f(
              $,
              {
                onClick: M,
                label: r.break,
                variant: "neutral",
                icon: zi,
                hideLabel: !0
              }
            ) }),
            /* @__PURE__ */ f(
              $,
              {
                onClick: c,
                label: r.clockOut,
                variant: "neutral",
                icon: Pi
              }
            )
          ] }),
          E === "break" && /* @__PURE__ */ w(ae, { children: [
            /* @__PURE__ */ f(
              $,
              {
                onClick: c,
                label: r.clockOut,
                variant: "neutral",
                icon: Pi,
                hideLabel: !0
              }
            ),
            /* @__PURE__ */ f(
              $,
              {
                onClick: a,
                label: r.resume,
                icon: Fi
              }
            )
          ] })
        ] })
      ] }),
      m && /* @__PURE__ */ f(
        tv,
        {
          data: n,
          trackedMinutes: t,
          remainingMinutes: g ? e : 0
        }
      )
    ] }),
    /* @__PURE__ */ f("div", { className: "mt-6 flex flex-row flex-wrap items-center justify-center gap-2 @xs:justify-start", children: P ? /* @__PURE__ */ w(ae, { children: [
      /* @__PURE__ */ f(
        fs,
        {
          label: r.selectLocation,
          hideLabel: !0,
          value: s,
          options: J,
          onChange: b,
          open: de,
          onOpenChange: ye,
          disabled: l,
          children: /* @__PURE__ */ f("div", { "aria-label": "Select location", children: /* @__PURE__ */ f(
            nv,
            {
              text: V?.name,
              placeholder: r.selectLocation,
              icon: V?.icon
            }
          ) })
        }
      ),
      x && v
    ] }) : /* @__PURE__ */ w(ae, { children: [
      o && V && /* @__PURE__ */ f(ae, { children: /* @__PURE__ */ f(jt, { text: V.name, icon: V.icon }) }),
      x && v,
      L && S && /* @__PURE__ */ f(jt, { text: S })
    ] }) })
  ] }) });
}
const rv = {
  done: yf,
  "in-progress": xf,
  todo: wf
}, sv = {
  done: "text-f1-icon-success",
  "in-progress": "text-f1-icon-info",
  todo: "text-f1-icon"
};
function iv({
  task: t,
  status: e,
  onClick: n,
  hideIcon: r = !1
}) {
  const s = () => {
    n?.(t);
  }, i = we(() => {
    if (!r)
      return rv[e];
  }, [e, r]);
  return /* @__PURE__ */ f(
    Ks,
    {
      id: t.id,
      title: t.text,
      icon: i,
      iconClassName: sv[e],
      alert: t.badge?.isPastDue ? {
        text: t.badge.text,
        level: "critical"
      } : void 0,
      rawTag: t.badge && !t.badge.isPastDue ? {
        text: t.badge.text,
        icon: vf
      } : void 0,
      count: t.counter,
      onClick: s
    }
  );
}
function yk({
  tasks: t,
  onClickTask: e,
  hideIcons: n = !1,
  maxTasksToShow: r = 5,
  emptyMessage: s = "No tasks assigned"
}) {
  const o = [
    { key: "done", status: "done" },
    { key: "inProgress", status: "in-progress" },
    { key: "todo", status: "todo" }
  ].flatMap(
    ({ key: a, status: c }) => (t[a] || []).map(
      (u) => typeof u == "string" ? {
        id: u,
        text: u
      } : u
    ).map(({ id: u, text: d, badge: h, counter: p }) => ({
      id: u,
      text: d,
      badge: h,
      counter: p,
      status: c
    }))
  ), l = !o.length;
  return /* @__PURE__ */ f("div", { className: "flex flex-col gap-0", children: l ? /* @__PURE__ */ f("p", { className: "pl-2 font-medium", children: s }) : o.slice(0, r).map((a) => /* @__PURE__ */ f(
    iv,
    {
      task: a,
      status: a.status,
      hideIcon: n,
      onClick: e
    },
    a.id
  )) });
}
var ov = Object.defineProperty, lv = Object.defineProperties, av = Object.getOwnPropertyDescriptors, yr = Object.getOwnPropertySymbols, Td = Object.prototype.hasOwnProperty, Ed = Object.prototype.propertyIsEnumerable, al = (t, e, n) => e in t ? ov(t, e, { enumerable: !0, configurable: !0, writable: !0, value: n }) : t[e] = n, Le = (t, e) => {
  for (var n in e || (e = {})) Td.call(e, n) && al(t, n, e[n]);
  if (yr) for (var n of yr(e)) Ed.call(e, n) && al(t, n, e[n]);
  return t;
}, jr = (t, e) => lv(t, av(e)), cv = (t, e) => {
  var n = {};
  for (var r in t) Td.call(t, r) && e.indexOf(r) < 0 && (n[r] = t[r]);
  if (t != null && yr) for (var r of yr(t)) e.indexOf(r) < 0 && Ed.call(t, r) && (n[r] = t[r]);
  return n;
}, dv = (t) => t.right - t.left < 5 || t.bottom && t.bottom - t.top < 5, uv = ({ spotsList: t, usedSpot: e }) => t.some((n) => n !== e && e.left === n.left), fv = ({ position: t, spot: e, stone: n }) => {
  if (t.left > e.left && e.right >= t.left && t.top + n.height > e.top) {
    if (e.bottom && e.bottom < t.top) return e;
    let r = Le({}, e);
    return r.right = t.left, r;
  }
  return null;
}, hv = ({ position: t, stone: e, spot: n }) => {
  if (t.left + e.width > n.left && t.top >= n.top) {
    if (n.bottom && n.bottom < t.top || n.right < t.left) return n;
    let r = Le({}, n);
    return r.bottom = t.top, r;
  }
  return null;
}, pv = ({ stone: t, position: e, spotsList: n, optimalSpot: r }) => {
  let s = Le({}, r);
  return s.left = e.left + t.width, dv(s) || uv({ usedSpot: s, spotsList: n }) ? null : s;
}, mv = ({ spots: t, position: e, optimalSpot: n, stone: r }) => t.map((s, i, o) => {
  if (s === n) return pv({ stone: r, position: e, optimalSpot: n, spotsList: o });
  let l = fv({ position: e, spot: s, stone: r });
  return l || hv({ position: e, stone: r, spot: s }) || s;
});
function gv(t, e) {
  for (let n = 0, r = e.length; n < r; n++) {
    let s = e[n];
    if (t(s)) return s;
  }
  return null;
}
var bv = (t, e) => e.filter(t), wv = (t, e) => [...e].sort(t), xv = (t, e) => t.top === e.top ? t.left < e.left ? -1 : 1 : t.top < e.top ? -1 : 1, yv = (t) => wv(xv, t), vv = ({ availableSpots: t, optimalSpot: e, containerSize: n, stone: r }) => {
  let s = { right: n, top: e.top + r.height, left: e.left };
  e.bottom && (s.bottom = e.bottom);
  for (let i = 0, o = t.length; i < o; i += 1) {
    let l = t[i];
    if (s.left < l.left && s.top < l.top) {
      s.right = l.left;
      break;
    }
  }
  return s;
}, kv = (t, e) => {
  let n = t.right - t.left >= e.width;
  if (!t.bottom) return n;
  let r = t.bottom - t.top >= e.height;
  return n && r;
}, Cv = ({ availableSpots: t, stone: e }) => gv((n) => kv(n, e), t);
function Sv({ stone: t, availableSpots: e, containerSize: n }) {
  let r = Cv({ availableSpots: e, stone: t }), s = { left: r.left, top: r.top }, i = vv({ optimalSpot: r, availableSpots: e.filter((a) => a !== r), stone: t, containerSize: n }), o = [...e, i], l = mv({ spots: o, position: s, optimalSpot: r, stone: t });
  return o = [...bv(Boolean, l)], o = yv(o), { position: s, availableSpots: o };
}
var Nv = ({ stones: t, containerSize: e }) => {
  let n = { positions: [], containerHeight: 0, availableSpots: [] };
  if (!t.length) return n;
  let r = 0, s = [], i = [{ top: 0, left: 0, right: e }];
  for (let o of t) {
    let l = Sv({ availableSpots: i, stone: o, containerSize: e }), a = l.position.top + o.height;
    r < a && (r = a), s.push(l.position), i = l.availableSpots;
  }
  return { availableSpots: i, positions: s, containerHeight: r };
}, Iv = (t) => t.reduce((e, n) => {
  if (!n) return e;
  let r = n.getBoundingClientRect();
  return e.push({ width: r.width, height: r.height }), e;
}, []), _v = ({ boxesRefs: t, wrapperRef: e, children: n, windowWidth: r, wrapperWidth: s }) => {
  let [{ positions: i, containerHeight: o, stones: l, availableSpots: a }, c] = K({ positions: [], containerHeight: null, stones: [], availableSpots: [] });
  return ue(() => {
    var u, d;
    let h = Iv(t.current), p = (d = (u = e.current) == null ? void 0 : u.offsetWidth) != null ? d : 0;
    if (p === null) return;
    let m = Nv({ stones: h, containerSize: p });
    c(jr(Le({}, m), { stones: h }));
  }, [n, t, e, r, s]), { positions: i, containerHeight: o, stones: l, availableSpots: a };
}, Av = (t) => ({ fade: `${t}ms opacity ease`, fadeMove: `
    ${t}ms opacity ease,
    ${t}ms top ease,
    ${t}ms left ease
  `, move: `
    ${t}ms top ease,
    ${t}ms left ease
  ` }), Tv = ({ transition: t, transitionDuration: e }) => t ? { transition: Av(e)[t] } : null, Ev = (t) => t ? jr(Le({}, t), { opacity: 1 }) : { opacity: 0, top: 0, left: 0 }, Dv = ({ style: t, position: e, transition: n, transitionDuration: r }) => Le(Le(jr(Le({}, t), { position: "absolute" }), Ev(e)), Tv({ transition: n, transitionDuration: r }));
function Rv(t, e, n) {
  let r;
  return function(...s) {
    r && clearTimeout(r), r = setTimeout(function() {
      r = null, t.apply(null, s);
    }, e);
  };
}
var Lv = () => {
  let [t, e] = K(), n = ge(Rv(e, 300));
  return ue(() => {
    let r = () => {
      n.current(window.innerWidth);
    };
    return window.addEventListener("resize", r), () => {
      window.removeEventListener("resize", r);
    };
  }, []), t;
}, Mv = (t) => {
  let [e, n] = K(null);
  return ue(() => {
    let r = new ResizeObserver((s) => {
      for (let i of s) n(i.contentRect.width);
    });
    return t.current && r.observe(t.current), () => {
      r.disconnect();
    };
  }, [t]), e;
}, Ov = (t) => {
  var e = t, { children: n, style: r, transition: s = !1, transitionDuration: i = 500, transitionStep: o = 50 } = e, l = cv(e, ["children", "style", "transition", "transitionDuration", "transitionStep"]);
  let a = ge([]), c = ge(null), u = Lv(), d = Mv(c), { positions: h, containerHeight: p } = _v({ boxesRefs: a, wrapperRef: c, children: n, windowWidth: u, wrapperWidth: d }), m = Le({ minHeight: p ?? 0, position: "relative" }, r);
  return f("div", jr(Le({ ref: c, style: m }, l), { children: ql.map(n, (g, b) => {
    if (typeof g != "object" || !g || !("type" in g)) return g;
    let x = { style: Dv({ style: g.props.style, position: h[b], transition: s, transitionDuration: i }), ref: (v) => a.current[b] = v };
    return Jf(g, Le(Le({}, g.props), x));
  }) }));
};
const Fv = { sm: 340, md: 480, lg: 640 }, cl = F(
  function({ children: e, widgetWidth: n = "sm" }, r) {
    const s = Fv[n], [i, o] = K(), l = ql.toArray(e), a = ge(null);
    return ue(() => {
      const c = () => {
        const u = a.current?.offsetWidth;
        u && o(Math.floor(u / s) || 1);
      };
      return c(), window.addEventListener("resize", c), () => {
        window.removeEventListener("resize", c);
      };
    }, [o, s]), /* @__PURE__ */ f("div", { ref: r, className: "text-f1-foreground", children: /* @__PURE__ */ f("div", { ref: a, children: i === 1 ? /* @__PURE__ */ f("div", { className: "flex flex-col gap-4 *:shadow", children: e }) : i && i > 1 && /* @__PURE__ */ f("div", { className: "relative -mr-4", children: /* @__PURE__ */ f(Ov, { children: l.map((c, u) => /* @__PURE__ */ f(
      "div",
      {
        style: {
          width: `${Math.floor(1 / i * 1e4) / 100 - 0.05}%`
        },
        className: "pb-[0.01px] pr-4 *:mb-4 *:shadow",
        children: c
      },
      u
    )) }, i) }) }) });
  }
), zv = ["sm", "lg", "md", "md", "lg", "sm", "lg", "lg", "sm", "sm", "md", "md"], vk = Oe(cl, () => /* @__PURE__ */ f(kl, { children: /* @__PURE__ */ f(cl, { children: zv.map((t, e) => /* @__PURE__ */ f(Un.Skeleton, { height: t }, e)) }) })), dl = ({ children: t }) => /* @__PURE__ */ f("div", { className: "flex min-h-40 flex-row items-stretch gap-4 text-f1-foreground @container [&>div]:min-w-[calc(100vw-64px)] @2xl:[&>div]:min-w-[calc(50vw-48px)]", children: t }), kk = Oe(
  F(function({ children: e }, n) {
    return /* @__PURE__ */ f(yn, { ref: n, showBar: !1, children: /* @__PURE__ */ f(dl, { children: e }) });
  }),
  () => /* @__PURE__ */ f(kl, { orientation: "horizontal", children: /* @__PURE__ */ w(dl, { children: [
    /* @__PURE__ */ f(Un.Skeleton, {}),
    /* @__PURE__ */ f(Un.Skeleton, {}),
    /* @__PURE__ */ f(Un.Skeleton, {})
  ] }) })
);
function Pv({
  title: t,
  description: e,
  emoji: n,
  actions: r
}) {
  if ((r?.length ?? 0) > 2)
    throw Error(
      "You can only provide up to two actions for the WidgetEmptyState"
    );
  return /* @__PURE__ */ f(
    kf,
    {
      title: t,
      description: e,
      ...n ? { emoji: n } : { variant: "warning" },
      actions: r
    }
  );
}
const Ck = Ge(
  Y("WidgetEmptyState", Pv)
), Dd = F(
  ({ title: t, children: e }, n) => (Tu(
    t,
    { disallowEmpty: !0 },
    { componentName: "WidgetSection" }
  ), /* @__PURE__ */ w("div", { ref: n, className: "flex flex-col gap-2", children: [
    /* @__PURE__ */ f("p", { className: "text-base font-medium text-f1-foreground-secondary", children: t }),
    e
  ] }))
);
Dd.displayName = "WidgetSection";
const Sk = Ge(
  Y("WidgetSection", Dd)
), Nk = ({
  identifier: t,
  allowedRoutes: e,
  disallowedRoutes: n,
  children: r
}) => {
  const s = Eu(), i = window.location.pathname, o = we(() => e?.length ? e.includes(i) : n?.length ? !n.includes(i) : !0, [i, e, n]), l = we(() => {
    let a = `The component ${t} is not allowed to be rendered in the current route.`;
    return e && e.length > 0 && (a += ` Allowed routes: ${e.join(", ")}`), n && n.length > 0 && (a += ` Disallowed routes: ${n.join(", ")}`), a;
  }, [t, e, n]);
  return o ? r : (s && console.error(l), null);
}, Ik = Ge(
  Cr(
    {
      name: "ScrollArea",
      type: "layout"
    },
    yn
  )
);
export {
  Xv as ActivityItemList,
  nw as ActivityItemListSkeleton,
  qk as AiPromotionChat,
  Yk as AiPromotionChatProvider,
  Jk as ApplicationFrame,
  GC as AreaChartWidget,
  ck as AutoGrid,
  Tk as Badge,
  KC as BarChartWidget,
  tw as BaseActivityItemList,
  AC as BaseBanner,
  lw as BaseCelebration,
  bw as BaseCommunityPost,
  Xk as BaseTabs,
  Zk as BreadcrumbSelect,
  Qk as Breadcrumbs,
  _s as CalendarEvent,
  uk as CalendarEventList,
  TC as CardSelectableContainer,
  eC as Carousel,
  wk as CategoryBarSection,
  Zv as Celebration,
  aw as CelebrationSkeleton,
  dk as ChartWidgetEmptyState,
  xk as ClockInControls,
  ek as CommunityPost,
  ww as CommunityPostSkeleton,
  tC as CompanySelector,
  pl as Counter,
  vk as Dashboard,
  nC as DaytimePage,
  Pw as DetailsItem,
  tk as DetailsItemsList,
  EC as Dialog,
  rC as Dropdown,
  Jv as EntitySelect,
  DC as F0ActionBar,
  Ra as F0AiBanner,
  gl as F0AvatarModule,
  qv as F0Callout,
  sC as F0TableOfContent,
  Yv as F0VersionHistory,
  Mk as F1SearchBox,
  RC as FILE_TYPES,
  LC as FileItem,
  Qv as HighlightBanner,
  hk as IndicatorsList,
  Ok as Input,
  iC as Item,
  oC as ItemSectionHeader,
  qC as LineChartWidget,
  lC as Menu,
  aC as MobileDropdown,
  ok as NotesTextEditor,
  ik as NotesTextEditorSkeleton,
  Fk as NumberInput,
  cC as OmniButton,
  rk as OneApprovalHistory,
  zk as OneCalendar,
  Pk as OneCalendarInternal,
  dC as OneDataCollection,
  Bk as OneDateNavigator,
  kf as OneEmptyState,
  $k as OnePagination,
  nk as OnePersonListItem,
  Nk as OneRestrictComponent,
  uC as Page,
  fC as PageHeader,
  YC as PieChartWidget,
  JC as PrivateBox,
  HC as RadarChart,
  XC as RadialProgressWidget,
  MC as ResourceHeader,
  Ea as RichTextDisplay,
  OC as RichTextEditor,
  Ik as ScrollArea,
  hC as SearchBar,
  FC as SectionHeader,
  fs as Select,
  Ek as Shortcut,
  pC as Sidebar,
  mC as SidebarFooter,
  gC as SidebarHeader,
  Cl as Spinner,
  ak as Split,
  lk as Stack,
  ZC as SummariesWidget,
  Hk as Switch,
  bC as Tabs,
  wC as TabsSkeleton,
  yk as TasksList,
  zC as Textarea,
  nc as ToggleGroup,
  rc as ToggleGroupItem,
  kr as Tooltip,
  bk as TwoColumnsList,
  QC as VerticalBarChartWidget,
  Dk as WeekStartDay,
  yw as Weekdays,
  Un as Widget,
  pk as WidgetAvatarsListItem,
  Ck as WidgetEmptyState,
  fk as WidgetHighlightButton,
  mk as WidgetInboxList,
  ll as WidgetInboxListItem,
  Sk as WidgetSection,
  gk as WidgetSimpleList,
  Ks as WidgetSimpleListItem,
  kk as WidgetStrip,
  jC as _RadarChart,
  PC as actionBarStatuses,
  xC as downloadAsCSV,
  yC as generateCSVContent,
  jk as getGranularityDefinition,
  Vk as getGranularityDefinitions,
  Uk as getGranularitySimpleDefinition,
  Wk as granularityDefinitions,
  Rk as modules,
  UC as predefinedPresets,
  Gk as rangeSeparator,
  BC as selectSizes,
  vC as useAiPromotionChat,
  kC as useDataCollectionData,
  CC as useDataCollectionSource,
  SC as useExportAction,
  NC as useInfiniteScrollPagination,
  IC as useSidebar
};
