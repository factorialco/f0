import { SectionProps } from './types';
export declare const Section: ({ id, title, description, lockedNote, questions, locked, hideQuestions, }: SectionProps & {
    hideQuestions?: boolean;
}) => import("react").JSX.Element;
