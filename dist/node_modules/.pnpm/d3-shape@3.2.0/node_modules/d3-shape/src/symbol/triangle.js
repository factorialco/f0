import { sqrt as l } from "../math.js";
const r = l(3), i = {
  draw(s, a) {
    const o = -l(a / (r * 3));
    s.moveTo(0, o * 2), s.lineTo(-r * o, -o), s.lineTo(r * o, -o), s.closePath();
  }
};
export {
  i as default
};
