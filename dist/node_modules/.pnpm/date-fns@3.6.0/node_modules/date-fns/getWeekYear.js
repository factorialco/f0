import { constructFrom as f } from "./constructFrom.js";
import { startOfWeek as l } from "./startOfWeek.js";
import { toDate as u } from "./toDate.js";
import { getDefaultOptions as k } from "./_lib/defaultOptions.js";
function O(r, t) {
  const s = u(r), e = s.getFullYear(), i = k(), n = t?.firstWeekContainsDate ?? t?.locale?.options?.firstWeekContainsDate ?? i.firstWeekContainsDate ?? i.locale?.options?.firstWeekContainsDate ?? 1, o = f(r, 0);
  o.setFullYear(e + 1, 0, n), o.setHours(0, 0, 0, 0);
  const c = l(o, t), a = f(r, 0);
  a.setFullYear(e, 0, n), a.setHours(0, 0, 0, 0);
  const m = l(a, t);
  return s.getTime() >= c.getTime() ? e + 1 : s.getTime() >= m.getTime() ? e : e - 1;
}
export {
  O as default,
  O as getWeekYear
};
