import { getDefaultOptions as S } from "./_lib/defaultOptions.js";
import { getRoundingMethod as z } from "./_lib/getRoundingMethod.js";
import { getTimezoneOffsetInMilliseconds as p } from "./_lib/getTimezoneOffsetInMilliseconds.js";
import { compareAsc as N } from "./compareAsc.js";
import { minutesInDay as x, minutesInMonth as D, minutesInYear as M, millisecondsInMinute as y } from "./constants.js";
import { toDate as f } from "./toDate.js";
import { enUS as R } from "./locale/en-US.js";
function E(l, u, i) {
  const I = S(), n = i?.locale ?? I.locale ?? R, d = N(l, u);
  if (isNaN(d))
    throw new RangeError("Invalid time value");
  const o = Object.assign({}, i, {
    addSuffix: i?.addSuffix,
    comparison: d
  });
  let m, c;
  d > 0 ? (m = f(u), c = f(l)) : (m = f(l), c = f(u));
  const s = z(i?.roundingMethod ?? "round"), h = c.getTime() - m.getTime(), r = h / y, O = p(c) - p(m), a = (h - O) / y, g = i?.unit;
  let e;
  if (g ? e = g : r < 1 ? e = "second" : r < 60 ? e = "minute" : r < x ? e = "hour" : a < D ? e = "day" : a < M ? e = "month" : e = "year", e === "second") {
    const t = s(h / 1e3);
    return n.formatDistance("xSeconds", t, o);
  } else if (e === "minute") {
    const t = s(r);
    return n.formatDistance("xMinutes", t, o);
  } else if (e === "hour") {
    const t = s(r / 60);
    return n.formatDistance("xHours", t, o);
  } else if (e === "day") {
    const t = s(a / x);
    return n.formatDistance("xDays", t, o);
  } else if (e === "month") {
    const t = s(a / D);
    return t === 12 && g !== "month" ? n.formatDistance("xYears", 1, o) : n.formatDistance("xMonths", t, o);
  } else {
    const t = s(a / M);
    return n.formatDistance("xYears", t, o);
  }
}
export {
  E as default,
  E as formatDistanceStrict
};
