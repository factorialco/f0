import { numericPatterns as s } from "../constants.js";
import { Parser as o } from "../Parser.js";
import { parseNDigits as a, parseNumericPattern as i } from "../utils.js";
class c extends o {
  priority = 70;
  parse(r, e, t) {
    switch (e) {
      case "H":
        return i(s.hour23h, r);
      case "Ho":
        return t.ordinalNumber(r, { unit: "hour" });
      default:
        return a(e.length, r);
    }
  }
  validate(r, e) {
    return e >= 0 && e <= 23;
  }
  set(r, e, t) {
    return r.setHours(t, 0, 0, 0), r;
  }
  incompatibleTokens = ["a", "b", "h", "K", "k", "t", "T"];
}
export {
  c as Hour0to23Parser
};
