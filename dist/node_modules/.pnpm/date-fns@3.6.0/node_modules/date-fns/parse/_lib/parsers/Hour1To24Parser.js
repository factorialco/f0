import { numericPatterns as o } from "../constants.js";
import { Parser as i } from "../Parser.js";
import { parseNDigits as a, parseNumericPattern as n } from "../utils.js";
class h extends i {
  priority = 70;
  parse(r, e, t) {
    switch (e) {
      case "k":
        return n(o.hour24h, r);
      case "ko":
        return t.ordinalNumber(r, { unit: "hour" });
      default:
        return a(e.length, r);
    }
  }
  validate(r, e) {
    return e >= 1 && e <= 24;
  }
  set(r, e, t) {
    const s = t <= 24 ? t % 24 : t;
    return r.setHours(s, 0, 0, 0), r;
  }
  incompatibleTokens = ["a", "b", "h", "H", "K", "t", "T"];
}
export {
  h as Hour1To24Parser
};
