import { Parser as G } from "../Parser.js";
class i extends G {
  priority = 140;
  parse(r, s, e) {
    switch (s) {
      // AD, BC
      case "G":
      case "GG":
      case "GGG":
        return e.era(r, { width: "abbreviated" }) || e.era(r, { width: "narrow" });
      // A, B
      case "GGGGG":
        return e.era(r, { width: "narrow" });
      default:
        return e.era(r, { width: "wide" }) || e.era(r, { width: "abbreviated" }) || e.era(r, { width: "narrow" });
    }
  }
  set(r, s, e) {
    return s.era = e, r.setFullYear(e, 0, 1), r.setHours(0, 0, 0, 0), r;
  }
  incompatibleTokens = ["R", "u", "t", "T"];
}
export {
  i as EraParser
};
