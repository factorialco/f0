import { Parser as t } from "../Parser.js";
import { dayPeriodEnumToHours as i } from "../utils.js";
class n extends t {
  priority = 80;
  parse(r, e, o) {
    switch (e) {
      case "b":
      case "bb":
      case "bbb":
        return o.dayPeriod(r, {
          width: "abbreviated",
          context: "formatting"
        }) || o.dayPeriod(r, {
          width: "narrow",
          context: "formatting"
        });
      case "bbbbb":
        return o.dayPeriod(r, {
          width: "narrow",
          context: "formatting"
        });
      default:
        return o.dayPeriod(r, {
          width: "wide",
          context: "formatting"
        }) || o.dayPeriod(r, {
          width: "abbreviated",
          context: "formatting"
        }) || o.dayPeriod(r, {
          width: "narrow",
          context: "formatting"
        });
    }
  }
  set(r, e, o) {
    return r.setHours(i(o), 0, 0, 0), r;
  }
  incompatibleTokens = ["a", "B", "H", "k", "t", "T"];
}
export {
  n as AMPMMidnightParser
};
