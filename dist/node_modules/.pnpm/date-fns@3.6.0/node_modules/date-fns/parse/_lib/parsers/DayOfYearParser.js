import { numericPatterns as t } from "../constants.js";
import { Parser as a } from "../Parser.js";
import { parseNDigits as i, parseNumericPattern as n, isLeapYearIndex as o } from "../utils.js";
class l extends a {
  priority = 90;
  subpriority = 1;
  parse(r, e, s) {
    switch (e) {
      case "D":
      case "DD":
        return n(t.dayOfYear, r);
      case "Do":
        return s.ordinalNumber(r, { unit: "date" });
      default:
        return i(e.length, r);
    }
  }
  validate(r, e) {
    const s = r.getFullYear();
    return o(s) ? e >= 1 && e <= 366 : e >= 1 && e <= 365;
  }
  set(r, e, s) {
    return r.setMonth(0, s), r.setHours(0, 0, 0, 0), r;
  }
  incompatibleTokens = [
    "Y",
    "R",
    "q",
    "Q",
    "M",
    "L",
    "w",
    "I",
    "d",
    "E",
    "i",
    "e",
    "c",
    "t",
    "T"
  ];
}
export {
  l as DayOfYearParser
};
