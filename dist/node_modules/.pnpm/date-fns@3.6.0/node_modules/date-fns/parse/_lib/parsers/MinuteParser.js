import { numericPatterns as s } from "../constants.js";
import { Parser as i } from "../Parser.js";
import { parseNDigits as n, parseNumericPattern as m } from "../utils.js";
class c extends i {
  priority = 60;
  parse(r, e, t) {
    switch (e) {
      case "m":
        return m(s.minute, r);
      case "mo":
        return t.ordinalNumber(r, { unit: "minute" });
      default:
        return n(e.length, r);
    }
  }
  validate(r, e) {
    return e >= 0 && e <= 59;
  }
  set(r, e, t) {
    return r.setMinutes(t, 0, 0), r;
  }
  incompatibleTokens = ["t", "T"];
}
export {
  c as MinuteParser
};
