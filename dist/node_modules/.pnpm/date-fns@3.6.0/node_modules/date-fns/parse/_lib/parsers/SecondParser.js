import { numericPatterns as t } from "../constants.js";
import { Parser as n } from "../Parser.js";
import { parseNDigits as o, parseNumericPattern as a } from "../utils.js";
class u extends n {
  priority = 50;
  parse(r, e, s) {
    switch (e) {
      case "s":
        return a(t.second, r);
      case "so":
        return s.ordinalNumber(r, { unit: "second" });
      default:
        return o(e.length, r);
    }
  }
  validate(r, e) {
    return e >= 0 && e <= 59;
  }
  set(r, e, s) {
    return r.setSeconds(s, 0), r;
  }
  incompatibleTokens = ["t", "T"];
}
export {
  u as SecondParser
};
