import { numericPatterns as o } from "../constants.js";
import { Parser as i } from "../Parser.js";
import { parseNDigits as u, parseNumericPattern as n } from "../utils.js";
class f extends i {
  priority = 70;
  parse(r, s, e) {
    switch (s) {
      case "h":
        return n(o.hour12h, r);
      case "ho":
        return e.ordinalNumber(r, { unit: "hour" });
      default:
        return u(s.length, r);
    }
  }
  validate(r, s) {
    return s >= 1 && s <= 12;
  }
  set(r, s, e) {
    const t = r.getHours() >= 12;
    return t && e < 12 ? r.setHours(e + 12, 0, 0, 0) : !t && e === 12 ? r.setHours(0, 0, 0, 0) : r.setHours(e, 0, 0, 0), r;
  }
  incompatibleTokens = ["H", "K", "k", "t", "T"];
}
export {
  f as Hour1to12Parser
};
