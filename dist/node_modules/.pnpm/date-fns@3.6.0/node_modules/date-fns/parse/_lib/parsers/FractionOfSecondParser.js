import { Parser as a } from "../Parser.js";
import { mapValue as o, parseNDigits as i } from "../utils.js";
class c extends a {
  priority = 30;
  parse(r, e) {
    const t = (s) => Math.trunc(s * Math.pow(10, -e.length + 3));
    return o(i(e.length, r), t);
  }
  set(r, e, t) {
    return r.setMilliseconds(t), r;
  }
  incompatibleTokens = ["t", "T"];
}
export {
  c as FractionOfSecondParser
};
