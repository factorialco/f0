import { numericPatterns as t } from "../constants.js";
import { Parser as o } from "../Parser.js";
import { parseNDigits as i, parseNumericPattern as u } from "../utils.js";
class f extends o {
  priority = 70;
  parse(r, s, e) {
    switch (s) {
      case "K":
        return u(t.hour11h, r);
      case "Ko":
        return e.ordinalNumber(r, { unit: "hour" });
      default:
        return i(s.length, r);
    }
  }
  validate(r, s) {
    return s >= 0 && s <= 11;
  }
  set(r, s, e) {
    return r.getHours() >= 12 && e < 12 ? r.setHours(e + 12, 0, 0, 0) : r.setHours(e, 0, 0, 0), r;
  }
  incompatibleTokens = ["h", "H", "k", "t", "T"];
}
export {
  f as Hour0To11Parser
};
