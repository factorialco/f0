import { setWeek as a } from "../../../setWeek.js";
import { startOfWeek as i } from "../../../startOfWeek.js";
import { numericPatterns as m } from "../constants.js";
import { Parser as o } from "../Parser.js";
import { parseNDigits as n, parseNumericPattern as p } from "../utils.js";
class d extends o {
  priority = 100;
  parse(r, e, t) {
    switch (e) {
      case "w":
        return p(m.week, r);
      case "wo":
        return t.ordinalNumber(r, { unit: "week" });
      default:
        return n(e.length, r);
    }
  }
  validate(r, e) {
    return e >= 1 && e <= 53;
  }
  set(r, e, t, s) {
    return i(a(r, t, s), s);
  }
  incompatibleTokens = [
    "y",
    "R",
    "u",
    "q",
    "Q",
    "M",
    "L",
    "I",
    "d",
    "D",
    "i",
    "t",
    "T"
  ];
}
export {
  d as LocalWeekParser
};
