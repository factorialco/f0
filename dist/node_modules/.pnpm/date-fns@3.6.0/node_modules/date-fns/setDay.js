import { addDays as d } from "./addDays.js";
import { toDate as l } from "./toDate.js";
import { getDefaultOptions as m } from "./_lib/defaultOptions.js";
function k(s, t, n) {
  const o = m(), c = n?.weekStartsOn ?? n?.locale?.options?.weekStartsOn ?? o.weekStartsOn ?? o.locale?.options?.weekStartsOn ?? 0, r = l(s), a = r.getDay(), i = (t % 7 + 7) % 7, e = 7 - c, f = t < 0 || t > 6 ? t - (a + e) % 7 : (i + e) % 7 - (a + e) % 7;
  return d(r, f);
}
export {
  k as default,
  k as setDay
};
