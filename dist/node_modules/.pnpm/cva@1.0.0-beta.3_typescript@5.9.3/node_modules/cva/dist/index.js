import { clsx as j } from "../../../../clsx@2.1.1/node_modules/clsx/dist/clsx.js";
const C = (i) => typeof i == "boolean" ? `${i}` : i === 0 ? "0" : i, A = (i) => {
  const v = function() {
    for (var e = arguments.length, t = new Array(e), n = 0; n < e; n++)
      t[n] = arguments[n];
    return j(t);
  };
  return {
    compose: function() {
      for (var e = arguments.length, t = new Array(e), n = 0; n < e; n++)
        t[n] = arguments[n];
      return (a) => {
        const s = Object.fromEntries(Object.entries(a || {}).filter((d) => {
          let [c] = d;
          return ![
            "class",
            "className"
          ].includes(c);
        }));
        return v(t.map((d) => d(s)), a?.class, a?.className);
      };
    },
    cva: (e) => (t) => {
      var n;
      if (e?.variants == null) return v(e?.base, t?.class, t?.className);
      const { variants: a, defaultVariants: s } = e, d = Object.keys(a).map((l) => {
        const o = t?.[l], u = s?.[l], r = C(o) || C(u);
        return a[l][r];
      }), c = {
        ...s,
        // remove `undefined` props
        ...t && Object.entries(t).reduce((l, o) => {
          let [u, r] = o;
          return typeof r > "u" ? l : {
            ...l,
            [u]: r
          };
        }, {})
      }, N = e == null || (n = e.compoundVariants) === null || n === void 0 ? void 0 : n.reduce((l, o) => {
        let { class: u, className: r, ...f } = o;
        return Object.entries(f).every((y) => {
          let [V, m] = y;
          const b = c[V];
          return Array.isArray(m) ? m.includes(b) : b === m;
        }) ? [
          ...l,
          u,
          r
        ] : l;
      }, []);
      return v(e?.base, d, N, t?.class, t?.className);
    },
    cx: v
  };
}, { cva: P } = A();
export {
  P as cva,
  A as defineConfig
};
