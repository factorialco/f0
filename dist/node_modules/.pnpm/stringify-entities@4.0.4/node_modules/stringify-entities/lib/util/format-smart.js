import { toHexadecimal as f } from "./to-hexadecimal.js";
import { toDecimal as i } from "./to-decimal.js";
import { toNamed as c } from "./to-named.js";
function u(m, l, e) {
  let r = f(m, l, e.omitOptionalSemicolons), t;
  if ((e.useNamedReferences || e.useShortestReferences) && (t = c(
    m,
    l,
    e.omitOptionalSemicolons,
    e.attribute
  )), (e.useShortestReferences || !t) && e.useShortestReferences) {
    const a = i(m, l, e.omitOptionalSemicolons);
    a.length < r.length && (r = a);
  }
  return t && (!e.useShortestReferences || t.length < r.length) ? t : r;
}
export {
  u as formatSmart
};
