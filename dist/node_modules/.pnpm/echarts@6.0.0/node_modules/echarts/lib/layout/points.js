import { map as T } from "../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import k from "../chart/helper/createRenderPlanner.js";
import { isDimensionStacked as y } from "../data/helper/dataStackHelper.js";
import { createFloat32Array as P } from "../util/vendor.js";
function O(x, D) {
  return {
    seriesType: x,
    plan: k(),
    reset: function(i) {
      var e = i.getData(), o = i.coordinateSystem, I = i.pipelineContext, s = D || I.large;
      if (o) {
        var t = T(o.dimensions, function(r) {
          return e.mapDimension(r);
        }).slice(0, 2), m = t.length, v = e.getCalculationInfo("stackResultDimension");
        y(e, t[0]) && (t[0] = v), y(e, t[1]) && (t[1] = v);
        var u = e.getStore(), c = e.getDimensionIndex(t[0]), C = e.getDimensionIndex(t[1]);
        return m && {
          progress: function(r, f) {
            for (var L = r.end - r.start, d = s && P(L * m), l = [], p = [], n = r.start, g = 0; n < r.end; n++) {
              var a = void 0;
              if (m === 1) {
                var S = u.get(c, n);
                a = o.dataToPoint(S, null, p);
              } else
                l[0] = u.get(c, n), l[1] = u.get(C, n), a = o.dataToPoint(l, null, p);
              s ? (d[g++] = a[0], d[g++] = a[1]) : f.setItemLayout(n, a.slice());
            }
            s && f.setLayout("points", d);
          }
        };
      }
    }
  };
}
export {
  O as default
};
