import { bind as h, map as d } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function s(a, t) {
  return t = t || [0, 0], d(["x", "y"], function(e, i) {
    var r = this.getAxis(e), o = t[i], n = a[i] / 2;
    return r.type === "category" ? r.getBandWidth() : Math.abs(r.dataToCoord(o - n) - r.dataToCoord(o + n));
  }, this);
}
function c(a) {
  var t = a.master.getRect();
  return {
    coordSys: {
      // The name exposed to user is always 'cartesian2d' but not 'grid'.
      type: "cartesian2d",
      x: t.x,
      y: t.y,
      width: t.width,
      height: t.height
    },
    api: {
      coord: function(e) {
        return a.dataToPoint(e);
      },
      size: h(s, a)
    }
  };
}
export {
  c as default
};
