import { __extends as T } from "../../../../../../tslib@2.3.0/node_modules/tslib/tslib.es6.js";
import { getBoundingRect as A } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/contain/text.js";
import s from "../Axis.js";
import { makeInner as y } from "../../util/model.js";
var C = y(), h = (
  /** @class */
  (function(v) {
    T(r, v);
    function r(t, a) {
      return v.call(this, "angle", t, a || [0, 360]) || this;
    }
    return r.prototype.pointToData = function(t, a) {
      return this.polar.pointToData(t, a)[this.dim === "radius" ? 0 : 1];
    }, r.prototype.calculateCategoryInterval = function() {
      var t = this, a = t.getLabelModel(), c = t.scale, i = c.getExtent(), p = c.count();
      if (i[1] - i[0] < 1)
        return 0;
      var e = i[0], m = t.dataToCoord(e + 1) - t.dataToCoord(e), f = Math.abs(m), g = A(e == null ? "" : e + "", a.getFont(), "center", "top"), x = Math.max(g.height, 7), u = x / f;
      isNaN(u) && (u = 1 / 0);
      var o = Math.max(0, Math.floor(u)), n = C(t.model), l = n.lastAutoInterval, d = n.lastTickCount;
      return l != null && d != null && Math.abs(l - o) <= 1 && Math.abs(d - p) <= 1 && l > o ? o = l : (n.lastTickCount = p, n.lastAutoInterval = o), o;
    }, r;
  })(s)
);
h.prototype.dataToAngle = s.prototype.dataToCoord;
h.prototype.angleToData = s.prototype.coordToData;
export {
  h as default
};
