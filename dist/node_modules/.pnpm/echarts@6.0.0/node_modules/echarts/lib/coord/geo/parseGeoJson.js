import { map as d, each as p, filter as h } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { GeoJSONLineStringGeometry as f, GeoJSONPolygonGeometry as g, GeoJSONRegion as m } from "./Region.js";
function S(r) {
  if (!r.UTF8Encoding)
    return r;
  var s = r, e = s.UTF8Scale;
  e == null && (e = 1024);
  var n = s.features;
  return p(n, function(c) {
    var t = c.geometry, a = t.encodeOffsets, o = t.coordinates;
    if (a)
      switch (t.type) {
        case "LineString":
          t.coordinates = l(o, a, e);
          break;
        case "Polygon":
          u(o, a, e);
          break;
        case "MultiLineString":
          u(o, a, e);
          break;
        case "MultiPolygon":
          p(o, function(i, v) {
            return u(i, a[v], e);
          });
      }
  }), s.UTF8Encoding = !1, s;
}
function u(r, s, e) {
  for (var n = 0; n < r.length; n++)
    r[n] = l(r[n], s[n], e);
}
function l(r, s, e) {
  for (var n = [], c = s[0], t = s[1], a = 0; a < r.length; a += 2) {
    var o = r.charCodeAt(a) - 64, i = r.charCodeAt(a + 1) - 64;
    o = o >> 1 ^ -(o & 1), i = i >> 1 ^ -(i & 1), o += c, i += t, c = o, t = i, n.push([o / e, i / e]);
  }
  return n;
}
function k(r, s) {
  return r = S(r), d(h(r.features, function(e) {
    return e.geometry && e.properties && e.geometry.coordinates.length > 0;
  }), function(e) {
    var n = e.properties, c = e.geometry, t = [];
    switch (c.type) {
      case "Polygon":
        var a = c.coordinates;
        t.push(new g(a[0], a.slice(1)));
        break;
      case "MultiPolygon":
        p(c.coordinates, function(i) {
          i[0] && t.push(new g(i[0], i.slice(1)));
        });
        break;
      case "LineString":
        t.push(new f([c.coordinates]));
        break;
      case "MultiLineString":
        t.push(new f(c.coordinates));
    }
    var o = new m(n[s || "name"], t, n.cp);
    return o.properties = n, o;
  });
}
export {
  k as default
};
