import { bind as a } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function n(r, t) {
  var i = this.getAxis(), o = t instanceof Array ? t[0] : t, e = (r instanceof Array ? r[0] : r) / 2;
  return i.type === "category" ? i.getBandWidth() : Math.abs(i.dataToCoord(o - e) - i.dataToCoord(o + e));
}
function h(r) {
  var t = r.getRect();
  return {
    coordSys: {
      type: "singleAxis",
      x: t.x,
      y: t.y,
      width: t.width,
      height: t.height
    },
    api: {
      coord: function(i) {
        return r.dataToPoint(i);
      },
      size: a(n, r)
    }
  };
}
export {
  h as default
};
