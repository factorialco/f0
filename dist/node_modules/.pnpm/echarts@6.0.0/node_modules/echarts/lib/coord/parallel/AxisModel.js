import { __extends as l } from "../../../../../../tslib@2.3.0/node_modules/tslib/tslib.es6.js";
import { clone as c, mixin as f } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import s from "../../model/Component.js";
import p from "../../model/mixin/makeStyleMapper.js";
import { asc as m } from "../../util/number.js";
import { AxisModelCommonMixin as v } from "../axisModelCommonMixin.js";
var h = (
  /** @class */
  (function(n) {
    l(r, n);
    function r() {
      var t = n !== null && n.apply(this, arguments) || this;
      return t.type = r.type, t.activeIntervals = [], t;
    }
    return r.prototype.getAreaSelectStyle = function() {
      return p([
        ["fill", "color"],
        ["lineWidth", "borderWidth"],
        ["stroke", "borderColor"],
        ["width", "width"],
        ["opacity", "opacity"]
        // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
        // So do not transfer decal directly.
      ])(this.getModel("areaSelectStyle"));
    }, r.prototype.setActiveIntervals = function(t) {
      var e = this.activeIntervals = c(t);
      if (e)
        for (var i = e.length - 1; i >= 0; i--)
          m(e[i]);
    }, r.prototype.getActiveState = function(t) {
      var e = this.activeIntervals;
      if (!e.length)
        return "normal";
      if (t == null || isNaN(+t))
        return "inactive";
      if (e.length === 1) {
        var i = e[0];
        if (i[0] <= t && t <= i[1])
          return "active";
      } else
        for (var o = 0, a = e.length; o < a; o++)
          if (e[o][0] <= t && t <= e[o][1])
            return "active";
      return "inactive";
    }, r;
  })(s)
);
f(h, v);
export {
  h as default
};
