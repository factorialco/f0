import { isArray as I, map as P } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { queryDataIndex as T } from "../../util/model.js";
function c(s, u) {
  var o = [], d = s.seriesIndex, n;
  if (d == null || !(n = u.getSeriesByIndex(d)))
    return {
      point: []
    };
  var e = n.getData(), i = T(e, s);
  if (i == null || i < 0 || I(i))
    return {
      point: []
    };
  var a = e.getItemGraphicEl(i), t = n.coordinateSystem;
  if (n.getTooltipPosition)
    o = n.getTooltipPosition(i) || [];
  else if (t && t.dataToPoint)
    if (s.isStacked) {
      var m = t.getBaseAxis(), v = t.getOtherAxis(m), p = v.dim, f = m.dim, g = p === "x" || p === "radius" ? 1 : 0, x = e.mapDimension(f), l = [];
      l[g] = e.get(x, i), l[1 - g] = e.get(e.getCalculationInfo("stackResultDimension"), i), o = t.dataToPoint(l) || [];
    } else
      o = t.dataToPoint(e.getValues(P(t.dimensions, function(y) {
        return e.mapDimension(y);
      }), i)) || [];
  else if (a) {
    var r = a.getBoundingRect().clone();
    r.applyTransform(a.transform), o = [r.x + r.width / 2, r.y + r.height / 2];
  }
  return {
    point: o,
    el: a
  };
}
export {
  c as default
};
