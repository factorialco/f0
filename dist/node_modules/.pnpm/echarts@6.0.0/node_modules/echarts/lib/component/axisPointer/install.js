import o from "../axis/AxisView.js";
import t from "./CartesianAxisPointer.js";
import n from "./AxisPointerModel.js";
import s from "./AxisPointerView.js";
import { isArray as a } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { collect as x } from "./modelHelper.js";
import P from "./axisTrigger.js";
function u(r) {
  o.registerAxisPointerClass("CartesianAxisPointer", t), r.registerComponentModel(n), r.registerComponentView(s), r.registerPreprocessor(function(i) {
    if (i) {
      (!i.axisPointer || i.axisPointer.length === 0) && (i.axisPointer = {});
      var e = i.axisPointer.link;
      e && !a(e) && (i.axisPointer.link = [e]);
    }
  }), r.registerProcessor(r.PRIORITY.PROCESSOR.STATISTIC, function(i, e) {
    i.getComponent("axisPointer").coordSysAxesInfo = x(i, e);
  }), r.registerAction({
    type: "updateAxisPointer",
    event: "updateAxisPointer",
    update: ":updateAxisPointer"
  }, P);
}
export {
  u as install
};
