import { defaults as l } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function o(r) {
  r.registerAction({
    type: "timelineChange",
    event: "timelineChanged",
    update: "prepareAndUpdate"
  }, function(t, n, i) {
    var e = n.getComponent("timeline");
    return e && t.currentIndex != null && (e.setCurrentIndex(t.currentIndex), !e.get("loop", !0) && e.isIndexMax() && e.getPlayState() && (e.setPlayState(!1), i.dispatchAction({
      type: "timelinePlayChange",
      playState: !1,
      from: t.from
    }))), n.resetOption("timeline", {
      replaceMerge: e.get("replaceMerge", !0)
    }), l({
      currentIndex: e.option.currentIndex
    }, t);
  }), r.registerAction({
    type: "timelinePlayChange",
    event: "timelinePlayChanged",
    update: "update"
  }, function(t, n) {
    var i = n.getComponent("timeline");
    i && t.playState != null && i.setPlayState(t.playState);
  });
}
export {
  o as installTimelineAction
};
