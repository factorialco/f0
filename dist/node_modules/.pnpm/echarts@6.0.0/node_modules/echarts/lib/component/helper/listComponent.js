import { normalizeCssArray as g } from "../../util/format.js";
import d from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/graphic/shape/Rect.js";
function y(t, i) {
  var r = g(i.get("padding")), a = i.getItemStyle(["color", "opacity"]);
  a.fill = i.get("backgroundColor");
  var e = new d({
    shape: {
      x: t.x - r[3],
      y: t.y - r[0],
      width: t.width + r[1] + r[3],
      height: t.height + r[0] + r[2],
      r: i.get("borderRadius")
    },
    style: a,
    silent: !0,
    z2: -1
  });
  return e;
}
export {
  y as makeBackground
};
