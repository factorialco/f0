import { isNumber as u } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function d(t) {
  var a = t.findComponents({
    mainType: "legend"
  });
  !a || !a.length || t.eachSeriesByType("graph", function(n) {
    var o = n.getCategoriesData(), l = n.getGraph(), g = l.data, f = o.mapArray(o.getName);
    g.filterSelf(function(i) {
      var m = g.getItemModel(i), e = m.getShallow("category");
      if (e != null) {
        u(e) && (e = f[e]);
        for (var r = 0; r < a.length; r++)
          if (!a[r].isSelected(e))
            return !1;
      }
      return !0;
    });
  });
}
export {
  d as default
};
