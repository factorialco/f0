import { lift as p } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/tool/color.js";
import { extend as s, isString as m } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import c from "../../visual/tokens.js";
function g(o) {
  var n = {};
  function u(e, a, i) {
    if (e.depth === 0)
      return c.color.neutral50;
    for (var t = e; t && t.depth > 1; )
      t = t.parentNode;
    var r = a.getColorFromPalette(t.name || t.dataIndex + "", n);
    return e.depth > 1 && m(r) && (r = p(r, (e.depth - 1) / (i - 1) * 0.5)), r;
  }
  o.eachSeriesByType("sunburst", function(e) {
    var a = e.getData(), i = a.tree;
    i.eachNode(function(t) {
      var r = t.getModel(), l = r.getModel("itemStyle").getItemStyle();
      l.fill || (l.fill = u(t, e, i.root.height));
      var f = a.ensureUniqueItemVisual(t.dataIndex, "style");
      s(f, l);
    });
  });
}
export {
  g as default
};
