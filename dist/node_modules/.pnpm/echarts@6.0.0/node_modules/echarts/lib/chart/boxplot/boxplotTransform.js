import e from "./prepareBoxplotData.js";
import { makePrintable as m, throwError as p } from "../../util/log.js";
import { SOURCE_FORMAT_ARRAY_ROWS as i } from "../../util/types.js";
var c = {
  type: "echarts:boxplot",
  transform: function(r) {
    var o = r.upstream;
    if (o.sourceFormat !== i) {
      var t = "";
      process.env.NODE_ENV !== "production" && (t = m("source data is not applicable for this boxplot transform. Expect number[][].")), p(t);
    }
    var a = e(o.getRawData(), r.config);
    return [{
      dimensions: ["ItemName", "Low", "Q1", "Q2", "Q3", "High"],
      data: a.boxData
    }, {
      data: a.outliers
    }];
  }
};
export {
  c as boxplotTransform
};
