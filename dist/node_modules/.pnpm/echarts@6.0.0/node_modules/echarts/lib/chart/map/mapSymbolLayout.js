import { each as c } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function h(f) {
  var g = {};
  f.eachSeriesByType("map", function(a) {
    var i = a.getMapType();
    if (!(a.getHostGeoModel() || g[i])) {
      var r = {};
      c(a.seriesGroup, function(t) {
        var n = t.coordinateSystem, e = t.originalData;
        t.get("showLegendSymbol") && f.getComponent("legend") && e.each(e.mapDimension("value"), function(v, u) {
          var m = e.getName(u), y = n.getRegion(m);
          if (!(!y || isNaN(v))) {
            var p = r[m] || 0, s = n.dataToPoint(y.getCenter());
            r[m] = p + 1, e.setItemLayout(u, {
              point: s,
              offset: p
            });
          }
        });
      });
      var o = a.getData();
      o.each(function(t) {
        var n = o.getName(t), e = o.getItemLayout(t) || {};
        e.showLabel = !r[n], o.setItemLayout(t, e);
      }), g[i] = !0;
    }
  });
}
export {
  h as default
};
