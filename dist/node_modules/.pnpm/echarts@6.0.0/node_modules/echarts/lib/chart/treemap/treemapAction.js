import { retrieveTargetInfo as f, aboveViewRoot as T } from "../helper/treeHelper.js";
import { noop as c } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
var a = ["treemapZoomToNode", "treemapRender", "treemapMove"];
function R(n) {
  for (var e = 0; e < a.length; e++)
    n.registerAction({
      type: a[e],
      update: "updateView"
    }, c);
  n.registerAction({
    type: "treemapRootToNode",
    update: "updateView"
  }, function(o, i) {
    i.eachComponent({
      mainType: "series",
      subType: "treemap",
      query: o
    }, m);
    function m(t, u) {
      var d = ["treemapZoomToNode", "treemapRootToNode"], r = f(o, d, t);
      if (r) {
        var p = t.getViewRoot();
        p && (o.direction = T(p, r.node) ? "rollUp" : "drillDown"), t.resetViewRoot(r.node);
      }
    }
  });
}
export {
  R as installTreemapAction
};
