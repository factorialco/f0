import { needFixJitter as p, fixJitter as l } from "../../util/jitter.js";
function z(u) {
  u.eachSeriesByType("scatter", function(y) {
    var r = y.coordinateSystem;
    if (r && (r.type === "cartesian2d" || r.type === "single")) {
      var t = r.getBaseAxis ? r.getBaseAxis() : null, m = t && p(y, t);
      if (m) {
        var i = y.getData();
        i.each(function(a) {
          var o = t.dim, v = t.orient, f = v === "horizontal" && t.type !== "category" || v === "vertical" && t.type === "category", e = i.getItemLayout(a), n = i.getItemVisual(a, "symbolSize"), g = n instanceof Array ? (n[1] + n[0]) / 2 : n;
          if (o === "y" || o === "single" && f) {
            var c = l(t, e[0], e[1], g / 2);
            i.setItemLayout(a, [e[0], c]);
          } else if (o === "x" || o === "single" && !f) {
            var c = l(t, e[1], e[0], g / 2);
            i.setItemLayout(a, [c, e[1]]);
          }
        });
      }
    }
  });
}
export {
  z as default
};
