import { each as c, map as g } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
function h(r, m) {
  var a = {};
  return c(r, function(t) {
    t.each(t.mapDimension("value"), function(e, n) {
      var o = "ec-" + t.getName(n);
      a[o] = a[o] || [], isNaN(e) || a[o].push(e);
    });
  }), r[0].map(r[0].mapDimension("value"), function(t, e) {
    for (var n = "ec-" + r[0].getName(e), o = 0, l = 1 / 0, p = -1 / 0, v = a[n].length, u = 0; u < v; u++)
      l = Math.min(l, a[n][u]), p = Math.max(p, a[n][u]), o += a[n][u];
    var f;
    return m === "min" ? f = l : m === "max" ? f = p : m === "average" ? f = o / v : f = o, v === 0 ? NaN : f;
  });
}
function y(r) {
  var m = {};
  r.eachSeriesByType("map", function(a) {
    var t = a.getHostGeoModel(), e = t ? "o" + t.id : "i" + a.getMapType();
    (m[e] = m[e] || []).push(a);
  }), c(m, function(a, t) {
    for (var e = h(g(a, function(o) {
      return o.getData();
    }), a[0].get("mapValueCalculation")), n = 0; n < a.length; n++)
      a[n].originalData = a[n].getData();
    for (var n = 0; n < a.length; n++)
      a[n].seriesGroup = a, a[n].needsDrawMap = n === 0 && !a[n].getHostGeoModel(), a[n].setData(e.cloneShallow()), a[n].mainSeries = a[0];
  });
}
export {
  y as default
};
