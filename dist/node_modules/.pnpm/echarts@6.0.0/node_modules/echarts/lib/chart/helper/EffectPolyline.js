import { __extends as c } from "../../../../../../tslib@2.3.0/node_modules/tslib/tslib.es6.js";
import m from "./Polyline.js";
import g from "./EffectLine.js";
import { dist as P } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/vector.js";
var x = (
  /** @class */
  (function(l) {
    c(f, l);
    function f() {
      var r = l !== null && l.apply(this, arguments) || this;
      return r._lastFrame = 0, r._lastFramePercent = 0, r;
    }
    return f.prototype.createLine = function(r, a, i) {
      return new m(r, a, i);
    }, f.prototype._updateAnimationPoints = function(r, a) {
      this._points = a;
      for (var i = [0], n = 0, e = 1; e < a.length; e++) {
        var s = a[e - 1], t = a[e];
        n += P(s, t), i.push(n);
      }
      if (n === 0) {
        this._length = 0;
        return;
      }
      for (var e = 0; e < i.length; e++)
        i[e] /= n;
      this._offsets = i, this._length = n;
    }, f.prototype._getLineLength = function() {
      return this._length;
    }, f.prototype._updateSymbolPosition = function(r) {
      var a = r.__t < 1 ? r.__t : 2 - r.__t, i = this._points, n = this._offsets, e = i.length;
      if (n) {
        var s = this._lastFrame, t;
        if (a < this._lastFramePercent) {
          var v = Math.min(s + 1, e - 1);
          for (t = v; t >= 0 && !(n[t] <= a); t--)
            ;
          t = Math.min(t, e - 2);
        } else {
          for (t = s; t < e && !(n[t] > a); t++)
            ;
          t = Math.min(t - 1, e - 2);
        }
        var _ = (a - n[t]) / (n[t + 1] - n[t]), o = i[t], h = i[t + 1];
        r.x = o[0] * (1 - _) + _ * h[0], r.y = o[1] * (1 - _) + _ * h[1];
        var p = r.__t < 1 ? h[0] - o[0] : o[0] - h[0], u = r.__t < 1 ? h[1] - o[1] : o[1] - h[1];
        r.rotation = -Math.atan2(u, p) - Math.PI / 2, this._lastFrame = t, this._lastFramePercent = a, r.ignore = !1;
      }
    }, f;
  })(g)
);
export {
  x as default
};
