function t(p, r, o) {
  for (var a = [p], n = [], e; e = a.pop(); )
    if (n.push(e), e.isExpand) {
      var f = e.children;
      if (f.length)
        for (var h = 0; h < f.length; h++)
          a.push(f[h]);
    }
  for (; e = n.pop(); )
    r(e, o);
}
function v(p, r) {
  for (var o = [p], a; a = o.pop(); )
    if (r(a), a.isExpand) {
      var n = a.children;
      if (n.length)
        for (var e = n.length - 1; e >= 0; e--)
          o.push(n[e]);
    }
}
export {
  t as eachAfter,
  v as eachBefore
};
