import { each as h, isString as S } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { isSeriesDataSchema as g } from "./SeriesDataSchema.js";
function _(e, r, s) {
  s = s || {};
  var u = s.byIndex, f = s.stackedCoordDimension, t, o, l;
  p(r) ? t = r : (o = r.schema, t = o.dimensions, l = r.store);
  var k = !!(e && e.get("stack")), n, i, m, c;
  if (h(t, function(a, y) {
    S(a) && (t[y] = a = {
      name: a
    }), k && !a.isExtraCoord && (!u && !n && a.ordinalMeta && (n = a), !i && a.type !== "ordinal" && a.type !== "time" && (!f || f === a.coordDim) && (i = a));
  }), i && !u && !n && (u = !0), i) {
    m = "__\0ecstackresult_" + e.id, c = "__\0ecstackedover_" + e.id, n && (n.createInvertedIndices = !0);
    var x = i.coordDim, v = i.type, d = 0;
    h(t, function(a) {
      a.coordDim === x && d++;
    });
    var D = {
      name: m,
      coordDim: x,
      coordDimIndex: d,
      type: v,
      isExtraCoord: !0,
      isCalculationCoord: !0,
      storeDimIndex: t.length
    }, C = {
      name: c,
      // This dimension contains stack base (generally, 0), so do not set it as
      // `stackedDimCoordDim` to avoid extent calculation, consider log scale.
      coordDim: c,
      coordDimIndex: d + 1,
      type: v,
      isExtraCoord: !0,
      isCalculationCoord: !0,
      storeDimIndex: t.length + 1
    };
    o ? (l && (D.storeDimIndex = l.ensureCalculationDimension(c, v), C.storeDimIndex = l.ensureCalculationDimension(m, v)), o.appendCalculationDimension(D), o.appendCalculationDimension(C)) : (t.push(D), t.push(C));
  }
  return {
    stackedDimension: i && i.name,
    stackedByDimension: n && n.name,
    isStackedByIndex: u,
    stackedOverDimension: c,
    stackResultDimension: m
  };
}
function p(e) {
  return !g(e.schema);
}
function b(e, r) {
  return !!r && r === e.getCalculationInfo("stackedDimension");
}
function I(e, r) {
  return b(e, r) ? e.getCalculationInfo("stackResultDimension") : r;
}
export {
  _ as enableDataStack,
  I as getStackedDimension,
  b as isDimensionStacked
};
