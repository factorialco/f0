import { makeInner as L, getDataItemValue as G, queryReferringComponents as A, SINGLE_REFERRING as W } from "../../util/model.js";
import { createHashMap as I, isTypedArray as E, isObject as _, isArray as k, isString as C, each as M } from "../../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { SOURCE_FORMAT_ARRAY_ROWS as Y, SERIES_LAYOUT_BY_ROW as b, SOURCE_FORMAT_OBJECT_ROWS as T, SOURCE_FORMAT_KEYED_COLUMNS as F, SOURCE_FORMAT_ORIGINAL as q } from "../../util/types.js";
var m = {
  Must: 1,
  Might: 2,
  Not: 3
  // Other cases
}, U = L();
function K(r) {
  U(r).datasetMap = I();
}
function j(r, i, S) {
  var f = {}, p = x(i);
  if (!p || !r)
    return f;
  var N = [], a = [], g = i.ecModel, y = U(g).datasetMap, c = p.uid + "_" + S.seriesLayoutBy, t, l;
  r = r.slice(), M(r, function(v, n) {
    var s = _(v) ? v : r[n] = {
      name: v
    };
    s.type === "ordinal" && t == null && (t = n, l = R(s)), f[s.name] = [];
  });
  var o = y.get(c) || y.set(c, {
    categoryWayDim: l,
    valueWayDim: 0
  });
  M(r, function(v, n) {
    var s = v.name, u = R(v);
    if (t == null) {
      var d = o.valueWayDim;
      e(f[s], d, u), e(a, d, u), o.valueWayDim += u;
    } else if (t === n)
      e(f[s], 0, u), e(N, 0, u);
    else {
      var d = o.categoryWayDim;
      e(f[s], d, u), e(a, d, u), o.categoryWayDim += u;
    }
  });
  function e(v, n, s) {
    for (var u = 0; u < s; u++)
      v.push(n + u);
  }
  function R(v) {
    var n = v.dimsDef;
    return n ? n.length : 1;
  }
  return N.length && (f.itemName = N), a.length && (f.seriesName = a), f;
}
function z(r, i, S) {
  var f = {}, p = x(r);
  if (!p)
    return f;
  var N = i.sourceFormat, a = i.dimensionsDefine, g;
  (N === T || N === F) && M(a, function(t, l) {
    (_(t) ? t.name : t) === "name" && (g = l);
  });
  var y = (function() {
    for (var t = {}, l = {}, o = [], e = 0, R = Math.min(5, S); e < R; e++) {
      var v = B(i.data, N, i.seriesLayoutBy, a, i.startIndex, e);
      o.push(v);
      var n = v === m.Not;
      if (n && t.v == null && e !== g && (t.v = e), (t.n == null || t.n === t.v || !n && o[t.n] === m.Not) && (t.n = e), s(t) && o[t.n] !== m.Not)
        return t;
      n || (v === m.Might && l.v == null && e !== g && (l.v = e), (l.n == null || l.n === l.v) && (l.n = e));
    }
    function s(u) {
      return u.v != null && u.n != null;
    }
    return s(t) ? t : s(l) ? l : null;
  })();
  if (y) {
    f.value = [y.v];
    var c = g ?? y.n;
    f.itemName = [c], f.seriesName = [c];
  }
  return f;
}
function x(r) {
  var i = r.get("data", !0);
  if (!i)
    return A(r.ecModel, "dataset", {
      index: r.get("datasetIndex", !0),
      id: r.get("datasetId", !0)
    }, W).models[0];
}
function Q(r) {
  return !r.get("transform", !0) && !r.get("fromTransformResult", !0) ? [] : A(r.ecModel, "dataset", {
    index: r.get("fromDatasetIndex", !0),
    id: r.get("fromDatasetId", !0)
  }, W).models;
}
function X(r, i) {
  return B(r.data, r.sourceFormat, r.seriesLayoutBy, r.dimensionsDefine, r.startIndex, i);
}
function B(r, i, S, f, p, N) {
  var a, g = 5;
  if (E(r))
    return m.Not;
  var y, c;
  if (f) {
    var t = f[N];
    _(t) ? (y = t.name, c = t.type) : C(t) && (y = t);
  }
  if (c != null)
    return c === "ordinal" ? m.Must : m.Not;
  if (i === Y) {
    var l = r;
    if (S === b) {
      for (var o = l[N], e = 0; e < (o || []).length && e < g; e++)
        if ((a = h(o[p + e])) != null)
          return a;
    } else
      for (var e = 0; e < l.length && e < g; e++) {
        var R = l[p + e];
        if (R && (a = h(R[N])) != null)
          return a;
      }
  } else if (i === T) {
    var v = r;
    if (!y)
      return m.Not;
    for (var e = 0; e < v.length && e < g; e++) {
      var n = v[e];
      if (n && (a = h(n[y])) != null)
        return a;
    }
  } else if (i === F) {
    var s = r;
    if (!y)
      return m.Not;
    var o = s[y];
    if (!o || E(o))
      return m.Not;
    for (var e = 0; e < o.length && e < g; e++)
      if ((a = h(o[e])) != null)
        return a;
  } else if (i === q)
    for (var u = r, e = 0; e < u.length && e < g; e++) {
      var n = u[e], d = G(n);
      if (!k(d))
        return m.Not;
      if ((a = h(d[N])) != null)
        return a;
    }
  function h(O) {
    var D = C(O);
    if (O != null && Number.isFinite(Number(O)) && O !== "")
      return D ? m.Might : m.Not;
    if (D && O !== "-")
      return m.Must;
  }
  return m.Not;
}
export {
  m as BE_ORDINAL,
  X as guessOrdinal,
  j as makeSeriesEncodeForAxisCoordSys,
  z as makeSeriesEncodeForNameBased,
  Q as queryDatasetUpstreamDatasetModels,
  x as querySeriesUpstreamDatasetModel,
  K as resetSourceDefaulter
};
