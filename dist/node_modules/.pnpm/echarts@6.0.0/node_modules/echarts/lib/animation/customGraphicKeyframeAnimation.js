import { isArray as K, each as E, keys as T, filter as _, indexOf as R } from "../../../../../zrender@6.0.0/node_modules/zrender/lib/core/util.js";
import { ELEMENT_ANIMATABLE_PROPS as k } from "./customGraphicTransition.js";
import { getAnimationConfig as S } from "./basicTransition.js";
import { warn as w } from "../util/log.js";
import { makeInner as x } from "../util/model.js";
var h = x(), D = ["percent", "easing", "shape", "style", "extra"];
function B(n) {
  n.stopAnimation("keyframe"), n.attr(h(n));
}
function I(n, e, c) {
  if (!(!c.isAnimationEnabled() || !e)) {
    if (K(e)) {
      E(e, function(r) {
        I(n, r, c);
      });
      return;
    }
    var y = e.keyframes, s = e.duration;
    if (c && s == null) {
      var d = S("enter", c, 0);
      s = d && d.duration;
    }
    if (!(!y || !s)) {
      var a = h(n);
      E(k, function(r) {
        if (!(r && !n[r])) {
          var i, m = !1;
          y.sort(function(t, f) {
            return t.percent - f.percent;
          }), E(y, function(t) {
            var f = n.animators, A = r ? t[r] : t;
            if (process.env.NODE_ENV !== "production" && t.percent >= 1 && (m = !0), !!A) {
              var o = T(A);
              if (r || (o = _(o, function(v) {
                return R(D, v) < 0;
              })), !!o.length) {
                i || (i = n.animate(r, e.loop, !0), i.scope = "keyframe");
                for (var u = 0; u < f.length; u++)
                  f[u] !== i && f[u].targetName === i.targetName && f[u].stopTracks(o);
                r && (a[r] = a[r] || {});
                var p = r ? a[r] : a;
                E(o, function(v) {
                  p[v] = ((r ? n[r] : n) || {})[v];
                }), i.whenWithKeys(s * t.percent, A, o, t.easing);
              }
            }
          }), i && (process.env.NODE_ENV !== "production" && (m || w("End frame with percent: 1 is missing in the keyframeAnimation.", !0)), i.delay(e.delay || 0).duration(s).start(e.easing));
        }
      });
    }
  }
}
export {
  I as applyKeyframeAnimation,
  B as stopPreviousKeyframeAnimationAndRestore
};
