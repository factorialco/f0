import { ObservableV2 as Ve } from "../../../../lib0@0.2.109/node_modules/lib0/observable.js";
import { last as Nt, from as z, isArray as ge, appendTo as mn, some as kn } from "../../../../lib0@0.2.109/node_modules/lib0/array.js";
import { floor as Kt, max as At, min as _n, abs as Ut } from "../../../../lib0@0.2.109/node_modules/lib0/math.js";
import { setIfUndefined as G, any as Sn, create as ne, copy as Wt } from "../../../../lib0@0.2.109/node_modules/lib0/map.js";
import { writeVarUint as _, writeUint8 as zt, writeVarString as Vt, writeAny as qt, writeVarUint8Array as L, IntDiffOptRleEncoder as Ft, UintOptRleEncoder as Bt, RleEncoder as pe, StringEncoder as bn, toUint8Array as B, writeUint8Array as Fe, createEncoder as Tt } from "../../../../lib0@0.2.109/node_modules/lib0/encoding.js";
import { createDecoder as ie, readVarUint as D, IntDiffOptRleDecoder as Jt, readVarUint8Array as x, UintOptRleDecoder as Ht, RleDecoder as we, readUint8 as me, StringDecoder as yn, readAny as ke } from "../../../../lib0@0.2.109/node_modules/lib0/decoding.js";
import { uint32 as Cn, uuidv4 as Dn } from "../../../../lib0@0.2.109/node_modules/lib0/random.js";
import { create as _e } from "../../../../lib0@0.2.109/node_modules/lib0/promise.js";
import { unexpectedCase as M, methodUnimplemented as R, create as mt } from "../../../../lib0@0.2.109/node_modules/lib0/error.js";
import { BIT2 as Se, BIT4 as jt, BIT1 as be, BIT3 as Yt, BITS5 as Lt, BIT8 as J, BIT7 as H, BIT6 as st } from "../../../../lib0@0.2.109/node_modules/lib0/binary.js";
import { callAll as Be } from "../../../../lib0@0.2.109/node_modules/lib0/function.js";
import { create as kt } from "../../../../lib0@0.2.109/node_modules/lib0/set.js";
import { warn as Je, print as En } from "../../../../lib0@0.2.109/node_modules/lib0/logging.js";
import { getUnixTime as In } from "../../../../lib0@0.2.109/node_modules/lib0/time.js";
import { iteratorMap as Gt, iteratorFilter as An } from "../../../../lib0@0.2.109/node_modules/lib0/iterator.js";
import { isEmpty as Tn, assign as Ln, equalFlat as On, deepFreeze as Mn, forEach as xn } from "../../../../lib0@0.2.109/node_modules/lib0/object.js";
import { getVariable as Rn } from "../../../../lib0@0.2.109/node_modules/lib0/environment.js";
import { RED as vn, UNBOLD as Nn, BOLD as Un, ORANGE as Vn } from "../../../../lib0@0.2.109/node_modules/lib0/logging.common.js";
class se {
  /**
   * @param {number} clock
   * @param {number} len
   */
  constructor(t, e) {
    this.clock = t, this.len = e;
  }
}
class tt {
  constructor() {
    this.clients = /* @__PURE__ */ new Map();
  }
}
const q = (i, t, e) => t.clients.forEach((n, s) => {
  const r = (
    /** @type {Array<GC|Item>} */
    i.doc.store.clients.get(s)
  );
  if (r != null) {
    const l = r[r.length - 1], o = l.id.clock + l.length;
    for (let c = 0, d = n[c]; c < n.length && d.clock < o; d = n[++c])
      Ke(i, r, d.clock, d.len, e);
  }
}), Fn = (i, t) => {
  let e = 0, n = i.length - 1;
  for (; e <= n; ) {
    const s = Kt((e + n) / 2), r = i[s], l = r.clock;
    if (l <= t) {
      if (t < l + r.len)
        return s;
      e = s + 1;
    } else
      n = s - 1;
  }
  return null;
}, ht = (i, t) => {
  const e = i.clients.get(t.client);
  return e !== void 0 && Fn(e, t.clock) !== null;
}, re = (i) => {
  i.clients.forEach((t) => {
    t.sort((s, r) => s.clock - r.clock);
    let e, n;
    for (e = 1, n = 1; e < t.length; e++) {
      const s = t[n - 1], r = t[e];
      s.clock + s.len >= r.clock ? s.len = At(s.len, r.clock + r.len - s.clock) : (n < e && (t[n] = r), n++);
    }
    t.length = n;
  });
}, Qt = (i) => {
  const t = new tt();
  for (let e = 0; e < i.length; e++)
    i[e].clients.forEach((n, s) => {
      if (!t.clients.has(s)) {
        const r = n.slice();
        for (let l = e + 1; l < i.length; l++)
          mn(r, i[l].clients.get(s) || []);
        t.clients.set(s, r);
      }
    });
  return re(t), t;
}, rt = (i, t, e, n) => {
  G(i.clients, t, () => (
    /** @type {Array<DeleteItem>} */
    []
  )).push(new se(e, n));
}, Bn = () => new tt(), Jn = (i) => {
  const t = Bn();
  return i.clients.forEach((e, n) => {
    const s = [];
    for (let r = 0; r < e.length; r++) {
      const l = e[r];
      if (l.deleted) {
        const o = l.id.clock;
        let c = l.length;
        if (r + 1 < e.length)
          for (let d = e[r + 1]; r + 1 < e.length && d.deleted; d = e[++r + 1])
            c += d.length;
        s.push(new se(o, c));
      }
    }
    s.length > 0 && t.clients.set(n, s);
  }), t;
}, le = (i, t) => {
  _(i.restEncoder, t.clients.size), z(t.clients.entries()).sort((e, n) => n[0] - e[0]).forEach(([e, n]) => {
    i.resetDsCurVal(), _(i.restEncoder, e);
    const s = n.length;
    _(i.restEncoder, s);
    for (let r = 0; r < s; r++) {
      const l = n[r];
      i.writeDsClock(l.clock), i.writeDsLen(l.len);
    }
  });
}, Hn = (i) => {
  const t = new tt(), e = D(i.restDecoder);
  for (let n = 0; n < e; n++) {
    i.resetDsCurVal();
    const s = D(i.restDecoder), r = D(i.restDecoder);
    if (r > 0) {
      const l = G(t.clients, s, () => (
        /** @type {Array<DeleteItem>} */
        []
      ));
      for (let o = 0; o < r; o++)
        l.push(new se(i.readDsClock(), i.readDsLen()));
    }
  }
  return t;
}, ye = (i, t, e) => {
  const n = new tt(), s = D(i.restDecoder);
  for (let r = 0; r < s; r++) {
    i.resetDsCurVal();
    const l = D(i.restDecoder), o = D(i.restDecoder), c = e.clients.get(l) || [], d = y(e, l);
    for (let h = 0; h < o; h++) {
      const a = i.readDsClock(), u = a + i.readDsLen();
      if (a < d) {
        d < u && rt(n, l, d, u - d);
        let f = v(c, a), g = c[f];
        for (!g.deleted && g.id.clock < a && (c.splice(f + 1, 0, It(t, g, a - g.id.clock)), f++); f < c.length && (g = c[f++], g.id.clock < u); )
          g.deleted || (u < g.id.clock + g.length && c.splice(f, 0, It(t, g, u - g.id.clock)), g.delete(t));
      } else
        rt(n, l, a, u - a);
    }
  }
  if (n.clients.size > 0) {
    const r = new Ot();
    return _(r.restEncoder, 0), le(r, n), r.toUint8Array();
  }
  return null;
}, He = Cn;
class et extends Ve {
  /**
   * @param {DocOpts} opts configuration
   */
  constructor({ guid: t = Dn(), collectionid: e = null, gc: n = !0, gcFilter: s = () => !0, meta: r = null, autoLoad: l = !1, shouldLoad: o = !0 } = {}) {
    super(), this.gc = n, this.gcFilter = s, this.clientID = He(), this.guid = t, this.collectionid = e, this.share = /* @__PURE__ */ new Map(), this.store = new $e(), this._transaction = null, this._transactionCleanups = [], this.subdocs = /* @__PURE__ */ new Set(), this._item = null, this.shouldLoad = o, this.autoLoad = l, this.meta = r, this.isLoaded = !1, this.isSynced = !1, this.isDestroyed = !1, this.whenLoaded = _e((d) => {
      this.on("load", () => {
        this.isLoaded = !0, d(this);
      });
    });
    const c = () => _e((d) => {
      const h = (a) => {
        (a === void 0 || a === !0) && (this.off("sync", h), d());
      };
      this.on("sync", h);
    });
    this.on("sync", (d) => {
      d === !1 && this.isSynced && (this.whenSynced = c()), this.isSynced = d === void 0 || d === !0, this.isSynced && !this.isLoaded && this.emit("load", [this]);
    }), this.whenSynced = c();
  }
  /**
   * Notify the parent document that you request to load data into this subdocument (if it is a subdocument).
   *
   * `load()` might be used in the future to request any provider to load the most current data.
   *
   * It is safe to call `load()` multiple times.
   */
  load() {
    const t = this._item;
    t !== null && !this.shouldLoad && m(
      /** @type {any} */
      t.parent.doc,
      (e) => {
        e.subdocsLoaded.add(this);
      },
      null,
      !0
    ), this.shouldLoad = !0;
  }
  getSubdocs() {
    return this.subdocs;
  }
  getSubdocGuids() {
    return new Set(z(this.subdocs).map((t) => t.guid));
  }
  /**
   * Changes that happen inside of a transaction are bundled. This means that
   * the observer fires _after_ the transaction is finished and that all changes
   * that happened inside of the transaction are sent as one message to the
   * other peers.
   *
   * @template T
   * @param {function(Transaction):T} f The function that should be executed as a transaction
   * @param {any} [origin] Origin of who started the transaction. Will be stored on transaction.origin
   * @return T
   *
   * @public
   */
  transact(t, e = null) {
    return m(this, t, e);
  }
  /**
   * Define a shared data type.
   *
   * Multiple calls of `ydoc.get(name, TypeConstructor)` yield the same result
   * and do not overwrite each other. I.e.
   * `ydoc.get(name, Y.Array) === ydoc.get(name, Y.Array)`
   *
   * After this method is called, the type is also available on `ydoc.share.get(name)`.
   *
   * *Best Practices:*
   * Define all types right after the Y.Doc instance is created and store them in a separate object.
   * Also use the typed methods `getText(name)`, `getArray(name)`, ..
   *
   * @template {typeof AbstractType<any>} Type
   * @example
   *   const ydoc = new Y.Doc(..)
   *   const appState = {
   *     document: ydoc.getText('document')
   *     comments: ydoc.getArray('comments')
   *   }
   *
   * @param {string} name
   * @param {Type} TypeConstructor The constructor of the type definition. E.g. Y.Text, Y.Array, Y.Map, ...
   * @return {InstanceType<Type>} The created type. Constructed with TypeConstructor
   *
   * @public
   */
  get(t, e = (
    /** @type {any} */
    E
  )) {
    const n = G(this.share, t, () => {
      const r = new e();
      return r._integrate(this, null), r;
    }), s = n.constructor;
    if (e !== E && s !== e)
      if (s === E) {
        const r = new e();
        r._map = n._map, n._map.forEach(
          /** @param {Item?} n */
          (l) => {
            for (; l !== null; l = l.left)
              l.parent = r;
          }
        ), r._start = n._start;
        for (let l = r._start; l !== null; l = l.right)
          l.parent = r;
        return r._length = n._length, this.share.set(t, r), r._integrate(this, null), /** @type {InstanceType<Type>} */
        r;
      } else
        throw new Error(`Type with the name ${t} has already been defined with a different constructor`);
    return (
      /** @type {InstanceType<Type>} */
      n
    );
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YArray<T>}
   *
   * @public
   */
  getArray(t = "") {
    return (
      /** @type {YArray<T>} */
      this.get(t, W)
    );
  }
  /**
   * @param {string} [name]
   * @return {YText}
   *
   * @public
   */
  getText(t = "") {
    return this.get(t, Z);
  }
  /**
   * @template T
   * @param {string} [name]
   * @return {YMap<T>}
   *
   * @public
   */
  getMap(t = "") {
    return (
      /** @type {YMap<T>} */
      this.get(t, Q)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlElement}
   *
   * @public
   */
  getXmlElement(t = "") {
    return (
      /** @type {YXmlElement<{[key:string]:string}>} */
      this.get(t, P)
    );
  }
  /**
   * @param {string} [name]
   * @return {YXmlFragment}
   *
   * @public
   */
  getXmlFragment(t = "") {
    return this.get(t, j);
  }
  /**
   * Converts the entire document into a js object, recursively traversing each yjs type
   * Doesn't log types that have not been defined (using ydoc.getType(..)).
   *
   * @deprecated Do not use this method and rather call toJSON directly on the shared types.
   *
   * @return {Object<string, any>}
   */
  toJSON() {
    const t = {};
    return this.share.forEach((e, n) => {
      t[n] = e.toJSON();
    }), t;
  }
  /**
   * Emit `destroy` event and unregister all event handlers.
   */
  destroy() {
    this.isDestroyed = !0, z(this.subdocs).forEach((e) => e.destroy());
    const t = this._item;
    if (t !== null) {
      this._item = null;
      const e = (
        /** @type {ContentDoc} */
        t.content
      );
      e.doc = new et({ guid: this.guid, ...e.opts, shouldLoad: !1 }), e.doc._item = t, m(
        /** @type {any} */
        t.parent.doc,
        (n) => {
          const s = e.doc;
          t.deleted || n.subdocsAdded.add(s), n.subdocsRemoved.add(this);
        },
        null,
        !0
      );
    }
    this.emit("destroyed", [!0]), this.emit("destroy", [this]), super.destroy();
  }
}
class jn {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(t) {
    this.dsCurrVal = 0, this.restDecoder = t;
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @return {number}
   */
  readDsClock() {
    return this.dsCurrVal += D(this.restDecoder), this.dsCurrVal;
  }
  /**
   * @return {number}
   */
  readDsLen() {
    const t = D(this.restDecoder) + 1;
    return this.dsCurrVal += t, t;
  }
}
class _t extends jn {
  /**
   * @param {decoding.Decoder} decoder
   */
  constructor(t) {
    super(t), this.keys = [], D(t), this.keyClockDecoder = new Jt(x(t)), this.clientDecoder = new Ht(x(t)), this.leftClockDecoder = new Jt(x(t)), this.rightClockDecoder = new Jt(x(t)), this.infoDecoder = new we(x(t), me), this.stringDecoder = new yn(x(t)), this.parentInfoDecoder = new we(x(t), me), this.typeRefDecoder = new Ht(x(t)), this.lenDecoder = new Ht(x(t));
  }
  /**
   * @return {ID}
   */
  readLeftID() {
    return new X(this.clientDecoder.read(), this.leftClockDecoder.read());
  }
  /**
   * @return {ID}
   */
  readRightID() {
    return new X(this.clientDecoder.read(), this.rightClockDecoder.read());
  }
  /**
   * Read the next client id.
   * Use this in favor of readID whenever possible to reduce the number of objects created.
   */
  readClient() {
    return this.clientDecoder.read();
  }
  /**
   * @return {number} info An unsigned 8-bit integer
   */
  readInfo() {
    return (
      /** @type {number} */
      this.infoDecoder.read()
    );
  }
  /**
   * @return {string}
   */
  readString() {
    return this.stringDecoder.read();
  }
  /**
   * @return {boolean}
   */
  readParentInfo() {
    return this.parentInfoDecoder.read() === 1;
  }
  /**
   * @return {number} An unsigned 8-bit integer
   */
  readTypeRef() {
    return this.typeRefDecoder.read();
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @return {number}
   */
  readLen() {
    return this.lenDecoder.read();
  }
  /**
   * @return {any}
   */
  readAny() {
    return ke(this.restDecoder);
  }
  /**
   * @return {Uint8Array}
   */
  readBuf() {
    return x(this.restDecoder);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @return {any}
   */
  readJSON() {
    return ke(this.restDecoder);
  }
  /**
   * @return {string}
   */
  readKey() {
    const t = this.keyClockDecoder.read();
    if (t < this.keys.length)
      return this.keys[t];
    {
      const e = this.stringDecoder.read();
      return this.keys.push(e), e;
    }
  }
}
class Yn {
  constructor() {
    this.restEncoder = Tt();
  }
  toUint8Array() {
    return B(this.restEncoder);
  }
  resetDsCurVal() {
  }
  /**
   * @param {number} clock
   */
  writeDsClock(t) {
    _(this.restEncoder, t);
  }
  /**
   * @param {number} len
   */
  writeDsLen(t) {
    _(this.restEncoder, t);
  }
}
class Gn extends Yn {
  /**
   * @param {ID} id
   */
  writeLeftID(t) {
    _(this.restEncoder, t.client), _(this.restEncoder, t.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(t) {
    _(this.restEncoder, t.client), _(this.restEncoder, t.clock);
  }
  /**
   * Use writeClient and writeClock instead of writeID if possible.
   * @param {number} client
   */
  writeClient(t) {
    _(this.restEncoder, t);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(t) {
    zt(this.restEncoder, t);
  }
  /**
   * @param {string} s
   */
  writeString(t) {
    Vt(this.restEncoder, t);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(t) {
    _(this.restEncoder, t ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(t) {
    _(this.restEncoder, t);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(t) {
    _(this.restEncoder, t);
  }
  /**
   * @param {any} any
   */
  writeAny(t) {
    qt(this.restEncoder, t);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(t) {
    L(this.restEncoder, t);
  }
  /**
   * @param {any} embed
   */
  writeJSON(t) {
    Vt(this.restEncoder, JSON.stringify(t));
  }
  /**
   * @param {string} key
   */
  writeKey(t) {
    Vt(this.restEncoder, t);
  }
}
class $n {
  constructor() {
    this.restEncoder = Tt(), this.dsCurrVal = 0;
  }
  toUint8Array() {
    return B(this.restEncoder);
  }
  resetDsCurVal() {
    this.dsCurrVal = 0;
  }
  /**
   * @param {number} clock
   */
  writeDsClock(t) {
    const e = t - this.dsCurrVal;
    this.dsCurrVal = t, _(this.restEncoder, e);
  }
  /**
   * @param {number} len
   */
  writeDsLen(t) {
    t === 0 && M(), _(this.restEncoder, t - 1), this.dsCurrVal += t;
  }
}
class Ot extends $n {
  constructor() {
    super(), this.keyMap = /* @__PURE__ */ new Map(), this.keyClock = 0, this.keyClockEncoder = new Ft(), this.clientEncoder = new Bt(), this.leftClockEncoder = new Ft(), this.rightClockEncoder = new Ft(), this.infoEncoder = new pe(zt), this.stringEncoder = new bn(), this.parentInfoEncoder = new pe(zt), this.typeRefEncoder = new Bt(), this.lenEncoder = new Bt();
  }
  toUint8Array() {
    const t = Tt();
    return _(t, 0), L(t, this.keyClockEncoder.toUint8Array()), L(t, this.clientEncoder.toUint8Array()), L(t, this.leftClockEncoder.toUint8Array()), L(t, this.rightClockEncoder.toUint8Array()), L(t, B(this.infoEncoder)), L(t, this.stringEncoder.toUint8Array()), L(t, B(this.parentInfoEncoder)), L(t, this.typeRefEncoder.toUint8Array()), L(t, this.lenEncoder.toUint8Array()), Fe(t, B(this.restEncoder)), B(t);
  }
  /**
   * @param {ID} id
   */
  writeLeftID(t) {
    this.clientEncoder.write(t.client), this.leftClockEncoder.write(t.clock);
  }
  /**
   * @param {ID} id
   */
  writeRightID(t) {
    this.clientEncoder.write(t.client), this.rightClockEncoder.write(t.clock);
  }
  /**
   * @param {number} client
   */
  writeClient(t) {
    this.clientEncoder.write(t);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeInfo(t) {
    this.infoEncoder.write(t);
  }
  /**
   * @param {string} s
   */
  writeString(t) {
    this.stringEncoder.write(t);
  }
  /**
   * @param {boolean} isYKey
   */
  writeParentInfo(t) {
    this.parentInfoEncoder.write(t ? 1 : 0);
  }
  /**
   * @param {number} info An unsigned 8-bit integer
   */
  writeTypeRef(t) {
    this.typeRefEncoder.write(t);
  }
  /**
   * Write len of a struct - well suited for Opt RLE encoder.
   *
   * @param {number} len
   */
  writeLen(t) {
    this.lenEncoder.write(t);
  }
  /**
   * @param {any} any
   */
  writeAny(t) {
    qt(this.restEncoder, t);
  }
  /**
   * @param {Uint8Array} buf
   */
  writeBuf(t) {
    L(this.restEncoder, t);
  }
  /**
   * This is mainly here for legacy purposes.
   *
   * Initial we incoded objects using JSON. Now we use the much faster lib0/any-encoder. This method mainly exists for legacy purposes for the v1 encoder.
   *
   * @param {any} embed
   */
  writeJSON(t) {
    qt(this.restEncoder, t);
  }
  /**
   * Property keys are often reused. For example, in y-prosemirror the key `bold` might
   * occur very often. For a 3d application, the key `position` might occur very often.
   *
   * We cache these keys in a Map and refer to them via a unique number.
   *
   * @param {string} key
   */
  writeKey(t) {
    const e = this.keyMap.get(t);
    e === void 0 ? (this.keyClockEncoder.write(this.keyClock++), this.stringEncoder.write(t)) : this.keyClockEncoder.write(e);
  }
}
const Xn = (i, t, e, n) => {
  n = At(n, t[0].id.clock);
  const s = v(t, n);
  _(i.restEncoder, t.length - s), i.writeClient(e), _(i.restEncoder, n);
  const r = t[s];
  r.write(i, n - r.id.clock);
  for (let l = s + 1; l < t.length; l++)
    t[l].write(i, 0);
}, je = (i, t, e) => {
  const n = /* @__PURE__ */ new Map();
  e.forEach((s, r) => {
    y(t, r) > s && n.set(r, s);
  }), Mt(t).forEach((s, r) => {
    e.has(r) || n.set(r, 0);
  }), _(i.restEncoder, n.size), z(n.entries()).sort((s, r) => r[0] - s[0]).forEach(([s, r]) => {
    Xn(
      i,
      /** @type {Array<GC|Item>} */
      t.clients.get(s),
      s,
      r
    );
  });
}, Kn = (i, t) => {
  const e = ne(), n = D(i.restDecoder);
  for (let s = 0; s < n; s++) {
    const r = D(i.restDecoder), l = new Array(r), o = i.readClient();
    let c = D(i.restDecoder);
    e.set(o, { i: 0, refs: l });
    for (let d = 0; d < r; d++) {
      const h = i.readInfo();
      switch (Lt & h) {
        case 0: {
          const a = i.readLen();
          l[d] = new T(w(o, c), a), c += a;
          break;
        }
        case 10: {
          const a = D(i.restDecoder);
          l[d] = new O(w(o, c), a), c += a;
          break;
        }
        default: {
          const a = (h & (H | J)) === 0, u = new k(
            w(o, c),
            null,
            // left
            (h & J) === J ? i.readLeftID() : null,
            // origin
            null,
            // right
            (h & H) === H ? i.readRightID() : null,
            // right origin
            a ? i.readParentInfo() ? t.get(i.readString()) : i.readLeftID() : null,
            // parent
            a && (h & st) === st ? i.readString() : null,
            // parentSub
            gn(i, h)
            // item content
          );
          l[d] = u, c += u.length;
        }
      }
    }
  }
  return e;
}, Wn = (i, t, e) => {
  const n = [];
  let s = z(e.keys()).sort((f, g) => f - g);
  if (s.length === 0)
    return null;
  const r = () => {
    if (s.length === 0)
      return null;
    let f = (
      /** @type {{i:number,refs:Array<GC|Item>}} */
      e.get(s[s.length - 1])
    );
    for (; f.refs.length === f.i; )
      if (s.pop(), s.length > 0)
        f = /** @type {{i:number,refs:Array<GC|Item>}} */
        e.get(s[s.length - 1]);
      else
        return null;
    return f;
  };
  let l = r();
  if (l === null)
    return null;
  const o = new $e(), c = /* @__PURE__ */ new Map(), d = (f, g) => {
    const p = c.get(f);
    (p == null || p > g) && c.set(f, g);
  };
  let h = (
    /** @type {any} */
    l.refs[
      /** @type {any} */
      l.i++
    ]
  );
  const a = /* @__PURE__ */ new Map(), u = () => {
    for (const f of n) {
      const g = f.id.client, p = e.get(g);
      p ? (p.i--, o.clients.set(g, p.refs.slice(p.i)), e.delete(g), p.i = 0, p.refs = []) : o.clients.set(g, [f]), s = s.filter((S) => S !== g);
    }
    n.length = 0;
  };
  for (; ; ) {
    if (h.constructor !== O) {
      const g = G(a, h.id.client, () => y(t, h.id.client)) - h.id.clock;
      if (g < 0)
        n.push(h), d(h.id.client, h.id.clock - 1), u();
      else {
        const p = h.getMissing(i, t);
        if (p !== null) {
          n.push(h);
          const S = e.get(
            /** @type {number} */
            p
          ) || { refs: [], i: 0 };
          if (S.refs.length === S.i)
            d(
              /** @type {number} */
              p,
              y(t, p)
            ), u();
          else {
            h = S.refs[S.i++];
            continue;
          }
        } else (g === 0 || g < h.length) && (h.integrate(i, g), a.set(h.id.client, h.id.clock + h.length));
      }
    }
    if (n.length > 0)
      h = /** @type {GC|Item} */
      n.pop();
    else if (l !== null && l.i < l.refs.length)
      h = /** @type {GC|Item} */
      l.refs[l.i++];
    else {
      if (l = r(), l === null)
        break;
      h = /** @type {GC|Item} */
      l.refs[l.i++];
    }
  }
  if (o.clients.size > 0) {
    const f = new Ot();
    return je(f, o, /* @__PURE__ */ new Map()), _(f.restEncoder, 0), { missing: c, update: f.toUint8Array() };
  }
  return null;
}, zn = (i, t) => je(i, t.doc.store, t.beforeState), qn = (i, t, e, n = new _t(i)) => m(t, (s) => {
  s.local = !1;
  let r = !1;
  const l = s.doc, o = l.store, c = Kn(n, l), d = Wn(s, o, c), h = o.pendingStructs;
  if (h) {
    for (const [u, f] of h.missing)
      if (f < y(o, u)) {
        r = !0;
        break;
      }
    if (d) {
      for (const [u, f] of d.missing) {
        const g = h.missing.get(u);
        (g == null || g > f) && h.missing.set(u, f);
      }
      h.update = Me([h.update, d.update]);
    }
  } else
    o.pendingStructs = d;
  const a = ye(n, s, o);
  if (o.pendingDs) {
    const u = new _t(ie(o.pendingDs));
    D(u.restDecoder);
    const f = ye(u, s, o);
    a && f ? o.pendingDs = Me([a, f]) : o.pendingDs = a || f;
  } else
    o.pendingDs = a;
  if (r) {
    const u = (
      /** @type {{update: Uint8Array}} */
      o.pendingStructs.update
    );
    o.pendingStructs = null, Qn(s.doc, u);
  }
}, e, !1), Qn = (i, t, e, n = _t) => {
  const s = ie(t);
  qn(s, i, e, new n(s));
};
class Zn {
  constructor() {
    this.l = [];
  }
}
const Ce = () => new Zn(), De = (i, t) => i.l.push(t), Ee = (i, t) => {
  const e = i.l, n = e.length;
  i.l = e.filter((s) => t !== s), n === i.l.length && console.error("[yjs] Tried to remove event handler that doesn't exist.");
}, Ye = (i, t, e) => Be(i.l, [t, e]);
class X {
  /**
   * @param {number} client client id
   * @param {number} clock unique per client id, continuous number
   */
  constructor(t, e) {
    this.client = t, this.clock = e;
  }
}
const ut = (i, t) => i === t || i !== null && t !== null && i.client === t.client && i.clock === t.clock, w = (i, t) => new X(i, t), Ge = (i) => {
  for (const [t, e] of i.doc.share.entries())
    if (e === i)
      return t;
  throw M();
}, St = (i, t) => {
  for (; t !== null; ) {
    if (t.parent === i)
      return !0;
    t = /** @type {AbstractType<any>} */
    t.parent._item;
  }
  return !1;
};
class Pn {
  /**
   * @param {ID|null} type
   * @param {string|null} tname
   * @param {ID|null} item
   * @param {number} assoc
   */
  constructor(t, e, n, s = 0) {
    this.type = t, this.tname = e, this.item = n, this.assoc = s;
  }
}
class ti {
  /**
   * @param {AbstractType<any>} type
   * @param {number} index
   * @param {number} [assoc]
   */
  constructor(t, e, n = 0) {
    this.type = t, this.index = e, this.assoc = n;
  }
}
const ei = (i, t, e = 0) => new ti(i, t, e), ft = (i, t, e) => {
  let n = null, s = null;
  return i._item === null ? s = Ge(i) : n = w(i._item.id.client, i._item.id.clock), new Pn(n, s, t, e);
}, ys = (i, t, e = 0) => {
  let n = i._start;
  if (e < 0) {
    if (t === 0)
      return ft(i, null, e);
    t--;
  }
  for (; n !== null; ) {
    if (!n.deleted && n.countable) {
      if (n.length > t)
        return ft(i, w(n.id.client, n.id.clock + t), e);
      t -= n.length;
    }
    if (n.right === null && e < 0)
      return ft(i, n.lastId, e);
    n = n.right;
  }
  return ft(i, null, e);
}, ni = (i, t) => {
  const e = K(i, t), n = t.clock - e.id.clock;
  return {
    item: e,
    diff: n
  };
}, Cs = (i, t, e = !0) => {
  const n = t.store, s = i.item, r = i.type, l = i.tname, o = i.assoc;
  let c = null, d = 0;
  if (s !== null) {
    if (y(n, s.client) <= s.clock)
      return null;
    const h = e ? ee(n, s) : ni(n, s), a = h.item;
    if (!(a instanceof k))
      return null;
    if (c = /** @type {AbstractType<any>} */
    a.parent, c._item === null || !c._item.deleted) {
      d = a.deleted || !a.countable ? 0 : h.diff + (o >= 0 ? 0 : 1);
      let u = a.left;
      for (; u !== null; )
        !u.deleted && u.countable && (d += u.length), u = u.left;
    }
  } else {
    if (l !== null)
      c = t.get(l);
    else if (r !== null) {
      if (y(n, r.client) <= r.clock)
        return null;
      const { item: h } = e ? ee(n, r) : { item: K(n, r) };
      if (h instanceof k && h.content instanceof U)
        c = h.content.type;
      else
        return null;
    } else
      throw M();
    o >= 0 ? d = c._length : d = 0;
  }
  return ei(c, d, i.assoc);
};
class ii {
  /**
   * @param {DeleteSet} ds
   * @param {Map<number,number>} sv state map
   */
  constructor(t, e) {
    this.ds = t, this.sv = e;
  }
}
const si = (i, t) => new ii(i, t), Ds = (i) => si(Jn(i.store), Mt(i.store)), F = (i, t) => t === void 0 ? !i.deleted : t.sv.has(i.id.client) && (t.sv.get(i.id.client) || 0) > i.id.clock && !ht(t.ds, i.id), Zt = (i, t) => {
  const e = G(i.meta, Zt, kt), n = i.doc.store;
  e.has(t) || (t.sv.forEach((s, r) => {
    s < y(n, r) && A(i, w(r, s));
  }), q(i, t.ds, (s) => {
  }), e.add(t));
};
class $e {
  constructor() {
    this.clients = /* @__PURE__ */ new Map(), this.pendingStructs = null, this.pendingDs = null;
  }
}
const Mt = (i) => {
  const t = /* @__PURE__ */ new Map();
  return i.clients.forEach((e, n) => {
    const s = e[e.length - 1];
    t.set(n, s.id.clock + s.length);
  }), t;
}, y = (i, t) => {
  const e = i.clients.get(t);
  if (e === void 0)
    return 0;
  const n = e[e.length - 1];
  return n.id.clock + n.length;
}, Xe = (i, t) => {
  let e = i.clients.get(t.id.client);
  if (e === void 0)
    e = [], i.clients.set(t.id.client, e);
  else {
    const n = e[e.length - 1];
    if (n.id.clock + n.length !== t.id.clock)
      throw M();
  }
  e.push(t);
}, v = (i, t) => {
  let e = 0, n = i.length - 1, s = i[n], r = s.id.clock;
  if (r === t)
    return n;
  let l = Kt(t / (r + s.length - 1) * n);
  for (; e <= n; ) {
    if (s = i[l], r = s.id.clock, r <= t) {
      if (t < r + s.length)
        return l;
      e = l + 1;
    } else
      n = l - 1;
    l = Kt((e + n) / 2);
  }
  throw M();
}, ri = (i, t) => {
  const e = i.clients.get(t.client);
  return e[v(e, t.clock)];
}, K = (
  /** @type {function(StructStore,ID):Item} */
  ri
), Pt = (i, t, e) => {
  const n = v(t, e), s = t[n];
  return s.id.clock < e && s instanceof k ? (t.splice(n + 1, 0, It(i, s, e - s.id.clock)), n + 1) : n;
}, A = (i, t) => {
  const e = (
    /** @type {Array<Item>} */
    i.doc.store.clients.get(t.client)
  );
  return e[Pt(i, e, t.clock)];
}, Ie = (i, t, e) => {
  const n = t.clients.get(e.client), s = v(n, e.clock), r = n[s];
  return e.clock !== r.id.clock + r.length - 1 && r.constructor !== T && n.splice(s + 1, 0, It(i, r, e.clock - r.id.clock + 1)), r;
}, li = (i, t, e) => {
  const n = (
    /** @type {Array<GC|Item>} */
    i.clients.get(t.id.client)
  );
  n[v(n, t.id.clock)] = e;
}, Ke = (i, t, e, n, s) => {
  if (n === 0)
    return;
  const r = e + n;
  let l = Pt(i, t, e), o;
  do
    o = t[l++], r < o.id.clock + o.length && Pt(i, t, r), s(o);
  while (l < t.length && t[l].id.clock < r);
};
class oi {
  /**
   * @param {Doc} doc
   * @param {any} origin
   * @param {boolean} local
   */
  constructor(t, e, n) {
    this.doc = t, this.deleteSet = new tt(), this.beforeState = Mt(t.store), this.afterState = /* @__PURE__ */ new Map(), this.changed = /* @__PURE__ */ new Map(), this.changedParentTypes = /* @__PURE__ */ new Map(), this._mergeStructs = [], this.origin = e, this.meta = /* @__PURE__ */ new Map(), this.local = n, this.subdocsAdded = /* @__PURE__ */ new Set(), this.subdocsRemoved = /* @__PURE__ */ new Set(), this.subdocsLoaded = /* @__PURE__ */ new Set(), this._needFormattingCleanup = !1;
  }
}
const Ae = (i, t) => t.deleteSet.clients.size === 0 && !Sn(t.afterState, (e, n) => t.beforeState.get(n) !== e) ? !1 : (re(t.deleteSet), zn(i, t), le(i, t.deleteSet), !0), Te = (i, t, e) => {
  const n = t._item;
  (n === null || n.id.clock < (i.beforeState.get(n.id.client) || 0) && !n.deleted) && G(i.changed, t, kt).add(e);
}, wt = (i, t) => {
  let e = i[t], n = i[t - 1], s = t;
  for (; s > 0; e = n, n = i[--s - 1]) {
    if (n.deleted === e.deleted && n.constructor === e.constructor && n.mergeWith(e)) {
      e instanceof k && e.parentSub !== null && /** @type {AbstractType<any>} */
      e.parent._map.get(e.parentSub) === e && e.parent._map.set(
        e.parentSub,
        /** @type {Item} */
        n
      );
      continue;
    }
    break;
  }
  const r = t - s;
  return r && i.splice(t + 1 - r, r), r;
}, ci = (i, t, e) => {
  for (const [n, s] of i.clients.entries()) {
    const r = (
      /** @type {Array<GC|Item>} */
      t.clients.get(n)
    );
    for (let l = s.length - 1; l >= 0; l--) {
      const o = s[l], c = o.clock + o.len;
      for (let d = v(r, o.clock), h = r[d]; d < r.length && h.id.clock < c; h = r[++d]) {
        const a = r[d];
        if (o.clock + o.len <= a.id.clock)
          break;
        a instanceof k && a.deleted && !a.keep && e(a) && a.gc(t, !1);
      }
    }
  }
}, hi = (i, t) => {
  i.clients.forEach((e, n) => {
    const s = (
      /** @type {Array<GC|Item>} */
      t.clients.get(n)
    );
    for (let r = e.length - 1; r >= 0; r--) {
      const l = e[r], o = _n(s.length - 1, 1 + v(s, l.clock + l.len - 1));
      for (let c = o, d = s[c]; c > 0 && d.id.clock >= l.clock; d = s[c])
        c -= 1 + wt(s, c);
    }
  });
}, We = (i, t) => {
  if (t < i.length) {
    const e = i[t], n = e.doc, s = n.store, r = e.deleteSet, l = e._mergeStructs;
    try {
      re(r), e.afterState = Mt(e.doc.store), n.emit("beforeObserverCalls", [e, n]);
      const o = [];
      e.changed.forEach(
        (c, d) => o.push(() => {
          (d._item === null || !d._item.deleted) && d._callObserver(e, c);
        })
      ), o.push(() => {
        e.changedParentTypes.forEach((c, d) => {
          d._dEH.l.length > 0 && (d._item === null || !d._item.deleted) && (c = c.filter(
            (h) => h.target._item === null || !h.target._item.deleted
          ), c.forEach((h) => {
            h.currentTarget = d, h._path = null;
          }), c.sort((h, a) => h.path.length - a.path.length), Ye(d._dEH, c, e));
        });
      }), o.push(() => n.emit("afterTransaction", [e, n])), Be(o, []), e._needFormattingCleanup && Li(e);
    } finally {
      n.gc && ci(r, s, n.gcFilter), hi(r, s), e.afterState.forEach((h, a) => {
        const u = e.beforeState.get(a) || 0;
        if (u !== h) {
          const f = (
            /** @type {Array<GC|Item>} */
            s.clients.get(a)
          ), g = At(v(f, u), 1);
          for (let p = f.length - 1; p >= g; )
            p -= 1 + wt(f, p);
        }
      });
      for (let h = l.length - 1; h >= 0; h--) {
        const { client: a, clock: u } = l[h].id, f = (
          /** @type {Array<GC|Item>} */
          s.clients.get(a)
        ), g = v(f, u);
        g + 1 < f.length && wt(f, g + 1) > 1 || g > 0 && wt(f, g);
      }
      if (!e.local && e.afterState.get(n.clientID) !== e.beforeState.get(n.clientID) && (En(Vn, Un, "[yjs] ", Nn, vn, "Changed the client-id because another client seems to be using it."), n.clientID = He()), n.emit("afterTransactionCleanup", [e, n]), n._observers.has("update")) {
        const h = new Gn();
        Ae(h, e) && n.emit("update", [h.toUint8Array(), e.origin, n, e]);
      }
      if (n._observers.has("updateV2")) {
        const h = new Ot();
        Ae(h, e) && n.emit("updateV2", [h.toUint8Array(), e.origin, n, e]);
      }
      const { subdocsAdded: o, subdocsLoaded: c, subdocsRemoved: d } = e;
      (o.size > 0 || d.size > 0 || c.size > 0) && (o.forEach((h) => {
        h.clientID = n.clientID, h.collectionid == null && (h.collectionid = n.collectionid), n.subdocs.add(h);
      }), d.forEach((h) => n.subdocs.delete(h)), n.emit("subdocs", [{ loaded: c, added: o, removed: d }, n, e]), d.forEach((h) => h.destroy())), i.length <= t + 1 ? (n._transactionCleanups = [], n.emit("afterAllTransactions", [n, i])) : We(i, t + 1);
    }
  }
}, m = (i, t, e = null, n = !0) => {
  const s = i._transactionCleanups;
  let r = !1, l = null;
  i._transaction === null && (r = !0, i._transaction = new oi(i, e, n), s.push(i._transaction), s.length === 1 && i.emit("beforeAllTransactions", [i]), i.emit("beforeTransaction", [i._transaction, i]));
  try {
    l = t(i._transaction);
  } finally {
    if (r) {
      const o = i._transaction === s[0];
      i._transaction = null, o && We(s, 0);
    }
  }
  return l;
};
class di {
  /**
   * @param {DeleteSet} deletions
   * @param {DeleteSet} insertions
   */
  constructor(t, e) {
    this.insertions = e, this.deletions = t, this.meta = /* @__PURE__ */ new Map();
  }
}
const Le = (i, t, e) => {
  q(i, e.deletions, (n) => {
    n instanceof k && t.scope.some((s) => s === i.doc || St(
      /** @type {AbstractType<any>} */
      s,
      n
    )) && ae(n, !1);
  });
}, Oe = (i, t, e) => {
  let n = null;
  const s = i.doc, r = i.scope;
  m(s, (o) => {
    for (; t.length > 0 && i.currStackItem === null; ) {
      const c = s.store, d = (
        /** @type {StackItem} */
        t.pop()
      ), h = /* @__PURE__ */ new Set(), a = [];
      let u = !1;
      q(o, d.insertions, (f) => {
        if (f instanceof k) {
          if (f.redone !== null) {
            let { item: g, diff: p } = ee(c, f.id);
            p > 0 && (g = A(o, w(g.id.client, g.id.clock + p))), f = g;
          }
          !f.deleted && r.some((g) => g === o.doc || St(
            /** @type {AbstractType<any>} */
            g,
            /** @type {Item} */
            f
          )) && a.push(f);
        }
      }), q(o, d.deletions, (f) => {
        f instanceof k && r.some((g) => g === o.doc || St(
          /** @type {AbstractType<any>} */
          g,
          f
        )) && // Never redo structs in stackItem.insertions because they were created and deleted in the same capture interval.
        !ht(d.insertions, f.id) && h.add(f);
      }), h.forEach((f) => {
        u = fn(o, f, h, d.insertions, i.ignoreRemoteMapChanges, i) !== null || u;
      });
      for (let f = a.length - 1; f >= 0; f--) {
        const g = a[f];
        i.deleteFilter(g) && (g.delete(o), u = !0);
      }
      i.currStackItem = u ? d : null;
    }
    o.changed.forEach((c, d) => {
      c.has(null) && d._searchMarker && (d._searchMarker.length = 0);
    }), n = o;
  }, i);
  const l = i.currStackItem;
  if (l != null) {
    const o = n.changedParentTypes;
    i.emit("stack-item-popped", [{ stackItem: l, type: e, changedParentTypes: o, origin: i }, i]), i.currStackItem = null;
  }
  return l;
};
class Es extends Ve {
  /**
   * @param {Doc|AbstractType<any>|Array<AbstractType<any>>} typeScope Limits the scope of the UndoManager. If this is set to a ydoc instance, all changes on that ydoc will be undone. If set to a specific type, only changes on that type or its children will be undone. Also accepts an array of types.
   * @param {UndoManagerOptions} options
   */
  constructor(t, {
    captureTimeout: e = 500,
    captureTransaction: n = (c) => !0,
    deleteFilter: s = () => !0,
    trackedOrigins: r = /* @__PURE__ */ new Set([null]),
    ignoreRemoteMapChanges: l = !1,
    doc: o = (
      /** @type {Doc} */
      ge(t) ? t[0].doc : t instanceof et ? t : t.doc
    )
  } = {}) {
    super(), this.scope = [], this.doc = o, this.addToScope(t), this.deleteFilter = s, r.add(this), this.trackedOrigins = r, this.captureTransaction = n, this.undoStack = [], this.redoStack = [], this.undoing = !1, this.redoing = !1, this.currStackItem = null, this.lastChange = 0, this.ignoreRemoteMapChanges = l, this.captureTimeout = e, this.afterTransactionHandler = (c) => {
      if (!this.captureTransaction(c) || !this.scope.some((S) => c.changedParentTypes.has(
        /** @type {AbstractType<any>} */
        S
      ) || S === this.doc) || !this.trackedOrigins.has(c.origin) && (!c.origin || !this.trackedOrigins.has(c.origin.constructor)))
        return;
      const d = this.undoing, h = this.redoing, a = d ? this.redoStack : this.undoStack;
      d ? this.stopCapturing() : h || this.clear(!1, !0);
      const u = new tt();
      c.afterState.forEach((S, b) => {
        const ue = c.beforeState.get(b) || 0, fe = S - ue;
        fe > 0 && rt(u, b, ue, fe);
      });
      const f = In();
      let g = !1;
      if (this.lastChange > 0 && f - this.lastChange < this.captureTimeout && a.length > 0 && !d && !h) {
        const S = a[a.length - 1];
        S.deletions = Qt([S.deletions, c.deleteSet]), S.insertions = Qt([S.insertions, u]);
      } else
        a.push(new di(c.deleteSet, u)), g = !0;
      !d && !h && (this.lastChange = f), q(
        c,
        c.deleteSet,
        /** @param {Item|GC} item */
        (S) => {
          S instanceof k && this.scope.some((b) => b === c.doc || St(
            /** @type {AbstractType<any>} */
            b,
            S
          )) && ae(S, !0);
        }
      );
      const p = [{ stackItem: a[a.length - 1], origin: c.origin, type: d ? "redo" : "undo", changedParentTypes: c.changedParentTypes }, this];
      g ? this.emit("stack-item-added", p) : this.emit("stack-item-updated", p);
    }, this.doc.on("afterTransaction", this.afterTransactionHandler), this.doc.on("destroy", () => {
      this.destroy();
    });
  }
  /**
   * Extend the scope.
   *
   * @param {Array<AbstractType<any> | Doc> | AbstractType<any> | Doc} ytypes
   */
  addToScope(t) {
    const e = new Set(this.scope);
    t = ge(t) ? t : [t], t.forEach((n) => {
      e.has(n) || (e.add(n), (n instanceof E ? n.doc !== this.doc : n !== this.doc) && Je("[yjs#509] Not same Y.Doc"), this.scope.push(n));
    });
  }
  /**
   * @param {any} origin
   */
  addTrackedOrigin(t) {
    this.trackedOrigins.add(t);
  }
  /**
   * @param {any} origin
   */
  removeTrackedOrigin(t) {
    this.trackedOrigins.delete(t);
  }
  clear(t = !0, e = !0) {
    (t && this.canUndo() || e && this.canRedo()) && this.doc.transact((n) => {
      t && (this.undoStack.forEach((s) => Le(n, this, s)), this.undoStack = []), e && (this.redoStack.forEach((s) => Le(n, this, s)), this.redoStack = []), this.emit("stack-cleared", [{ undoStackCleared: t, redoStackCleared: e }]);
    });
  }
  /**
   * UndoManager merges Undo-StackItem if they are created within time-gap
   * smaller than `options.captureTimeout`. Call `um.stopCapturing()` so that the next
   * StackItem won't be merged.
   *
   *
   * @example
   *     // without stopCapturing
   *     ytext.insert(0, 'a')
   *     ytext.insert(1, 'b')
   *     um.undo()
   *     ytext.toString() // => '' (note that 'ab' was removed)
   *     // with stopCapturing
   *     ytext.insert(0, 'a')
   *     um.stopCapturing()
   *     ytext.insert(0, 'b')
   *     um.undo()
   *     ytext.toString() // => 'a' (note that only 'b' was removed)
   *
   */
  stopCapturing() {
    this.lastChange = 0;
  }
  /**
   * Undo last changes on type.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  undo() {
    this.undoing = !0;
    let t;
    try {
      t = Oe(this, this.undoStack, "undo");
    } finally {
      this.undoing = !1;
    }
    return t;
  }
  /**
   * Redo last undo operation.
   *
   * @return {StackItem?} Returns StackItem if a change was applied
   */
  redo() {
    this.redoing = !0;
    let t;
    try {
      t = Oe(this, this.redoStack, "redo");
    } finally {
      this.redoing = !1;
    }
    return t;
  }
  /**
   * Are undo steps available?
   *
   * @return {boolean} `true` if undo is possible
   */
  canUndo() {
    return this.undoStack.length > 0;
  }
  /**
   * Are redo steps available?
   *
   * @return {boolean} `true` if redo is possible
   */
  canRedo() {
    return this.redoStack.length > 0;
  }
  destroy() {
    this.trackedOrigins.delete(this), this.doc.off("afterTransaction", this.afterTransactionHandler), super.destroy();
  }
}
function* ai(i) {
  const t = D(i.restDecoder);
  for (let e = 0; e < t; e++) {
    const n = D(i.restDecoder), s = i.readClient();
    let r = D(i.restDecoder);
    for (let l = 0; l < n; l++) {
      const o = i.readInfo();
      if (o === 10) {
        const c = D(i.restDecoder);
        yield new O(w(s, r), c), r += c;
      } else if ((Lt & o) !== 0) {
        const c = (o & (H | J)) === 0, d = new k(
          w(s, r),
          null,
          // left
          (o & J) === J ? i.readLeftID() : null,
          // origin
          null,
          // right
          (o & H) === H ? i.readRightID() : null,
          // right origin
          // @ts-ignore Force writing a string here.
          c ? i.readParentInfo() ? i.readString() : i.readLeftID() : null,
          // parent
          c && (o & st) === st ? i.readString() : null,
          // parentSub
          gn(i, o)
          // item content
        );
        yield d, r += d.length;
      } else {
        const c = i.readLen();
        yield new T(w(s, r), c), r += c;
      }
    }
  }
}
class ui {
  /**
   * @param {UpdateDecoderV1 | UpdateDecoderV2} decoder
   * @param {boolean} filterSkips
   */
  constructor(t, e) {
    this.gen = ai(t), this.curr = null, this.done = !1, this.filterSkips = e, this.next();
  }
  /**
   * @return {Item | GC | Skip |null}
   */
  next() {
    do
      this.curr = this.gen.next().value || null;
    while (this.filterSkips && this.curr !== null && this.curr.constructor === O);
    return this.curr;
  }
}
class fi {
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  constructor(t) {
    this.currClient = 0, this.startClock = 0, this.written = 0, this.encoder = t, this.clientStructs = [];
  }
}
const gi = (i, t) => {
  if (i.constructor === T) {
    const { client: e, clock: n } = i.id;
    return new T(w(e, n + t), i.length - t);
  } else if (i.constructor === O) {
    const { client: e, clock: n } = i.id;
    return new O(w(e, n + t), i.length - t);
  } else {
    const e = (
      /** @type {Item} */
      i
    ), { client: n, clock: s } = e.id;
    return new k(
      w(n, s + t),
      null,
      w(n, s + t - 1),
      null,
      e.rightOrigin,
      e.parent,
      e.parentSub,
      e.content.splice(t)
    );
  }
}, Me = (i, t = _t, e = Ot) => {
  if (i.length === 1)
    return i[0];
  const n = i.map((h) => new t(ie(h)));
  let s = n.map((h) => new ui(h, !0)), r = null;
  const l = new e(), o = new fi(l);
  for (; s = s.filter((u) => u.curr !== null), s.sort(
    /** @type {function(any,any):number} */
    (u, f) => {
      if (u.curr.id.client === f.curr.id.client) {
        const g = u.curr.id.clock - f.curr.id.clock;
        return g === 0 ? u.curr.constructor === f.curr.constructor ? 0 : u.curr.constructor === O ? 1 : -1 : g;
      } else
        return f.curr.id.client - u.curr.id.client;
    }
  ), s.length !== 0; ) {
    const h = s[0], a = (
      /** @type {Item | GC} */
      h.curr.id.client
    );
    if (r !== null) {
      let u = (
        /** @type {Item | GC | null} */
        h.curr
      ), f = !1;
      for (; u !== null && u.id.clock + u.length <= r.struct.id.clock + r.struct.length && u.id.client >= r.struct.id.client; )
        u = h.next(), f = !0;
      if (u === null || // current decoder is empty
      u.id.client !== a || // check whether there is another decoder that has has updates from `firstClient`
      f && u.id.clock > r.struct.id.clock + r.struct.length)
        continue;
      if (a !== r.struct.id.client)
        it(o, r.struct, r.offset), r = { struct: u, offset: 0 }, h.next();
      else if (r.struct.id.clock + r.struct.length < u.id.clock)
        if (r.struct.constructor === O)
          r.struct.length = u.id.clock + u.length - r.struct.id.clock;
        else {
          it(o, r.struct, r.offset);
          const g = u.id.clock - r.struct.id.clock - r.struct.length;
          r = { struct: new O(w(a, r.struct.id.clock + r.struct.length), g), offset: 0 };
        }
      else {
        const g = r.struct.id.clock + r.struct.length - u.id.clock;
        g > 0 && (r.struct.constructor === O ? r.struct.length -= g : u = gi(u, g)), r.struct.mergeWith(
          /** @type {any} */
          u
        ) || (it(o, r.struct, r.offset), r = { struct: u, offset: 0 }, h.next());
      }
    } else
      r = { struct: (
        /** @type {Item | GC} */
        h.curr
      ), offset: 0 }, h.next();
    for (let u = h.curr; u !== null && u.id.client === a && u.id.clock === r.struct.id.clock + r.struct.length && u.constructor !== O; u = h.next())
      it(o, r.struct, r.offset), r = { struct: u, offset: 0 };
  }
  r !== null && (it(o, r.struct, r.offset), r = null), pi(o);
  const c = n.map((h) => Hn(h)), d = Qt(c);
  return le(l, d), l.toUint8Array();
}, ze = (i) => {
  i.written > 0 && (i.clientStructs.push({ written: i.written, restEncoder: B(i.encoder.restEncoder) }), i.encoder.restEncoder = Tt(), i.written = 0);
}, it = (i, t, e) => {
  i.written > 0 && i.currClient !== t.id.client && ze(i), i.written === 0 && (i.currClient = t.id.client, i.encoder.writeClient(t.id.client), _(i.encoder.restEncoder, t.id.clock + e)), t.write(i.encoder, e), i.written++;
}, pi = (i) => {
  ze(i);
  const t = i.encoder.restEncoder;
  _(t, i.clientStructs.length);
  for (let e = 0; e < i.clientStructs.length; e++) {
    const n = i.clientStructs[e];
    _(t, n.written), Fe(t, n.restEncoder);
  }
}, xe = "You must not compute changes after the event-handler fired.";
class xt {
  /**
   * @param {T} target The changed type.
   * @param {Transaction} transaction
   */
  constructor(t, e) {
    this.target = t, this.currentTarget = t, this.transaction = e, this._changes = null, this._keys = null, this._delta = null, this._path = null;
  }
  /**
   * Computes the path from `y` to the changed type.
   *
   * @todo v14 should standardize on path: Array<{parent, index}> because that is easier to work with.
   *
   * The following property holds:
   * @example
   *   let type = y
   *   event.path.forEach(dir => {
   *     type = type.get(dir)
   *   })
   *   type === event.target // => true
   */
  get path() {
    return this._path || (this._path = wi(this.currentTarget, this.target));
  }
  /**
   * Check if a struct is deleted by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  deletes(t) {
    return ht(this.transaction.deleteSet, t.id);
  }
  /**
   * @type {Map<string, { action: 'add' | 'update' | 'delete', oldValue: any, newValue: any }>}
   */
  get keys() {
    if (this._keys === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw mt(xe);
      const t = /* @__PURE__ */ new Map(), e = this.target;
      /** @type Set<string|null> */
      this.transaction.changed.get(e).forEach((s) => {
        if (s !== null) {
          const r = (
            /** @type {Item} */
            e._map.get(s)
          );
          let l, o;
          if (this.adds(r)) {
            let c = r.left;
            for (; c !== null && this.adds(c); )
              c = c.left;
            if (this.deletes(r))
              if (c !== null && this.deletes(c))
                l = "delete", o = Nt(c.content.getContent());
              else
                return;
            else
              c !== null && this.deletes(c) ? (l = "update", o = Nt(c.content.getContent())) : (l = "add", o = void 0);
          } else if (this.deletes(r))
            l = "delete", o = Nt(
              /** @type {Item} */
              r.content.getContent()
            );
          else
            return;
          t.set(s, { action: l, oldValue: o });
        }
      }), this._keys = t;
    }
    return this._keys;
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {Array<{insert?: string | Array<any> | object | AbstractType<any>, retain?: number, delete?: number, attributes?: Object<string, any>}>}
   */
  get delta() {
    return this.changes.delta;
  }
  /**
   * Check if a struct is added by this event.
   *
   * In contrast to change.deleted, this method also returns true if the struct was added and then deleted.
   *
   * @param {AbstractStruct} struct
   * @return {boolean}
   */
  adds(t) {
    return t.id.clock >= (this.transaction.beforeState.get(t.id.client) || 0);
  }
  /**
   * This is a computed property. Note that this can only be safely computed during the
   * event call. Computing this property after other changes happened might result in
   * unexpected behavior (incorrect computation of deltas). A safe way to collect changes
   * is to store the `changes` or the `delta` object. Avoid storing the `transaction` object.
   *
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    let t = this._changes;
    if (t === null) {
      if (this.transaction.doc._transactionCleanups.length === 0)
        throw mt(xe);
      const e = this.target, n = kt(), s = kt(), r = [];
      if (t = {
        added: n,
        deleted: s,
        delta: r,
        keys: this.keys
      }, /** @type Set<string|null> */
      this.transaction.changed.get(e).has(null)) {
        let o = null;
        const c = () => {
          o && r.push(o);
        };
        for (let d = e._start; d !== null; d = d.right)
          d.deleted ? this.deletes(d) && !this.adds(d) && ((o === null || o.delete === void 0) && (c(), o = { delete: 0 }), o.delete += d.length, s.add(d)) : this.adds(d) ? ((o === null || o.insert === void 0) && (c(), o = { insert: [] }), o.insert = o.insert.concat(d.content.getContent()), n.add(d)) : ((o === null || o.retain === void 0) && (c(), o = { retain: 0 }), o.retain += d.length);
        o !== null && o.retain === void 0 && c();
      }
      this._changes = t;
    }
    return (
      /** @type {any} */
      t
    );
  }
}
const wi = (i, t) => {
  const e = [];
  for (; t._item !== null && t !== i; ) {
    if (t._item.parentSub !== null)
      e.unshift(t._item.parentSub);
    else {
      let n = 0, s = (
        /** @type {AbstractType<any>} */
        t._item.parent._start
      );
      for (; s !== t._item && s !== null; )
        !s.deleted && s.countable && (n += s.length), s = s.right;
      e.unshift(n);
    }
    t = /** @type {AbstractType<any>} */
    t._item.parent;
  }
  return e;
}, I = () => {
  Je("Invalid access: Add Yjs type to a document before reading data.");
}, qe = 80;
let oe = 0;
class mi {
  /**
   * @param {Item} p
   * @param {number} index
   */
  constructor(t, e) {
    t.marker = !0, this.p = t, this.index = e, this.timestamp = oe++;
  }
}
const ki = (i) => {
  i.timestamp = oe++;
}, Qe = (i, t, e) => {
  i.p.marker = !1, i.p = t, t.marker = !0, i.index = e, i.timestamp = oe++;
}, _i = (i, t, e) => {
  if (i.length >= qe) {
    const n = i.reduce((s, r) => s.timestamp < r.timestamp ? s : r);
    return Qe(n, t, e), n;
  } else {
    const n = new mi(t, e);
    return i.push(n), n;
  }
}, Rt = (i, t) => {
  if (i._start === null || t === 0 || i._searchMarker === null)
    return null;
  const e = i._searchMarker.length === 0 ? null : i._searchMarker.reduce((r, l) => Ut(t - r.index) < Ut(t - l.index) ? r : l);
  let n = i._start, s = 0;
  for (e !== null && (n = e.p, s = e.index, ki(e)); n.right !== null && s < t; ) {
    if (!n.deleted && n.countable) {
      if (t < s + n.length)
        break;
      s += n.length;
    }
    n = n.right;
  }
  for (; n.left !== null && s > t; )
    n = n.left, !n.deleted && n.countable && (s -= n.length);
  for (; n.left !== null && n.left.id.client === n.id.client && n.left.id.clock + n.left.length === n.id.clock; )
    n = n.left, !n.deleted && n.countable && (s -= n.length);
  return e !== null && Ut(e.index - s) < /** @type {YText|YArray<any>} */
  n.parent.length / qe ? (Qe(e, n, s), e) : _i(i._searchMarker, n, s);
}, lt = (i, t, e) => {
  for (let n = i.length - 1; n >= 0; n--) {
    const s = i[n];
    if (e > 0) {
      let r = s.p;
      for (r.marker = !1; r && (r.deleted || !r.countable); )
        r = r.left, r && !r.deleted && r.countable && (s.index -= r.length);
      if (r === null || r.marker === !0) {
        i.splice(n, 1);
        continue;
      }
      s.p = r, r.marker = !0;
    }
    (t < s.index || e > 0 && t === s.index) && (s.index = At(t, s.index + e));
  }
}, vt = (i, t, e) => {
  const n = i, s = t.changedParentTypes;
  for (; G(s, i, () => []).push(e), i._item !== null; )
    i = /** @type {AbstractType<any>} */
    i._item.parent;
  Ye(n._eH, e, t);
};
class E {
  constructor() {
    this._item = null, this._map = /* @__PURE__ */ new Map(), this._start = null, this.doc = null, this._length = 0, this._eH = Ce(), this._dEH = Ce(), this._searchMarker = null;
  }
  /**
   * @return {AbstractType<any>|null}
   */
  get parent() {
    return this._item ? (
      /** @type {AbstractType<any>} */
      this._item.parent
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item|null} item
   */
  _integrate(t, e) {
    this.doc = t, this._item = e;
  }
  /**
   * @return {AbstractType<EventType>}
   */
  _copy() {
    throw R();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {AbstractType<EventType>}
   */
  clone() {
    throw R();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} _encoder
   */
  _write(t) {
  }
  /**
   * The first non-deleted item
   */
  get _first() {
    let t = this._start;
    for (; t !== null && t.deleted; )
      t = t.right;
    return t;
  }
  /**
   * Creates YEvent and calls all type observers.
   * Must be implemented by each type.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} _parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, e) {
    !t.local && this._searchMarker && (this._searchMarker.length = 0);
  }
  /**
   * Observe all events that are created on this type.
   *
   * @param {function(EventType, Transaction):void} f Observer function
   */
  observe(t) {
    De(this._eH, t);
  }
  /**
   * Observe all events that are created by this type and its children.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  observeDeep(t) {
    De(this._dEH, t);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(EventType,Transaction):void} f Observer function
   */
  unobserve(t) {
    Ee(this._eH, t);
  }
  /**
   * Unregister an observer function.
   *
   * @param {function(Array<YEvent<any>>,Transaction):void} f Observer function
   */
  unobserveDeep(t) {
    Ee(this._dEH, t);
  }
  /**
   * @abstract
   * @return {any}
   */
  toJSON() {
  }
}
const Ze = (i, t, e) => {
  i.doc ?? I(), t < 0 && (t = i._length + t), e < 0 && (e = i._length + e);
  let n = e - t;
  const s = [];
  let r = i._start;
  for (; r !== null && n > 0; ) {
    if (r.countable && !r.deleted) {
      const l = r.content.getContent();
      if (l.length <= t)
        t -= l.length;
      else {
        for (let o = t; o < l.length && n > 0; o++)
          s.push(l[o]), n--;
        t = 0;
      }
    }
    r = r.right;
  }
  return s;
}, Pe = (i) => {
  i.doc ?? I();
  const t = [];
  let e = i._start;
  for (; e !== null; ) {
    if (e.countable && !e.deleted) {
      const n = e.content.getContent();
      for (let s = 0; s < n.length; s++)
        t.push(n[s]);
    }
    e = e.right;
  }
  return t;
}, Is = (i, t) => {
  const e = [];
  let n = i._start;
  for (; n !== null; ) {
    if (n.countable && F(n, t)) {
      const s = n.content.getContent();
      for (let r = 0; r < s.length; r++)
        e.push(s[r]);
    }
    n = n.right;
  }
  return e;
}, ot = (i, t) => {
  let e = 0, n = i._start;
  for (i.doc ?? I(); n !== null; ) {
    if (n.countable && !n.deleted) {
      const s = n.content.getContent();
      for (let r = 0; r < s.length; r++)
        t(s[r], e++, i);
    }
    n = n.right;
  }
}, tn = (i, t) => {
  const e = [];
  return ot(i, (n, s) => {
    e.push(t(n, s, i));
  }), e;
}, Si = (i) => {
  let t = i._start, e = null, n = 0;
  return {
    [Symbol.iterator]() {
      return this;
    },
    next: () => {
      if (e === null) {
        for (; t !== null && t.deleted; )
          t = t.right;
        if (t === null)
          return {
            done: !0,
            value: void 0
          };
        e = t.content.getContent(), n = 0, t = t.right;
      }
      const s = e[n++];
      return e.length <= n && (e = null), {
        done: !1,
        value: s
      };
    }
  };
}, en = (i, t) => {
  i.doc ?? I();
  const e = Rt(i, t);
  let n = i._start;
  for (e !== null && (n = e.p, t -= e.index); n !== null; n = n.right)
    if (!n.deleted && n.countable) {
      if (t < n.length)
        return n.content.getContent()[t];
      t -= n.length;
    }
}, bt = (i, t, e, n) => {
  let s = e;
  const r = i.doc, l = r.clientID, o = r.store, c = e === null ? t._start : e.right;
  let d = [];
  const h = () => {
    d.length > 0 && (s = new k(w(l, y(o, l)), s, s && s.lastId, c, c && c.id, t, null, new Y(d)), s.integrate(i, 0), d = []);
  };
  n.forEach((a) => {
    if (a === null)
      d.push(a);
    else
      switch (a.constructor) {
        case Number:
        case Object:
        case Boolean:
        case Array:
        case String:
          d.push(a);
          break;
        default:
          switch (h(), a.constructor) {
            case Uint8Array:
            case ArrayBuffer:
              s = new k(w(l, y(o, l)), s, s && s.lastId, c, c && c.id, t, null, new dt(new Uint8Array(
                /** @type {Uint8Array} */
                a
              ))), s.integrate(i, 0);
              break;
            case et:
              s = new k(w(l, y(o, l)), s, s && s.lastId, c, c && c.id, t, null, new at(
                /** @type {Doc} */
                a
              )), s.integrate(i, 0);
              break;
            default:
              if (a instanceof E)
                s = new k(w(l, y(o, l)), s, s && s.lastId, c, c && c.id, t, null, new U(a)), s.integrate(i, 0);
              else
                throw new Error("Unexpected content type in insert operation");
          }
      }
  }), h();
}, nn = () => mt("Length exceeded!"), sn = (i, t, e, n) => {
  if (e > t._length)
    throw nn();
  if (e === 0)
    return t._searchMarker && lt(t._searchMarker, e, n.length), bt(i, t, null, n);
  const s = e, r = Rt(t, e);
  let l = t._start;
  for (r !== null && (l = r.p, e -= r.index, e === 0 && (l = l.prev, e += l && l.countable && !l.deleted ? l.length : 0)); l !== null; l = l.right)
    if (!l.deleted && l.countable) {
      if (e <= l.length) {
        e < l.length && A(i, w(l.id.client, l.id.clock + e));
        break;
      }
      e -= l.length;
    }
  return t._searchMarker && lt(t._searchMarker, s, n.length), bt(i, t, l, n);
}, bi = (i, t, e) => {
  let s = (t._searchMarker || []).reduce((r, l) => l.index > r.index ? l : r, { index: 0, p: t._start }).p;
  if (s)
    for (; s.right; )
      s = s.right;
  return bt(i, t, s, e);
}, rn = (i, t, e, n) => {
  if (n === 0)
    return;
  const s = e, r = n, l = Rt(t, e);
  let o = t._start;
  for (l !== null && (o = l.p, e -= l.index); o !== null && e > 0; o = o.right)
    !o.deleted && o.countable && (e < o.length && A(i, w(o.id.client, o.id.clock + e)), e -= o.length);
  for (; n > 0 && o !== null; )
    o.deleted || (n < o.length && A(i, w(o.id.client, o.id.clock + n)), o.delete(i), n -= o.length), o = o.right;
  if (n > 0)
    throw nn();
  t._searchMarker && lt(
    t._searchMarker,
    s,
    -r + n
    /* in case we remove the above exception */
  );
}, yt = (i, t, e) => {
  const n = t._map.get(e);
  n !== void 0 && n.delete(i);
}, ce = (i, t, e, n) => {
  const s = t._map.get(e) || null, r = i.doc, l = r.clientID;
  let o;
  if (n == null)
    o = new Y([n]);
  else
    switch (n.constructor) {
      case Number:
      case Object:
      case Boolean:
      case Array:
      case String:
      case Date:
      case BigInt:
        o = new Y([n]);
        break;
      case Uint8Array:
        o = new dt(
          /** @type {Uint8Array} */
          n
        );
        break;
      case et:
        o = new at(
          /** @type {Doc} */
          n
        );
        break;
      default:
        if (n instanceof E)
          o = new U(n);
        else
          throw new Error("Unexpected content type");
    }
  new k(w(l, y(r.store, l)), s, s && s.lastId, null, null, t, e, o).integrate(i, 0);
}, he = (i, t) => {
  i.doc ?? I();
  const e = i._map.get(t);
  return e !== void 0 && !e.deleted ? e.content.getContent()[e.length - 1] : void 0;
}, ln = (i) => {
  const t = {};
  return i.doc ?? I(), i._map.forEach((e, n) => {
    e.deleted || (t[n] = e.content.getContent()[e.length - 1]);
  }), t;
}, on = (i, t) => {
  i.doc ?? I();
  const e = i._map.get(t);
  return e !== void 0 && !e.deleted;
}, yi = (i, t) => {
  const e = {};
  return i._map.forEach((n, s) => {
    let r = n;
    for (; r !== null && (!t.sv.has(r.id.client) || r.id.clock >= (t.sv.get(r.id.client) || 0)); )
      r = r.left;
    r !== null && F(r, t) && (e[s] = r.content.getContent()[r.length - 1]);
  }), e;
}, gt = (i) => (i.doc ?? I(), An(
  i._map.entries(),
  /** @param {any} entry */
  (t) => !t[1].deleted
));
class Ci extends xt {
}
class W extends E {
  constructor() {
    super(), this._prelimContent = [], this._searchMarker = [];
  }
  /**
   * Construct a new YArray containing the specified items.
   * @template {Object<string,any>|Array<any>|number|null|string|Uint8Array} T
   * @param {Array<T>} items
   * @return {YArray<T>}
   */
  static from(t) {
    const e = new W();
    return e.push(t), e;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, e) {
    super._integrate(t, e), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  /**
   * @return {YArray<T>}
   */
  _copy() {
    return new W();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YArray<T>}
   */
  clone() {
    const t = new W();
    return t.insert(0, this.toArray().map(
      (e) => e instanceof E ? (
        /** @type {typeof el} */
        e.clone()
      ) : e
    )), t;
  }
  get length() {
    return this.doc ?? I(), this._length;
  }
  /**
   * Creates YArrayEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, e) {
    super._callObserver(t, e), vt(this, t, new Ci(this, t));
  }
  /**
   * Inserts new content at an index.
   *
   * Important: This function expects an array of content. Not just a content
   * object. The reason for this "weirdness" is that inserting several elements
   * is very efficient when it is done as a single operation.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  yarray.insert(0, ['a'])
   *  // Insert numbers 1, 2 at position 1
   *  yarray.insert(1, [1, 2])
   *
   * @param {number} index The index to insert content at.
   * @param {Array<T>} content The array of content
   */
  insert(t, e) {
    this.doc !== null ? m(this.doc, (n) => {
      sn(
        n,
        this,
        t,
        /** @type {any} */
        e
      );
    }) : this._prelimContent.splice(t, 0, ...e);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<T>} content Array of content to append.
   *
   * @todo Use the following implementation in all types.
   */
  push(t) {
    this.doc !== null ? m(this.doc, (e) => {
      bi(
        e,
        this,
        /** @type {any} */
        t
      );
    }) : this._prelimContent.push(...t);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<T>} content Array of content to prepend.
   */
  unshift(t) {
    this.insert(0, t);
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} length The number of elements to remove. Defaults to 1.
   */
  delete(t, e = 1) {
    this.doc !== null ? m(this.doc, (n) => {
      rn(n, this, t, e);
    }) : this._prelimContent.splice(t, e);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {T}
   */
  get(t) {
    return en(this, t);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<T>}
   */
  toArray() {
    return Pe(this);
  }
  /**
   * Returns a portion of this YArray into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<T>}
   */
  slice(t = 0, e = this.length) {
    return Ze(this, t, e);
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Array<any>}
   */
  toJSON() {
    return this.map((t) => t instanceof E ? t.toJSON() : t);
  }
  /**
   * Returns an Array with the result of calling a provided function on every
   * element of this YArray.
   *
   * @template M
   * @param {function(T,number,YArray<T>):M} f Function that produces an element of the new Array
   * @return {Array<M>} A new array with each element being the result of the
   *                 callback function
   */
  map(t) {
    return tn(
      this,
      /** @type {any} */
      t
    );
  }
  /**
   * Executes a provided function once on every element of this YArray.
   *
   * @param {function(T,number,YArray<T>):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    ot(this, t);
  }
  /**
   * @return {IterableIterator<T>}
   */
  [Symbol.iterator]() {
    return Si(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(Wi);
  }
}
const Di = (i) => new W();
class Ei extends xt {
  /**
   * @param {YMap<T>} ymap The YArray that changed.
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed.
   */
  constructor(t, e, n) {
    super(t, e), this.keysChanged = n;
  }
}
class Q extends E {
  /**
   *
   * @param {Iterable<readonly [string, any]>=} entries - an optional iterable to initialize the YMap
   */
  constructor(t) {
    super(), this._prelimContent = null, t === void 0 ? this._prelimContent = /* @__PURE__ */ new Map() : this._prelimContent = new Map(t);
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, e) {
    super._integrate(t, e), this._prelimContent.forEach((n, s) => {
      this.set(s, n);
    }), this._prelimContent = null;
  }
  /**
   * @return {YMap<MapType>}
   */
  _copy() {
    return new Q();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YMap<MapType>}
   */
  clone() {
    const t = new Q();
    return this.forEach((e, n) => {
      t.set(n, e instanceof E ? (
        /** @type {typeof value} */
        e.clone()
      ) : e);
    }), t;
  }
  /**
   * Creates YMapEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, e) {
    vt(this, t, new Ei(this, t, e));
  }
  /**
   * Transforms this Shared Type to a JSON object.
   *
   * @return {Object<string,any>}
   */
  toJSON() {
    this.doc ?? I();
    const t = {};
    return this._map.forEach((e, n) => {
      if (!e.deleted) {
        const s = e.content.getContent()[e.length - 1];
        t[n] = s instanceof E ? s.toJSON() : s;
      }
    }), t;
  }
  /**
   * Returns the size of the YMap (count of key/value pairs)
   *
   * @return {number}
   */
  get size() {
    return [...gt(this)].length;
  }
  /**
   * Returns the keys for each element in the YMap Type.
   *
   * @return {IterableIterator<string>}
   */
  keys() {
    return Gt(
      gt(this),
      /** @param {any} v */
      (t) => t[0]
    );
  }
  /**
   * Returns the values for each element in the YMap Type.
   *
   * @return {IterableIterator<MapType>}
   */
  values() {
    return Gt(
      gt(this),
      /** @param {any} v */
      (t) => t[1].content.getContent()[t[1].length - 1]
    );
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  entries() {
    return Gt(
      gt(this),
      /** @param {any} v */
      (t) => (
        /** @type {any} */
        [t[0], t[1].content.getContent()[t[1].length - 1]]
      )
    );
  }
  /**
   * Executes a provided function on once on every key-value pair.
   *
   * @param {function(MapType,string,YMap<MapType>):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    this.doc ?? I(), this._map.forEach((e, n) => {
      e.deleted || t(e.content.getContent()[e.length - 1], n, this);
    });
  }
  /**
   * Returns an Iterator of [key, value] pairs
   *
   * @return {IterableIterator<[string, MapType]>}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * Remove a specified element from this YMap.
   *
   * @param {string} key The key of the element to remove.
   */
  delete(t) {
    this.doc !== null ? m(this.doc, (e) => {
      yt(e, this, t);
    }) : this._prelimContent.delete(t);
  }
  /**
   * Adds or updates an element with a specified key and value.
   * @template {MapType} VAL
   *
   * @param {string} key The key of the element to add to this YMap
   * @param {VAL} value The value of the element to add
   * @return {VAL}
   */
  set(t, e) {
    return this.doc !== null ? m(this.doc, (n) => {
      ce(
        n,
        this,
        t,
        /** @type {any} */
        e
      );
    }) : this._prelimContent.set(t, e), e;
  }
  /**
   * Returns a specified element from this YMap.
   *
   * @param {string} key
   * @return {MapType|undefined}
   */
  get(t) {
    return (
      /** @type {any} */
      he(this, t)
    );
  }
  /**
   * Returns a boolean indicating whether the specified key exists or not.
   *
   * @param {string} key The key to test.
   * @return {boolean}
   */
  has(t) {
    return on(this, t);
  }
  /**
   * Removes all elements from this YMap.
   */
  clear() {
    this.doc !== null ? m(this.doc, (t) => {
      this.forEach(function(e, n, s) {
        yt(t, s, n);
      });
    }) : this._prelimContent.clear();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(zi);
  }
}
const Ii = (i) => new Q(), V = (i, t) => i === t || typeof i == "object" && typeof t == "object" && i && t && On(i, t);
class te {
  /**
   * @param {Item|null} left
   * @param {Item|null} right
   * @param {number} index
   * @param {Map<string,any>} currentAttributes
   */
  constructor(t, e, n, s) {
    this.left = t, this.right = e, this.index = n, this.currentAttributes = s;
  }
  /**
   * Only call this if you know that this.right is defined
   */
  forward() {
    this.right === null && M(), this.right.content.constructor === C ? this.right.deleted || nt(
      this.currentAttributes,
      /** @type {ContentFormat} */
      this.right.content
    ) : this.right.deleted || (this.index += this.right.length), this.left = this.right, this.right = this.right.right;
  }
}
const Re = (i, t, e) => {
  for (; t.right !== null && e > 0; )
    t.right.content.constructor === C ? t.right.deleted || nt(
      t.currentAttributes,
      /** @type {ContentFormat} */
      t.right.content
    ) : t.right.deleted || (e < t.right.length && A(i, w(t.right.id.client, t.right.id.clock + e)), t.index += t.right.length, e -= t.right.length), t.left = t.right, t.right = t.right.right;
  return t;
}, pt = (i, t, e, n) => {
  const s = /* @__PURE__ */ new Map(), r = n ? Rt(t, e) : null;
  if (r) {
    const l = new te(r.p.left, r.p, r.index, s);
    return Re(i, l, e - r.index);
  } else {
    const l = new te(null, t._start, 0, s);
    return Re(i, l, e);
  }
}, cn = (i, t, e, n) => {
  for (; e.right !== null && (e.right.deleted === !0 || e.right.content.constructor === C && V(
    n.get(
      /** @type {ContentFormat} */
      e.right.content.key
    ),
    /** @type {ContentFormat} */
    e.right.content.value
  )); )
    e.right.deleted || n.delete(
      /** @type {ContentFormat} */
      e.right.content.key
    ), e.forward();
  const s = i.doc, r = s.clientID;
  n.forEach((l, o) => {
    const c = e.left, d = e.right, h = new k(w(r, y(s.store, r)), c, c && c.lastId, d, d && d.id, t, null, new C(o, l));
    h.integrate(i, 0), e.right = h, e.forward();
  });
}, nt = (i, t) => {
  const { key: e, value: n } = t;
  n === null ? i.delete(e) : i.set(e, n);
}, hn = (i, t) => {
  for (; i.right !== null; ) {
    if (!(i.right.deleted || i.right.content.constructor === C && V(
      t[
        /** @type {ContentFormat} */
        i.right.content.key
      ] ?? null,
      /** @type {ContentFormat} */
      i.right.content.value
    ))) break;
    i.forward();
  }
}, dn = (i, t, e, n) => {
  const s = i.doc, r = s.clientID, l = /* @__PURE__ */ new Map();
  for (const o in n) {
    const c = n[o], d = e.currentAttributes.get(o) ?? null;
    if (!V(d, c)) {
      l.set(o, d);
      const { left: h, right: a } = e;
      e.right = new k(w(r, y(s.store, r)), h, h && h.lastId, a, a && a.id, t, null, new C(o, c)), e.right.integrate(i, 0), e.forward();
    }
  }
  return l;
}, $t = (i, t, e, n, s) => {
  e.currentAttributes.forEach((u, f) => {
    s[f] === void 0 && (s[f] = null);
  });
  const r = i.doc, l = r.clientID;
  hn(e, s);
  const o = dn(i, t, e, s), c = n.constructor === String ? new N(
    /** @type {string} */
    n
  ) : n instanceof E ? new U(n) : new $(n);
  let { left: d, right: h, index: a } = e;
  t._searchMarker && lt(t._searchMarker, e.index, c.getLength()), h = new k(w(l, y(r.store, l)), d, d && d.lastId, h, h && h.id, t, null, c), h.integrate(i, 0), e.right = h, e.index = a, e.forward(), cn(i, t, e, o);
}, ve = (i, t, e, n, s) => {
  const r = i.doc, l = r.clientID;
  hn(e, s);
  const o = dn(i, t, e, s);
  t: for (; e.right !== null && (n > 0 || o.size > 0 && (e.right.deleted || e.right.content.constructor === C)); ) {
    if (!e.right.deleted)
      switch (e.right.content.constructor) {
        case C: {
          const { key: c, value: d } = (
            /** @type {ContentFormat} */
            e.right.content
          ), h = s[c];
          if (h !== void 0) {
            if (V(h, d))
              o.delete(c);
            else {
              if (n === 0)
                break t;
              o.set(c, d);
            }
            e.right.delete(i);
          } else
            e.currentAttributes.set(c, d);
          break;
        }
        default:
          n < e.right.length && A(i, w(e.right.id.client, e.right.id.clock + n)), n -= e.right.length;
          break;
      }
    e.forward();
  }
  if (n > 0) {
    let c = "";
    for (; n > 0; n--)
      c += `
`;
    e.right = new k(w(l, y(r.store, l)), e.left, e.left && e.left.lastId, e.right, e.right && e.right.id, t, null, new N(c)), e.right.integrate(i, 0), e.forward();
  }
  cn(i, t, e, o);
}, an = (i, t, e, n, s) => {
  let r = t;
  const l = ne();
  for (; r && (!r.countable || r.deleted); ) {
    if (!r.deleted && r.content.constructor === C) {
      const d = (
        /** @type {ContentFormat} */
        r.content
      );
      l.set(d.key, d);
    }
    r = r.right;
  }
  let o = 0, c = !1;
  for (; t !== r; ) {
    if (e === t && (c = !0), !t.deleted) {
      const d = t.content;
      if (d.constructor === C) {
        const { key: h, value: a } = (
          /** @type {ContentFormat} */
          d
        ), u = n.get(h) ?? null;
        (l.get(h) !== d || u === a) && (t.delete(i), o++, !c && (s.get(h) ?? null) === a && u !== a && (u === null ? s.delete(h) : s.set(h, u))), !c && !t.deleted && nt(
          s,
          /** @type {ContentFormat} */
          d
        );
      }
    }
    t = /** @type {Item} */
    t.right;
  }
  return o;
}, Ai = (i, t) => {
  for (; t && t.right && (t.right.deleted || !t.right.countable); )
    t = t.right;
  const e = /* @__PURE__ */ new Set();
  for (; t && (t.deleted || !t.countable); ) {
    if (!t.deleted && t.content.constructor === C) {
      const n = (
        /** @type {ContentFormat} */
        t.content.key
      );
      e.has(n) ? t.delete(i) : e.add(n);
    }
    t = t.left;
  }
}, Ti = (i) => {
  let t = 0;
  return m(
    /** @type {Doc} */
    i.doc,
    (e) => {
      let n = (
        /** @type {Item} */
        i._start
      ), s = i._start, r = ne();
      const l = Wt(r);
      for (; s; )
        s.deleted === !1 && (s.content.constructor === C ? nt(
          l,
          /** @type {ContentFormat} */
          s.content
        ) : (t += an(e, n, s, r, l), r = Wt(l), n = s)), s = s.right;
    }
  ), t;
}, Li = (i) => {
  const t = /* @__PURE__ */ new Set(), e = i.doc;
  for (const [n, s] of i.afterState.entries()) {
    const r = i.beforeState.get(n) || 0;
    s !== r && Ke(
      i,
      /** @type {Array<Item|GC>} */
      e.store.clients.get(n),
      r,
      s,
      (l) => {
        !l.deleted && /** @type {Item} */
        l.content.constructor === C && l.constructor !== T && t.add(
          /** @type {any} */
          l.parent
        );
      }
    );
  }
  m(e, (n) => {
    q(i, i.deleteSet, (s) => {
      if (s instanceof T || !/** @type {YText} */
      s.parent._hasFormatting || t.has(
        /** @type {YText} */
        s.parent
      ))
        return;
      const r = (
        /** @type {YText} */
        s.parent
      );
      s.content.constructor === C ? t.add(r) : Ai(n, s);
    });
    for (const s of t)
      Ti(s);
  });
}, Ne = (i, t, e) => {
  const n = e, s = Wt(t.currentAttributes), r = t.right;
  for (; e > 0 && t.right !== null; ) {
    if (t.right.deleted === !1)
      switch (t.right.content.constructor) {
        case U:
        case $:
        case N:
          e < t.right.length && A(i, w(t.right.id.client, t.right.id.clock + e)), e -= t.right.length, t.right.delete(i);
          break;
      }
    t.forward();
  }
  r && an(i, r, t.right, s, t.currentAttributes);
  const l = (
    /** @type {AbstractType<any>} */
    /** @type {Item} */
    (t.left || t.right).parent
  );
  return l._searchMarker && lt(l._searchMarker, t.index, -n + e), t;
};
class Oi extends xt {
  /**
   * @param {YText} ytext
   * @param {Transaction} transaction
   * @param {Set<any>} subs The keys that changed
   */
  constructor(t, e, n) {
    super(t, e), this.childListChanged = !1, this.keysChanged = /* @__PURE__ */ new Set(), n.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.keysChanged.add(s);
    });
  }
  /**
   * @type {{added:Set<Item>,deleted:Set<Item>,keys:Map<string,{action:'add'|'update'|'delete',oldValue:any}>,delta:Array<{insert?:Array<any>|string, delete?:number, retain?:number}>}}
   */
  get changes() {
    if (this._changes === null) {
      const t = {
        keys: this.keys,
        delta: this.delta,
        added: /* @__PURE__ */ new Set(),
        deleted: /* @__PURE__ */ new Set()
      };
      this._changes = t;
    }
    return (
      /** @type {any} */
      this._changes
    );
  }
  /**
   * Compute the changes in the delta format.
   * A {@link https://quilljs.com/docs/delta/|Quill Delta}) that represents the changes on the document.
   *
   * @type {Array<{insert?:string|object|AbstractType<any>, delete?:number, retain?:number, attributes?: Object<string,any>}>}
   *
   * @public
   */
  get delta() {
    if (this._delta === null) {
      const t = (
        /** @type {Doc} */
        this.target.doc
      ), e = [];
      m(t, (n) => {
        const s = /* @__PURE__ */ new Map(), r = /* @__PURE__ */ new Map();
        let l = this.target._start, o = null;
        const c = {};
        let d = "", h = 0, a = 0;
        const u = () => {
          if (o !== null) {
            let f = null;
            switch (o) {
              case "delete":
                a > 0 && (f = { delete: a }), a = 0;
                break;
              case "insert":
                (typeof d == "object" || d.length > 0) && (f = { insert: d }, s.size > 0 && (f.attributes = {}, s.forEach((g, p) => {
                  g !== null && (f.attributes[p] = g);
                }))), d = "";
                break;
              case "retain":
                h > 0 && (f = { retain: h }, Tn(c) || (f.attributes = Ln({}, c))), h = 0;
                break;
            }
            f && e.push(f), o = null;
          }
        };
        for (; l !== null; ) {
          switch (l.content.constructor) {
            case U:
            case $:
              this.adds(l) ? this.deletes(l) || (u(), o = "insert", d = l.content.getContent()[0], u()) : this.deletes(l) ? (o !== "delete" && (u(), o = "delete"), a += 1) : l.deleted || (o !== "retain" && (u(), o = "retain"), h += 1);
              break;
            case N:
              this.adds(l) ? this.deletes(l) || (o !== "insert" && (u(), o = "insert"), d += /** @type {ContentString} */
              l.content.str) : this.deletes(l) ? (o !== "delete" && (u(), o = "delete"), a += l.length) : l.deleted || (o !== "retain" && (u(), o = "retain"), h += l.length);
              break;
            case C: {
              const { key: f, value: g } = (
                /** @type {ContentFormat} */
                l.content
              );
              if (this.adds(l)) {
                if (!this.deletes(l)) {
                  const p = s.get(f) ?? null;
                  V(p, g) ? g !== null && l.delete(n) : (o === "retain" && u(), V(g, r.get(f) ?? null) ? delete c[f] : c[f] = g);
                }
              } else if (this.deletes(l)) {
                r.set(f, g);
                const p = s.get(f) ?? null;
                V(p, g) || (o === "retain" && u(), c[f] = p);
              } else if (!l.deleted) {
                r.set(f, g);
                const p = c[f];
                p !== void 0 && (V(p, g) ? p !== null && l.delete(n) : (o === "retain" && u(), g === null ? delete c[f] : c[f] = g));
              }
              l.deleted || (o === "insert" && u(), nt(
                s,
                /** @type {ContentFormat} */
                l.content
              ));
              break;
            }
          }
          l = l.right;
        }
        for (u(); e.length > 0; ) {
          const f = e[e.length - 1];
          if (f.retain !== void 0 && f.attributes === void 0)
            e.pop();
          else
            break;
        }
      }), this._delta = e;
    }
    return (
      /** @type {any} */
      this._delta
    );
  }
}
class Z extends E {
  /**
   * @param {String} [string] The initial value of the YText.
   */
  constructor(t) {
    super(), this._pending = t !== void 0 ? [() => this.insert(0, t)] : [], this._searchMarker = [], this._hasFormatting = !1;
  }
  /**
   * Number of characters of this text type.
   *
   * @type {number}
   */
  get length() {
    return this.doc ?? I(), this._length;
  }
  /**
   * @param {Doc} y
   * @param {Item} item
   */
  _integrate(t, e) {
    super._integrate(t, e);
    try {
      this._pending.forEach((n) => n());
    } catch (n) {
      console.error(n);
    }
    this._pending = null;
  }
  _copy() {
    return new Z();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YText}
   */
  clone() {
    const t = new Z();
    return t.applyDelta(this.toDelta()), t;
  }
  /**
   * Creates YTextEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, e) {
    super._callObserver(t, e);
    const n = new Oi(this, t, e);
    vt(this, t, n), !t.local && this._hasFormatting && (t._needFormattingCleanup = !0);
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @public
   */
  toString() {
    this.doc ?? I();
    let t = "", e = this._start;
    for (; e !== null; )
      !e.deleted && e.countable && e.content.constructor === N && (t += /** @type {ContentString} */
      e.content.str), e = e.right;
    return t;
  }
  /**
   * Returns the unformatted string representation of this YText type.
   *
   * @return {string}
   * @public
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Apply a {@link Delta} on this shared YText type.
   *
   * @param {Array<any>} delta The changes to apply on this element.
   * @param {object}  opts
   * @param {boolean} [opts.sanitize] Sanitize input delta. Removes ending newlines if set to true.
   *
   *
   * @public
   */
  applyDelta(t, { sanitize: e = !0 } = {}) {
    this.doc !== null ? m(this.doc, (n) => {
      const s = new te(null, this._start, 0, /* @__PURE__ */ new Map());
      for (let r = 0; r < t.length; r++) {
        const l = t[r];
        if (l.insert !== void 0) {
          const o = !e && typeof l.insert == "string" && r === t.length - 1 && s.right === null && l.insert.slice(-1) === `
` ? l.insert.slice(0, -1) : l.insert;
          (typeof o != "string" || o.length > 0) && $t(n, this, s, o, l.attributes || {});
        } else l.retain !== void 0 ? ve(n, this, s, l.retain, l.attributes || {}) : l.delete !== void 0 && Ne(n, s, l.delete);
      }
    }) : this._pending.push(() => this.applyDelta(t));
  }
  /**
   * Returns the Delta representation of this YText type.
   *
   * @param {Snapshot} [snapshot]
   * @param {Snapshot} [prevSnapshot]
   * @param {function('removed' | 'added', ID):any} [computeYChange]
   * @return {any} The Delta representation of this type.
   *
   * @public
   */
  toDelta(t, e, n) {
    this.doc ?? I();
    const s = [], r = /* @__PURE__ */ new Map(), l = (
      /** @type {Doc} */
      this.doc
    );
    let o = "", c = this._start;
    function d() {
      if (o.length > 0) {
        const a = {};
        let u = !1;
        r.forEach((g, p) => {
          u = !0, a[p] = g;
        });
        const f = { insert: o };
        u && (f.attributes = a), s.push(f), o = "";
      }
    }
    const h = () => {
      for (; c !== null; ) {
        if (F(c, t) || e !== void 0 && F(c, e))
          switch (c.content.constructor) {
            case N: {
              const a = r.get("ychange");
              t !== void 0 && !F(c, t) ? (a === void 0 || a.user !== c.id.client || a.type !== "removed") && (d(), r.set("ychange", n ? n("removed", c.id) : { type: "removed" })) : e !== void 0 && !F(c, e) ? (a === void 0 || a.user !== c.id.client || a.type !== "added") && (d(), r.set("ychange", n ? n("added", c.id) : { type: "added" })) : a !== void 0 && (d(), r.delete("ychange")), o += /** @type {ContentString} */
              c.content.str;
              break;
            }
            case U:
            case $: {
              d();
              const a = {
                insert: c.content.getContent()[0]
              };
              if (r.size > 0) {
                const u = (
                  /** @type {Object<string,any>} */
                  {}
                );
                a.attributes = u, r.forEach((f, g) => {
                  u[g] = f;
                });
              }
              s.push(a);
              break;
            }
            case C:
              F(c, t) && (d(), nt(
                r,
                /** @type {ContentFormat} */
                c.content
              ));
              break;
          }
        c = c.right;
      }
      d();
    };
    return t || e ? m(l, (a) => {
      t && Zt(a, t), e && Zt(a, e), h();
    }, "cleanup") : h(), s;
  }
  /**
   * Insert text at a given index.
   *
   * @param {number} index The index at which to start inserting.
   * @param {String} text The text to insert at the specified position.
   * @param {TextAttributes} [attributes] Optionally define some formatting
   *                                    information to apply on the inserted
   *                                    Text.
   * @public
   */
  insert(t, e, n) {
    if (e.length <= 0)
      return;
    const s = this.doc;
    s !== null ? m(s, (r) => {
      const l = pt(r, this, t, !n);
      n || (n = {}, l.currentAttributes.forEach((o, c) => {
        n[c] = o;
      })), $t(r, this, l, e, n);
    }) : this._pending.push(() => this.insert(t, e, n));
  }
  /**
   * Inserts an embed at a index.
   *
   * @param {number} index The index to insert the embed at.
   * @param {Object | AbstractType<any>} embed The Object that represents the embed.
   * @param {TextAttributes} [attributes] Attribute information to apply on the
   *                                    embed
   *
   * @public
   */
  insertEmbed(t, e, n) {
    const s = this.doc;
    s !== null ? m(s, (r) => {
      const l = pt(r, this, t, !n);
      $t(r, this, l, e, n || {});
    }) : this._pending.push(() => this.insertEmbed(t, e, n || {}));
  }
  /**
   * Deletes text starting from an index.
   *
   * @param {number} index Index at which to start deleting.
   * @param {number} length The number of characters to remove. Defaults to 1.
   *
   * @public
   */
  delete(t, e) {
    if (e === 0)
      return;
    const n = this.doc;
    n !== null ? m(n, (s) => {
      Ne(s, pt(s, this, t, !0), e);
    }) : this._pending.push(() => this.delete(t, e));
  }
  /**
   * Assigns properties to a range of text.
   *
   * @param {number} index The position where to start formatting.
   * @param {number} length The amount of characters to assign properties to.
   * @param {TextAttributes} attributes Attribute information to apply on the
   *                                    text.
   *
   * @public
   */
  format(t, e, n) {
    if (e === 0)
      return;
    const s = this.doc;
    s !== null ? m(s, (r) => {
      const l = pt(r, this, t, !1);
      l.right !== null && ve(r, this, l, e, n);
    }) : this._pending.push(() => this.format(t, e, n));
  }
  /**
   * Removes an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(t) {
    this.doc !== null ? m(this.doc, (e) => {
      yt(e, this, t);
    }) : this._pending.push(() => this.removeAttribute(t));
  }
  /**
   * Sets or updates an attribute.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that is to be set.
   * @param {any} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(t, e) {
    this.doc !== null ? m(this.doc, (n) => {
      ce(n, this, t, e);
    }) : this._pending.push(() => this.setAttribute(t, e));
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @param {String} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {any} The queried attribute value.
   *
   * @public
   */
  getAttribute(t) {
    return (
      /** @type {any} */
      he(this, t)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @note Xml-Text nodes don't have attributes. You can use this feature to assign properties to complete text-blocks.
   *
   * @return {Object<string, any>} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes() {
    return ln(this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(qi);
  }
}
const Mi = (i) => new Z();
class Xt {
  /**
   * @param {YXmlFragment | YXmlElement} root
   * @param {function(AbstractType<any>):boolean} [f]
   */
  constructor(t, e = () => !0) {
    this._filter = e, this._root = t, this._currentNode = /** @type {Item} */
    t._start, this._firstCall = !0, t.doc ?? I();
  }
  [Symbol.iterator]() {
    return this;
  }
  /**
   * Get the next node.
   *
   * @return {IteratorResult<YXmlElement|YXmlText|YXmlHook>} The next node.
   *
   * @public
   */
  next() {
    let t = this._currentNode, e = t && t.content && /** @type {any} */
    t.content.type;
    if (t !== null && (!this._firstCall || t.deleted || !this._filter(e)))
      do
        if (e = /** @type {any} */
        t.content.type, !t.deleted && (e.constructor === P || e.constructor === j) && e._start !== null)
          t = e._start;
        else
          for (; t !== null; ) {
            const n = t.next;
            if (n !== null) {
              t = n;
              break;
            } else t.parent === this._root ? t = null : t = /** @type {AbstractType<any>} */
            t.parent._item;
          }
      while (t !== null && (t.deleted || !this._filter(
        /** @type {ContentType} */
        t.content.type
      )));
    return this._firstCall = !1, t === null ? { value: void 0, done: !0 } : (this._currentNode = t, { value: (
      /** @type {any} */
      t.content.type
    ), done: !1 });
  }
}
class j extends E {
  constructor() {
    super(), this._prelimContent = [];
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get firstChild() {
    const t = this._first;
    return t ? t.content.getContent()[0] : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, e) {
    super._integrate(t, e), this.insert(
      0,
      /** @type {Array<any>} */
      this._prelimContent
    ), this._prelimContent = null;
  }
  _copy() {
    return new j();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlFragment}
   */
  clone() {
    const t = new j();
    return t.insert(0, this.toArray().map((e) => e instanceof E ? e.clone() : e)), t;
  }
  get length() {
    return this.doc ?? I(), this._prelimContent === null ? this._length : this._prelimContent.length;
  }
  /**
   * Create a subtree of childNodes.
   *
   * @example
   * const walker = elem.createTreeWalker(dom => dom.nodeName === 'div')
   * for (let node in walker) {
   *   // `node` is a div node
   *   nop(node)
   * }
   *
   * @param {function(AbstractType<any>):boolean} filter Function that is called on each child element and
   *                          returns a Boolean indicating whether the child
   *                          is to be included in the subtree.
   * @return {YXmlTreeWalker} A subtree and a position within it.
   *
   * @public
   */
  createTreeWalker(t) {
    return new Xt(this, t);
  }
  /**
   * Returns the first YXmlElement that matches the query.
   * Similar to DOM's {@link querySelector}.
   *
   * Query support:
   *   - tagname
   * TODO:
   *   - id
   *   - attribute
   *
   * @param {CSS_Selector} query The query on the children.
   * @return {YXmlElement|YXmlText|YXmlHook|null} The first element that matches the query or null.
   *
   * @public
   */
  querySelector(t) {
    t = t.toUpperCase();
    const n = new Xt(this, (s) => s.nodeName && s.nodeName.toUpperCase() === t).next();
    return n.done ? null : n.value;
  }
  /**
   * Returns all YXmlElements that match the query.
   * Similar to Dom's {@link querySelectorAll}.
   *
   * @todo Does not yet support all queries. Currently only query by tagName.
   *
   * @param {CSS_Selector} query The query on the children
   * @return {Array<YXmlElement|YXmlText|YXmlHook|null>} The elements that match this query.
   *
   * @public
   */
  querySelectorAll(t) {
    return t = t.toUpperCase(), z(new Xt(this, (e) => e.nodeName && e.nodeName.toUpperCase() === t));
  }
  /**
   * Creates YXmlEvent and calls observers.
   *
   * @param {Transaction} transaction
   * @param {Set<null|string>} parentSubs Keys changed on this type. `null` if list was modified.
   */
  _callObserver(t, e) {
    vt(this, t, new vi(this, e, t));
  }
  /**
   * Get the string representation of all the children of this YXmlFragment.
   *
   * @return {string} The string representation of all children.
   */
  toString() {
    return tn(this, (t) => t.toString()).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, e = {}, n) {
    const s = t.createDocumentFragment();
    return n !== void 0 && n._createAssociation(s, this), ot(this, (r) => {
      s.insertBefore(r.toDOM(t, e, n), null);
    }), s;
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {number} index The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insert(t, e) {
    this.doc !== null ? m(this.doc, (n) => {
      sn(n, this, t, e);
    }) : this._prelimContent.splice(t, 0, ...e);
  }
  /**
   * Inserts new content at an index.
   *
   * @example
   *  // Insert character 'a' at position 0
   *  xml.insert(0, [new Y.XmlText('text')])
   *
   * @param {null|Item|YXmlElement|YXmlText} ref The index to insert content at
   * @param {Array<YXmlElement|YXmlText>} content The array of content
   */
  insertAfter(t, e) {
    if (this.doc !== null)
      m(this.doc, (n) => {
        const s = t && t instanceof E ? t._item : t;
        bt(n, this, s, e);
      });
    else {
      const n = (
        /** @type {Array<any>} */
        this._prelimContent
      ), s = t === null ? 0 : n.findIndex((r) => r === t) + 1;
      if (s === 0 && t !== null)
        throw mt("Reference item not found");
      n.splice(s, 0, ...e);
    }
  }
  /**
   * Deletes elements starting from an index.
   *
   * @param {number} index Index at which to start deleting elements
   * @param {number} [length=1] The number of elements to remove. Defaults to 1.
   */
  delete(t, e = 1) {
    this.doc !== null ? m(this.doc, (n) => {
      rn(n, this, t, e);
    }) : this._prelimContent.splice(t, e);
  }
  /**
   * Transforms this YArray to a JavaScript Array.
   *
   * @return {Array<YXmlElement|YXmlText|YXmlHook>}
   */
  toArray() {
    return Pe(this);
  }
  /**
   * Appends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to append.
   */
  push(t) {
    this.insert(this.length, t);
  }
  /**
   * Prepends content to this YArray.
   *
   * @param {Array<YXmlElement|YXmlText>} content Array of content to prepend.
   */
  unshift(t) {
    this.insert(0, t);
  }
  /**
   * Returns the i-th element from a YArray.
   *
   * @param {number} index The index of the element to return from the YArray
   * @return {YXmlElement|YXmlText}
   */
  get(t) {
    return en(this, t);
  }
  /**
   * Returns a portion of this YXmlFragment into a JavaScript Array selected
   * from start to end (end not included).
   *
   * @param {number} [start]
   * @param {number} [end]
   * @return {Array<YXmlElement|YXmlText>}
   */
  slice(t = 0, e = this.length) {
    return Ze(this, t, e);
  }
  /**
   * Executes a provided function on once on every child element.
   *
   * @param {function(YXmlElement|YXmlText,number, typeof self):void} f A function to execute on every element of this YArray.
   */
  forEach(t) {
    ot(this, t);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(Zi);
  }
}
const xi = (i) => new j();
class P extends j {
  constructor(t = "UNDEFINED") {
    super(), this.nodeName = t, this._prelimAttrs = /* @__PURE__ */ new Map();
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const t = this._item ? this._item.next : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const t = this._item ? this._item.prev : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * Integrate this type into the Yjs instance.
   *
   * * Save this struct in the os
   * * This type is sent to other client
   * * Observer functions are fired
   *
   * @param {Doc} y The Yjs instance
   * @param {Item} item
   */
  _integrate(t, e) {
    super._integrate(t, e), /** @type {Map<string, any>} */
    this._prelimAttrs.forEach((n, s) => {
      this.setAttribute(s, n);
    }), this._prelimAttrs = null;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   *
   * @return {YXmlElement}
   */
  _copy() {
    return new P(this.nodeName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlElement<KV>}
   */
  clone() {
    const t = new P(this.nodeName), e = this.getAttributes();
    return xn(e, (n, s) => {
      typeof n == "string" && t.setAttribute(s, n);
    }), t.insert(0, this.toArray().map((n) => n instanceof E ? n.clone() : n)), t;
  }
  /**
   * Returns the XML serialization of this YXmlElement.
   * The attributes are ordered by attribute-name, so you can easily use this
   * method to compare YXmlElements
   *
   * @return {string} The string representation of this type.
   *
   * @public
   */
  toString() {
    const t = this.getAttributes(), e = [], n = [];
    for (const o in t)
      n.push(o);
    n.sort();
    const s = n.length;
    for (let o = 0; o < s; o++) {
      const c = n[o];
      e.push(c + '="' + t[c] + '"');
    }
    const r = this.nodeName.toLocaleLowerCase(), l = e.length > 0 ? " " + e.join(" ") : "";
    return `<${r}${l}>${super.toString()}</${r}>`;
  }
  /**
   * Removes an attribute from this YXmlElement.
   *
   * @param {string} attributeName The attribute name that is to be removed.
   *
   * @public
   */
  removeAttribute(t) {
    this.doc !== null ? m(this.doc, (e) => {
      yt(e, this, t);
    }) : this._prelimAttrs.delete(t);
  }
  /**
   * Sets or updates an attribute.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that is to be set.
   * @param {KV[KEY]} attributeValue The attribute value that is to be set.
   *
   * @public
   */
  setAttribute(t, e) {
    this.doc !== null ? m(this.doc, (n) => {
      ce(n, this, t, e);
    }) : this._prelimAttrs.set(t, e);
  }
  /**
   * Returns an attribute value that belongs to the attribute name.
   *
   * @template {keyof KV & string} KEY
   *
   * @param {KEY} attributeName The attribute name that identifies the
   *                               queried value.
   * @return {KV[KEY]|undefined} The queried attribute value.
   *
   * @public
   */
  getAttribute(t) {
    return (
      /** @type {any} */
      he(this, t)
    );
  }
  /**
   * Returns whether an attribute exists
   *
   * @param {string} attributeName The attribute name to check for existence.
   * @return {boolean} whether the attribute exists.
   *
   * @public
   */
  hasAttribute(t) {
    return (
      /** @type {any} */
      on(this, t)
    );
  }
  /**
   * Returns all attribute name/value pairs in a JSON Object.
   *
   * @param {Snapshot} [snapshot]
   * @return {{ [Key in Extract<keyof KV,string>]?: KV[Key]}} A JSON Object that describes the attributes.
   *
   * @public
   */
  getAttributes(t) {
    return (
      /** @type {any} */
      t ? yi(this, t) : ln(this)
    );
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks={}] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Node} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, e = {}, n) {
    const s = t.createElement(this.nodeName), r = this.getAttributes();
    for (const l in r) {
      const o = r[l];
      typeof o == "string" && s.setAttribute(l, o);
    }
    return ot(this, (l) => {
      s.appendChild(l.toDOM(t, e, n));
    }), n !== void 0 && n._createAssociation(s, this), s;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(Qi), t.writeKey(this.nodeName);
  }
}
const Ri = (i) => new P(i.readKey());
class vi extends xt {
  /**
   * @param {YXmlElement|YXmlText|YXmlFragment} target The target on which the event is created.
   * @param {Set<string|null>} subs The set of changed attributes. `null` is included if the
   *                   child list changed.
   * @param {Transaction} transaction The transaction instance with which the
   *                                  change was created.
   */
  constructor(t, e, n) {
    super(t, n), this.childListChanged = !1, this.attributesChanged = /* @__PURE__ */ new Set(), e.forEach((s) => {
      s === null ? this.childListChanged = !0 : this.attributesChanged.add(s);
    });
  }
}
class Ct extends Q {
  /**
   * @param {string} hookName nodeName of the Dom Node.
   */
  constructor(t) {
    super(), this.hookName = t;
  }
  /**
   * Creates an Item with the same effect as this Item (without position effect)
   */
  _copy() {
    return new Ct(this.hookName);
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlHook}
   */
  clone() {
    const t = new Ct(this.hookName);
    return this.forEach((e, n) => {
      t.set(n, e);
    }), t;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlElement.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object.<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type
   * @return {Element} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, e = {}, n) {
    const s = e[this.hookName];
    let r;
    return s !== void 0 ? r = s.createDom(this) : r = document.createElement(this.hookName), r.setAttribute("data-yjs-hook", this.hookName), n !== void 0 && n._createAssociation(r, this), r;
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   */
  _write(t) {
    t.writeTypeRef(Pi), t.writeKey(this.hookName);
  }
}
const Ni = (i) => new Ct(i.readKey());
class Dt extends Z {
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get nextSibling() {
    const t = this._item ? this._item.next : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  /**
   * @type {YXmlElement|YXmlText|null}
   */
  get prevSibling() {
    const t = this._item ? this._item.prev : null;
    return t ? (
      /** @type {YXmlElement|YXmlText} */
      /** @type {ContentType} */
      t.content.type
    ) : null;
  }
  _copy() {
    return new Dt();
  }
  /**
   * Makes a copy of this data type that can be included somewhere else.
   *
   * Note that the content is only readable _after_ it has been included somewhere in the Ydoc.
   *
   * @return {YXmlText}
   */
  clone() {
    const t = new Dt();
    return t.applyDelta(this.toDelta()), t;
  }
  /**
   * Creates a Dom Element that mirrors this YXmlText.
   *
   * @param {Document} [_document=document] The document object (you must define
   *                                        this when calling this method in
   *                                        nodejs)
   * @param {Object<string, any>} [hooks] Optional property to customize how hooks
   *                                             are presented in the DOM
   * @param {any} [binding] You should not set this property. This is
   *                               used if DomBinding wants to create a
   *                               association to the created DOM type.
   * @return {Text} The {@link https://developer.mozilla.org/en-US/docs/Web/API/Element|Dom Element}
   *
   * @public
   */
  toDOM(t = document, e, n) {
    const s = t.createTextNode(this.toString());
    return n !== void 0 && n._createAssociation(s, this), s;
  }
  toString() {
    return this.toDelta().map((t) => {
      const e = [];
      for (const s in t.attributes) {
        const r = [];
        for (const l in t.attributes[s])
          r.push({ key: l, value: t.attributes[s][l] });
        r.sort((l, o) => l.key < o.key ? -1 : 1), e.push({ nodeName: s, attrs: r });
      }
      e.sort((s, r) => s.nodeName < r.nodeName ? -1 : 1);
      let n = "";
      for (let s = 0; s < e.length; s++) {
        const r = e[s];
        n += `<${r.nodeName}`;
        for (let l = 0; l < r.attrs.length; l++) {
          const o = r.attrs[l];
          n += ` ${o.key}="${o.value}"`;
        }
        n += ">";
      }
      n += t.insert;
      for (let s = e.length - 1; s >= 0; s--)
        n += `</${e[s].nodeName}>`;
      return n;
    }).join("");
  }
  /**
   * @return {string}
   */
  toJSON() {
    return this.toString();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   */
  _write(t) {
    t.writeTypeRef(ts);
  }
}
const Ui = (i) => new Dt();
class de {
  /**
   * @param {ID} id
   * @param {number} length
   */
  constructor(t, e) {
    this.id = t, this.length = e;
  }
  /**
   * @type {boolean}
   */
  get deleted() {
    throw R();
  }
  /**
   * Merge this struct with the item to the right.
   * This method is already assuming that `this.id.clock + this.length === this.id.clock`.
   * Also this method does *not* remove right from StructStore!
   * @param {AbstractStruct} right
   * @return {boolean} whether this merged with right
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   * @param {number} encodingRef
   */
  write(t, e, n) {
    throw R();
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, e) {
    throw R();
  }
}
const Vi = 0;
class T extends de {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {GC} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.constructor !== t.constructor ? !1 : (this.length += t.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, e) {
    e > 0 && (this.id.clock += e, this.length -= e), Xe(t.doc.store, this);
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeInfo(Vi), t.writeLen(this.length - e);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, e) {
    return null;
  }
}
class dt {
  /**
   * @param {Uint8Array} content
   */
  constructor(t) {
    this.content = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.content];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentBinary}
   */
  copy() {
    return new dt(this.content);
  }
  /**
   * @param {number} offset
   * @return {ContentBinary}
   */
  splice(t) {
    throw R();
  }
  /**
   * @param {ContentBinary} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeBuf(this.content);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 3;
  }
}
const Fi = (i) => new dt(i.readBuf());
class ct {
  /**
   * @param {number} len
   */
  constructor(t) {
    this.len = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.len;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentDeleted}
   */
  copy() {
    return new ct(this.len);
  }
  /**
   * @param {number} offset
   * @return {ContentDeleted}
   */
  splice(t) {
    const e = new ct(this.len - t);
    return this.len = t, e;
  }
  /**
   * @param {ContentDeleted} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.len += t.len, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
    rt(t.deleteSet, e.id.client, e.id.clock, this.len), e.markDeleted();
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeLen(this.len - e);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 1;
  }
}
const Bi = (i) => new ct(i.readLen()), un = (i, t) => new et({ guid: i, ...t, shouldLoad: t.shouldLoad || t.autoLoad || !1 });
class at {
  /**
   * @param {Doc} doc
   */
  constructor(t) {
    t._item && console.error("This document was already integrated as a sub-document. You should create a second instance instead with the same guid."), this.doc = t;
    const e = {};
    this.opts = e, t.gc || (e.gc = !1), t.autoLoad && (e.autoLoad = !0), t.meta !== null && (e.meta = t.meta);
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.doc];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentDoc}
   */
  copy() {
    return new at(un(this.doc.guid, this.opts));
  }
  /**
   * @param {number} offset
   * @return {ContentDoc}
   */
  splice(t) {
    throw R();
  }
  /**
   * @param {ContentDoc} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
    this.doc._item = e, t.subdocsAdded.add(this.doc), this.doc.shouldLoad && t.subdocsLoaded.add(this.doc);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
    t.subdocsAdded.has(this.doc) ? t.subdocsAdded.delete(this.doc) : t.subdocsRemoved.add(this.doc);
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeString(this.doc.guid), t.writeAny(this.opts);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 9;
  }
}
const Ji = (i) => new at(un(i.readString(), i.readAny()));
class $ {
  /**
   * @param {Object} embed
   */
  constructor(t) {
    this.embed = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.embed];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentEmbed}
   */
  copy() {
    return new $(this.embed);
  }
  /**
   * @param {number} offset
   * @return {ContentEmbed}
   */
  splice(t) {
    throw R();
  }
  /**
   * @param {ContentEmbed} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeJSON(this.embed);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 5;
  }
}
const Hi = (i) => new $(i.readJSON());
class C {
  /**
   * @param {string} key
   * @param {Object} value
   */
  constructor(t, e) {
    this.key = t, this.value = e;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !1;
  }
  /**
   * @return {ContentFormat}
   */
  copy() {
    return new C(this.key, this.value);
  }
  /**
   * @param {number} _offset
   * @return {ContentFormat}
   */
  splice(t) {
    throw R();
  }
  /**
   * @param {ContentFormat} _right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} _transaction
   * @param {Item} item
   */
  integrate(t, e) {
    const n = (
      /** @type {YText} */
      e.parent
    );
    n._searchMarker = null, n._hasFormatting = !0;
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeKey(this.key), t.writeJSON(this.value);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 6;
  }
}
const ji = (i) => new C(i.readKey(), i.readJSON());
class Et {
  /**
   * @param {Array<any>} arr
   */
  constructor(t) {
    this.arr = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentJSON}
   */
  copy() {
    return new Et(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentJSON}
   */
  splice(t) {
    const e = new Et(this.arr.slice(t));
    return this.arr = this.arr.slice(0, t), e;
  }
  /**
   * @param {ContentJSON} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.arr = this.arr.concat(t.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    const n = this.arr.length;
    t.writeLen(n - e);
    for (let s = e; s < n; s++) {
      const r = this.arr[s];
      t.writeString(r === void 0 ? "undefined" : JSON.stringify(r));
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 2;
  }
}
const Yi = (i) => {
  const t = i.readLen(), e = [];
  for (let n = 0; n < t; n++) {
    const s = i.readString();
    s === "undefined" ? e.push(void 0) : e.push(JSON.parse(s));
  }
  return new Et(e);
}, Gi = Rn("node_env") === "development";
class Y {
  /**
   * @param {Array<any>} arr
   */
  constructor(t) {
    this.arr = t, Gi && Mn(t);
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.arr.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.arr;
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentAny}
   */
  copy() {
    return new Y(this.arr);
  }
  /**
   * @param {number} offset
   * @return {ContentAny}
   */
  splice(t) {
    const e = new Y(this.arr.slice(t));
    return this.arr = this.arr.slice(0, t), e;
  }
  /**
   * @param {ContentAny} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.arr = this.arr.concat(t.arr), !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    const n = this.arr.length;
    t.writeLen(n - e);
    for (let s = e; s < n; s++) {
      const r = this.arr[s];
      t.writeAny(r);
    }
  }
  /**
   * @return {number}
   */
  getRef() {
    return 8;
  }
}
const $i = (i) => {
  const t = i.readLen(), e = [];
  for (let n = 0; n < t; n++)
    e.push(i.readAny());
  return new Y(e);
};
class N {
  /**
   * @param {string} str
   */
  constructor(t) {
    this.str = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return this.str.length;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return this.str.split("");
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentString}
   */
  copy() {
    return new N(this.str);
  }
  /**
   * @param {number} offset
   * @return {ContentString}
   */
  splice(t) {
    const e = new N(this.str.slice(t));
    this.str = this.str.slice(0, t);
    const n = this.str.charCodeAt(t - 1);
    return n >= 55296 && n <= 56319 && (this.str = this.str.slice(0, t - 1) + "�", e.str = "�" + e.str.slice(1)), e;
  }
  /**
   * @param {ContentString} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.str += t.str, !0;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeString(e === 0 ? this.str : this.str.slice(e));
  }
  /**
   * @return {number}
   */
  getRef() {
    return 4;
  }
}
const Xi = (i) => new N(i.readString()), Ki = [
  Di,
  Ii,
  Mi,
  Ri,
  xi,
  Ni,
  Ui
], Wi = 0, zi = 1, qi = 2, Qi = 3, Zi = 4, Pi = 5, ts = 6;
class U {
  /**
   * @param {AbstractType<any>} type
   */
  constructor(t) {
    this.type = t;
  }
  /**
   * @return {number}
   */
  getLength() {
    return 1;
  }
  /**
   * @return {Array<any>}
   */
  getContent() {
    return [this.type];
  }
  /**
   * @return {boolean}
   */
  isCountable() {
    return !0;
  }
  /**
   * @return {ContentType}
   */
  copy() {
    return new U(this.type._copy());
  }
  /**
   * @param {number} offset
   * @return {ContentType}
   */
  splice(t) {
    throw R();
  }
  /**
   * @param {ContentType} right
   * @return {boolean}
   */
  mergeWith(t) {
    return !1;
  }
  /**
   * @param {Transaction} transaction
   * @param {Item} item
   */
  integrate(t, e) {
    this.type._integrate(t.doc, e);
  }
  /**
   * @param {Transaction} transaction
   */
  delete(t) {
    let e = this.type._start;
    for (; e !== null; )
      e.deleted ? e.id.clock < (t.beforeState.get(e.id.client) || 0) && t._mergeStructs.push(e) : e.delete(t), e = e.right;
    this.type._map.forEach((n) => {
      n.deleted ? n.id.clock < (t.beforeState.get(n.id.client) || 0) && t._mergeStructs.push(n) : n.delete(t);
    }), t.changed.delete(this.type);
  }
  /**
   * @param {StructStore} store
   */
  gc(t) {
    let e = this.type._start;
    for (; e !== null; )
      e.gc(t, !0), e = e.right;
    this.type._start = null, this.type._map.forEach(
      /** @param {Item | null} item */
      (n) => {
        for (; n !== null; )
          n.gc(t, !0), n = n.left;
      }
    ), this.type._map = /* @__PURE__ */ new Map();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    this.type._write(t);
  }
  /**
   * @return {number}
   */
  getRef() {
    return 7;
  }
}
const es = (i) => new U(Ki[i.readTypeRef()](i)), ee = (i, t) => {
  let e = t, n = 0, s;
  do
    n > 0 && (e = w(e.client, e.clock + n)), s = K(i, e), n = e.clock - s.id.clock, e = s.redone;
  while (e !== null && s instanceof k);
  return {
    item: s,
    diff: n
  };
}, ae = (i, t) => {
  for (; i !== null && i.keep !== t; )
    i.keep = t, i = /** @type {AbstractType<any>} */
    i.parent._item;
}, It = (i, t, e) => {
  const { client: n, clock: s } = t.id, r = new k(
    w(n, s + e),
    t,
    w(n, s + e - 1),
    t.right,
    t.rightOrigin,
    t.parent,
    t.parentSub,
    t.content.splice(e)
  );
  return t.deleted && r.markDeleted(), t.keep && (r.keep = !0), t.redone !== null && (r.redone = w(t.redone.client, t.redone.clock + e)), t.right = r, r.right !== null && (r.right.left = r), i._mergeStructs.push(r), r.parentSub !== null && r.right === null && r.parent._map.set(r.parentSub, r), t.length = e, r;
}, Ue = (i, t) => kn(
  i,
  /** @param {StackItem} s */
  (e) => ht(e.deletions, t)
), fn = (i, t, e, n, s, r) => {
  const l = i.doc, o = l.store, c = l.clientID, d = t.redone;
  if (d !== null)
    return A(i, d);
  let h = (
    /** @type {AbstractType<any>} */
    t.parent._item
  ), a = null, u;
  if (h !== null && h.deleted === !0) {
    if (h.redone === null && (!e.has(h) || fn(i, h, e, n, s, r) === null))
      return null;
    for (; h.redone !== null; )
      h = A(i, h.redone);
  }
  const f = h === null ? (
    /** @type {AbstractType<any>} */
    t.parent
  ) : (
    /** @type {ContentType} */
    h.content.type
  );
  if (t.parentSub === null) {
    for (a = t.left, u = t; a !== null; ) {
      let b = a;
      for (; b !== null && /** @type {AbstractType<any>} */
      b.parent._item !== h; )
        b = b.redone === null ? null : A(i, b.redone);
      if (b !== null && /** @type {AbstractType<any>} */
      b.parent._item === h) {
        a = b;
        break;
      }
      a = a.left;
    }
    for (; u !== null; ) {
      let b = u;
      for (; b !== null && /** @type {AbstractType<any>} */
      b.parent._item !== h; )
        b = b.redone === null ? null : A(i, b.redone);
      if (b !== null && /** @type {AbstractType<any>} */
      b.parent._item === h) {
        u = b;
        break;
      }
      u = u.right;
    }
  } else if (u = null, t.right && !s) {
    for (a = t; a !== null && a.right !== null && (a.right.redone || ht(n, a.right.id) || Ue(r.undoStack, a.right.id) || Ue(r.redoStack, a.right.id)); )
      for (a = a.right; a.redone; ) a = A(i, a.redone);
    if (a && a.right !== null)
      return null;
  } else
    a = f._map.get(t.parentSub) || null;
  const g = y(o, c), p = w(c, g), S = new k(
    p,
    a,
    a && a.lastId,
    u,
    u && u.id,
    f,
    t.parentSub,
    t.content.copy()
  );
  return t.redone = p, ae(S, !0), S.integrate(i, 0), S;
};
class k extends de {
  /**
   * @param {ID} id
   * @param {Item | null} left
   * @param {ID | null} origin
   * @param {Item | null} right
   * @param {ID | null} rightOrigin
   * @param {AbstractType<any>|ID|null} parent Is a type if integrated, is null if it is possible to copy parent from left or right, is ID before integration to search for it.
   * @param {string | null} parentSub
   * @param {AbstractContent} content
   */
  constructor(t, e, n, s, r, l, o, c) {
    super(t, c.getLength()), this.origin = n, this.left = e, this.right = s, this.rightOrigin = r, this.parent = l, this.parentSub = o, this.redone = null, this.content = c, this.info = this.content.isCountable() ? Se : 0;
  }
  /**
   * This is used to mark the item as an indexed fast-search marker
   *
   * @type {boolean}
   */
  set marker(t) {
    (this.info & jt) > 0 !== t && (this.info ^= jt);
  }
  get marker() {
    return (this.info & jt) > 0;
  }
  /**
   * If true, do not garbage collect this Item.
   */
  get keep() {
    return (this.info & be) > 0;
  }
  set keep(t) {
    this.keep !== t && (this.info ^= be);
  }
  get countable() {
    return (this.info & Se) > 0;
  }
  /**
   * Whether this item was deleted or not.
   * @type {Boolean}
   */
  get deleted() {
    return (this.info & Yt) > 0;
  }
  set deleted(t) {
    this.deleted !== t && (this.info ^= Yt);
  }
  markDeleted() {
    this.info |= Yt;
  }
  /**
   * Return the creator clientID of the missing op or define missing items and return null.
   *
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, e) {
    if (this.origin && this.origin.client !== this.id.client && this.origin.clock >= y(e, this.origin.client))
      return this.origin.client;
    if (this.rightOrigin && this.rightOrigin.client !== this.id.client && this.rightOrigin.clock >= y(e, this.rightOrigin.client))
      return this.rightOrigin.client;
    if (this.parent && this.parent.constructor === X && this.id.client !== this.parent.client && this.parent.clock >= y(e, this.parent.client))
      return this.parent.client;
    if (this.origin && (this.left = Ie(t, e, this.origin), this.origin = this.left.lastId), this.rightOrigin && (this.right = A(t, this.rightOrigin), this.rightOrigin = this.right.id), this.left && this.left.constructor === T || this.right && this.right.constructor === T)
      this.parent = null;
    else if (!this.parent)
      this.left && this.left.constructor === k ? (this.parent = this.left.parent, this.parentSub = this.left.parentSub) : this.right && this.right.constructor === k && (this.parent = this.right.parent, this.parentSub = this.right.parentSub);
    else if (this.parent.constructor === X) {
      const n = K(e, this.parent);
      n.constructor === T ? this.parent = null : this.parent = /** @type {ContentType} */
      n.content.type;
    }
    return null;
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, e) {
    if (e > 0 && (this.id.clock += e, this.left = Ie(t, t.doc.store, w(this.id.client, this.id.clock - 1)), this.origin = this.left.lastId, this.content = this.content.splice(e), this.length -= e), this.parent) {
      if (!this.left && (!this.right || this.right.left !== null) || this.left && this.left.right !== this.right) {
        let n = this.left, s;
        if (n !== null)
          s = n.right;
        else if (this.parentSub !== null)
          for (s = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; s !== null && s.left !== null; )
            s = s.left;
        else
          s = /** @type {AbstractType<any>} */
          this.parent._start;
        const r = /* @__PURE__ */ new Set(), l = /* @__PURE__ */ new Set();
        for (; s !== null && s !== this.right; ) {
          if (l.add(s), r.add(s), ut(this.origin, s.origin)) {
            if (s.id.client < this.id.client)
              n = s, r.clear();
            else if (ut(this.rightOrigin, s.rightOrigin))
              break;
          } else if (s.origin !== null && l.has(K(t.doc.store, s.origin)))
            r.has(K(t.doc.store, s.origin)) || (n = s, r.clear());
          else
            break;
          s = s.right;
        }
        this.left = n;
      }
      if (this.left !== null) {
        const n = this.left.right;
        this.right = n, this.left.right = this;
      } else {
        let n;
        if (this.parentSub !== null)
          for (n = /** @type {AbstractType<any>} */
          this.parent._map.get(this.parentSub) || null; n !== null && n.left !== null; )
            n = n.left;
        else
          n = /** @type {AbstractType<any>} */
          this.parent._start, this.parent._start = this;
        this.right = n;
      }
      this.right !== null ? this.right.left = this : this.parentSub !== null && (this.parent._map.set(this.parentSub, this), this.left !== null && this.left.delete(t)), this.parentSub === null && this.countable && !this.deleted && (this.parent._length += this.length), Xe(t.doc.store, this), this.content.integrate(t, this), Te(
        t,
        /** @type {AbstractType<any>} */
        this.parent,
        this.parentSub
      ), /** @type {AbstractType<any>} */
      (this.parent._item !== null && /** @type {AbstractType<any>} */
      this.parent._item.deleted || this.parentSub !== null && this.right !== null) && this.delete(t);
    } else
      new T(this.id, this.length).integrate(t, 0);
  }
  /**
   * Returns the next non-deleted item
   */
  get next() {
    let t = this.right;
    for (; t !== null && t.deleted; )
      t = t.right;
    return t;
  }
  /**
   * Returns the previous non-deleted item
   */
  get prev() {
    let t = this.left;
    for (; t !== null && t.deleted; )
      t = t.left;
    return t;
  }
  /**
   * Computes the last content address of this Item.
   */
  get lastId() {
    return this.length === 1 ? this.id : w(this.id.client, this.id.clock + this.length - 1);
  }
  /**
   * Try to merge two items
   *
   * @param {Item} right
   * @return {boolean}
   */
  mergeWith(t) {
    if (this.constructor === t.constructor && ut(t.origin, this.lastId) && this.right === t && ut(this.rightOrigin, t.rightOrigin) && this.id.client === t.id.client && this.id.clock + this.length === t.id.clock && this.deleted === t.deleted && this.redone === null && t.redone === null && this.content.constructor === t.content.constructor && this.content.mergeWith(t.content)) {
      const e = (
        /** @type {AbstractType<any>} */
        this.parent._searchMarker
      );
      return e && e.forEach((n) => {
        n.p === t && (n.p = this, !this.deleted && this.countable && (n.index -= this.length));
      }), t.keep && (this.keep = !0), this.right = t.right, this.right !== null && (this.right.left = this), this.length += t.length, !0;
    }
    return !1;
  }
  /**
   * Mark this Item as deleted.
   *
   * @param {Transaction} transaction
   */
  delete(t) {
    if (!this.deleted) {
      const e = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      this.countable && this.parentSub === null && (e._length -= this.length), this.markDeleted(), rt(t.deleteSet, this.id.client, this.id.clock, this.length), Te(t, e, this.parentSub), this.content.delete(t);
    }
  }
  /**
   * @param {StructStore} store
   * @param {boolean} parentGCd
   */
  gc(t, e) {
    if (!this.deleted)
      throw M();
    this.content.gc(t), e ? li(t, this, new T(this.id, this.length)) : this.content = new ct(this.length);
  }
  /**
   * Transform the properties of this type to binary and write it to an
   * BinaryEncoder.
   *
   * This is called when this Item is sent to a remote peer.
   *
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder The encoder to write data to.
   * @param {number} offset
   */
  write(t, e) {
    const n = e > 0 ? w(this.id.client, this.id.clock + e - 1) : this.origin, s = this.rightOrigin, r = this.parentSub, l = this.content.getRef() & Lt | (n === null ? 0 : J) | // origin is defined
    (s === null ? 0 : H) | // right origin is defined
    (r === null ? 0 : st);
    if (t.writeInfo(l), n !== null && t.writeLeftID(n), s !== null && t.writeRightID(s), n === null && s === null) {
      const o = (
        /** @type {AbstractType<any>} */
        this.parent
      );
      if (o._item !== void 0) {
        const c = o._item;
        if (c === null) {
          const d = Ge(o);
          t.writeParentInfo(!0), t.writeString(d);
        } else
          t.writeParentInfo(!1), t.writeLeftID(c.id);
      } else o.constructor === String ? (t.writeParentInfo(!0), t.writeString(o)) : o.constructor === X ? (t.writeParentInfo(!1), t.writeLeftID(o)) : M();
      r !== null && t.writeString(r);
    }
    this.content.write(t, e);
  }
}
const gn = (i, t) => ns[t & Lt](i), ns = [
  () => {
    M();
  },
  // GC is not ItemContent
  Bi,
  // 1
  Yi,
  // 2
  Fi,
  // 3
  Xi,
  // 4
  Hi,
  // 5
  ji,
  // 6
  es,
  // 7
  $i,
  // 8
  Ji,
  // 9
  () => {
    M();
  }
  // 10 - Skip is not ItemContent
], is = 10;
class O extends de {
  get deleted() {
    return !0;
  }
  delete() {
  }
  /**
   * @param {Skip} right
   * @return {boolean}
   */
  mergeWith(t) {
    return this.constructor !== t.constructor ? !1 : (this.length += t.length, !0);
  }
  /**
   * @param {Transaction} transaction
   * @param {number} offset
   */
  integrate(t, e) {
    M();
  }
  /**
   * @param {UpdateEncoderV1 | UpdateEncoderV2} encoder
   * @param {number} offset
   */
  write(t, e) {
    t.writeInfo(is), _(t.restEncoder, this.length - e);
  }
  /**
   * @param {Transaction} transaction
   * @param {StructStore} store
   * @return {null | number}
   */
  getMissing(t, e) {
    return null;
  }
}
const pn = (
  /** @type {any} */
  typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : {}
), wn = "__ $YJS$ __";
pn[wn] === !0 && console.error("Yjs was already imported. This breaks constructor checks and will lead to issues! - https://github.com/yjs/yjs/issues/438");
pn[wn] = !0;
export {
  ti as AbsolutePosition,
  de as AbstractStruct,
  E as AbstractType,
  W as Array,
  Y as ContentAny,
  dt as ContentBinary,
  ct as ContentDeleted,
  at as ContentDoc,
  $ as ContentEmbed,
  C as ContentFormat,
  Et as ContentJSON,
  N as ContentString,
  U as ContentType,
  et as Doc,
  T as GC,
  X as ID,
  k as Item,
  Q as Map,
  Pn as RelativePosition,
  O as Skip,
  ii as Snapshot,
  Z as Text,
  oi as Transaction,
  Es as UndoManager,
  _t as UpdateDecoderV2,
  Gn as UpdateEncoderV1,
  Ot as UpdateEncoderV2,
  P as XmlElement,
  j as XmlFragment,
  Ct as XmlHook,
  Dt as XmlText,
  Ci as YArrayEvent,
  xt as YEvent,
  Ei as YMapEvent,
  Oi as YTextEvent,
  vi as YXmlEvent,
  Qn as applyUpdateV2,
  Ti as cleanupYTextFormatting,
  ut as compareIDs,
  Cs as createAbsolutePositionFromRelativePosition,
  Bn as createDeleteSet,
  Jn as createDeleteSetFromStructStore,
  w as createID,
  ys as createRelativePositionFromTypeIndex,
  si as createSnapshot,
  v as findIndexSS,
  Ge as findRootTypeKey,
  K as getItem,
  Ie as getItemCleanEnd,
  A as getItemCleanStart,
  y as getState,
  ht as isDeleted,
  St as isParentOf,
  q as iterateDeletedStructs,
  Qt as mergeDeleteSets,
  Me as mergeUpdatesV2,
  qn as readUpdateV2,
  Ds as snapshot,
  m as transact,
  Is as typeListToArraySnapshot,
  yi as typeMapGetAllSnapshot
};
