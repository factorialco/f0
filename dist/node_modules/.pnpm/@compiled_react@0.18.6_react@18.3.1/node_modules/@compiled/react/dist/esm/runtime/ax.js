function u(t) {
  if (!t.length)
    return;
  if (t.length === 1 && t[0] && // checking to see if `classNames[0]` is a string that contains other classnames
  !t[0].includes(" "))
    return t[0];
  const r = {};
  for (const n of t) {
    if (!n)
      continue;
    const e = n.split(" ");
    for (const o of e) {
      const f = o.startsWith("_") ? o.slice(0, 5) : o;
      r[f] = o;
    }
  }
  let i = "";
  for (const n in r)
    i += r[n] + " ";
  if (i)
    return i.trimEnd();
}
export {
  u as default
};
