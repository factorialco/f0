const r = document.createElement("i");
function c(t) {
  const n = "&" + t + ";";
  r.innerHTML = n;
  const e = r.textContent;
  return (
    // @ts-expect-error: TypeScript is wrong that `textContent` on elements can
    // yield `null`.
    e.charCodeAt(e.length - 1) === 59 && t !== "semi" || e === n ? !1 : e
  );
}
export {
  c as decodeNamedCharacterReference
};
