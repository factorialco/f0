import c from "./Point.js";
import { createIntersectContext as m } from "./BoundingRect.js";
var g = Math.min, M = Math.max, _ = Math.abs, h = [0, 0], u = [0, 0], a = m(), l = a.minTv, d = a.maxTv, O = (function() {
  function x(t, o) {
    this._corners = [], this._axes = [], this._origin = [0, 0];
    for (var e = 0; e < 4; e++)
      this._corners[e] = new c();
    for (var e = 0; e < 2; e++)
      this._axes[e] = new c();
    t && this.fromBoundingRect(t, o);
  }
  return x.prototype.fromBoundingRect = function(t, o) {
    var e = this._corners, i = this._axes, n = t.x, v = t.y, f = n + t.width, s = v + t.height;
    if (e[0].set(n, v), e[1].set(f, v), e[2].set(f, s), e[3].set(n, s), o)
      for (var r = 0; r < 4; r++)
        e[r].transform(o);
    c.sub(i[0], e[1], e[0]), c.sub(i[1], e[3], e[0]), i[0].normalize(), i[1].normalize();
    for (var r = 0; r < 2; r++)
      this._origin[r] = i[r].dot(e[0]);
  }, x.prototype.intersect = function(t, o, e) {
    var i = !0, n = !o;
    return o && c.set(o, 0, 0), a.reset(e, !n), !this._intersectCheckOneSide(this, t, n, 1) && (i = !1, n) || !this._intersectCheckOneSide(t, this, n, -1) && (i = !1, n) || !n && !a.negativeSize && c.copy(o, i ? a.useDir ? a.dirMinTv : l : d), i;
  }, x.prototype._intersectCheckOneSide = function(t, o, e, i) {
    for (var n = !0, v = 0; v < 2; v++) {
      var f = t._axes[v];
      if (t._getProjMinMaxOnAxis(v, t._corners, h), t._getProjMinMaxOnAxis(v, o._corners, u), a.negativeSize || h[1] < u[0] || h[0] > u[1]) {
        if (n = !1, a.negativeSize || e)
          return n;
        var s = _(u[0] - h[1]), r = _(h[0] - u[1]);
        g(s, r) > d.len() && (s < r ? c.scale(d, f, -s * i) : c.scale(d, f, r * i));
      } else if (!e) {
        var s = _(u[0] - h[1]), r = _(h[0] - u[1]);
        (a.useDir || g(s, r) < l.len()) && ((s < r || !a.bidirectional) && (c.scale(l, f, s * i), a.useDir && a.calcDirMTV()), (s >= r || !a.bidirectional) && (c.scale(l, f, -r * i), a.useDir && a.calcDirMTV()));
      }
    }
    return n;
  }, x.prototype._getProjMinMaxOnAxis = function(t, o, e) {
    for (var i = this._axes[t], n = this._origin, v = o[0].dot(i) + n[t], f = v, s = v, r = 1; r < o.length; r++) {
      var p = o[r].dot(i) + n[t];
      f = g(p, f), s = M(p, s);
    }
    e[0] = f + a.touchThreshold, e[1] = s - a.touchThreshold, a.negativeSize = e[1] < e[0];
  }, x;
})();
export {
  O as default
};
