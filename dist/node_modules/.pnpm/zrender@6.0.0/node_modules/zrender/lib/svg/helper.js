import { isFunction as g, logError as v, RADIAN_TO_DEGREE as o, retrieve2 as l } from "../core/util.js";
import { parse as w } from "../tool/color.js";
import x from "../core/env.js";
var a = Math.round;
function G(e) {
  var t;
  if (!e || e === "transparent")
    e = "none";
  else if (typeof e == "string" && e.indexOf("rgba") > -1) {
    var n = w(e);
    n && (e = "rgb(" + n[0] + "," + n[1] + "," + n[2] + ")", t = n[3]);
  }
  return {
    color: e,
    opacity: t ?? 1
  };
}
var p = 1e-4;
function y(e) {
  return e < p && e > -p;
}
function i(e) {
  return a(e * 1e3) / 1e3;
}
function h(e) {
  return a(e * 1e4) / 1e4;
}
function R(e) {
  return "matrix(" + i(e[0]) + "," + i(e[1]) + "," + i(e[2]) + "," + i(e[3]) + "," + h(e[4]) + "," + h(e[5]) + ")";
}
var M = {
  left: "start",
  right: "end",
  center: "middle",
  middle: "middle"
};
function T(e, t, n) {
  return n === "top" ? e += t / 2 : n === "bottom" && (e -= t / 2), e;
}
function X(e) {
  return e && (e.shadowBlur || e.shadowOffsetX || e.shadowOffsetY);
}
function Y(e) {
  var t = e.style, n = e.getGlobalScale();
  return [
    t.shadowColor,
    (t.shadowBlur || 0).toFixed(2),
    (t.shadowOffsetX || 0).toFixed(2),
    (t.shadowOffsetY || 0).toFixed(2),
    n[0],
    n[1]
  ].join(",");
}
function S(e) {
  return e && !!e.image;
}
function m(e) {
  return e && !!e.svgElement;
}
function I(e) {
  return S(e) || m(e);
}
function N(e) {
  return e.type === "linear";
}
function P(e) {
  return e.type === "radial";
}
function _(e) {
  return e && (e.type === "linear" || e.type === "radial");
}
function k(e) {
  return "url(#" + e + ")";
}
function A(e) {
  var t = e.getGlobalScale(), n = Math.max(t[0], t[1]);
  return Math.max(Math.ceil(Math.log(n) / Math.log(10)), 1);
}
function B(e) {
  var t = e.x || 0, n = e.y || 0, u = (e.rotation || 0) * o, f = l(e.scaleX, 1), s = l(e.scaleY, 1), d = e.skewX || 0, c = e.skewY || 0, r = [];
  return (t || n) && r.push("translate(" + t + "px," + n + "px)"), u && r.push("rotate(" + u + ")"), (f !== 1 || s !== 1) && r.push("scale(" + f + "," + s + ")"), (d || c) && r.push("skew(" + a(d * o) + "deg, " + a(c * o) + "deg)"), r.join(" ");
}
var C = (function() {
  return x.hasGlobalWindow && g(window.btoa) ? function(e) {
    return window.btoa(unescape(encodeURIComponent(e)));
  } : typeof Buffer < "u" ? function(e) {
    return Buffer.from(e).toString("base64");
  } : function(e) {
    return process.env.NODE_ENV !== "production" && v("Base64 isn't natively supported in the current environment."), null;
  };
})();
export {
  M as TEXT_ALIGN_TO_ANCHOR,
  T as adjustTextY,
  C as encodeBase64,
  k as getIdURL,
  R as getMatrixStr,
  A as getPathPrecision,
  B as getSRTTransformString,
  Y as getShadowKey,
  X as hasShadow,
  y as isAroundZero,
  _ as isGradient,
  S as isImagePattern,
  N as isLinearGradient,
  I as isPattern,
  P as isRadialGradient,
  m as isSVGPattern,
  G as normalizeColor,
  i as round3,
  h as round4
};
