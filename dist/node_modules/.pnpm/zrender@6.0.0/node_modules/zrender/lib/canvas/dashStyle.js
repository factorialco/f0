import { map as o, isNumber as u, isArray as i } from "../core/util.js";
function h(r, t) {
  return !r || r === "solid" || !(t > 0) ? null : r === "dashed" ? [4 * t, 2 * t] : r === "dotted" ? [t] : u(r) ? [r] : i(r) ? r : null;
}
function e(r) {
  var t = r.style, s = t.lineDash && t.lineWidth > 0 && h(t.lineDash, t.lineWidth), f = t.lineDashOffset;
  if (s) {
    var a = t.strokeNoScale && r.getLineScale ? r.getLineScale() : 1;
    a && a !== 1 && (s = o(s, function(n) {
      return n / a;
    }), f /= a);
  }
  return [s, f];
}
export {
  e as getLineDash,
  h as normalizeLineDash
};
