import { cubicRootAt as n, cubicAt as b } from "../core/curve.js";
import { trim as e } from "../core/util.js";
var o = /cubic-bezier\(([0-9,\.e ]+)\)/;
function N(i) {
  var u = i && o.exec(i);
  if (u) {
    var r = u[1].split(","), a = +e(r[0]), t = +e(r[1]), v = +e(r[2]), f = +e(r[3]);
    if (isNaN(a + t + v + f))
      return;
    var m = [];
    return function(c) {
      return c <= 0 ? 0 : c >= 1 ? 1 : n(0, a, v, 1, c, m) && b(0, t, f, 1, m[0]);
    };
  }
}
export {
  N as createCubicEasingFunc
};
