import { quadraticProjectPoint as m } from "../core/curve.js";
function s(o, a, e, f, u, i, c, t, n) {
  if (c === 0)
    return !1;
  var r = c;
  if (n > a + r && n > f + r && n > i + r || n < a - r && n < f - r && n < i - r || t > o + r && t > e + r && t > u + r || t < o - r && t < e - r && t < u - r)
    return !1;
  var l = m(o, a, e, f, u, i, t, n, null);
  return l <= r / 2;
}
export {
  s as containStroke
};
