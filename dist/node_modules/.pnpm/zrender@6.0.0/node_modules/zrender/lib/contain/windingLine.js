function v(e, r, d, i, f, n) {
  if (n > r && n > i || n < r && n < i || i === r)
    return 0;
  var t = (n - r) / (i - r), a = i < r ? 1 : -1;
  (t === 1 || t === 0) && (a = i < r ? 0.5 : -0.5);
  var u = t * (d - e) + e;
  return u === f ? 1 / 0 : u > f ? a : 0;
}
export {
  v as default
};
