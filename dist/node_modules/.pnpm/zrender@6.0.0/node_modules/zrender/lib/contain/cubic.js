import { cubicProjectPoint as s } from "../core/curve.js";
function P(o, e, f, u, a, c, i, l, m, t, n) {
  if (m === 0)
    return !1;
  var r = m;
  if (n > e + r && n > u + r && n > c + r && n > l + r || n < e - r && n < u - r && n < c - r && n < l - r || t > o + r && t > f + r && t > a + r && t > i + r || t < o - r && t < f - r && t < a - r && t < i - r)
    return !1;
  var p = s(o, e, f, u, a, c, i, l, t, n, null);
  return p <= r / 2;
}
export {
  P as containStroke
};
