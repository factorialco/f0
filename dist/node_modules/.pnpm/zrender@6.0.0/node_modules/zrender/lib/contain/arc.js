import { normalizeRadian as v } from "./util.js";
var i = Math.PI * 2;
function q(p, I, M, r, a, P, h, o, u) {
  if (h === 0)
    return !1;
  var m = h;
  o -= p, u -= I;
  var c = Math.sqrt(o * o + u * u);
  if (c - m > M || c + m < M)
    return !1;
  if (Math.abs(r - a) % i < 1e-4)
    return !0;
  if (P) {
    var b = r;
    r = v(a), a = v(b);
  } else
    r = v(r), a = v(a);
  r > a && (a += i);
  var f = Math.atan2(u, o);
  return f < 0 && (f += i), f >= r && f <= a || f + i >= r && f + i <= a;
}
export {
  q as containStroke
};
