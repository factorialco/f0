function u(n) {
  const t = n.values, r = Object.keys(n.values).filter((e) => typeof t[t[e]] != "number").map((e) => t[e]), s = Array.from(new Set(r.map((e) => typeof e)));
  return {
    type: s.length === 1 ? s[0] === "string" ? "string" : "number" : ["string", "number"],
    enum: r
  };
}
export {
  u as parseNativeEnumDef
};
