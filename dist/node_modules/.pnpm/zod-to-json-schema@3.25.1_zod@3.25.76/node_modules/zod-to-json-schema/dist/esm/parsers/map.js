import { parseDef as r } from "../parseDef.js";
import { parseRecordDef as i } from "./record.js";
import { parseAnyDef as a } from "./any.js";
function y(e, t) {
  if (t.mapStrategy === "record")
    return i(e, t);
  const m = r(e.keyType._def, {
    ...t,
    currentPath: [...t.currentPath, "items", "items", "0"]
  }) || a(t), p = r(e.valueType._def, {
    ...t,
    currentPath: [...t.currentPath, "items", "items", "1"]
  }) || a(t);
  return {
    type: "array",
    maxItems: 125,
    items: {
      type: "array",
      items: [m, p],
      minItems: 2,
      maxItems: 2
    }
  };
}
export {
  y as parseMapDef
};
