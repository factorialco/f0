function n(r, t) {
  const e = typeof r.value;
  return e !== "bigint" && e !== "number" && e !== "boolean" && e !== "string" ? {
    type: Array.isArray(r.value) ? "array" : "object"
  } : t.target === "openApi3" ? {
    type: e === "bigint" ? "integer" : e,
    enum: [r.value]
  } : {
    type: e === "bigint" ? "integer" : e,
    const: r.value
  };
}
export {
  n as parseLiteralDef
};
