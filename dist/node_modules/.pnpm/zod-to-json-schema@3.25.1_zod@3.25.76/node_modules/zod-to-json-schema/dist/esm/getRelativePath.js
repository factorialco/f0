const i = (t, n) => {
  let e = 0;
  for (; e < t.length && e < n.length && t[e] === n[e]; e++)
    ;
  return [(t.length - e).toString(), ...n.slice(e)].join("/");
};
export {
  i as getRelativePath
};
