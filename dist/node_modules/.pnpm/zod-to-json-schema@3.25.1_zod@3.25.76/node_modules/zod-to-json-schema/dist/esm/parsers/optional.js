import { parseDef as e } from "../parseDef.js";
import { parseAnyDef as o } from "./any.js";
const i = (n, t) => {
  if (t.currentPath.toString() === t.propertyPath?.toString())
    return e(n.innerType._def, t);
  const r = e(n.innerType._def, {
    ...t,
    currentPath: [...t.currentPath, "anyOf", "1"]
  });
  return r ? {
    anyOf: [
      {
        not: o(t)
      },
      r
    ]
  } : o(t);
};
export {
  i as parseOptionalDef
};
