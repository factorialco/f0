import { parseDef as n } from "../parseDef.js";
function a(r, m) {
  return r.rest ? {
    type: "array",
    minItems: r.items.length,
    items: r.items.map((t, e) => n(t._def, {
      ...m,
      currentPath: [...m.currentPath, "items", `${e}`]
    })).reduce((t, e) => e === void 0 ? t : [...t, e], []),
    additionalItems: n(r.rest._def, {
      ...m,
      currentPath: [...m.currentPath, "additionalItems"]
    })
  } : {
    type: "array",
    minItems: r.items.length,
    maxItems: r.items.length,
    items: r.items.map((t, e) => n(t._def, {
      ...m,
      currentPath: [...m.currentPath, "items", `${e}`]
    })).reduce((t, e) => e === void 0 ? t : [...t, e], [])
  };
}
export {
  a as parseTupleDef
};
