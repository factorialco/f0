import { parseDef as f } from "./parseDef.js";
import { getRefs as h } from "./Refs.js";
import { parseAnyDef as s } from "./parsers/any.js";
const A = (m, t) => {
  const e = h(t);
  let i = typeof t == "object" && t.definitions ? Object.entries(t.definitions).reduce((p, [o, c]) => ({
    ...p,
    [o]: f(c._def, {
      ...e,
      currentPath: [...e.basePath, e.definitionPath, o]
    }, !0) ?? s(e)
  }), {}) : void 0;
  const a = typeof t == "string" ? t : t?.name, r = f(
    m._def,
    e,
    !1
  ) ?? s(e);
  e.flags.hasReferencedOpenAiAnyType && (i || (i = {}), i[e.openAiAnyTypeName] || (i[e.openAiAnyTypeName] = {
    // Skipping "object" as no properties can be defined and additionalProperties must be "false"
    type: ["string", "number", "integer", "boolean", "array", "null"],
    items: {
      $ref: e.$refStrategy === "relative" ? "1" : [
        ...e.basePath,
        e.definitionPath,
        e.openAiAnyTypeName
      ].join("/")
    }
  }));
  const n = a === void 0 ? i ? {
    ...r,
    [e.definitionPath]: i
  } : r : {
    $ref: [
      ...e.$refStrategy === "relative" ? [] : e.basePath,
      e.definitionPath,
      a
    ].join("/"),
    [e.definitionPath]: {
      ...i,
      [a]: r
    }
  };
  return e.target === "jsonSchema7" ? n.$schema = "http://json-schema.org/draft-07/schema#" : (e.target === "jsonSchema2019-09" || e.target === "openAi") && (n.$schema = "https://json-schema.org/draft/2019-09/schema#"), e.target === "openAi" && ("anyOf" in n || "oneOf" in n || "allOf" in n || "type" in n && Array.isArray(n.type)) && console.warn("Warning: OpenAI may not support schemas with unions as roots! Try wrapping it in an object property."), n;
};
export {
  A as zodToJsonSchema
};
