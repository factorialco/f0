import { Editor } from '@tiptap/react';
interface LinkPopupProps {
    editor: Editor;
    disabled: boolean;
}
export declare const LinkPopup: ({ editor, disabled }: LinkPopupProps) => import("react").JSX.Element;
export {};
