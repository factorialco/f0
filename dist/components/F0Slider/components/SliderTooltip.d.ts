import { CSSProperties } from 'react';
interface SliderTooltipProps {
    visible: boolean;
    content: string;
    style?: CSSProperties;
}
export declare const SliderTooltip: ({ visible, content, style, }: SliderTooltipProps) => import("react").JSX.Element;
export {};
