import { F0TagListProps, TagType } from './types';
export declare const F0TagList: {
    <T extends TagType>({ type, tags, max, remainingCount: initialRemainingCount, }: F0TagListProps<T>): import("react").JSX.Element;
    displayName: string;
};
