import { TOCItem, TOCItemAction } from '../../../experimental/Navigation/F0TableOfContent';
export declare const mockOtherActions: TOCItemAction[];
export declare const mockTOCData: (setActiveItem: (id: string) => void) => TOCItem[];
export declare const courseModulesData: (setActiveItem: (id: string) => void) => TOCItem[];
