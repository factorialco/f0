import { SidebarChat } from './types';
export declare const SidebarChatItem: ({ chat, isActive, onClick, }: {
    chat: SidebarChat;
    isActive: boolean;
    onClick: () => void;
}) => import("react").JSX.Element;
