import { SizeToken } from '../types';
export declare const dimensionVariants: {
    width: Record<SizeToken, string>;
    height: Record<SizeToken, string>;
    minWidth: Record<SizeToken, string>;
    minHeight: Record<SizeToken, string>;
    maxWidth: Record<SizeToken, string>;
    maxHeight: Record<SizeToken, string>;
};
