import { NumericValue } from '../types';
/**
 * Gets the final value of a numeric value independent the usage of value or value_x100
 * @param numericValue
 * @returns
 */
export declare const numericFinalValue: (numericValue: NumericValue) => number | undefined;
