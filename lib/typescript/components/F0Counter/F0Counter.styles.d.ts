/**
 * Container (pill) variants for `F0Counter`.
 *
 * Sizing and tokens follow the F0-React-Native-Uniwind Figma spec.
 * Only `default` and `primary` have visible borders; `selected` and
 * `notification` are borderless — matching Figma exactly.
 */
export declare const f0CounterContainerVariants: import("node_modules/tailwind-variants/dist").TVReturnType<{
    size: {
        md: string;
        sm: string;
    };
    type: {
        default: string;
        primary: string;
        selected: string;
        notification: string;
    };
}, undefined, "flex items-center justify-center rounded-xs grow-0", {
    size: {
        md: string;
        sm: string;
    };
    type: {
        default: string;
        primary: string;
        selected: string;
        notification: string;
    };
}, undefined, import("node_modules/tailwind-variants/dist").TVReturnType<{
    size: {
        md: string;
        sm: string;
    };
    type: {
        default: string;
        primary: string;
        selected: string;
        notification: string;
    };
}, undefined, "flex items-center justify-center rounded-xs grow-0", unknown, unknown, undefined>>;
//# sourceMappingURL=F0Counter.styles.d.ts.map