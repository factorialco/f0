# Approved Driver.js adaptation

Driver.js 1.4.0 runtime, declarations, stylesheet and MIT license are copied unchanged from the installed Factorial monorepo package. `tourStyles.css` is copied unchanged from `frontend/src/modules/get_started/lib/tours/tourStyles.css`.

User approved this exact renderer and appearance on 2026-09-12. Runtime is vendored inside this prototype to avoid changing shared package dependencies during parallel Home work. The original One cursor remains separate. `navigation-tour.css` enables navigation rail and skip/pause controls while the panel spotlight is active. No tracking, model calls or production demo actions are imported.
