import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the People tab (chrome lives in ProjectsLayout). */
export function PeopleBody() {
  return (
    <OneEmptyState
      emoji="🧑‍🤝‍🧑"
      title="People"
      description="Manage who can be assigned to projects and their availability. This tab is a placeholder — the focus of this prototype is the Projects overview."
    />
  )
}
