import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the Jobs and rates tab (chrome lives in ProjectsLayout). */
export function JobsRatesBody() {
  return (
    <OneEmptyState
      emoji="💶"
      title="Jobs and rates"
      description="Define billable jobs and their hourly rates. This tab is a placeholder — the focus of this prototype is the Projects overview."
    />
  )
}
