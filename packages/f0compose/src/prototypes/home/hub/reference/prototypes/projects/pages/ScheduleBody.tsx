import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the Schedule tab (chrome lives in ProjectsLayout). */
export function ScheduleBody() {
  return (
    <OneEmptyState
      emoji="📅"
      title="Schedule"
      description="Plan project work across people and weeks. This tab is a placeholder — the focus of this prototype is the Projects overview."
    />
  )
}
