import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the Tracking tab (chrome lives in ProjectsLayout). */
export function TrackingBody() {
  return (
    <OneEmptyState
      emoji="⏱️"
      title="Tracking"
      description="Track time and progress logged against each project. This tab is a placeholder — the focus of this prototype is the Projects overview."
    />
  )
}
