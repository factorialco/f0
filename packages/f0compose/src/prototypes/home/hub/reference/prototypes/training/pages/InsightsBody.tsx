import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the Insights tab (chrome lives in TrainingLayout). */
export function InsightsBody() {
  return (
    <OneEmptyState
      emoji="📊"
      title="Insights"
      description="Analyze training completion, validity and engagement. This tab is a placeholder — the focus of this prototype is the Courses catalog."
    />
  )
}
