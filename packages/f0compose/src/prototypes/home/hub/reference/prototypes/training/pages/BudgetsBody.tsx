import { OneEmptyState } from "@factorialco/f0-react"

/** Placeholder body for the Budgets tab (chrome lives in TrainingLayout). */
export function BudgetsBody() {
  return (
    <OneEmptyState
      emoji="💰"
      title="Budgets"
      description="Plan and track training budgets per team and period. This tab is a placeholder — the focus of this prototype is the Courses catalog."
    />
  )
}
