// Responsive utility names from F0 List and itemActions.
// The composer utility pass must emit these after its base utilities,
// which otherwise override the responsive rules in F0’s earlier CSS bundle.
export const f0ListResponsiveUtilities =
  "md:after:block md:flex md:flex-row md:gap-1 md:gap-2 md:hidden md:inline md:items-center md:p-2 md:p-3 md:pl-3 md:pr-4 md:w-full"
