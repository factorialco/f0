import type { PolicyRulesHandle } from "../usePolicyRulesData"

/**
 * Exceptions — intentionally a fully blank canvas for now. No hero,
 * no empty-state card; the surface isn't designed yet, so we leave it
 * clean rather than showing placeholder chrome.
 */
export function ExceptionsView(_props: { rules: PolicyRulesHandle }) {
  return null
}
