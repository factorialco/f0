import type { PolicyRulesHandle } from "../usePolicyRulesData"

/**
 * Card controls — intentionally a fully blank canvas for now (mirrors
 * Exceptions). The nav entry is clickable, but the surface isn't
 * designed yet, so we leave it clean rather than showing placeholder
 * chrome.
 */
export function CardControlsView(_props: { rules: PolicyRulesHandle }) {
  return null
}
