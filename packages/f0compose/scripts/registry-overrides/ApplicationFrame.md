## Description

The product shell. Wraps a page with a collapsible sidebar and main content area.

In f0compose prototypes, **DO NOT** import or render ApplicationFrame yourself — the framework wraps your prototype automatically via `FactorialShell`. Just return the page body from your prototype's default export.
