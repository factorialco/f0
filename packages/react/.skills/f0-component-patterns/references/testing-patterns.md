# Testing Patterns

This file has moved. Load the **`f0-unit-testing`** skill for all unit test patterns:

- Standard imports (`zeroRender`, Vitest, `@/testing/test-utils`)
- `zeroRender` vs raw `render`
- `userEvent` with fake timers
- `vi.hoisted()` pattern
- Helper functions for multi-step interactions
- Deferred promise pattern
- Query priority and coverage rules

**Skill location:** `packages/react/.skills/f0-unit-testing/SKILL.md`
