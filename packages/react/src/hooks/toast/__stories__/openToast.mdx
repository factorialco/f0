import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as OpenToastStories from "./openToast.stories"

<Meta of={OpenToastStories} />

# openToast

## Introduction

### Definition

`openToast` is an imperative function for triggering toast notifications. It is **not a hook** — you can call it from anywhere (event handlers, async callbacks, even outside React), as long as your app is wrapped in `<F0Provider>` (which mounts the `ToastProvider`). Open toasts live in a module-level store that the provider renders into a portal.

### Purpose

- **No hook, no wiring:** Call `openToast(...)` directly — no hook to call, no `useState`, no conditional rendering
- **Automatic lifecycle:** Toasts auto-dismiss after a configurable duration and clean up after themselves
- **Consistent UX:** Standardized toast patterns with stacking, hover-to-expand, and smooth animations
- **Flexible API:** Support for variants, descriptions, action buttons/links, custom durations, and persistent toasts
- **Programmatic control:** Dismiss individual toasts (`closeToast`) or all at once (`closeAllToasts`) from anywhere in your code

The provider handles rendering, positioning, stacking, timers, and cleanup automatically.

## Basic Usage

### Simple toast

The minimum required option is `title`:

```tsx
import { openToast } from "@/hooks/toast"

function MyComponent() {
  return (
    <button onClick={() => openToast({ title: "Profile updated" })}>
      Save Profile
    </button>
  )
}
```

### Toast with description

Use `description` to provide additional context below the title:

```tsx
openToast({
  title: "File uploaded",
  description: "report-q4-2024.pdf has been uploaded successfully.",
})
```

## Variants

Toasts support four visual variants to communicate intent. The default variant is `"default"`.

### Default

```tsx
openToast({ variant: "default", title: "Changes saved" })
```

### Success

```tsx
openToast({ variant: "success", title: "Payment processed" })
```

### Warning

```tsx
openToast({ variant: "warning", title: "Storage almost full" })
```

### Error

```tsx
openToast({ variant: "error", title: "Upload failed" })
```

## Actions

Toasts can include up to two actions: one button and one link. Actions are dismissed automatically when clicked, unless `keepOpen` is set.

### Button action

Use a button action for in-page operations:

```tsx
openToast({
  title: "Message deleted",
  variant: "default",
  actions: {
    type: "button",
    label: "Undo",
    onClick: () => restoreMessage(),
  },
})
```

### Link action

Use a link action to navigate the user to a related page:

```tsx
openToast({
  title: "Report ready",
  variant: "success",
  actions: {
    type: "link",
    label: "View report",
    href: "/reports/latest",
  },
})
```

### Button + link (combined)

You can combine one button and one link:

```tsx
openToast({
  title: "Export complete",
  variant: "success",
  actions: [
    {
      type: "button",
      label: "Undo",
      onClick: () => cancelExport(),
    },
    {
      type: "link",
      label: "Download",
      href: "/exports/file.csv",
    },
  ],
})
```

## Duration and Persistence

### Default duration

Toasts auto-dismiss after **10 seconds** by default.

### Custom duration

Override the duration (in milliseconds) via the `duration` option:

```tsx
// Dismiss after 3 seconds
openToast({ title: "Copied to clipboard", duration: 3000 })

// Dismiss after 30 seconds
openToast({ title: "Background sync started", duration: 30000 })
```

### Persistent toasts

Set `persistent: true` to keep a toast visible until the user manually closes it or you dismiss it programmatically. When `persistent` is set, `duration` is ignored.

```tsx
const id = openToast({
  title: "Uploading…",
  description: "Your file is being uploaded. Please wait.",
  variant: "default",
  persistent: true,
})

// Dismiss it later when done
await uploadFile()
closeToast(id)
```

## Programmatic Control

### Returning the toast ID

`openToast()` returns the ID of the created toast, which you can use to remove it later:

```tsx
const id = openToast({ title: "Saving…", persistent: true })

await save()

closeToast(id)
```

### Custom ID

Provide your own `id` to make referencing the toast easier — for example, to avoid duplicate toasts for the same resource:

```tsx
openToast({
  id: "sync-status",
  title: "Syncing…",
  persistent: true,
})

// Calling with the same id again replaces the existing toast
openToast({
  id: "sync-status",
  title: "Sync complete",
  variant: "success",
})
```

### closeToast

Dismiss a specific toast by ID:

```tsx
import { openToast, closeToast } from "@/hooks/toast"

const id = openToast({ title: "Processing…", persistent: true })

// Later...
closeToast(id)
```

### closeAllToasts

Dismiss all currently visible toasts at once:

```tsx
import { closeAllToasts } from "@/hooks/toast"

// e.g. on logout
closeAllToasts()
```

## Interactive Demo

<Canvas of={OpenToastStories.ToastPlayground} />

## Type Reference

### ToastOptions

Options passed to the `openToast()` function.

```tsx
type ToastOptions = {
  /**
   * The title of the toast.
   */
  title?: string

  /**
   * Optional description displayed below the title.
   */
  description?: string

  /**
   * The visual variant of the toast.
   * @default "default"
   */
  variant?: F0ToastVariant

  /**
   * The actions to display in the toast.
   * Supports a single button, a single link, or a [button, link] tuple.
   */
  actions?:
    | F0ToastAction
    | [F0ToastAction]
    | [ToastActionButton, ToastActionLink]

  /**
   * Optional id for the toast. If not provided, a random id will be generated.
   * Providing the same id twice replaces the existing toast.
   */
  id?: ToastId
} & (
  | {
      /**
       * The duration of the toast in milliseconds.
       * @default 10000
       */
      duration?: number
      persistent?: undefined
    }
  | {
      duration?: undefined
      /**
       * If true, the toast will not auto-dismiss.
       * The user must close it manually, or call closeToast(id) / closeAllToasts().
       */
      persistent: true
    }
)
```

### Functions

```tsx
/**
 * Show a toast. Returns the id of the created toast.
 */
function openToast(options: ToastOptions): ToastId

/**
 * Dismiss a toast by id.
 */
function closeToast(id: ToastId): void

/**
 * Dismiss all currently visible toasts.
 */
function closeAllToasts(): void
```

### F0ToastVariant

```tsx
type F0ToastVariant = "default" | "success" | "warning" | "error"
```

### F0ToastAction

Action types supported by the `actions` option.

```tsx
type ToastActionButton = {
  type: "button"
  label: string
  onClick: () => void
  icon?: IconType
  /**
   * If true, the toast will not close when this action is clicked.
   */
  keepOpen?: boolean
}

type ToastActionLink = {
  type: "link"
  label: string
  href: string
  /**
   * If true, the toast will not close when this action is clicked.
   */
  keepOpen?: boolean
}

type F0ToastAction = ToastActionButton | ToastActionLink
```
