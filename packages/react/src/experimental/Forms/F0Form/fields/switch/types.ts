import type { BaseFieldDefinition } from "../types"

/**
 * Switch field definition (renders Switch component)
 */
export interface SwitchFieldDefinition extends BaseFieldDefinition {
  type: "switch"
}
