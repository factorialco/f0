import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks"
import * as PersonStories from "./F0AvatarPerson.stories"
import { DoDonts } from "@/lib/storybook-utils/do-donts"

import { F0AvatarPerson } from "../F0AvatarPerson"

<Meta of={PersonStories} />

# AvatarPerson

The avatar for an individual — an employee, user or contact.

---

## Overview

### Purpose

- Represent a specific person consistently across the interface
- Help distinguish people at a glance in lists, headers and assignments
- Convey a person's state, such as deactivated

### Anatomy

Person avatars are always circular — the only avatar type that is, setting
people apart from the square shape used by every other type.

<Canvas of={PersonStories.Default} />

<Controls of={PersonStories.Default} />

### Variants

#### States

A person can be shown as deactivated, which replaces the content with a muted
person icon to signal that the account is no longer active.

<Canvas of={PersonStories.Deactivated} />

#### Badge

A badge marks a status on the avatar's corner.

<Canvas of={PersonStories.WithBadge} />

---

## Guidelines

### Design best practices

#### When to use

- The entity is always a person and known at build time
- Profile headers, employee rows, comment authors, assignees

#### When NOT to use

- The entity type is dynamic (could be a team or company): use `F0Avatar`
- The entity is not a person: use the matching `F0Avatar*` component

<DoDonts
  do={{
    description:
      "Pass the person's real first and last name, so the initials and the derived color identify them even with no photo",
    children: <F0AvatarPerson size="lg" firstName="Dani" lastName="Moreno" />,
  }}
  dont={{
    description:
      "Don't paper over missing data with a placeholder name — every unknown person then shares the same initials and the same color",
    children: <F0AvatarPerson size="lg" firstName="Unknown" lastName="User" />,
  }}
/>

---

## Behavior

### Image fallback

With no `src` — or if the image fails to load — the avatar shows the initials
from the name over a color derived deterministically from it, so the same person
always keeps the same color.

#### With a photo

<Canvas of={PersonStories.WithImage} />

---

## Accessibility

The avatar is decorative by default: unless you pass `aria-label` or
`aria-labelledby`, it renders `aria-hidden` and screen readers skip it entirely.

- In a row, header or comment where the person's name is already text, leave the
  label off and let that text carry the meaning. A labelled avatar next to the
  same words is announced twice.
- When the avatar stands alone — an assignee cell, a compact stack — pass
  `aria-label` with the person's name so it is reachable at all.
- `aria-labelledby` takes the **id** of the element holding the name, not the
  name itself.
- `deactivated` is conveyed by colour and a muted icon only. If that state
  matters, say it in text next to the avatar.
