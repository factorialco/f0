import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks"
import * as FlagStories from "./F0AvatarFlag.stories"
import { DoDonts } from "@/lib/storybook-utils/do-donts"
import { F0AvatarFlag } from "../F0AvatarFlag.tsx"

<Meta of={FlagStories} />

# AvatarFlag

The avatar for a country or locale.

---

## Overview

### Purpose

- Represent a country, region or locale by its flag
- Make locale-specific items recognizable at a glance

### Anatomy

A flag avatar is square and displays the flag image for the given country or
region code.

<Canvas of={FlagStories.Default} />

<Controls of={FlagStories.Default} />

---

## Guidelines

### Design best practices

#### When to use

- The entity is always a country, region or locale
- Language and region pickers, localized content, nationality fields

#### When NOT to use

- The entity type is dynamic: use `F0Avatar`
- The entity is not a country/locale: use the matching `F0Avatar*` component
#### Do's and don'ts

<DoDonts
  do={{
    description:
      "Use it for a place, from the country code already on the record, and name that place in text.",
    guidelines: [
      "Workplace location, legal entity country, nationality field",
      "A flag on its own is decoration, not a label",
    ],
    children: (
      <div className="flex items-center gap-2">
        <F0AvatarFlag flag="es" size="lg" />
        <span>Barcelona, Spain</span>
      </div>
    ),
  }}
  dont={{
    description: "Don't use a flag to stand for a language.",
    guidelines: [
      "Spanish is not the Spanish flag — it mislabels employees in Mexico or Argentina",
      "In a locale picker, show the language name and region code instead",
    ],
    children: (
      <div className="flex items-center gap-2">
        <F0AvatarFlag flag="es" size="lg" />
        <span>Spanish</span>
      </div>
    ),
  }}
/>

