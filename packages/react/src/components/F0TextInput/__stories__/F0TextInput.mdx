import { Canvas, Controls, Meta, Unstyled } from "@storybook/addon-docs/blocks"

import * as Stories from "./F0TextInput.stories"
import { DoDonts } from "@/lib/storybook-utils/do-donts"
import { F0TextInput } from "../index"

<Meta of={Stories} />

# F0TextInput

The canonical writable text field — a single-line box where text, emails, passwords or other free-form values are typed. A thin wrapper over [F0InputField](?path=/docs/components-primitives-f0inputfield--documentation) that adds text-specific behavior: automatic password masking, a `"private"` mode for sensitive non-credential data, and an `onPressEnter` shortcut. For numeric data use [F0NumberInput](?path=/docs/components-inputs-number-input--documentation); for durations use [F0DurationInput](?path=/docs/components-inputs-duration-input--documentation); for search queries use [F0SearchInput](?path=/docs/components-inputs-search-input--documentation).

## Anatomy

<Canvas of={Stories.Primary} />

<Controls of={Stories.Primary} />

## Guidelines

### Design best practices

**When to use**

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use F0TextInput when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Free-form single-line text</td>
        <td>
          Names, titles, descriptions, usernames, codes (invoice numbers, IDs
          that may include letters)
        </td>
      </tr>
      <tr>
        <td>Structured text values</td>
        <td>
          Email addresses and URLs — pass the matching <code>type</code> and{" "}
          <code>autocomplete</code>
        </td>
      </tr>
      <tr>
        <td>Identifiers that look numeric but must preserve formatting</td>
        <td>
          Postal codes, credit card numbers — pass the right{" "}
          <code>autocomplete</code> so the value keeps leading zeros and
          separators. For phone numbers use <code>F0PhoneInput</code> instead —
          it handles country dial codes, national formatting and structured
          output
        </td>
      </tr>
      <tr>
        <td>A login password the browser should help fill</td>
        <td>
          Use <code>type="password"</code> — see <strong>Behavior</strong> below
        </td>
      </tr>
      <tr>
        <td>
          Sensitive, non-credential data a password manager must never capture
        </td>
        <td>
          Use <code>type="private"</code> — SSNs, national IDs, account numbers,
          tokens. See <strong>Behavior</strong> below
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**When not to use**

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use instead</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>The value is a quantity to compute with</td>
        <td>
          <code>F0NumberInput</code>
        </td>
      </tr>
      <tr>
        <td>The value is a duration in hours and minutes</td>
        <td>
          <code>F0DurationInput</code>
        </td>
      </tr>
      <tr>
        <td>The value is a search query that filters a list</td>
        <td>
          <code>F0SearchInput</code>
        </td>
      </tr>
      <tr>
        <td>The value spans multiple lines</td>
        <td>
          <code>F0TextAreaInput</code>
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**Do's and don'ts**

<DoDonts
  do={{
    description:
      'Use type="private" for sensitive, non-credential data — SSNs, national IDs, account numbers, tokens — with a concrete label so the eye toggle\'s accessible name is unambiguous.',
    children: (
      <F0TextInput
        type="private"
        label="Social security number"
        value="123-45-6789"
      />
    ),
  }}
  dont={{
    description:
      "Don't use type=\"password\" outside of an actual login password. It shows a lock icon and invites password managers to offer to save the value — the wrong signal for a one-time token or someone else's SSN.",
    children: (
      <F0TextInput
        type="password"
        label="API token"
        placeholder="Paste your token"
      />
    ),
  }}
/>

### Content best practices

- **Label**: Sentence case, no period. Name the value concretely ("Social security number", not "Number")
- **Placeholder**: An example of the expected format, never a substitute for the label
- **Hint**: One short sentence clarifying the format or constraint when the label is not enough

### Behavior

The `type` prop accepts every native HTML input type **except `"number"`**, which is blocked at the type level — for numeric data use `F0NumberInput`. Common values are `"text"` (default), `"email"`, `"password"`, `"url"`, and `"tel"`. It also accepts the F0-specific `"private"`. `type="file"` is accepted but provides no drag-and-drop, validation or preview — use a dedicated upload component for files.

**Password mode**

When `type="password"`:

- A lock icon replaces any `icon` prop.
- A trailing eye-toggle button appears, with i18n labels ("Show password" / "Hide password"), to flip between masked and visible text.
- The `buttonToggle` prop becomes private — consumers cannot override the eye toggle while in password mode.

<Canvas of={Stories.Password} />

**Private mode**

`type="private"` is an F0-specific subtype for **sensitive, non-credential data** — Social Security numbers, national IDs, account numbers, tokens. It masks the value like `password`, but differs in two ways that matter:

- **No lock icon** — the consumer `icon` prop is respected (default: none), so the field does not read as a login credential.
- **Password managers are disabled** — the field sets `autocomplete="off"` plus `data-1p-ignore`, `data-lpignore`, `data-bwignore` and `data-form-type="other"`, so 1Password, LastPass, Bitwarden and browser autofill never offer to save or fill it.

A trailing eye-toggle flips between masked and visible text; its accessible name is built from the field label (e.g. "Show social security number") so screen-reader users can tell multiple private fields apart. As with password mode, the `buttonToggle` prop is private. Use `type="password"` for the user's own login password, where autofill is helpful; use `type="private"` for secrets a password manager must never capture.

<Canvas of={Stories.Private} />

**Enter key**

`onPressEnter` fires when `Enter` is pressed while the input has focus. Useful for form-less search boxes, inline editing, or single-field submit flows. It does not call `preventDefault` — wrap the input in a `<form onSubmit>` if native form submission is also needed.

**Inline variant**

`variant="inline"` is the detail-row presentation: a form that reads as rows, where the value prints as text and becomes its own editor when the row activates it. With no `variant`, nothing changes — the field presentation is still the default.

The variant is presentational only. It holds no mode state: the parent passes `editing` and the component draws the matching presentation. When the user does something that would end an edit, the component reports it through `onDismiss` and keeps drawing the editor until `editing` changes. A component that closed itself would leave the row stuck, because the row decides whether a value commits, reverts, or stays in edit with a validation error showing.

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Prop</th>
        <th className="text-left">Accepted when</th>
        <th className="text-left">What it does</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>variant</code>
        </td>
        <td>Always</td>
        <td>
          <code>"field"</code> (default) or <code>"inline"</code>
        </td>
      </tr>
      <tr>
        <td>
          <code>editing</code>
        </td>
        <td>
          <code>variant="inline"</code> only
        </td>
        <td>
          <code>false</code> (default) draws the value as plain text;{" "}
          <code>true</code> draws the input and focuses it
        </td>
      </tr>
      <tr>
        <td>
          <code>onDismiss</code>
        </td>
        <td>
          <code>variant="inline"</code> only
        </td>
        <td>
          Reports <code>"commit"</code> on Enter, <code>"escape"</code> on
          Escape, and <code>"blur"</code> when focus leaves
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

At rest the value is plain text: no border, no background, no input element, no tab stop — the activator belongs to the row. An empty value shows the `placeholder` in the secondary foreground colour, and a value too long for the box is cut with an ellipsis. Both presentations fill the box they are given in both axes and put the first glyph at the same x, so the text does not move when the row is activated.

`label` stays required. Pass `hideLabel`, as every inline field renderer does, and it becomes the value's accessible name instead of a visible label.

```tsx
const [editing, setEditing] = useState(false)

<F0TextInput
  variant="inline"
  label="Job title"
  hideLabel
  value={value}
  editing={editing}
  onChange={setValue}
  onDismiss={(reason) => {
    if (reason === "escape") setValue(valueAtActivation)
    setEditing(false)
  }}
/>
```
