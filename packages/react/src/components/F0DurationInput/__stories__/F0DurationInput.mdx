import { Canvas, Controls, Meta, Unstyled } from "@storybook/addon-docs/blocks"

import * as Stories from "./F0DurationInput.stories"

<Meta of={Stories} />

# F0DurationInput

`F0DurationInput` is a compound input for entering time durations. It renders one editable numeric segment per visible unit (days, hours, minutes, seconds) inside a single bordered container, and exposes the total as **seconds** through a controlled `value` / `onChange` pair.

Use it in product code wherever a user must type a duration. It shares chrome conventions (label, status, sizes, disabled, readonly) with [InputField](?path=/docs/components-primitives-inputfield--documentation), but is not a wrapper of it — it ships its own container so the segments can share a single focus ring.

---

## Overview

### Anatomy

<Canvas of={Stories.Default} />

- **Label**: text describing the input's purpose. Always rendered into the DOM (use `hideLabel` to keep it screen-reader only).
- **Segments**: one `<input>` per visible unit, with an inline suffix (`d`, `h`, `min`, `s` by default).
- **Bullet separator**: rendered between segments.
- **Message**: optional `status` message (`error` / `warning` / `info`) below the field.

<Controls of={Stories.Default} />

### Variants

#### Sizes

<Canvas of={Stories.SmallSize} />

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Size</th>
        <th>Height</th>
        <th>When to use</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>sm</code>
        </td>
        <td>32px</td>
        <td>Dense layouts, inline filters, table cells.</td>
      </tr>
      <tr>
        <td>
          <code>md</code>
        </td>
        <td>40px (default)</td>
        <td>Forms and primary input surfaces.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

#### Unit selection

Pass `units` to control which segments are rendered. The order is always
`days → hours → minutes → seconds`, regardless of the array order.

<Canvas of={Stories.AllUnits} />

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Unit</th>
        <th>Default suffix</th>
        <th>Seconds per unit</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>days</code>
        </td>
        <td>
          <code>d</code>
        </td>
        <td>86400</td>
      </tr>
      <tr>
        <td>
          <code>hours</code>
        </td>
        <td>
          <code>h</code>
        </td>
        <td>3600</td>
      </tr>
      <tr>
        <td>
          <code>minutes</code>
        </td>
        <td>
          <code>min</code>
        </td>
        <td>60</td>
      </tr>
      <tr>
        <td>
          <code>seconds</code>
        </td>
        <td>
          <code>s</code>
        </td>
        <td>1</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

Defaults to `["hours", "minutes"]` when `units` is omitted or empty.

---

## Guidelines

### Design best practices

#### When to use

- The user must enter a length of time (timer, break duration, leave balance, SLA, contract length).
- The duration is naturally expressed in mixed units (`1h 30min`, `2d 4h`).
- You need a tighter, segmented surface instead of a free-text input.

#### When NOT to use

- The value is a **point in time** (date, time of day, datetime). Use a date or time picker instead.
- The value is a **single dimension-less number** (count, percentage). Use [F0NumberInput](?path=/docs/components-inputs-numberinput--documentation).
- You need to express ranges (`from / to`). Compose two date pickers or a range picker.

---

## Behavior

What `F0DurationInput` guarantees when used as a controlled input.

A11y wiring is not visually demonstrable. It includes: `role="group"` on the container with `aria-label` derived from `label` or `ariaLabel`, per-segment `aria-label` (overridable via `fields[unit].ariaLabel`), `inputMode="numeric"`, `aria-disabled`, `aria-readonly`, and `aria-invalid` propagation. A `console.warn` fires in development when neither `label` nor `ariaLabel` is provided.

### Value model

`value` and `onChange` always work in **seconds**. The component converts to and from visible segments internally:

- `value={5400}` with `units={["hours", "minutes"]}` renders `1h 30min`.
- `value={90061}` with `units={["days", "hours", "minutes", "seconds"]}` renders `1d 1h 1min 1s`.
- Non-finite or negative values are coerced to `0`.

Segments that are **not visible** do not contribute to the emitted value. With `units={["hours", "minutes"]}`, the seconds component is dropped — `5430` becomes `5400` (`1h 30min`) on the next normalisation.

### Rollover

While typing, the input keeps raw values visible so the user can see what they entered. On blur, it normalises into the visible units:

- Typing `75` into minutes with `units={["hours", "minutes"]}` displays `0h 75min` while editing.
- On blur, the input recomputes from total seconds and displays `1h 15min`.
- If hours are not visible, `75min` stays as `75min`.

### Per-field config

Each unit accepts an optional config via `fields={{ [unit]: { ... } }}`:

- `max` — clamps the typed value. Typing `99` into a field with `max: 59` clamps to `59`.
- `maxVisibleDigits` — caps the width of the segment in `ch` units. Defaults to `2`. Increase it when `max` allows three-digit values (e.g. `max: 999`).
- `suffix` — overrides the default suffix (`d`, `h`, `min`, `s`).
- `ariaLabel` — overrides the segment's accessible name.

### Status messages

Pass `status={{ type, message }}` to colour the border and render a message below the field. The same precedence rules as [InputField](?path=/docs/components-primitives-inputfield--documentation) apply: `error` > `warning` > `info`.

<Canvas of={Stories.WithError} />

### Custom suffixes

<Canvas of={Stories.CustomSuffixes} />

---

## Props

### General

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>id</code>
        </td>
        <td>
          <code>string</code>
        </td>
        <td>Applied to the container element.</td>
      </tr>
      <tr>
        <td>
          <code>size</code>
        </td>
        <td>
          <code>"sm" | "md"</code>
        </td>
        <td>
          Defaults to <code>"md"</code>.
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### Value

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>value</code>
        </td>
        <td>
          <code>number</code>
        </td>
        <td>Total duration in seconds. Required.</td>
      </tr>
      <tr>
        <td>
          <code>onChange</code>
        </td>
        <td>
          <code>(seconds: number) =&gt; void</code>
        </td>
        <td>Fired on every keystroke with the total in seconds. Required.</td>
      </tr>
      <tr>
        <td>
          <code>onBlur</code>
        </td>
        <td>
          <code>() =&gt; void</code>
        </td>
        <td>Fired after the input normalises visible units.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### Units and fields

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>units</code>
        </td>
        <td>
          <code>("days" | "hours" | "minutes" | "seconds")[]</code>
        </td>
        <td>
          Visible segments. Order in the array is ignored; segments always
          render <code>days → hours → minutes → seconds</code>. Defaults to{" "}
          <code>["hours", "minutes"]</code>.
        </td>
      </tr>
      <tr>
        <td>
          <code>fields</code>
        </td>
        <td>
          <code>
            Partial&lt;Record&lt;DurationUnit, DurationFieldConfig&gt;&gt;
          </code>
        </td>
        <td>
          Per-unit overrides for <code>max</code>, <code>maxVisibleDigits</code>
          , <code>suffix</code>, <code>ariaLabel</code>.
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### State

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>status</code>
        </td>
        <td>
          <code>{`{ type: "error" | "warning" | "info"; message: string }`}</code>
        </td>
        <td>Colours the border and renders the message below the field.</td>
      </tr>
      <tr>
        <td>
          <code>disabled</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>Blocks editing and removes segments from the tab order.</td>
      </tr>
      <tr>
        <td>
          <code>readonly</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>Keeps the value visible and focusable but blocks edits.</td>
      </tr>
      <tr>
        <td>
          <code>required</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>Renders the required marker next to the label.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### Label

<Unstyled>
  <table>
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>label</code>
        </td>
        <td>
          <code>string</code>
        </td>
        <td>Visible label and accessible name. Required.</td>
      </tr>
      <tr>
        <td>
          <code>ariaLabel</code>
        </td>
        <td>
          <code>string</code>
        </td>
        <td>
          Overrides the accessible name. Falls back to <code>label</code> when
          omitted.
        </td>
      </tr>
      <tr>
        <td>
          <code>hideLabel</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>Hides the visible label; still rendered for screen readers.</td>
      </tr>
      <tr>
        <td>
          <code>aria-describedby</code>
        </td>
        <td>
          <code>string</code>
        </td>
        <td>Propagated to the container and to every segment.</td>
      </tr>
      <tr>
        <td>
          <code>aria-invalid</code>
        </td>
        <td>
          <code>boolean | "true" | "false" | "grammar" | "spelling"</code>
        </td>
        <td>Propagated to the container and to every segment.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>
