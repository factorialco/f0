import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as HourDistributionStories from "./hourDistribution.stories"

<Meta of={HourDistributionStories} />

### hourDistribution

Renders a compact bar chart of worked hours per day. Hover over each bar to see the date and hours worked. Used in OneDataCollection table columns for hour distribution.

#### Value type

```
type value = {
  dataPoints: Array<{
    date: string   // ISO or YYYY-MM-DD
    minutes: number
    plannedMinutes?: number  // when set, underworked days (minutes < plannedMinutes) use orange/yellow; else teal
  }>
}
```

#### HourDistributionWithData

<Canvas of={HourDistributionStories.HourDistributionWithData} />

#### HourDistributionShortPeriod

<Canvas of={HourDistributionStories.HourDistributionShortPeriod} />

#### HourDistributionEmpty

<Canvas of={HourDistributionStories.HourDistributionEmpty} />
