/**
 * Moved to `@/patterns/ApplicationFrame/SidePanel` once the panel stopped
 * belonging to the AI chat. Re-exported under its old name so this path — and
 * the kit's own imports — keep working.
 */
export { SidePanelResizeHandle as ResizeHandle } from "@/patterns/ApplicationFrame/SidePanel/SidePanelResizeHandle"
