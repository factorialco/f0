/**
 * Moved to `@/patterns/ApplicationFrame/SidePanel` once the panel stopped
 * belonging to the AI chat — hosting arbitrary content was never a chat
 * concern. Re-exported under its old name so this path keeps working.
 */
export { SidePanelContentWindow as HostedPanelWindow } from "@/patterns/ApplicationFrame/SidePanel/SidePanelContentWindow"
