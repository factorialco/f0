import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks"
import * as BarStories from "./Bar.stories"
import * as LineStories from "./Line.stories"
import * as FunnelStories from "./Funnel.stories"
import * as PieStories from "./Pie.stories"
import * as RadarStories from "./Radar.stories"
import * as GaugeStories from "./Gauge.stories"
import * as HeatmapStories from "./Heatmap.stories"
import * as ScatterStories from "./Scatter.stories"
import * as SkeletonsStories from "./Skeletons.stories"
import * as EmptyStatesStories from "./EmptyStates.stories"
import * as PointClickStories from "./PointClick.stories"

<Meta title="F0DataChart/Overview" />

# F0DataChart

Unified chart component. Uses a discriminated `type` prop to select the chart variant.

```tsx
import { F0DataChart } from "@factorialco/f0-react"
```

## Quick Reference

| Type      | Best for                             | Key props                                                  |
| --------- | ------------------------------------ | ---------------------------------------------------------- |
| `bar`     | Comparing categories or time periods | `series`, `categories`, `orientation`, `stacked`           |
| `line`    | Trends over time                     | `series`, `categories`, `lineType`, `showArea`, `showDots` |
| `funnel`  | Conversion pipelines, hiring stages  | `series`, `sort`, `orient`, `showConversion`, `colorScale` |
| `pie`     | Part-of-whole distributions          | `series`, `innerRadius`, `showPercentage`                  |
| `radar`   | Multi-dimensional comparisons        | `series`, `indicators`, `showArea`                         |
| `gauge`   | Single KPI / progress indicator      | `value`, `min`, `max`, `name`                              |
| `heatmap` | Density across two dimensions        | `data`, `xCategories`, `yCategories`, `showLabels`         |
| `scatter` | Relationship between two measures    | `series`, `pointSize`, `scaleAxes`, `xAxisName`            |

All variants share an optional `emptyState` prop — see [Empty states](#empty-states).

---

## Empty states

When data is empty (empty `series`, empty `data`, or a missing gauge `value`), `F0DataChart` auto-renders a centered message so you never see a bare axis. No prop is required — it is the default behavior. All-zero datasets are **not** empty — `[0, 0, 0]` is a legitimate zero-valued chart and renders as usual.

Default copy is **"No data available"** / **"Try a different date or fewer filters"**.

<Canvas of={EmptyStatesStories.EmptyBar} />

Use the `emptyState` prop to customize copy, replace the UI entirely, or skip detection:

```ts
type F0DataChartEmptyStateProps = {
  title?: string
  description?: string
  /** Render-prop escape hatch — replaces the entire UI. */
  render?: () => ReactNode
  /** Skip empty detection and render the chart as usual. */
  disabled?: boolean
}
```

### Custom copy

<Canvas of={EmptyStatesStories.CustomCopy} />

### Render-prop escape hatch

<Canvas of={EmptyStatesStories.RenderPropEscapeHatch} />

### Disabling detection

For charts where zero values are legitimate (e.g. a "0 errors per day" timeline), set `emptyState.disabled` to bypass detection.

<Canvas of={EmptyStatesStories.Disabled} />

---

## Bar Chart

Compare values across categories. Supports vertical/horizontal orientation, stacking, targets, and per-bar colors. Bars are responsive: at narrow widths the legend and axes are progressively hidden so the chart never feels crowded.

<Canvas of={BarStories.SingleSeries} />
<Controls of={BarStories.SingleSeries} />

### Multiple Series

<Canvas of={BarStories.MultipleSeries} />

### With Targets

Renders a gradient fade from the bar top up to the target value. Hovering that gradient shows the bar's own tooltip, so a period with nothing attained yet — where the gradient is the whole column — still reports its target.

<Canvas of={BarStories.WithTargets} />

### Overachievement

`highlightOverachievement` draws the stretch of a bar that passed its target in a darker shade of its own color, split at the target — otherwise a bar that beat its target looks like one that landed exactly on it. `showTargetProgress` adds `value / target` to the tooltip, under the target row.

<Canvas of={BarStories.WithOverachievement} />

### Horizontal

<Canvas of={BarStories.Horizontal} />

### Stacked

<Canvas of={BarStories.Stacked} />

### Per-Bar Colors

Override individual bar colors using F0 design tokens.

<Canvas of={BarStories.PerBarColors} />

### Labels, grid density, and overflow

Bar labels use `valueFormatter`, with configurable size and collision handling:

- `labelFontSize` controls label text size.
- `hideOverflowingLabels` hides labels that do not fit instead of allowing overlap.
- `hideAllLabelsOnOverflow` keeps the chart from showing a ragged partial label set.
- `labelFitPadding` controls the clearance used by the fit check.
- `valueAxisSplitNumber` suggests how many value-axis segments and grid lines to draw.

<Canvas of={BarStories.Snapshot} />
<Canvas of={BarStories.OverflowingLabels} />

### Precise tooltip values

Tooltips use `valueFormatter` like the rest of the chart. Add `tooltipValueFormatter` when the axis and labels must stay compact but the tooltip should carry the exact figure. The Customization section below covers how the two divide the work.

<Canvas of={BarStories.PreciseTooltip} />

### Responsive Snapshot

The same chart rendered in every breakpoint (small / medium / large) and every series count (low / normal / large) — mirrors the AI Analytics Figma matrix.

<Canvas of={BarStories.ResponsiveSnapshotMatrix} />
<Canvas of={BarStories.ResponsiveSnapshotMatrixHorizontal} />

---

## Line Chart

Visualize trends over time. Supports linear, smooth, and step interpolation, area fill, dashed lines, and data point dots. Single-series charts get a soft area gradient under the line; multi-series charts always render lines only because overlapping fills become unreadable. The chart is responsive: at narrow widths the legend and axes are progressively hidden.

<Canvas of={LineStories.SingleLineWithArea} />
<Controls of={LineStories.SingleLineWithArea} />

### Multiple Lines

<Canvas of={LineStories.MultipleLines} />

### Line Styles

`lineType` controls how segments are drawn — `linear`, `smooth` (bezier curves), or `step` (staircase). The story below renders all three side by side so you can compare them.

<Canvas of={LineStories.LineStyles} />

### Actual vs Target (dashed series)

Use `dashed: true` on a series to render a target / projection line.

<Canvas of={LineStories.ActualVsTarget} />

### With Dots

<Canvas of={LineStories.WithDots} />

### Responsive Snapshot

The same chart rendered in every breakpoint (small / medium / large) and every series count (low / normal / large) — mirrors the AI Analytics Figma matrix.

<Canvas of={LineStories.ResponsiveSnapshotMatrix} />

---

## Funnel Chart

Visualize conversion pipelines (e.g. hiring, sales). Supports horizontal/vertical orientation, color scale gradients, and conversion percentages.

<Canvas of={FunnelStories.Default} />
<Controls of={FunnelStories.Default} />

### With Conversion

<Canvas of={FunnelStories.WithConversion} />

### Custom Colors

<Canvas of={FunnelStories.CustomColors} />

### Ascending Sort

<Canvas of={FunnelStories.Ascending} />

### Vertical

<Canvas of={FunnelStories.Vertical} />

---

## Pie Chart

Show part-of-whole distributions. Set `innerRadius > 0` for a donut variant.

<Canvas of={PieStories.Default} />
<Controls of={PieStories.Default} />

### Donut

<Canvas of={PieStories.Donut} />

### Custom Colors

<Canvas of={PieStories.CustomColors} />

### With Percentage

<Canvas of={PieStories.WithPercentage} />

### Responsive Snapshot

<Canvas of={PieStories.ResponsiveSnapshotMatrix} />

---

## Radar Chart

Compare multiple dimensions at once. Useful for performance reviews, team comparisons, or skill assessments.

<Canvas of={RadarStories.Default} />
<Controls of={RadarStories.Default} />

### Multiple Series

<Canvas of={RadarStories.MultipleSeries} />

### No Area Fill

<Canvas of={RadarStories.NoArea} />

### Custom Colors

<Canvas of={RadarStories.CustomColors} />

### Responsive Snapshot

<Canvas of={RadarStories.ResponsiveSnapshotMatrix} />

---

## Gauge Chart

Single-value KPI indicator. Shows progress toward a goal within a configurable range.

<Canvas of={GaugeStories.Default} />
<Controls of={GaugeStories.Default} />

### Custom Range

<Canvas of={GaugeStories.CustomRange} />

### Custom Color

<Canvas of={GaugeStories.CustomColor} />

### With Formatter

<Canvas of={GaugeStories.WithFormatter} />

### Responsive Snapshot

<Canvas of={GaugeStories.ResponsiveSnapshotMatrix} />

---

## Heatmap Chart

Visualize density or intensity across two categorical dimensions. Cells use a single-hue sequential color scale (light → saturated) and reveal their exact value only on hover via tooltip — matching the GitHub commits style. At the narrowest breakpoint (`< 220px`) the heatmap is intentionally not rendered ("Not supported") to match the AI Analytics Figma.

<Canvas of={HeatmapStories.Low} />
<Controls of={HeatmapStories.Low} />

### Medium density

Rounded squares with visible gaps between cells.

<Canvas of={HeatmapStories.Medium} />

### Large density

At high density rounded squares collapse into perfect dots — the same `borderRadius` math used at low density continues to work.

<Canvas of={HeatmapStories.Large} />

### With Formatter

<Canvas of={HeatmapStories.WithFormatter} />

### Responsive Snapshot

The same chart rendered in every breakpoint (small / medium / large) and density (low / normal / large) — mirrors the Figma matrix at file `1smmEIugmR0CNeu7NvK33y` node `10218-20575`.

<Canvas of={HeatmapStories.ResponsiveSnapshotMatrix} />

---

## Scatter Chart

Show the relationship between two measures — one on each axis, one point per entity. Unlike every other cartesian variant there is no category axis: both axes are continuous, so `categories` does not apply. Each point may carry a `label` identifying it, which becomes the tooltip header.

Reach for scatter when the question is "does X relate to Y?". For a trend over time use `line`; for a comparison across categories use `bar`.

Because a point has two coordinates and no third measure, its tooltip has no headline value — both coordinates read as rows, the same shape radar uses for its indicators. Set `xAxisName` and `yAxisName` to label those rows ("salary", "tenure"); without them the rows show bare values.

<Canvas of={ScatterStories.Default} />
<Controls of={ScatterStories.Default} />

### Color split

Pass more than one series to split the points by a group dimension. The legend appears only when there is a split to explain — a single-series scatter would otherwise show one chip restating the title.

<Canvas of={ScatterStories.ColorSplit} />

### Axis scaling

Both axes fit their data range by default. Set `scaleAxes: false` to anchor them at the origin — meaningful when distance from zero matters, wasteful when the values sit far from it.

<Canvas of={ScatterStories.AnchoredAtOrigin} />

### Point size

<Canvas of={ScatterStories.CustomPointSize} />

### Density

Points render at 70% opacity so overlapping regions read as density rather than a solid block.

<Canvas of={ScatterStories.Dense} />

### Tuple points

Points can be bare `[x, y]` tuples when they need no identity, instead of `{ x, y, label }` objects.

<Canvas of={ScatterStories.TuplePoints} />

### Responsive Snapshot

Both axes carry numeric ticks that are equally load-bearing, so unlike bar and line — which drop their category axis first — scatter shows or hides both axes together.

<Canvas of={ScatterStories.ResponsiveSnapshotMatrix} />

> **Note:** scatter cannot be converted to or from other chart types in the dashboard chart-type switcher. A measure-against-measure payload has no meaningful bar or line equivalent.

---

## Skeletons

Loading placeholders that match each chart type's visual structure. Import them directly:

```tsx
import {
  BarChartSkeleton,
  LineChartSkeleton,
  FunnelChartSkeleton,
} from "@factorialco/f0-react"
```

<Canvas of={SkeletonsStories.Bar} />
<Canvas of={SkeletonsStories.Line} />
<Canvas of={SkeletonsStories.Funnel} />

---

## Customization

### Point click

Pass `onPointClick` when a host needs to respond to the chart mark a reader
selects. Without the callback, chart clicks remain inert.

```tsx
<F0DataChart
  type="bar"
  categories={["Barcelona", "Madrid"]}
  series={[{ name: "Headcount", data: [39, 43] }]}
  onPointClick={(point) => {
    console.log(point.category, point.seriesName, point.value)
  }}
/>
```

The callback includes the category, series, raw numeric value, configured
indices, `source: "pointer"`, and viewport coordinates for anchoring a host-owned action. `values`
keeps the complete numeric tuple for chart types such as scatter and heatmap;
`series` keeps every resolved series when a line click represents a whole
category. `category` is the clicked mark's ECharts name—an axis category, pie
slice, funnel stage, scatter label, or gauge/radar item name—and can be empty.
`seriesName` is the configured series name and can also be empty when ECharts
does not report one.

This callback is intentionally pointer-only. ECharts renders marks into one
canvas rather than focusable DOM nodes. Hosts using this callback must provide
an equivalent keyboard-accessible data or action surface outside the canvas. A
future keyboard path can add another `source` value without breaking pointer
consumers.

**Mark-sized targets**

Bar segments, slices, cells, and points respond only when the mark itself is
clicked. Use this story to compare the visible value with the callback payload.

<Canvas of={PointClickStories.OnAMark} />

**Line plot targets**

Lines are too thin to target reliably, so the complete plot column is
clickable. The nearest series becomes the headline value and `series` contains
all visible values at the resolved category, matching the axis tooltip. When a
handler is present, the pointer cursor identifies the clickable plot; axis
labels retain the default cursor because they are not point targets.

<Canvas of={PointClickStories.AnywhereInAPlotArea} />

**Multi-measure points**

For scatter charts, read `values` rather than only `value`: the tuple preserves
both measures that define the point.

<Canvas of={PointClickStories.BothMeasuresOfAScatterPoint} />

- WHEN `onPointClick` is omitted → THEN clicking the chart produces no callback or visual change.
- WHEN a bar, pie, funnel, gauge, heatmap, radar, or scatter mark is clicked → THEN the callback reports that ECharts series item.
- WHEN a line plot column is clicked → THEN the callback reports the nearest visible series as the headline and every visible series at that category in `series`.
- WHEN any value in a point tuple is `null`, `undefined`, empty, or non-numeric → THEN the click is ignored; a genuine zero is still reported.
- WHEN a successful pick is reported → THEN the chart dismisses its hover tooltip so the host can present one action at the same anchor.

### Legend selection changes

Legend-bearing charts (`bar`, `line`, `funnel`, `pie`, `radar`, and `scatter`)
accept `onLegendSelectionChange`. After a reader toggles a legend item, the
callback reports the final `{ [legendName]: visible }` mapping, including the
chart's isolate and restore behavior. Companion data or action surfaces can use
that mapping to stay aligned with what remains visible on the canvas. Gauge has
no legend, and heatmap uses a visual scale rather than an interactive legend,
so neither variant accepts this prop.

<Canvas of={PointClickStories.LegendSelectionChanges} />

### Value & Category Formatters

Every chart type takes `valueFormatter` for its numbers — axis ticks, labels, and the hover tooltip. One formatter is all most charts need: the tooltip reads the value the way the rest of the chart does, so a unit or a currency can't go missing on hover. Bar and line charts also support `categoryFormatter` for axis labels.

`tooltipValueFormatter` overrides it for the tooltip alone. Reach for it when the axis has to stay compact but the tooltip should be exact:

```tsx
<F0DataChart
  type="line"
  valueFormatter={(v) => `€${(v / 1_000_000).toFixed(1)}M`} // axis: "€4.0M"
  tooltipValueFormatter={(v) => `€${v.toLocaleString()}`} // tooltip: "€4,000,000"
  categoryFormatter={(v) => v.slice(0, 3)}
  // ...
/>
```

With neither, values render as a plain localized number.

<Canvas of={LineStories.CustomFormatters} />

### ECharts Escape Hatch

Bar and line chart types accept an `echartsOptions` prop for advanced customization. Options are shallow-merged on top of the generated config.

```tsx
<F0DataChart
  type="bar"
  echartsOptions={{
    animation: false,
    grid: { left: 80 },
  }}
  // ...
/>
```

### Color Tokens

Use F0 design tokens for custom colors: `"malibu"`, `"purple"`, `"grass"`, `"orange"`, `"red"`, `"viridian"`, `"barbie"`, `"lilac"`, `"smoke"`, `"army"`, `"flubber"`, `"indigo"`, `"camel"`, `"radical"`, `"yellow"`.
