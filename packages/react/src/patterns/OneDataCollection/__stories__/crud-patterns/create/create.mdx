import { Meta, Unstyled } from "@storybook/addon-docs/blocks"
import { DoDonts } from "@/lib/storybook-utils/do-donts"

import * as Stories from "./create.stories"

<Meta of={Stories} />

# Create pattern

Create is the entry point for adding a new resource from a Data Collection. The
collection toolbar should expose the action, and the chosen container should
match the amount of guidance, structure, and time the user needs to complete
the task.

## When to use

- Use a default dialog when the form is short, focused, and can be completed in
  one pass.
- Use a wizard dialog when creation requires multiple steps, staged decisions,
  or review before submission.
- Use Kanban lane create when the new resource should start in a specific lane
  or workflow state. **Recommended** for scoped Kanban creation.
- Use Editable Table add-row actions for lightweight rows that can be created
  inline. **Recommended** only when the row does not need a full setup form.
- Keep create actions at collection level instead of attaching them to a
  specific row.
- Preserve the collection context so users can return to the list after
  creation.

## Pattern options

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Container</th>
        <th className="text-left">When to use</th>
        <th className="text-left">Tradeoff</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--table-create-dialog">Default dialog</a>
        </td>
        <td>Standard create flows that fit in a compact overlay.</td>
        <td>Less space for complex setup, long content, or review steps.</td>
      </tr>
      <tr>
        <td>
          <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--table-create-right-dialog">Right dialog</a>
        </td>
        <td>
          Table flows where users should keep the collection visible while
          filling the form.
        </td>
        <td>
          Less focused than a centered dialog and narrower for dense forms.
        </td>
      </tr>
      <tr>
        <td>
          <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--create-wizard-dialog">Wizard dialog</a>
        </td>
        <td>
          Guided setup with sequential steps, progressive disclosure, or staged
          validation.
        </td>
        <td>Higher interaction cost for simple tasks.</td>
      </tr>
      <tr>
        <td>
          <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--kanban-create-lane">Kanban lane create</a>
        </td>
        <td>
          <strong>Recommended</strong> when the lane defines the new resource's
          initial state.
        </td>
        <td>Not enough when creation needs a full setup or review flow.</td>
      </tr>
      <tr>
        <td>
          <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--editable-table-create-row">Editable Table add row</a>
        </td>
        <td>
          <strong>Recommended</strong> for lightweight row creation with
          sensible defaults.
        </td>
        <td>Too weak for complex resources or required setup steps.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

## Decision criteria

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Question</th>
        <th className="text-left">Recommended pattern</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Can the user complete the task with a single focused form?</td>
        <td>Default dialog</td>
      </tr>
      <tr>
        <td>Does the user need to keep the table visible while creating?</td>
        <td>Right dialog</td>
      </tr>
      <tr>
        <td>Does the flow require multiple stages or a review step?</td>
        <td>Wizard dialog</td>
      </tr>
      <tr>
        <td>Does the user need help understanding what comes next?</td>
        <td>Wizard dialog</td>
      </tr>
      <tr>
        <td>Is the task frequent and lightweight?</td>
        <td>Default dialog</td>
      </tr>
      <tr>
        <td>Does the new Kanban item belong directly in one lane?</td>
        <td>Kanban lane create</td>
      </tr>
      <tr>
        <td>Can the user create the resource as a simple editable row?</td>
        <td>Editable Table add row</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

## Guidance

Use one clear collection primary action for creation. Label it with the user
goal, not the implementation detail. Keep the dialog title aligned with the
action label so users immediately understand they are in the correct flow.

When a wizard is needed, split the flow into steps that match a real decision
model. Each step should have a distinct purpose. Do not turn a short single-form
create flow into a wizard only to make it feel more structured.

For table-heavy contexts, a right-position dialog can work as an alternative
when people need to preserve list context while creating. Keep this as a table
variant, not the default create pattern.

For Kanban, lane create is **Recommended** when the lane is meaningful input:
the user is not only creating a resource, they are creating it in a known state.
For Editable Table, add-row is **Recommended** only for lightweight records that
can be created with defaults and refined inline.

## Dos and don'ts

<DoDonts
  do={{
    description:
      "Use a default dialog for short create flows that users can finish quickly without leaving the collection.",
  }}
  dont={{
    description:
      "Do not send simple create flows into a multi-step wizard when one form would be clearer.",
  }}
/>

<DoDonts
  do={{
    description:
      "Use a wizard when the task benefits from progressive disclosure, staged validation, or an explicit review step.",
  }}
  dont={{
    description:
      "Do not hide a create action inside row-level interactions when it creates a new resource for the whole collection.",
  }}
/>

## Related stories

- <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--table-create-dialog">Default dialog</a>
- <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--table-create-right-dialog">Right dialog</a>
- <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--create-wizard-dialog">Wizard dialog</a>
- <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--kanban-create-lane">Kanban lane create</a>
- <a target="_parent" href="/?path=/story/patterns-data-collection-crud-patterns-create--editable-table-create-row">Editable Table add row</a>
- <a target="_parent" href="/?path=/docs/patterns-data-collection-crud-patterns-overview--documentation">CRUD patterns overview</a>
- <a target="_parent" href="/?path=/docs/experimental-crud-patterns-create-update--documentation">Agnostic: Create & Update principles</a>
