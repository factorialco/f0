import { Meta, Unstyled } from "@storybook/addon-docs/blocks"
import { DoDonts } from "@/lib/storybook-utils/do-donts"

import * as Stories from "./delete.stories"

<Meta of={Stories} />

# Delete pattern

Delete patterns cover destructive actions in a Data Collection. The confirmation
step must make the scope and consequence explicit before the user commits.

## When to use

- Use a confirmation dialog for single destructive actions.
- Use a scoped confirmation dialog for bulk destructive actions.
- Keep delete available as an explicit item or bulk action in every
  visualization. **Recommended** placement changes by view, but confirmation
  rules do not.
- Name the affected resource or selection in the title whenever possible.
- Explain irreversible consequences in the body when data loss is involved.

## Pattern options

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Pattern</th>
        <th className="text-left">When to use</th>
        <th className="text-left">What the confirmation must clarify</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--table-single-delete">
            Table single delete
          </a>
        </td>
        <td>Deleting one resource from row-level actions.</td>
        <td>The item name and the destructive outcome.</td>
      </tr>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--table-bulk-delete">
            Table bulk delete
          </a>
        </td>
        <td>Deleting a selected set of resources from bulk actions.</td>
        <td>The selected scope, count, and irreversible effect.</td>
      </tr>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--card-delete">
            Card delete
          </a>
        </td>
        <td>Deleting one resource from the Card ellipsis.</td>
        <td>The card resource name and destructive outcome.</td>
      </tr>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--card-delete-from-dialog">
            Card delete from dialog
          </a>
        </td>
        <td>Deleting one resource from the read dialog header ellipsis.</td>
        <td>The dialog resource name and destructive outcome.</td>
      </tr>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--kanban-bulk-delete">
            Kanban bulk delete
          </a>
        </td>
        <td>Deleting selected Kanban cards from bulk actions.</td>
        <td>The selected card count and irreversible effect.</td>
      </tr>
      <tr>
        <td>
          <a href="/?path=/story/patterns-data-collection-crud-patterns-delete--editable-table-delete">
            Editable Table delete
          </a>
        </td>
        <td>Deleting one resource from the row actions dropdown.</td>
        <td>The row resource name and destructive outcome.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

## Decision criteria

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Question</th>
        <th className="text-left">Recommended pattern</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Is the user deleting one specific item?</td>
        <td>Single delete confirmation</td>
      </tr>
      <tr>
        <td>Is the user acting on a selection or bulk state?</td>
        <td>Bulk delete confirmation</td>
      </tr>
      <tr>
        <td>Can the resource be restored later?</td>
        <td>State the recovery path or the irreversible consequence clearly</td>
      </tr>
      <tr>
        <td>Is the action destructive enough to need explicit confirmation?</td>
        <td>Use a confirmation dialog before executing the action</td>
      </tr>
      <tr>
        <td>Are selected Kanban cards being deleted together?</td>
        <td>
          Use Kanban bulk delete confirmation when selected scope is visible
        </td>
      </tr>
      <tr>
        <td>Is the current visualization Card?</td>
        <td>
          Use the card ellipsis, or the read dialog destructive action if no
          ellipsis exists
        </td>
      </tr>
      <tr>
        <td>Is the current visualization Editable Table?</td>
        <td>Use the row actions dropdown with confirmation</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

## Guidance

Destructive actions should never depend only on iconography or placement. The
user should understand what will be removed and whether the change can be
reverted. This is especially important in bulk flows, where the risk of deleting
the wrong scope is higher.

Keep the confirmation concise. The goal is not to repeat the entire item
details, but to state enough context to prevent accidental deletion.

Table, List, Card, Kanban, and Editable Table all follow the same delete rule:
the user must confirm the affected item or selected scope before the destructive
change runs. In Card views, use the card ellipsis because card click is a read
affordance. If a product implementation does not expose an ellipsis, put delete
inside the read dialog as a destructive outline action instead. In Kanban views,
use selected-card bulk delete because Kanban supports selection but does not
currently expose per-card item actions. Moving cards should only update state,
never destroy data. In Editable Table, use the row actions dropdown and keep
delete separate from editable cell interactions.

Using the read dialog header ellipsis is the fallback Card pattern when the card
surface itself does not expose the action menu. This keeps delete inside the
`F0Dialog` API and avoids inventing a custom destructive footer action.

## Dos and don'ts

<DoDonts
  do={{
    description:
      "Name the resource or affected count in the confirmation title so the user can verify scope before deleting.",
  }}
  dont={{
    description:
      "Do not use vague confirmation copy such as 'Are you sure?' without naming what will be deleted.",
  }}
/>

<DoDonts
  do={{
    description:
      "Explain when the action cannot be undone, especially for bulk destructive flows.",
  }}
  dont={{
    description:
      "Do not trigger destructive bulk actions immediately from selection controls without an explicit confirmation step.",
  }}
/>

## Related stories

- [Table single delete](/?path=/story/patterns-data-collection-crud-patterns-delete--table-single-delete)
- [Table bulk delete](/?path=/story/patterns-data-collection-crud-patterns-delete--table-bulk-delete)
- [Card delete](/?path=/story/patterns-data-collection-crud-patterns-delete--card-delete)
- [Card delete from dialog](/?path=/story/patterns-data-collection-crud-patterns-delete--card-delete-from-dialog)
- [Editable Table delete](/?path=/story/patterns-data-collection-crud-patterns-delete--editable-table-delete)
- [Kanban bulk delete](/?path=/story/patterns-data-collection-crud-patterns-delete--kanban-bulk-delete)
- [CRUD patterns overview](/?path=/docs/patterns-data-collection-crud-patterns-overview--documentation)
