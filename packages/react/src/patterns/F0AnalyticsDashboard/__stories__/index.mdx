import { Canvas, Meta, Controls, Unstyled } from "@storybook/addon-docs/blocks"
import * as Stories from "./index.stories"
import * as AskOneStories from "./AskOne.stories"
import * as WidgetDropStories from "../../../kits/ai/F0AiChat/__stories__/F0AiChatWidgetDrop.stories"
import { DoDonts } from "@/lib/storybook-utils/do-donts"

<Meta of={Stories} />

# Analytics dashboard

F0AnalyticsDashboard presents related metrics, charts, and tables as one report with shared filters, optional export, and an editable grid.

## Anatomy

<Canvas of={Stories.MixedDashboard} />

<Controls of={Stories.MixedDashboard} />

## Guidelines

### Design best practices

**When to use**

<Unstyled>
  <table className="dark:text-f1-foreground-inverse/80 mb-8 w-full">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use F0AnalyticsDashboard when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>One report combines related visualizations</td>
        <td>
          Metrics, charts, and tables must share context and filter values.
        </td>
      </tr>
      <tr>
        <td>A report supports reusable saved views</td>
        <td>
          The parent can own filter and layout state through the controlled
          callbacks.
        </td>
      </tr>
      <tr>
        <td>A report needs a combined export</td>
        <td>Multiple dashboard items belong in one workbook.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**When not to use**

<Unstyled>
  <table className="dark:text-f1-foreground-inverse/80 mb-8 w-full">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use instead</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>A page contains one searchable table</td>
        <td>
          <code>OneDataCollection</code> with its collection controls.
        </td>
      </tr>
      <tr>
        <td>A control affects only one visualization</td>
        <td>An item-specific control owned by that visualization.</td>
      </tr>
      <tr>
        <td>Information is a transient summary</td>
        <td>A compact KPI or summary surface without dashboard editing.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**Do's and don'ts**

<DoDonts
  do={{
    description: "Keep report filters global and predictable.",
    guidelines: [
      "Use one applied filter state for every participating item",
      "Keep filter labels consistent with the report's data vocabulary",
      "Persist only committed values",
    ],
  }}
  dont={{
    description: "Avoid mixing report and item scope.",
    guidelines: [
      "Don't present a dashboard filter when it changes only one item",
      "Don't persist temporary picker selections before Apply",
      "Don't silently retain values for removed filter definitions",
    ],
  }}
/>

### Widget information hints

Use `info` for the definition of the figure itself. A string reveals a short
tooltip; structured content reveals a titled card with an optional link to the
definition's source. Keep this separate from `description`, which states what
the widget shows, and `explanation`, which documents how the result was
calculated.

In the story below, hover or focus the information icons beside **Headcount by
Department** and **Avg. Salary**. The other widgets intentionally omit `info`,
so the new affordance can be compared with the unchanged header.

<Canvas of={Stories.MixedDashboard} />

- WHEN an item provides `info` → THEN an information trigger appears beside its title on chart, metric, and collection widgets.
- WHEN `info` is omitted → THEN the header layout and interaction remain unchanged.
- WHEN the widget is in an error state → THEN the information trigger remains available to explain what the failed widget was meant to measure.
- WHEN the title is long → THEN it truncates around the fixed-size trigger instead of pushing the trigger out of the header.

### Ask One from a widget

When an enabled AI chat is mounted, each titled widget adds **Ask One** to its
actions menu. Choosing it opens the chat and places the widget title in the
composer's quote chip, ready for a question.

**Menu action**

The story leaves the widget menu open so the action, icon, and placement can be
reviewed directly.

<Canvas of={AskOneStories.WidgetMenuAction} />

**Quoted in the chat**

This story completes the action and leaves the resulting quote visible in the
real chat composer.

<Canvas of={AskOneStories.WidgetQuotedInChat} />

**Drag shortcut in edit mode**

On a multi-item editable dashboard, dragging a titled widget into the visible
chat is a pointer shortcut for the same action. The automated story finishes
with the quote visible and the dashboard order unchanged.

<Canvas of={WidgetDropStories.DragWidgetToQuote} />

Pass `onAskAi` when the host needs to own the action instead of sending it to
the mounted chat:

```tsx
<F0AnalyticsDashboard
  items={items}
  onAskAi={({ id, title }) => {
    openProductChat({ widgetId: id, quote: title })
  }}
/>
```

Pass `onAskAiTarget` when the mounted chat should keep the built-in quote,
open, focus, and fullscreen behavior but the host also needs to attach hidden
analytical context. The observer receives the widget identity plus the exact
`quote` object that remains pending until the user submits or dismisses it:

```tsx
<F0AnalyticsDashboard
  items={items}
  onAskAiTarget={({ id, point, selection, quote }) => {
    stageAnalyticalContext({ widgetId: id, point, selection, quote })
  }}
/>
```

- WHEN an enabled AI chat is mounted and a widget has a title → THEN its actions menu includes **Ask One**.
- WHEN a widget is in an error state but still has a title → THEN **Ask One** remains available from its error header.
- WHEN no chat is available and `onAskAi` is omitted → THEN the action is hidden instead of offering a dead command.
- WHEN **Ask One** uses the built-in behavior → THEN the dashboard exits widget fullscreen, quotes the title, and opens the chat.
- WHEN `onAskAiTarget` observes the built-in behavior → THEN it receives the widget `id`, `title`, and exact pending `quote` before F0 stages that quote, without replacing any chat behavior.
- WHEN `onAskAi` is provided → THEN the action calls the host with the widget `id` and `title` without changing chat or fullscreen state.
- WHEN `onAskAiTarget` observes the built-in behavior → THEN it receives the widget `id`, `title`, and exact pending `quote` before F0 stages that quote, without replacing any chat behavior.
- WHEN a directly rendered widget has `onAskAi` but no `itemId` → THEN the action is hidden rather than sending an empty ID.
- WHEN a widget is dropped on chat → THEN it follows the same built-in,
  observed, or host-owned behavior as the menu action.
- WHEN the reorder grip is only clicked → THEN no drag invitation appears and
  no widget action runs.
- WHEN the pointer drag is cancelled or ends outside chat → THEN no quote or
  host action is created.
- WHEN a widget has no title → THEN the action is hidden because there is nothing to quote.

### Ask One from a chart point

Charts with an available chat or `onAskAi` handler let the reader click a mark
before choosing **Ask One**. The intermediate action is intentional: ordinary
chart exploration must not open the chat on every click.

**Interactive point flow**

In this story, click the Engineering bar to reveal the anchored action. Choose
**Ask One** to review the category, series, and formatted value in the focused
chat composer.

<Canvas of={AskOneStories.ChartPointFlow} />

**Observed point target**

This story keeps the built-in formatted quote while exposing the raw point to
the non-overriding observer.

<Canvas of={AskOneStories.PointTargetObserver} />

The host-owned callback receives the raw point as `point`, in addition to the
widget `id` and `title`. `point.values` preserves every measure carried by one
mark, while `point.series` preserves every visible series resolved by a line
chart click. `point.source` distinguishes `"pointer"` from `"keyboard"`;
keyboard-resolved points use `0` for both viewport coordinates because there is
no pointer position to anchor to.

```tsx
<F0AnalyticsDashboard
  items={items}
  onAskAi={({ id, title, point }) => {
    openProductChat({ widgetId: id, quote: title, point })
  }}
/>
```

- WHEN a pointer user clicks a chart mark → THEN **Ask One** appears beside that mark without opening the chat yet.
- WHEN a keyboard user tabs to the chart's point action → THEN a visible **Ask One** trigger opens a menu containing every currently visible point, with the same formatted context as pointer selection.
- WHEN a chart contains more than 100 points → THEN the keyboard menu pages them with **Previous** and **Next** actions instead of mounting the entire dataset at once.
- WHEN the built-in action is chosen → THEN the quote keeps the chart title, visible point context, and tooltip-formatted values, opens the chat, and focuses its composer.
- WHEN `onAskAiTarget` observes the built-in action → THEN it receives that exact pending quote together with the raw point.
- WHEN a scatter point is quoted → THEN both the X and Y measures are included with their axis names and formatters.
- WHEN a line category is quoted → THEN every visible series at that category is included, matching the axis tooltip.
- WHEN `onAskAi` owns the action → THEN it receives the raw point and the built-in chat state remains unchanged.
- WHEN `onAskAiTarget` observes the built-in action → THEN it receives that exact pending quote together with the raw point.
- WHEN the chart has no title → THEN pointer and keyboard point actions are hidden because there is no widget context to quote.
- WHEN the chart moves because of scrolling or resizing, or the reader presses <kbd>Escape</kbd> or clicks elsewhere → THEN the anchored action closes.
- WHEN a screen reader reaches the point action → THEN its trigger is named **Ask One: _chart title_** and each menu item exposes the complete formatted point context.

### Ask One from a chart area

The dashboard adds one **Draw to ask One** action when an AI chat or host
`onAskAi` handler is available and at least one bar, line, heatmap, or scatter
widget can be selected. Activating it enables drawing on every compatible chart
and places a blocking explanation over incompatible widgets. A completed area
attaches the selected values to the composer without sending a message, so the
reader can add a question such as “What happened here?” first.

<Canvas of={AskOneStories.ChartAreaSelectionMode} />

The companion icon in each compatible chart opens the same bounded selection
contract without a drag. This reference story also demonstrates the
non-overriding `onAskAiTarget` observer: the host receives the raw control
selection while F0 still stages and retains its built-in quote.

<Canvas of={AskOneStories.ChartAreaSelectionWithoutDrag} />

The host-owned callback receives the bounded raw selection as `selection`.
`selection.points` contains at most 100 real chart data points and
`selection.totalPointCount` carries the uncapped count. `point` and `selection`
are mutually exclusive. Polygon selections have `selection.source: "pointer"`;
the no-drag companion emits the same contract with
`selection.source: "control"`.

```tsx
<F0AnalyticsDashboard
  items={items}
  onAskAi={({ id, title, selection }) => {
    openProductChat({ widgetId: id, quote: title, selection })
  }}
/>
```

- WHEN selection mode starts → THEN the dashboard action changes to **Cancel selection** and one persistent live region explains the drawing and cancellation path.
- WHEN selection mode starts → THEN incompatible widgets receive a blocking overlay and compatible charts remain available for drawing.
- WHEN the completed polygon contains data → THEN the built-in behavior exits fullscreen, attaches a bounded formatted quote, opens chat, focuses the composer without sending, and keeps the completed area visible.
- WHEN `onAskAiTarget` observes the built-in area action → THEN it receives that exact pending quote together with the bounded raw selection.
- WHEN the quote is sent, removed, replaced, or the reader chooses **Clear selection** → THEN the completed area is cleared.
- WHEN `onAskAi` owns the action → THEN it receives the widget identity and raw selection without changing built-in chat state.
- WHEN the completed polygon contains no data → THEN selection remains active and the status asks the reader to draw around at least one point.
- WHEN the reader presses <kbd>Escape</kbd> or chooses **Cancel selection** → THEN selection mode closes without attaching anything.
- WHEN the dashboard has no drawable bar, line, heatmap, or scatter chart → THEN the dashboard area-selection action is hidden. This includes compatible chart types with empty or failed data, a blank title, table view, or a heatmap whose plot is too narrow to draw reliably.

### Report filters

Report filters are one applied state shared by every participating item. Pass `filtersValue` with `onFiltersChange` to own that state — for example to hydrate a saved report and persist each committed change — or pass only `defaultFilters` to let the dashboard own it.

```tsx
const [filtersValue, setFiltersValue] = useState<FiltersState<typeof filters>>(
  savedReport.filters
)

<F0AnalyticsDashboard
  filters={filters}
  filtersValue={filtersValue}
  onFiltersChange={setFiltersValue}
  items={items}
/>
```

- WHEN `filtersValue` is provided → THEN the dashboard is controlled and waits for the parent to pass each committed value back.
- WHEN only `defaultFilters` is provided → THEN the dashboard owns subsequent committed filter state.
- WHEN Apply, Clear, chip removal, or a preset commits a change → THEN `onFiltersChange` receives the next applied state and participating items refetch with it.
- WHEN the picker closes without applying → THEN temporary selections are discarded.
- WHEN an item sets `useDashboardFilters=false` → THEN that item fetches with an empty filter object.
- WHEN `items` is empty → THEN the filter bar remains usable without a grid item fetch.

### Usage examples

Every example keeps the applied value visible above the report, so committed changes can be reviewed without inspecting component internals.

**Empty controlled state**

Start from an empty saved value. There are no applied chips or filter counter until a reviewer commits the first report filter.

<Canvas of={Stories.ReportFiltersEmpty} />

**Pre-applied and commit lifecycle**

Start from a saved Department filter. Draft changes roll back on dismiss; Apply, Clear, chip removal, and presets update the visible parent state.

<Canvas of={Stories.ReportFiltersPreApplied} />

**Scalar values**

Review search text, one selected option, a single date, and a single number.

<Canvas of={Stories.ReportFilterScalarValues} />

**Collection and range values**

Review multiple selected options, a date range, and a numeric range. Boolean and valueless filters are not native `OneFilterPicker` filter types, and are intentionally not represented as supported dashboard controls.

<Canvas of={Stories.ReportFilterRangeAndMultipleValues} />

**With dashboard export**

The export control uses the applied report filter value for participating
items. Items with `useDashboardFilters=false` are exported with an empty filter
object, matching their on-screen fetch behavior.

<Canvas of={Stories.WithExport} />

**With no report data**

The filter bar remains usable when valid report filters return empty chart data.

<Canvas of={Stories.EmptyDashboard} />

## Accessibility

### Keyboard interaction

<Unstyled>
  <table className="dark:text-f1-foreground-inverse/80 mb-8 w-full">
    <thead>
      <tr>
        <th className="text-left">Key</th>
        <th className="text-left">Action</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <kbd>Tab</kbd>
        </td>
        <td>
          Moves through the filter trigger, presets, applied chips, export, and
          item actions.
        </td>
      </tr>
      <tr>
        <td>
          <kbd>Enter</kbd> / <kbd>Space</kbd>
        </td>
        <td>Opens controls and activates focused filter options or actions.</td>
      </tr>
      <tr>
        <td>
          <kbd>Escape</kbd>
        </td>
        <td>
          Closes the filter dialog, an anchored chart-point action, or active
          chart-area selection.
        </td>
      </tr>
      <tr>
        <td>
          <kbd>Tab</kbd>, then <kbd>Enter</kbd> / <kbd>Space</kbd>
        </td>
        <td>
          Reaches and reveals the chart's <strong>Ask One</strong> trigger, then
          opens its keyboard-accessible data-point menu.
        </td>
      </tr>
      <tr>
        <td>
          <kbd>ArrowUp</kbd> / <kbd>ArrowDown</kbd>
        </td>
        <td>Moves through data points in the open chart action menu.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### Screen reader behavior

- The filter trigger keeps the stable name "Filters"; applied filter labels are exposed as its accessible description, while a visible counter shows how many report filters are active.
- The filter popover has a dialog name, and filter options expose native checkbox semantics.
- Applied filter removal buttons keep the stable name "Close"; the filter label and selected value are exposed as their accessible descriptions.
- The dashboard area-selection action changes its name from "Draw to ask One" to "Cancel selection" while drawing. After completion it returns to "Draw to ask One", and the selected chart exposes an icon-only "Clear selection" button immediately outside the retained polygon. The button flips to another edge when needed so it remains inside the chart surface.
- A persistent polite status region announces drawing instructions and empty selections. While drawing, incompatible widgets are inert and hidden from the accessibility tree; the dashboard instruction explains that those widgets are unavailable.
- Each compatible chart exposes a compact icon-only "Select chart values without drawing" menu while drawing is active. Its accessible name also includes the widget title so assistive-technology users can distinguish charts. Keyboard, touch, and single-pointer users can select the same bounded data-point contract without a drag gesture; the normal idle dashboard still has no per-widget drawing controls.
