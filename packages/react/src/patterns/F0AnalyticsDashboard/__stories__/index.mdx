import { Canvas, Meta, Controls, Unstyled } from "@storybook/addon-docs/blocks"
import * as Stories from "./index.stories"
import { DoDonts } from "@/lib/storybook-utils/do-donts"

<Meta of={Stories} />

# Analytics dashboard

F0AnalyticsDashboard presents related metrics, charts, and tables as one report with shared filters, optional export, and an editable grid.

## Anatomy

<Canvas of={Stories.MixedDashboard} />

<Controls of={Stories.MixedDashboard} />

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Prop</th>
        <th className="text-left">Type</th>
        <th className="text-left">Default</th>
        <th className="text-left">Required</th>
        <th className="text-left">Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <code>items</code>
        </td>
        <td>
          <code>DashboardItem&lt;Filters&gt;[]</code>
        </td>
        <td>—</td>
        <td>Yes</td>
        <td>
          Ordered metric, chart, and collection definitions rendered in the
          shared grid.
        </td>
      </tr>
      <tr>
        <td>
          <code>filters</code>
        </td>
        <td>
          <code>Filters</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>
          Dashboard-level filter definitions. Every participating item receives
          the applied values.
        </td>
      </tr>
      <tr>
        <td>
          <code>filtersLoading</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>
          <code>false</code>
        </td>
        <td>—</td>
        <td>
          Shows a filter-bar skeleton while definitions or options are loading.
        </td>
      </tr>
      <tr>
        <td>
          <code>presets</code>
        </td>
        <td>
          <code>PresetsDefinition&lt;Filters&gt;</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>Quick-select report filter configurations.</td>
      </tr>
      <tr>
        <td>
          <code>defaultFilters</code>
        </td>
        <td>
          <code>FiltersState&lt;Filters&gt;</code>
        </td>
        <td>
          <code>empty object</code>
        </td>
        <td>—</td>
        <td>
          Initial applied values for uncontrolled usage. Ignored when{" "}
          <code>filtersValue</code> is provided.
        </td>
      </tr>
      <tr>
        <td>
          <code>filtersValue</code>
        </td>
        <td>
          <code>FiltersState&lt;Filters&gt;</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>Applied values for controlled usage.</td>
      </tr>
      <tr>
        <td>
          <code>onFiltersChange</code>
        </td>
        <td>
          <code>(value: FiltersState&lt;Filters&gt;) =&gt; void</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>
          Receives committed Apply, Clear, chip removal, and preset transitions.
        </td>
      </tr>
      <tr>
        <td>
          <code>navigationFilters</code>
        </td>
        <td>
          <code>NavigationFiltersDefinition</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>
          Navigation-oriented filters, such as date navigation, merged into item
          fetch values.
        </td>
      </tr>
      <tr>
        <td>
          <code>editMode</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>
          <code>false</code>
        </td>
        <td>—</td>
        <td>Enables item rearrangement, resizing, and deletion controls.</td>
      </tr>
      <tr>
        <td>
          <code>onLayoutChange</code>
        </td>
        <td>
          <code>(layout: DashboardItemLayout[]) =&gt; void</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>Receives the ordered item layout after an edit.</td>
      </tr>
      <tr>
        <td>
          <code>enableExport</code>
        </td>
        <td>
          <code>boolean</code>
        </td>
        <td>
          <code>false</code>
        </td>
        <td>—</td>
        <td>Displays the dashboard-level export control.</td>
      </tr>
      <tr>
        <td>
          <code>exportFilename</code>
        </td>
        <td>
          <code>string</code>
        </td>
        <td>
          <code>"dashboard"</code>
        </td>
        <td>—</td>
        <td>Base filename for Excel export.</td>
      </tr>
      <tr>
        <td>
          <code>onExportReady</code>
        </td>
        <td>
          <code>
            (exportFn: (() =&gt; Promise&lt;void&gt;) | undefined) =&gt; void
          </code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>
          Exposes the current dashboard export function to a parent surface.
        </td>
      </tr>
      <tr>
        <td>
          <code>resetKey</code>
        </td>
        <td>
          <code>number</code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>Forces the grid back to its initial layout when incremented.</td>
      </tr>
      <tr>
        <td>
          <code>onTransformChart</code>
        </td>
        <td>
          <code>
            (itemId: string, newType: string, orientation?: "vertical" |
            "horizontal") =&gt; void
          </code>
        </td>
        <td>—</td>
        <td>—</td>
        <td>Receives chart-type changes requested from an item menu.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

## Guidelines

### Design best practices

**When to use**

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use F0AnalyticsDashboard when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>One report combines related visualizations</td>
        <td>
          Metrics, charts, and tables must share context and filter values.
        </td>
      </tr>
      <tr>
        <td>A report supports reusable saved views</td>
        <td>
          The parent can own filter and layout state through the controlled
          callbacks.
        </td>
      </tr>
      <tr>
        <td>A report needs a combined export</td>
        <td>Multiple dashboard items belong in one workbook.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**When not to use**

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Situation</th>
        <th className="text-left">Use instead</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>A page contains one searchable table</td>
        <td>
          <code>OneDataCollection</code> with its collection controls.
        </td>
      </tr>
      <tr>
        <td>A control affects only one visualization</td>
        <td>An item-specific control owned by that visualization.</td>
      </tr>
      <tr>
        <td>Information is a transient summary</td>
        <td>A compact KPI or summary surface without dashboard editing.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

**Do's and don'ts**

<DoDonts
  do={{
    description: "Keep report filters global and predictable.",
    guidelines: [
      "Use one applied filter state for every participating item",
      "Keep filter labels consistent with the report's data vocabulary",
      "Persist only committed values",
    ],
  }}
  dont={{
    description: "Avoid mixing report and item scope.",
    guidelines: [
      "Don't present a dashboard filter when it changes only one item",
      "Don't persist temporary picker selections before Apply",
      "Don't silently retain values for removed filter definitions",
    ],
  }}
/>

### Behavior

- WHEN <code>filtersValue</code> is provided → THEN the dashboard is controlled and waits for the parent to pass each committed value back.
- WHEN only <code>defaultFilters</code> is provided → THEN the dashboard owns subsequent committed filter state.
- WHEN Apply, Clear, chip removal, or a preset commits a change → THEN <code>onFiltersChange</code> receives the next applied state and participating items refetch with it.
- WHEN the picker closes without applying → THEN temporary selections are discarded.
- WHEN an item sets <code>useDashboardFilters=false</code> → THEN that item fetches with an empty filter object.
- WHEN <code>items</code> is empty → THEN the filter bar remains usable without a grid item fetch.

### Usage examples

**Controlled report filters**

Keep saved report state outside the component and feed every committed value back through <code>filtersValue</code>.

<Canvas of={Stories.ControlledFilters} />

**Dashboard export**

Enable export when the report contains multiple related items that belong in one workbook.

<Canvas of={Stories.WithExport} />

**Empty report data**

Keep report controls available when valid filters produce no rows or chart points.

<Canvas of={Stories.EmptyDashboard} />

## Accessibility

### Keyboard interaction

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Key</th>
        <th className="text-left">Action</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <kbd>Tab</kbd>
        </td>
        <td>
          Moves through the filter trigger, presets, applied chips, export, and
          item actions.
        </td>
      </tr>
      <tr>
        <td>
          <kbd>Enter</kbd> / <kbd>Space</kbd>
        </td>
        <td>Opens controls and activates focused filter options or actions.</td>
      </tr>
      <tr>
        <td>
          <kbd>Escape</kbd>
        </td>
        <td>Closes the filter dialog and rolls back temporary selections.</td>
      </tr>
    </tbody>
  </table>
</Unstyled>

### Screen reader behavior

- The filter trigger includes the labels and count of applied report filters.
- The filter popover has a dialog name, and filter options expose native checkbox semantics.
- Applied filter removal buttons include the filter label and selected value in their accessible names.
