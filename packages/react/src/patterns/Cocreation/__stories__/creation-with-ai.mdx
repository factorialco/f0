import { Meta, Unstyled } from "@storybook/addon-docs/blocks"

import chatFullscreen from "./assets/chat-fullscreen.png"
import splitEmpty from "./assets/split-empty.png"

<Meta title="Co-creation/Walkthrough" />

# Walkthrough

This is an end-to-end walkthrough of the
[Co-creation](?path=/docs/patterns-co-creation--documentation) pattern — the abstract flow
grounded in one real resource. The resource here is a survey, but the structure is the
pattern's, not the survey's: the user starts on a collection (Collection), describes what they
want in a chat (Chat), then refines the resulting draft on a canvas while the conversation
continues (Split). Each phase below maps to what the survey example shows; the
mock itself runs full screen, so open it in its own canvas:

<Unstyled>
  <a
    target="_parent"
    href="/?path=/story/patterns-co-creation-walkthrough--welcome-screen"
    className="mb-8 inline-flex items-center gap-2 rounded-lg border border-solid border-f1-border bg-f1-background px-5 py-4 font-medium text-f1-foreground no-underline transition-all hover:shadow dark:border-f1-border-secondary dark:text-f1-foreground-inverse dark:hover:border-f1-border"
  >
    Open the interactive flow ↗
  </a>
</Unstyled>

The mock ships as five stories, all under **Walkthrough**, covering the two ways a flow can open
the conversation and the out-of-credits state of each:

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Story</th>
        <th className="text-left">Entry approach</th>
        <th className="text-left">What it shows</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <a
            target="_parent"
            href="/?path=/story/patterns-co-creation-walkthrough--welcome-screen"
          >
            Welcome Screen
          </a>
        </td>
        <td>Welcome screen</td>
        <td>
          A full-width welcome screen of entry-point cards and starter-prompt
          suggestions. Uses the Engagement / eNPS data.
        </td>
      </tr>
      <tr>
        <td>
          <a
            target="_parent"
            href="/?path=/story/patterns-co-creation-walkthrough--guided-chat-triage"
          >
            Guided Chat · Triage
          </a>
        </td>
        <td>Guided chat</td>
        <td>
          One clarifying question up front that triages the survey type, then a
          type-scoped template gallery. Uses the Training data.
        </td>
      </tr>
      <tr>
        <td>
          <a
            target="_parent"
            href="/?path=/story/patterns-co-creation-walkthrough--guided-chat-entry-actions"
          >
            Guided Chat · Entry Actions
          </a>
        </td>
        <td>Guided chat</td>
        <td>
          One clarifying question up front whose options are entry actions —
          Empty Survey, Use a Template, and the most-used templates.
        </td>
      </tr>
      <tr>
        <td>
          <a
            target="_parent"
            href="/?path=/story/patterns-co-creation-walkthrough--welcome-screen-no-credits"
          >
            Welcome Screen · No Credits
          </a>
        </td>
        <td>Welcome screen</td>
        <td>
          The welcome-screen flow with AI credits exhausted — the soft
          credit-warning banner and the typed-message redirect into guided
          entry.
        </td>
      </tr>
      <tr>
        <td>
          <a
            target="_parent"
            href="/?path=/story/patterns-co-creation-walkthrough--guided-chat-no-credits"
          >
            Guided Chat · No Credits
          </a>
        </td>
        <td>Guided chat</td>
        <td>
          The guided-chat flow with credits exhausted. Guided creation
          (templates, blank survey) needs no credits, so it still works.
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

The phase narrative below follows the **Welcome Screen** story. The
[Guided Chat](#guided-chat) subsection covers how the guided stories differ at the Chat phase;
from Split on, every story behaves the same.

## Flow

### Collection

The flow starts on the collection the user is already working in — a Surveys data collection in
this example. The user opens the AI from the header One switch (side panel) or the **Create**
primary button (full width).

### Chat

<Unstyled>
  <figure className="mx-auto mb-8 mt-0 max-w-2xl">
    <img
      src={chatFullscreen}
      alt="The full-width chat welcome screen: a quick-start suggestion button above the composer, the composer itself, and four starting-point cards below it."
      className="block w-full rounded-lg border border-solid border-f1-border dark:border-f1-border-secondary"
    />
    <figcaption className="mt-2 text-sm text-f1-foreground-secondary">
      The chat opens full width. The user types what they want (or dictates it
      with the mic), taps a quick-start suggestion above the composer, or picks
      a starting-point card below.
    </figcaption>
  </figure>
</Unstyled>

The chat opens with welcome cards offering a few starting points. The cards are
data-driven (`welcomeScreenCards`) and rendered below the composer on the full-width welcome
screen; they're optional, and you can show up to four. Which starting points to offer is up to
each flow — the survey example fills all four:

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Starting point</th>
        <th className="text-left">What it does</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <strong>Empty Survey</strong>
        </td>
        <td>
          Opens a blank survey and walks the user through it with clarifying
          questions, then a draft-confirmation card before any questions are
          written.
        </td>
      </tr>
      <tr>
        <td>
          <strong>Templates</strong>
        </td>
        <td>
          Opens the templates canvas to browse and preview pre-made surveys.
        </td>
      </tr>
      <tr>
        <td>
          <strong>Employee NPS</strong>
        </td>
        <td>
          Opens the eNPS template in a read-only preview; "Use this template"
          then seeds the survey with the eNPS question locked so scores stay
          comparable, the rest editable.
        </td>
      </tr>
      <tr>
        <td>
          <strong>Employee Engagement</strong>
        </td>
        <td>
          Opens the employee-engagement template in a read-only preview to
          review before "Use this template" seeds it for editing.
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

Above the composer sits a separate, optional row of quick-start buttons — the welcome
suggestions (`welcomeScreenSuggestions`). They are **example prompts, not flows**: each button is
a group (here, the "Create a Survey for..." pill) that opens a short list of starter prompts, and
picking one sends its text straight to the AI as if the user had typed it. That is the line
between the two entry points — **a suggestion sends a prompt, a card triggers a flow** (open a
canvas, seed a template). Use suggestions to show the user _what to say_ and to lower the
blank-composer cost. When a flow includes them, populate them with prompts tied to the domain or
resource being created — the survey example offers "Employee satisfaction survey," "Onboarding
feedback survey," and "Remote work pulse check." They render on both the side-panel and
full-width welcome screens, and are independent of the cards: provide either, both, or neither.
See [Building blocks](?path=/docs/patterns-co-creation-building-blocks--documentation) for the
full cards-vs-suggestions contract.

Clarifying questions narrow the intent before anything is drafted — here, the survey's type,
audience, and length. When the answers are in, the AI does **not** write anything yet: it posts a
**draft confirmation** card ("Draft a first set of questions") and waits. The canvas, the resource
card, and the drafted questions all stay hidden until the user decides. **Apply** opens the survey
on the canvas, runs the "applying changes" overlay, and fills in the starter questions — moving the
flow into Split. **Discard** hands over a blank survey instead, still ready to shape by chat. This
mirrors the accept/reject on every later edit; see
[Confirm before drafting, confirm before editing](?path=/docs/patterns-co-creation-standard-flow--documentation).

#### Guided Chat

The Welcome Screen above is one way to open the conversation. The **Guided Chat** stories show the
other: instead of a full-width welcome screen, the flow posts an opening line on the user's behalf
and immediately asks **one** clarifying question. There is no blank canvas of choices — the
conversation is already moving.

The single question takes one of two shapes:

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Shape</th>
        <th className="text-left">The question triages…</th>
        <th className="text-left">What follows</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <strong>Triage</strong> (Guided Chat · Triage)
        </td>
        <td>
          The resource <em>type</em> — here Satisfaction, Effectiveness, or
          Knowledge Test.
        </td>
        <td>
          A template gallery scoped to the chosen type, with "Empty Survey"
          first.
        </td>
      </tr>
      <tr>
        <td>
          <strong>Entry actions</strong> (Guided Chat · Entry Actions)
        </td>
        <td>
          The <em>action</em> — Empty Survey, Use a Template, or one of the
          most-used templates.
        </td>
        <td>
          The blank-survey drafting conversation, the full gallery, or a
          template preview, per the pick.
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

Prefer Guided Chat when the flow has **one clear first action**: a started conversation beats a
blank canvas. See
[When to use Welcome Screen vs Guided Chat](?path=/docs/patterns-co-creation-guidelines--documentation)
for the decision.

Because Guided Chat has no welcome screen to fall back to, closing the gallery or preview **before**
a survey is picked warns the user first ("Leave creation?") — closing there would abandon the
in-progress creation. In the entry-actions shape, picking "Empty Survey" runs the same clarifying →
draft-confirmation chain described above.

### Split

<Unstyled>
  <figure className="mx-auto mb-8 mt-0 max-w-2xl">
    <img
      src={splitEmpty}
      alt="The split view: a survey builder fills the center while the chat docks on the right, showing the resource card and a clarifying question."
      className="block w-full rounded-lg border border-solid border-f1-border dark:border-f1-border-secondary"
    />
    <figcaption className="mt-2 text-sm text-f1-foreground-secondary">
      After choosing Empty survey: the resource canvas fills the center, the
      chat docks on the right with the resource card and clarifying questions.
    </figcaption>
  </figure>
</Unstyled>

The resource canvas fills the center while the chat docks on the right. Every co-created
resource needs a way to read the draft and a way to edit it; here the survey canvas offers those
as two modes:

<Unstyled>
  <table className="mb-8 w-full dark:text-f1-foreground-inverse/80">
    <thead>
      <tr>
        <th className="text-left">Mode</th>
        <th className="text-left">What the user sees</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <strong>Preview</strong>
        </td>
        <td>
          The survey rendered as it will be answered (the answering form), for
          reviewing a template or draft.
        </td>
      </tr>
      <tr>
        <td>
          <strong>Edit</strong>
        </td>
        <td>
          The survey builder, with a Questions tab for the survey content and a
          Settings tab for metadata (title, description, participants, schedule,
          visibility, editors).
        </td>
      </tr>
    </tbody>
  </table>
</Unstyled>

The canvas header carries the resource actions — here **Save** and a primary **Publish**
— so the user can keep the draft or ship it without leaving the flow.

As the conversation continues, the AI proposes changes (for example, adding an open-ended
comment question). Each proposal appears as a human-in-the-loop confirmation card; the user
accepts or rejects it, and an overlay covers the canvas while an accepted change is applied.

### Out of credits

The two **· No Credits** stories show the flow when AI credits are exhausted. A soft
credit-warning banner sits above the composer with a "Get credits" CTA and a dismiss; it only
ever appears on the text input, never over a clarifying panel. Free-text AI needs credits, but
**guided creation does not** — templates and blank surveys still work — so a typed message is
never a dead end: it gets a short "can't do that right now" reply and redirects into the
credit-free guided-entry panel (Empty Survey / Use a Template / most-used templates). Once a survey
exists, typed edit requests get an out-of-credits reply with an upgrade nudge instead of a
proposal.

## Composed building blocks

This flow assembles the blocks documented in
[Building blocks](?path=/docs/patterns-co-creation-building-blocks--documentation):
`F0OneSwitch`, `F0AiChat` (including its data-driven welcome cards and voice dictation),
`F0ClarifyingPanel`, `F0CardHorizontal` (the in-chat resource and proposal cards), and
`F0AiProcessingOverlay`. Only the surfaces specific to this example — the survey's answering
form and builder — come from the
[Surveys](?path=/docs/sds-surveys-surveyformbuilder--documentation) SDS.
