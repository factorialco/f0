/**
 * The box an inline (detail-row) control fills: the form's 40px row height,
 * plus the 1px transparent border and `px-3` that `F0InputField` paints, so a
 * toggle starts at the same x as the text in the rows above and below it.
 */
export const inlineControlBox =
  "flex h-full min-h-[40px] w-full items-center border border-solid border-transparent px-3"
